---
phase: 5.1-game-speed-difficulty
verified: 2026-09-24T20:56:37Z
status: passed
score: 17/17 must-have truths verified (3 verified-as-owner-amended on value wording only)
gaps: []
owner_amendments:
  - "Tier VALUES retuned at the 5.1-06 live feel-check (owner, 2026-09-25, commit 4502143): draft 2.0/3.0/4.5/6.0 -> FINAL relaxed 3.0 / normal 6.0 / fast 7.5 / expert 9.0 A/s; DEFAULTS['speed'] 3.0 -> 6.0 (fresh setups land on 'normal'); game_engine.SPEED_A_PER_S stays 3.0 as v1 baseline + kwarg fallback only (production always injects from setup['speed']). Supersedes the ROADMAP goal clause 'the current baseline (0.3 A/tick = 3.0 A/s) remains the default tier' and the stale '(3.0 A/s)' parenthetical in GAME-11's text. The MECHANISM contract (selector + named tiers + default tier + constant-per-run + persisted + backcompat) is fully satisfied. Per ROADMAP notes: 'final values approved by the owner at the feel-check (values are data-only edits in setup_logic.SPEED_TIERS).'"
---

# Phase 5.1: Game Speed / Difficulty Verification Report

**Phase Goal:** The Setup tab offers a game speed/difficulty selector; the chosen speed is constant within a run (GAME-08 intact) and persists via Save/Load Setup — the current baseline (0.3 Å/tick = 3.0 Å/s) remains the default tier.
**Verified:** 2026-09-24T20:56:37Z
**Status:** PASSED
**Re-verification:** No — initial verification
**Requirement:** GAME-11 (exists at .planning/REQUIREMENTS.md:37; table status "Pending" at line 145 — orchestrator-owned to mark Complete)

## Goal Achievement

All plan-level must-haves verified against the ACTUAL SOURCE (grep patterns from plan frontmatter key_links matched literally; code bodies read, not SUMMARY claims). Gates re-run by the verifier: **678 unittests OK, all 3 gates PASS** (syntax+plugin-path, AST purity, scoped unittest).

**Owner amendment (not a gap):** at the 5.1-06 live feel-check (2026-09-25, commit `4502143`) the owner rejected ALL draft tier values as too slow ("only expert feel like playing") and finalized **relaxed 3.0 / normal 6.0 / fast 7.5 / expert 9.0 A/s**, moving `DEFAULTS['speed']` 3.0 → 6.0 so fresh setups land on 'normal'. This supersedes the goal's "3.0 default" wording; the mechanism contract is intact. Related: the Phase 2 "engine referee" note ("speed constant 3.0") is superseded by tier injection — speed remains constant-within-run, so GAME-08 holds.

### Observable Truths (per-plan must_haves)

| Plan | # | Truth | Verified How | Verdict |
| --- | --- | --- | --- | --- |
| 5.1-01 | 1 | GameEngine with speed_a_per_s=X advances head AND chain by X·dt per tick (2.0→0.2, 6.0→0.6 @ dt=0.1) | game_engine.py:658-659 `self.speed_a_per_s * dt` + `_translate_chain` same-delta (line 665); test_engine_speed.py TestPerTierDisplacement + TestTrainFollowParity (15 tests OK scoped) | ✓ VERIFIED |
| 5.1-01 | 2 | Without the kwarg, behavior identical to before (3.0 A/s → 0.3 A/tick) | Default `speed_a_per_s=SPEED_A_PER_S` (=3.0, line 80) on both `__init__` (line 256) and `reset` (line 271); TestDefaultPreserved pins 0.3 | ✓ VERIFIED |
| 5.1-01 | 3 | Speed fixed at construction/reset; NO mid-run mutation path (GAME-08 by construction) | Repo-wide grep `\.speed_a_per_s\s*=` → exactly ONE hit (game_engine.py:295, storage in reset); docstring "NEVER mutated mid-run" | ✓ VERIFIED |
| 5.1-02 | 1 | Tier names map to speeds via ONE ordered data table | setup_logic.py:114-119 `SPEED_TIERS` tuple-of-tuples (py3.6-safe, insertion-ordered); owner-RETUNED values 3.0/6.0/7.5/9.0 (draft 2.0/3.0/4.5/6.0 superseded — see amendment) | ✓ VERIFIED (as owner-amended: values) |
| 5.1-02 | 2 | Setup dict MISSING 'speed' merges to default tier, not validation failure | merge_defaults (setup_logic.py:145-163) `dict(DEFAULTS)` + update; TestMergeDefaults: `{'schema_version':1}` → speed 6.0 | ✓ VERIFIED (as owner-amended: default value) |
| 5.1-02 | 3 | Any speed > 0 validates and round-trips save/load (custom legal) | validate() line 265-267 accept-as-is (>0 only); TestCustomSpeedAccepted; TestSaveLoadRoundTrip speed=4.5 | ✓ VERIFIED |
| 5.1-03 | 1 | speed_note renders ONE self-explanatory line (tier + A/s) for any tier or custom | hud_logic.py:159-176 `def speed_note`; TestSpeedNote exact formats for relaxed/normal/fast/expert + custom (test_hud_content.py 57 tests OK scoped) | ✓ VERIFIED |
| 5.1-03 | 2 | No hidden counters (GAME-04 policy untouched) | TestSpeedNote.test_count_free_game_04_guard (line 627) sweeps all tiers | ✓ VERIFIED |
| 5.1-04 | 1 | Setup tab shows 'Speed' group + tier combo with A/s labels, defaulting to normal on fresh setup | gui_setup.py:129-133 (combo from SPEED_TIERS, userData=aps), 199-204 (`QGroupBox('Speed')`, 'Tier:' row), apply_state findData→DEFAULTS; default = 6.0 'normal' (plan's draft "(3.0 A/s)" superseded) | ✓ VERIFIED (as owner-amended: default value) |
| 5.1-04 | 2 | Combo change writes setup['speed'] via collect_state; apply_state restores with default-tier fallback | gui_setup.py:259-261 (`setup['speed'] = currentData()`), 295-305 (findData → DEFAULTS['speed'] → idx 0); signal→_refresh_status wired line 230 | ✓ VERIFIED |
| 5.1-05 | 1 | GO! builds engine at setup's speed — single injection point with DEFAULTS fallback | gui_game.py:494-500 `_build_engine` → `speed_a_per_s=setup.get('speed', setup_logic.DEFAULTS['speed'])`; exactly ONE production `GameEngine(` site repo-wide | ✓ VERIFIED |
| 5.1-05 | 2 | begin_game logs exactly ONE speed line per run (never per tick), beside the mode note | gui_game.py:432-434 (after stack_mode_note, before 'Get ready...'); source-scan pin `source.count('speed_note(') == 1` (test_hud_content.py:641-651) | ✓ VERIFIED |
| 5.1-05 | 3 | Restart replays anchor setup's speed, zero new wiring; mid-run changes next-run only | gui_game.py:1242-1266 `_on_restart` → `begin_game(self._anchor.setup)`; GAME-08 doc note in place | ✓ VERIFIED |
| 5.1-06 | 1 | Pure chain SPEED_TIERS→save→load→merge→validate→GameEngine→step proves SC3 end-to-end for EVERY tier | tests/test_phase51_integration.py TestPerTierPureChain (expectations DERIVED from SPEED_TIERS/DEFAULTS — tweak-proof); 4 tests OK scoped | ✓ VERIFIED |
| 5.1-06 | 2 | Absent-key file loads, merges to default tier, validates clean | TestAbsentKeyBackcompat: asserts merged==DEFAULTS['speed'] AND `speed_tier_for` == 'normal' (default is a NAMED tier) | ✓ VERIFIED (as owner-amended: value) |
| 5.1-06 | 3 | Full WSL gate suite green on completed phase | Verifier re-ran: `python3.6 tests/run_gates.py` → gates 1/2/3 PASS, **Ran 678 tests — OK** | ✓ VERIFIED |
| 5.1-06 | 4 | Live human feel-check approves tiers; verdict table recorded | 5.1-06-SUMMARY.md:98-121 verdict table **APPROVED 2026-09-25** (steps 1-5, 7-9 PASS; 6 SKIPPED by owner; 7 = values rejected → retune); commit 4502143 applied owner values | ✓ VERIFIED (as owner-amended: values retuned post-approval) |

**Score:** 17/17 truths verified — 14 clean, 3 verified-as-owner-amended (value/wording clauses only; every mechanism clause holds).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `serpentrum/game_engine.py` | trailing kwarg speed_a_per_s, stored + consumed; contains "speed_a_per_s" | ✓ VERIFIED | kwarg at :256/:271, storage :295, loud ≤0 ValueError :292-294, consumed :658-659; SPEED_A_PER_S=3.0 constant intact (v1 baseline + fallback only) |
| `tests/test_engine_speed.py` | ≥60 lines, per-tier/default/determinism pins | ✓ VERIFIED | 223 lines; 15 tests OK scoped |
| `serpentrum/setup_logic.py` | SPEED_TIERS + merge_defaults + speed_tier_for | ✓ VERIFIED | :114-119 / :145-163 / :166-179; DEFAULTS['speed']=6.0 owner-final |
| `tests/test_setup_logic.py` | tier pin + merge matrix + tier resolution + round-trip | ✓ VERIFIED | 544 lines; 61 tests OK scoped; exact SPEED_TIERS tuple pin (retuned) |
| `serpentrum/hud_logic.py` | def speed_note pure builder | ✓ VERIFIED | :159-176; purity gate 2 PASS |
| `tests/test_hud_content.py` | TestSpeedNote + one-call-site pin | ✓ VERIFIED | 672 lines; 57 tests OK scoped; pins retuned values |
| `serpentrum/gui_setup.py` | Speed QGroupBox + speed_combo wired into collect/apply/_refresh_status | ✓ VERIFIED | :131-133, :201-204, :230, :259-261, :295-305 |
| `serpentrum/gui_game.py` | _build_engine injection + once-per-run note + restart note | ✓ VERIFIED | :432-434, :444-448, :494-500, :1252-1259 |
| `tests/test_phase51_integration.py` | ≥50 lines, per-tier chain + backcompat + custom pins | ✓ VERIFIED | 132 lines; 4 tests OK scoped; derives expectations (zero edits needed at retune — by design) |
| `.planning/phases/5.1-.../5.1-06-SUMMARY.md` | feel-check verdict table + owner-final values | ✓ VERIFIED | contains "verdict"; APPROVED table at :98-121 |

### Key Link Verification

| From | To | Via (frontmatter pattern) | Status | Details |
| --- | --- | --- | --- | --- |
| game_engine step() movement | self.speed_a_per_s | `self\.speed_a_per_s \* dt` | ✓ MATCH | :658, :659 (head x/y) |
| game_engine __init__/reset | SPEED_A_PER_S default | `speed_a_per_s=SPEED_A_PER_S` | ✓ MATCH | :256, :271 |
| setup_logic merge_defaults | DEFAULTS | `dict\(DEFAULTS\)` | ✓ MATCH | :142 (new_setup), :161 (merge_defaults) |
| setup_logic speed_tier_for | SPEED_TIERS | `SPEED_TIERS` | ✓ MATCH | :176 loop |
| hud_logic speed_note | line format | `speed: %s \(%\.1f A/s\)` | ✓ MATCH | :176 |
| gui_setup combo population | SPEED_TIERS | `SPEED_TIERS` | ✓ MATCH | :132 |
| gui_setup collect_state | setup['speed'] | `setup\['speed'\]` | ✓ MATCH | :261 |
| gui_setup apply_state | findData fallback | `findData` | ✓ MATCH | :299 + fallback :302 |
| gui_game _build_engine | GameEngine kwarg | `speed_a_per_s=setup\.get\('speed'` | ✓ MATCH | :500 (single production site) |
| gui_game begin_game | hud_logic.speed_note | `speed_note\(` | ✓ MATCH | :433 — exactly ONE call site in src/ |

### Success Criteria Cross-Check (ROADMAP Phase 5.1)

| SC | Criterion | Verdict | Evidence |
| --- | --- | --- | --- |
| SC1 | Setup tab speed/difficulty selector with named tiers; default = v1 baseline | ✓ VERIFIED-AS-OWNER-AMENDED | Selector + 4 named tiers verified (gui_setup.py). Owner retune (2026-09-25): default = 'normal' 6.0; the v1 baseline 3.0 survives as the 'relaxed' tier floor AND as `SPEED_A_PER_S`/kwarg fallback. The "default = 3.0" literal is superseded by the owner's feel-check authority (ROADMAP notes: "final values approved by the owner at the feel-check"); the mechanism (named tiers + a default tier) is intact. |
| SC2 | Speed applies at GO!, constant for the whole run; Restart keeps the selection | ✓ VERIFIED | Single production construction site (:500) reading the setup dict once; zero mid-run mutation paths (repo-wide assignment grep → 1 hit, in reset); `_on_restart` replays `self._anchor.setup`. Owner feel-check steps 3/4/9 confirm live: same-tier restart → same layout, mid-run switch does NOT change running speed, tier switch + Restart → new tier. |
| SC3 | Save→Load reproduces speed; pre-feature files load default tier (backcompat) | ✓ VERIFIED | Pure round-trip proven end-to-end (integration chain per tier + absent-key merge + custom acceptance). Phase-8 Save/Load BUTTONS (SETUP-07/08) intentionally out of scope — this phase proves the pure half they will inherit, exactly as planned. |
| SC4 | Live human feel-check approves final tier values (checkpoint) | ✓ VERIFIED | 5.1-06-SUMMARY verdict table APPROVED 2026-09-25; owner finalized values; applied as data-only edit (commit 4502143). Per orchestrator directive, GUI-live behavior is owner-verified in that checkpoint — no re-verification demanded. |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
| --- | --- | --- |
| GAME-11 | Evidence complete (table row currently "Pending" — orchestrator to mark Complete) | None. Requirement exists at REQUIREMENTS.md:37; the "(3.0 Å/s)" parenthetical in its text carries the same stale default as the goal — superseded by the owner amendment; the operative contract (selector + GAME-08 constant-per-run + persisted + default tier) is fully satisfied. |

### Gate Results (verifier-executed)

- `python3.6 tests/run_gates.py` → gate 1 syntax+plugin-path **PASS**, gate 2 AST purity **PASS**, gate 3 unittest **PASS** — **Ran 678 tests, OK**
- Scoped: `test_phase51_integration.py` 4 OK · `test_engine_speed.py` 15 OK · `test_setup_logic.py` 61 OK · `test_hud_content.py` 57 OK

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | TODO/FIXME/placeholder/not-implemented/stub scan over all 10 phase files: 0 hits | — | — |

### Notes

- **Seed coupling (documented accepted, not a gap):** `spawn.seed_from_setup` crc32-hashes the whole setup dict, so a tier switch re-seeds the deterministic pickup sequence — same semantics as changing box size; pinned in TestSeedCouplingPin (integration) and documented in `_on_restart`.
- **fast 7.5 / expert 9.0** are owner "try" values pending a future feel round; future tweaks are single-table edits (`SPEED_TIERS`) + the exact tuple pin; the integration test needs zero edits (derives from the table by design).

### Human Verification Required

None outstanding — SC4's live GUI feel-check was performed and APPROVED by the owner in real Windows PyMOL 2.5.0 on 2026-09-25 (verdict table in 5.1-06-SUMMARY.md). Per verification scope, GUI-live truths are owner-verified there and not re-demanded.

### Gaps Summary

No gaps. Every plan must-have truth, artifact, and key-link pattern verified against source; gates re-run green (678 tests); SC1-SC4 all verified (SC1/SC4 carry the documented owner amendment on tier VALUES, which the ROADMAP itself authorizes as a data-only owner edit at the feel-check).

---

_Verified: 2026-09-24T20:56:37Z_
_Verifier: OpenCode (gsd-verifier)_
