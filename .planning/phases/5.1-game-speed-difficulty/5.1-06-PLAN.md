---
phase: 5.1-game-speed-difficulty
plan: 06
type: execute
wave: 3
depends_on: ["5.1-04", "5.1-05"]
files_modified:
  - tests/test_phase51_integration.py
  - serpentrum/setup_logic.py
  - tests/test_setup_logic.py
  - tests/test_hud_content.py
  - tests/test_engine_speed.py
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The pure chain SPEED_TIERS -> save_setup -> load_setup -> merge_defaults -> validate -> GameEngine(speed_a_per_s=...) -> step proves SC3 end-to-end at the pure layer for every tier"
    - "A setup file WITHOUT a speed key loads, merges to the default tier (3.0), and validates clean (SC3 backcompat)"
    - "The full WSL gate suite (syntax + plugin-path + purity + entire unittest suite) is green on the completed phase"
    - "A live human feel-check in real Windows PyMOL approves the tiers (SC4) — verdict table recorded, final tier values decided by the owner"
  artifacts:
    - path: tests/test_phase51_integration.py
      provides: "per-tier pure integration chain + absent-key backcompat + custom-speed acceptance pins (derives expectations from SPEED_TIERS so value tweaks do not break it)"
      min_lines: 50
    - path: .planning/phases/5.1-game-speed-difficulty/5.1-06-SUMMARY.md
      provides: "feel-check verdict table + owner-finalized tier values"
      contains: "verdict"
  key_links:
    - from: "tests/test_phase51_integration.py"
      to: "setup_logic.merge_defaults + GameEngine(speed_a_per_s=...)"
      via: "the load -> merge -> validate -> construct -> step chain (SC3's test-level proof; Phase-8 Save/Load buttons inherit it for free)"
      pattern: "speed_a_per_s"
---

<objective>
Close Phase 5.1: prove the whole speed chain purely, run the full gates, then put the feature in front of the owner for the live feel-check (SC4) — and apply the owner's final tier values as a data-only edit.

Purpose: SC3 is proven by pure-layer round-trip tests (the Save/Load BUTTONS are Phase 8's SETUP-07/08 — the wiring will inherit this for free); SC4 requires a human in real Windows PyMOL. This is the ONLY plan with a checkpoint, and it is the final plan of the phase.

Output: integration test file, green gates, an approved feel-check verdict, owner-finalized tier values encoded in the single data table.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-setup-persistence.md
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-engine-speed.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key facts for this plan:
- **Feel-check precedents:** 04-09 (12-step manual checklist with per-step EXPECT lines and a PASS/FAIL verdict table) is the template; 05-16 shows per-item approvals ACCUMULATE across rounds when values may iterate — use that model here (approve tiers individually; iterate only what fails).
- **Calibration anchor:** 04-09 recorded a ~6 s medium-box wall-cross at 3.0 A/s on the THEN-current box; presets were enlarged twice since (now medium +/-55). RE-MEASURE; nominal cross time ~= usable span / speed (medium usable 108 A: ~54 s relaxed / ~36 s normal / ~24 s fast / ~18 s expert).
- **No Save/Load buttons exist until Phase 8** — tell the owner that SC3 is proven at the pure layer (the integration test below) and the Phase-8 buttons will inherit it.
- **Accepted side effects to surface (not fix):** tier switch re-seeds the deterministic pickup sequence (crc32 over the whole setup dict); the spawn-pool cooldown stays tick-based (~10 s wall) so at expert the head travels ~60 A during a spawn pause; turn sweep wall time (~0.6 s) is tier-invariant.
- Draft tiers: relaxed 2.0 / normal 3.0 / fast 4.5 / expert 6.0 A/s — owner finalizes at this checkpoint; `SPEED_TIERS` is the single data table (value tweaks = one-line edits + test-pin updates).
- Reference SYMBOLS, never line numbers; Phase 5.2 landed first — its GUI additions are untouched by the feel-check steps.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure integration chain test + full gate pass</name>
  <files>tests/test_phase51_integration.py</files>
  <action>
    Create `tests/test_phase51_integration.py` (house conventions: sys.path self-insert header; NO `__init__.py`; unittest). It DERIVES expectations from `setup_logic.SPEED_TIERS` so owner value tweaks never break this file:

    - Per-tier chain (parametrize over `SPEED_TIERS`): `s = setup_logic.new_setup()`; `s['speed'] = aps`; `loaded = setup_logic.load_setup(setup_logic.save_setup(s))`; `merged = setup_logic.merge_defaults(loaded)`; `errors, _ = setup_logic.validate(merged)` -> no errors; `engine = game_engine.GameEngine(head=(0.0, 0.0), heading='right', box_min=(x0, y0), box_max=(x1, y1)` from `setup_logic.BOX_PRESETS['medium']`, `speed_a_per_s=merged['speed'])`; `engine.step(0.1)` -> head advanced EXACTLY `aps * 0.1` along +x (assertAlmostEqual delta 1e-9). This is the SC3 + SC2 pure proof.
    - Absent-key backcompat: build a JSON dict with all DEFAULTS keys EXCEPT `speed` (schema_version 1, demo_set 'set_a', head_molecule 'random', box_preset 'medium', xtb_path None, win_cap_molecules 10, atom_budget 100, broadening_fwhm 16.0), `json.dumps` it, `load_setup` -> assert `'speed' not in loaded`; `merge_defaults(loaded)` -> `speed == 3.0` and `validate` clean (SC3's "files written before this feature load with the default tier").
    - Custom speed acceptance: `merge_defaults` a dict with `speed: 50.0` -> `validate` clean (accept-as-is pin) and `speed_tier_for(50.0) == 'custom'`.
    - Seed-coupling documentation pin: `spawn.seed_from_setup(merged_with_speed_A)` != `spawn.seed_from_setup(merged_with_speed_B)` for two different tier values (accepted side effect, deterministic per tier).

    Then run the FULL default gate suite. If anything fails, fix within this phase's scope (symbol-level fixes only; no redesign) and re-run.
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_phase51_integration.py" -v -> all pass; python3.6 tests/run_gates.py -> FULL suite green</verify>
  <done>The complete pure chain (tier table -> persistence -> validation -> engine movement) is proven per tier, plus absent-key backcompat; all gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — live feel-check of the speed tiers (SC4)</name>
  <files>.planning/phases/5.1-game-speed-difficulty/5.1-06-SUMMARY.md</files>
  <what-built>
    The complete GAME-11 speed/difficulty feature: a Setup-tab Speed group with a 4-tier combo (relaxed/normal/fast/expert), the chosen speed injected into the engine at GO! via _build_engine, constant within the run (GAME-08), Restart-aware, surfaced once per run in the info box, and persisted through the pure save/load round-trip with absent-key backcompat. All WSL gates + the new integration chain are green (Task 1).
  </what-built>
  <how-to-verify>
    In REAL Windows PyMOL 2.5.0 (open the plugin, Setup tab). Record a PASS/FAIL verdict per step; approve tiers INDIVIDUALLY (accumulating model — only re-test what fails or gets retuned):

    1. Setup tab shows a 'Speed' group with a Tier combo listing 'relaxed (2.0 A/s)', 'normal (3.0 A/s)', 'fast (4.5 A/s)', 'expert (6.0 A/s)'. EXPECT: default selection = 'normal (3.0 A/s)'.
    2. Leave normal -> Start -> GO! -> steer ~30 s. EXPECT: feel IDENTICAL to the pre-feature baseline (regression check) and ONE info-box line 'speed: normal (3.0 A/s) - steering cadence and turn time are unchanged' logged at run start (never repeated per tick).
    3. Restart. EXPECT: new run again at normal; the pickup layout matches the first normal run (same-tier restart is deterministic).
    4. Mid-run: switch the tier combo on the Setup tab. EXPECT: the RUNNING speed is unchanged (GAME-08) — the change takes effect only on the next Start/Restart.
    5. Select relaxed (2.0) -> Start -> GO! -> steer ~30 s. EXPECT: clearly slower; note your subjective verdict.
    6. On medium, drive perpendicular wall-to-wall and time the crossing. EXPECT: roughly consistent with the tier (nominal ~54 s relaxed / ~36 s normal / ~24 s fast / ~18 s expert on the current +/-55 box — re-measured values are the record, the 04-09 ~6 s anchor predates two box enlargements).
    7. Repeat steering + verdict for fast (4.5) and expert (6.0). EXPECT: progressively harder; note the reaction window to fresh pickups (shorter at speed).
    8. At expert, observe a spawn-pool cooldown pause (~10 s). EXPECT: the head keeps travelling (~60 A) during the pause — confirm the tick-based cooldown stays acceptable (it is constant wall-time by design).
    9. Switch tier, then Restart. EXPECT: the new run runs at the NEWLY selected tier (anchor semantics), and the pickup layout differs from the previous tier's run (accepted crc32(setup) re-seed) while staying deterministic within a tier.
    10. Owner decisions: (a) FINAL tier values — the drafts 2.0/3.0/4.5/6.0 may be adjusted (values live in ONE table, setup_logic.SPEED_TIERS); (b) optional wording tweak of the speed note line; (c) confirm the accept-as-is rule (hand-tuned speeds stay loadable). Note: Save/Load BUTTONS are Phase 8 — SC3 is proven by the pure round-trip tests; the buttons will inherit it.

    Verdict: PASS/FAIL table over steps 1-10 + the final tier values.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with adjusted tier values / wording) or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply owner-finalized tier values (conditional, data-only)</name>
  <files>serpentrum/setup_logic.py, tests/test_setup_logic.py, tests/test_hud_content.py, tests/test_engine_speed.py</files>
  <action>
    IF the owner approved the draft values unchanged: no code change — record "tier values approved as drafted" in the SUMMARY and stop.

    IF the owner adjusted any value(s): edit ONLY the `SPEED_TIERS` table in `serpentrum/setup_logic.py` (single data edit — no logic changes anywhere), then update the value pins that hardcode the drafts:
    - `tests/test_setup_logic.py`: the exact `SPEED_TIERS` tuple pin.
    - `tests/test_hud_content.py`: the `speed_note` expected strings only if a value change alters the rendered number (labels derive from the table at runtime; only the test literals need the new value).
    - `tests/test_engine_speed.py`: any literal tier speeds used in cases (2.0/4.5/6.0) if they were changed.
    Names/order stay unless the owner also renamed tiers. Then re-run the full gates. Commit: `feat(5.1-06): owner-finalized speed tier values (feel-check)` — or `docs(5.1-06): record approved tier values` if only the SUMMARY changed.
  </action>
  <verify>python3.6 tests/run_gates.py green after the value edit (or a no-op confirmation if values unchanged)</verify>
  <done>The shipped tier values are exactly what the owner approved; all pins consistent; gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> full WSL gate suite green on the completed phase (pure integration chain included).
- The feel-check verdict table exists in the SUMMARY with a per-tier owner verdict and the final tier values.
- Cross-checks against the phase success criteria: SC1 (selector + default), SC2 (applies at GO!, constant, Restart-aware — verified live in steps 2-4, 9), SC3 (pure round-trip + backcompat — Task 1), SC4 (checkpoint approved — Task 2).
</verification>

<success_criteria>
- Every tier survives the pure chain: table -> save -> load -> merge -> validate -> engine steps at tier speed.
- Pre-feature setup files (no speed key) load at the default tier.
- All gates green; the human feel-check is APPROVED with owner-finalized tier values encoded in SPEED_TIERS.
- GAME-08 holds live: mid-run combo changes never affect the running game.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-06-SUMMARY.md`
</output>
