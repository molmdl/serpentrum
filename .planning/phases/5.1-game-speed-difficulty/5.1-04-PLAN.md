---
phase: 5.1-game-speed-difficulty
plan: 04
type: execute
wave: 2
depends_on: ["5.1-02"]
files_modified:
  - serpentrum/gui_setup.py
autonomous: true
must_haves:
  truths:
    - "Setup tab shows a 'Speed' group with a tier combo listing relaxed/normal/fast/expert with their A/s values, defaulting to normal (3.0 A/s) on a fresh setup"
    - "Changing the tier combo writes setup['speed'] immediately via collect_state (the anchor tracks it live), and apply_state restores the combo from a loaded speed with fallback to the default tier"
  artifacts:
    - path: serpentrum/gui_setup.py
      provides: "QGroupBox('Speed') + speed_combo populated from setup_logic.SPEED_TIERS, wired into collect_state/apply_state/_refresh_status"
      contains: "speed_combo"
  key_links:
    - from: "gui_setup.py speed_combo population"
      to: "setup_logic.SPEED_TIERS"
      via: "addItem(label, userData=aps) per tier (box_combo pattern)"
      pattern: "SPEED_TIERS"
    - from: "gui_setup.py collect_state"
      to: "setup dict key 'speed'"
      via: "currentData() write (speed moves from preserved-key to widget-written)"
      pattern: "setup\\['speed'\\]"
    - from: "gui_setup.py apply_state"
      to: "speed_combo selection"
      via: "findData(setup['speed']) with fallback to the default-tier index"
      pattern: "findData"
---

<objective>
Add the Setup-tab speed/difficulty selector: a `QGroupBox('Speed')` with a tier `QComboBox`, 1:1 copy of the proven `box_combo` pattern, wired into the existing collect/apply round-trip.

Purpose: SC1 of GAME-11 (named tiers, default = the v1 baseline). The tier NAME is a UI label; the stored value stays the NUMERIC `setup['speed']` (schema untouched by plan 5.1-02). GUI verdicts are human-verify only (01-05 decision) — the live feel-check happens in plan 5.1-06; this plan's verification is the WSL gate suite.

Output: the selector in `serpentrum/gui_setup.py`, fully round-tripped.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-setup-persistence.md
@.planning/ROADMAP.md
@.planning/STATE.md

Locked constraints — do NOT revisit:
- **Pattern = `box_combo`** (the Box-preset dropdown): `QComboBox` + `addItem(preset.title(), preset)` populated from a pure-schema constant, read via `currentData()` in collect_state, restored via `findData` in apply_state, signal wired to `_refresh_status`. A speed-tier combo is a 1:1 copy with `setup_logic.SPEED_TIERS` as the source.
- **New `QGroupBox('Speed')`** with a `QFormLayout` row (house pattern for new sections — like the Box group). Do NOT restructure existing groups; 5.2 may add its own sibling group.
- **Stored value stays NUMERIC** (`speed` key in A/s); combo `userData` = the A/s float; label = `'%s (%.1f A/s)' % (name, aps)` — ASCII 'A/s' (house style, no Å character).
- **Default selection = the tier whose value == `setup_logic.DEFAULTS['speed']`** (3.0 = 'normal'). apply_state fallback for a non-tier loaded value (e.g. hand-edited 50.0): `findData` fails -> select the default-tier index (house fallback pattern: demo->idx 0, head->Random). The dict still holds 50.0 until the next collect_state normalizes it — that is the accepted house semantics.
- **`.exec_()` stays banned** (AST purity gate); no modal anything; `gui_setup.py` is already in the GUI purity allowlist (pymol.Qt only).
- **collect_state docstring update:** `speed` moves from the "preserved non-widget keys" list to widget-written (same as `box_preset`).
- python3.6 syntax (%-formatting); NO new imports beyond what exists (`setup_logic` is already imported).
- Reference SYMBOLS, never line numbers. **5.2 robustness:** Phase 5.2 executes first and adds its own consent control to gui_setup.py (possibly its own QGroupBox + collect/apply entries). Locate every seam by symbol name (`_build_widgets`, `_build_layout`, `_wire_signals`, `collect_state`, `apply_state`, the `box_combo` block) and add the Speed control ALONGSIDE 5.2's additions without reordering or rewriting them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Speed group widgets, layout, and signal wiring</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
    In `_build_widgets`: create `self.speed_combo = QtWidgets.QComboBox(self)`; populate by iterating `setup_logic.SPEED_TIERS` in order: `self.speed_combo.addItem('%s (%.1f A/s)' % (name, aps), aps)`. Do NOT add a sentinel item (unlike the demo combo) — every tier is a real value.

    In `_build_layout`: build `speed_group = QtWidgets.QGroupBox('Speed', self)` with a `QFormLayout`; `speed_form.addRow('Tier:', self.speed_combo)`; add the group to the main vertical layout AFTER the Win cap group (order relative to any 5.2-added group is not critical; just append a sibling group — do not reorder existing ones).

    In `_wire_signals`: `self.speed_combo.currentIndexChanged.connect(self._refresh_status)` (mirrors the box_combo wiring) so the status label re-validates on tier change.
  </action>
  <verify>python3.6 tests/run_gates.py (purity + syntax + full suite green); grep confirms gui_setup.py contains `QGroupBox('Speed'`, `speed_combo`, and `SPEED_TIERS`</verify>
  <done>Speed group + tier combo exist, populated from SPEED_TIERS with ASCII labels, signal wired to _refresh_status; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: collect_state / apply_state round-trip + docstring truth</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
    In `collect_state` (right after the box-preset block, same style): read `speed = self.speed_combo.currentData()`; `if speed is not None: setup['speed'] = speed` (guarded write, mirroring box_combo). Update the collect_state docstring: remove `speed` from the preserved-non-widget list and note it is now widget-written via the tier combo.

    In `apply_state` (right after the box-preset block, same style): `idx = self.speed_combo.findData(setup.get('speed', setup_logic.DEFAULTS['speed']))`; if `idx < 0`, fall back to `idx = self.speed_combo.findData(setup_logic.DEFAULTS['speed'])`; if still `< 0`, `idx = 0`; then `self.speed_combo.setCurrentIndex(idx)`. Use the DEFAULTS reference (not a literal 3.0) so the default tier has ONE source of truth. This runs under the existing `_loading` guard like every other widget restore.

    Do NOT touch `_on_apply`, `_on_start`, or any signal plumbing — Start already emits `collect_state()` and Restart already re-reads the anchor (verified in plan 5.1-05's documentation task; no wiring here).
  </action>
  <verify>python3.6 tests/run_gates.py green; grep confirms `setup['speed']` write in collect_state and the findData fallback in apply_state; the docstring no longer lists speed as preserved</verify>
  <done>Tier selection round-trips: collect writes setup['speed'] from currentData; apply restores from the dict with default-tier fallback; fresh setups land on normal (3.0 A/s).</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> full WSL gate suite green (gui_setup.py compiles under 3.6, passes AST purity as a GUI module, and the whole unittest suite stays green).
- Source checks: combo populated from `SPEED_TIERS` (not hardcoded literals), `currentData`/`findData` round-trip present, no `.exec_()` anywhere.
- Visual/interactive verdict is deliberately deferred to plan 5.1-06's human feel-check (headless widget construction is a proven dead end — 01-05).
</verification>

<success_criteria>
- The Setup tab offers a speed/difficulty selector with the four named tiers and their A/s values, defaulting to normal (3.0 A/s) — SC1's UI half.
- The selection rides the existing setup dict (numeric `speed` key) with zero schema change, so persistence (SC3) works through the already-green save/load round-trip.
- No restructuring of existing groups, no modal dialogs, gates fully green.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-04-SUMMARY.md`
</output>
