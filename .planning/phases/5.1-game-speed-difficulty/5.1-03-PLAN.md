---
phase: 5.1-game-speed-difficulty
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/hud_logic.py
  - tests/test_hud_content.py
autonomous: true
must_haves:
  truths:
    - "speed_note renders ONE self-explanatory info line naming the tier and its A/s value for any tier or custom speed"
    - "The line carries no hidden counters — only the tier name and speed value the user chose/owns (GAME-04 hidden-counts policy untouched)"
  artifacts:
    - path: serpentrum/hud_logic.py
      provides: "speed_note(tier_name, speed) pure builder following the stack_mode_note once-per-run pattern"
      contains: "def speed_note"
    - path: tests/test_hud_content.py
      provides: "TestSpeedNote cases: exact format per tier, %.1f rendering, custom-name rendering, count-free guard"
      contains: "speed_note"
  key_links:
    - from: "serpentrum/hud_logic.py speed_note"
      to: "the once-per-run info-box line format"
      via: "single %-format return (ASCII 'A/s' house style; caller resolves the tier name via setup_logic.speed_tier_for)"
      pattern: "speed: %s \\(%.1f A/s\\)"
---

<objective>
Add the PURE half of the once-per-run speed line: `hud_logic.speed_note(tier_name, speed)`.

Purpose: research recommends surfacing the chosen speed once per run in the rolling info box (educator value, "clear but sufficient in-game explanation", no speed display exists anywhere today). This plan builds and tests the pure string builder on the proven `stack_mode_note` precedent; the GameTab log call lands in plan 5.1-05. Pure = WSL-testable; no GUI verdict needed for the string itself.

Output: one new pure builder + its test class. TDD: RED -> GREEN, 2 atomic commits.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-setup-persistence.md
@.planning/ROADMAP.md
@.planning/STATE.md

Locked constraints — do NOT revisit:
- **Pattern = `hud_logic.stack_mode_note`**: a pure builder returning ONE line (or None), logged ONCE per `begin_game`. Read that function as the structural template (docstring shape: what/why/who-calls-it).
- **Signature decided:** `speed_note(tier_name, speed)` — the tier NAME is resolved by the caller (plan 5.1-05 calls `setup_logic.speed_tier_for(speed)`); hud_logic only renders sentences. hud_logic does NOT import setup_logic.
- **GAME-04 hidden-counts policy untouched:** the line must never contain molecule/atom counts. Speed and tier name are fine (user-chosen, not hidden state).
- **ASCII 'A/s'** (house style of every user-visible string; no Å character), %-formatting, python3.6 syntax, PURE module (no pymol/Qt/numpy).
- The note is always a line (speed always exists) — no None case needed, unlike stack_mode_note.
- Reference SYMBOLS, never line numbers (Phase 5.2 may land first and shift lines; it may also add note builders to hud_logic.py — append, do not restructure).
</context>

<feature>
  <name>speed_note: the once-per-run speed info-box line builder</name>
  <files>serpentrum/hud_logic.py, tests/test_hud_content.py</files>
  <behavior>
    RED first — add a `TestSpeedNote` class to `tests/test_hud_content.py` (the file where note builders are tested — `stack_mode_note`'s cases live there). Cases (input -> expected):

    1. `speed_note('normal', 3.0)` == `'speed: normal (3.0 A/s) - steering cadence and turn time are unchanged'` (exact string).
    2. `speed_note('fast', 4.5)` == `'speed: fast (4.5 A/s) - steering cadence and turn time are unchanged'`; `speed_note('expert', 6.0)` renders `'6.0'` (%.1f always one decimal).
    3. `speed_note('custom', 3.2)` == `'speed: custom (3.2 A/s) - steering cadence and turn time are unchanged'` (hand-tuned speeds surface honestly).
    4. GAME-04 guard: for every case, the returned line contains NO digit-run that could be a molecule/atom count beyond the speed value itself — assert the line matches the exact template with only (tier, value) substituted (a format-shaped assertion is enough).
    5. Trailing speeds like `2.0` render `'2.0'` (relaxed), never `'2'`.

    Run: all new tests MUST FAIL (speed_note does not exist).

    Commit: `test(5.1-03): add failing tests for hud_logic.speed_note`
  </behavior>
  <implementation>
    GREEN — in `serpentrum/hud_logic.py`, add next to `stack_mode_note`:

    - `def speed_note(tier_name, speed):` returning `'speed: %s (%.1f A/s) - steering cadence and turn time are unchanged' % (tier_name, speed)`.
    - Docstring following stack_mode_note's shape: what it is (the once-per-run speed line), why (the tier is invisible otherwise — no speed display exists in the HUD), who logs it (begin_game, ONCE per run — Start or Restart — never per tick), and that the tier name comes from `setup_logic.speed_tier_for` (exact match or 'custom').

    Nothing else changes. Run: new tests PASS + full file suite green. REFACTOR only if obvious.

    Commit: `feat(5.1-03): speed_note HUD builder (pure, once-per-run line)`
  </implementation>
</feature>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v` -> all pass (existing note-builder classes untouched and green)
- `python3.6 tests/run_gates.py` -> syntax + plugin-path safety + AST purity + FULL suite green
</verification>

<success_criteria>
- `speed_note(tier_name, speed)` exists as a pure builder with the exact pinned format for tier and custom names.
- The GUI log call does NOT exist yet (that is plan 5.1-05's single wiring point).
- GAME-04 discipline holds: no counts in the line.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-03-SUMMARY.md`
</output>
