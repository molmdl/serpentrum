---
phase: 5.1-game-speed-difficulty
plan: 06
subsystem: testing
tags: [speed-tiers, integration-test, unittest, setup-persistence, game-engine]

# Dependency graph
requires:
  - phase: 5.1-02
    provides: SPEED_TIERS table + merge_defaults + speed_tier_for in setup_logic
  - phase: 5.1-04
    provides: setup-tab Speed tier selector round-trip
  - phase: 5.1-05
    provides: engine speed_a_per_s kwarg injection + once-per-run speed note
provides:
  - Per-tier pure integration chain test (SC3 + SC2 proof at the pure layer)
  - Absent-key backcompat pin (pre-feature files default-tier cleanly)
  - Custom-speed acceptance + seed-coupling documentation pins
  - Full gate suite green on the completed phase (678 unittests)
affects: [phase-8 (Save/Load buttons inherit this pure round-trip), phase-5.2]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Integration tests derive expectations from SPEED_TIERS/DEFAULTS, never hardcode tier values"

key-files:
  created:
    - tests/test_phase51_integration.py
  modified: []

key-decisions:
  - "Integration test derives all expectations from setup_logic.SPEED_TIERS and DEFAULTS so owner value tweaks at the feel-check never break the test file"
  - "Seed coupling (crc32 over the whole setup dict re-seeds per tier) pinned as a documented accepted side effect, not a bug"

patterns-established:
  - "Chain test: table -> save -> load -> merge -> validate -> GameEngine -> step, parametrized over the tier table"

# Metrics
duration: ~10 min (Task 1 only; checkpoint pending)
completed: 2026-09-21
---

# Phase 5.1 Plan 06: Phase close — pure integration chain + feel-check Summary

**Pure end-to-end proof that every SPEED_TIERS entry survives the chain table -> save -> load -> merge -> validate -> GameEngine -> step, plus backcompat and acceptance pins; gates fully green (678 tests).**

## Performance

- **Duration:** ~10 min (Task 1; plan paused at checkpoint)
- **Started:** 2026-09-20T18:40:34Z
- **Completed:** in progress (Task 2 checkpoint pending)
- **Tasks:** 1 of 3 (Task 1 complete; Task 2 = blocking human-verify checkpoint; Task 3 = conditional post-checkpoint)
- **Files modified:** 1 created, 0 edited

## Accomplishments

- **SC3 + SC2 pure proof (Task 1):** `tests/test_phase51_integration.py` runs the complete pure chain — `SPEED_TIERS` -> `new_setup` -> `save_setup` -> `load_setup` -> `merge_defaults` -> `validate` -> `GameEngine(speed_a_per_s=...)` -> `step(0.1)` — for EVERY tier, asserting the head advances exactly `aps * 0.1` along +x (delta 1e-9). All expectations are DERIVED from `setup_logic.SPEED_TIERS` / `DEFAULTS`, so owner value tweaks at the feel-check never break the file.
- **Absent-key backcompat (SC3):** a setup file written WITHOUT a `speed` key loads raw (key stays absent), merges to `DEFAULTS['speed']`, validates clean, and resolves to the named `normal` tier — pre-feature files load with the default tier.
- **Custom-speed acceptance:** a hand-tuned `speed: 50.0` merges and validates clean and resolves to `'custom'` — the accept-as-is rule (combo is the UI constraint, not the schema).
- **Seed-coupling pin:** `spawn.seed_from_setup` crc32-hashes the whole setup dict, so two different tiers produce different deterministic seeds — pinned as a DOCUMENTED ACCEPTED side effect (same semantics as changing box size), with per-tier determinism verifiable by re-hash.
- **Full gate suite green:** `python3.6 tests/run_gates.py` -> gate 1 (syntax + plugin-path) PASS, gate 2 (AST purity) PASS, gate 3 (unittests) PASS — 678 tests (674 baseline + 4 new), all green.

## Task Commits

1. **Task 1: Pure integration chain test + full gate pass** — `0410d57` (test)

_Plan metadata commit + any Task 3 commit land after the checkpoint._

## Files Created/Modified

- `tests/test_phase51_integration.py` (created, 132 lines) — per-tier pure chain + absent-key backcompat + custom-speed acceptance + seed-coupling pin; house conventions (sys.path self-insert, no `__init__.py`, unittest)

## Decisions Made

- **Derived expectations:** the integration test resolves expected speeds from `SPEED_TIERS` / `DEFAULTS` at runtime rather than pinning literals, so the owner's final tier-value edit (Task 3, single-table data edit) never requires touching this file. Rationale: plan 5.1-06 + orchestrator context mandate tweak-safe tests.
- **Default-tier tie to table:** the backcompat test asserts `merged['speed']` equals `DEFAULTS['speed']` AND that `speed_tier_for` resolves it to `'normal'` — pinning that the shipped default must be a named tier, not a custom value.

## Deviations from Plan

**1. [Rule 3 - Blocking] Worktree `tmp/exec-5.1-06` did not exist at spawn**

- **Found during:** execution start (plan file unreadable per Step 0)
- **Issue:** orchestrator context said the worktree existed at b13c24b + merged waves; it did not (worktree list showed only 5.1-01..5.1-05; branch `exec/5.1-06` absent)
- **Fix:** created it from the current main HEAD (c831dd9 — merge of 5.1-05, i.e. b13c24b + all of waves 1+2 merged, satisfying the stated precondition): `git worktree add tmp/exec-5.1-06 -b exec/5.1-06`
- **Impact:** none on scope; all work committed on `exec/5.1-06` as instructed

No other deviations — Task 1 executed exactly as written.

## Feel-check verdict table (Task 2 checkpoint)

**PENDING — post-checkpoint placeholder.** The live feel-check (SC4) is a blocking `checkpoint:human-verify` in real Windows PyMOL 2.5.0 with per-step EXPECT lines (plan Task 2, steps 1-10, accumulating verdict model per 05-16). The per-step PASS/FAIL verdict table and the owner-finalized tier values will be recorded here ONLY after the owner's checkpoint verdict:

| Step | Verdict | Notes |
|---|---|---|
| 1. Speed group + 4-tier combo, default normal | _pending_ | |
| 2. normal == pre-feature baseline + once-per-run speed note | _pending_ | |
| 3. Restart same tier -> same pickup layout | _pending_ | |
| 4. Mid-run combo switch does NOT change running speed (GAME-08) | _pending_ | |
| 5. relaxed (2.0) feel verdict | _pending_ | |
| 6. medium wall-cross timing per tier | _pending_ | |
| 7. fast (4.5) / expert (6.0) feel verdicts | _pending_ | |
| 8. expert spawn-cooldown pause acceptable | _pending_ | |
| 9. tier switch + Restart -> new tier, re-seeded layout | _pending_ | |
| 10. Owner decisions: final tier values / wording / accept-as-is | _pending_ | |

**Final tier values:** _pending owner checkpoint (drafts: relaxed 2.0 / normal 3.0 / fast 4.5 / expert 6.0 A/s; live in the single table `setup_logic.SPEED_TIERS`)_

## Issues Encountered

None during Task 1. (Worktree-creation deviation above; baseline gates were green on entry as expected.)

## Next Phase Readiness

- Task 2 checkpoint is BLOCKING: the phase is not complete until the owner approves the live feel-check and finalizes tier values.
- Task 3 (conditional, data-only): if the owner adjusts values, edit ONLY `setup_logic.SPEED_TIERS` plus the literal pins in `tests/test_setup_logic.py` / `tests/test_hud_content.py` / `tests/test_engine_speed.py`, then re-run gates. If approved as drafted, record and stop.
- Phase 8 (SETUP-07/08 Save/Load buttons) inherits SC3's pure round-trip wiring for free.
- `.planning/STATE.md` was intentionally NOT touched (orchestrator-owned).
