---
phase: 5.1-game-speed-difficulty
plan: 03
subsystem: ui
tags: [hud, info-box, speed-tier, pure-builder, tdd, python3.6]

# Dependency graph
requires:
  - phase: 05-game-core
    provides: "hud_logic.stack_mode_note — the once-per-run begin_game note-builder precedent this builder copies 1:1"
  - phase: 02-foundation
    provides: "setup_logic schema with the numeric 'speed' key (the value speed_note renders)"
provides:
  - "hud_logic.speed_note(tier_name, speed) — pure builder returning the once-per-run speed info-box line: 'speed: <tier> (<s> A/s) - steering cadence and turn time are unchanged'"
  - "TestSpeedNote pins: exact per-tier strings, %.1f one-decimal rendering, custom-name rendering, format-shaped GAME-04 count-free guard"
affects:
  - 5.1-05 (the GUI wiring plan — begin_game logs speed_note ONCE per run next to the stack_mode_note call site)
  - 5.2 (listens-in note builders may append to hud_logic — append, never restructure)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Once-per-run info-box note builder: pure %-format builder returning ONE line, GUI logs it ONCE per begin_game (Start or Restart), never per tick"

key-files:
  created: []
  modified:
    - serpentrum/hud_logic.py
    - tests/test_hud_content.py

key-decisions:
  - "speed_note(tier_name, speed): tier NAME resolved by the caller (5.1-05 calls setup_logic.speed_tier_for); hud_logic never imports setup_logic — renders sentences only"
  - "No None case: speed always exists (schema guarantees number > 0), unlike stack_mode_note's zero-stackable conditional"
  - "Profile/format assertion for GAME-04: the line is pinned as a regex shape with only (tier, value) substituted — no other digit-run can ever read as a hidden molecule/atom count"

patterns-established:
  - "Tier-name-as-argument split: pure HUD builders receive the resolved display name; the tier-resolution logic stays in setup_logic (one-resolution rule)"

# Metrics
duration: 7 min
completed: 2026-09-20
---

# Phase 5.1 Plan 03: speed_note HUD Builder Summary

**Pure `hud_logic.speed_note(tier_name, speed)` builder rendering the once-per-run speed line (`speed: <tier> (N.N A/s) - steering cadence and turn time are unchanged`) on the stack_mode_note precedent — TDD RED→GREEN, WSL-tested, GUI wiring deferred to plan 5.1-05**

## Performance

- **Duration:** 7 min
- **Started:** 2026-09-20T18:11:32Z
- **Completed:** 2026-09-20T18:17:45Z
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 2

## Accomplishments

- `hud_logic.speed_note(tier_name, speed)` added next to `stack_mode_note`: one %-formatted line, ASCII 'A/s' house style, docstring pinning what (once-per-run speed line — no speed display exists in the HUD), why (the tier is invisible otherwise), and who (begin_game logs ONCE per run — Start or Restart — never per tick; the plan 5.1-05 wiring point).
- `TestSpeedNote` test class in `tests/test_hud_content.py` (where `stack_mode_note`'s cases live): exact per-tier strings (normal 3.0, fast 4.5, expert 6.0), `%.1f` one-decimal pins (`6.0` and `2.0` never render as integers), custom-name rendering (3.2), and a format-shaped GAME-04 guard regex.
- GAME-04 hidden-counts policy explicitly preserved: the line carries only the user-chosen tier + speed (owned/visible values), never a molecule/atom count.
- Pure-layer only: `grep speed_note` over `serpentrum/gui*.py` returns zero matches — the begin_game call site does not exist yet (5.1-05's single wiring point, per success criterion).

## Task Commits (TDD RED → GREEN)

1. **RED: failing TestSpeedNote tests** — `ce01837` (test)
2. **GREEN: speed_note builder** — `0561111` (feat)

**Plan metadata:** pending (this SUMMARY's commit)

## Files Created/Modified

- `serpentrum/hud_logic.py` — added `speed_note(tier_name, speed)` pure builder (+21 lines)
- `tests/test_hud_content.py` — added `TestSpeedNote` class (5 cases) + `import re` (+57 lines)

## Decisions Made

None beyond the plan's locked constraints — signature, format string, pattern (stack_mode_note), ASCII 'A/s', and GAME-04 discipline were all plan-locked and followed exactly.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. RED phase failed exactly as required (AttributeError × 5, existing 51 tests green); GREEN phase passed all 56 file tests on first run.

## Verification

- `python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v` → 56 tests, OK (existing note-builder classes untouched and green)
- `python3.6 tests/run_gates.py` → all gates green: syntax + plugin-path safety PASS, AST purity PASS (hud_logic stays PURE — stdlib only), unittest 645/645 PASS (640 baseline + 5 new)
- Must-haves: `def speed_note` present in `serpentrum/hud_logic.py`; format pattern `speed: %s (%.1f A/s)` pinned; no GUI call site exists (verified via grep)

## Next Phase Readiness

- Ready for 5.1-04 (GUI speed combo) and **5.1-05 (engine kwarg + wiring)**: the log call `hud_logic.speed_note(setup_logic.speed_tier_for(speed), speed)` lands ONCE in `begin_game`, next to the existing `stack_mode_note` call site (`gui_game.py:417-420` at Phase-5 state).
- No blockers.

---
*Phase: 5.1-game-speed-difficulty*
*Completed: 2026-09-20*
