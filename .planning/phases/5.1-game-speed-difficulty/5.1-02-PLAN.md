---
phase: 5.1-game-speed-difficulty
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/setup_logic.py
  - tests/test_setup_logic.py
autonomous: true
must_haves:
  truths:
    - "Tier names map to draft speeds via ONE ordered data table (relaxed 2.0 / normal 3.0 / fast 4.5 / expert 6.0) — value tweaks are single-table edits after the feel-check"
    - "A setup dict MISSING the speed key merges to the default tier (3.0) instead of failing validation (schema backcompat for pre-feature files)"
    - "Any speed > 0 still validates and round-trips save/load (custom hand-tuned speeds stay legal; the combo is the UI constraint, not the schema)"
  artifacts:
    - path: serpentrum/setup_logic.py
      provides: "SPEED_TIERS ordered tier table, merge_defaults absent-key backcompat helper, speed_tier_for name resolution"
      contains: "SPEED_TIERS"
    - path: tests/test_setup_logic.py
      provides: "tier-table pin, merge_defaults behavior matrix, tier-name resolution, non-default speed round-trip, accept-as-is validation pin"
      contains: "merge_defaults"
  key_links:
    - from: "serpentrum/setup_logic.py merge_defaults"
      to: "DEFAULTS"
      via: "defaults overlay (dict(DEFAULTS) updated with the loaded dict; DEFAULTS never mutated)"
      pattern: "dict\\(DEFAULTS\\)"
    - from: "serpentrum/setup_logic.py speed_tier_for"
      to: "SPEED_TIERS"
      via: "exact-match tier resolution, 'custom' fallback (feeds the HUD note)"
      pattern: "SPEED_TIERS"
---

<objective>
Give the PURE schema layer the speed-tier data table, the absent-key backcompat merge, and tier-name resolution.

Purpose: SC1 (named tiers with a default) and SC3 (save/load backcompat) live here. The numeric `'speed'` key already exists end-to-end in the schema (DEFAULTS, validate, save/load) — this plan adds ONLY: the tier table the UI and HUD consume, the `merge_defaults` helper that makes absent-key files load at the default tier, and `speed_tier_for` for the HUD note. No schema key is invented, no version bump.

Output: `SPEED_TIERS`, `merge_defaults`, `speed_tier_for` in `serpentrum/setup_logic.py` + an extended test matrix. TDD: RED -> GREEN -> (REFACTOR), 2-3 atomic commits.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-setup-persistence.md
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-engine-speed.md
@.planning/ROADMAP.md
@.planning/STATE.md

The setup-persistence research file is the AUTHORITY (schema analysis, backcompat mechanics, load path behavior). Locked constraints — do NOT revisit:

- **Numeric `speed` key STAYS.** No `speed_tier` string key; no SCHEMA_VERSION bump (`load_setup` hard-rejects version != 1, which would brick existing files). Old files already contain `speed: 3.0` and load at the default tier with zero code change.
- **Do NOT tighten `validate()`** to tier membership — keep "number > 0" so hand-tuned speeds stay loadable (accept-as-is decision).
- **`load_setup` does NO defaults-merge** (pinned by `test_load_does_not_full_validate`) — do NOT change load_setup's contract; `merge_defaults` is a separate explicit helper the caller applies.
- **`EXPECTED_DEFAULTS` in tests/test_setup_logic.py is UNCHANGED** — `speed: 3.0` is already pinned there; any DEFAULTS change would be a visible reviewable schema diff (none is wanted).
- **Draft tier values are owner-finalized at the feel-check** (Phase 5.1-06) — that is why the tiers live in ONE data table.
- python3.6 syntax only (%-formatting), PURE module (json/random stdlib + intra-package imports only).

**EXECUTOR NOTE (5.2 sequencing):** Phase 5.2 (generic upload stacking consent) executes BEFORE this phase and needs the IDENTICAL seam ("absent consent key = OFF"). BEFORE writing any code, grep `serpentrum/setup_logic.py` for `def merge_defaults`. If it already exists (landed by Phase 5.2), REUSE it exactly as-is — extend its tests only if the speed-absent case is not already covered, and do NOT duplicate, rename, or move it. If it does not exist yet, add it per the spec below (5.2 will then reuse yours).
</context>

<feature>
  <name>SPEED_TIERS table + merge_defaults backcompat + speed_tier_for resolution</name>
  <files>serpentrum/setup_logic.py, tests/test_setup_logic.py</files>
  <behavior>
    RED first — extend `tests/test_setup_logic.py` (house style: `EXPECTED_DEFAULTS` block at top, `_mutated(**overrides)` helper, one class per concern). Cases (input -> expected):

    1. `setup_logic.SPEED_TIERS == (('relaxed', 2.0), ('normal', 3.0), ('fast', 4.5), ('expert', 6.0))` — EXACT tuple pin (insertion order matters: it drives the GUI combo order). This pin is the visible reviewable diff when the owner finalizes values at the feel-check.
    2. `merge_defaults({})` == `dict(DEFAULTS)` (all 9 documented keys incl. `speed: 3.0`).
    3. `merge_defaults({'schema_version': 1, 'speed': 4.5})` -> `speed == 4.5`, every other key at its documented default; the INPUT dict is not mutated; `DEFAULTS` is not mutated by the call.
    4. `merge_defaults` preserves unknown EXTRA keys from the loaded dict (e.g. `'note': 'x'` survives — unknown keys are tolerated everywhere today).
    5. `speed_tier_for`: `2.0 -> 'relaxed'`, `3.0 -> 'normal'`, `4.5 -> 'fast'`, `6.0 -> 'expert'`, `3.1 -> 'custom'`, `None -> 'custom'`, `'fast' -> 'custom'` (non-number defensive; exact float match only — no nearest-match guessing).
    6. Accept-as-is pin: `validate(_mutated(speed=50.0))` reports NO `'speed'` error (custom speeds legal; the combo is the UI constraint, not the schema).
    7. Round-trip: `s = new_setup(); s['speed'] = 4.5`; `load_setup(save_setup(s)) == s` (extends TestSaveLoadRoundTrip to a non-default speed).
    8. `EXPECTED_DEFAULTS` (top of file) still passes unchanged — `speed: 3.0` was already in it.

    Run: the tier-table, merge_defaults, and speed_tier_for tests MUST FAIL (symbols don't exist). Cases 6-8 pass already — they are pins protecting decisions, not RED drivers.

    Commit: `test(5.1-02): add failing tests for SPEED_TIERS + merge_defaults + speed_tier_for`
  </behavior>
  <implementation>
    GREEN — minimal code in `serpentrum/setup_logic.py`:
    - `SPEED_TIERS = (('relaxed', 2.0), ('normal', 3.0), ('fast', 4.5), ('expert', 6.0))` — tuple-of-tuples (insertion-ordered, py3.6-safe; NEVER a dict for ordering), placed near BOX_PRESETS with a comment: draft values, owner-finalized at the 5.1 feel-check; SINGLE source for the GUI combo (5.1-04), the HUD note (5.1-03/05) and tier-name resolution; value tweaks are edits to THIS table only. Also note in the comment that `'speed'` stays the NUMERIC schema key (backcompat free; no version bump).
    - `def merge_defaults(loaded):` -> `merged = dict(DEFAULTS); merged.update(loaded); return merged` with a docstring explaining: absent keys fall back to documented defaults — the backcompat seam for setup files written before a key existed (Phase 5.1 speed; Phase 5.2 consent key), applied by CALLERS after `load_setup` (load_setup itself keeps its no-merge contract).
      (Per the executor note above: SKIP if 5.2 already landed it — reuse instead.)
    - `def speed_tier_for(speed):` -> exact match over `SPEED_TIERS` values -> the tier name; anything else -> `'custom'`. Docstring: used by the HUD speed note; deliberately NOT used to tighten validate().
    - Do NOT touch `validate()`, `save_setup`, `load_setup`, `DEFAULTS`, or `SCHEMA_VERSION`.

    Run: new tests PASS + full file suite green. REFACTOR only if obvious. Commit: `feat(5.1-02): speed tier table + defaults-merge backcompat in setup_logic`.
  </implementation>
</feature>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` -> all pass
- `python3.6 tests/run_gates.py` -> syntax + plugin-path safety + AST purity + FULL suite green (existing validate/round-trip/load-error pins untouched and green)
</verification>

<success_criteria>
- `SPEED_TIERS`, `merge_defaults`, `speed_tier_for` exist in pure `setup_logic.py` with the exact pinned behaviors.
- The numeric `speed` key, validate()'s "> 0" rule, load_setup's no-merge contract, and SCHEMA_VERSION 1 are all unchanged.
- Absent-key backcompat proven by test: a dict without `speed` merges to 3.0 and validates clean.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-02-SUMMARY.md`
</output>
