---
phase: 5.1-game-speed-difficulty
plan: 04
subsystem: ui
tags: [pymol, pyqt5, qcombobox, setup-tab, speed-tiers, gui_setup]

# Dependency graph
requires:
  - phase: 5.1-game-speed-difficulty (plan 5.1-02)
    provides: setup_logic.SPEED_TIERS (ordered relaxed 2.0 / normal 3.0 / fast 4.5 / expert 6.0), DEFAULTS['speed'] = 3.0, merge_defaults, speed_tier_for
  - phase: 03-setup-tab (plan 03-07)
    provides: box_combo QComboBox + currentData/findData round-trip pattern, _refresh_status wiring, _loading guard, anchor discipline
provides:
  - QGroupBox('Speed') with tier QComboBox on the Setup tab, populated from setup_logic.SPEED_TIERS with '%s (%.1f A/s)' labels
  - collect_state writes setup['speed'] live from the tier combo (speed moved from preserved-key to widget-written)
  - apply_state restores the combo via findData with default-tier fallback (DEFAULTS reference, not a literal)
affects: [5.1-game-speed-difficulty (5.1-05/5.1-06 engine wiring + feel-check), 5.2 (sibling Setup-tab group insertion point)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Named-tier selector = 1:1 box_combo pattern copy: QComboBox + addItem(label, userData=float) over a pure-schema constant, currentData() guarded write in collect_state, findData + DEFAULTS-tier fallback in apply_state, currentIndexChanged -> _refresh_status"
    - "House fallback semantics for foreign values: loaded non-tier speed selects the default tier; dict keeps the custom value until the next collect_state normalizes it"

key-files:
  created: []
  modified:
    - serpentrum/gui_setup.py

key-decisions:
  - "Stored speed value STAYS numeric (setup['speed'] in A/s); the tier name is a UI label only (combo userData = the A/s float) — zero schema change, free backcompat"
  - "apply_state default-tier lookup references setup_logic.DEFAULTS['speed'] (never a literal 3.0) so the default tier has one source of truth"
  - "Speed group appended AFTER the Win cap group as a new sibling section — existing groups untouched, 5.2 can append its own sibling group at the same seam"

patterns-established:
  - "Tier-combo round-trip: SPEED_TIERS iteration in _build_widgets / currentData guarded write in collect_state / findData(setup.get->DEFAULTS->idx 0) fallback chain in apply_state"
  - "New Setup-tab section = new sibling QGroupBox + QFormLayout row appended to the main QVBoxLayout (never restructure existing groups)"

# Metrics
duration: 5 min
completed: 2026-09-20
---

# Phase 5.1 Plan 04: Setup-tab Speed/Difficulty Selector Summary

**Setup-tab Speed group with a SPEED_TIERS-backed tier QComboBox (relaxed/normal/fast/expert + A/s labels, defaulting to normal 3.0 A/s), fully round-tripped through collect_state/apply_state on the numeric speed key with zero schema change**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-20T18:25:11Z
- **Completed:** 2026-09-20T18:29:28Z
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments

- `QGroupBox('Speed')` + `QFormLayout` `'Tier:'` row with `speed_combo` populated by iterating `setup_logic.SPEED_TIERS` — labels `'%s (%.1f A/s)'` (ASCII, house style), no sentinel item; appended after the Win cap group as a new sibling section.
- `collect_state` guarded write: `speed = self.speed_combo.currentData()` -> `setup['speed'] = speed`, inserted right after the box-preset block; docstring updated (speed removed from preserved-non-widget list, now widget-written via the tier combo).
- `apply_state` restore: `findData(setup.get('speed', setup_logic.DEFAULTS['speed']))` with fallback to the default-tier index, then idx 0, under the existing `_loading` guard — fresh setups land on normal (3.0 A/s); foreign values (e.g. hand-edited 50.0) display the default tier until the next collect normalizes (house semantics).
- `currentIndexChanged` wired to `_refresh_status` so the status label re-validates and the anchor tracks the tier live — Restart picks up a tier change via anchor semantics (5.1-RESEARCH GO!/Restart trace).
- Verification: `python3.6 tests/run_gates.py` fully green (**673 tests**, 3/3 gates); source greps confirm `QGroupBox('Speed'`, `speed_combo`, `SPEED_TIERS`, `findData` fallback; zero `.exec_()` tokens.

## Task Commits

Each task was committed atomically:

1. **Task 1: Speed group widgets, layout, and signal wiring** - `1141633` (feat)
2. **Task 2: collect_state / apply_state round-trip + docstring truth** - `3d2b221` (feat)

## Files Created/Modified

- `serpentrum/gui_setup.py` — Speed section widgets (`_build_widgets`), Speed group layout after Win cap (`_build_layout`), `_refresh_status` wiring (`_wire_signals`), speed write (`collect_state`), findData fallback restore (`apply_state`), docstring truth updates.

## Decisions Made

- Speed group placed **after the Win cap group** (plan: "order relative to any 5.2-added group is not critical"). Phase 5.2 has NOT executed, so gui_setup.py was in its Phase-5 state; seams located by symbol name and appended without restructuring.
- Default-tier fallback uses `setup_logic.DEFAULTS['speed']` exclusively (no literal 3.0 anywhere) — one source of truth for the default tier.
- No new imports (`setup_logic` was already imported); no schema change; no sentinel combo item.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Section-count docstrings updated five -> six**

- **Found during:** Task 1 (Speed group widgets, layout, and signal wiring)
- **Issue:** Class docstring and `_build_layout` docstring stated "Five QGroupBox sections" / "5 QGroupBox sections"; adding the Speed group made both stale (docstring-truth violation — the same housekeeping class as the plan's collect_state docstring update).
- **Fix:** Updated both to six and added 'Speed' to the section list.
- **Files modified:** serpentrum/gui_setup.py
- **Verification:** Greps confirm the counts; gates green.
- **Committed in:** `1141633` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (docstring truth)
**Impact on plan:** Cosmetic documentation accuracy only; no code-behavior or scope change.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 5.1-05 (parallel, separate worktree) wires the engine/HUD side; this plan's Setup-tab control is the SC1 UI half of GAME-11.
- `setup['speed']` now stays live on the anchor from the tier combo, so `gui_game._build_engine` reading `setup.get('speed', ...)` (5.1-05) and Restart re-reading the anchor (already existing semantics) both work with no further Setup-tab changes.
- The visual/interactive verdict is deferred to plan 5.1-06's human feel-check (headless widget construction remains a proven dead end — 01-05 decision).
- 5.2 can append its own sibling group at the same layout seam without touching this control; collect_state's shallow-copy preserved-keys flow still carries any 5.2 keys.

---
*Phase: 5.1-game-speed-difficulty*
*Completed: 2026-09-20*
