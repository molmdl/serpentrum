---
phase: 5.1-game-speed-difficulty
plan: 05
type: execute
wave: 2
depends_on: ["5.1-01", "5.1-02", "5.1-03"]
files_modified:
  - serpentrum/gui_game.py
  - tests/test_hud_content.py
autonomous: true
must_haves:
  truths:
    - "GO! builds the engine at the setup dict's speed — the single production injection point (_build_engine) passes speed_a_per_s from setup['speed'] with the DEFAULTS fallback"
    - "begin_game logs exactly one 'speed: <tier> (<x.x> A/s)' line per run (never per tick), right beside the existing once-per-run mode note"
    - "Restart replays the anchor setup's speed with ZERO new wiring — documented in _on_restart (mid-run combo changes apply next run only, GAME-08)"
  artifacts:
    - path: serpentrum/gui_game.py
      provides: "engine speed injection in _build_engine + once-per-run speed_note log in begin_game + TICK-narrative doc generalization + restart note"
      contains: "speed_a_per_s="
    - path: tests/test_hud_content.py
      provides: "one-call-site pin for speed_note (mirrors the stack_mode_note single-call-site test)"
      contains: "speed_note"
  key_links:
    - from: "gui_game.py _build_engine"
      to: "GameEngine(speed_a_per_s=...)"
      via: "setup.get('speed', setup_logic.DEFAULTS['speed']) at the ONE construction site"
      pattern: "speed_a_per_s=setup\\.get\\('speed'"
    - from: "gui_game.py begin_game"
      to: "hud_logic.speed_note"
      via: "tier resolved via setup_logic.speed_tier_for, logged once via _log"
      pattern: "speed_note\\("
---

<objective>
Wire the setup's chosen speed into the running game: engine injection at the single construction site, the once-per-run HUD speed line, and the doc-narrative generalization.

Purpose: SC2 of GAME-11 (speed applies at GO!, constant within the run, Restart keeps the selection). All semantics are ALREADY correct by construction — Start emits a setup snapshot, the engine is built once per `begin_game` and never re-reads setup, and Restart re-runs `begin_game(self._anchor.setup)`. This plan is therefore SMALL: one kwarg pass-through, one log line, one source-scan test, and doc comments.

Output: wired `gui_game.py` + the one-call-site pin test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-engine-speed.md
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-setup-persistence.md
@.planning/ROADMAP.md
@.planning/STATE.md

Locked constraints — do NOT revisit:
- **GAME-08:** the engine gets its speed ONCE at `begin_game` (via `_build_engine`); nothing may re-read or mutate it mid-run. Mid-run Setup-combo changes write the anchor only and apply at the NEXT Start/Restart — document, do not "fix".
- **Single injection point:** `_build_engine`'s `GameEngine(...)` construction is the only production call site. Pass `speed_a_per_s=setup.get('speed', setup_logic.DEFAULTS['speed'])` (DEFAULTS reference, not a literal).
- **Restart needs NO wiring:** `_on_restart` already re-reads `self._anchor.setup` and re-runs `begin_game` -> `_build_engine`. Add a docstring NOTE only.
- **HUD line:** `hud_logic.speed_note(tier_name, speed)` (from plan 5.1-03) + `setup_logic.speed_tier_for(speed)` (from plan 5.1-02); logged ONCE per `begin_game`, right beside the existing `stack_mode_note` block, BEFORE the 'Get ready...' line. `gui_game.py` already imports both modules.
- **Spawn seed side effect (accepted, document only):** `spawn.seed_from_setup` crc32-hashes the WHOLE setup dict, so a tier change re-seeds the deterministic pickup sequence next run. Do NOT "fix".
- **Doc narrative:** the TICK_DT/TICK_INTERVAL_MS comment block still tells the "0.3 A/tick at SPEED_A_PER_S=3.0" story as THE parameter — generalize it (cadence is the timer's job; per-tick distance = speed_a_per_s * dt from the setup tier; 3.0 default -> 0.3).
- python3.6 syntax (%-formatting); gui_game.py is a GUI purity-allowlisted module.
- Reference SYMBOLS, never line numbers. **5.2 robustness:** Phase 5.2 executes first and may add consent wiring inside `begin_game`/`_build_engine` — locate seams by symbol (`_build_engine`'s GameEngine call, the `stack_mode_note` logging block, `_on_restart`) and add alongside; do not restructure 5.2's code.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine speed injection + once-per-run speed note + restart note + call-site pin</name>
  <files>serpentrum/gui_game.py, tests/test_hud_content.py</files>
  <action>
    In `_build_engine`: extend the `GameEngine(...)` construction with `speed_a_per_s=setup.get('speed', setup_logic.DEFAULTS['speed'])` and mention the tier injection in the method docstring.

    In `begin_game`: after the existing `stack_mode_note` logging block, add the speed line ONCE:
    `speed_val = setup.get('speed', setup_logic.DEFAULTS['speed'])` then
    `self._log(hud_logic.speed_note(setup_logic.speed_tier_for(speed_val), speed_val))`
    (before the 'Get ready...' log). Docstrings updated where they describe begin_game's once-per-run notes.

    In `_on_restart` docstring: add one line — Restart replays the ANCHOR's setup (speed included) through begin_game -> _build_engine; a mid-run Setup-tab tier change therefore applies to the NEXT run only (GAME-08 constant-within-run), and a tier switch re-seeds the deterministic pickup sequence (accepted crc32(setup) semantics, same as changing the box size).

    In `tests/test_hud_content.py`: add `test_begin_game_has_exactly_one_speed_note_call_site` mirroring the existing stack_mode_note one-call-site test (read `serpentrum/gui_game.py` source, assert `source.count('speed_note(') == 1`) — pins the once-per-run wiring against duplication or deletion.
  </action>
  <verify>python3.6 tests/run_gates.py green (incl. the new call-site pin); grep confirms `speed_a_per_s=setup.get('speed'` in _build_engine and exactly one `speed_note(` call in gui_game.py</verify>
  <done>GO! builds the engine at the setup's speed and logs the speed line once per run; Restart semantics documented; call-site pin green.</done>
</task>

<task type="auto">
  <name>Task 2: TICK-narrative doc generalization in gui_game.py</name>
  <files>serpentrum/gui_game.py</files>
  <action>
    Update the `TICK_DT` / `TICK_INTERVAL_MS` comment block so the default-tier framing does not rot: the 100 ms timer + dt=0.1 are the CADENCE (determinism: jitter is eaten by QTimer, not the simulation); the per-tick distance is `speed_a_per_s * dt` from the setup-selected tier injected at `_build_engine` (Phase 5.1 GAME-11); at the default tier (3.0 A/s) that is still 0.3 A/tick. Keep the "playtesting-tunable" note but point tunability at `SPEED_TIERS` in setup_logic (data-only edits).

    NO functional change in this task — comments/docstrings only. NO changes to the timer construction, `_begin_play`, pause/resume, or teardown paths.
  </action>
  <verify>python3.6 tests/run_gates.py green; grep confirms the TICK comment block mentions speed_a_per_s / the tier injection and no longer presents 0.3 A/tick as THE fixed parameter</verify>
  <done>Doc narrative generalized: cadence vs per-tick distance are clearly separated; the default tier is named as one tier among four.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> full WSL gate suite green (including the new one-call-site pin).
- Source checks: exactly one `speed_note(` call site in gui_game.py; `speed_a_per_s=` present in `_build_engine` only (no other engine construction in production code); `_on_restart` untouched functionally.
</verification>

<success_criteria>
- SC2 mechanics wired: the selected speed applies at GO! (engine built from the snapshot), stays constant for the whole run (single construction, no re-reads), and Restart replays the anchor's selection with zero new wiring.
- The once-per-run speed line exists beside the established mode-note precedent; GAME-04 hidden-counts untouched (speed_note carries no counts).
- Doc comments no longer present 0.3 A/tick as THE fixed parameter.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-05-SUMMARY.md`
</output>
