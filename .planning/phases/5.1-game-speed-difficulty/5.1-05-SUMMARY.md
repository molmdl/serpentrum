---
phase: 5.1-game-speed-difficulty
plan: 05
subsystem: game-rules
tags: [game-speed, difficulty-tiers, hud, gui_game, engine-injection]

# Dependency graph
requires:
  - phase: 5.1-01
    provides: GameEngine per-instance speed_a_per_s kwarg (trailing, default SPEED_A_PER_S=3.0)
  - phase: 5.1-02
    provides: setup_logic.SPEED_TIERS + speed_tier_for(speed) + merge_defaults
  - phase: 5.1-03
    provides: hud_logic.speed_note(tier_name, speed) once-per-run builder
provides:
  - Engine speed injection at the single production construction site (_build_engine)
  - Once-per-run 'speed: <tier> (<x.x> A/s)' HUD log beside the stack_mode_note precedent, before 'Get ready...'
  - _on_restart docstring NOTE documenting GAME-08 restart/next-run semantics + accepted crc32(setup) re-seed
  - Source-scan pin: exactly one speed_note( call site in gui_game.py
  - TICK-narrative generalization: cadence (100 ms / dt=0.1) vs per-tick distance (speed_a_per_s * dt)
affects: [5.1-04 (Setup-tab tier combo feeds setup['speed'] here), Phase 5.2 (seams located by symbol alongside begin_game/_build_engine)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single production injection point: engine construction reads setup['speed'] with DEFAULTS fallback, once per begin_game (GAME-08 by construction)"
    - "One-shot HUD note pins: source-scan test asserting exactly one call site per once-per-run log builder (stack_mode_note precedent extended to speed_note)"

key-files:
  created: []
  modified:
    - serpentrum/gui_game.py
    - tests/test_hud_content.py

key-decisions:
  - "None revisited - all constraints locked by plan (single injection point, DEFAULTS fallback reference not literal, Restart needs zero new wiring, spawn re-seed accepted/documented)"

patterns-established:
  - "Once-per-run HUD line: resolve speed once via setup_logic.speed_tier_for, log hud_logic.speed_note(...) beside stack_mode_note, before 'Get ready...'"

# Metrics
duration: 12min
completed: 2026-09-20
---

# Phase 5.1 Plan 05: Engine Speed Injection + Once-Per-Run Speed Note Summary

**Setup-chosen speed wired into the running game: one kwarg pass-through at the single GameEngine construction site, one once-per-run 'speed: <tier> (<x.x> A/s)' HUD line beside the mode-note precedent, restart semantics documented, and the TICK narrative generalized (cadence vs per-tick distance).**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-09-20T18:25:38Z
- **Completed:** 2026-09-20T18:38Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments

- **GAME-11 SC2 wired (GO! speed applies):** `_build_engine` passes `speed_a_per_s=setup.get('speed', setup_logic.DEFAULTS['speed'])` at the ONLY production `GameEngine(...)` call site (grep-verified: one construction in `serpentrum/`). Engine reads its speed once per `begin_game`; nothing re-reads or mutates it mid-run (GAME-08 by construction).
- **Once-per-run HUD speed line:** `begin_game` resolves `speed_val` once, logs `hud_logic.speed_note(setup_logic.speed_tier_for(speed_val), speed_val)` right after the `stack_mode_note` block, before `'Get ready...'`. GAME-04 untouched — the line carries tier + A/s only, no counts.
- **Restart documented, zero new wiring:** `_on_restart` docstring NOTE records that Restart replays the anchor's setup (speed included) through `begin_game -> _build_engine`; mid-run Setup-tab tier changes apply to the NEXT run only (GAME-08); a tier switch re-seeds the deterministic pickup sequence (accepted crc32(setup) semantics, same as changing the box size — documented, NOT "fixed").
- **Call-site pin test:** `test_begin_game_has_exactly_one_speed_note_call_site` mirrors the `stack_mode_note` one-shot pin (`source.count('speed_note(') == 1`).
- **TICK narrative generalized:** comment block now separates CADENCE (100 ms QTimer / dt=0.1, the timer's job; jitter eaten by QTimer for determinism) from per-tick DISTANCE (`speed_a_per_s * dt` from the setup tier injected at `_build_engine`); 3.0 A/s -> 0.3 A/tick is named as the default 'normal' tier among four, not THE fixed parameter; tunability pointed at `SPEED_TIERS` in setup_logic (data-only edits). Comments only, zero functional change.

## Task Commits

Each task was committed atomically (branch `exec/5.1-05`):

1. **Task 1: Engine speed injection + once-per-run speed note + restart note + call-site pin** — `fd5e814` (feat)
2. **Task 2: TICK-narrative doc generalization** — `7c7da2f` (docs)

## Files Created/Modified

- `serpentrum/gui_game.py` — speed_a_per_s kwarg at `_build_engine`'s GameEngine construction; once-per-run speed_note log in `begin_game`; `_on_restart` docstring NOTE; `_build_engine`/`begin_game` docstring updates; TICK_DT/TICK_INTERVAL_MS comment-block generalization
- `tests/test_hud_content.py` — `test_begin_game_has_exactly_one_speed_note_call_site` in TestSpeedNote

## Verification

- `python3.6 tests/run_gates.py` — **green, 674 tests** (syntax + plugin-path safety + AST purity + scoped unittests, incl. the new call-site pin)
- `grep "speed_a_per_s=setup.get('speed'"` — present at the `_build_engine` construction (line 500), the only `speed_a_per_s=` in `serpentrum/gui_game.py`
- `grep -c "speed_note(" serpentrum/gui_game.py` — exactly 1 (the begin_game call site)
- `_on_restart` untouched functionally (docstring-only change)

## Decisions Made

None — all constraints locked by the plan (single injection point, DEFAULTS reference not literal, Restart needs zero new wiring, spawn-seed side effect accepted and documented only).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 5.1-04 (Setup-tab speed-tier combo) can feed `setup['speed']` — this plan's injection point consumes it with the DEFAULTS fallback for pre-combo or hand-edited dicts.
- Phase 5.2 may add consent wiring inside `begin_game`/`_build_engine` — seams were extended alongside existing symbols (stack_mode_note block, GameEngine call) without restructuring.
- No blockers.

---
*Phase: 5.1-game-speed-difficulty*
*Completed: 2026-09-20*
