---
phase: 5.1-game-speed-difficulty
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_speed.py
  - tests/test_engine_core.py
autonomous: true
must_haves:
  truths:
    - "A GameEngine constructed with speed_a_per_s=X advances head AND chain by X*dt per movement tick along the heading (2.0 -> 0.2 A/tick, 6.0 -> 0.6 A/tick at dt=0.1)"
    - "Engines constructed WITHOUT the kwarg behave exactly as before (3.0 A/s default -> 0.3 A/tick) — every existing test call site stays green untouched"
    - "Speed is fixed at construction/reset and has no mid-run mutation path (GAME-08 true by construction)"
  artifacts:
    - path: serpentrum/game_engine.py
      provides: "trailing kwarg speed_a_per_s=SPEED_A_PER_S on __init__/reset, stored as self.speed_a_per_s and consumed by step()'s movement branch"
      contains: "speed_a_per_s"
    - path: tests/test_engine_speed.py
      provides: "per-tier displacement, default-preservation, loud <=0 rejection, determinism, and seed-coupling pins"
      min_lines: 60
  key_links:
    - from: "serpentrum/game_engine.py step() movement branch"
      to: "self.speed_a_per_s"
      via: "head-advance arithmetic (replaces the module constant at the one consumption site)"
      pattern: "self\\.speed_a_per_s \\* dt"
    - from: "serpentrum/game_engine.py __init__/reset"
      to: "SPEED_A_PER_S module constant"
      via: "kwarg DEFAULT (constant stays 3.0 as the test-pinned v1 value; never mutated at runtime)"
      pattern: "speed_a_per_s=SPEED_A_PER_S"
---

<objective>
Give GameEngine a per-instance speed so a setup-selected tier can drive movement while the 3.0 A/s baseline stays the untouched default.

Purpose: the engine half of GAME-11 — the ONLY engine edit in Phase 5.1. Everything else (tier table, Setup UI, GameTab wiring) feeds this one kwarg. Keeping it a defaulted trailing kwarg preserves all ~104 existing GameEngine call sites (02-10 precedent: extend the constructor with keyword args with safe defaults).

Output: `speed_a_per_s` kwarg on `__init__`/`reset` consumed by `step()`'s movement branch, plus a new per-tier test suite. TDD: RED -> GREEN -> (REFACTOR), 2-3 atomic commits.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-game-speed-difficulty/5.1-RESEARCH-engine-speed.md
@.planning/ROADMAP.md
@.planning/STATE.md

The research file is the AUTHORITY (speed plumbing map + calibrated-constant safety audit + recommended knob (a) = per-instance step speed at the fixed 100 ms cadence).

Locked constraints (do NOT revisit):
- **GAME-08**: speed constant within a run — ONE instance attribute, set once at construction/reset, never mutated mid-run.
- Module constant `SPEED_A_PER_S = 3.0` STAYS (test-pinned by `tests/test_engine_core.py` `test_speed_constant_pinned`); it becomes the kwarg DEFAULT, not a replacement lookup. NEVER mutate the module constant at runtime (Pitfall-7 anchor discipline).
- The kwarg is TRAILING (after `atom_budget`) on BOTH `__init__` and `reset`; the ctor docstring already instructs callers to pass by name. Movement/collision test suites derive expectations from the constant (`test_engine_rules.py`, `test_engine_tail_follow.py`, `test_engine_win_desync.py`) — they must stay green UNMODIFIED.
- The 100 ms timer cadence (`TICK_DT`/`TICK_INTERVAL_MS` in gui_game.py) is a deliberately SEPARATE knob — do NOT couple anything here to tick interval.
- Collision/capture constants are position-based and step-size-safe across 0.2-0.6 A/tick (research audit) — do NOT touch `BODY_COLLISION_RADIUS_A`, `PICKUP_RADIUS_A`, `BOUNDARY_MARGIN_A`, `TURN_TICKS`, `EXHAUST_COOLDOWN_TICKS`, or anything in spawn.py.
- python3.6 syntax only, %-formatting, PURE module (stdlib only — no pymol/Qt/numpy).
- Reference SYMBOLS, never line numbers (Phase 5.2 may land first and shift lines).
</context>

<feature>
  <name>GameEngine speed_a_per_s trailing kwarg</name>
  <files>serpentrum/game_engine.py, tests/test_engine_speed.py (new), tests/test_engine_core.py (module-docstring sentence only)</files>
  <behavior>
    RED first — create `tests/test_engine_speed.py` following house conventions (sys.path self-insert header like every tests/test_*.py; NO `__init__.py`; plain unittest; `DELTA = 1e-9` float style mirroring test_engine_core.py). Cases (input -> expected):

    1. `GameEngine(head=(0,0), heading='right')` with NO speed kwarg, `step(0.1)` -> head == `(0.3, 0.0)` (baseline default unchanged).
    2. `GameEngine(..., speed_a_per_s=2.0)`, `step(0.1)` -> head == `(0.2, 0.0)`; `speed_a_per_s=6.0` -> `(0.6, 0.0)`; heading `'up'` with 6.0 -> `(0.0, 0.6)` (movement follows the heading unit vector, never hardcoded +x).
    3. Train-follow parity: engine with one test segment record (build fresh per call, mirroring test_engine_core.py's make_segment shape: dict with 'molecule_id', 'centroid', 'atoms', 'atoms_n') + `speed_a_per_s=6.0`, `step(0.1)` -> the segment's centroid translated by the SAME `(0.6, 0.0)` delta.
    4. `reset(..., speed_a_per_s=4.5)` re-seeds the instance speed; a later `reset()` WITHOUT the kwarg restores the 3.0 default.
    5. `GameEngine(speed_a_per_s=0)` and `speed_a_per_s=-1.0` -> ValueError (loud contract, mirroring the unknown-heading ValueError).
    6. Determinism: two engines built with identical args including `speed_a_per_s=4.5`, stepped through an identical sequence including a queued turn -> identical head positions and event sequences (mirror test_engine_core.py's two-engine determinism pattern).
    7. Existing pin untouched: `game_engine.SPEED_A_PER_S == 3.0` still passes (lives in test_engine_core.py — confirm green; do NOT edit that assertion).
    8. Seed-coupling pin (documented ACCEPTED side effect — do NOT "fix"): `spawn.seed_from_setup({'speed': 2.0})` != `spawn.seed_from_setup({'speed': 4.5})`; `seed_from_setup(d)` == `seed_from_setup(dict(d))` (same-tier restart reproduces the pickup sequence; a tier switch deterministically re-seeds — same semantics as changing the box size). Import `serpentrum.spawn` in this test file and document the acceptance in the test docstring.

    Run the new file: every new engine test MUST FAIL (the kwarg does not exist yet). The constant pin and the seed-coupling half may pass already — that is fine; they are pins, not RED drivers.

    Commit: `test(5.1-01): add failing tests for engine speed_a_per_s kwarg`
  </behavior>
  <implementation>
    GREEN — minimal code to make the RED tests pass:
    - Add trailing kwarg `speed_a_per_s=SPEED_A_PER_S` to BOTH `__init__` and `reset` (`__init__` forwards it to reset). Validate in reset (single validation point): `speed_a_per_s <= 0` -> `ValueError('speed_a_per_s %r must be > 0' % (speed_a_per_s,))`. Store `self.speed_a_per_s`.
    - In `step()`'s movement branch, replace the module constant with `self.speed_a_per_s` at the head-advance arithmetic (the ONLY consumption change — `_translate_chain` already receives the same delta).
    - Doc comments in the same commit: the module-constant comment becomes "v1 default tier + test pin; the per-run tier is injected via the speed_a_per_s kwarg (Phase 5.1 GAME-11)"; the `step()` docstring's "At SPEED_A_PER_S = 3.0 ..." example generalizes to "at speed_a_per_s = 3.0 ... movement is speed_a_per_s * dt per tick".
    - `tests/test_engine_core.py`: update ONLY the module-docstring sentence that presents 0.3 A/tick as THE engine parameter (now: the default tier of the Phase-5.1 selector). ZERO assertion changes in that file.

    Run: new tests PASS; then the FULL suite via the gates (below) — existing suites stay green unmodified.

    REFACTOR only if an obvious cleanup appears; tests must stay green. Commit: `feat(5.1-01): per-instance engine speed (default 3.0 preserved)` [+ `refactor(5.1-01): ...` only if you actually refactored].
  </implementation>
</feature>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_engine_speed.py" -v` -> all pass
- `python3.6 tests/run_gates.py` -> syntax walk + plugin-path safety + AST purity + FULL unittest suite green (existing speed-derived expectations in test_engine_rules / test_engine_tail_follow / test_engine_win_desync untouched and green)
</verification>

<success_criteria>
- `speed_a_per_s` exists as a trailing kwarg on `__init__`/`reset`, defaults to `SPEED_A_PER_S` (3.0), is the sole movement multiplier in `step()`, and rejects <= 0 loudly.
- All pre-existing tests pass unmodified (except the one test_engine_core.py docstring sentence); the new per-tier, determinism, and seed-coupling pins pass.
- The 100 ms cadence and all collision/capture/cooldown constants are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-game-speed-difficulty/5.1-01-SUMMARY.md`
</output>
