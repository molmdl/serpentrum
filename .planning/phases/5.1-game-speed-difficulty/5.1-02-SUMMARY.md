---
phase: 5.1-game-speed-difficulty
plan: 02
subsystem: schema
tags: [setup-logic, pure-module, json-schema, backcompat, speed-tiers, tdd]

# Dependency graph
requires:
  - phase: 02-core
    provides: setup_logic schema (numeric 'speed' key, DEFAULTS, validate, save/load round-trip)
provides:
  - SPEED_TIERS ordered (name, A/s) tier table — single source for the GUI combo, HUD note, tier resolution
  - merge_defaults(loaded) absent-key backcompat helper (also the seam Phase 5.2 reuses for its consent key)
  - speed_tier_for(speed) exact-match tier-name resolution with 'custom' fallback
affects: [5.1-03 hud speed note, 5.1-04 setup-tab speed combo, 5.1-05 game-tab surfacing, 5.1-06 feel-check, 5.2 upload consent (reuses merge_defaults)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single ordered data table for tier names->values (tuple-of-tuples, py3.6 insertion-ordered; value tweaks = one-table edits)"
    - "Absent-key backcompat via explicit defaults-overlay helper applied by CALLERS (load path keeps its no-merge contract)"
    - "Exact-match resolution with honest 'custom' fallback (no nearest-match guessing)"

key-files:
  created: []
  modified:
    - serpentrum/setup_logic.py
    - tests/test_setup_logic.py

key-decisions:
  - "Numeric 'speed' key STAYS at schema level (no speed_tier string key, no SCHEMA_VERSION bump) — old files load at default tier for free"
  - "validate() NOT tightened to tier membership — custom speeds > 0 stay legal (accept-as-is); the combo is the UI constraint, not the schema"
  - "SPEED_TIERS is a tuple-of-tuples (insertion order drives GUI combo order); draft values owner-finalized at the 5.1-06 feel-check"
  - "Tier values 2.0/3.0/4.5/6.0 pinned EXACTLY in tests — the pin is the visible reviewable diff for feel-check tweaks"

patterns-established:
  - "merge_defaults: dict(DEFAULTS) overlay, loaded keys win, unknown extra keys preserved, no mutation of input or DEFAULTS"

# Metrics
duration: 12min
completed: 2026-09-20
---

# Phase 5.1 Plan 02: Speed Tier Table + Defaults-Merge Backcompat Summary

**Pure schema layer gains SPEED_TIERS ordered tier table (relaxed 2.0 / normal 3.0 / fast 4.5 / expert 6.0), merge_defaults absent-key backcompat helper, and speed_tier_for exact-match tier resolution — zero schema churn (numeric 'speed' key, validate '> 0' rule, load_setup no-merge contract, SCHEMA_VERSION 1 all unchanged).**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-09-20T18:10:57Z
- **Completed:** 2026-09-20T18:23Z
- **Tasks:** 2 (TDD RED + GREEN; no REFACTOR needed)
- **Files modified:** 2

## Accomplishments

- `SPEED_TIERS` single ordered data table placed near `BOX_PRESETS` — the ONE source for the setup-tab combo (5.1-04), the HUD speed note (5.1-03/05), and tier-name resolution; draft values flagged for owner finalization at the 5.1-06 feel-check.
- `merge_defaults(loaded)` backcompat seam: `dict(DEFAULTS)` overlay — absent keys fall back to documented defaults (a dict missing `speed` merges to 3.0 and validates clean); Phase 5.2 reuses this exact seam for its "absent consent key = OFF" requirement.
- `speed_tier_for(speed)` exact-match resolution (2.0→relaxed … 6.0→expert) with an honest `'custom'` fallback (3.1, None, 'fast' all custom) — feeds the HUD note; deliberately NOT used to tighten validate().
- Accept-as-is and non-default round-trip pinned: `validate(speed=50.0)` reports no speed error; `speed=4.5` save/load round-trips byte-identically.
- Gate suite green at 653 unittest (640 baseline + 13 new); all four gates PASS (syntax + plugin-path, AST purity, unittest).

## Task Commits

Each task was committed atomically (TDD RED → GREEN):

1. **RED: failing tests for SPEED_TIERS + merge_defaults + speed_tier_for** - `e1ad3b4` (test)
   - New symbols failed on import (RED confirmed); pins 6-8 passed already as decision-protecting pins.
2. **GREEN: speed tier table + defaults-merge backcompat in setup_logic** - `a8da68f` (feat)
   - All 61 file-suite tests pass; full gate suite green at 653.

**Plan metadata:** (SUMMARY commit follows)

## Files Created/Modified

- `serpentrum/setup_logic.py` — added `SPEED_TIERS` (tuple-of-tuples with draft/finalize comment), `merge_defaults` (docstring: caller-applied seam, load_setup keeps no-merge contract), `speed_tier_for` (docstring: HUD note feed, not a validate() tightener). `validate()`, `save_setup`, `load_setup`, `DEFAULTS`, `SCHEMA_VERSION` untouched.
- `tests/test_setup_logic.py` — added `TestSpeedTierTable` (exact ordered tuple pin + default-is-a-tier), `TestMergeDefaults` (6-case behavior matrix: empty/partial/absent-speed/no-input-mutation/no-DEFAULTS-mutation/extra-keys), `TestSpeedTierFor` (exact-match + custom fallback), `TestCustomSpeedAccepted` (accept-as-is pin), and `test_round_trip_identical_non_default_speed`. `EXPECTED_DEFAULTS` unchanged.

## Decisions Made

None beyond the plan — all locked constraints honored exactly:

- Numeric `speed` key stays (no `speed_tier` string key, no SCHEMA_VERSION bump).
- `validate()` NOT tightened to tier membership (custom speeds stay legal).
- `load_setup` keeps its no-merge contract (pinned `test_load_does_not_full_validate` untouched and green).
- `EXPECTED_DEFAULTS` unchanged.
- `def merge_defaults` did not exist (Phase 5.2 not yet executed) — added per spec; 5.2 will reuse it.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `SPEED_TIERS` ready for 5.1-04 (setup-tab combo) and `speed_tier_for` for 5.1-03/05 (HUD speed note).
- `merge_defaults` ready for Phase 5.2's consent-key backcompat seam.
- Tier values are draft — owner finalizes at the 5.1-06 feel-check checkpoint; the exact tuple pin in `TestSpeedTierTable` is the reviewable diff for that tweak.
- No blockers.

---
*Phase: 5.1-game-speed-difficulty*
*Completed: 2026-09-20*
