---
phase: 5.1-game-speed-difficulty
plan: 01
subsystem: game-engine
tags: [game-engine, speed, difficulty, GAME-08, GAME-11, tdd, unittest, python3.6]

# Dependency graph
requires:
  - phase: 02-06-movement-core
    provides: GameEngine step() movement branch, SPEED_A_PER_S constant, reset() contract
  - phase: 02-13-rigid-sweep
    provides: queued-turn sweep mechanics used by the determinism test
provides:
  - trailing kwarg speed_a_per_s=SPEED_A_PER_S on GameEngine.__init__/reset (single validation point in reset; <= 0 -> ValueError)
  - self.speed_a_per_s consumed by step()'s movement branch (sole speed multiplier; default 3.0 preserved)
  - tests/test_engine_speed.py per-tier/default-preservation/determinism/seed-coupling suite (15 tests)
affects:
  - 5.1-02 (setup_logic tier table — feeds this kwarg)
  - 5.1-03 (GameTab/_build_engine wiring — injects setup['speed'] into speed_a_per_s)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-instance engine tunable as trailing defaulted kwarg (02-10 precedent), module constant kept as test-pinned DEFAULT"
    - "GAME-08 by construction: one instance attribute set once at construction/reset, never mutated mid-run"

key-files:
  created:
    - tests/test_engine_speed.py
  modified:
    - serpentrum/game_engine.py
    - tests/test_engine_core.py

key-decisions:
  - "reset() is the single validation point (speed_a_per_s <= 0 -> ValueError); __init__ forwards — mirrors the unknown-heading loud contract"
  - "Module constant SPEED_A_PER_S stays 3.0 as the kwarg DEFAULT (test-pinned); never mutated at runtime"
  - "Spawn seed coupling (crc32 over setup dict re-seeds per tier) pinned as an ACCEPTED side effect, not a bug"

patterns-established:
  - "Speed knob (a) from research: per-instance Å/s at fixed 100 ms/0.1 s cadence — tick interval never coupled to difficulty"
  - "New per-tier suites construct engines with the kwarg; pre-existing suites keep the default untouched"

# Metrics
duration: 8 min
completed: 2026-09-20
---

# Phase 5.1 Plan 01: Engine speed_a_per_s kwarg Summary

**Per-instance GameEngine forward speed via trailing defaulted kwarg (speed_a_per_s, default SPEED_A_PER_S = 3.0) consumed by step()'s movement branch — GAME-08 stays true by construction and all ~104 pre-existing call sites stay green untouched.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-09-20T18:10:28Z
- **Completed:** 2026-09-20T18:17:48Z
- **Tasks:** 2 (TDD RED, TDD GREEN; no REFACTOR needed)
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- `speed_a_per_s=SPEED_A_PER_S` trailing kwarg on `__init__`/`reset`; reset validates `<= 0` -> `ValueError('speed_a_per_s %r must be > 0')` (single validation point; __init__ forwards)
- `step()` movement branch advances head by `self.speed_a_per_s * dt` at the ONE consumption site; `_translate_chain` already rides the same delta, so train-follow parity is automatic
- New `tests/test_engine_speed.py`: 8 case groups / 15 tests — default preservation, per-tier displacement (2.0/6.0), heading-direction parity, train-follow segment parity, reset re-seed + default restore, loud `<= 0` rejection, two-engine determinism with a queued turn, constant pin, and the spawn seed-coupling pin (documented accepted side effect)
- Gates green: 655 unittests (640 pre-existing unmodified + 15 new); syntax/plugin-path/purity gates all PASS

## Task Commits

Each task was committed atomically:

1. **RED: failing speed_a_per_s test suite** — `30b8ad9` (test)
2. **GREEN: per-instance engine speed (default 3.0 preserved)** — `b4e85fb` (feat)

_No REFACTOR commit — no obvious cleanup appeared; docs landed in the GREEN commit as planned._

## Files Created/Modified

- `tests/test_engine_speed.py` (created, 215 lines) — the Phase-5.1 engine speed suite
- `serpentrum/game_engine.py` — trailing kwarg on `__init__`/`reset`, validation in reset, `self.speed_a_per_s` stored + consumed in `step()`; doc updates (module-constant comment, `step()` docstring example, class state doc entry)
- `tests/test_engine_core.py` — one module-docstring sentence reframing 0.3 Å/tick as the DEFAULT tier of the Phase-5.1 selector (zero assertion changes)

## Decisions Made

- Validation lives in `reset()` alone (single point; `__init__` forwards) so construction and reset share one loud contract — mirrors the unknown-heading ValueError precedent.
- `SPEED_A_PER_S` untouched as a module constant: it is now the kwarg DEFAULT and the existing pin in `test_engine_core.py` still guards the v1 baseline value.
- The seed-coupling behavior (`spawn.seed_from_setup` re-seeds when speed changes) is pinned in a test as an ACCEPTED side effect with a docstring instructing future maintainers not to "fix" it.

## Deviations from Plan

None — plan executed exactly as written. (One small documentation addition: a `speed_a_per_s` entry in the GameEngine class state docstring, keeping the state inventory accurate — inside the GREEN commit's planned doc-update scope, no behavior change.)

## Issues Encountered

None. RED failed exactly as expected (11 kwarg-dependent tests errored on the missing kwarg; the pin/seed-coupling tests passed as pins); GREEN passed immediately and the full gate suite stayed green.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Ready for 5.1-02 (setup_logic tier table) and 5.1-03 (GameTab `_build_engine` wiring): the engine knob exists and is tested; wiring just passes `speed_a_per_s=setup.get('speed', 3.0)`.
- No blockers. GAME-08 guaranteed by construction (no mid-run mutation path).

---
*Phase: 5.1-game-speed-difficulty*
*Completed: 2026-09-20*
