---
phase: 03-molecules-in-the-viewer-setup-tab
plan: 07
type: execute
wave: 4
depends_on: ["03-01", "03-06"]
files_modified:
  - serpentrum/gui_setup.py
  - serpentrum/gui.py
  - serpentrum/__init__.py
autonomous: true

must_haves:
  truths:
    - "The Setup tab is a live form, not a placeholder: demo-set dropdown (Set A + Upload…), box-preset dropdown, head-molecule dropdown (default Random), xtb auto-detect checkbox + path field + Browse, win-cap spinbox (1..20) with the inline hessian warning label, persistent status label — per the researched widget inventory [SETUP-02..06]"
    - "Apply runs setup_logic.validate first (errors -> QMessageBox.warning + status label, never .exec_()); on success routes demo vs upload by the combo's currentData(): setloader.load_demo_set / load_upload -> rejection errors surface in the 'Upload rejected' modal; accepted records go to pymol_bridge.materialize(setup, records) and the viewer shows box + head [SC1, SC2]"
    - "Win cap > 10 shows setup_logic.HESSIAN_WARNING inline (non-modal) immediately on valueChanged and on apply_state; <= 10 hides it [SETUP-06 / SC4]"
    - "xtb: auto-detect checked -> field disabled + setup['xtb_path']=None; unchecked -> field enabled with Browse; Apply shows the xtbenv.detect_binary result (resolved path or the not-found advisory) in the status label [SETUP-05 / SC3]"
    - "The live setup dict anchors on pmg_tk.startup._serpentrum.setup (added to _SerpentrumState) so it survives dialog close/reopen AND Plugin-Manager reload; the registration contract (self.tabs handle, Setup stays page 0, exactly one addTab per page, reserved bottom row untouched) is preserved"
  artifacts:
    - path: "serpentrum/gui_setup.py"
      provides: "SetupTab(QWidget): 5 QGroupBox sections + status label + temporary Apply/Cleanup buttons; collect_state/apply_state with _loading guard; calls setloader (pure) + pymol_bridge (cmd-seam); ZERO cmd imports, ZERO .exec_ calls"
      min_lines: 220
      contains: "SetupTab"
    - path: "serpentrum/gui.py"
      provides: "PluginDialog(anchor_state=None) constructing SetupTab as page 0 (placeholders remain for Game/Spectra); self.tabs contract + reserved bottom row unchanged; find_existing unchanged"
      contains: "SetupTab"
    - path: "serpentrum/__init__.py"
      provides: "_SerpentrumState.setup field initialized to setup_logic.new_setup() on first anchor creation (lazy relative import inside _anchor — ENTRY purity preserved); run_plugin_gui passes the state into PluginDialog"
      contains: "setup"
  key_links:
    - from: "serpentrum/gui_setup.py _on_apply"
      to: "serpentrum/setloader.py load_demo_set/load_upload"
      via: "record building (pure, testable) before any cmd call; rejection errors -> QMessageBox.warning lines"
      pattern: "load_demo_set|load_upload"
    - from: "serpentrum/gui_setup.py _on_apply"
      to: "serpentrum/pymol_bridge.py materialize/cleanup_srp"
      via: "the ONLY path from the form to cmd.* (GUI class bans direct pymol imports)"
      pattern: "materialize|cleanup_srp"
    - from: "serpentrum/gui.py"
      to: "pmg_tk.startup._serpentrum (anchor)"
      via: "anchor_state passed from run_plugin_gui -> PluginDialog -> SetupTab; setup dict write-back on Apply"
      pattern: "anchor_state|_serpentrum"
---

<objective>
Build the Setup tab: `serpentrum/gui_setup.py` (SetupTab QWidget — the full configuration form per the researched inventory), wire it into `gui.py` as page 0, and anchor the live setup dict on the `_serpentrum` state object in `__init__.py`.

Purpose: SETUP-02..06 are UI requirements — the pure halves (setup_logic schema/validation, xtbenv detection) and the viewer half (pymol_bridge) already exist; this plan is the wiring that makes SC1-SC4 user-visible. Error surfacing follows the researched split: one-shot blocking errors -> static QMessageBox.warning (no .exec_() token; pre-game modals are permitted, Pitfall 5); the hessian-cost advisory -> inline QLabel. The two TEMPORARY Setup-page buttons (Apply/Show + Cleanup) are the Phase-3 control split — the canonical 6-button bottom row stays Phase 8 (SETUP-07) and the reserved bottom QHBoxLayout in gui.py stays untouched.

Output: serpentrum/gui_setup.py, rewired serpentrum/gui.py, anchored serpentrum/__init__.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-setup-ui.md
@serpentrum/gui.py
@serpentrum/__init__.py
@serpentrum/setup_logic.py
@serpentrum/xtbenv.py
@serpentrum/setloader.py
@serpentrum/pymol_bridge.py

Load-bearing verified facts (03-RESEARCH-setup-ui.md; trust over memory):
- Qt imports ONLY via `from pymol.Qt import QtWidgets, QtCore` (gui_setup is GUI class after 03-01; bare pymol/pmg_tk banned at any level; PyQt5/numpy banned; .exec_() banned — the static convenience methods QMessageBox.warning(...) and QFileDialog.getOpenFileName(self, caption, dir, filter) carry NO .exec_() token and are the sanctioned forms). QFileDialog returns a (path, filter) TUPLE.
- Thread boundary: handlers run on the Qt main thread = the PyMOL gui thread; call pymol_bridge/setloader directly from handlers. NO worker/queue/QTimer for Phase 3 (sub-second ops); NEVER a thread calling cmd.* (no threads at all here).
- Widget inventory (research §2.1) + layout (§2.2): QVBoxLayout of QGroupBox/QFormLayout sections — 'Molecule set' (demo combo + upload row), 'Box' (preset combo), 'Head molecule' (combo, default Random), 'xtb' (auto-detect checkbox; path QLineEdit + Browse), 'Win cap' (spinbox 1..20 + hidden warning QLabel); then status QLabel (word-wrapped), stretch, QHBoxLayout [Apply/Show in Viewer] [Cleanup] — TEMPORARY buttons INSIDE the page, NOT the reserved bottom row (gui.py:56-57 stays untouched).
- Combo idioms: addItem(label, userData=...); read currentData(). Demo combo: one entry per setup_logic.KNOWN_SETS (label 'Demo Set A', userData 'set_a') + 'Upload…' (userData '__upload__'). Selecting '__upload__' reveals the upload row (Browse QPushButton + read-only QLineEdit); selecting a real set hides it.
- collect_state()/apply_state(state) round-trip is the established pattern (bioCHEMeleon gui_setup.py:539-617); a self._loading flag guards apply_state against cascading recompute; __init__ calls apply_state(the anchored dict).
- HEAD-COMB + demo_set/'__upload__' routing decision (planning-level, binding): the setup dict's demo_set field only ever holds KNOWN_SETS values (setup_logic.validate rejects anything else). The combo's '__upload__' userData is UI routing truth ONLY: collect_state() maps it to the LAST selected real set (initialized 'set_a'); _on_apply branches on the combo's currentData() — '__upload__' -> setloader.load_upload(path from the upload field), else setloader.load_demo_set(currentData()). The head combo populates from loaded records: entries 'Random' (userData 'random') + one per record (label = record['name'], userData = record['id']); a successful load repopulates it, keeping the current selection when still present.
- xtb wiring (research §7.3): checkbox checked -> path field disabled, setup['xtb_path'] = None; unchecked -> enabled; Browse -> QFileDialog.getOpenFileName. After validate() passes, call xtbenv.detect_binary(configured_path=setup['xtb_path']) and append to the status label: 'xtb: <resolved path>' or 'xtb not found — set a manual path or add xtb to PATH' (advisory in Phase 3; blocking is Phase 6).
- Hessian warning (SETUP-06): spinbox.setRange(1, 20); on valueChanged AND on apply_state: value > 10 -> warning QLabel set to setup_logic.HESSIAN_WARNING and shown; else hidden. Inline, never a modal. The status label separately shows validate()'s (errors, warnings) verdict; refresh it on every field change (direct update — the researched debounce is unnecessary at Phase-3 widget density).
- Apply path: setup = collect_state(); errors, warnings = setup_logic.validate(setup); errors -> QMessageBox.warning(self, 'Cannot apply', '\n'.join(errors)) + status label + STOP. Then record building per routing above: load errors (rejections formatted '<name>: <reason>' by setloader) -> QMessageBox.warning(self, 'Upload rejected', '\n'.join(errors)) for uploads / 'Cannot load set' for demo + status + STOP. Then bridge.materialize(setup, records) -> returned errors -> 'Load failed' warning + status (materialize never raises for data problems). Success -> status 'Box + head materialized' + ('; head will be randomized at game start' when head_molecule == 'random') + xtb note + validate() warnings; write the dict back to the anchor.
- Cleanup path: n = pymol_bridge.cleanup_srp() -> status 'Cleanup removed <n> srp_* object(s)'.
- Anchor persistence (research §8): _SerpentrumState gains a `setup` class attr (None); _anchor() initializes state.setup = new_setup() on FIRST creation only (inside the existing hasattr branch; `from . import setup_logic` lazily inside the function — relative import, ENTRY-legal). SetupTab reads/writes it via the anchor object; NEVER module globals (Pitfall 7). run_plugin_gui passes state into PluginDialog(anchor_state=state).
- gui.py contract: keep self.tabs (QTabWidget), page order Setup->Game->Spectra, EXACTLY one addTab(page, label) per page, the reserved bottom QHBoxLayout with its Phase-8 comment, and find_existing() UNCHANGED. Replace only the Setup placeholder page: page 0 = SetupTab(anchor_state, self.tabs); pages 1-2 keep the placeholder QLabel loop for Game/Spectra (update _TAB_DEFS accordingly — Setup's placeholder text is retired; keep Game/Spectra hint texts). PluginDialog.__init__ gains keyword arg anchor_state=None (default keeps any existing construction path working).
- Verification reality: GUI modules cannot be imported under WSL python3.6 (pymol.Qt) — zero WSL unit tests are POSSIBLE for gui_setup/gui (same constraint Phase 1's 01-02 accepted). Verification = gates (py3.6 compile, purity, full suite) + grep contract checks; behavior verdicts belong to the phase's human-verify (03-08). Offscreen Qt is a DEAD END (STATE.md decision 01-05) — do not attempt it.
- Hard conventions: python3.6 (%-formatting — NO f-string =, no dataclasses); QtCore.Signal not needed (no custom signals in Phase 3); every user-facing string simple and explanatory (AGENTS.md UI standard); docstrings stay free of banned literals ('exec_(', 'from PyQt5', 'import PyQt5' as literal text — 01-02 lesson).
</context>

<tasks>

<task type="auto">
  <name>Task 1: serpentrum/gui_setup.py — SetupTab form + Apply/Cleanup handlers</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
Create serpentrum/gui_setup.py per <context>:
- Module docstring: role, the GUI purity class note (pymol.Qt only; cmd calls go through pymol_bridge), the temp-button Phase-3/Phase-8 split, the anchor persistence design.
- Imports: `from pymol.Qt import QtWidgets, QtCore` (module level, GUI-legal); relative: `from . import setup_logic, setloader, xtbenv, pymol_bridge`.
- class SetupTab(QtWidgets.QWidget): __init__(self, anchor_state=None, parent=None): self._anchor = anchor_state; self._loading = False; self._setup = (anchor_state.setup if anchor_state is not None and getattr(anchor_state, 'setup', None) is not None else setup_logic.new_setup()); build the layout per <context> (5 QGroupBox/QFormLayout sections + status QLabel + stretch + temp-button QHBoxLayout); wire signals (demo combo currentIndexChanged -> _on_source_changed; win cap valueChanged -> _on_cap_changed; every editable widget -> _refresh_status); call apply_state(self._setup).
- collect_state(): read widgets into a copy of self._setup — demo combo '__upload__' maps to the last real set (track self._last_real_set, initialized from the dict, default 'set_a'); demo combo real value -> demo_set + self._last_real_set; box combo -> box_preset; head combo -> head_molecule (userData); xtb checkbox unchecked -> xtb_path = field text stripped (empty string stays as-is — validate() reports it), checked -> None; win cap -> win_cap_molecules; assign to self._setup; if self._anchor is not None: self._anchor.setup = self._setup; return self._setup.
- apply_state(setup): set self._loading True; populate every widget from the dict (demo combo: match userData, fallback first real set; head combo: 'random' + any ids already known; xtb checkbox from xtb_path is None; path field from xtb_path or ''); _on_cap_changed(); self._loading False.
- _on_source_changed(): reveal/hide the upload row (setVisible) per currentData() == '__upload__'; _refresh_status().
- _on_cap_changed(): show the hessian QLabel with setup_logic.HESSIAN_WARNING when the spinbox value > 10, else hide (guard with self._loading).
- _refresh_status(): if self._loading: return; errors, warnings = setup_logic.validate(collect_state()); status text = 'errors: ' + '; '.join(errors) when errors, else ('warning: ' + '; '.join(warnings) when warnings, else 'ready').
- _on_apply(): full flow per <context> (validate -> record building via setloader with the __upload__ routing -> head-combo repopulation keeping the current selection -> pymol_bridge.materialize -> status + xtb detect note + anchor write-back). All dialogs are static QMessageBox.warning(self, title, body).
- _on_cleanup(): n = pymol_bridge.cleanup_srp(); status 'Cleanup removed %d srp_* object(s)' % n.
- NO .exec_ anywhere; NO `from pymol import`; no threads; %-formatting.
  </action>
  <verify>`python3.6 -m py_compile serpentrum/gui_setup.py` -> OK; `python3.6 tools/check_purity.py` -> clean (gui_setup classified GUI: pymol.Qt allowed, no bare pymol); `grep -n "exec_\|from pymol import\|import numpy\|from PyQt5" serpentrum/gui_setup.py` -> no matches; `python3.6 tests/run_gates.py` -> exit 0</verify>
  <done>SetupTab complete: full widget inventory, validate-driven status, static-modal error surfacing, inline hessian warning, bridge-only cmd access. Commit: `feat(03-07): SetupTab form with Apply/Cleanup wired to setloader + pymol_bridge`.</done>
</task>

<task type="auto">
  <name>Task 2: gui.py Setup-page registration + __init__.py anchor setup field</name>
  <files>serpentrum/gui.py, serpentrum/__init__.py</files>
  <action>
1. serpentrum/gui.py:
   - Add `from .gui_setup import SetupTab` (relative import — exempt; the file's only pymol.Qt import form stays).
   - PluginDialog.__init__(self, parent=None, anchor_state=None): construct page 0 as `setup_page = SetupTab(anchor_state, self.tabs)` and `self.tabs.addTab(setup_page, 'Setup')` — still EXACTLY one addTab for Setup, page 0. Keep the Game/Spectra placeholder loop (restructure _TAB_DEFS to only Game/Spectra entries, or keep the list and skip the setup entry — keep the structure minimal and the docstring contract current: Setup is now the live SetupTab; Game/Spectra placeholders remain). The reserved bottom QHBoxLayout + comment stay byte-identical. find_existing() untouched. Update the module/class docstrings (Setup placeholder text retired).
2. serpentrum/__init__.py:
   - _SerpentrumState class body: add `setup = None` beside dialog/controller.
   - In _anchor()'s creation branch, after instantiating the state: lazily `from . import setup_logic` (inside the function — ENTRY-legal relative import) and `state.setup = setup_logic.new_setup()`. Only on FIRST creation (the hasattr branch) — reload/double-import must NOT reset the user's dict.
   - run_plugin_gui(): pass the anchor into the dialog — `state.dialog = existing if existing is not None else PluginDialog(anchor_state=state)` (the existing-adoption branch stays; an adopted orphan keeps its own SetupTab).
   - Update the module docstring ONLY if it mentions tab contents (keep module level stdlib-only: the setup_logic import is INSIDE _anchor).
3. Gate + suite verification: python3.6 gates green (test_skeleton.py's zero-stub import of serpentrum must still pass — module level untouched; purity ENTRY rules still hold).
  </action>
  <verify>`python3.6 tests/run_gates.py` -> exit 0; `python3.6 -m unittest discover -s tests -p "test_skeleton.py" -v` -> OK (import + anchor contract intact); `python3.6 tools/check_purity.py` -> clean; `grep -n "addTab" serpentrum/gui.py` -> exactly 3 call sites (Setup + placeholder loop); `grep -n "state.setup = setup_logic.new_setup()" serpentrum/__init__.py` -> inside _anchor creation branch only</verify>
  <done>Setup page live in the dialog shell; setup dict anchored for reload survival; registration contract + reserved row preserved; ENTRY purity intact. Commit: `feat(03-07): register SetupTab as page 0 + anchor the live setup dict`.</done>
</task>

<task type="auto">
  <name>Task 3: contract grep sweep + full gate pass</name>
  <files>serpentrum/gui_setup.py, serpentrum/gui.py, serpentrum/__init__.py</files>
  <action>
1. Sweep the three files for the hard contracts (fix anything found):
   - `grep -n "exec_" serpentrum/gui_setup.py serpentrum/gui.py` -> zero matches.
   - `grep -nE "from (pymol|PyQt5) import|import numpy|import pmg_tk" serpentrum/gui_setup.py` -> zero matches; gui.py keeps ONLY its pymol.Qt import.
   - `grep -n "cmd\." serpentrum/gui_setup.py` -> zero matches (all cmd access via pymol_bridge).
   - Docstrings free of banned literals ('exec_(', 'from PyQt5', 'import PyQt5') — 01-02 lesson (the AST checker is docstring-proof but keep the discipline).
2. Run `python3.6 tests/run_gates.py` (syntax + safety + purity + full suite) -> exit 0.
3. Record in the task notes: the human-verify checklist items this plan hands to 03-08 (form completeness, Apply materialization, rejection modal, warning label, xtb status, cleanup count).
  </action>
  <verify>all greps clean; `python3.6 tests/run_gates.py` -> exit 0; `python3.6 tools/check_purity.py` -> clean</verify>
  <done>Contracts machine-checked; gates green; GUI work hand-off notes ready for 03-08. Commit: `chore(03-07): GUI contract sweep + gate pass`.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> exit 0 (py3.6 compile of all three files; purity classes correct: gui_setup+gui GUI, __init__ ENTRY, everything else PURE)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (no WSL test imports the Qt modules — none should exist)
- grep contracts (Task 3) all clean
- Setup stays page 0 with exactly one addTab; reserved bottom row untouched (`grep -n "Phase 8" serpentrum/gui.py` -> the reserved-row comment still present)
</verification>

<success_criteria>
- SETUP-02/03/04/05/06 each have their Setup-tab control live and wired to the pure layer (setloader/setup_logic/xtbenv) and the cmd-seam (pymol_bridge) — SC1-SC4 user-visible pending human-verify
- Error surfacing per research: static QMessageBox.warning for blocking errors (upload rejection lists '<name>: <reason>' lines), inline QLabel for the hessian advisory, persistent status label for validate() verdicts
- Live setup dict anchored on pmg_tk.startup._serpentrum (survives close/reopen + reload); registration contract and reserved Phase-8 row untouched
- No cmd/PyQt5/exec_/thread violations; gates green
</success_criteria>

<output>
After completion, create `.planning/phases/03-molecules-in-the-viewer-setup-tab/03-07-SUMMARY.md`
</output>
