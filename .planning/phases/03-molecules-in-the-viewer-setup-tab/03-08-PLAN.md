---
phase: 03-molecules-in-the-viewer-setup-tab
plan: 08
type: execute
wave: 5
depends_on: ["03-03", "03-05", "03-06", "03-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Every automated gate is green before the human looks at anything: default gates (syntax+plugin-path safety, purity, full unittest suite), the REQUIRED smokes (SMOKE-OK SKELETON + SMOKE-OK VIEWER-BRIDGE flushed sentinels), and the xtb probe"
    - "A human, in REAL Windows PyMOL, confirms the phase's user-visible contract end-to-end: Setup-tab form drives demo Set A loading (SC1), the boundary box + chosen head materialize and frame (SC2), xtb auto-detect/manual override status shows (SC3), the win-cap hessian warning appears when raised (SC4), uploads gate with a clear rejection reason (SC1/DATA-03), and Cleanup removes only srp_* objects — including in a FRESH PROCESS after a .pse session save/reload (SC5/INFRA-04)"
  artifacts:
    - path: ".planning/phases/03-molecules-in-the-viewer-setup-tab/03-08-SUMMARY.md"
      provides: "the phase-closing summary recording gate output, the human verdict, and any fixes made to reach green"
      contains: "03-08"
  key_links:
    - from: "this plan's gate run"
      to: "tests/run_gates.py (all flags)"
      via: "python3.6 tests/run_gates.py [--smoke] [--xtb] — exit 0 + flushed sentinels required before the checkpoint opens"
      pattern: "run_gates"
    - from: "the checkpoint verdict"
      to: "ROADMAP Phase 3 SC1-SC5"
      via: "each numbered verify step maps to one success criterion; 'approved' closes the phase"
      pattern: "SC[1-5]"
---

<objective>
Close Phase 3: run every automated gate (fixing anything red), then put the finished Setup tab + viewer bridge in front of the human in REAL Windows PyMOL — the roadmap-mandated human-verify ("uploaded set loads; box renders") including the fresh-process cleanup-after-.pse-reload proof. Offscreen Qt is a dead end (STATE.md decision 01-05), so dialog/scene verdicts are human-only by design.

Purpose: SC1-SC5 are user-perspective claims; plans 03-01..03-07 proved the mechanisms (unit tests + headless smokes). This plan converts them into verified outcomes and hands the orchestrator the phase verdict.

Output: all gates green + one approved human-verify checkpoint + 03-08-SUMMARY.md. No code changes expected; if a gate fails, this plan fixes it (scope: whatever the failure requires, keeping purity/contract discipline).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-setup-ui.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-viewer-bridge.md
@tests/run_gates.py

Load-bearing facts:
- Gate verdicts: default gates by exit code; --smoke verdicts ONLY by flushed SMOKE-OK sentinels (SMOKE-OK SKELETON, SMOKE-OK VIEWER-BRIDGE) — never exit codes through the .bat (run_gates.py:140). --xtb asserts 'xtb version' AND 'normal termination' in the Windows-xtb probe output.
- The human-verify flow uses the REAL dialog (modeless, .show()) in real Windows PyMOL launched via setenv.bat / the usual desktop flow. The demo data must already be in place (03-05's checkpoint); if the human skipped it, the demo-leg steps below fail — the checkpoint instructions say what to do.
- Cleanup-after-.pse-reload (SC5) verifies the fresh-process contract: .pse keeps viewer objects but NO plugin state, so the reopened plugin must still remove exactly srp_* (pymol_bridge.cleanup_srp is name-based by construction).
- Anything the human reports broken comes back as concrete fix work INSIDE this plan (respecting all phase contracts: purity classes, no .exec_, srp_* naming, no user-molecule mutation), then the affected verify steps re-run. The phase is NOT done on a partial approval.
</context>

<tasks>

<task type="auto">
  <name>Task 1: full automated gate pass (fix anything red)</name>
  <files></files>
  <action>
Run, in order, from the repo root (WSL):
1. `python3.6 tests/run_gates.py` -> must exit 0 (gate 1 syntax + plugin-path safety, gate 2 purity, gate 3 full unittest suite).
2. `python3.6 tests/run_gates.py --smoke` -> gate 4 PASS with BOTH required smoke sentinels echoed: 'SMOKE-OK SKELETON' and 'SMOKE-OK VIEWER-BRIDGE'.
3. `python3.6 tests/run_gates.py --xtb` -> gate 5 PASS ('xtb version' + 'normal termination').
If ANY gate fails: diagnose, fix (smallest change that restores the contract — purity classes, py3.6 syntax, fixtures, smoke steps per the owning plans), re-run all three until green. Record every fix + its rationale in the task notes (these flow into the summary). Do NOT weaken a gate or a smoke assertion to get green; do NOT skip the demo-data dependency silently (if serpentrum/data lacks the 5 SDFs or manifest.json, report it and let the checkpoint step handle the human conversation).
  </action>
  <verify>`python3.6 tests/run_gates.py && python3.6 tests/run_gates.py --smoke && python3.6 tests/run_gates.py --xtb` -> all exit 0 with SMOKE-OK SKELETON + SMOKE-OK VIEWER-BRIDGE echoed in the --smoke output</verify>
  <done>Every automated gate green with sentinels recorded. Commit any fixes: `fix(03-08): <what the gate run surfaced>`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN verifies the Setup tab + viewer bridge in real Windows PyMOL</name>
  <files></files>
  <what-built>The complete Phase 3: Setup tab form (demo set / upload / box preset / head molecule / xtb path / win cap), upload gate (<=3 rings, explicit H), viewer bridge (srp_box CGO + srp_head molecule, one-shot framing), fresh-process-safe cleanup, anchored setup state.</what-built>
  <how-to-verify>
    Launch real Windows PyMOL (not headless). Install/load the plugin from the repo root as in Phase 1 (plugin path pointing at the repo). Then:
    1. Menu 'serpentrum' -> the 3-tab dialog opens modeless; the SETUP tab now shows the real form (group boxes, dropdowns, spinbox) — not the Phase-1 placeholder. [SETUP-02..06 present]
    2. Demo leg: leave demo set = 'Demo Set A', box preset 'medium', head 'Random' -> click 'Apply / Show in Viewer' -> a clearly visible orange boundary box (12 edges) AND a head molecule (spheres) appear, framed on screen. [SC2]
    3. Head pick: switch the head dropdown to 'Naphthalene' (or any listed molecule) -> Apply -> that molecule replaces srp_head. [SETUP-04]
    4. Upload leg — rejection: click 'Upload…', Browse to an SDF containing a >3-ring molecule (any 4-fused-ring PAH you have; or a skeletal no-H file) -> a 'Upload rejected' modal names the molecule with the reason (rings limit / hydrogens); nothing loads. [SC1, DATA-03]
    5. Upload leg — acceptance: Browse to a valid single- or multi-record SDF/mol2 (<=3 rings, explicit H; the shipped demo SDFs or your own) -> Apply -> its first accepted molecule materializes as the head; the head dropdown now lists the uploaded molecules. [SC1]
    6. Cleanup: load any molecule of your own as a normal PyMOL object (e.g. cmd.load or File->Open some PDB, object named anything NOT starting with srp_) -> click 'Cleanup' -> every srp_* object vanishes, YOUR object survives; the status label reports the removed count. [SC5, INFRA-04]
    7. Session survival: File -> Save Session to a .pse -> fully restart PyMOL -> open that session (srp_box/srp_head reappear, no plugin state) -> reopen the serpentrum dialog -> click 'Cleanup' -> the srp_* objects vanish again and your own object still survives. [SC5 fresh-process contract]
    8. xtb: leave 'auto-detect' checked -> Apply -> the status label shows the resolved xtb path, or the 'xtb not found — set a manual path or add xtb to PATH' note (both outcomes PASS this step; note which you saw). Uncheck it, Browse to your xtb.exe (e.g. C:\xtb-6.7.1\bin\xtb.exe), Apply -> no validation error. [SC3, SETUP-05]
    9. Win cap: raise the spinbox to 15 -> the hessian-cost warning label appears under it (no modal); lower it back to 10 -> it hides. [SC4, SETUP-06]
    10. Modeless sanity: with the dialog open, rotate/zoom the viewer and type in the PyMOL command line — everything stays responsive. [INFRA-05 held]
  </how-to-verify>
  <resume-signal>Type "approved" — or list the failing step numbers + what you saw (those become this plan's fix work, then the affected steps re-verify).</resume-signal>
</task>

</tasks>

<verification>
- All three gate runs exit 0; both required smoke sentinels flushed and recorded in the summary
- The human-verify checkpoint resolved with "approved" (or every reported issue fixed and re-approved)
- 03-08-SUMMARY.md records: gate outputs, the verify-step verdicts (1-10), any fixes made, and the SC1-SC5 mapping
</verification>

<success_criteria>
- Phase 3 closed: SC1-SC5 each human-confirmed (or machine-proven where the gate covers them), SETUP-02..06 + DATA-03 + INFRA-04 delivered
- The roadmap's human-verify note discharged: uploaded set loads; box renders; cleanup-after-.pse-reload proven in a fresh process
- No gate weakened, no contract violated by any fix made under this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-molecules-in-the-viewer-setup-tab/03-08-SUMMARY.md`
</output>
