---
phase: 03-molecules-in-the-viewer-setup-tab
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/setup_logic.py
  - tests/test_setup_logic.py
autonomous: true

must_haves:
  truths:
    - "setup_logic._xtb_path_problems(path) returns EXACTLY what xtbenv.validate_binary_path(path) returns for every input class (None/non-str/empty, missing, directory, quoted, valid) — one implementation, zero drift [SETUP-05 groundwork]"
    - "validate()'s xtb_path error messages are byte-identical to before the unification (the existing test_setup_logic matrix stays green UNMODIFIED)"
    - "The stale 'parallel wave-1 plan / keep in sync' docstring rationale is gone; the module docstring states the unification"
  artifacts:
    - path: "serpentrum/setup_logic.py"
      provides: "_xtb_path_problems delegating to serpentrum.xtbenv.validate_binary_path (intra-package relative import, purity-exempt); docstring updated"
      contains: "validate_binary_path"
    - path: "tests/test_setup_logic.py"
      provides: "new unification cross-check test class proving _xtb_path_problems == xtbenv.validate_binary_path across the full input matrix; pre-existing matrix untouched"
      min_lines: 260
  key_links:
    - from: "serpentrum/setup_logic.py _xtb_path_problems"
      to: "serpentrum/xtbenv.py validate_binary_path"
      via: "direct delegation (returns the call's result) — the 02-02-SUMMARY.md:109 unification note discharged"
      pattern: "validate_binary_path"
    - from: "tests/test_setup_logic.py"
      to: "serpentrum.xtbenv.validate_binary_path"
      via: "cross-check assertion over the shared input matrix (None, '', missing, dir, quoted, valid)"
      pattern: "validate_binary_path"
---

<objective>
Unify the two identical xtb-path validators: make `setup_logic._xtb_path_problems` delegate to `xtbenv.validate_binary_path` instead of carrying a private copy of the rules. This discharges the explicit Phase-2 handoff note (02-02-SUMMARY.md:109: "setup_logic.validate should call xtbenv.validate_binary_path for xtb_path problems") and eliminates the drift risk flagged in 03-RESEARCH-upload-gate.md §7.2/Pitfall E.

Purpose: SETUP-05's validation contract ("manual path that overrides detection") is checked by validate() on every Apply; if the two rule copies ever diverge, the Setup tab and the runtime detector disagree about what a valid path is. Phase 2 kept them separate only because xtbenv was a PARALLEL wave-1 plan that might not exist in the worktree — that constraint is gone (both modules shipped, Phase 2 verified).

Output: serpentrum/setup_logic.py (delegation + docstring), tests/test_setup_logic.py (cross-check test).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-upload-gate.md
@serpentrum/setup_logic.py
@serpentrum/xtbenv.py
@tests/test_setup_logic.py
@tests/test_xtbenv.py

Load-bearing facts:
- The two implementations are rule-identical TODAY (setup_logic.py:100-127 vs xtbenv.py:113-138): (a) empty/None/non-str -> 'xtb path is empty'; (b) missing -> "'<path>' does not exist"; (c) directory -> "'<path>' is not a file"; (d) quotes -> "path contains quote character(s): '<path>'". Messages must stay byte-identical after unification.
- setup_logic.py's docstring (lines 26-30 and _xtb_path_problems' own docstring) explains the local copy was a PARALLEL-worktree workaround — replace that rationale with the unification note.
- The import must be a RELATIVE intra-package import (`from . import xtbenv` or `from .xtbenv import validate_binary_path`) — relative imports are purity-exempt; both modules are PURE so a plain stdlib-side import is also legal, but relative keeps the package-internal dependency explicit.
- tests/test_setup_logic.py owns an error-string matrix for validate(); it is NOT edited except for the APPENDED cross-check class. It must stay green unmodified — that IS the byte-identity proof.
- The ownership split stays as documented (03-RESEARCH-upload-gate.md §7.2): validate() owns "is the setup dict valid?"; xtbenv.detect_binary owns "what exe do I actually run?". This plan unifies only the shared RULES, not the concerns.
- Hard conventions: python3.6, %-formatting; discovery `python3.6 -m unittest discover -s tests -p "test_*.py" -v`; zero stubs; setup_logic stays PURE.
</context>

<tasks>

<task type="auto">
  <name>Task 1: delegate _xtb_path_problems to xtbenv.validate_binary_path + docstring update</name>
  <files>serpentrum/setup_logic.py</files>
  <action>
1. Add `from .xtbenv import validate_binary_path` at module level (relative import, purity-exempt; xtbenv is PURE stdlib so this keeps setup_logic PURE).
2. Rewrite _xtb_path_problems to a thin delegator: keep the exact name and signature (validate() calls it; tests may reference it), body = `return validate_binary_path(path)`, docstring updated to state it delegates to xtbenv.validate_binary_path (single source of the rules; unified in Phase 3 per 02-02-SUMMARY.md:109) and that the message contract is unchanged.
3. Update the module docstring's stale paragraph (lines ~26-30: "xtbenv is a PARALLEL wave-1 plan... keep in sync"): replace with "the xtb-path rules live in serpentrum.xtbenv.validate_binary_path; _xtb_path_problems delegates to it (unified Phase 3)".
Do NOT touch validate(), save_setup, load_setup, randomize_head, DEFAULTS, BOX_PRESETS, or any other function. Do NOT touch serpentrum/xtbenv.py.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` -> the PRE-EXISTING matrix passes unmodified (byte-identical messages); `python3.6 tests/run_gates.py` -> all gates green</verify>
  <done>Single-source validator; messages unchanged; docstrings state the unification. Commit: `refactor(03-03): unify xtb path validation via xtbenv.validate_binary_path`.</done>
</task>

<task type="auto">
  <name>Task 2: cross-check test pinning the unification across the full input matrix</name>
  <files>tests/test_setup_logic.py</files>
  <action>
Append a new test class (do not modify existing classes) e.g. `class XtbPathUnificationTest(unittest.TestCase)`:
- Import serpentrum.xtbenv alongside setup_logic.
- Build the input matrix: None, '' (empty str), 5 (non-str), a path that does not exist (e.g. os.path.join(tempfile.gettempdir(), 'srp_no_such_xtb.exe')), a DIRECTORY (tempfile.mkdtemp via addCleanup), a path containing a quote ("C:\\x'tb.exe" — never created on disk), and a REAL existing file (tempfile-written dummy file via addCleanup).
- For each input: assertEqual(setup_logic._xtb_path_problems(p), xtbenv.validate_binary_path(p)) — exact list equality (order + content).
- One extra assertion: for the missing-path input, the returned list equals the historical literals ["'%s' does not exist" % p] (pins the message contract against accidental rewording).
Run the file scoped, then the full suite + gates.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` -> OK including the new class; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> exit 0</verify>
  <done>Unification pinned by tests: both validators agree on every input class; the historical message literals are locked. Full suite + gates green. Commit: `test(03-03): pin xtb validator unification cross-check`.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` -> OK (existing matrix unmodified + new unification class)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK
- `python3.6 tests/run_gates.py` -> exit 0 (setup_logic still PURE; relative import exempt)
- `grep -n "keep in sync" serpentrum/setup_logic.py` -> no matches (stale rationale removed)
- `grep -n "validate_binary_path" serpentrum/setup_logic.py` -> the delegation present
</verification>

<success_criteria>
- One implementation of the xtb-path rules (xtbenv.validate_binary_path); setup_logic delegates; drift risk eliminated [SETUP-05 groundwork]
- Message contract byte-identical (existing matrix green unmodified; new cross-check + literal pins)
- SETUP-05's Setup-tab plan (03-07) can rely on validate() and detect_binary agreeing by construction
</success_criteria>

<output>
After completion, create `.planning/phases/03-molecules-in-the-viewer-setup-tab/03-03-SUMMARY.md`
</output>
