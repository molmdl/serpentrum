---
phase: 03-molecules-in-the-viewer-setup-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_purity.py
  - tests/test_purity_gates.py
autonomous: true

must_haves:
  truths:
    - "A module listed in BRIDGE_MODULES (serpentrum/pymol_bridge.py) may import pymol/pmg_tk at module level AND inside function bodies with zero violations — the first purity class that permits `from pymol import cmd`"
    - "BRIDGE modules are still flagged for PyQt5/numpy imports at ANY level, and .exec_() calls stay banned in BRIDGE (the bridge builds no dialogs, never touches Qt/numpy)"
    - "serpentrum/gui_setup.py is classified GUI: pymol.Qt imports clean at any level; a bare `from pymol import cmd` in it is flagged at any level"
    - "The real repo still passes check_tree clean — listing not-yet-existing module paths (pymol_bridge.py, gui_setup.py) breaks nothing because check_tree walks only existing files"
  artifacts:
    - path: "tools/check_purity.py"
      provides: "BRIDGE_MODULES = {'serpentrum/pymol_bridge.py'} constant, classify() BRIDGE branch, _check_import_node BRIDGE rule, GUI_MODULES extended to {'serpentrum/gui.py', 'serpentrum/gui_setup.py'}, module docstring class table updated"
      contains: "BRIDGE_MODULES"
    - path: "tests/test_purity_gates.py"
      provides: "7 new fixture cases proving the BRIDGE + extended-GUI contracts (clean module-level/body pymol, banned PyQt5/numpy/exec_, gui_setup pymol.Qt clean, gui_setup bare-pymol flagged)"
      min_lines: 280
  key_links:
    - from: "tests/test_purity_gates.py"
      to: "tools/check_purity.py (classify/_check_import_node)"
      via: "synthetic serpentrum/-shaped fixture trees asserting exact violations per class"
      pattern: "check_tree|BRIDGE"
    - from: "tools/check_purity.py"
      to: "serpentrum/pymol_bridge.py (03-06) and serpentrum/gui_setup.py (03-07)"
      via: "allowlist entries that make the downstream modules legally importable under the gate — everything in waves 2-4 depends on this extension"
      pattern: "pymol_bridge|gui_setup"
---

<objective>
Extend the Phase-1 AST purity checker with the consolidated Phase-3 module-class decisions: a new BRIDGE allowlist class for the cmd-seam module `serpentrum/pymol_bridge.py` (allows pymol/pmg_tk at any level; bans PyQt5/numpy anywhere; keeps the .exec_() ban) and the deliberate extension of GUI_MODULES with `serpentrum/gui_setup.py`.

Purpose: Phase 3 is the FIRST phase that calls `cmd.*`, and the current checker has NO class that permits `from pymol import cmd` (ENTRY = lazy-only, GUI = pymol.Qt-only, everything else PURE). Both new Phase-3 modules (pymol_bridge.py in wave 3, gui_setup.py in wave 4) are ILLEGAL to create until this extension lands — that is why this plan is wave 1 and everything downstream depends on it. This is the researched recommendation (03-RESEARCH-setup-ui.md §5.2, 03-RESEARCH-viewer-bridge.md §3.2-3.3), adopted as a consolidated default decision per the phase planning directives — no decision checkpoint.

Output: extended tools/check_purity.py + extended tests/test_purity_gates.py (fixture-first: tests prove the new classes before/with the implementation).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-setup-ui.md
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-RESEARCH-viewer-bridge.md
@tools/check_purity.py
@tests/test_purity_gates.py

Load-bearing facts (verified in the research, trust over memory):
- Current classes (check_purity.py): ENTRY (`serpentrum/__init__.py`, pymol/pmg_tk only lazily in bodies), GUI (`GUI_MODULES = {'serpentrum/gui.py'}`, ONLY pymol.Qt forms at any level), PURE (default; pymol/pmg_tk/PyQt5/numpy banned anywhere). `.exec_()` is flagged in EVERY class by a separate ast.Call walk (check_purity.py:162-169) — that rule stays UNCHANGED; the BRIDGE branch only governs imports.
- Relative imports (ImportFrom with level > 0 or module None) are always exempt (check_purity.py:75-78) — pure/bridge/gui modules import each other this way.
- check_tree walks only EXISTING .py files under serpentrum/ (check_purity.py:179-183), so adding 'serpentrum/pymol_bridge.py' to BRIDGE_MODULES and 'serpentrum/gui_setup.py' to GUI_MODULES before those files exist does NOT break `test_real_repo_clean` (case 10).
- Exact BRIDGE spec (03-RESEARCH-viewer-bridge.md §3.2): `BRIDGE_MODULES = {'serpentrum/pymol_bridge.py'}`; classify() returns 'BRIDGE' for it; _check_import_node BRIDGE branch = allow pymol/pmg_tk roots at ANY level, flag PyQt5/numpy roots at any level with message mentioning the root and 'banned anywhere'.
- Hard conventions: python3.6 syntax only, %-formatting; tests/ has NO `__init__.py`; fixture-tree pattern copies tests/test_purity_gates.py's `make_tree` (tempfile.mkdtemp + addCleanup shutil.rmtree — never shell rm).
- Scope guard: this plan ONLY extends the checker + its self-tests. It does NOT create serpentrum/pymol_bridge.py or serpentrum/gui_setup.py (03-06/03-07 own those).
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): failing fixture cases for the BRIDGE class + extended GUI_MODULES</name>
  <files>tests/test_purity_gates.py</files>
  <action>
Append to tests/test_purity_gates.py (inside PurityFixtureTest, mirroring cases 5/6 style) SEVEN new cases. They MUST fail now: `classify()` has no BRIDGE branch (a pymol_bridge.py fixture today falls to PURE and gets flagged), and gui_setup.py is not in GUI_MODULES.
1. test_bridge_module_level_pymol_clean: tree with `serpentrum/__init__.py` ('"""Entry."""\n') + `serpentrum/pymol_bridge.py` containing a module-level `from pymol import cmd` and a trivial function -> check_tree == [] (0 violations).
2. test_bridge_body_pymol_clean: `serpentrum/pymol_bridge.py` with `from pymol import cmd` INSIDE a function body + `import pmg_tk.startup` in another body -> [].
3. test_bridge_pyqt5_banned: `serpentrum/pymol_bridge.py` with module-level `from PyQt5 import QtWidgets` -> exactly 1 violation; rel == 'serpentrum/pymol_bridge.py'; msg contains 'PyQt5'.
4. test_bridge_numpy_banned: `serpentrum/pymol_bridge.py` with `import numpy` inside a function body -> exactly 1 violation mentioning 'numpy'.
5. test_bridge_exec_call_flagged: `serpentrum/pymol_bridge.py` with a function calling `dialog.exec_()` -> exactly 1 violation mentioning 'exec_' (proves the modeless ban covers BRIDGE).
6. test_gui_setup_pymol_qt_clean: tree with `serpentrum/gui_setup.py` containing module-level `from pymol.Qt import QtWidgets, QtCore` -> [] (gui_setup is GUI class after this plan).
7. test_gui_setup_bare_pymol_flagged: `serpentrum/gui_setup.py` with module-level `from pymol import cmd` AND a second tree with `import pmg_tk.startup` inside a function -> each tree yields exactly 1 violation on 'serpentrum/gui_setup.py' mentioning the banned root (mirrors case 6's any-level rule).
Docstrings/comments in fixtures may mention banned text (AST-invisible) — do not add banned imports outside the intended ones. Run the file scoped: `python3.6 -m unittest discover -s tests -p "test_purity_gates.py" -v` -> the 7 new cases FAIL, the existing 10 stay green.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_purity_gates.py" -v` -> exactly the 7 new cases fail (AssertionError on violation lists), all pre-existing cases pass</verify>
  <done>7 failing fixture cases encode the BRIDGE + gui_setup contracts. Commit: `test(03-01): add failing BRIDGE-class and gui_setup purity fixture cases`.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): checker extension — BRIDGE_MODULES, classify branch, import rule, GUI_MODULES</name>
  <files>tools/check_purity.py</files>
  <action>
1. Below GUI_MODULES add (with the WHY comment from 03-RESEARCH-viewer-bridge.md §3.2):
   `BRIDGE_MODULES = {'serpentrum/pymol_bridge.py'}` — "Explicit cmd-bridge allowlist — the ONLY modules (besides ENTRY-lazy) that may import pymol.cmd. Allows pymol/pmg_tk at module level AND in bodies; bans PyQt5/numpy everywhere (Qt stays in GUI modules; numpy never needed in the bridge — pure modules do the math). .exec_() stays banned (the bridge builds no dialogs)."
2. Extend GUI_MODULES to `{'serpentrum/gui.py', 'serpentrum/gui_setup.py'}` (deliberate extension per STATE.md decision 01-03; gui_setup.py is created by plan 03-07).
3. classify(): add `if rel_path in BRIDGE_MODULES: return 'BRIDGE'` between the GUI and PURE returns; update the docstring return-line.
4. _check_import_node(): add a BRIDGE branch AFTER the GUI branch — allow pymol/pmg_tk at any level (no flag), and for each root in sorted(roots & {'PyQt5', 'numpy'}) flag with message `'bridge module imports %r (banned anywhere)' % root`. The PURE else-branch and the .exec_() walk stay untouched.
5. Update the module docstring classification table: add the BRIDGE row (path serpentrum/pymol_bridge.py, class BRIDGE, "pymol/pmg_tk allowed at any level; PyQt5/numpy never; .exec_() never") and note gui_setup.py in the GUI row.
Run the RED tests -> all green. Then `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (full suite — test_real_repo_clean must stay green: neither new module exists yet) and `python3.6 tests/run_gates.py` (all gates green).
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_purity_gates.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> exit 0 all gates green; `python3.6 tools/check_purity.py` -> 'clean'</verify>
  <done>BRIDGE class live (pymol/pmg_tk any level; PyQt5/numpy+exec_ banned), gui_setup.py in GUI_MODULES, docstring table current, real repo clean, full gates green. Commit: `feat(03-01): add BRIDGE purity class + gui_setup GUI allowlist entry`.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_purity_gates.py" -v` -> OK (17 cases: 10 pre-existing + 7 new)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (no regressions anywhere)
- `python3.6 tests/run_gates.py` -> exit 0
- `grep -n "BRIDGE_MODULES" tools/check_purity.py` -> the constant + classify + docstring hits
- No serpentrum/pymol_bridge.py or serpentrum/gui_setup.py created (scope guard: those are 03-06/03-07)
</verification>

<success_criteria>
- BRIDGE_MODULES allowlist live and self-tested: pymol_bridge.py-shaped fixtures pass with pymol/pmg_tk at any level and fail on PyQt5/numpy/exec_
- GUI_MODULES deliberately extended with serpentrum/gui_setup.py (pymol.Qt clean, bare pymol flagged)
- Real repo still clean; full gate suite green; downstream wave-2+ plans can now legally create the bridge and setup-tab modules
</success_criteria>

<output>
After completion, create `.planning/phases/03-molecules-in-the-viewer-setup-tab/03-01-SUMMARY.md`
</output>
