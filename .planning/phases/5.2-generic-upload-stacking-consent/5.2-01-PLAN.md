---
phase: 5.2-generic-upload-stacking-consent
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/setup_logic.py
  - tests/test_setup_logic.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A fresh setup carries 'generic_stack_consent': False (OFF by default — SC1)"
    - "A persisted setup file WITHOUT the consent key merges to False and validates clean (absent key = OFF backcompat — SC1)"
    - "A non-bool consent value (e.g. 'yes', 1) produces a validate() error naming the key; validate never raises (never-raises contract preserved)"
    - "consent True round-trips byte-identically through save_setup/load_setup"
  artifacts:
    - path: serpentrum/setup_logic.py
      provides: "DEFAULTS 10th key 'generic_stack_consent': False + validate() bool type check"
      contains: "generic_stack_consent"
    - path: tests/test_setup_logic.py
      provides: "EXPECTED_DEFAULTS pin gains the key (reviewable schema diff); bool-validate + round-trip + merge-absent-key tests"
      contains: "generic_stack_consent"
  key_links:
    - from: "serpentrum/setup_logic.py DEFAULTS"
      to: "gui_setup.py / gui_game.py consumers"
      via: "setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent']) fallback shape (5.1-05 speed precedent)"
      pattern: "generic_stack_consent"
---

<objective>
Add the consent schema key `generic_stack_consent` (bool, default False = OFF) to setup_logic's documented DEFAULTS with a validate() bool type-check — the pure schema half of STACK-06 SC1.

Purpose: every downstream consumer (Setup checkbox, overlay builder, GameTab notes) reads this ONE key with the 5.1 `setup.get(key, DEFAULTS[key])` fallback shape; absent-key backcompat reuses the already-landed `merge_defaults` seam whose docstring explicitly names this phase.
Output: `serpentrum/setup_logic.py` with the 10th DEFAULTS key + bool validation; `tests/test_setup_logic.py` with the updated EXPECTED_DEFAULTS pin and new tests. No SCHEMA_VERSION bump.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md
@.planning/phases/5.1-game-speed-difficulty/5.1-02-SUMMARY.md

Key facts (verified against the live tree; research file:line map §1):
- `serpentrum/setup_logic.py:49-59` — DEFAULTS currently has 9 keys, all scalar/None. The consent key slots as the 10th, after `'speed': 3.0` (:58).
- `setup_logic.py:40` — SCHEMA_VERSION = 1. DO NOT bump (5.1-02 precedent comment at :104-106: backcompat is free via defaults-overlay; v=1 hard-rejects anything else at :297-323).
- `setup_logic.py:138-156` — `merge_defaults(loaded)`: dict(DEFAULTS) overlay, loaded wins, unknown keys pass through, never mutates. Its docstring (:143-145) already says "Phase 5.2 reuses this seam for its consent key, 'absent key = OFF'". ZERO production callers today (Save/Load buttons are Phase 8) — persistence is proven at the pure layer, exactly as 5.1 resolved it.
- `setup_logic.py:203-279` — validate(): never raises; errors are data. Message style: `'<key> %r ...' % (value,)`. Existing checks end with head_molecule; the new check slots after it, before the warnings block.
- `tests/test_setup_logic.py:42-53` — EXPECTED_DEFAULTS pin (9 keys). Adding the key BREAKS `test_new_setup_returns_documented_defaults` (:65-67) — updating the pin IS the reviewable schema diff (house pattern, 5.1-02).
- Bool trap: `_is_number` (:175-179) exists because bools pass isinstance(x, int). The consent check is the INVERSE: the value must be a bool (`isinstance(v, bool)`).

RESOLVED DECISIONS owned by this plan (from the research open questions):
- EQ-setup-1: Save/Load BUTTONS stay Phase 8 — persistence is proven at the pure layer (save/load round-trip + merge_defaults backcompat), inheriting the 5.1 answer verbatim. Do NOT pull buttons forward.
- EQ-setup-2: validate() GAINS one additive check — "must be a bool". Rationale: consent is a safety-relevant opt-in; a truthy non-bool ('yes') from a hand-edited file silently enabling consent is a real hazard. This deliberately differs from 5.1's accept-as-is speed precedent (numbers > 0 are a designed continuum; consent is a boolean gate). Never-raises contract untouched (the check appends an error string).
- Key name: `generic_stack_consent` (snake_case, full-word; 'speed'/'demo_set'/'win_cap_molecules' precedent). Default False.
- Date stamping in this plan's artifacts: UTC (2026-09-23).

Byte-identical-OFF pins that must stay green: tests/test_setup_logic.py full file EXCEPT the EXPECTED_DEFAULTS pin (which this plan itself updates) — especially merge_defaults matrix (:368-404), save/load round-trip, never-raises matrix (:242-267).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for the consent key</name>
  <files>tests/test_setup_logic.py</files>
  <action>
    In tests/test_setup_logic.py (house conventions: python3.6, %-formatting, unittest, no f-strings, no sys.modules stubs):

    1. Update the `EXPECTED_DEFAULTS` dict (:42-53) to add `'generic_stack_consent': False` as the 10th entry (after `'speed': 3.0`). This makes `test_new_setup_returns_documented_defaults` FAIL against the current code — the RED state. Add a comment above the key: "STACK-06 consent (Phase 5.2): OFF by default; absent key = OFF via merge_defaults."
    2. Add class `TestGenericStackConsent` with:
       - `test_default_is_off`: `new_setup()['generic_stack_consent']` is exactly `False` (identity check `is False`, not falsy-equal).
       - `test_validate_accepts_bool_true_and_false`: `validate(_mutated(generic_stack_consent=True))` and `... =False)` each produce NO error mentioning 'generic_stack_consent' (other pre-existing warnings/errors unaffected — assert via `not any('generic_stack_consent' in e for e in errors)`).
       - `test_validate_rejects_non_bool`: for values `'yes'`, `1`, `None`, `0`: `validate(_mutated(generic_stack_consent=v))` yields exactly one error containing `'generic_stack_consent'` and the word `'boolean'`. NOTE the `None` case: `setup.get('generic_stack_consent')` returning None must ALSO be an error here (explicit None is not consent) — write the check under test accordingly in Task 2 (`v = setup.get('generic_stack_consent', False)` then `if not isinstance(v, bool): errors.append(...)` — None fails isinstance, so it errors; but a dict MISSING the key defaults to False and passes. Pin both: missing-key passes, explicit None fails).
       - `test_missing_key_merges_to_off`: `setup_logic.merge_defaults({'schema_version': 1, 'demo_set': 'set_a', 'head_molecule': 'random', 'box_preset': 'medium', 'xtb_path': None, 'win_cap_molecules': 10, 'atom_budget': 100, 'broadening_fwhm': 16.0, 'speed': 3.0})` (all 9 OLD keys, no consent key) → merged['generic_stack_consent'] is False and `validate(merged)` has no errors.
       - `test_round_trip_preserves_true`: `s = new_setup(); s['generic_stack_consent'] = True; loaded = load_setup(save_setup(s))` → `loaded['generic_stack_consent']` is True (raw load preserves; load_setup keeps its no-merge contract — do NOT merge in this assertion).
       - `test_validate_never_raises_on_weird_consent`: `validate({'generic_stack_consent': object()})` (plus a minimal valid rest of the dict) returns errors, raises nothing.
    3. Run: `python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` — the new tests FAIL (no key in DEFAULTS, no validate check); `test_new_setup_returns_documented_defaults` FAILS on the updated pin. Confirm RED.
    4. Commit RED: `test(5.2-01): failing tests for generic_stack_consent schema key`

    Do NOT touch any other test file. Do NOT add tests to test_phase51_integration.py (the 5.2 integration chain is plan 5.2-08's file).
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v reports failures in TestGenericStackConsent + test_new_setup_returns_documented_defaults, and NO unexpected failures elsewhere</verify>
  <done>RED confirmed: the updated EXPECTED_DEFAULTS pin + new consent tests fail against the unmodified setup_logic.py; all other tests in the file still pass.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — DEFAULTS key + validate bool check</name>
  <files>serpentrum/setup_logic.py</files>
  <action>
    In serpentrum/setup_logic.py:

    1. DEFAULTS (:49-59): add `'generic_stack_consent': False,` as the last entry (after `'speed': 3.0,`) with the inline comment `# STACK-06 (Phase 5.2): generic upload pi-stack consent; OFF by default — absent key = OFF via merge_defaults (docstring below).` The scalar invariant comment (:46-48) still holds (bool is scalar).
    2. validate(): after the head_molecule check and BEFORE the warnings block, add:
       ```python
       consent = setup.get('generic_stack_consent', False)
       if not isinstance(consent, bool):
           errors.append('generic_stack_consent %r must be a boolean'
                         % (consent,))
       ```
       This preserves: never-raises (isinstance never raises), missing-key-passes (get default False), explicit-None-fails (None is not bool). Update the validate() docstring's errors list to add the line `generic_stack_consent  not a bool` and note the missing-key-passes semantics.
    3. Do NOT touch: SCHEMA_VERSION, merge_defaults, save_setup, load_setup, SPEED_TIERS, BOX_PRESETS, KNOWN_SETS.
    4. Run the file suite green, then the FULL gate suite: `python3.6 tests/run_gates.py` — all four gates PASS (syntax + plugin-path safety, AST purity, unittest discovery across the whole suite). The rest of the suite (test_phase51_integration.py EXPECTED pins etc.) must be unaffected — merge_defaults/validate additions are purely additive.
    5. Commit GREEN: `feat(5.2-01): generic_stack_consent schema key + bool validation (STACK-06 SC1)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v -> all pass; python3.6 tests/run_gates.py -> FULL suite green (baseline 678+ tests, count grows by the new tests)</verify>
  <done>DEFAULTS carries 'generic_stack_consent': False; validate() enforces bool-with-missing-key-passes; EXPECTED_DEFAULTS pin updated; full gates green; SCHEMA_VERSION untouched.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (full suite).
- `grep -n "generic_stack_consent" serpentrum/setup_logic.py` shows exactly: the DEFAULTS entry + the validate check + the docstring line.
- OFF-path invariants: `merge_defaults({})['generic_stack_consent'] is False`; absent-key merged dict validates clean (pinned in Task 1).
</verification>

<success_criteria>
- SC1 schema half: the consent key exists with OFF default, persists through the save/load round-trip (pure layer), and absent-key files merge to OFF.
- No SCHEMA_VERSION bump; merge_defaults untouched; all pre-existing pins green.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-01-SUMMARY.md`
</output>
