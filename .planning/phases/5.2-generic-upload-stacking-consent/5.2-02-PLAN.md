---
phase: 5.2-generic-upload-stacking-consent
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/generic_stack.py
  - tests/test_generic_stack.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The generic interaction is a well-formed entry dict reusing the APPROVED Set-A geometry EXACTLY (distance_a 3.383 / lateral_offset_a 1.231 — the DATA-02 encoding of 3.60 A @ 20 deg; NO new numbers)"
    - "overlay_stacking_data(data, False) returns the input object UNCHANGED (byte-identical OFF by construction); ON returns a fresh deepcopy with the generic entry appended LAST (shadows nothing — first-match order intact)"
    - "The generic entry targets ONLY the '__upload__' sentinel — demo sets can never match it"
    - "has_stack_entry_for(record, stacking_data) recomputes stackability from ANY anchored dataset (None-safe fallback to the load-time flag) — the consent carrier is the overlay, not a flag"
    - "history_name decorates only generic placements ('<name> (generic pi-stack)') so recap grouping separates them with ZERO breakdown_lines edits"
  artifacts:
    - path: serpentrum/generic_stack.py
      provides: "GENERIC_INTERACTION constant + overlay_stacking_data + history_name + has_stack_entry_for (pure, stdlib-only + molecule_data.interaction_for)"
      min_lines: 60
    - path: tests/test_generic_stack.py
      provides: "structural shape pins, overlay matrix, real-dataset citation cross-pin, helper behavior matrix"
      min_lines: 80
  key_links:
    - from: "serpentrum/generic_stack.py GENERIC_INTERACTION"
      to: "molecule_data.interaction_for / placement.resolve_skip / hud_logic.pickup_block"
      via: "the entry-dict contract (id/mode/name/distance_a/lateral_offset_a/uncertainty_a/citation/explanation/applies_to/status) consumed verbatim by unchanged machinery"
      pattern: "pi_stack_generic"
    - from: "serpentrum/generic_stack.py overlay_stacking_data"
      to: "serpentrum/data/stacking_pi_stack.json (read-only)"
      via: "deepcopy of the LOADED dataset — the FILE is never modified (shipping contract; exactly-one-APPROVED pin stays green)"
      pattern: "deepcopy"
---

<objective>
Create the pure module `serpentrum/generic_stack.py`: the code-borne generic interaction dict (reusing the human-approved DATA-02 geometry verbatim), the consent-gated in-memory dataset overlay builder, the recap name decorator, and the consent-blind stackability helper — the data half of STACK-06.

Purpose: the research's Approach A — express the consent policy as DATA through the existing `interaction_for` lookup, so approval gate, mode gate, placement math, citation resolution, and HUD rendering all consume the generic entry UNCHANGED. The dataset FILE is never touched.
Output: one new pure module + its test file. No existing module modified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md

Key facts (research file:line map rows 1-4 + answers 2/3):
- Shipped entry `serpentrum/data/stacking_pi_stack.json:4-15`: pi_stack_pd, mode pi_stack, distance_a 3.383, lateral_offset_a 1.231, citation janiak2000, applies_to ["set_a"], APPROVED. Citations table :17-21 — `janiak2000.short = 'Janiak 2000'`, approved true.
- Entry validation field rules `molecule_data.py:251-330`: id, mode, name, distance_a (0<d<=10), lateral_offset_a (>=0), uncertainty_a (null or >=0), citation (must resolve), explanation (str), applies_to.sets (non-empty), status DRAFT|APPROVED. An in-memory dict just needs to be well-formed — validation is file-load-time only — but the module docstring + tests pin the shape against these rules.
- Lookup `molecule_data.py:346-362` (interaction_for): first-match file order; appending the generic entry LAST shadows nothing (set_a still first-matches pi_stack_pd).
- Approval gate `molecule_data.py:333-343` (shipped_interactions) enforced at `placement.py:120-124` via id-membership — the generic entry carries status 'APPROVED' as the structural gate (the GEOMETRY is what is already human-approved per DATA-02; the flag is mechanism, not a new approval claim).
- Eligibility signal: `setloader.py:242-263` _upload_stack_ring — `record['stack_ring']` present = canonical planar 6-ring (computed at LOAD, consent-independent).
- Upload sentinel `setloader.py:65` UPLOAD_SET_ID = '__upload__'; `setloader.py:99-117` _build_record computes has_stack_entry = interaction_for({'set': set_id}, stacking_data) is not None at LOAD time.
- module_data.interaction_for is the ONLY serpentrum import this module needs (plus stdlib copy). Purity: no pymol/Qt/numpy anywhere; python3.6 → %-formatting, NO f-strings.

RESOLVED DECISIONS owned by this plan:
- EQ-engine-4 (builder home): NEW pure module `serpentrum/generic_stack.py` — molecule_data stays load/validate-only (its tests pin that contract); placement stays taxonomy/math. One file, one concern, trivially traceable to STACK-06.
- EQ-engine-5 (label alphabet): ASCII 'pi-stack' everywhere in code/tests/HUD (house style: dataset name is ASCII; hud 'A/s' ASCII). The π glyph appears only in planning docs. Final literals owner-approved at the 5.2-09 feel-check.
- EQ-engine-6 + EQ-setup-7 (recap grouping): ZERO breakdown_lines edits — the recap distinction is achieved by decorating the ENROLLED history name via `history_name()` (generic placements group under '<molecule> (generic pi-stack)', first-appearance grouping separates them automatically). The stored `interaction_id` key stays as-is (unused by grouping).
- EQ-engine-2 (has_stack_entry site, helper half): `has_stack_entry_for(record, stacking_data)` is the PURE half of the consent-aware stamping; the GUI call site lands in plan 5.2-06. Semantics: the ANCHORED dataset is the consent carrier — recompute `interaction_for` against it; None dataset falls back to the load-time flag (preserves the gui_setup.py:497-499 dataset-unavailable degradation).
- Generic entry id: `pi_stack_generic`. Mode: 'pi_stack' (the only mode v1 places — placement.py:88).
- DRAFT literals (pinned here, owner-amendable at 5.2-09):
  - name: `generic pi-stack (illustrative geometry - user-approved)`
  - explanation: `Generic fallback reusing the approved idealized Set A pi-stack geometry (3.60 A @ 20 deg off-normal); this upload molecule was not measured - the geometry is illustrative, user-approved via explicit consent; crystalline-context scope caveat applies (DATA_SOURCES.md [Janiak 2000]).`
  - recap decoration suffix: ` (generic pi-stack)`

Byte-identical-OFF pins that must stay green: tests/test_stacking_dataset.py:166-185 (exactly-one-APPROVED shipped entry — this plan does NOT touch the JSON), tests/test_placement.py:161-176/604-621 (upload → SKIP_NO_ENTRY), tests/test_phase5_integration.py:454-495, tests/test_setloader_stack_ring.py full.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for generic_stack</name>
  <files>tests/test_generic_stack.py</files>
  <action>
    Create tests/test_generic_stack.py (house header: sys.path self-insert like tests/test_phase51_integration.py:32-41; NO __init__.py; unittest; %-formatting). Import `from serpentrum import generic_stack, molecule_data, setloader` (module absent → ImportError = RED).

    Class TestGenericInteraction (structural mirror of molecule_data field rules):
    - test_required_fields_exact_values: GENERIC_INTERACTION has exactly the keys {'id','mode','name','distance_a','lateral_offset_a','uncertainty_a','citation','explanation','applies_to','status'}; id == 'pi_stack_generic'; mode == 'pi_stack'; distance_a == 3.383; lateral_offset_a == 1.231; uncertainty_a is None; citation == 'janiak2000'; status == 'APPROVED'; applies_to == {'sets': ['__upload__']} (ONLY the sentinel).
    - test_name_label: name == 'generic pi-stack (illustrative geometry - user-approved)'.
    - test_explanation_honesty: explanation contains 'illustrative', 'user-approved', '3.60', and 'not measured' (the no-invented-chemistry framing); is a str; no '\\n'.
    - test_ascii_only: every char of id/name/explanation has ord < 128 (ASCII house style — EQ-engine-5).
    - test_geometry_encodes_approved_ideal: sqrt(3.383**2 + 1.231**2) assertAlmostEqual 3.60 (places=3); math.degrees(math.atan2(1.231, 3.383)) assertAlmostEqual 20.0 (places=2) — pins that the REUSED numbers still decode to the approved 3.60 A @ 20 deg.
    - test_citation_resolves_in_shipped_dataset: load the real dataset (`molecule_data.load_stacking(setloader.default_stacking_path())`); assert 'janiak2000' in data['citations'], its 'short' == 'Janiak 2000', its 'approved' is True — the generic entry cites ONLY an already-approved citation (no new citation invented).

    Class TestOverlayStackingData:
    - test_off_returns_same_object: `data = <real loaded dataset>`; `generic_stack.overlay_stacking_data(data, False) is data` (identity — OFF consumes the original verbatim); and a deep-equality check that data is unchanged after the call.
    - test_on_returns_fresh_copy_with_entry_last: overlay ON → result is not data; result['interactions'][-1]['id'] == 'pi_stack_generic'; result['interactions'][:-1] == data['interactions'] (nothing shadowed); len grows by exactly 1; the ORIGINAL data['interactions'] length unchanged (no mutation).
    - test_on_entry_is_not_the_module_constant: `result['interactions'][-1] is not generic_stack.GENERIC_INTERACTION` AND mutating the appended dict (e.g. its 'name') leaves GENERIC_INTERACTION unchanged (mutation isolation — deepcopy per append).
    - test_none_safe: overlay_stacking_data(None, True) is None; overlay_stacking_data(None, False) is None.

    Class TestHistoryName:
    - test_generic_decorated: history_name('benzene', GENERIC_INTERACTION) == 'benzene (generic pi-stack)'.
    - test_dataset_entry_unchanged: history_name('benzene', {'id': 'pi_stack_pd', ...minimal}) == 'benzene' (any id != the generic id → unchanged).

    Class TestHasStackEntryFor:
    - test_none_dataset_falls_back: has_stack_entry_for({'set': '__upload__', 'has_stack_entry': False}, None) is False; has_stack_entry_for({'set': 'set_a', 'has_stack_entry': True}, None) is True (dataset-unavailable degradation preserves the load-time flag).
    - test_upload_original_dataset_false: real loaded dataset + {'set': '__upload__'} → False (STACK-03 keying intact).
    - test_upload_overlay_true: overlay ON + {'set': '__upload__'} → True.
    - test_demo_identical_both_ways: {'set': 'set_a'} → True on the original AND True on the overlay (the appended entry shadows nothing — definitional-identity pin for the 5.2-06 restamp).

    Run: `python3.6 -m unittest discover -s tests -p "test_generic_stack.py" -v` → ImportError/RED. Commit RED: `test(5.2-02): failing tests for generic_stack module (entry/overlay/helpers)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_generic_stack.py" -v fails with ImportError (module absent); no other test file touched</verify>
  <done>RED confirmed: the full behavioral contract for generic_stack exists as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement serpentrum/generic_stack.py</name>
  <files>serpentrum/generic_stack.py</files>
  <action>
    Create serpentrum/generic_stack.py — a PURE module (stdlib + `from . import molecule_data` only; no pymol/Qt/numpy; %-formatting; module docstring names STACK-06 and the reuse-not-invent rule):

    1. Module docstring: state the invariants — the geometry numbers are REUSED from the human-approved DATA-02 Set-A entry (never invented); the dataset FILE is the shipping contract and is never modified (the overlay is in-memory only); consent is carried by the overlay itself, so consumers stay consent-blind.
    2. `GENERIC_INTERACTION` module constant: the exact dict pinned in Task 1. Build 'applies_to' inline as {'sets': ['__upload__']}. Comment each field with its provenance (distance_a/lateral_offset_a = the shipped pi_stack_pd encoding of 3.60 A @ 20 deg; citation janiak2000 = already approved in the shipped citations table; status APPROVED = the structural approval gate the placement taxonomy enforces — the geometry itself is the human-approved DATA-02 decision).
    3. `overlay_stacking_data(stacking_data, consent)`:
       - consent falsy → return stacking_data UNCHANGED (same object; zero-copy OFF path).
       - stacking_data is None → return None (dataset-unavailable degradation, either consent state).
       - else → `data = copy.deepcopy(stacking_data)`; `data['interactions'].append(copy.deepcopy(GENERIC_INTERACTION))`; return data. Appending LAST = first-match order intact (research row: "appending ['__upload__'] entry LAST shadows nothing").
    4. `history_name(molecule_name, interaction)`: return `'%s (generic pi-stack)' % (molecule_name,)` when `interaction.get('id') == GENERIC_INTERACTION['id']`, else molecule_name unchanged. Docstring: this is the recap-grouping mechanism (SC4) — first-appearance grouping in hud_logic.breakdown_lines separates the decorated name with zero breakdown_lines edits.
    5. `has_stack_entry_for(record, stacking_data)`: if stacking_data is None → `record.get('has_stack_entry')`; else → `molecule_data.interaction_for({'set': record.get('set')}, stacking_data) is not None`. Docstring: mirrors setloader._build_record's definition; the None-dataset fallback preserves gui_setup's degradation contract; used by the gui_game consent-aware restamp (plan 5.2-06).
    6. Run the file suite green, then full gates: `python3.6 tests/run_gates.py` (AST purity gate must classify the new module PURE — no imports beyond stdlib + molecule_data).
    7. Commit GREEN: `feat(5.2-02): generic_stack module — generic entry, overlay builder, recap name, stackability helper (STACK-06)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_generic_stack.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green</verify>
  <done>generic_stack.py exists (pure), all four behaviors pinned, dataset file untouched, gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `git status` shows serpentrum/data/stacking_pi_stack.json UNMODIFIED (the shipping contract).
- `python3.6 -c "from serpentrum import generic_stack; print(generic_stack.GENERIC_INTERACTION['distance_a'], generic_stack.GENERIC_INTERACTION['lateral_offset_a'])"` prints `3.383 1.231`.
</verification>

<success_criteria>
- The generic entry is structurally identical to a dataset entry and reuses the approved geometry exactly; overlay OFF = identity; overlay ON = non-shadowing append; helpers pure and pinned.
- Downstream machinery (lookup/approval/mode/placement/HUD) can consume the entry with zero changes — proven by plans 5.2-03/04/06 building directly on these symbols.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-02-SUMMARY.md`
</output>
