---
phase: 5.2-generic-upload-stacking-consent
plan: 07
subsystem: docs
tags: [data-sources, attribution, stacking, consent, janiak2000, labeling]

# Dependency graph
requires:
  - phase: 5.2-generic-upload-stacking-consent
    provides: 5.2-02 generic_stack.py pinned interaction literal; 5.2-04 hud_logic consent note + skip-reason literals; [JAN2000] SCOPE CAVEAT (2026-09-20, from Phase 2 DATA_SOURCES)
provides:
  - DATA_SOURCES.md §2 sub-entry documenting the consent-gated generic upload stacking mode (STACK-06 SC4 docs half)
  - Reused-geometry statement (3.383/1.231 -> 3.60 A @ 20 deg) with zero new claims
  - Consent/labeling disclosure framing documented (OFF default, illustrative/user-approved label)
  - SCOPE CAVEAT applicability carried from [JAN2000] to the generic mode
affects: [5.2-09 feel-check, phase-8 data-prep, data-attribution audits]

# Tech tracking
tech-stack:
  added: []
  patterns: ["docs-as-approval-contract: generic mode documented under the same DRAFT-headed [JAN2000] anchor; geometry REUSED not re-measured", "grep-for-grep docs<->code literal consistency check"]

key-files:
  created: []
  modified:
    - serpentrum/data/DATA_SOURCES.md

key-decisions:
  - "Docs placement: new §2 sub-entry immediately after [JAN2000] (NOT §3 — §3 is do-not-ship; the generic mode SHIPS, consent-gated)"
  - "§2 heading intro untouched (no content enumeration to amend)"
  - "Consent-note literal quoted unbroken in docs; code keeps its adjacent-fragment split (regex-joined equality verified)"

patterns-established:
  - "Quoted in-game literals in DATA_SOURCES.md kept single-line and ASCII-faithful so docs and HUD strings match grep-for-grep"

# Metrics
duration: 5min
completed: 2026-09-25
---

# Phase 5.2 Plan 07: DATA_SOURCES generic upload stacking mode docs Summary

**§2 sub-entry documenting the consent-gated generic upload stacking mode: reuses the approved [JAN2000]/COD 3.60 Å geometry, OFF-by-default explicit consent, illustrative/user-approved labeling, crystalline-context scope caveat carried over — no new claims.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-25T20:10:42Z
- **Completed:** 2026-09-25T20:15:03Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- DATA_SOURCES.md documents the generic upload stacking mode as a §2 sub-entry immediately after [JAN2000], per the plan's placement decision (§3 rejected — it is the do-not-ship list; the generic mode ships, consent-gated).
- The sub-entry covers all six plan-specified points: (a) opt-in on the Setup tab ('Generic stacking' group); (b) geometry REUSED not re-measured — same 3.383/1.231 Å encoding (3.60 Å @ 20 deg) as the Set-A entry; (c) 'illustrative geometry - user-approved' label on every in-game line; (d) consent OFF by default and explicit; (e) ring-less uploads skip with an explicit reason; (f) [JAN2000] SCOPE CAVEAT applies unchanged and the consent framing IS the Phase-8 crystalline-state framing landed early.
- Numeral audit of the new sub-entry: only the approved 3.60/3.383/1.231/20 appear (citation-year 2000 in pre-approved anchor names, CC0 license name, Phase 5.2/8 reference numbers — no new chemistry claims).
- DRAFT header, status legend, §1, §3, §4, and every existing entry/DOI/number untouched; `stacking_pi_stack.json` unmodified (verified via git status).

## Task Commits

Each task was committed atomically:

1. **Task 1: §2 generic-mode sub-entry** - `3960b14` (docs)
2. **Task 2: Docs ↔ in-game literal consistency check** - `d1f72f3` (docs)

**Plan metadata:** see below (docs: complete plan)

## Files Created/Modified
- `serpentrum/data/DATA_SOURCES.md` — added the 26-line §2 sub-entry "Generic upload stacking mode (Phase 5.2, consent-gated)"

## Decisions Made
- **Consent-note literal quoting:** the code (`hud_logic.py:243-244`) splits the consent note across two adjacent string fragments, so a naive fixed-string grep can never find the full sentence there. The docs quote it unbroken on its own line; code-side equality was verified by regex-joining the fragments (`return ('generic pi-stack enabled: illustrative geometry - ' ... 'user-approved [Janiak 2000]')`). This satisfies the grep-for-grep intent without touching test-pinned code.
- **Idle tips:** left untouched per the plan's RESOLVED DECISION (EQ-setup-4: dead surface, no live caller).

## Deviations from Plan

None - plan executed exactly as written.

(The Task 2 wording commit was itself prescribed by the plan: "Commit (if fixes were needed)" — the fixes were the literal-consistency additions, committed with the plan's prescribed message.)

## Issues Encountered
- **`'Generic stacking'` group title absent from `serpentrum/gui_setup.py` in this worktree.** Plan 5.2-07 depends only on 5.2-02/5.2-04; the Setup-tab 'Generic stacking' group (plan 5.2-05) is on a parallel branch not yet merged into `exec/5.2-07`. Per the context digest this is a wave-timing artifact, NOT a docs/code wording mismatch: the docs keep the plan-sanctioned DRAFT literal `'Generic stacking'`. The four-way grep check is therefore 3/4 code-verified + 1/4 verified against the plan's DRAFT wording; the fourth literal should be re-greppable against `gui_setup.py` once the orchestrator merges the wave branches.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- SC4 docs half complete; the generic mode is documented with the approved citation, reused-geometry statement, consent/labeling framing, and scope caveat.
- Ready for 5.2-09 (feel-check): docs and pinned in-game literals agree; any wording amendment there must update DATA_SOURCES.md and the code literals together.
- Watch: after the wave merge, re-run `grep -F "Generic stacking" serpentrum/gui_setup.py` to close out the fourth literal check.

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
