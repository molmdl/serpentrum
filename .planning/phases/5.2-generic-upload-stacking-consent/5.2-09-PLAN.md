---
phase: 5.2-generic-upload-stacking-consent
plan: 09
type: execute
wave: 5
depends_on: ["5.2-05", "5.2-06", "5.2-07", "5.2-08"]
files_modified:
  - .planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md
  - serpentrum/generic_stack.py
  - serpentrum/hud_logic.py
  - serpentrum/gui_setup.py
  - serpentrum/data/DATA_SOURCES.md
  - tests/test_generic_stack.py
  - tests/test_hud_content.py
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The FULL WSL gate suite is green on the completely merged phase tree, with every OFF-path pin explicitly re-run (SC3 integrity)"
    - "A live human feel-check in real Windows PyMOL approves the consent UX + every DRAFT literal (SC5), with a per-step verdict table recorded"
    - "Consent ON: a ring-bearing upload captures and places at the approved geometry with the DBG line dot=1.000000 d=3.6000 plane=3.3830 lat=1.2310 (SC2 live verification)"
    - "Consent OFF: upload behavior is byte-identical to today (skip line, demo-mode note, no consent line, recap taxonomy) (SC3 live verification)"
    - "Owner-amended wording (if any) is applied as literal-only edits with pins updated and gates re-run"
  artifacts:
    - path: .planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md
      provides: "feel-check verdict table + owner-approved final literals + head-eligibility confirmation"
      contains: "verdict"
  key_links:
    - from: "5.2-09 checkpoint steps"
      to: "SRP_DEBUG=1 DBG capture line"
      via: "the criterion-2 geometry verification (dot/d/plane/lat) read live from the info box"
      pattern: "d=3.6000"
---

<objective>
Close Phase 5.2: final full-gates pass on the merged tree, then the ONE human-verify checkpoint — the live feel-check of the consent UX, labeling, DBG geometry verification, and OFF-path byte-identicality — and apply any owner wording amendments as literal-only edits.

Purpose: SC5 requires a human in real Windows PyMOL; every GUI verdict in this phase lands here (01-05 decision). The checkpoint model is 5.1-06 / 04-09 (numbered steps, per-step EXPECT lines, PASS/FAIL verdict table).
Output: green gates, an approved feel-check verdict table, owner-finalized literals.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md
@.planning/phases/5.1-game-speed-difficulty/5.1-06-PLAN.md
@.planning/phases/05-stacking-game-rules/05-16-SUMMARY.md

Key facts for this plan:
- **Checkpoint precedents:** 5.1-06 (closest model — gates task + ONE blocking checkpoint + conditional literal-application task) and 04-09 (12-step EXPECT-line checklist + verdict table). Per-item approvals ACCUMULATE across rounds if wording iterates (05-16 model).
- **DBG procedure (from the Phase-5 handoff record):** launch fresh Windows PyMOL with `set SRP_DEBUG=1` in the shell FIRST (cmd.exe: `set SRP_DEBUG=1` then launch PyMOL via the usual route); the DBG capture lines print to the plugin info box. See 05-16-SUMMARY for the tracer contract.
- **Upload test files:** `tmp/upload_test/` holds the human-checkpoint files (accept_naphthalene.sdf, accept_benzene.mol2 — ring-bearing; reject_4_rings.sdf, reject_no_h.sdf — gate-rejected, NOT usable for the ring-less skip case). For the ring-less skip case, upload ANY acyclic/5-ring molecule SDF the owner has locally (any small molecule without a 6-ring ring-cycle, e.g. an alkane/ether chain) — regenerating fixture files is NOT required; the owner picks a file.
- **DRAFT literals under approval** (all pinned in tests as proposed; owner may amend any — amendments are literal-only edits + pin updates): group title 'Generic stacking'; checkbox label; disclosure sub-line; entry name 'generic pi-stack (illustrative geometry - user-approved)'; entry explanation; consent note 'generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]'; consent-ON zero-stackable variant line; skip reason 'no aromatic ring for generic pi-stack'; recap decoration ' (generic pi-stack)'.
- **Accepted side effects to surface (not fix):** toggling consent re-seeds the deterministic pickup sequence (crc32 over the whole setup dict — same documented semantics as the 5.1 tier change); Save/Load BUTTONS are Phase 8 (persistence proven purely in 5.2-01/08); the C2 clause adds one Setup status-label line when nothing can stack (absorbed feature, fires in both consent states).
- Reference SYMBOLS, never line numbers, in all steps.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final full-gates pass + OFF-pin sweep on the merged tree</name>
  <files>.planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md</files>
  <action>
    On the fully merged phase tree (all waves landed):
    1. Run the FULL gate suite: `python3.6 tests/run_gates.py` — must be green.
    2. Run the explicit OFF-pin sweep and record the counts:
       `python3.6 -m unittest tests.test_placement tests.test_hud_content tests.test_phase5_integration tests.test_setloader_stack_ring tests.test_stacking_dataset tests.test_setup_logic tests.test_phase52_integration -v`
       Every pre-existing OFF pin must be green UNTOUCHED (the byte-identical-OFF invariant), plus the new 5.2 tests.
    3. Record in the SUMMARY: gate verdict, test count delta vs the 5.1 baseline (678), the OFF-pin sweep result.
    Commit (if anything needed fixing): `fix(5.2-09): <symbol-level fix>` else proceed.
  </action>
  <verify>python3.6 tests/run_gates.py green; the OFF-pin sweep command exits green with zero pre-existing-pin failures</verify>
  <done>The merged tree is gate-green with the OFF invariant explicitly re-proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — live feel-check of consent UX + labeling + DBG geometry (SC5)</name>
  <files>.planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md</files>
  <what-built>
    The complete STACK-06 feature: Setup-tab 'Generic stacking' consent checkbox (OFF default) with disclosure sub-line; consent-gated in-memory dataset overlay anchored at Apply; consent-aware capture restamp + SKIP_GENERIC_NO_RING taxonomy class; once-per-run consent disclosure note; generic-labeled per-capture info line and recap groups; absorbed C2 zero-stackable status clause; DATA_SOURCES.md generic-mode entry. Full gates + pure integration chain green (Task 1).
  </what-built>
  <how-to-verify>
    In REAL Windows PyMOL 2.5.0 (plugin loaded; for DBG steps launch with `set SRP_DEBUG=1` in the shell first). Record a PASS/FAIL verdict per step; wording amendments accumulate across rounds (only re-test what changes).

    1. FRESH STATE / CONTROL VISIBILITY: open the Setup tab. EXPECT: a 'Generic stacking' group sits BELOW the Speed group; one checkbox labeled 'Allow generic pi-stack for uploads (illustrative geometry - user-approved)'; a disclosure sub-line mentioning the reused Set A geometry (3.60 A @ 20 deg); checkbox UNCHECKED by default.
    2. TOGGLE + PERSISTENCE WIRING: toggle ON then OFF. EXPECT: no errors; the status label refreshes ('ready' or unchanged); nothing pops up modal (inline control only). Toggle ON, Apply, switch tabs away and back. EXPECT: checkbox still ON (state round-trip). (Full save/load persistence is pure-layer proven until Phase 8 buttons exist.)
    3. CONSENT OFF REGRESSION (SC3 live): leave consent OFF; upload `tmp/upload_test/accept_naphthalene.sdf` (ring-bearing); Apply; Start; steer into a pickup. EXPECT: the info box logs 'skipped <name>: no verified stacking entry for this molecule (no invented chemistry)' (repeats coalesce as '(xN)'); a 'this set has no stacking entries - demonstration mode: ...' note at run start; NO generic-consent line anywhere; no placement occurs.
    4. C2 CLAUSE (absorbed seam): with consent OFF, upload a ring-less molecule SDF; Apply. EXPECT: the Apply status line now ALSO carries the zero-stackable clause ('this set has no stacking entries - demonstration mode: practice steering; only a crash ends the run') — new absorbed advisory, same wording source as the in-game note.
    5. CONSENT ON / RING-BEARING PLACEMENT (SC2 live): set consent ON; upload accept_naphthalene.sdf again; Apply; Start (SRP_DEBUG=1 session). EXPECT at run start: ONE line 'generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]' (and NO demo-mode note). On capture: '+ <name>: generic pi-stack (illustrative geometry - user-approved), 3.38 A plane gap (centroid 3.60 A @ 20.0 deg off-normal) - <explanation> [Janiak 2000]'. DBG capture line: EXPECT exactly `dot=1.000000 d=3.6000 plane=3.3830 lat=1.2310` (values, not formatting).
    6. UPLOAD-ONLY WIN (SC2): set Win cap to 2-3; keep capturing the same species. EXPECT: the run reaches 'won'; the completion recap shows 'stacked <N>x <name> (generic pi-stack) at 3.38 A plane gap [Janiak 2000]' — a DISTINCT labeled group.
    7. CONSENT ON / RING-LESS UPLOAD (SC3): with consent still ON, upload a ring-less molecule SDF (owner-provided acyclic/5-ring file); capture. EXPECT: 'skipped <name>: no aromatic ring for generic pi-stack'; recap line 'skipped <N>x <name>: no aromatic ring for generic pi-stack'.
    8. DEMO-SET UNCHANGED (SC3 integrity): switch to the demo set (set_a), consent ON, play briefly. EXPECT: dataset behavior byte-identical to pre-5.2 — placement lines read 'pi-pi stacking (parallel-displaced)' (NOT the generic label); no consent-driven change to demo molecules.
    9. RE-SEED DISCLOSURE: toggle consent and Restart. EXPECT: the pickup layout may differ from the previous run (accepted crc32(setup) re-seed — acknowledged, not a defect).
    10. WORDING APPROVAL: owner approves or amends EVERY DRAFT literal listed in the context block. Also confirm: (a) Save/Load buttons stay Phase 8 (persistence purely proven); (b) the C2 clause wording; (c) DATA_SOURCES.md §2 generic-mode entry reads correctly (docs glance).
    11. HEAD PARITY (EQ-engine-8 confirmation): observe the uploaded HEAD with consent ON. EXPECT: a ring-bearing upload head renders edge-on exactly as in Phase 5 (no head-level consent effect); a ring-less head renders as-stored.

    Verdict: PASS/FAIL table over steps 1-11 + the final approved literals.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with amended wording literals) or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply owner-amended wording (conditional, literal-only)</name>
  <files>serpentrum/generic_stack.py, serpentrum/hud_logic.py, serpentrum/gui_setup.py, serpentrum/data/DATA_SOURCES.md, tests/test_generic_stack.py, tests/test_hud_content.py</files>
  <action>
    IF the owner approved all DRAFT literals unchanged: no code change — record "literals approved as drafted" in the SUMMARY and stop.

    IF the owner amended any literal: apply literal-ONLY edits (no logic, no structure):
    - serpentrum/generic_stack.py: GENERIC_INTERACTION 'name'/'explanation' (if amended).
    - serpentrum/hud_logic.py: generic_consent_note line, stack_mode_note consent-ON variant line, _REASON_TEXT SKIP_GENERIC_NO_RING text (if amended).
    - serpentrum/gui_setup.py: group title / checkbox label / disclosure sub-line (if amended).
    - serpentrum/data/DATA_SOURCES.md: the quoted in-game literal + prose (keep no-new-claims discipline).
    - tests/test_generic_stack.py + tests/test_hud_content.py: update ONLY the literal pins for amended strings (ASCII rule still applies to in-game strings; no f-strings).
    Re-run the full gates. Commit: `feat(5.2-09): owner-approved generic-stacking wording (feel-check)` — or `docs(5.2-09): record approved literals` if only the SUMMARY changed.
  </action>
  <verify>python3.6 tests/run_gates.py green after the literal edits (or a no-op confirmation if unchanged)</verify>
  <done>The shipped literals are exactly what the owner approved; all pins consistent; gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green on the completed phase (final bar).
- The feel-check verdict table exists in the SUMMARY with per-step verdicts and the final literals.
- Cross-checks against the phase success criteria: SC1 (steps 1-2), SC2 (steps 5-6 incl. DBG line), SC3 (steps 3-4, 7-8), SC4 (steps 6-7 + docs glance in 10), SC5 (this checkpoint).
</verification>

<success_criteria>
- All gates green; the human feel-check APPROVED with owner-finalized literals; the DBG capture line verifies the approved geometry live; OFF-path byte-identicality confirmed live (step 3).
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md`
</output>
