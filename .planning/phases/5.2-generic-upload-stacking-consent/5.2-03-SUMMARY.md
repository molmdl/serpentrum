---
phase: 5.2-generic-upload-stacking-consent
plan: 03
subsystem: game-logic
tags: [placement, skip-taxonomy, generic-stack, consent, tdd, STACK-06]

# Dependency graph
requires:
  - phase: 5.2-generic-upload-stacking-consent
    provides: 5.2-02 generic_stack module (GENERIC_INTERACTION, overlay_stacking_data)
provides:
  - SKIP_GENERIC_NO_RING outcome code (consent-ON ring-less skip class, STACK-06 SC3)
  - resolve_skip id-comparison branch distinguishing generic vs set_a ring-less skips
  - 7 new generic-branch test pins + extended constants pin (37/37 placement suite green)
affects: [5.2-04 (hud_logic._REASON_TEXT entry for the new code), 5.2-06 (GUI restamp producing the tested record shapes), 5.2-09 (owner-amendable reason text)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Taxonomy sibling code: same-step branch chosen by WHICH entry matched (order unchanged)"
    - "Consent-as-data: overlay carries consent; resolve_skip stays consent-blind"

key-files:
  created: []
  modified:
    - serpentrum/placement.py (one import, one constant + comment, one branch, docstrings)
    - tests/test_placement.py (constants list + TestGenericSkipClass with 7 tests)

key-decisions:
  - "SKIP_GENERIC_NO_RING is a sibling of SKIP_NO_RING at the SAME ring-check step — taxonomy ORDER unchanged, chosen by WHICH entry matched (EQ-engine-1/EQ-setup-5)"
  - ":116 has_stack_entry pre-guard left EXACTLY as-is — OFF byte-identical + protects the stacking_data=None degradation path"
  - "Interim reason text is the hud_logic 'unclassified outcome' fallback; the human text lands in plan 5.2-04 (pinned as a test today)"

patterns-established:
  - "Generic-code reachability requires BOTH the overlay AND the restamp — neither alone flips the taxonomy"

# Metrics
duration: 3 min
completed: 2026-09-25
---

# Phase 5.2 Plan 03: SKIP_GENERIC_NO_RING Taxonomy Branch Summary

**Consented ring-less uploads now skip with a distinct `SKIP_GENERIC_NO_RING` class — sibling of `SKIP_NO_RING` chosen by which entry matched — while the consent-OFF taxonomy stays byte-identical (pre-guard untouched, all 34 pre-existing pins green unmodified).**

## Performance

- **Duration:** 3 min
- **Started:** 2026-09-25T19:55:29Z
- **Completed:** 2026-09-25T19:58:36Z
- **Tasks:** 2/2 (TDD: RED -> GREEN)
- **Files modified:** 2

## Accomplishments

- `SKIP_GENERIC_NO_RING` constant added to the placement outcome-code taxonomy (the one-taxonomy surface) with a comment pinning its sibling-of-SKIP_NO_RING semantics and OFF-unreachability.
- `resolve_skip`'s ring-check step gains exactly one id-comparison branch: no `stack_ring` + generic entry matched → `SKIP_GENERIC_NO_RING`; otherwise → `SKIP_NO_RING`. The `:116` `has_stack_entry` pre-guard, approval gate, and mode gate are all untouched.
- 7 new test pins in `TestGenericSkipClass`: ring-less-under-consent → generic code; ring-bearing-under-consent → stackable; OFF → `SKIP_NO_ENTRY` (guard path); set_a ring-less → `SKIP_NO_RING` (id-keyed branch); OFF overlay identity (dataset alone cannot flip); interim unclassified-outcome reason-text pin; full `resolve()` placed shape via the generic entry (`pi_stack_generic`, citation `Janiak 2000`, 3.6000 Å geometry fidelity).
- SC3 placed half verified end-to-end: consent-ON ring-bearing upload resolves `'placed'` through the generic entry at the reused DATA-02 geometry.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — failing tests for the generic ring-less skip class** - `7602f42` (test)
2. **Task 2: GREEN — SKIP_GENERIC_NO_RING constant + branch** - `daf0c29` (feat)

**Plan metadata:** `docs(5.2-03)` commit below

## Files Created/Modified

- `serpentrum/placement.py` — one import (`generic_stack`), one constant + block comment, one branch in `resolve_skip`, module + docstring taxonomy updates (+21/-5)
- `tests/test_placement.py` — constants list gains `SKIP_GENERIC_NO_RING`; new `TestGenericSkipClass` (7 tests) exercising overlay + restamp shapes (+110/-1)

## Decisions Made

None - followed plan as specified (implementation shape, constant placement, docstring wording, and branch form all resolved by the plan's context block; executed verbatim).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. RED failed exactly as predicted (3 AttributeErrors on the missing constant, zero pre-existing failures); GREEN passed on the first run.

## Verification

- `python3.6 tests/run_gates.py` — all gates green (syntax + plugin-path safety, AST purity, scoped unittest).
- OFF-path byte-identical sweep: `python3.6 -m unittest tests.test_placement tests.test_phase5_integration tests.test_setloader_stack_ring tests.test_stacking_dataset` — 73/73 green, zero edits to pre-existing assertions.
- `grep -n "has_stack_entry" serpentrum/placement.py` — only docstring mentions + the single `:130` guard read; no new reads added.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **5.2-04** owns the human-readable reason text: `hud_logic._REASON_TEXT[SKIP_GENERIC_NO_RING]` (DRAFT 'no aromatic ring for generic pi-stack', owner-amendable at 5.2-09) — the interim unclassified-outcome fallback is pinned in `test_new_reason_code_not_yet_in_reason_text_is_fine` as a clean diff target.
- **5.2-06** owns the GUI controller restamp that produces the consent-ON record shapes these tests model (`has_stack_entry=True` via `generic_stack.has_stack_entry_for` over the overlay).
- No blockers.

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
