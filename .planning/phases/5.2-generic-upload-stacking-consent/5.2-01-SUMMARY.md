---
phase: 5.2-generic-upload-stacking-consent
plan: 01
subsystem: schema
tags: [setup-logic, pure-module, json-schema, backcompat, consent, stack-06, tdd]

# Dependency graph
requires:
  - phase: 5.1-game-speed-difficulty
    provides: merge_defaults absent-key backcompat seam (docstring explicitly names Phase 5.2 reuse), EXPECTED_DEFAULTS reviewable-diff pin pattern, no-SCHEMA_VERSION-bump precedent
provides:
  - setup_logic DEFAULTS key 'generic_stack_consent' (bool, default False = OFF) — the ONE schema key every 5.2 consumer reads via setup.get(key, DEFAULTS[key])
  - validate() bool type-check for the consent key with missing-key-passes semantics (explicit non-bool errors, never-raises contract untouched)
  - Consent-ON save/load round-trip proven at the pure layer (Phase 8 buttons inherit for free)
affects: [5.2 setup-tab checkbox, 5.2 overlay builder, 5.2 GameTab consent note, 5.2 consent-aware skip/warning logic, Phase 8 save/load buttons]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Absent-key backcompat: bool consent key slots into the 5.1-02 defaults-overlay seam with zero new code (absent key = OFF)"
    - "Bool validation is the INVERSE of the _is_number bool trap: consent check requires isinstance(v, bool); speed checks reject bools"
    - "Safety-relevant opt-ins validate strictly at the schema layer (truthy non-bool 'yes' from hand-edited files must error, not silently enable) — accept-as-is (5.1-02 speed precedent) does NOT apply to consent"

key-files:
  created: []
  modified:
    - serpentrum/setup_logic.py
    - tests/test_setup_logic.py

key-decisions:
  - "Save/Load BUTTONS stay Phase 8 (EQ-setup-1) — persistence proven at the pure layer (round-trip + merge_defaults backcompat), no production merge_defaults caller needed today"
  - "validate() GAINS one additive bool check (EQ-setup-2) — consent is safety-relevant; a truthy non-bool silently enabling it is a real hazard; never-raises contract untouched"
  - "Key named 'generic_stack_consent', default False (OFF) — locked design decision"
  - "No SCHEMA_VERSION bump — v=1 hard-reject untouched; backcompat is free via the defaults-overlay seam"

patterns-established:
  - "OPT-IN consent keys: OFF default in DEFAULTS + missing-key-passes/explicit-non-bool-errors validate semantics + pure-layer round-trip pin"

# Metrics
duration: 4min
completed: 2026-09-25
---

# Phase 5.2 Plan 01: generic_stack_consent Schema Key Summary

**Consent schema key `generic_stack_consent` (bool, default False = OFF) landed in setup_logic DEFAULTS as the 10th key, with an additive validate() bool type-check (missing-key-passes, explicit non-bool errors) — the pure schema half of STACK-06 SC1, zero SCHEMA_VERSION churn.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-09-25T19:46:22Z
- **Completed:** 2026-09-25T19:49:55Z
- **Tasks:** 2 (TDD RED + GREEN; no REFACTOR needed)
- **Files modified:** 2

## Accomplishments

- `DEFAULTS` gains `'generic_stack_consent': False` as the 10th key (after `'speed'`) with a STACK-06 comment — the single schema key every downstream 5.2 consumer reads via `setup.get(key, DEFAULTS[key])`.
- `validate()` gains one additive check after head_molecule (before the warnings block): `generic_stack_consent %r must be a boolean` when the key is present but not a bool; a missing key PASSES (absent = OFF backcompat), an explicit `None` FAILS. Never-raises contract untouched (weird values like `object()` return errors as data).
- Absent-key backcompat reuses the 5.1-02 `merge_defaults` seam exactly as its docstring promised: a 9-key pre-5.2 dict merges to consent False and validates clean; consent-ON save/load round-trips True at the pure layer (Phase 8 buttons inherit for free).
- EXPECTED_DEFAULTS pin updated to 10 keys — the updated pin IS the reviewable schema diff (house pattern from 5.1-02).
- Full gates green: 685 unittests PASS; gates 1-3 (syntax + plugin-path, AST purity, unittest) all PASS. SCHEMA_VERSION stays 1.

## Task Commits

Each task was committed atomically (TDD RED → GREEN):

1. **RED: failing tests for generic_stack_consent schema key** - `7dae522` (test)
   - EXPECTED_DEFAULTS gained the key (fails `test_new_setup_returns_documented_defaults`); new `TestGenericStackConsent` class (7 tests: default OFF identity, bool-accept matrix, non-bool reject for 'yes'/1/None/0, missing-key-passes vs explicit-None-fails, absent-key merge-to-OFF + clean validate, consent-ON round-trip, never-raises weird-value pin). 6 expected failures; all other tests stayed green. `test_validate_accepts_bool_true_and_false` and `test_round_trip_preserves_true` passed already as decision-protecting pins.
2. **GREEN: generic_stack_consent schema key + bool validation** - `a0ada3f` (feat)
   - All 68 file-suite tests pass; full gate suite green at 685.

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `serpentrum/setup_logic.py` — DEFAULTS 10th key with STACK-06 comment; validate() bool check after head_molecule with an inline comment recording the safety-rationale and the inverse-of-`_is_number` bool-trap framing; validate() docstring errors list extended with missing-key-passes semantics. SCHEMA_VERSION, merge_defaults, save_setup, load_setup, SPEED_TIERS, BOX_PRESETS, KNOWN_SETS untouched.
- `tests/test_setup_logic.py` — EXPECTED_DEFAULTS pin (10 keys w/ STACK-06 comment); new `TestGenericStackConsent` class; module docstring scope line extended. All byte-identical-OFF pins untouched and green (merge_defaults matrix, save/load round-trips, never-raises matrix).

## Decisions Made

None beyond the plan's resolved decisions — all honored exactly:

- Save/Load BUTTONS stay Phase 8 (EQ-setup-1): pure-layer persistence proof only.
- validate() gained the additive bool check (EQ-setup-2) with missing-key-passes semantics.
- Key name `generic_stack_consent`, default False; no SCHEMA_VERSION bump.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Downstream consumers (Setup-tab checkbox, overlay builder, GameTab consent note) can all read `setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])` — the 5.1 injection-point shape.
- crc32 whole-dict spawn re-seed note (research §16): toggling consent changes the deterministic pickup sequence on next Start/Restart — flag at the 5.2-09 feel-check.
- No blockers.

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
