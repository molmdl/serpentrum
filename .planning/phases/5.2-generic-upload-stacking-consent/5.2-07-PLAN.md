---
phase: 5.2-generic-upload-stacking-consent
plan: 07
type: execute
wave: 4
depends_on: ["5.2-02", "5.2-04"]
files_modified:
  - serpentrum/data/DATA_SOURCES.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "DATA_SOURCES.md describes the generic upload stacking mode: consent-gated, reuses the ALREADY-APPROVED idealized Set-A geometry (3.60 A @ 20 deg), labeled illustrative/user-approved, citing [JAN2000] with its SCOPE CAVEAT (SC4 docs half)"
    - "The docs wording is consistent with the in-game DRAFT literals (generic pi-stack / illustrative geometry - user-approved) and stays inside the DRAFT-headed, human-approval-gated document discipline"
    - "No new chemistry claim, distance, or citation appears anywhere in the docs — the only numbers are the already-approved 3.60/3.383/1.231/20 deg set"
  artifacts:
    - path: serpentrum/data/DATA_SOURCES.md
      provides: "generic-mode description sub-entry with citation + scope-caveat framing"
      contains: "generic"
  key_links:
    - from: "serpentrum/data/DATA_SOURCES.md generic-mode entry"
      to: "serpentrum/generic_stack.py GENERIC_INTERACTION"
      via: "the docs describe exactly the code-borne entry (same geometry numbers, same citation, same framing)"
      pattern: "Janiak 2000"
---

<objective>
Update DATA_SOURCES.md with the generic upload stacking mode description — the docs half of SC4.

Purpose: the repo rule is that ALL claims/citations are verified and human-approved; the generic mode reuses the already-approved [Janiak 2000] citation and geometry, and the "illustrative — user-approved" framing IS the Phase-8-requested crystalline-context wording landing early (owner flag 2026-09-20). The docs must say exactly that, and nothing more.
Output: DATA_SOURCES.md only (DRAFT-headed stays; no dataset file changes).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md

Key facts (research setup §7 docs surfaces):
- DATA_SOURCES.md structure: DRAFT-headed (:1); status legend (:8-10); §1 molecule SDFs (:12-44); §2 interaction rule + measured distances incl. [JAN2000] (:46-124); §3 UNVERIFIED do-not-ship (:126-135); §4 precision (:137-144).
- The [JAN2000] entry (:48-76) carries the 2026-09-20 SCOPE CAVEAT (:63-75): crystalline-context corpus (metal-complex crystal structures), solution-phase NOT verified, Phase 8 TODO for explicit crystalline-state framing.
- House "idealized" wording precedent: biphenyl already "labeled idealized" (:37-38) and §3 :129-130 says biphenyl "uses the set's generic approved rule, labeled idealized" — reuse this framing.
- Dataset self-description precedent: stacking_pi_stack.json:12 "Idealized pairwise geometry - neat Set A crystals are herringbone".
- The generic-mode description belongs as a NEW SUB-ENTRY in §2 (after the [JAN2000] block) — NOT in §3 (§3 is the do-not-ship list; the generic mode SHIPS, consent-gated).

RESOLVED DECISIONS owned by this plan:
- EQ-setup-4 (idle tips): DEAD surface — hud_logic.idle_tip has no live caller; do NOT mention or wire idle tips in the docs. The live in-game explanation channels are the info-box lines + Setup status_label.
- Docs placement: §2 sub-entry titled for the generic mode (DRAFT heading: `Generic upload stacking mode (Phase 5.2, consent-gated)`), stating: (a) what it does — uploads carrying a canonical planar 6-ring can stack at the Set-A idealized geometry when the user opts in on the Setup tab; (b) that the geometry is REUSED, not re-measured — the same 3.60 A @ 20 deg encoding (3.383/1.231) as the set_a entry; (c) the label — "illustrative geometry - user-approved" appears on every in-game line; (d) consent is OFF by default and explicit; (e) ring-less uploads still skip with a reason; (f) scope — the [JAN2000] SCOPE CAVEAT applies unchanged (crystalline-context corpus; solution-phase not verified); the consent framing is the explicit-crystalline-state acknowledgment Phase 8 formalizes.
- No new numbers/claims: the ONLY permitted numerals are 3.60, 3.383, 1.231, 20 (all already approved via DATA-02).
- ASCII in docs is NOT required (docs already use UTF-8 prose), but keep the in-game literal quotes ASCII-faithful ('generic pi-stack (illustrative geometry - user-approved)') so docs and HUD strings match grep-for-grep.
</context>

<tasks>

<task type="auto">
  <name>Task 1: §2 generic-mode sub-entry</name>
  <files>serpentrum/data/DATA_SOURCES.md</files>
  <action>
    1. Read DATA_SOURCES.md fully first (it is the human-approval contract — edit surgically, add nothing beyond the scope below).
    2. In §2, immediately AFTER the [JAN2000] entry block (so the caveat reads as applying to the new mode too), add the sub-entry per the context spec (6 points a-f). Keep it ~10-14 lines of prose. Cross-reference the [JAN2000] anchor and the SCOPE CAVEAT explicitly. State the OFF default and the Setup-tab control location ('Generic stacking' group). Quote the in-game label literal once, ASCII-faithful.
    3. Update the §2 heading intro sentence ONLY if it enumerates contents (read first; if it does, add the sub-entry to the enumeration; if not, leave it).
    4. Do NOT touch: §1, §3, §4, the status legend, the DRAFT header, any existing entry, any DOI/number.
    5. Run the full gates `python3.6 tests/run_gates.py` (docs changes must not affect anything — sanity gate pass). Commit: `docs(5.2-07): DATA_SOURCES generic upload stacking mode (STACK-06 SC4)`
  </action>
  <verify>git diff serpentrum/data/DATA_SOURCES.md shows ONLY the new sub-entry (+ enumeration line if applicable); `grep -c "3.383\|1.231" serpentrum/data/DATA_SOURCES.md` does not decrease; python3.6 tests/run_gates.py green</verify>
  <done>The generic mode is documented with the approved citation, the reused-geometry statement, the consent/labeling framing, and the scope caveat — no new claims.</done>
</task>

<task type="auto">
  <name>Task 2: Docs ↔ in-game literal consistency check</name>
  <files>serpentrum/data/DATA_SOURCES.md</files>
  <action>
    1. Cross-check every literal the docs quote against the live tree (grep, not memory):
       - `generic pi-stack (illustrative geometry - user-approved)` — must appear in serpentrum/generic_stack.py (GENERIC_INTERACTION['name']).
       - `generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]` — serpentrum/hud_logic.py (generic_consent_note).
       - `no aromatic ring for generic pi-stack` — serpentrum/hud_logic.py (_REASON_TEXT).
       - `Generic stacking` (group title) — serpentrum/gui_setup.py.
    2. Fix ANY mismatch by aligning the DOCS to the code literals (code literals are the test-pinned source of truth; wording amendments happen only at the 5.2-09 feel-check, which would then update both).
    3. Verify no new chemistry claims: grep the new sub-entry for numerals — only 3.60/3.383/1.231/20 permitted.
    4. Commit (if fixes were needed): `docs(5.2-07): align generic-mode wording with pinned in-game literals` — else no commit (Task 1's commit stands).
  </action>
  <verify>each of the four greps in step 1 finds the literal in BOTH the docs quote and the named source file; numerals check passes</verify>
  <done>Docs and code literals agree grep-for-grep; zero unapproved claims.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `git status` shows serpentrum/data/stacking_pi_stack.json unmodified.
- The §2 sub-entry exists, cites [JAN2000], mentions the SCOPE CAVEAT, and quotes the ASCII in-game label.
</verification>

<success_criteria>
- SC4 docs half: DATA_SOURCES.md describes the generic mode accurately with the approved citation and scope framing; idle tips untouched (dead surface).
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-07-SUMMARY.md`
</output>
