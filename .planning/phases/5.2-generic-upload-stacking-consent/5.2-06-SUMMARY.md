---
phase: 5.2-generic-upload-stacking-consent
plan: 06
subsystem: game-rules
tags: [gui_game, consent, generic-pi-stack, capture-path, hud, recap, begin_game]

# Dependency graph
requires:
  - phase: 5.2-02
    provides: serpentrum/generic_stack.py - GENERIC_INTERACTION, overlay_stacking_data, history_name, has_stack_entry_for
  - phase: 5.2-03
    provides: placement.SKIP_GENERIC_NO_RING consent-ON ring-less taxonomy code
  - phase: 5.2-04
    provides: hud_logic._REASON_TEXT generic literal, stack_mode_note(records, consent) consent-aware builder, generic_consent_note(consent) None-when-OFF builder
  - phase: 5.1-05
    provides: the once-per-run notes block + one-shot source-scan pin precedent (speed_note)
provides:
  - once-per-run consent read in begin_game (DEFAULTS fallback) threading into stack_mode_note(records, consent)
  - once-per-run generic-consent disclosure line between the mode note and the speed note (None when OFF - zero new lines/log-order drift when OFF)
  - consent-aware has_stack_entry restamp at the capture seam via generic_stack.has_stack_entry_for (the ONE consent-awareness touchpoint in the capture path; placement.py untouched)
  - generic-labeled stacked_history enrollment via generic_stack.history_name (SC4 recap grouping)
  - one-shot source-scan pin for generic_consent_note call-site count
affects: [5.2-07 (setup-side overlay anchoring consumes the same consent carrier), 5.2-09 (feel-check human-verify of the disclosure line + consent-ON flow)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Controller restamp: consent-awareness expressed by re-deriving the record flag from the anchored dataset at the capture seam, leaving the pinned taxonomy module untouched"
    - "Consent-carrier dataset: the anchored stacking_data (overlay vs original, anchored at Apply) is the single source of consent; consumers just re-run interaction_for"
    - "One-shot call-site pins: source.count('<fn>(') == 1 extended to generic_consent_note (third such pin after stack_mode_note/speed_note)"

key-files:
  created: []
  modified:
    - serpentrum/gui_game.py
    - tests/test_hud_content.py

key-decisions:
  - "EQ-engine-2 resolved as option (i): controller restamp via the pure helper generic_stack.has_stack_entry_for - placement.py never edited, every taxonomy pin stays green by construction"
  - "EQ-engine-6/EQ-setup-7: recap authorship decorates ONLY the history name (history_name); breakdown_lines untouched; interaction_id stays stored"
  - "EQ-engine-7: consent read ONCE per begin_game and passed to stack_mode_note; disclosure line slots between the mode note and the speed note so speed stays always-last-before-Get-ready (zero order drift when OFF)"
  - "EQ-engine-8: head eligibility confirmed UNCHANGED by reading (_build_head_state branches on stack_ring presence only) - zero code"

patterns-established:
  - "OFF byte-identity by construction: consent OFF makes the restamp a no-op (interaction_for('__upload__') -> None on the unmodified dataset), the disclosure line None, the mode note the original C1 line, and the enrolled name undecorated"

# Metrics
duration: 7min
completed: 2026-09-25
---

# Phase 5.2 Plan 06: GameTab Consent Wiring Summary

**Runtime half of the generic upload stacking consent (SC2/SC3/SC4): begin_game reads the consent flag once per run, logs the once-per-run disclosure line between the mode and speed notes, and the capture seam restamps record stackability from the anchored overlay via the pure generic_stack helper - generic placements enroll recap names decorated '(generic pi-stack)' with placement.py, the taxonomy, and head eligibility all untouched.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-09-25T20:10:12Z
- **Completed:** 2026-09-25T20:16:51Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments

- **Once-per-run consent read + disclosure (SC2):** `begin_game` reads `consent = setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])` once, passes it to the consent-aware `hud_logic.stack_mode_note(records, consent)` at the single pinned call site, and logs `hud_logic.generic_consent_note(consent)` between the mode note and the speed note. None when OFF: OFF runs gain zero lines and the log order is byte-identical (speed note stays always-last-before-'Get ready...'). The begin_game docstring records that Restart replays the anchor's setup dict so the consent state and the anchored overlay stay in sync.
- **Consent-aware capture restamp (SC2/SC3):** the enrichment block in `_handle_stack_event` stamps 'set' first, then restamps `resolve_rec['has_stack_entry'] = generic_stack.has_stack_entry_for(record, stacking_data)` against the ANCHORED stacking_data (the consent carrier). OFF reduces to the load-time value by definition; the None-dataset degradation falls back to the load-time flag inside the helper. Consent-ON ring-less uploads fall through to SKIP_GENERIC_NO_RING with the taxonomy order unchanged; placement.py has zero edits.
- **Generic-labeled recap enrollment (SC4):** the stacked_history enrollment now stores `'name': generic_stack.history_name(name, outcome['interaction'])`, grouping generic placements under `'<molecule> (generic pi-stack)'` distinct from dataset groups - breakdown_lines untouched, interaction_id still stored.
- **EQ-engine-8 confirmation (no code):** read `_build_head_state`/`pymol_bridge._select_head_record` - head eligibility branches on `'stack_ring' in record` presence only; the consent flag has zero head-level effect. Upload heads with rings are already edge-on; ring-less degrade flat. Documented, nothing changed.
- **One-shot pin extended:** `test_begin_game_has_exactly_one_generic_consent_call_site` (source.count('generic_consent_note(') == 1) mirrors the stack_mode_note/speed_note pins; all three now green together.

## Task Commits

Each task was committed atomically (branch `exec/5.2-06`):

1. **Task 1: begin_game consent read + notes block** - `2e09707` (feat)
2. **Task 2: Capture-path restamp + generic-labeled enrollment** - `5e273b1` (feat)

## Files Created/Modified

- `serpentrum/gui_game.py` - generic_stack import (alphabetical, after game_engine); consent read + consent-aware mode note + disclosure line in begin_game; docstring narrations (begin_game consent line + Restart sync; _handle_stack_event 5.2 restamp touchpoint paragraph); enrichment reorder + consent-aware restamp; decorated stacked_history enrollment
- `tests/test_hud_content.py` - `test_begin_game_has_exactly_one_generic_consent_call_site` in TestGenericConsentNote

## Verification

- `python3.6 tests/run_gates.py` - **green, 721 tests** (syntax + plugin-path safety + AST purity + scoped unittests, incl. the new one-shot pin and both pre-existing one-shot scans)
- `grep -n "generic_stack" serpentrum/gui_game.py` - import + exactly three consumption sites (has_stack_entry_for, history_name; comment/docstring mentions only otherwise)
- `grep -c "stack_mode_note(" / "speed_note(" / "generic_consent_note(" serpentrum/gui_game.py` - each exactly 1
- OFF sweep: `python3.6 -m unittest tests.test_placement tests.test_hud_content tests.test_phase5_integration` - **115 tests OK**, all pure OFF pins (test_placement upload-with-ring-still-skips, test_hud_content C1/OFF matrix, test_phase5_integration s5) green UNTOUCHED
- Byte-identical-OFF statement upheld: OFF -> restamp no-op (interaction_for('__upload__') -> None on the unmodified dataset), disclosure line None, stack_mode_note the original C1 line, enrolled name undecorated. GUI-layer verdicts remain human-verify (5.2-09).

## Decisions Made

None revisited - all resolved decisions were locked by the plan (EQ-engine-2 option i controller restamp, EQ-engine-6/EQ-setup-7 name-only decoration, EQ-engine-7 notes placement, EQ-engine-8 confirm-by-reading no-change).

## Deviations from Plan

None - plan executed exactly as written. (During Task 2 a stray garbled comment fragment was introduced and immediately cleaned within the same edit pass before commit; final commentary is as planned.)

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 5.2-07 (setup-side overlay anchoring at Apply) lands the overlay the restamp already consumes; the restamp degrades gracefully (load-time fallback) until that anchor ships the overlay.
- 5.2-09 feel-check owns the GUI human-verify of the disclosure line and the consent-ON capture flow (DRAFT wordings owner-amendable there).
- No blockers.

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
