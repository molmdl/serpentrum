---
phase: 5.2-generic-upload-stacking-consent
plan: 08
type: execute
wave: 4
depends_on: ["5.2-01", "5.2-02", "5.2-03", "5.2-04"]
files_modified:
  - tests/test_phase52_integration.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The pure chain consent ON → overlay → interaction_for → resolve_skip/resolve → placed(3.383/1.231, citation 'Janiak 2000') is proven end-to-end, including the decoded 3.60 A centroid step (SC2 pure half)"
    - "The pure chain consent OFF → original dataset → upload capture skips SKIP_NO_ENTRY is re-proven at integration level (byte-identical OFF — SC3 integrity)"
    - "Absent-key schema chain: no consent key → merge_defaults → False → validate clean (SC1 backcompat, integration level)"
    - "An upload-only game with consent ON REACHES 'won' through repeated generic placements (SC2 win path, pure engine drive)"
    - "The recap line for generic placements is distinct and labeled (decorated name + [Janiak 2000]) (SC4 pure half)"
  artifacts:
    - path: tests/test_phase52_integration.py
      provides: "schema chain + overlay/taxonomy chain + placement outcome + notes chain + upload-only win scenario + recap distinction"
      min_lines: 120
  key_links:
    - from: "tests/test_phase52_integration.py win scenario"
      to: "game_engine step loop + placement.resolve with the overlay"
      via: "mirror of the test_phase5_integration drive/capture helper pattern with upload records + overlay dataset"
      pattern: "pi_stack_generic"
---

<objective>
Prove the COMPOSED 5.2 chain purely (schema → overlay → taxonomy → placement → notes → recap) and the upload-only win path — the pure-layer integration half of SC1/SC2/SC3/SC4, mirroring how 5.1-06 proved its chain without GUI.

Purpose: headless dialog assertions are impossible (01-05), so the phase's automated proof lives here; the GUI wiring verdicts belong to the 5.2-09 feel-check. This plan also runs the FULL gate suite as the pre-checkpoint green bar.
Output: tests/test_phase52_integration.py (new file) + a full-gates pass.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md

Key facts:
- House integration template: tests/test_phase51_integration.py:32-41 header (sys.path self-insert, NO __init__.py in tests/ — gate 1), fixed DT = 0.1, unittest classes.
- The drive/capture helper pattern to MIRROR (module-local copies — tests never import each other): tests/test_phase5_integration.py drive() + capture extraction (its s4 full-run pattern :242-266 and s5 upload-skip shape :454-495).
- Real molecules for fixtures: `setloader.load_upload(<path to a demo SDF>, stacking_path=setloader.default_stacking_path())` reuses a shipped demo file AS an upload (test_hud_content.py real-upload pin precedent, :560-569 region) — records carry set='__upload__', has_stack_entry=False, stack_ring present for ring-bearing species (naphthalene/benzene/anthracene/phenanthrene/biphenyl SDFs ship in serpentrum/data/).
- The restamp semantics to mirror at the pure layer: gui_game's restamp (plan 5.2-06) computes `generic_stack.has_stack_entry_for(record, stacking_data)` — in these tests, construct resolve_rec equivalents the same way (the GUI restamp itself is human-verify territory).
- Consent flag source in the chain: `setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])` after merge_defaults.
- Byte-identical-OFF assertion strategy: the OFF chain assertions coexist in this file with the ON chain (explicit False and absent-key paths); the pre-existing pins in test_placement/test_hud_content/test_phase5_integration remain the primary OFF contract — this file re-proves the COMPOSED path.

RESOLVED DECISIONS owned by this plan:
- The DBG-decode pre-verification: criterion SC2's "placed d = 3.60 Å verified by DBG capture line" is LIVE-verified at 5.2-09; this file pre-verifies the same decode PURELY — first-capture tail (head ring centroid via placement.tail_frame) vs placed ring centroid (stacking.ring_frame over outcome['placed_atoms'] + record['stack_ring']), distance assertAlmostEqual 3.60 places=3, and the plane/lateral decomposition ≈ 3.383/1.231 (the _dbg_capture_line math, hud_logic.debug_capture_trace inputs).
- Win-scenario scale: cap=2, one ring-bearing upload species (fast, deterministic); captures driven via engine.step until both 'stacked' events fired; assert result 'won', molecules_stacked == 2, len(segments) == 2, both history entries carry interaction_id 'pi_stack_generic' and decorated names.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + overlay/taxonomy + notes chain tests</name>
  <files>tests/test_phase52_integration.py</files>
  <action>
    Create tests/test_phase52_integration.py (house header, imports: json, os, sys, unittest + serpentrum setup_logic, molecule_data, setloader, generic_stack, placement, hud_logic).

    Class TestConsentSchemaChain:
    - test_absent_key_merges_to_off_and_validates: dict with the 9 pre-5.2 keys (no consent key) → json.dumps → setup_logic.load_setup → assert key absent → merge_defaults → merged['generic_stack_consent'] is False → validate(merged) no errors.
    - test_consent_true_round_trips_and_validates: new_setup + consent True → save_setup → load_setup → merge_defaults → validate clean → merged value is True.
    - test_non_bool_consent_flags_in_merged_chain: merge a dict carrying generic_stack_consent='yes' → validate reports exactly the boolean error (save_setup would refuse — never-raises validate preserved).
    - test_consent_toggle_reseeds_spawn (documentation pin): spawn.seed_from_setup differs for two setups differing ONLY in generic_stack_consent (accepted crc32 whole-dict semantics, 5.1 precedent).

    Class TestOverlayTaxonomyChain (real dataset + real upload records):
    - load real dataset once in setUp; build overlay = generic_stack.overlay_stacking_data(data, True); load_upload a ring-bearing demo SDF as an upload.
    - test_off_upload_skips_no_entry: resolve_skip(record-shape {'set': '__upload__', 'has_stack_entry': False, ...}, ORIGINAL data) == placement.SKIP_NO_ENTRY.
    - test_on_ring_bearing_upload_stackable: restamped shape (has_stack_entry = generic_stack.has_stack_entry_for(record, overlay) → True) + overlay → resolve_skip returns None.
    - test_on_ringless_upload_skips_generic: synthesize a ring-less record (strip 'stack_ring' from a deep-copied upload record; keep set/has_stack_entry=True restamped shape) → resolve_skip == placement.SKIP_GENERIC_NO_RING.
    - test_demo_records_identical_both_datasets: a set_a-shaped record resolves pi_stack_pd on original AND overlay (interaction_for ids equal).
    - test_overlay_demo_side_effect_free: for EVERY demo record in load_demo_set('set_a'), interaction_for(record, overlay) == interaction_for(record, original) (no demo leakage).

    Class TestNotesChain:
    - test_off_upload_only_note_is_c1_line: stack_mode_note(upload_records, False) == the C1 literal.
    - test_on_ring_bearing_upload_only_note_suppressed: stack_mode_note(upload_records_with_stack_ring, True) is None.
    - test_on_ringless_upload_only_note_is_variant: stack_mode_note(ringless_records, True) == the variant literal.
    - test_consent_note_pair: generic_consent_note(False) is None; generic_consent_note(True) == the disclosure literal.

    Run the file suite. Commit: `test(5.2-08): pure integration chains — schema, overlay taxonomy, notes (STACK-06)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_phase52_integration.py" -v -> all pass</verify>
  <done>SC1 backcompat + SC3 taxonomy + note contracts proven over the real shipped dataset and real upload-parsed records.</done>
</task>

<task type="auto">
  <name>Task 2: Placement outcome + upload-only win path + recap distinction</name>
  <files>tests/test_phase52_integration.py</files>
  <action>
    Class TestGenericPlacementOutcome (real benzene-or-naphthalene upload record + overlay):
    - test_resolve_places_generic: build the full resolve() argument set mirroring test_phase5_integration's s4 fixture shapes (records_by_id, head_atoms + head_stack_ring from the record, heading, empty segments, existing_atoms = head atoms, box from setup_logic.BOX_PRESETS['medium'], display_z = pymol-free constant — mirror the test file's display-z constant, do NOT import pymol); outcome['status'] == 'placed'; outcome['interaction']['id'] == 'pi_stack_generic'; outcome['interaction']['distance_a'] == 3.383; outcome['citation_short'] == 'Janiak 2000'.
    - test_placed_step_decodes_approved_geometry: recompute the placed ring frame (stacking.ring_frame over outcome['placed_atoms'] with the record's stack_ring) + the first-capture tail frame (placement.tail_frame with the same inputs resolve used); centroid distance assertAlmostEqual 3.60 (places=3); plane gap ≈ 3.383 and lateral ≈ 1.231 (places=3) — the DBG capture-line decode, proven purely.
    - test_off_same_record_still_skips: identical geometry chain but ORIGINAL dataset → resolve returns {'status': 'skipped', 'code': placement.SKIP_NO_ENTRY} (composed-path OFF re-proof).

    Class TestUploadOnlyWinPath (engine drive; mirror the test_phase5_integration drive/capture helpers as module-local functions):
    - test_upload_only_game_wins_under_consent: cap=2; head + pickups from ONE ring-bearing upload species; overlay dataset; resolve_rec built WITH the restamped has_stack_entry (has_stack_entry_for). Drive engine.step(0.1) until two 'stacked' events; resolve each through placement.resolve (skip→tail→place→gate order, the controller transcription); attach on placed (engine.attach_segment) / reject+despawn on non-placed. ASSERT: engine.result == 'won'; molecules_stacked == 2; len(engine.segments) == 2; BOTH placed outcomes carried interaction_id 'pi_stack_generic'; the composed stacked_history (built the way gui_game enrolls it, with generic_stack.history_name decorating the name) shows decorated entries; hud_logic.breakdown_lines over that history yields a line matching 'stacked 2x <name> (generic pi-stack) at 3.38 A plane gap [Janiak 2000]'.
    - test_recap_distinct_from_dataset_group: a mixed history (one set_a-molecule stacked entry with name/pi_stack_pd distance + one generic decorated entry for the SAME molecule name) → breakdown_lines yields TWO distinct stacked lines, the generic one carrying '(generic pi-stack)' and both carrying '[Janiak 2000]'.

    Then run the FULL gate suite: `python3.6 tests/run_gates.py`. If any pre-existing pin fails, fix within phase scope (symbol-level only) and re-run. Commit: `test(5.2-08): generic placement decode + upload-only win path + recap distinction (STACK-06 SC2/SC4)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_phase52_integration.py" -v -> all pass; python3.6 tests/run_gates.py -> FULL suite green</verify>
  <done>The composed chain is proven: consent ON wins an upload-only game at the approved geometry with labeled recap; consent OFF skips identically to today.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> FULL suite green (this is the pre-checkpoint bar; 5.2-09 re-runs it on the fully merged tree).
- The OFF assertions inside this file + every pre-existing pin green.
- No pymol/Qt/numpy imports anywhere in the new file (AST purity gate enforces).
</verification>

<success_criteria>
- SC1/SC2/SC3/SC4 pure halves all proven end-to-end; the win path exists at the pure layer; the OFF path is re-proven composed.
- The full suite is green with the new integration file included.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-08-SUMMARY.md`
</output>
