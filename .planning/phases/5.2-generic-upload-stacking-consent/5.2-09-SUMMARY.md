---
phase: 5.2-generic-upload-stacking-consent
plan: 09
subsystem: testing
tags: [pymol-plugin, phase-gate, human-verify, checkpoint, generic-stacking, consent-ux, stacking-game, phase-closing]

# Dependency graph
requires:
  - phase: 5.2-generic-upload-stacking-consent
    provides: 5.2-01..08 (schema consent key, Setup checkbox + disclosure, dataset overlay builder, GameTab wiring, capture restamp + SKIP_GENERIC_NO_RING taxonomy, once-per-run consent note, generic-labeled capture/recap, C2 status clause, DATA_SOURCES entry, pure integration chains)
  - phase: 05-stacking-game-rules
    provides: DBG capture tracer contract (dot/d/plane/lat lines), checkpoint verdict-table model (05-16)
  - phase: 5.1-game-speed-difficulty
    provides: feel-check checkpoint precedent (5.1-06: gates task + ONE blocking checkpoint + conditional literal-application task)
provides:
  - Phase 5.2 closing gate evidence: full gates green on the merged tree (740 tests), explicit OFF-pin sweep green (230 tests, zero pre-existing-pin failures)
  - (pending) approved feel-check verdict table + owner-finalized wording literals
affects: [phase-8 (Save/Load buttons + full DATA_SOURCES sign-off), 5.3-randomized-pickup-spawning (builds on the approved Phase-5.2 base)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "phase-closing model: gates task (auto) -> ONE blocking checkpoint:human-verify (owner in real Windows PyMOL) -> conditional literal-only wording application task"

key-files:
  created:
    - .planning/phases/5.2-generic-upload-stacking-consent/5.2-09-SUMMARY.md
  modified: []

key-decisions:
  - "(pending Task 2) owner verdict on consent UX / labeling / DBG geometry; approval or amendment of every DRAFT literal"

patterns-established: []

# Metrics
duration: in progress (checkpoint window open)
completed: (pending)
---

# Phase 5.2 Plan 09: Phase-Closing Gates + Human Checkpoint Summary

**STATUS: AWAITING HUMAN CHECKPOINT (Task 2). Task 1 complete: merged tree gate-green with the OFF invariant explicitly re-proven.**

## Performance

- **Started (Task 1):** 2026-09-25T20:26Z
- **Completed:** pending Task 2 (human checkpoint) + Task 3 (conditional literal application)
- **Tasks:** 1 of 3 complete

## Task 1 — Final full-gates pass + OFF-pin sweep on the merged tree

Full gate suite (`python3.6 tests/run_gates.py`):

| Gate | Verdict |
|------|---------|
| gate 1: syntax + plugin-path safety | PASS |
| gate 2: purity (AST) | PASS |
| gate 3: unittest (scoped discover) | PASS — 740 tests |

**Test count delta vs 5.1 baseline (678): +62** (740 total).

Explicit OFF-pin sweep:
`python3.6 -m unittest tests.test_placement tests.test_hud_content tests.test_phase5_integration tests.test_setloader_stack_ring tests.test_stacking_dataset tests.test_setup_logic tests.test_phase52_integration -v`
→ **230 tests, OK — every pre-existing OFF pin green UNTOUCHED, plus the new 5.2 tests.**

Nothing needed fixing on the merged tree; no `fix(5.2-09)` commit was required.

**VERDICT: The merged tree is gate-green with the OFF invariant explicitly re-proven.**

## Task 2 — CHECKPOINT (blocking human-verify): AWAITING OWNER

Live feel-check of the consent UX, labeling, DBG geometry verification, and OFF-path byte-identicality in real Windows PyMOL 2.5.0 (SC5). Per-step PASS/FAIL verdict table over steps 1-11 + final approved literals to be recorded here after the owner checkpoint.

(pending)

## Task 3 — Apply owner-amended wording (conditional, literal-only): AWAITING CHECKPOINT RESOLUTION

- IF all DRAFT literals approved unchanged: record "literals approved as drafted" — no code change.
- IF any literal amended: literal-only edits to generic_stack.py / hud_logic.py / gui_setup.py / DATA_SOURCES.md + pin-only test updates; full gates re-run; commit `feat(5.2-09)` (or `docs(5.2-09)` if SUMMARY-only).

(pending)

## DRAFT literals under approval

Pinned in tests as proposed; owner may amend any (amendments = literal-only edits + pin updates):

- group title 'Generic stacking'
- checkbox label 'Allow generic pi-stack for uploads (illustrative geometry - user-approved)'
- disclosure sub-line (reused Set A geometry 3.60 A @ 20 deg)
- entry name 'generic pi-stack (illustrative geometry - user-approved)'
- entry explanation
- consent note 'generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]'
- consent-ON zero-stackable variant line
- skip reason 'no aromatic ring for generic pi-stack'
- recap decoration ' (generic pi-stack)'

## Accepted side effects (surface, not fix)

- Toggling consent re-seeds the deterministic pickup sequence (crc32 over the whole setup dict — same documented semantics as the 5.1 tier change).
- Save/Load BUTTONS are Phase 8.
- The C2 clause adds one Setup status-label line when nothing can stack (absorbed feature, fires in both consent states).

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: (pending)*
