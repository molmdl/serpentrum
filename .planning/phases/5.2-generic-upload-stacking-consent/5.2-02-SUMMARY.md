---
phase: 5.2-generic-upload-stacking-consent
plan: 02
subsystem: data
tags: [pi-stack, consent, overlay, dataset, pure-module, tdd]

requires:
  - phase: 02-stacking-dataset
    provides: human-approved DATA-02 pi-stack geometry (3.383/1.231) + janiak2000 approved citation
  - phase: 03-upload-sentinel
    provides: '__upload__' sentinel + has_stack_entry load-time flag (STACK-03 skip-policy keying)
provides:
  - GENERIC_INTERACTION constant (code-borne generic entry, approved geometry reused verbatim)
  - overlay_stacking_data consent-gated in-memory dataset overlay builder
  - history_name recap-grouping decoration (' (generic pi-stack)')
  - has_stack_entry_for consent-blind stackability helper (pure half of restamping)
affects: [5.2-generic-upload-stacking-consent (plans 03/04/06 consume these symbols)]

tech-stack:
  added: []
  patterns:
    - "Consent-as-data: policy expressed via in-memory dataset overlay through the existing interaction_for lookup — downstream consumers unchanged"
    - "Overlay OFF = object identity (zero-copy); ON = deepcopy + non-shadowing append-last"

key-files:
  created:
    - serpentrum/generic_stack.py
    - tests/test_generic_stack.py
  modified: []

key-decisions:
  - "Generic entry id 'pi_stack_generic', applies_to ['__upload__'] only — demo sets unreachable, set_a still first-matches shipped pi_stack_pd"
  - "status 'APPROVED' is the structural placement gate; the human-approval claim itself is the reused DATA-02 geometry + approved citation"
  - "ASCII 'pi-stack' label alphabet in all code/HUD literals (EQ-engine-5); pi glyph only in planning docs"
  - "Recap distinction via history_name() decoration — zero breakdown_lines edits (EQ-engine-6/EQ-setup-7)"

patterns-established:
  - "Consent carrier = anchored dataset: helpers recompute interaction_for against it; None dataset falls back to load-time flags"

duration: 4min
completed: 2026-09-25
---

# Phase 5.2 Plan 02: generic_stack module Summary

**Pure generic-stack module: code-borne generic entry reusing the approved DATA-02 geometry verbatim, consent-gated in-memory dataset overlay, recap name decorator, and consent-blind stackability helper — the data half of STACK-06.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-25T19:46:35Z
- **Completed:** 2026-09-25T19:50:20Z
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 2 created, 0 existing touched

## Accomplishments

- `GENERIC_INTERACTION` pins the exact approved geometry (distance_a 3.383 / lateral_offset_a 1.231 → 3.60 A @ 20.0 deg off-normal, asserted numerically in tests), cites only the already-approved `janiak2000`, targets ONLY `['__upload__']`, and carries honest illustrative-geometry labeling in ASCII.
- `overlay_stacking_data` implements the consent-as-data mechanism: OFF returns the same object (zero-copy, byte-identical to today), ON returns a deepcopy with the generic entry appended LAST so first-match order shadows nothing; None-safe in both consent states; the appended entry is a deepcopy so the module constant can never be corrupted through it.
- `history_name` (recap grouping, SC4) and `has_stack_entry_for` (pure half of consent-aware restamping, EQ-engine-2) are pinned with 16 behavioral tests.
- Full gates green: 694 unittests, syntax + plugin-path safety, AST purity (new module classified PURE).

## Task Commits

Each task was committed atomically (TDD cycle):

1. **Task 1: RED — failing tests for generic_stack** - `6b7ab11` (test)
2. **Task 2: GREEN — implement serpentrum/generic_stack.py** - `3a5ae52` (feat)

## Files Created/Modified

- `serpentrum/generic_stack.py` — generic entry constant, overlay builder, recap name decorator, stackability helper (PURE: stdlib copy + molecule_data)
- `tests/test_generic_stack.py` — 16 tests across 4 classes mirroring molecule_data field rules and pinning all behaviors

## Decisions Made

None beyond plan-pinned literals — plan executed exactly as specified. All resolved decisions (builder home, label alphabet, recap mechanism, helper semantics, DRAFT literals) were already owned by the plan and were implemented verbatim.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 5.2-03/04/06 can build directly on `GENERIC_INTERACTION`, `overlay_stacking_data`, `history_name`, and `has_stack_entry_for` — the success criterion ("downstream machinery consumes the entry with zero changes") is structurally proven by the overlay tests round-tripping through the real shipped dataset.
- Byte-identical-OFF pins all still green: stacking dataset tests (exactly-one-APPROVED shipped entry), placement upload → SKIP_NO_ENTRY, phase-5 integration, setloader stack-ring suite — the JSON was never touched.
- DRAFT literals (name/explanation/suffix) are owner-amendable at the 5.2-09 feel-check.

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
