---
phase: 5.2-generic-upload-stacking-consent
plan: 04
type: tdd
wave: 3
depends_on: ["5.2-03"]
files_modified:
  - serpentrum/hud_logic.py
  - tests/test_hud_content.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "SKIP_GENERIC_NO_RING renders 'no aromatic ring for generic pi-stack' (SC3's specific reason class, ASCII)"
    - "stack_mode_note(records) with consent omitted/unspecified is byte-identical to today (C1 line + None matrix unchanged — the checkpoint-pinned OFF contract)"
    - "stack_mode_note(records, consent=True) suppresses the demo-mode note for ring-bearing upload pools and returns a consent-variant line for ring-less upload pools (the two once-per-run lines can never contradict — single wording source)"
    - "generic_consent_note(False) is None (OFF runs gain zero log lines — byte-identical log order); generic_consent_note(True) returns the labeled disclosure line"
  artifacts:
    - path: serpentrum/hud_logic.py
      provides: "_REASON_TEXT entry for SKIP_GENERIC_NO_RING + stack_mode_note consent param + _any_stackable predicate + generic_consent_note builder"
      contains: "generic_consent_note"
    - path: tests/test_hud_content.py
      provides: "exact-wording pins for all three surfaces + OFF-contract re-pins + recap prefix pin for the new code"
      contains: "generic pi-stack"
  key_links:
    - from: "serpentrum/hud_logic.py _REASON_TEXT"
      to: "serpentrum/placement.py SKIP_GENERIC_NO_RING"
      via: "outcome-code import (one taxonomy, two consumers — hud_logic.py:44-48 rule)"
      pattern: "SKIP_GENERIC_NO_RING"
    - from: "serpentrum/hud_logic.py generic_consent_note"
      to: "gui_game.py begin_game once-per-run block"
      via: "caller gates on the consent flag; None-when-OFF preserves log order (speed_note/stack_mode_note precedent)"
      pattern: "generic_consent_note"
---

<objective>
Land the three HUD labeling surfaces for the generic mode in hud_logic (PURE): the SKIP_GENERIC_NO_RING reason literal, the consent-aware stack_mode_note (with the OFF contract byte-identical), and the once-per-run generic_consent_note disclosure builder.

Purpose: every in-game line a user sees for generic stacking composes from these builders — SC2's "every HUD line labeled" and SC3's specific skip reason ride this plan. Wording is DRAFT (pinned in tests as proposed) pending owner approval at the 5.2-09 feel-check.
Output: hud_logic.py + three builder changes; test_hud_content.py + exact pins.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md
@.planning/phases/5.1-game-speed-difficulty/5.1-05-SUMMARY.md

Key facts:
- `hud_logic.py:57-73` _REASON_TEXT — keyed by placement outcome-code constants (one taxonomy, two consumers). The new entry keys on placement.SKIP_GENERIC_NO_RING (exists after plan 5.2-03). Recap prefix-labeling (`:222` startswith('SKIP_') → 'skipped Nx') needs NO change — the new code auto-renders; pin it anyway.
- `hud_logic.py:134-156` stack_mode_note(records) — the C1 line is CHECKPOINT-PINNED (tests/test_hud_content.py:505-582 exact wording + one-shot call-site scan :571-582 counting `stack_mode_note(` occurrences in gui_game.py == 1). Adding a KEYWORD param with default False keeps both pins green; the line literals stay byte-identical.
- `hud_logic.py:159-177` speed_note — the once-per-run builder precedent (5.1-05 landed). generic_consent_note mirrors the stack_mode_note None-contract (NOT speed_note's always-line contract): OFF runs must add ZERO lines (byte-identical log order).
- hud_logic NEVER imports setup_logic (pinned :168-170) — the consent flag arrives as a plain bool argument; no new imports needed (placement is already imported).
- hud_logic.py:44-48 house rule: hud_logic composes, never writes chemistry — the generic note/label wording is consent/labeling framing (not chemistry claims), same class as the existing demo-mode note.

RESOLVED DECISIONS owned by this plan:
- EQ-setup-3 + EQ-setup-6 (C2 keying + C1/consent alignment): the C2 Setup-side soft warning (plan 5.2-05) REUSES stack_mode_note(records, consent) — ONE wording source, so the Apply-time warning and the begin_game note can never contradict. The setup-research's separate DRAFT C2 text is DROPPED in favor of C1-consistent reuse. Predicate: `_any_stackable(records, consent)` = any(record.get('has_stack_entry')) OR (consent AND 'stack_ring' in record) — module-PRIVATE helper (its only consumers are stack_mode_note itself; tests exercise it through stack_mode_note).
- EQ-engine-7 (consent-ON variant line): when consent is ON and NO record is stackable under the consent-aware predicate, the note must NOT claim "no stacking entries" (false under consent ON). DRAFT variant: `no molecule has a planar aromatic 6-ring for generic pi-stack - demonstration mode: practice steering; only a crash ends the run`.
- EQ-engine-7b (consent disclosure note): DRAFT line `generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]` — signature `generic_consent_note(consent)`, returns None when consent is falsy (None-when-OFF contract, mirroring stack_mode_note).
- Byte-identical-OFF statement: with consent False (or omitted), stack_mode_note output is byte-identical for ALL inputs (the C1 line and the None matrix); generic_consent_note returns None; the new _REASON_TEXT entry is unreachable OFF (the code never fires — placement guard). Existing pins test_hud_content.py:111-113, 140-146, 183-272, 305-355, 505-582 stay green UNTOUCHED.
- Known accepted edge (document in the stack_mode_note docstring): under consent ON with a dataset-unavailable Apply, ring-bearing uploads satisfy the predicate (stack_ring present) but nothing can actually stack — the Apply-time 'stacking dataset unavailable' warning already covers that degraded state; the note is not the right place to re-detect it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for the three HUD surfaces</name>
  <files>tests/test_hud_content.py</files>
  <action>
    In tests/test_hud_content.py (house conventions; reuse existing fixture styles — plain dict records, the real-setloader integration pins pattern):

    1. Class TestGenericReasonText:
       - test_generic_no_ring_reason_text: `hud_logic.reason_text(placement.SKIP_GENERIC_NO_RING, None, 'cyclohexane')` == 'skipped cyclohexane: no aromatic ring for generic pi-stack'.
       - test_recap_prefix_labels_generic_skip: `hud_logic.breakdown_lines([{'name': 'cyclohexane', 'outcome': placement.SKIP_GENERIC_NO_RING}])` == ['skipped 1x cyclohexane: no aromatic ring for generic pi-stack'] (the startswith('SKIP_') prefix needs no change — pin it).
    2. Class TestStackModeNoteConsent (NOT touching the existing TestStackModeNote — its OFF pins stay as-is):
       - test_off_default_byte_identical: for the SAME inputs the existing NOTE constant uses, `hud_logic.stack_mode_note(records)` (no consent arg) == the existing C1 NOTE literal, and `hud_logic.stack_mode_note(records, False)` == the same (explicit-False == default).
       - test_consent_on_ring_bearing_upload_pool_suppresses: records = [{'id': 'upload_0', 'has_stack_entry': False, 'stack_ring': [0, 1, 2, 3, 4, 5]}] → stack_mode_note(records, True) is None (the false-positive fix).
       - test_consent_on_ringless_upload_pool_variant: records = [{'id': 'upload_0', 'has_stack_entry': False}] → stack_mode_note(records, True) == the DRAFT variant line (exact pin): 'no molecule has a planar aromatic 6-ring for generic pi-stack - demonstration mode: practice steering; only a crash ends the run'; assert no '\\n'.
       - test_consent_on_demo_pool_suppresses: records = [{'id': 'benzene', 'has_stack_entry': True}] → stack_mode_note(records, True) is None.
       - test_consent_on_mixed_pool_suppresses: one has_stack_entry=True demo + one bare upload → None.
       - test_consent_on_empty_records_none: stack_mode_note([], True) is None (empty-records branch unchanged).
       - test_consent_on_missing_stack_ring_key_counts_unstackable: records = [{'id': 'upload_0', 'has_stack_entry': False}] (key absent, not False) → variant line (the predicate reads key PRESENCE for stack_ring, mirroring setloader's omit-on-ineligible contract).
       - test_real_upload_set_off_note_unchanged: the existing real-data pin shape (load_upload of the demo naphthalene file) with consent omitted → C1 NOTE (real-data OFF re-pin).
    3. Class TestGenericConsentNote:
       - test_off_is_none: generic_consent_note(False) is None; generic_consent_note(0) is None; generic_consent_note(None) is None.
       - test_on_exact_wording: generic_consent_note(True) == 'generic pi-stack enabled: illustrative geometry - user-approved [Janiak 2000]'; no '\\n'; ASCII-only.
    4. Run file suite → new classes fail (TypeError on unexpected kwarg / AttributeError / wrong text); ALL existing pins green. Commit RED: `test(5.2-04): failing tests for generic HUD surfaces (reason text, consent-aware note, consent note)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v -> new classes fail, zero pre-existing failures</verify>
  <done>RED confirmed: all three surfaces' contracts pinned; the C1 OFF pins untouched.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the three hud_logic surfaces</name>
  <files>serpentrum/hud_logic.py</files>
  <action>
    In serpentrum/hud_logic.py:

    1. _REASON_TEXT: add after the SKIP_NO_RING entry:
       ```python
       placement.SKIP_GENERIC_NO_RING:
           'no aromatic ring for generic pi-stack',
       ```
       (comment: Phase 5.2 consent-ON class — DRAFT wording, owner-amendable at the feel-check.)
    2. Add the private predicate above stack_mode_note:
       ```python
       def _any_stackable(records, consent):
           """Consent-aware stackability over records (Phase 5.2).

           has_stack_entry (load-time dataset match) OR — when generic
           consent is ON — record-level 'stack_ring' presence (the
           canonical planar 6-ring eligibility signal; setloader
           _upload_stack_ring). Pure data; no setup_logic import.
           """
           for record in records:
               if record.get('has_stack_entry'):
                   return True
               if consent and 'stack_ring' in record:
                   return True
           return False
       ```
    3. stack_mode_note: signature becomes `def stack_mode_note(records, consent=False):`. Body: `if not records: return None`; `if _any_stackable(records, consent): return None`; then `if consent:` return the DRAFT variant line, else return the UNCHANGED C1 literal. Docstring: document the consent param, the predicate shift, the variant wording, the single-wording-source C1/C2 alignment (plan 5.2-05 reuses this builder), and the accepted dataset-unavailable edge.
    4. Add generic_consent_note after speed_note (mirroring its docstring style):
       ```python
       def generic_consent_note(consent):
           """The once-per-run generic-consent disclosure line (Phase 5.2).

           Logged ONCE per begin_game when generic upload stacking is ON
           (STACK-06: explicit, non-silent consent). None when consent is
           OFF — the stack_mode_note None-contract, NOT speed_note's
           always-line contract: OFF runs must gain ZERO log lines so the
           once-per-run log order stays byte-identical. ASCII wording;
           the label is consent framing, not chemistry (hud_logic never
           writes chemistry). Caller gates on the setup flag (plan 5.2-06).
           """
           if not consent:
               return None
           return ('generic pi-stack enabled: illustrative geometry - '
                   'user-approved [Janiak 2000]')
       ```
    5. Run file suite green → full gates. Commit GREEN: `feat(5.2-04): generic HUD surfaces — reason text, consent-aware mode note, consent note (STACK-06 SC2/SC3)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green (all C1/OFF pins untouched and green)</verify>
  <done>All three surfaces land; OFF contracts byte-identical; wording pinned as DRAFT for the feel-check.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- OFF sweep: `python3.6 -m unittest tests.test_hud_content -v` — every pre-existing pin (skip literals :305-355, C1 wording + one-shot scan :505-582, recap taxonomy :183-272) green UNTOUCHED.
- `grep -c "stack_mode_note(" serpentrum/gui_game.py` still 1 (the one-shot scan pin holds — this plan does not touch gui_game).
</verification>

<success_criteria>
- SC3: the specific generic skip reason renders through the one-taxonomy surface.
- SC2 labeling half: the consent disclosure line exists (None-when-OFF); the demo-mode note can never contradict the consent state.
- All DRAFT literals pinned as proposed — the 5.2-09 feel-check is the approval gate.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-04-SUMMARY.md`
</output>
