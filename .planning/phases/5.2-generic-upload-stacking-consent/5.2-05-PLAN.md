---
phase: 5.2-generic-upload-stacking-consent
plan: 05
type: execute
wave: 4
depends_on: ["5.2-01", "5.2-02", "5.2-04"]
files_modified:
  - serpentrum/gui_setup.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The Setup tab shows an inline opt-in control for generic upload stacking (checkbox in a new sibling group AFTER Speed) — OFF by default (SC1; modal popup purity-banned)"
    - "The consent choice round-trips through collect_state/apply_state with the DEFAULTS fallback, so persisted files without the key restore OFF (SC1 backcompat at the widget layer)"
    - "Apply anchors the consent-gated overlay dataset: consent ON → the anchored stacking_data carries the generic entry; OFF → the original dataset object (SC2/SC3 data path)"
    - "Apply appends the consent-aware zero-stackable warning clause to the status parts (the absorbed C2 seam) reusing stack_mode_note — one wording source with the begin_game note"
  artifacts:
    - path: serpentrum/gui_setup.py
      provides: "'Generic stacking' QGroupBox + checkbox, collect/apply/restore wiring, overlay anchor in _on_apply, C2 clause in the success parts, docstring inventory 6→7 sections"
      contains: "generic_stack_consent"
  key_links:
    - from: "serpentrum/gui_setup.py _on_apply"
      to: "serpentrum/generic_stack.py overlay_stacking_data"
      via: "self._anchor.stacking_data = overlay_stacking_data(loaded, consent) — the overlay reaches resolve_skip AND resolve via the capture read-site (gui_game.py:907-909)"
      pattern: "overlay_stacking_data"
    - from: "serpentrum/gui_setup.py _on_apply parts"
      to: "serpentrum/hud_logic.py stack_mode_note"
      via: "hud_logic.stack_mode_note(records, consent) appended when not None (C2 seam absorption; C1/C2 single wording source)"
      pattern: "stack_mode_note"
---

<objective>
Wire the Setup tab: the inline consent checkbox (new 'Generic stacking' sibling group after Speed), the consent-gated overlay anchoring in _on_apply, and the absorbed C2 soft warning — the UI half of SC1 plus the Apply-time data path for SC2/SC3.

Purpose: Start ALWAYS applies first (model-A handoff), so anchoring the overlay at Apply guarantees the capture path (resolve_skip + resolve) sees the consent decision on every run with zero per-capture plumbing; the C2 seam lands on the same surface the debug session proposed.
Output: gui_setup.py only. GUI verdicts are human-verify (01-05 decision) — the live feel-check is plan 5.2-09.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md
@.planning/phases/5.1-game-speed-difficulty/5.1-04-SUMMARY.md

Key facts (research setup §2/§3, engine map rows "Dataset anchor"/"Resolve-path"):
- Group-box order (gui_setup.py:146-218): Molecule set → Box → Head molecule → xtb → Win cap → Speed (:199-204) → status_label (:206-207) → stretch → btn_row. The new group inserts AFTER the Speed group's `layout.addWidget(speed_group)`, BEFORE `layout.addWidget(self.status_label)` (5.1-04 sibling-section precedent).
- Widget creation goes in _build_widgets (~:115-127 region): QCheckBox precedent xtb_auto_check (:115-116); word-wrap + disclosure sub-line precedent hessian_label (:124-127, setWordWrap(True), stylesheet color). Keep the consent sub-line ALWAYS VISIBLE (not hidden like hessian_label) — it is the disclosure sentence.
- QCheckBox wiring precedent: `xtb_auto_check.stateChanged.connect(self._on_xtb_auto_changed)` in _wire_signals; for consent the house-minimum wire is `stateChanged` → `self._refresh_status` (its own _loading guard at :367 makes a dedicated handler unnecessary — toggling consent has no immediate side effect beyond status refresh).
- collect_state (:239-273): widget-written keys listed in the docstring (:243-247) — ADD generic_stack_consent to that docstring list; write `setup['generic_stack_consent'] = self.generic_stack_check.isChecked()`; the bare bool is trivially dict-safe (03-07 discipline).
- apply_state (:275-328): inside the _loading guard, `self.generic_stack_check.setChecked(bool(setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])))` — mirrors the speed/xtb restore pattern.
- _on_apply (:440-581): `setup = self.collect_state()` at :461 (consent in scope); dataset loaded fresh :503-505; anchored :511. The overlay anchor REPLACES the :511 assignment:
  `self._anchor.stacking_data = generic_stack.overlay_stacking_data(stacking_data, consent)`.
  overlay_stacking_data is None-safe (dataset-unavailable path: stacking_data None → None anchored — unchanged degradation).
- C2 seam (:560-576): parts list built at :560-576. Append the clause after the head-random note, BEFORE the xtb detect parts:
  ```python
  zero_stackable = hud_logic.stack_mode_note(records, consent)
  if zero_stackable is not None:
      parts.append(zero_stackable)
  ```
  `records` is in scope (:474-490). Requires `from . import hud_logic` (NOT currently imported by gui_setup — verified) and `from . import generic_stack` (new module).
- Docstring inventory: module docstring :1-36 + :57-66 say "Six QGroupBox sections" (:60-63); _build_layout docstring :147 says 6 sections — update all three literals to seven and name the new section.
- Purity: gui_setup.py is already GUI-classified (tools/check_purity.py:62-63); NO .exec_() anywhere (banned :189-196) — the checkbox is inline, never a modal. Static QMessageBox.warning paths stay as-is.

RESOLVED DECISIONS owned by this plan:
- EQ-engine-3 (overlay anchor point): AT APPLY (gui_setup.py:503-511) — consent is in scope, the dataset is loaded fresh anyway, the C2 warning sees the same truth, and the capture read-site needs zero plumbing. begin_game/per-capture anchoring REJECTED (rebuild churn + split-brain risk with the C2 signal).
- DRAFT UI wording (owner-amendable at 5.2-09): group title `Generic stacking`; checkbox label `Allow generic pi-stack for uploads (illustrative geometry - user-approved)`; disclosure sub-line `reuses the approved Set A pi-stack geometry (3.60 A @ 20 deg off-normal) for uploads carrying a planar aromatic 6-ring`.
- Scope note (NOT an OFF violation): the C2 clause ADDS one status_label clause on the Apply path when the set has zero stackable records — this is the DELIBERATE absorbed feature from the debug session (.planning/debug/resolved/05-upload-only-endless-run.md:80-83), firing in both consent states via the consent-aware predicate. The byte-identical-OFF invariant governs GAME behavior (skip taxonomy, in-game HUD lines), which this plan does not touch.
- Byte-identical-OFF pins that must stay green: tests/test_setup_logic.py (schema from 5.2-01), tests/test_hud_content.py C1/OFF pins (5.2-04 keeps them), and every placement pin — none of these files are touched by this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consent group — checkbox widget + wiring + state round-trip</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
    1. _build_widgets: after the Speed section widgets, create the consent widgets:
       ```python
       # --- Generic stacking section (Phase 5.2, plan 5.2-05; STACK-06
       #     consent — inline checkbox, modal popups are purity-banned) ---
       self.generic_stack_check = QtWidgets.QCheckBox(
           'Allow generic pi-stack for uploads '
           '(illustrative geometry - user-approved)', self)
       self.generic_stack_note_label = QtWidgets.QLabel(
           'reuses the approved Set A pi-stack geometry '
           '(3.60 A @ 20 deg off-normal) for uploads carrying a '
           'planar aromatic 6-ring', self)
       self.generic_stack_note_label.setWordWrap(True)
       ```
       (Sub-line is a plain always-visible QLabel — hessian-label styling pattern WITHOUT the hide()/color treatment; DRAFT wording.)
    2. _build_layout: after `layout.addWidget(speed_group)` add:
       ```python
       # --- Generic stacking group (Phase 5.2; sibling AFTER Speed,
       #     before the status label — 5.1-04 precedent) ---
       generic_group = QtWidgets.QGroupBox('Generic stacking', self)
       generic_form = QtWidgets.QFormLayout(generic_group)
       generic_form.addRow('', self.generic_stack_check)
       generic_form.addRow('', self.generic_stack_note_label)
       layout.addWidget(generic_group)
       ```
    3. _wire_signals: `self.generic_stack_check.stateChanged.connect(self._refresh_status)` (house minimum; _refresh_status's _loading guard covers apply_state restores).
    4. collect_state: add `setup['generic_stack_consent'] = self.generic_stack_check.isChecked()` to the widget-written block; update the docstring key list (:243-247) to name generic_stack_consent.
    5. apply_state: inside the _loading guard region, add the setChecked restore with the DEFAULTS fallback (exact shape in context). Position it after the speed restore, keeping the restore order mirrors the layout order.
    6. Docstring inventory: update the three section-count literals (module docstring :57-66 'Six QGroupBox sections' → 'Seven QGroupBox sections' + name 'Generic stacking'; _build_layout docstring :147 '6 QGroupBox sections' → 7).
    7. Run the full gates: `python3.6 tests/run_gates.py` (purity gate must stay green — no .exec_(), no new GUI-class violations; no GUI unit tests exist or are added — 01-05 decision).
    8. Commit: `feat(5.2-05): Setup-tab generic-stacking consent checkbox (STACK-06 SC1)`
  </action>
  <verify>python3.6 tests/run_gates.py -> full suite green (purity + syntax + plugin-path + unittest); grep confirms generic_stack_check wired in _wire_signals, written in collect_state, restored in apply_state</verify>
  <done>The consent control exists inline (checkbox, OFF default via apply_state fallback), round-trips through collect/apply, and the section-count docstrings match the built layout.</done>
</task>

<task type="auto">
  <name>Task 2: Overlay anchor + C2 warning clause in _on_apply</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
    1. Imports: add `from . import generic_stack` and `from . import hud_logic` (alphabetical among the relative imports, :40-46).
    2. _on_apply, after the successful load_stacking try/except block (:503-509) and BEFORE the anchor assignments (:510-511), read the consent flag:
       ```python
       consent = setup.get(
           'generic_stack_consent',
           setup_logic.DEFAULTS['generic_stack_consent'])
       ```
       then change the stacking_data anchor line to:
       ```python
       self._anchor.stacking_data = generic_stack.overlay_stacking_data(
           stacking_data, consent)
       ```
       Add a comment naming the mechanism: the overlay is the consent carrier — OFF anchors the ORIGINAL dataset object (byte-identical capture path), ON appends the generic entry last (shadows nothing); the capture read-site (gui_game) consumes it with zero plumbing.
    3. C2 clause in the success parts (:560-576): after the head-random note block and before the xtb detect block, insert the zero_stackable clause (exact shape in context). Comment: the absorbed C2 soft warning (debug session 05-upload-only) — soft status clause, HARD block REJECTED; reuses stack_mode_note so the Apply warning and the begin_game note share ONE wording source (consent-aware).
    4. Update the _on_apply docstring flow list (step 6 'Success: status message...') to mention the zero-stackable clause.
    5. Run the full gates. Commit: `feat(5.2-05): Apply anchors the consent overlay + absorbed C2 zero-stackable warning (STACK-06)`
  </action>
  <verify>python3.6 tests/run_gates.py -> full suite green; grep confirms overlay_stacking_data call at the anchor site and stack_mode_note in the parts construction; serpentrum/data/stacking_pi_stack.json unmodified in git status</verify>
  <done>Apply anchors the consent-gated overlay (OFF = original object) and appends the consent-aware zero-stackable clause; the capture path consumes the consent decision with zero gui_game plumbing for the dataset itself.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green on the full suite.
- Static shape checks: `grep -n "generic_stack" serpentrum/gui_setup.py` shows the import, the overlay call, and the widget; `grep -n "exec_" serpentrum/gui_setup.py` shows ZERO matches (purity ban).
- OFF-path statement: with the checkbox OFF, overlay_stacking_data returns the ORIGINAL loaded dataset object — the capture path consumes exactly today's data; no OFF-path pin exists at the GUI layer (human-verify), and every pure-layer OFF pin (placement/hud/setup) is untouched by this plan.
</verification>

<success_criteria>
- SC1: inline opt-in control present, OFF by default, persists via the collect/apply round-trip with absent-key = OFF restore.
- SC2/SC3 data path: the overlay is anchored at Apply exactly when consent is ON.
- C2 seam absorbed as a soft status clause with single-source wording.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-05-SUMMARY.md`
</output>
