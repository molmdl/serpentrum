---
phase: 5.2-generic-upload-stacking-consent
plan: 06
type: execute
wave: 4
depends_on: ["5.2-02", "5.2-04"]
files_modified:
  - serpentrum/gui_game.py
  - tests/test_hud_content.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "With consent ON, a ring-bearing upload capture resolves stackable (the controller restamp recomputes has_stack_entry from the ANCHORED overlay dataset) and places via the generic entry — the win path works in an upload-only game (SC2)"
    - "Every generic placement's info-box line and end-of-run recap group carry the generic label; the recap group is DISTINCT from dataset-entry groups even for identical molecule names (SC2/SC4)"
    - "With consent ON, begin_game logs the consent disclosure line ONCE between the stack_mode note and the speed note; with consent OFF it logs NOTHING (byte-identical log order)"
    - "With consent OFF, the restamp is a semantic no-op (interaction_for on the original dataset returns None for '__upload__') and the taxonomy skips exactly as today (SC3 integrity)"
  artifacts:
    - path: serpentrum/gui_game.py
      provides: "consent-aware restamp in _handle_stack_event, consent read + stack_mode_note consent arg + generic_consent_note slot in begin_game, history_name enrollment, generic_stack import"
      contains: "has_stack_entry_for"
    - path: tests/test_hud_content.py
      provides: "source-scan pin: exactly one generic_consent_note call site in gui_game.py (speed_note/stack_mode_note precedent)"
      contains: "generic_consent_note"
  key_links:
    - from: "serpentrum/gui_game.py _handle_stack_event"
      to: "serpentrum/generic_stack.py has_stack_entry_for"
      via: "resolve_rec['has_stack_entry'] recomputed from the anchored (possibly overlaid) dataset before placement.resolve_skip"
      pattern: "has_stack_entry_for"
    - from: "serpentrum/gui_game.py stacked_history enrollment"
      to: "hud_logic.breakdown_lines"
      via: "history 'name' decorated by generic_stack.history_name — first-appearance grouping separates generic placements with zero breakdown_lines edits"
      pattern: "history_name"
---

<objective>
Wire the GameTab: the consent-aware stackability restamp at the capture seam, the once-per-run consent disclosure note, the consent argument into stack_mode_note, and the generic-labeled history enrollment — the runtime half of SC2/SC3/SC4.

Purpose: the controller restamp is the ONE consent-awareness touchpoint in the capture path (research answer 4a option (i)); placement.py stays untouched, so every taxonomy pin stays green by construction. The notes block gains exactly one conditional line.
Output: gui_game.py edits + one source-scan pin in test_hud_content.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-setup-consent.md
@.planning/phases/5.1-game-speed-difficulty/5.1-05-SUMMARY.md

Key facts:
- gui_game imports (:55-81): game_engine, hud_logic, pymol_bridge, setup_logic, molfile, orientation, placement, spawn, stacking, input. ADD `from . import generic_stack` (pure module; gui_game is GUI-allowlisted). molecule_data is NOT needed (the restamp goes through generic_stack.has_stack_entry_for).
- begin_game once-per-run block (:422-435): C1 note :426-428 (`note = hud_logic.stack_mode_note(records)`), speed note :429-434 (ALWAYS logged), 'Get ready...' :435. The consent line slots BETWEEN the stack_mode note and the speed note (speed note stays always-last-before-Get-ready — zero order drift when OFF).
- _handle_stack_event enrichment (:919-924): `resolve_rec = dict(pickup_rec)`; stack_ring copied if present; `resolve_rec['has_stack_entry'] = record.get('has_stack_entry')` at :923; `resolve_rec['set'] = record.get('set')` at :924. THE restamp seam.
- Enrollment (:979-985): stacked_history.append({'name': name, 'outcome': 'stacked', 'distance_a': ..., 'citation_short': ..., 'interaction_id': ...}) where `name = record['name']` (:964).
- Seed stamp gui_game.py:1137 (`seed['has_stack_entry'] = record.get('has_stack_entry')`) — LEAVE AS-IS: the restamp overwrites resolve_rec's value per-capture; the seed shape contract (test seams) stays stable.
- Restart (:1245-1263) replays the ANCHOR's setup dict through begin_game — consent included; the anchor's stacking_data (overlay or original, anchored at Apply) is unchanged by Restart, so consent state and dataset stay in sync by construction. Document this in the begin_game docstring.
- HEAD eligibility (EQ-engine-8): NO change — _build_head_state/_select_head_record branch on stack_ring presence only; an eligible upload head is already edge-on; there is no head-level consent effect anywhere in the code. Confirm-by-reading, change nothing.
- Source-scan pins: `source.count('stack_mode_note(') == 1` (:571-582) and the speed_note scan (:640-652) — both must stay green (this plan does not add call sites for those two builders; the stack_mode_note CALL SITE stays exactly one, now passing the consent arg).

RESOLVED DECISIONS owned by this plan:
- EQ-engine-2 (has_stack_entry site): option (i) controller restamp via the PURE helper generic_stack.has_stack_entry_for(record, stacking_data) — placement.py untouched (its :116 pre-guard keeps the None-dataset degradation working), setloader untouched, and the restamp is consent-BLIND (it reads the anchored dataset, which IS the consent carrier from plan 5.2-05). Restamp code (replaces :923-924, order matters — 'set' stamped BEFORE the recompute):
  ```python
  resolve_rec['set'] = record.get('set')
  # Phase 5.2 (STACK-06): consent-aware stackability. The ANCHORED
  # dataset is the consent carrier (Apply anchored the overlay when
  # consent is ON), so recompute from it: OFF reduces to the load-time
  # value by definition (interaction_for('__upload__') -> None on the
  # unmodified dataset); the None-dataset degradation falls back to the
  # load-time flag inside the helper.
  resolve_rec['has_stack_entry'] = generic_stack.has_stack_entry_for(
      record, stacking_data)
  ```
- EQ-engine-6/EQ-setup-7 (recap authorship): the enrollment decorates ONLY the history name via `generic_stack.history_name(name, outcome['interaction'])` — breakdown_lines untouched; generic placements group distinctly even when a demo molecule shares the molecule name (distinct group keys). interaction_id stays stored (unchanged line).
- EQ-engine-7 (notes): `consent = setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])` read ONCE in begin_game; `hud_logic.stack_mode_note(records, consent)`; then the conditional consent-line log. DOC NOTE: under consent ON, a demo-set run behaves identically to today (the predicate suppresses nothing for stackable demo pools; the disclosure line is the only addition).
- EQ-engine-8 (heads): confirmed no change — record in the SUMMARY, zero code.
- Byte-identical-OFF statement: OFF → restamp no-op (definitional identity), consent line None (no log), stack_mode_note original line, enrollment name undecorated (dataset interactions never match the generic id). GUI-layer verdicts are human-verify (5.2-09); pure-layer OFF pins (test_placement, test_hud_content, test_phase5_integration) are untouched by this plan and must stay green in the gates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: begin_game consent read + notes block</name>
  <files>serpentrum/gui_game.py, tests/test_hud_content.py</files>
  <action>
    1. Imports: add `from . import generic_stack` (after `from . import game_engine`, alphabetical).
    2. begin_game (:422-435): before the C1 note, read the flag once:
       ```python
       consent = setup.get(
           'generic_stack_consent',
           setup_logic.DEFAULTS['generic_stack_consent'])
       ```
       Change :426 to `note = hud_logic.stack_mode_note(records, consent)`. After the `if note is not None: self._log(note)` block and BEFORE the speed-note block (:429), insert:
       ```python
       # STACK-06 (Phase 5.2): the once-per-run generic-consent
       # disclosure — explicit, non-silent consent made visible in-game.
       # None when OFF: OFF runs gain ZERO lines (log order byte-identical).
       consent_line = hud_logic.generic_consent_note(consent)
       if consent_line is not None:
           self._log(consent_line)
       ```
    3. begin_game docstring: extend the notes narration with the consent line (once per run, between the mode note and the speed note; Restart replays the anchor setup dict so the consent state and the anchored overlay stay in sync).
    4. In tests/test_hud_content.py, add (mirroring the speed_note source-scan pin at :640-652):
       `test_begin_game_has_exactly_one_generic_consent_call_site` — reads serpentrum/gui_game.py source, asserts `source.count('generic_consent_note(') == 1`.
    5. Run the full gates `python3.6 tests/run_gates.py`. Commit: `feat(5.2-06): begin_game consent disclosure + consent-aware mode note (STACK-06 SC2)`
  </action>
  <verify>python3.6 tests/run_gates.py -> full suite green (incl. the new source-scan pin and both pre-existing one-shot scans)</verify>
  <done>The consent flag is read once per run; the disclosure line slots between the mode and speed notes; OFF runs log nothing new; one-shot scan pins all green.</done>
</task>

<task type="auto">
  <name>Task 2: Capture-path restamp + generic-labeled enrollment</name>
  <files>serpentrum/gui_game.py</files>
  <action>
    1. _handle_stack_event enrichment (:919-924): reorder + restamp exactly as specified in the context block ('set' first, then has_stack_entry via generic_stack.has_stack_entry_for(record, stacking_data)). Update the surrounding comment block (:913-918) to name the 5.2 consent-awareness and the None-dataset fallback.
    2. Enrollment (:979-985): change the 'name' value to `generic_stack.history_name(name, outcome['interaction'])` with a comment: the recap-group label decoration (SC4) — generic placements group under '<molecule> (generic pi-stack)', distinct from dataset groups even for shared molecule names; breakdown_lines untouched.
    3. _handle_stack_event docstring: add a 5.2 paragraph — the restamp is the single consent-awareness touchpoint; SKIP_GENERIC_NO_RING is the consent-ON ring-less outcome (taxonomy order unchanged); the DECORATED history name feeds the recap.
    4. Confirm-by-reading (no code): head eligibility (EQ-engine-8) — note the confirmation in the plan SUMMARY.
    5. Run the full gates. Commit: `feat(5.2-06): consent-aware capture restamp + generic recap enrollment (STACK-06 SC2/SC3/SC4)`
  </action>
  <verify>python3.6 tests/run_gates.py -> full suite green; every pure OFF pin (test_placement :604-621 upload-with-ring-still-skips, test_hud_content C1/OFF matrix, test_phase5_integration s5) green UNTOUCHED</verify>
  <done>The capture path recomputes stackability from the anchored overlay; generic placements enroll decorated names; OFF behavior is provably unchanged (all pins green, zero pure-file edits).</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green on the full suite.
- `grep -n "generic_stack" serpentrum/gui_game.py` shows the import + exactly three consumption sites (has_stack_entry_for, history_name, and none other).
- `grep -c "stack_mode_note(" serpentrum/gui_game.py` == 1; `grep -c "speed_note(" serpentrum/gui_game.py` == 1; `grep -c "generic_consent_note(" serpentrum/gui_game.py` == 1.
- OFF sweep: `python3.6 -m unittest tests.test_placement tests.test_hud_content tests.test_phase5_integration -v` all green.
</verification>

<success_criteria>
- SC2 runtime half: consent-ON ring-bearing uploads resolve stackable and enroll generic-labeled history; the disclosure note logs once.
- SC3 integrity: OFF capture path byte-identical (restamp no-op; every pure pin green unmodified).
- SC4 runtime half: recap groups generic placements distinctly via the decorated name.
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-06-SUMMARY.md`
</output>
