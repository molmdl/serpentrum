---
phase: 5.2-generic-upload-stacking-consent
plan: 05
subsystem: ui
tags: [pyqt5, gui_setup, consent-checkbox, generic-stacking, overlay-anchor, c2-warning, pymol-plugin, python3.6]

# Dependency graph
requires:
  - phase: 5.2-generic-upload-stacking-consent (plan 5.2-01)
    provides: setup_logic.DEFAULTS['generic_stack_consent']=False + validate bool check (absent key = OFF backcompat)
  - phase: 5.2-generic-upload-stacking-consent (plan 5.2-02)
    provides: generic_stack.overlay_stacking_data (None-safe, OFF = object-identity pass-through, ON appends generic entry last)
  - phase: 5.2-generic-upload-stacking-consent (plan 5.2-04)
    provides: hud_logic.stack_mode_note(records, consent=False) consent-aware zero-stackable note (single wording source for C1/C2)
  - phase: 5.1-game-speed-difficulty (plan 5.1-04)
    provides: sibling-QGroupBox-section insertion precedent + speed restore pattern
provides:
  - Inline opt-in 'Generic stacking' QCheckBox on the Setup tab (SC1 UI half), OFF by default via DEFAULTS fallback
  - generic_stack_consent round-trips collect_state/apply_state (widget-written key, absent = OFF restore)
  - _on_apply anchors the consent-gated stacking overlay (SC2/SC3 Apply-time data path; capture read-site needs zero plumbing)
  - Absorbed C2 soft zero-stackable warning in the Apply success parts (consent-aware, single wording source with begin_game note)
affects: [5.2-06/5.2-07/5.2-08 (engine + game-tab wiring consume the anchored overlay), 5.2-09 (feel-check owns UI wording verdict)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Overlay anchor at Apply (EQ-engine-3): Start ALWAYS Applies first (model-A handoff), so anchoring the consent overlay in _on_apply reaches every capture with zero per-capture plumbing"
    - "Setup-tab binary consent = QCheckBox 1:1 xtb_auto_check pattern copy: stateChanged -> _refresh_status (house minimum; its own _loading guard covers apply_state restores)"
    - "C2 soft warning = pure hud_logic builder reused between Setup Apply parts and begin_game notes (single wording source, no drift)"

key-files:
  created: []
  modified:
    - serpentrum/gui_setup.py

key-decisions:
  - "Consent read hoisted to immediately after validate() in _on_apply so BOTH the overlay anchor and the C2 parts clause consume one flag (the anchor block's `if anchor and records` guard would otherwise leave consent unbound on the anchor-less path)"
  - "Disclosure sub-line is a plain always-visible word-wrapped QLabel (hessian-label pattern WITHOUT hide()/color) -- it is the disclosure sentence, DRAFT wording owner-amendable at 5.2-09"

patterns-established:
  - "Docstring-truth housekeeping: section-count literals follow the built layout (six -> seven across class + _build_layout docstrings)"
  - "Absolute-deviation guard: consent-relevant reads use setup.get(key, setup_logic.DEFAULTS[key]) -- never a literal default"

# Metrics
duration: 8 min
completed: 2026-09-25
---

# Phase 5.2 Plan 05: Setup Consent Checkbox + Overlay Anchor Summary

**Setup-tab 'Generic stacking' consent checkbox round-tripped through collect/apply, with _on_apply anchoring the consent-gated stacking overlay (OFF = byte-identical original dataset object) and appending the absorbed C2 consent-aware zero-stackable warning to the Apply status**

## Performance

- **Duration:** 8 min
- **Started:** 2026-09-25T20:07:00Z
- **Completed:** 2026-09-25T20:15:22Z
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments

- SC1 (UI half): new `QGroupBox('Generic stacking')` sibling section after Speed with an inline opt-in `generic_stack_check` QCheckBox plus an always-visible word-wrapped disclosure sub-line (DRAFT wording, owner-amendable at 5.2-09); `stateChanged -> _refresh_status` house-minimum wire; OFF default via `apply_state` DEFAULTS fallback; `generic_stack_consent` widget-written in `collect_state` (docstring key list updated).
- SC2/SC3 data path: `_on_apply` now anchors `generic_stack.overlay_stacking_data(stacking_data, consent)` — OFF anchors the ORIGINAL dataset object (object identity = byte-identical capture path), ON appends the generic upload entry LAST (first-match lookup shadows nothing), None-safe on the dataset-unavailable path; the gui_game capture read-site consumes it with zero plumbing (EQ-engine-3: anchor at Apply).
- Absorbed C2 (debug session 05-upload-only): the success `parts` list now appends `hud_logic.stack_mode_note(records, consent)` when not None — a SOFT status clause (HARD block rejected) sharing ONE consent-aware wording source with the begin_game note.
- Gates: `python3.6 tests/run_gates.py` fully green (all 3 gates; 720 tests); zero `.exec_()` tokens (purity ban); `serpentrum/data/stacking_pi_stack.json` unmodified; byte-identical-OFF pins untouched (tests/test_setup_logic.py, tests/test_hud_content.py, placement pins — none touched).

## Task Commits

Each task was committed atomically:

1. **Task 1: Consent group — checkbox widget + wiring + state round-trip** - `4f66daa` (feat)
2. **Task 2: Overlay anchor + C2 warning clause in _on_apply** - `69d2e99` (feat)

## Files Created/Modified

- `serpentrum/gui_setup.py` — consent widgets (`_build_widgets`), 'Generic stacking' group layout after Speed (`_build_layout`), `stateChanged -> _refresh_status` wiring (`_wire_signals`), `generic_stack_consent` write (`collect_state`), `setChecked` restore with DEFAULTS fallback after the speed restore (`apply_state`), consent read + overlay anchor + absorbed C2 clause (`_on_apply`), docstring-truth updates (six -> seven sections; _on_apply flow steps 3/6; collect_state key list).

## Decisions Made

- Followed the plan's resolved decisions verbatim: overlay anchor AT APPLY (EQ-engine-3, begin_game/per-capture anchoring rejected); DRAFT UI wording for group title/checkbox label/disclosure sub-line; C2 absorbed as a soft clause reusing `hud_logic.stack_mode_note`.
- Consent read hoisted to just after `validate()` in `_on_apply` (see deviation 1) — one flag consumed by both the anchor and the C2 clause.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Hoisted the `consent` read out of the anchor block**

- **Found during:** Task 2 (Overlay anchor + C2 warning clause in _on_apply)
- **Issue:** The plan scoped `consent` inside `if self._anchor is not None and records:` (after the load_stacking try/except), but the C2 clause in the success `parts` (outside that block) also consumes `consent` — on the anchor-less branch (`self._anchor is None` or empty `records`) the C2 line would raise `NameError`.
- **Fix:** Read `consent` once immediately after the validate gate (`setup.get('generic_stack_consent', setup_logic.DEFAULTS['generic_stack_consent'])`, absent key = OFF); both the anchor and the C2 clause consume that one binding.
- **Files modified:** serpentrum/gui_setup.py
- **Verification:** full gates green (720 tests); C2 clause references the hoisted flag.
- **Committed in:** `69d2e99` (Task 2 commit)

**2. [Rule 3 - Blocking] Docstring-inventory count literals: two, not three**

- **Found during:** Task 1 (docstring inventory step)
- **Issue:** The plan context said three section-count literals exist ("module docstring :1-36 + :57-66 + :147"); the module docstring :1-36 contains no count literal — only the class docstring (:60, "Six QGroupBox sections") and `_build_layout` docstring (:147, "6 QGroupBox sections") do.
- **Fix:** Updated the two existing literals six -> seven and named 'Generic stacking'; no third edit was possible or needed.
- **Files modified:** serpentrum/gui_setup.py
- **Verification:** greps confirm both literals read seven; gates green.
- **Committed in:** `4f66daa` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 bug-scope hoist, 1 docstring-inventory correction)
**Impact on plan:** Both are plan-intent-preserving mechanics (hoist avoids a latent NameError; literal count correction is cosmetic). No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The anchored `stacking_data` on `_serpentrum` is now consent-gated: plans 5.2-06/5.2-07/5.2-08 (game-tab wiring: begin_game consent note, resolve-skip consent-awareness, enrollment/recap) consume the overlay from the same anchor with zero further Setup-tab changes.
- `setup['generic_stack_consent']` rides the read-once-per-run discipline automatically (same dict `_build_engine` already snapshots).
- Byte-identical-OFF remains the invariant: with the checkbox OFF the anchor holds the exact object `molecule_data.load_stacking` returned.
- All GUI wording (group title, checkbox label, disclosure sub-line, C2 clause rendering) is DRAFT — the live feel-check verdict is plan 5.2-09 (01-05 decision: GUI verdicts are human-verify only).

---
*Phase: 5.2-generic-upload-stacking-consent*
*Completed: 2026-09-25*
