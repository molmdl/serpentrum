---
phase: 5.2-generic-upload-stacking-consent
plan: 03
type: tdd
wave: 2
depends_on: ["5.2-02"]
files_modified:
  - serpentrum/placement.py
  - tests/test_placement.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Under consent ON (overlay dataset), an upload WITHOUT a planar 6-ring skips with the NEW specific reason class SKIP_GENERIC_NO_RING (SC3)"
    - "Under consent ON, an upload WITH a canonical planar 6-ring passes resolve_skip (stackable) and resolves to a placed outcome carrying the generic entry (SC2 half)"
    - "Under consent OFF, an upload still skips SKIP_NO_ENTRY — the has_stack_entry pre-guard at placement.py:116 is UNTOUCHED, so the OFF path is byte-identical by construction (SC3 integrity)"
    - "Demo-set taxonomy is unchanged: set_a records still resolve pi_stack_pd and still emit SKIP_NO_RING on ring-less demo records"
  artifacts:
    - path: serpentrum/placement.py
      provides: "SKIP_GENERIC_NO_RING constant + generic-id branch at the ring-check step of resolve_skip (+ docstring updates); pre-guard and all other steps untouched"
      contains: "SKIP_GENERIC_NO_RING"
    - path: tests/test_placement.py
      provides: "generic-branch tests (overlay dataset) + new constant in the strings pin; all existing OFF pins untouched and green"
      contains: "SKIP_GENERIC_NO_RING"
  key_links:
    - from: "serpentrum/placement.py resolve_skip"
      to: "serpentrum/generic_stack.py GENERIC_INTERACTION"
      via: "interaction['id'] == generic_stack.GENERIC_INTERACTION['id'] branch at the SKIP_NO_RING step"
      pattern: "generic_stack\\.GENERIC_INTERACTION"
---

<objective>
Extend the placement skip taxonomy with the consent-ON ring-less reason class `SKIP_GENERIC_NO_RING` — the ONLY change placement.py needs for 5.2 (the has_stack_entry consent-awareness lives in the GUI controller restamp, plan 5.2-06; the overlay carries the consent).

Purpose: ROADMAP SC3 names the specific reason class "no aromatic ring for generic π-stack" — a ring-less upload under consent ON must skip with THAT class, distinguishable from the consent-OFF skip (SKIP_NO_ENTRY, "no verified stacking entry") and from the demo-set ring-less skip (SKIP_NO_RING). The taxonomy ORDER is unchanged: the new code is a sibling of SKIP_NO_RING at the same ring-check step, chosen by which entry matched.
Output: placement.py + the new code/branch/docstrings; test_placement.py + generic-branch tests. Everything else in placement.py untouched.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-RESEARCH-engine-generic-entry.md

Key facts (research answer 4a + map row "Skip taxonomy"):
- `placement.py:77-85` — outcome code constants; :102-129 resolve_skip. Order: SKIP_NO_ENTRY (:115-119, PRE-GUARDED on `record.get('has_stack_entry')` at :116) → SKIP_NOT_APPROVED (:120-124) → SKIP_MODE (:125-126) → SKIP_NO_RING (:127-128). SKIP_NONPLANAR surfaces at placement time (:247-248).
- THE INVARIANT: the :116 pre-guard stays EXACTLY as-is. Under consent OFF, uploads carry has_stack_entry=False (load-time, '__upload__' keying) → the guard short-circuits to SKIP_NO_ENTRY before interaction_for — byte-identical to today, AND it protects the stacking_data=None degradation path (gui_setup.py:497-499 anchors None on dataset load failure; the guard prevents interaction_for(None) crashes). Do NOT drop the guard (research option (ii) REJECTED for exactly this reason).
- Under consent ON, the GUI controller restamps has_stack_entry consent-aware (plan 5.2-06 via generic_stack.has_stack_entry_for) — so the guard PASSES for ring-bearing uploads and the lookup finds the generic entry. This plan only adds the branch that distinguishes the ring-less case by the FOUND entry's id.
- `placement.py:15` (module docstring taxonomy list) and the resolve_skip docstring rule list need the new code documented.
- `placement.py:88` _SUPPORTED_MODE = 'pi_stack' — the generic entry's mode matches; the mode gate passes.
- `tests/test_placement.py:226-227` — test_outcome_code_constants_are_strings iterates a fixed name list: ADD 'SKIP_GENERIC_NO_RING'.
- Existing OFF pins that must stay green UNTOUCHED: :161-176 (upload → SKIP_NO_ENTRY + taxonomy-order comments), :604-621 (upload WITH stack_ring STILL skips SKIP_NO_ENTRY — the core OFF pin), :623-640 (skip never touches geometry), :226 (constants test).

RESOLVED DECISIONS owned by this plan:
- EQ-engine-1 + EQ-setup-5 (reason class): NEW code `SKIP_GENERIC_NO_RING` (constant in placement.py, the one-taxonomy surface) with DRAFT reason text 'no aromatic ring for generic pi-stack' (ASCII; wording owner-amendable at 5.2-09; the TEXT lands in hud_logic._REASON_TEXT in plan 5.2-04 — this plan owns the CODE only).
- Implementation shape: guard untouched; ONLY the ring-check step gains the id-comparison branch:
  ```python
  if 'stack_ring' not in record:
      if interaction['id'] == generic_stack.GENERIC_INTERACTION['id']:
          return SKIP_GENERIC_NO_RING
      return SKIP_NO_RING
  return None
  ```
  Requires `from . import generic_stack` at the top of placement.py (pure→pure; generic_stack imports only stdlib + molecule_data — no import cycle).
- Byte-identical-OFF statement for this plan: with the overlay absent, uploads never pass the :116 guard, so the new branch is UNREACHABLE when OFF; set_a records resolve pi_stack_pd (id != generic id) → SKIP_NO_RING as today. Every existing pin stays green with ZERO edits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for the generic ring-less skip class</name>
  <files>tests/test_placement.py</files>
  <action>
    In tests/test_placement.py (house conventions; the file already has fixture atoms + a loaded real dataset in setUp — reuse them; import generic_stack at the top with the other serpentrum imports):

    1. ADD 'SKIP_GENERIC_NO_RING' to the name list in test_outcome_code_constants_are_strings (:226-227) — RED immediately (AttributeError: no such constant).
    2. Add class TestGenericSkipClass with:
       - test_upload_ringless_under_consent_skips_generic_no_ring: build `overlay = generic_stack.overlay_stacking_data(self.stacking_data, True)`; record `{'set': '__upload__', 'has_stack_entry': True, 'atoms': self.benzene_raw}` (NO 'stack_ring' key — the consent-ON restamp shape produced by plan 5.2-06). `placement.resolve_skip(rec, overlay)` == `placement.SKIP_GENERIC_NO_RING`.
       - test_upload_ring_bearing_under_consent_is_stackable: same overlay; record `{'set': '__upload__', 'has_stack_entry': True, 'stack_ring': list(<a benzene 6-ring index list — reuse the fixture ring the file already uses for _BENZENE_RING-style cases>), 'atoms': self.benzene_raw}` → resolve_skip returns None (stackable).
       - test_upload_off_still_skips_no_entry: SAME ring-less record but with `'has_stack_entry': False` and the ORIGINAL dataset → SKIP_NO_ENTRY (the guard path — the byte-identical core, re-asserted next to the new branch).
       - test_generic_branch_requires_generic_entry_id: a DRAFT-shaped or set_a record that lacks stack_ring but matches pi_stack_pd → SKIP_NO_RING (NOT the generic code): `{'set': 'set_a', 'has_stack_entry': True, 'atoms': self.benzene_raw}` on the ORIGINAL dataset → SKIP_NO_RING.
       - test_generic_branch_off_overlay_identity: `generic_stack.overlay_stacking_data(self.stacking_data, False)` passed to resolve_skip with the has_stack_entry=True upload record → still SKIP_NO_ENTRY (the guard fires on the record flag, NOT the dataset — an OFF-run record never carries True; this pins that the DATASET alone cannot flip the taxonomy without the restamp).
       - test_new_reason_code_not_yet_in_reason_text_is_fine: `hud_logic._reason(placement.SKIP_GENERIC_NO_RING, None)` returns 'unclassified outcome SKIP_GENERIC_NO_RING' TODAY (the text lands in plan 5.2-04) — pin the interim fallback so the taxonomy test doesn't secretly depend on hud_logic text. (Import hud_logic — the file already does for other pins; if not, add it.)
       - test_full_resolve_generic_placement_outcome_shape: with the overlay + the ring-bearing upload record as pickup_rec AND a minimal records_by_id/head fixture mirroring the existing resolve-test shapes in this file, `placement.resolve(...)` returns {'status': 'placed', ...} with outcome['interaction']['id'] == 'pi_stack_generic' and outcome['citation_short'] == 'Janiak 2000'. Mirror the argument shapes the file's existing resolve tests use (records_by_id, head_atoms, head_stack_ring, heading, segments, existing_atoms, box, display_z).
    3. Run file suite → the new tests fail (AttributeError / assertion), ALL existing tests still pass. Commit RED: `test(5.2-03): failing tests for SKIP_GENERIC_NO_RING taxonomy branch`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_placement.py" -v -> new class fails, zero pre-existing failures</verify>
  <done>RED confirmed: generic-branch contract exists as failing tests; every existing OFF pin untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — SKIP_GENERIC_NO_RING constant + branch</name>
  <files>serpentrum/placement.py</files>
  <action>
    In serpentrum/placement.py:

    1. Add `from . import generic_stack` to the imports (after `from . import molecule_data`).
    2. Add the constant after SKIP_NO_RING (:80), with comment:
       ```python
       SKIP_GENERIC_NO_RING = 'SKIP_GENERIC_NO_RING'
       # Phase 5.2 (STACK-06): consent-ON generic entry matched (the overlay
       # anchored at Apply) but the upload carries no canonical planar
       # 6-ring. Sibling of SKIP_NO_RING at the SAME taxonomy step — chosen
       # by WHICH entry matched, so the ORDER is unchanged. Unreachable when
       # consent is OFF: uploads never pass the has_stack_entry pre-guard
       # below (load-time False via the '__upload__' keying).
       ```
    3. In resolve_skip's ring-check (:127-128), insert the id-comparison branch exactly as specified in the context block. Do NOT touch the :116 pre-guard, the approval gate, the mode gate, or anything else.
    4. Docstrings: module docstring taxonomy list (~:43-44) gains `SKIP_GENERIC_NO_RING` (consent-ON generic entry, no planar 6-ring); resolve_skip docstring rule 4 becomes "SKIP_NO_RING / SKIP_GENERIC_NO_RING — record carries no 'stack_ring' (the generic code when the FOUND entry is the 5.2 generic upload entry)".
    5. Run file suite green → full gates `python3.6 tests/run_gates.py`. Commit GREEN: `feat(5.2-03): SKIP_GENERIC_NO_RING consent-ON skip class (STACK-06 SC3)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_placement.py" -v -> all pass incl. the new class; python3.6 tests/run_gates.py -> full suite green</verify>
  <done>The taxonomy gains exactly one code + one branch; guard/order untouched; OFF pins green; the consent-ON placed shape resolves through the generic entry.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- OFF-path byte-identical sweep (run explicitly): `python3.6 -m unittest tests.test_placement tests.test_phase5_integration tests.test_setloader_stack_ring tests.test_stacking_dataset -v` — every pre-existing pin green with ZERO edits to those assertions.
- `grep -n "has_stack_entry" serpentrum/placement.py` still shows ONLY the :116 guard region (no new reads).
</verification>

<success_criteria>
- SC3 half: consent-ON ring-less uploads resolve to SKIP_GENERIC_NO_RING; consent-ON ring-bearing uploads resolve stackable → placed via the generic entry.
- SC3 integrity: consent-OFF taxonomy byte-identical (guard untouched, new branch unreachable, all OFF pins green unmodified).
</success_criteria>

<output>
After completion, create `.planning/phases/5.2-generic-upload-stacking-consent/5.2-03-SUMMARY.md`
</output>
