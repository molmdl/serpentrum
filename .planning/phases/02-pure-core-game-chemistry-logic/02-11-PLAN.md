---
phase: 02-pure-core-game-chemistry-logic
plan: 11
type: execute
wave: 2
depends_on: ["02-05"]
files_modified:
  - serpentrum/data/stacking_pi_stack.json
  - serpentrum/data/DATA_SOURCES.md
  - tests/test_stacking_dataset.py
autonomous: false

must_haves:
  truths:
    - "serpentrum/data/stacking_pi_stack.json loads through molecule_data.load_stacking with zero DataErrors; exactly one interaction (id pi_stack_pd, mode pi_stack) with status DRAFT before the human decision"
    - "The stored (distance_a, lateral_offset_a) pair encodes the verified geometry: sqrt(d^2 + l^2) lands within 0.005 of a clean 2-decimal centroid-centroid value (3.60 for the recommended option) and atan2(l, d) is 20 deg +/- 0.5"
    - "shipped_interactions(data) returns [] while DRAFT (DATA-02: nothing ships unapproved) and the single entry once APPROVED — the suite passes in BOTH states"
    - "DATA_SOURCES.md documents every cited source (DOI, license, verification status) and names the UNVERIFIED items (3.3/3.4 A) that must never ship"
    - "The stored distance value changes ONLY by explicit human decision at the blocking checkpoint: option-a -> 3.383/1.231 APPROVED, option-b -> 3.355/1.221 APPROVED, option-c -> stays DRAFT"
  artifacts:
    - path: "serpentrum/data/stacking_pi_stack.json"
      provides: "the shipped pi-stack interaction dataset (schema_version 1, one pi_stack_pd interaction, DRAFT status, 3 verified citations) validated through the 02-05 loader"
      contains: "pi_stack_pd"
    - path: "serpentrum/data/DATA_SOURCES.md"
      provides: "the DATA-04 draft attribution document: PubChem CIDs, Janiak 2000 abstract-only status, COD measured stacks 3.555/3.570/3.580 A with licenses, herringbone negative evidence, UNVERIFIED list"
      contains: "10.1039/b003010o"
    - path: "tests/test_stacking_dataset.py"
      provides: "loader-validated dataset tests (decision-agnostic: pass in DRAFT and APPROVED states) + DATA_SOURCES.md marker checks + loader tamper-proofing check"
      min_lines: 90
  key_links:
    - from: "tests/test_stacking_dataset.py"
      to: "serpentrum/molecule_data.py load_stacking"
      via: "loads the real shipped dataset through the validated loader (structure rules from 02-05)"
      pattern: "load_stacking"
    - from: "serpentrum/data/stacking_pi_stack.json"
      to: "serpentrum/data/DATA_SOURCES.md"
      via: "every citations[].doi in the dataset appears in DATA_SOURCES.md"
      pattern: "doi"
    - from: "serpentrum/data/stacking_pi_stack.json interactions[0]"
      to: "derived centroid-centroid geometry (sqrt(d^2+l^2), atan2 angle)"
      via: "test computes the composed centroid distance and 20 deg off-normal angle from the file's own two fields"
      pattern: "atan2|sqrt"
---

<objective>
Materialize the Demo Set A pi-stack interaction dataset (recommended: 3.6 A centroid-centroid with ~20 deg parallel displacement, encoded as the place_pickup component pair distance_a=3.383 / lateral_offset_a=1.231, status DRAFT) plus the draft DATA_SOURCES.md attribution document into serpentrum/data/, prove both through the 02-05 validated loader — then STOP at a BLOCKING human decision checkpoint on the stored value (repo hard rule DATA-02: no invented data ships without explicit human approval).

Purpose: STACK-02 demands stacking be data-driven ("interaction mode + distance + citation load from a validated data file, never code constants"). The demo-data research pinned every parameter: three measured CC0 COD stacks (3.555 / 3.570 / 3.580 A centroid-centroid, interplanar 3.345-3.478 A, offsets 0.83-1.20 A) and the Janiak 2000 abstract (~20 deg ring-normal angle, usual stacking up to 3.8 A; 3.4 A UNVERIFIED). The files ship DRAFT; only the human flips the status.

Output: serpentrum/data/stacking_pi_stack.json + serpentrum/data/DATA_SOURCES.md + tests/test_stacking_dataset.py + a recorded decision (or an explicit DRAFT hold).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md
@serpentrum/molecule_data.py
@tests/test_molecule_data.py

Wave/dependency note: 02-05 (wave 1) delivered serpentrum/molecule_data.py with load_manifest/load_stacking, structural validation (DataError with path + entry index), and the DRAFT/APPROVED status policy (shipped_interactions). Its loader validates the pure-core research §6 schema: schema_version == 1; interactions with id/mode/name/distance_a (float > 0, <= 10)/lateral_offset_a (float >= 0)/citation (key resolving in citations)/explanation/applies_to.sets (non-empty)/status in {'DRAFT','APPROVED'}; unique ids; citations entries with short/doi/approved. READ serpentrum/molecule_data.py and tests/test_molecule_data.py FIRST and map this plan's dataset onto the loader's actual accepted schema — if the loader rejects any field of the dataset below, adapt the DATASET to the loader (never weaken the loader in this plan; molecule_data.py is not in this plan's files).

Load-bearing verified facts (02-RESEARCH-demo-data.md, live-verified 2026-09-07):
- Janiak 2000 full text is CLOSED everywhere (Unpaywall: oa_status closed, no repository copy; RSC 403). The ABSTRACT verifies: usual pi interaction is offset/slipped (parallel-displaced) stacking; ring normal vs centroid vector ~20 deg; centroid-centroid up to 3.8 A. NO accessible source states 3.3 or 3.4 A — both UNVERIFIED.
- Measured CC0 COD stacks (computed from CIFs this session, script + arithmetic in research §3): phenanthrene-TCNB COD 4003564 = 3.555 A (perp 3.345, offset 1.204, 2.7 deg, DOI 10.1021/acs.chemmater.0c01184); pyrene COD 2100607 = 3.570 A (perp 3.472, offset 0.829, 0.0 deg, DOI 10.1107/S0108768106026814); pyrene COD 2100608 = 3.580 A (perp 3.478, offset 0.851). All three round to 3.6 A.
- Neat Set A crystals (benzene/naphthalene/anthracene/phenanthrene) are herringbone — NO pi-stacks (closest contacts 4.6-5.1 A at 50-86 deg). The game uses idealized pairwise interaction geometry, labeled as such.
- Recommendation: store 3.6 A centroid-centroid + 20 deg displacement (offset = d*sin 20 deg = 1.231 A at d = 3.6). 3.4 A offered only with an institutional-access caveat (UNVERIFIED).
- place_pickup composition (02-04's locked API, research §3): placed ring centroid = tail_centroid + tail_normal*distance_a + lateral_offset_a*tail_ref. Therefore distance_a is the PERPENDICULAR (ring-normal) component and lateral_offset_a the in-plane component; the composed centroid-centroid distance is sqrt(distance_a^2 + lateral_offset_a^2) and the off-normal angle is atan2(lateral, distance_a). Encoding the recommended geometry: distance_a = 3.383 (= 3.6*cos20deg), lateral_offset_a = 1.231 (= 3.6*sin20deg) -> sqrt = 3.60001, angle = 19.998 deg. Option-b encoding: 3.355 / 1.221 (from 3.57) -> sqrt = 3.57016, angle = 19.993 deg.
- Repo hard rule DATA-02: every shipped distance is pinned from a real source and explicitly human-approved. DATA-04: DATA_SOURCES.md in bioCHEMeleon format; CSD/CCDC data cited, never redistributed.
- Loader/location convention (research §6): data files live inside the plugin package; tests locate them via os.path.join(os.path.dirname(serpentrum.__file__), 'data', ...). serpentrum/data/ is plugin-path-safe (.json/.md are invisible to the purity walk and findPlugins).
- Python 3.6.9, tests/ has NO __init__.py, sys.path self-insert per test file, zero sys.modules stubs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the DRAFT stacking dataset JSON + DATA_SOURCES.md</name>
  <files>serpentrum/data/stacking_pi_stack.json, serpentrum/data/DATA_SOURCES.md</files>
  <action>
Create serpentrum/data/stacking_pi_stack.json with EXACTLY this content (status DRAFT, recommended-value components):

```json
{
  "schema_version": 1,
  "interactions": [
    {
      "id": "pi_stack_pd",
      "mode": "pi_stack",
      "name": "pi-pi stacking (parallel-displaced)",
      "distance_a": 3.383,
      "lateral_offset_a": 1.231,
      "uncertainty_a": null,
      "citation": "janiak2000",
      "explanation": "Aromatic rings stack face-to-face but slightly offset (~20 deg off the ring normal), not eclipsed. distance_a is the perpendicular separation component and lateral_offset_a the lateral component consumed directly by stacking.place_pickup; together they encode a centroid-centroid distance of sqrt(3.383^2 + 1.231^2) = 3.60 A at atan2(1.231, 3.383) = 20.0 deg off-normal. Basis: measured parallel-displaced stacks 3.555/3.570/3.580 A (COD 4003564, 2100607, 2100608) all round to 3.6 A; Janiak 2000 abstract: usual stacking is parallel-displaced, ring-normal angle ~20 deg, centroid-centroid up to 3.8 A. Idealized pairwise geometry - neat Set A crystals are herringbone (see DATA_SOURCES.md).",
      "applies_to": {"sets": ["set_a"]},
      "status": "DRAFT"
    }
  ],
  "citations": {
    "janiak2000": {"short": "Janiak 2000", "doi": "10.1039/b003010o", "approved": false},
    "cod4003564": {"short": "COD 4003564 (phenanthrene-TCNB cocrystal)", "doi": "10.1021/acs.chemmater.0c01184", "approved": false},
    "cod2100607": {"short": "COD 2100607 (pyrene, 298 K)", "doi": "10.1107/S0108768106026814", "approved": false}
  }
}
```

Every number above is transcribed from 02-RESEARCH-demo-data.md §2/§4/§6 (the DOI strings are copied verbatim from the research). Do NOT "improve" any value.

Create serpentrum/data/DATA_SOURCES.md by transcribing 02-RESEARCH-demo-data.md §5 (the draft content block, "bioCHEMeleon format") essentially verbatim, with only these mechanical adaptations:
- keep the title line `# DATA_SOURCES.md — serpentrum demo data sources & licenses (DRAFT — NOT APPROVED)` and the STATUS LEGEND exactly as drafted
- keep every [VERIFIED]/[UNVERIFIED] marker, DOI, license note, COD entry, and the "Known UNVERIFIED items" section verbatim (3.3/3.4 A must be listed as do-not-ship)
- add the precision footnote from research §8 item 6: computed distances carry experimental uncertainties of ~0.01-0.05 A per entry; immaterial at the shipped 0.1 A rounding
- the molecule-geometry section (PubChem CIDs 241/931/8418/995/7095) stays marked as Phase-8 shipping (CIDs verified 2026-09-07; SDF files themselves are not part of this plan)

Verify the dataset through the real loader (from repo root):
`python3.6 -c "import sys, os, json; sys.path.insert(0, '.'); from serpentrum import molecule_data; d = molecule_data.load_stacking(os.path.join('serpentrum', 'data', 'stacking_pi_stack.json')); i = d['interactions'][0]; print(i['status'], i['distance_a'], i['lateral_offset_a'], len(d['citations']))"` → prints `DRAFT 3.383 1.231 3`

Note: serpentrum/data/stacking_pi_stack.json is THE shipped interaction dataset (contract-fixed path). If a leftover serpentrum/data/demos/stacking.json exists from wave 1, leave it in place but flag it in the summary — the shipped path is this one.
  </action>
  <verify>the loader one-liner above prints `DRAFT 3.383 1.231 3`; `python3.6 -c "import json; json.load(open('serpentrum/data/stacking_pi_stack.json'))"` exits 0; `grep -c "UNVERIFIED" serpentrum/data/DATA_SOURCES.md` >= 2</verify>
  <done>Draft dataset + DATA_SOURCES.md exist at the contract-fixed paths, byte-clean JSON, loader-validated, every value traceable to the research document</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_stacking_dataset.py — loader-validated, decision-agnostic dataset tests</name>
  <files>tests/test_stacking_dataset.py</files>
  <action>
Write tests/test_stacking_dataset.py (sys.path self-insert preamble). The tests MUST pass in BOTH the pre-checkpoint (DRAFT) and post-checkpoint (APPROVED, possibly option-b values) states — assert invariants, never the specific chosen values:

1. Locate the dataset the plugin-runtime-safe way: `os.path.join(os.path.dirname(serpentrum.__file__), 'data', 'stacking_pi_stack.json')`; assert it exists.
2. `data = molecule_data.load_stacking(path)` succeeds with no DataError (this IS the structural validation: schema_version 1, interaction fields, citation resolution, applies_to).
3. Exactly one interaction: id 'pi_stack_pd', mode 'pi_stack', non-empty name; `status in ('DRAFT', 'APPROVED')`.
4. Derived-geometry consistency computed FROM THE FILE (no hardcoded chemistry in the assertion beyond the two approved headline values): `d = entry['distance_a']; l = entry['lateral_offset_a']`; centroid = math.sqrt(d*d + l*l); assert abs(centroid - round(centroid, 2)) <= 0.005 (clean 2-decimal headline: 3.60 or 3.57); angle = math.degrees(math.atan2(l, d)); assert abs(angle - 20.0) <= 0.5 (the verified Janiak displacement).
5. Citation integrity: entry['citation'] resolves in data['citations']; every citation entry has non-empty 'short' and 'doi' and an 'approved' bool; every doi in data['citations'] is one of the three verified DOIs (10.1039/b003010o, 10.1021/acs.chemmater.0c01184, 10.1107/S0108768106026814).
6. Shipping policy: `molecule_data.shipped_interactions(data)` returns [] iff status == 'DRAFT', else the single entry (DATA-02 gate works in both states).
7. DATA_SOURCES.md exists next to the dataset; is non-trivial (> 60 lines); contains: 'DRAFT', all three DOI strings, 'UNVERIFIED', '4003564', '2100607', '3.555', 'herringbone'.
8. Tamper-proofing (proves the real file passes because it is VALID, not because validation is absent): copy the dataset into a tempfile.mkdtemp dir (cleanup via addCleanup + shutil.rmtree — never shell rm), delete the 'distance_a' key from interactions[0], assert molecule_data.load_stacking raises molecule_data.DataError naming the file.
  </action>
  <verify>`python3.6 -m unittest tests.test_stacking_dataset -v` → all OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK</verify>
  <done>Real dataset validated through the 02-05 loader; geometry composition and shipping policy asserted decision-agnostically; loader tamper-proofing proven; suite green</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which stacking distance value ships in serpentrum/data/stacking_pi_stack.json (and whether its status flips DRAFT -> APPROVED)</decision>
  <context>Repo hard rule DATA-02: no invented data; every shipped distance needs explicit human approval. Research (02-RESEARCH-demo-data.md) pinned: 3.555 / 3.570 / 3.580 A centroid-centroid from three CC0 COD CIFs (phenanthrene-TCNB 4003564, pyrene 2100607/2100608 — interplanar 3.345-3.478 A, offsets 0.83-1.20 A) and the ~20 deg ring-normal displacement from the VERIFIED Janiak 2000 abstract (usual stacking up to 3.8 A). 3.4 A is UNVERIFIED — no accessible source states it (Janiak full text closed everywhere; RSC 403; Unpaywall no-copy). The dataset currently holds the recommended encoding (distance_a 3.383 = 3.6*cos20deg, lateral_offset_a 1.231 = 3.6*sin20deg -> centroid-centroid 3.60 A at 20.0 deg) with status DRAFT.</context>
  <options>
    <option id="option-a">
      <name>3.6 A + 20 deg (RECOMMENDED)</name>
      <pros>Every parameter verified: rounds all three measured CC0 stacks (3.555/3.570/3.580); consistent with Janiak's <= 3.8 A abstract rule; 20 deg displacement abstract-verified. Dataset keeps distance_a 3.383 / lateral_offset_a 1.231 (centroid-centroid 3.60 A).</pros>
      <cons>0.1 A rounding of the measured values (documented in the explanation string).</cons>
    </option>
    <option id="option-b">
      <name>3.57 A exact (COD 2100607 pyrene)</name>
      <pros>Traceability-maximal: a single measured CIF value, no rounding. Dataset becomes distance_a 3.355 / lateral_offset_a 1.221 (3.57*cos/sin 20deg -> centroid-centroid 3.570 A); citation flips to cod2100607.</pros>
      <cons>Bound to one structure/temperature point; discards the phenanthrene-TCNB corroboration from the headline value.</cons>
    </option>
    <option id="option-c">
      <name>Hold DRAFT pending institutional-access verification of 3.4 A</name>
      <pros>Keeps the door open for the (unverified) 3.4 A candidate if the human can read Janiak 2000 or Hunter-Sanders full text via authorized access and finds a stated value.</pros>
      <cons>No APPROVED value ships now; DATA-02 sign-off for Set A completes in Phase 8 (dataset stays DRAFT, current values unchanged).</cons>
    </option>
  </options>
  <resume-signal>Select: option-a / option-b / option-c</resume-signal>
</task>

Post-decision actions (perform immediately after the user responds, still inside this plan):

- **option-a**: keep distance_a 3.383 / lateral_offset_a 1.231; edit the interaction's "status" to "APPROVED" and set "approved": true on all three citation entries. Re-run `python3.6 -m unittest tests.test_stacking_dataset -v` (must stay green — the tests are decision-agnostic).
- **option-b**: set distance_a 3.355 / lateral_offset_a 1.221; set citation to "cod2100607"; flip status to "APPROVED" and all "approved" flags to true. Re-run the dataset tests (must stay green).
- **option-c**: change nothing in the dataset (stays DRAFT at 3.383/1.231); record in the SUMMARY that DATA-02 approval for the Set A distance completes in Phase 8 (the Phase-8 gate).
- In every case: record the decision, the chosen values, and the final status in the plan SUMMARY's "Decisions Made" section; run `python3.6 tests/run_gates.py` once more before finishing.

</tasks>

<verification>
- `python3.6 -m unittest tests.test_stacking_dataset -v` → OK in the post-decision state
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK
- `python3.6 tests/run_gates.py` → exit 0 (dataset .json/.md are plugin-path-safe and invisible to the purity walk)
- `python3.6 -c "import json; d = json.load(open('serpentrum/data/stacking_pi_stack.json')); i = d['interactions'][0]; print(i['status'], i['distance_a'], i['lateral_offset_a'])"` → reflects the human's decision (APPROVED 3.383 1.231 / APPROVED 3.355 1.221 / DRAFT 3.383 1.231)
- DATA_SOURCES.md retains DRAFT header + all three DOIs + UNVERIFIED section regardless of decision (full checklist approval is Phase 8)
</verification>

<success_criteria>
- The shipped interaction dataset is a validated data file (never code constants): loads through molecule_data.load_stacking, one pi_stack_pd interaction, citations resolve, applies_to set_a
- Stored pair encodes a verified centroid-centroid distance at ~20 deg off-normal, composed arithmetically in the tests from the file's own fields
- DATA_SOURCES.md draft complete with licenses + UNVERIFIED list (3.3/3.4 A marked do-not-ship)
- The ONLY route to a status/value change was the blocking human decision checkpoint; the decision is recorded in the SUMMARY
- Tests pass in both DRAFT and APPROVED states; gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-11-SUMMARY.md`
</output>
