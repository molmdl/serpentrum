---
phase: 02-pure-core-game-chemistry-logic
plan: 14
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-07", "02-08", "02-12", "02-13"]
files_modified:
  - tests/test_integration_pure_core.py
  - AGENTS.md
autonomous: true

must_haves:
  truths:
    - "The SHIPPED dataset drives a real placement: loading serpentrum/data/stacking_pi_stack.json through molecule_data and calling stacking.place_pickup with the file's own (distance_a, lateral_offset_a) yields a ring-centroid-to-tail-centroid distance equal to the file-derived sqrt(d^2+l^2) within 1e-4 — STACK-01 with zero code constants"
    - "The stacking math reproduces the committed dimer2 geometry exactly: place_pickup at the fixture's own 3.4 / 0.0 equals fragment 2 within 1e-6 A over all 13 atoms, and the legal stack passes check_clash while a 1.5 A placement fires it (phase success criterion 1)"
    - "Atom counts flow through the whole chain: the parser's n_atoms (26) equals the xyz fragment + placed counts; 72 == 3*26 - 6 g98 modes; 78 == 3*26 vibspectrum modes with correspondence at the computed offset"
    - "The xtb success contract holds on fixture bytes: evaluate_run with ohess.err ('normal termination of xtb') + both expected files -> ok; repro_oh.err (stderr success, NO files) -> NOT ok; bad.err ('abnormal termination') -> NOT ok (criterion 3)"
    - "The full python3.6 suite passes under tests/run_gates.py with zero sys.modules stubs and stdlib-only pure modules (criterion 4); AGENTS.md audited against research R10 and edited only on real drift"
  artifacts:
    - path: "tests/test_integration_pure_core.py"
      provides: "the cross-module chain: molecule_data -> xyzio -> stacking (frame/placement/clash) -> xtbenv contract -> spectra parse + mode counts -> xyzio round-trip"
      min_lines: 150
    - path: "AGENTS.md"
      provides: "gate/convention documentation, touched ONLY if the R10 audit finds real drift (expected: no edit)"
  key_links:
    - from: "tests/test_integration_pure_core.py"
      to: "serpentrum/molecule_data.load_stacking"
      via: "the placement distance is read FROM the dataset file (a schema/format regression breaks the chain)"
      pattern: "load_stacking"
    - from: "tests/test_integration_pure_core.py"
      to: "serpentrum/stacking + serpentrum/xyzio + serpentrum/spectra + serpentrum/xtbenv"
      via: "single flow where each stage consumes the previous stage's output (placed atom count becomes the expected parser n_atoms)"
      pattern: "place_pickup|read_xyz|parse_g98|evaluate_run"
    - from: "tests/test_integration_pure_core.py"
      to: "serpentrum/setup_logic BOX_PRESETS"
      via: "the integration boundary box comes from setup_logic's medium preset (setup -> engine seam)"
      pattern: "BOX_PRESETS|box_preset"
---

<objective>
Prove the pure core works as ONE system: a single integration test that chains the shipped dataset -> xyz geometry -> stacking placement (both the exact dimer2 reproduction and the dataset-driven placement) -> xtbenv success contract on fixture bytes -> spectra parsing with 3N mode-count consistency -> xyzio round-trip — then bring the full suite to green under the gates and audit AGENTS.md against research R10 (touch only on real drift).

Purpose: Phases 5-7 will wire these modules into the live game. Unit tests prove each module in isolation; this plan proves the SEAMS: the distance comes from the data file (STACK-01/02), the placed atom count becomes the parser's expected n_atoms, the xtb contract consumes the same run's fixture bytes, and the xyz handoff format round-trips. A regression in any module or schema breaks this chain even if every unit test passes — that is the point.

Output: tests/test_integration_pure_core.py + the full-suite green run + an AGENTS.md audit record.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md
@serpentrum/spectra.py
@serpentrum/xtbenv.py
@serpentrum/xyzio.py
@serpentrum/stacking.py
@serpentrum/molecule_data.py
@serpentrum/game_engine.py
@serpentrum/setup_logic.py
@serpentrum/cgo_build.py
@serpentrum/data/stacking_pi_stack.json

Wave/dependency note: ALL prior Phase-2 plans are merged by the time this wave-4 plan runs: 02-01/02 (spectra g98 parser, xtbenv), 02-03 (xyzio), 02-04 (stacking math + clash gate), 02-05 (molecule_data loader), 02-06 (engine core), 02-07 (setup_logic), 02-08 (cgo_build), 02-09..02-13 (wave 2-3 completions). serpentrum/data/stacking_pi_stack.json exists (02-11, wave 2 — DRAFT or APPROVED per the human decision; this test is decision-agnostic). READ the actual signatures in serpentrum/stacking.py, serpentrum/xyzio.py, serpentrum/xtbenv.py, serpentrum/molecule_data.py, serpentrum/setup_logic.py before writing the test — the research APIs are the contract, but adapt call sites to the implementations' real parameter names (the GEOMETRIC/BEHAVIORAL contracts are what is fixed).

Load-bearing verified facts (probed against committed fixtures, 2026-09-06/07):
- dimer2.xyz: 26 atoms; fragment 1 = atoms 1-13, fragment 2 = atoms 14-26; fragment2 = fragment1 + (0, 0, 3.4) EXACTLY (max per-coordinate error 0.0 over all 13 atoms — verified twice this session); ring atoms of each fragment = indices 0-5 (element order CCCCCCHHHHHOH); comment 'stacked phenol dimer 3.4A z-offset'. The 3.4/0.0 placement values are FIXTURE KNOWLEDGE (the committed file's measured geometry) — not chemistry claims; the shipped dataset intentionally stores the VERIFIED 3.6-equivalent components instead.
- place_pickup composition (02-04's locked API): placed centroid = tail_centroid + tail_normal*distance_a + lateral_offset_a*ref. Dataset pair (3.383, 1.231) composes to centroid-centroid 3.60001 A at 19.998 deg (option-a values); (3.355, 1.221) -> 3.57016 at 19.993 deg (option-b). The test derives the expected centroid distance as sqrt(d^2 + l^2) FROM THE FILE.
- check_clash threshold 2.5 A: the legal 3.4 A eclipsed stack passes (min inter-fragment contact in dimer2 is 3.4000 A); a 1.5 A separation fires.
- xtbenv contract (research §4): evaluate_run(exit_code, stderr_text, expected_files, present_files) -> RunVerdict(ok, problems). Fixture bytes: ohess.err / co2.err / dimer2.err / repro_oh.err all = 'normal termination of xtb' + CRLF; bad.err = 'abnormal termination of xtb'. repro_oh is the load-bearing case: stderr success with NO output files must NOT be ok (the run produced no Hessian/frequency output — the expected-files leg is load-bearing, not belt-and-braces). Exit codes are NOT recorded in fixtures — tests use synthetic rc values (0 and 128 from PITFALLS 3).
- spectra: g98.out → 26 atoms / 72 modes (3*26 - 6); vibspectrum → 78 modes (3*26); correspondence offset = 3*n_atoms - len(g98_modes) = 6 (computed, cross-checked against the vibspectrum trivial-row count); tolerances freq 0.01 / intensity 1e-4 (2-decimal freq column, 4-decimal intensity column; exact boundary cases at mode 77 = 0.0049 and mode 59 = 5.0e-5).
- xyzio: write_xyz(elements, coords, comment) / read_xyz(path) -> (comment, [(symbol, x, y, z)]); ELEMENT_SYMBOLS validation; round-trip equality within 1e-8.
- setup_logic.BOX_PRESETS['medium'] == ((-18.0, -18.0), (18.0, 18.0)) — use it as the integration boundary box (the real setup -> engine seam).
- Test conventions: sys.path self-insert per file; discovery `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (never `-t .`); ZERO sys.modules stubs (the suite's import graph is pure — test_skeleton.py proves the pattern); tempfile.mkdtemp + shutil.rmtree in addCleanup (never shell rm).
- Research R10 verdict (§10): new Phase-2 modules need NO AGENTS.md edits (default-PURE classification covers them; gates auto-cover; fixture-path conventions belong in plan summaries, not AGENTS.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_integration_pure_core.py — the cross-module chain</name>
  <files>tests/test_integration_pure_core.py</files>
  <action>
Write ONE integration test module (sys.path self-insert preamble) where data flows through the chain — not parallel independent tests. Fixture paths: tests/fixtures/xtb/ copies (g98.out, vibspectrum, dimer2.xyz, ohess.err, repro_oh.err, bad.err) + serpentrum/data/stacking_pi_stack.json. Adapt every call to the actual merged signatures (read the modules first). Structure as ordered test methods sharing a module-level helper that builds the chain once, or as one long test method per stage-group — either way the DATA must flow:

STAGE 1 — dataset (STACK-02): `data = molecule_data.load_stacking(<packaged path to serpentrum/data/stacking_pi_stack.json>)`; entry = data['interactions'][0]; d = entry['distance_a']; l = entry['lateral_offset_a']; expected_centroid = math.sqrt(d*d + l*l). Assert d > 0 and l >= 0.

STAGE 2 — geometry (xyzio): comment, atoms = xyzio.read_xyz(tests/fixtures/xtb/dimer2.xyz); assert len(atoms) == 26; f1 = atoms[:13]; f2 = atoms[13:] (13/13 split is documented fixture knowledge); f1_coords = [(x, y, z) for (s, x, y, z) in f1].

STAGE 3a — EXACT REPRODUCTION (phase criterion 1): c, n, ref = stacking.ring_frame(f1_coords, [0, 1, 2, 3, 4, 5]); placed = stacking.place_pickup(f1_coords, [0, 1, 2, 3, 4, 5], c, n, ref, distance_a=3.4, lateral_offset_a=0.0); assert max(|placed[i] - f2_coords[i]| over all 3 coords, i in 0..12) < 1e-6 (the committed eclipse is a pure z-translation of 3.4). Then clash gate: check_clash(placed, f1_coords, box from setup_logic.BOX_PRESETS['medium']) is None; and placing at distance 1.5 instead → check_clash fires (returns a violation dict).

STAGE 3b — DATASET-DRIVEN PLACEMENT (STACK-01, decision-agnostic): placed2 = stacking.place_pickup(f1_coords, [0..5], c, n, ref, distance_a=d, lateral_offset_a=l); ring_centroid2 = mean of placed2[0..5]; measured = distance(ring_centroid2, c); assert abs(measured - expected_centroid) <= 1e-4; angle = math.degrees(math.atan2(l, d)); assert abs(angle - 20.0) <= 0.5; check_clash(placed2, f1_coords, box) is None (perp component >= 3.35 clears the 2.5 A threshold).

STAGE 4 — xtb contract (criterion 3) on fixture bytes: v_ok = xtbenv.evaluate_run(0, read(ohess.err), ('g98.out', 'vibspectrum'), ('g98.out', 'vibspectrum')) → v_ok.ok is True and problems == []; v_repro = evaluate_run(0, read(repro_oh.err), ('g98.out', 'vibspectrum'), ()) → ok is False and any problem mentions a missing file (stderr success is NOT sufficient); v_bad = evaluate_run(128, read(bad.err), ('g98.out', 'vibspectrum'), ()) → ok is False.

STAGE 5 — parse + mode counts (criterion 2): g = spectra.parse_g98(tests/fixtures/xtb/g98.out); assert g.n_atoms == 26 == len(f1) + len(placed2) (the placed atom count from the stacking stage IS the expected parser atom count — the xyz/parse layers must agree); assert len(g.modes) == 3 * 26 - 6 == 72; vs = spectra.parse_vibspectrum(tests/fixtures/xtb/vibspectrum); assert len(vs.modes) == 3 * 26 == 78; offset = 3 * 26 - len(g.modes) (computed); for j in range(72): abs(vs.modes[j + offset].freq - g.modes[j].freq) <= 0.01 and intensity <= 1e-4.

STAGE 6 — handoff round-trip: elements = [s for (s, x, y, z) in f1] + ['C'] * 13 (or reuse placed2's symbols if the stacking API returns them — use whatever the real place_pickup returns; the 26-atom set is f1 + a pickup of the same molecule); coords = f1_coords + [tuple(p) for p in placed2]; text = xyzio.write_xyz(elements, coords, comment='serpentrum integration round-trip'); write to a tempfile.mkdtemp file (addCleanup shutil.rmtree), xyzio.read_xyz back → 26 atoms, symbols identical, coords equal within 1e-8.

Document at the top of the file what makes this a REAL integration proof (adapt research R11's four points to the dataset-driven reality): the distance is read from the dataset (schema/format regressions break the chain); the placed atom count feeds the parser assertion; the contract evaluation consumes fixture bytes from the same run the parser fixtures came from; the xyz round-trip proves the Phase-6 handoff format.
  </action>
  <verify>`python3.6 -m unittest tests.test_integration_pure_core -v` → all stages OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK</verify>
  <done>Single-chain integration proven: dataset-driven placement matches the file-derived centroid distance (1e-4), dimer2 reproduced exactly (1e-6), clash gate both ways, xtb contract on fixture bytes (incl. the repro_oh negative case), 3N mode counts + correspondence, xyz round-trip</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green + zero-stub audit + AGENTS.md R10 audit (edit only on drift)</name>
  <files>AGENTS.md</files>
  <action>
1. Full suite + gates: run `python3.6 tests/run_gates.py` → must exit 0 (gate 1 syntax + plugin-path safety, gate 2 purity — ALL serpentrum/ modules classify PURE, gate 3 scoped unittest over every Phase-2 test file). Also run `python3.6 -m unittest discover -s tests -p "test_*.py" -v` directly and confirm OK.
2. Zero-stub audit (INFRA-02): `grep -rn "sys.modules\\[" tests/` → expect NO matches anywhere (assertions use assertNotIn membership checks, never assignment). `grep -rln "import numpy\\|from numpy\\|import pymol\\|from pymol\\|import pmg_tk\\|from pmg_tk\\|PyQt5" serpentrum/*.py` → expect NO matches (all eight Phase-2 modules pure stdlib). If either grep finds something, that is a gate failure to fix here (small wrap/rewrite), not to document away.
3. AGENTS.md audit against research R10 (§10) — verify each claim still holds on the finished tree:
   a. every serpentrum/*.py classifies PURE automatically: `python3.6 tools/check_purity.py` exits 0
   b. the discovery command convention is unchanged and green (run_gates gate 3)
   c. the zero-stub rule is intact (audit step 2)
   d. no genuinely NEW convention emerged that AGENTS.md contradicts. NOTE per R10: fixture-path conventions and plan-level decisions are documented in plan SUMMARYs, NOT AGENTS.md.
   Expected outcome: NO edit — record "R10 audit: no drift, AGENTS.md unchanged" in the plan SUMMARY. ONLY if a claim demonstrably drifted (e.g. a module had to become GUI-classed, which would itself be a phase-contract violation — or a gate behavior changed), make the MINIMAL touch-up to the affected AGENTS.md bullet and record the reason in the SUMMARY.
4. Confirm the Phase-2 success criteria are now testably true and list them in the SUMMARY: (1) stacking data-driven + dimer2 reproduction; (2) spectra parser on real fixtures incl. negatives + zero-intensity + loud corrupt failure; (3) xtb contract + detection pure-tested; (4) full suite green, zero stubs, stdlib-only.
  </action>
  <verify>`python3.6 tests/run_gates.py` → exit 0; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → OK; `grep -rn "sys.modules\\[" tests/` → no matches; `python3.6 tools/check_purity.py` → exit 0</verify>
  <done>Whole Phase-2 suite green under the gates with zero sys.modules stubs and stdlib-only pure modules; AGENTS.md audited (no drift expected — recorded either way)</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_integration_pure_core -v` → OK (all stages)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → OK (every Phase-2 module's tests)
- `python3.6 tests/run_gates.py` → exit 0
- `python3.6 tools/check_purity.py` → exit 0
- `grep -rn "sys.modules\\[" tests/` → no matches
- `grep -rln "numpy\\|pymol\\|pmg_tk\\|PyQt5" serpentrum/*.py` → no matches
</verification>

<success_criteria>
- The integration chain passes with data flowing through: dataset distance -> placement -> measured centroid distance (1e-4); fixture geometry -> exact dimer2 reproduction (1e-6) + clash gate both ways; placed atom count -> parser n_atoms (26) -> 72/78 mode counts + correspondence; fixture .err bytes -> xtb contract verdicts (ok / not-ok-repro_oh / not-ok-bad); xyz write -> read round-trip identical
- Full Phase-2 suite green on WSL python3.6.9 under tests/run_gates.py; zero sys.modules stubs; all serpentrum/ modules stdlib-only PURE (INFRA-02, criterion 4)
- AGENTS.md matches reality per the R10 audit (no drift expected; any drift minimally corrected and recorded)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-14-SUMMARY.md`
</output>
