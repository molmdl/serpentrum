---
phase: 02-pure-core-game-chemistry-logic
plan: 10
type: execute
wave: 2
depends_on: ["02-06"]
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_rules.py
autonomous: true

must_haves:
  truths:
    - "Head entering the BOUNDARY_MARGIN_A=1.0 margin of the AABB emits ('crashed', 'boundary'), sets finished=True / result='crashed', clears the pending queue, and every later step() is a no-op — for all four walls, inclusive at the margin-adjusted wall"
    - "Self-collision is head-centroid vs the chain POLYLINE EDGES built from segment centroids (GAME-05's literal 'head-centroid vs chain segments' model — authoritative): squared point-to-segment distance < BODY_COLLISION_RADIUS_A**2 (2.0 A) on any checked edge (edges[:n-1-SEGMENT_SKIP_RECENT]; the 2 newest edges exempt) emits ('crashed', 'body')"
    - "Head within PICKUP_RADIUS_A=3.0 of a live pickup's centroid emits ('stacked', pickup) exactly once and claims it; pickup records carry 'atoms'; attach_segment() appends the frozen segment (GAME-10); reject_pickup(id, reason) rolls the counters back, re-arms the pickup, and RETURNS the canonical 3-tuple ('refused', pickup_id, reason)"
    - "molecules_stacked reaching cap emits ('won',) with finished=True / result='won'; crossing atom_budget emits ('budget_warning', atoms_total) exactly once per run without stopping the game"
    - "Counters (molecules_stacked, atoms_total, pickups_remaining) are exact after every capture, rejection, and event"
  artifacts:
    - path: "serpentrum/game_engine.py"
      provides: "collision rules + pickup capture/attach/reject + win-cap/budget counters layered on the 02-06 movement core, with constants BODY_COLLISION_RADIUS_A=2.0, SEGMENT_SKIP_RECENT=2, PICKUP_RADIUS_A=3.0, BOUNDARY_MARGIN_A=1.0, SWEEP_PICKUP_CLEARANCE_A=2.5 (atom-level sweep clearance consumed by 02-13) and the documented radius < 3.4 A stacking-distance constraint"
      contains: "PICKUP_RADIUS_A"
    - path: "tests/test_engine_rules.py"
      provides: "deterministic boundary/body/pickup/win/budget scenario tests on exact axis movements (with shown arithmetic for every exact-tick assertion)"
      min_lines: 160
  key_links:
    - from: "serpentrum/game_engine.py step()"
      to: "_point_segment_distance_sq (head-centroid vs chain polyline edges)"
      via: "shared module-level helper used by both the forward-motion body check and (in 02-13) the swept pre-check body leg"
      pattern: "_point_segment_distance_sq|_segment_distance"
    - from: "tests/test_engine_rules.py"
      to: "serpentrum.game_engine.GameEngine"
      via: "direct state injection (head/heading/segments/pickups as plain data) + step(dt) event assertions"
      pattern: "GameEngine"
    - from: "serpentrum/game_engine.py constants"
      to: "3.4 A committed stacking distance (dimer2 fixture)"
      via: "constraint comment: BODY_COLLISION_RADIUS_A must stay < 3.4 minus margin so the head never 'collides' with the segment just stacked"
      pattern: "3\\.4"
---

<objective>
Layer the GAME-05/GAME-06 collision-and-rules half onto the 02-06 movement core: AABB boundary crash, segment-based self-collision (head-centroid vs chain polyline edges with a neck exemption), pickup capture with controller-confirm/attach-or-reject semantics, and the score/win-cap/atom-budget counters with their crash/win/warning events.

Purpose: The 02-06 core moves and queues directions but cannot end a run or stack a molecule (its trimmed movement-only scope). This plan makes the engine a complete referee: GAME-05 ("segment-based collision: head-centroid vs chain segments" — realized as the polyline through segment centroids, the authoritative model), GAME-06 (win at cap), GAME-04 counters (hidden atom budget for the SPECTRA-06 pre-xtb check), and the STACK-05 controller seam ('stacked' event -> controller places -> attach_segment or reject_pickup).

Output: extended serpentrum/game_engine.py + tests/test_engine_rules.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@serpentrum/game_engine.py
@tests/test_engine_core.py

Wave/dependency note: 02-06 (wave 1) delivered the MOVEMENT-ONLY core: GameEngine(head=(0.0,0.0), heading='right', segments=None) with continuous-2D state, heading stored as a unit vector, the segments state seam (index 0 = oldest, records `{'molecule_id', 'centroid': (x, y), 'atoms': [(sym, x, y, z)], 'atoms_n': int}`, new stacks APPEND at the end, engine copies caller dicts), request_direction (180-degree/same/buffer-full rules vs the CURRENT heading, max 1 pending), step(dt) constant-speed forward movement emitting ('moved', (x, y)) with pending deliberately INERT, pause/resume/reset. 02-06 has NO collision, capture, counter, box, or end-of-run code — this plan adds all of it. READ serpentrum/game_engine.py and tests/test_engine_core.py FIRST — this plan EXTENDS that file, never breaks its public contract (extend the constructor/reset with keyword args with safe defaults; 02-06's tests never pass them), and tests/test_engine_core.py must stay green (owned by 02-06, movement tests only — never edit it; this plan adds its own tests/test_engine_rules.py).

Load-bearing verified facts / locked decisions (research §7 + ROADMAP split):
- Engine model is CONTINUOUS 2D coordinates (not grid) — GAME-05's "head-centroid vs chain segments" and GAME-10's rigid pivot sweeps are incompatible with grid quantization (research resolves the contradiction; ARCHITECTURE.md "grid" wording is stale).
- **Body model (AUTHORITATIVE, chosen at revision): head-centroid vs chain POLYLINE EDGES built from segment centroids** — GAME-05's literal wording. The chain polyline connects segment centroids c[0..n-1] (index 0 = oldest); the head's 2D distance to each CHECKED edge uses the standard clamped point-to-segment projection. Per-atom sphere checks are NOT used (that was a superseded draft variant).
- Constants (ONE documented block; skip any 02-06 already defines — 02-06 defines only SPEED_A_PER_S + DIRS):
```python
BODY_COLLISION_RADIUS_A = 2.0   # head-centroid vs chain-edge clearance.
# MUST stay < 3.4 A (committed dimer2 stacking distance) minus margin, else the
# head would collide with the segment just stacked behind it.
SEGMENT_SKIP_RECENT = 2         # newest chain edges exempt from self-collision
PICKUP_RADIUS_A = 3.0           # head-vs-pickup-CENTROID capture distance
BOUNDARY_MARGIN_A = 1.0         # = BODY_COLLISION_RADIUS_A / 2 — the ONE margin
# constant name everywhere (02-13's boundary leg reuses it; HEAD_WALL_MARGIN_A is retired)
SWEEP_PICKUP_CLEARANCE_A = 2.5  # atom-level clearance for 02-13's sweep pickup leg
# (same rationale as 02-04 check_clash's 2.5 A inter-fragment threshold; defined
# here because this plan owns the constants block — 02-13 consumes it)
```
- Boundary crash: `hx <= x0 + BOUNDARY_MARGIN_A or hx >= x1 - BOUNDARY_MARGIN_A or hy <= y0 + BOUNDARY_MARGIN_A or hy >= y1 - BOUNDARY_MARGIN_A` (inclusive at the margin-adjusted walls).
- Event order per research §7: ('moved',) -> boundary hit -> ('crashed', 'boundary'); then head vs polyline edges -> ('crashed', 'body'); then head within pickup radius -> ('stacked', pickup); then molecules_stacked >= cap -> ('won',). Emission stops at the first crash; a crashed/won engine no-ops on later steps. A crash also clears the pending queue (turn-state hygiene; 02-13 builds on this).
- Pickup flow (STACK-05 seam): capture marks the pickup claimed and DOES the counter increments; the controller then runs stacking.place_pickup + check_clash and calls attach_segment(...) on success or reject_pickup(...) on clash. attach_segment is counter-NEUTRAL (capture already counted) and appends the frozen segment record. reject_pickup ROLLS BACK the counters, re-arms the pickup, tracks a per-pickup refusal count, and RETURNS ('refused', pickup_id, reason) — the CANONICAL 3-tuple payload used by every plan and test (engine methods outside step() cannot emit onto step's event list, so the controller logs the returned tuple).
- Pickup records carry `'atoms'`: `{'id': str, 'centroid': (x, y), 'atoms': [(sym, x, y, z), ...], 'atoms_n': len(atoms)}` — 'atoms' is REQUIRED because 02-13's swept-region pickup leg consumes atom positions (atom-level clearance).
- Win semantics: `molecules_stacked >= cap` -> ('won',) on the capture tick (research §7 pins >=; GAME-06's "exceeds" wording is resolved by the engine spec). Never fires on a crash tick (crash stops processing first).
- atom_budget is a WARNING-LEVEL check, never a hard stop (SPECTRA-06 warns; the hard pre-xtb re-check is Phase 6's runner).
- Determinism: pure float math, no RNG, no wall-clock. Tests set engine state directly (plain data) and use assertAlmostEqual for positions.
- Python 3.6.9 syntax only (%-formatting precedent, no dataclasses); serpentrum/game_engine.py stays PURE (stdlib math only); tests use zero sys.modules stubs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boundary collision (AABB + BOUNDARY_MARGIN_A) + crash semantics</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Add the ONE documented constants block from <context> (BODY_COLLISION_RADIUS_A, SEGMENT_SKIP_RECENT, PICKUP_RADIUS_A, BOUNDARY_MARGIN_A, SWEEP_PICKUP_CLEARANCE_A with the WHY comments — the 3.4 A stacking-distance constraint and the BOUNDARY_MARGIN_A-is-the-one-margin-name note).
2. Extend the constructor/reset with keyword args `box_min=None, box_max=None` (each an (x, y) tuple). Default None = no boundary checking (02-06's free-moving tests never pass a box, so they stay untouched; the Phase-4 controller passes the setup_logic box preset). Add state fields `finished` (False) and `result` (None) — 02-06's trimmed scope has no end-of-run state, so this plan introduces it.
3. In step(dt), after the head moves and the ('moved',) event is emitted: if box_min/box_max are set and
   `hx <= x0 + BOUNDARY_MARGIN_A or hx >= x1 - BOUNDARY_MARGIN_A or hy <= y0 + BOUNDARY_MARGIN_A or hy >= y1 - BOUNDARY_MARGIN_A`
   → emit ('crashed', 'boundary'), set finished=True, result='crashed', clear the pending queue, and STOP event processing for this tick. All later step() calls return [] immediately while finished. (02-06 has no boundary code — add it fresh here; do not look for code to reconcile.)

Create tests/test_engine_rules.py (sys.path self-insert preamble; construct the engine exactly as test_engine_core.py does, then pass/assign state directly). Deterministic scenarios (SPEED 3.0, dt 0.1 → 0.3 A/step; box ((-18.0, -18.0), (18.0, 18.0)) → margin walls at ±17.0):
- four walls: place head near each wall with heading into it (e.g. head (16.8, 0) heading (1, 0) → first step to 17.1 > 17.0 → crash); assert ('crashed', 'boundary') exactly once, finished True, result 'crashed'
- inclusive edge: head set EXACTLY to (17.0, 0.0), heading (1, 0) → crash on next step (comparison is inclusive at the margin-adjusted wall)
- just inside: head (16.6, 0), one step → 16.9 < 17.0 → alive (no crash event)
- box None: no box set → head walks past where a wall would be with zero crash events
- after crash: a second step() returns [] and mutates nothing (head unchanged). (Pending-clearing on crash stays contract prose only: 02-06's forward-compat rule forbids stepping with a non-empty pending in this suite, and post-02-13 the step-start pop consumes pending before a crash can fire — do not build a stepped-with-pending test here.)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → boundary tests OK; `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → core + rules both OK (02-06 unbroken)</verify>
  <done>All four walls crash with the exact margin semantics (inclusive at wall−1.0); None box disables checking; crashed engine is inert with pending cleared; 02-06 suite still green</done>
</task>

<task type="auto">
  <name>Task 2: Polyline-edge self-collision (head-centroid vs chain edges, skip recent 2)</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Pure helper (module-level function so 02-13's sweep pre-check body leg can reuse it):
```python
def _point_segment_distance_sq(px, py, ax, ay, bx, by):
    """Squared distance from point (px, py) to segment (a -> b) in 2D.
    Standard clamped projection; compare against BODY_COLLISION_RADIUS_A ** 2."""
```
2. In step(dt), after the boundary check: build the chain polyline from segment centroids `c[0..n-1]` (index 0 = oldest/first stacked, n-1 = newest/nearest head). Check edges `(c[i], c[i+1])` for `i in range(0, n - 1 - SEGMENT_SKIP_RECENT)` — i.e. the SEGMENT_SKIP_RECENT edges nearest the head are exempt (the neck). If the head's squared distance to any checked edge `< BODY_COLLISION_RADIUS_A ** 2` (STRICT <) → emit ('crashed', 'body'), finished=True, result='crashed', clear pending, stop event processing. n small enough that range() is empty (n <= 1 + SEGMENT_SKIP_RECENT) → no body check possible.

Tests (append to tests/test_engine_rules.py; inject segment records directly — engine state is plain data; SHOW the arithmetic in comments for every exact-tick assertion):
- body crash (exact tick, arithmetic shown): head (0.0, 0.0) heading (1, 0); segments with centroids c0=(6.0, 1.0), c1=(10.0, 1.0), c2=(14.0, 5.0), c3=(18.0, 9.0) (n=4 → checked edges = edge 0 only: (c0,c1), the horizontal line y=1.0 for x in [6,10]). Head travels y=0 at +0.3/step. Step 14: head (4.2, 0.0) → nearest edge point is the clamped endpoint (6.0, 1.0) → distance sqrt(1.8² + 1.0²) = sqrt(4.24) ≈ 2.059 > 2.0 → alive. Step 15: head (4.5, 0.0) → sqrt(1.5² + 1.0²) = sqrt(3.25) ≈ 1.803 < 2.0 → ('crashed', 'body'), finished True, result 'crashed'. Assert the exact tick (15) and head (4.5, 0.0).
- neck exemption (skip proof): same c0/c1, but c2=(3.0, 0.0), c3=(9.0, 0.0) — edges (c1,c2) and (c2,c3) are the exempt newest 2; edge (c2,c3) is the horizontal line y=0.0 from x=3..9 — the head's own path (distance 0.0 at every tick!) — yet NO crash comes from it (skipped); the crash still arrives from edge 0 at step 15 exactly as above. This proves the skip window, not luck.
- strict comparison: edge from (0.0, 2.0) to (4.0, 2.0); head at (2.0, 0.0) → distance exactly 2.0 → NOT crashed (STRICT <); head at (2.0, 0.01) → distance 1.99 → crashed on next step
- crash precedence: construct a tick where the head crosses both a wall and an edge — assert exactly one ('crashed', ...) event and it is 'boundary' (boundary check runs first)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → all OK; `python3.6 -m unittest tests.test_engine_core -v` → 02-06 suite still OK</verify>
  <done>Body collision fires only on checked (non-recent) polyline edges with strict < 2.0 A at the exact computed tick; neck exemption proven with a distance-0 edge; boundary precedence exact; 02-06 unbroken</done>
</task>

<task type="auto">
  <name>Task 3: Pickup capture/attach/reject + score/win-cap/atom-budget counters + win event</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Extend the constructor/reset with keyword args `pickups=None` (list of records `{'id': str, 'centroid': (x, y), 'atoms': [(sym, x, y, z), ...], 'atoms_n': len(atoms)}` — 'atoms' REQUIRED, copied by the engine like segments), `cap=None`, `atom_budget=None` — only enforced when not None (02-06's tests never pass them). Add a `live_pickup_ids` set (all ids initially; capture removes; reject re-adds). Counters `molecules_stacked`, `atoms_total`, `pickups_remaining` are NEW state (02-06's trimmed scope has none — add them here, initialized 0 / 0 / len(pickups)).
2. In step(dt), after the body check (crash stops everything): scan pickups in list order; the FIRST live pickup whose squared centroid distance to the head `<= PICKUP_RADIUS_A ** 2` (inclusive) is captured: remove from live_pickup_ids, molecules_stacked += 1, atoms_total += pickup['atoms_n'], pickups_remaining -= 1, emit ('stacked', pickup_record) (the record includes 'atoms'). At most ONE capture per tick (deterministic list order).
3. `attach_segment(molecule_id, centroid, atoms)`: append `{'molecule_id', 'centroid', 'atoms', 'atoms_n': len(atoms)}` to segments (the frozen rigid body — GAME-10 invariant). Counter-NEUTRAL (capture already counted) — this is the controller's call AFTER stacking.place_pickup succeeds.
4. `reject_pickup(pickup_id, reason='clash')`: re-add the id to live_pickup_ids, roll the counters back (molecules_stacked -= 1, atoms_total -= atoms_n, pickups_remaining += 1), track a per-pickup refusal count, and RETURN the canonical 3-tuple `('refused', pickup_id, reason)`. This is the STACK-05 seam: the controller calls it when stacking.place_pickup + check_clash reject the placement. (step() events cannot be emitted from controller-called methods — the controller logs the returned tuple.)
5. Budget warning: after any capture, if atom_budget is not None and atoms_total > atom_budget and not self._budget_warned → self._budget_warned = True, emit ('budget_warning', atoms_total). Never a crash; at most once per run (flag cleared by reset()).
6. Win: after a capture, if cap is not None and molecules_stacked >= cap → emit ('won',), finished=True, result='won', clear pending. Win is checked AFTER capture on the same tick and never fires on a crash tick (crash stops processing first).

Tests (append; SHOW arithmetic for exact positions):
- capture: head (0,0) heading (1,0), pickup {'id': 'p1', 'centroid': (2.5, 0.0), 'atoms': [('C', 2.5, 0.0, 0.0)], 'atoms_n': 1} (distance 2.5 <= 3.0) → first step emits ('stacked', <record incl. 'atoms'>); counters become 1 / 1 / 0; pickup no longer live
- attach: attach_segment('p1', (2.5, 0.0), [('C', 2.5, 0.0, 0.0)]) → segments[-1] == {'molecule_id': 'p1', 'centroid': (2.5, 0.0), 'atoms': [('C', 2.5, 0.0, 0.0)], 'atoms_n': 1} (the frozen GAME-10 record); counters unchanged by attach (1 / 1 / 0)
- no capture: pickup at (5.0, 0.0) → run 20 steps, no stacked event, counters unchanged
- exactly at radius: head placed so the post-step distance is exactly 3.0 (e.g. head (0,0), pickup (3.3, 0.0), one step of 0.3 → distance 3.0) → captured (inclusive <=)
- no refire while claimed: continue stepping past a captured pickup → no second ('stacked', ...) event
- reject + recapture: reject_pickup('p1') RETURNS ('refused', 'p1', 'clash') (assert the return value — the canonical 3-tuple); counters back to 0/0/1, pickup live again; stepping → captures again, counters 1/1/0; refusal count tracked
- win cap: cap=2, two pickups in the path → second capture emits ('won',) on that same tick, finished True, result 'won'; a later step() returns []
- budget: atom_budget=10, one pickup atoms_n=20 → ('budget_warning', 20) exactly once even after further captures
- event ordering: a tick that both moves and captures emits [('moved', ...), ('stacked', ...)] in that order; a crashing tick emits [('moved', ...), ('crashed', ...)] and nothing after
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → all OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK; `python3.6 tests/run_gates.py` → exit 0</verify>
  <done>Capture/attach/reject semantics (canonical ('refused', id, reason) payload, atoms-carrying records, frozen-segment attach), counters, win-at-cap, and once-per-run budget warning all exact; event ordering per research; full suite + gates green, zero sys.modules stubs</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_engine_rules -v` → OK (boundary, body, pickups, counters, win, budget, ordering)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → OK (02-06's test_engine_core.py untouched and green)
- `python3.6 tests/run_gates.py` → exit 0 (game_engine.py stays PURE stdlib-only)
- `grep -n "3\\.4" serpentrum/game_engine.py` → the radius-vs-stacking-distance constraint comment is present
</verification>

<success_criteria>
- GAME-05 crash semantics complete under the authoritative polyline-edge model: boundary (all 4 walls, inclusive BOUNDARY_MARGIN_A-adjusted edges) and body self-collision (strict < 2.0 A on non-recent edges built from segment centroids); crashed engine inert with pending cleared
- Pickup capture/attach/reject seam ('stacked' events; ('refused', pickup_id, reason) returns; 'atoms' on every pickup record) ready for the Phase-5 controller + STACK-05 clash gate
- Win at cap ('won') and once-per-run budget warning ('budget_warning') per research §7; counters exact
- Continuous-2D determinism preserved; 02-06 tests unmodified and green; purity + full gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-10-SUMMARY.md`
</output>
