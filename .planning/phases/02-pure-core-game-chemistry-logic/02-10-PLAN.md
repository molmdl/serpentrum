---
phase: 02-pure-core-game-chemistry-logic
plan: 10
type: execute
wave: 2
depends_on: ["02-06"]
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_rules.py
autonomous: true

must_haves:
  truths:
    - "Head entering the 1.0 A boundary margin of the AABB emits ('crashed', 'boundary'), sets finished=True / result='crashed', and every later step() is a no-op — for all four walls"
    - "Head-centroid within BODY_COLLISION_RADIUS_A=2.0 of a chain polyline edge (excluding the SEGMENT_SKIP_RECENT=2 most recent edges) emits ('crashed', 'body'); the two most recent edges never collide, so turning past the neck is legal"
    - "Head within PICKUP_RADIUS_A=3.0 of a live pickup emits ('stacked', pickup) exactly once and claims it; reject_pickup() returns the pickup to live, rolls the counters back, and emits ('refused', reason)"
    - "molecules_stacked reaching cap emits ('won',) with finished=True / result='won'; crossing atom_budget emits ('budget_warning',) exactly once per run without stopping the game"
    - "Counters (molecules_stacked, atoms_total, pickups_remaining) are exact after every capture, rejection, and event"
  artifacts:
    - path: "serpentrum/game_engine.py"
      provides: "collision rules + pickup capture/reject + win-cap/budget counters layered on the 02-06 core, with constants BODY_COLLISION_RADIUS_A=2.0, SEGMENT_SKIP_RECENT=2, PICKUP_RADIUS_A=3.0, BOUNDARY_MARGIN_A=1.0 and the documented radius < 3.4 A stacking-distance constraint"
      contains: "PICKUP_RADIUS_A"
    - path: "tests/test_engine_rules.py"
      provides: "deterministic boundary/body/pickup/win/budget scenario tests on exact axis movements"
      min_lines: 140
  key_links:
    - from: "serpentrum/game_engine.py step()"
      to: "point-to-segment distance helper (head-centroid vs chain polyline edges)"
      via: "shared helper used by both the forward-motion body check and (later) 02-13's swept pre-check"
      pattern: "_point_segment_distance|_segment_distance"
    - from: "tests/test_engine_rules.py"
      to: "serpentrum.game_engine.GameEngine"
      via: "direct state injection (head/heading/segments/pickups as plain data) + step(dt) event assertions"
      pattern: "GameEngine"
    - from: "serpentrum/game_engine.py constants"
      to: "3.4 A committed stacking distance (dimer2 fixture)"
      via: "constraint comment: BODY_COLLISION_RADIUS_A must stay < 3.4 minus margin so the head never 'collides' with the segment just stacked"
      pattern: "3\\.4"
---

<objective>
Layer the GAME-05/GAME-06 collision-and-rules half onto the 02-06 engine core: AABB boundary crash, segment-based self-collision (head-centroid vs chain polyline edges with a neck exemption), pickup capture with controller-confirm/reject semantics, and the score/win-cap/atom-budget counters with their crash/win/warning events.

Purpose: The 02-06 core can move and queue directions but cannot end a run or stack a molecule. This plan makes the engine a complete referee: GAME-05 ("segment-based collision: head-centroid vs chain segments", crash still completes the snake), GAME-06 (win at cap), GAME-04 counters (hidden atom budget for the SPECTRA-06 pre-xtb check), and the STACK-05 controller seam ('stacked' event -> controller places -> reject_pickup on clash).

Output: extended serpentrum/game_engine.py + tests/test_engine_rules.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@serpentrum/game_engine.py
@tests/test_engine_core.py

Wave/dependency note: 02-06 (wave 1) delivered the continuous-2D core: GameEngine with head (x, y) floats, heading (unit vector), segments list of records `{'molecule_id', 'centroid': (x, y), 'atoms': [(sym, x, y, z)], 'atoms_n': int}`, pending direction queue, 180-degree rejection in request_direction, step(dt) forward movement emitting event tuples, pause/reset. READ serpentrum/game_engine.py and tests/test_engine_core.py FIRST — this plan EXTENDS that file, never breaks its public contract, and tests/test_engine_core.py must stay green (that file is owned by 02-06 and must not be edited).

Load-bearing verified facts / locked decisions (research §7):
- Engine model is CONTINUOUS 2D coordinates (not grid) — GAME-05's "head-centroid vs chain segments" and GAME-10's rigid pivot sweeps are incompatible with grid quantization (research resolves the contradiction; ARCHITECTURE.md "grid" wording is stale).
- Constants: SPEED_A_PER_S = 3.0, BODY_COLLISION_RADIUS_A = 2.0, SEGMENT_SKIP_RECENT = 2, PICKUP_RADIUS_A = 3.0. **BODY_COLLISION_RADIUS_A (2.0) MUST stay < 3.4 A (committed dimer2 stacking distance) minus margin** — otherwise the head would "collide" with the molecule just stacked behind it. Document this constraint as a comment at the constant.
- Boundary margin: BODY_COLLISION_RADIUS_A / 2 = 1.0 A (this plan's pinned value, per research "decide exact margin in the plan"). Crash condition uses the margin-adjusted walls inclusive: `head.x <= x0 + m or head.x >= x1 - m or head.y <= y0 + m or head.y >= y1 - m`.
- Event order per research §7: ('moved',) -> boundary hit -> ('crashed', 'boundary'); then head vs segment capsules -> ('crashed', 'body'); then head within pickup radius -> ('stacked', pickup); then molecules_stacked >= cap -> ('won',). Emission stops at the first crash; a crashed/won engine no-ops on later steps.
- Win semantics: `molecules_stacked >= cap` -> ('won',) (research §7 pins >=; GAME-06's "exceeds" wording is resolved by the engine spec).
- atom_budget is a WARNING-LEVEL check, never a hard stop (SPECTRA-06 warns; the hard pre-xtb re-check is Phase 6's runner).
- STACK-05 seam: on ('stacked', pickup) the controller runs stacking.place_pickup + check_clash; on clash it calls engine.reject_pickup(...) and the pickup "stays" (returns to live).
- Determinism: pure float math, no RNG, no wall-clock. Tests set engine state directly (plain data) and use assertAlmostEqual for positions.
- Python 3.6.9 syntax only (%-formatting precedent, no dataclasses); serpentrum/game_engine.py stays PURE (stdlib math only); tests use zero sys.modules stubs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boundary collision (AABB + 1.0 A margin) + crash semantics</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Module constants (skip any 02-06 already defines; add only missing ones, in one documented block):
```python
BODY_COLLISION_RADIUS_A = 2.0   # head-centroid vs chain-edge capsule radius.
# MUST stay < 3.4 A (committed dimer2 stacking distance) minus margin, else the
# head would collide with the segment just stacked behind it.
SEGMENT_SKIP_RECENT = 2         # newest chain edges exempt from self-collision
PICKUP_RADIUS_A = 3.0           # head-vs-pickup capture distance (~stacking distance)
BOUNDARY_MARGIN_A = 1.0         # = BODY_COLLISION_RADIUS_A / 2 (pinned this plan)
```
2. Engine gains state fields `box_min` and `box_max` (each an (x, y) tuple or None). Default None = no boundary checking (so 02-06's free-moving tests are untouched); the Phase-4 controller passes the setup_logic box preset. Set them however 02-06's reset/constructor pattern makes cleanest (reset kwarg or direct attribute; do not break 02-06's signature).
3. In step(dt), after the head moves and the ('moved',) event is emitted: if box_min/box_max are set and
   `hx <= x0 + BOUNDARY_MARGIN_A or hx >= x1 - BOUNDARY_MARGIN_A or hy <= y0 + BOUNDARY_MARGIN_A or hy >= y1 - BOUNDARY_MARGIN_A`
   → emit ('crashed', 'boundary'), set finished=True, result='crashed', clear the pending queue (turn-state hygiene; 02-13 builds on this), and STOP event processing for this tick. All later step() calls return [] immediately while finished.
4. If 02-06 already has any boundary/crash code, reconcile it to exactly this contract (margins, inclusive comparisons, event payload, finished/result flags) and keep tests/test_engine_core.py green.

Create tests/test_engine_rules.py (sys.path self-insert preamble; construct the engine exactly as test_engine_core.py does, then set state directly). Deterministic scenarios (SPEED 3.0, dt 0.1 → 0.3 A/step; box ((-18.0, -18.0), (18.0, 18.0))):
- four walls: place head near each wall with heading into it (e.g. head (16.8, 0) heading (1, 0) → first step to 17.1 > 17.0 → crash); assert ('crashed', 'boundary') exactly once, finished True, result 'crashed'
- inclusive edge: head set EXACTLY to (17.0, 0.0), heading (1, 0) → crash on next step (comparison is inclusive at the margin-adjusted wall)
- just inside: head (16.6, 0), one step → 16.9 < 17.0 → alive (no crash event)
- box None: no box set → head walks past where a wall would be with zero crash events
- after crash: a second step() returns [] and mutates nothing (head unchanged)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → boundary tests OK; `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → core + rules both OK (02-06 unbroken)</verify>
  <done>All four walls crash with the exact margin semantics (inclusive at wall−1.0); None box disables checking; crashed engine is inert; 02-06 suite still green</done>
</task>

<task type="auto">
  <name>Task 2: Segment-based self-collision (head-centroid vs chain polyline edges, skip recent 2)</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Pure helper (module-level function so 02-13's sweep pre-check can reuse it):
```python
def _point_segment_distance_sq(px, py, ax, ay, bx, by):
    """Squared distance from point (px, py) to segment (a -> b) in 2D.
    Standard clamped projection; compare against BODY_COLLISION_RADIUS_A ** 2."""
```
2. In step(dt), after the boundary check: build the chain polyline from segment centroids `c[0..n-1]` (index 0 = oldest/first stacked, n-1 = newest/nearest head). Check edges `(c[i], c[i+1])` for `i in range(0, n - 1 - SEGMENT_SKIP_RECENT)` — i.e. the SEGMENT_SKIP_RECENT edges nearest the head are exempt (the neck). If the head's squared distance to any checked edge `< BODY_COLLISION_RADIUS_A ** 2` (STRICT <) → emit ('crashed', 'body'), finished=True, result='crashed', clear pending, stop event processing. n small enough that range() is empty (n <= 1 + SEGMENT_SKIP_RECENT) → no body check possible.

Tests (append to tests/test_engine_rules.py; inject segment records directly — engine state is plain data):
- body crash: head (-5.0, 0.0) heading (1, 0); segments with centroids c0=(-2.0, 0.5), c1=(3.0, 0.5), c2=(6.0, 3.0), c3=(8.0, 6.0) (n=4 → only edge 0 checked). Loop step(0.1) up to 30 times: head crosses under edge 0 (y=0.5, distance 0.5 < 2.0) → ('crashed', 'body'), result 'crashed'
- neck exemption: head (4.0, 0.0) heading (1, 0); segments c0=(-10.0, -10.0), c1=(0.0, -6.0), c2=(4.0, 0.0), c3=(8.0, 4.0) — edges 1 and 2 pass through the head's position but are SKIPPED; edge 0's nearest point is 7.2 A away → step several times, assert NO crash and the head moves
- strict comparison: edge from (0.0, 2.0) to (4.0, 2.0); head at (2.0, 0.0) → distance exactly 2.0 → NOT crashed; head at (2.0, 0.01) → distance 1.99 → crashed on next step
- crash precedence: construct a tick where the head crosses both a wall and an edge — assert exactly one ('crashed', ...) event and it is 'boundary' (boundary check runs first)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → all OK; `python3.6 -m unittest tests.test_engine_core -v` → 02-06 suite still OK</verify>
  <done>Body collision fires only on checked (non-recent) edges with strict < 2.0 A; neck exemption proven; boundary precedence exact; 02-06 unbroken</done>
</task>

<task type="auto">
  <name>Task 3: Pickup capture/reject + score/win-cap/atom-budget counters + win event</name>
  <files>serpentrum/game_engine.py, tests/test_engine_rules.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Engine gains `pickups`: list of records `{'id': str, 'centroid': (x, y), 'atoms_n': int}` (set via reset/constructor kwarg or direct assignment; default empty), plus a `live_pickup_ids` set (all ids initially; capture removes; reject re-adds). Also `cap` (molecules win cap or None) and `atom_budget` (or None) — only enforced when not None (keeps 02-06's free tests unaffected). Counters `molecules_stacked`, `atoms_total`, `pickups_remaining` must already exist from 02-06's state — wire them here; add if missing.
2. In step(dt), after the body check (crash stops everything): scan pickups in list order; the FIRST live pickup whose squared distance to the head `<= PICKUP_RADIUS_A ** 2` (inclusive) is captured: remove from live_pickup_ids, molecules_stacked += 1, atoms_total += pickup['atoms_n'], pickups_remaining -= 1, emit ('stacked', pickup_record). At most ONE capture per tick (deterministic list order).
3. `reject_pickup(pickup_id, reason='clash')`: re-add the id to live_pickup_ids, roll the counters back (molecules_stacked -= 1, atoms_total -= atoms_n, pickups_remaining += 1), track a per-pickup refusal count, emit ('refused', reason). This is the STACK-05 seam: the controller calls it when stacking.place_pickup + check_clash reject the placement.
4. Budget warning: after any capture, if atom_budget is not None and atoms_total > atom_budget and not self._budget_warned → self._budget_warned = True, emit ('budget_warning', atoms_total). Never a crash; at most once per run (flag cleared by reset()).
5. Win: after a capture, if cap is not None and molecules_stacked >= cap → emit ('won',), finished=True, result='won', clear pending. Note: win is checked AFTER capture and never fires on the same tick as a crash (crash stops processing first).

Tests (append):
- capture: head (0,0) heading (1,0), pickup {'id': 'p1', 'centroid': (2.5, 0.0), 'atoms_n': 13} (distance 2.5 <= 3.0) → first step emits ('stacked', p1); counters become 1 / 13 / 0; pickup no longer live
- no capture: pickup at (5.0, 0.0) → head reaches 4.7 after 15 steps... run 20 steps, no stacked event, counters unchanged
- exactly at radius: head placed so the post-step distance is exactly 3.0 (e.g. head (0,0), pickup (3.3, 0.0), one step of 0.3 → distance 3.0) → captured (inclusive <=)
- no refire while claimed: continue stepping past a captured pickup → no second ('stacked', ...) event
- reject + recapture: reject_pickup('p1') → ('refused', 'clash') emitted, counters back to 0/0/1, pickup live again; stepping → captures again, counters 1/13/0; refusal count tracked
- win cap: cap=2, two pickups in the path → second capture emits ('won',), finished True, result 'won'; a later step() returns []
- budget: atom_budget=10, one pickup atoms_n=20 → ('budget_warning', 20) exactly once even after further captures
- event ordering: a tick that both moves and captures emits [('moved',), ('stacked', ...)] in that order; a crashing tick emits [('moved',), ('crashed', ...)] and nothing after
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_rules -v` → all OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK; `python3.6 tests/run_gates.py` → exit 0</verify>
  <done>Capture/reject semantics, counters, win-at-cap, and once-per-run budget warning all exact; event ordering per research; full suite + gates green, zero sys.modules stubs</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_engine_rules -v` → OK (boundary, body, pickups, counters, win, budget, ordering)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → OK (02-06's test_engine_core.py untouched and green)
- `python3.6 tests/run_gates.py` → exit 0 (game_engine.py stays PURE stdlib-only)
- `grep -n "3\\.4" serpentrum/game_engine.py` → the radius-vs-stacking-distance constraint comment is present
</verification>

<success_criteria>
- GAME-05 crash semantics complete: boundary (all 4 walls, inclusive margin-adjusted edges) and segment-based body collision (strict < 2.0 A on non-recent edges); crashed engine inert
- Pickup capture/reject seam ('stacked'/'refused' events) ready for the Phase-5 controller + STACK-05 clash gate
- Win at cap ('won') and once-per-run budget warning ('budget_warning') per research §7; counters exact
- Continuous-2D determinism preserved; 02-06 tests unmodified and green; purity + full gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-10-SUMMARY.md`
</output>
