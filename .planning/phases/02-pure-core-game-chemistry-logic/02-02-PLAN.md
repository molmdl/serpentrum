---
phase: 02-pure-core-game-chemistry-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/xtbenv.py
  - tests/test_xtbenv.py
autonomous: true

must_haves:
  truths:
    - "evaluate_run returns ok=True ONLY for exit 0 + 'normal termination' in stderr + all expected files present; the repro_oh story (stderr success but zero output files) is rejected — stderr success alone is NOT success"
    - "The bad-run contract failure produces one human-readable problem per failed leg (exit code, termination line, missing files)"
    - "detect_binary resolves a validated configured path first, then 'xtb.exe' (Windows), then 'xtb' (Linux) through an INJECTED which function — tests need no xtb install and no sys.modules stubs"
    - "validate_binary_path rejects missing files, directories, empty paths, and any path containing quote characters"
    - "build_argv always returns a LIST [exe, input, '--ohess'] and raises on quote characters (never a shell string)"
    - "new_run_dir creates a fresh 'srp_'-prefixed directory under the caller's base (never the session dir)"
  artifacts:
    - path: "serpentrum/xtbenv.py"
      provides: "XTB_OHESS/EXPECTED_FILES/STDERR_SUCCESS/STDERR_FAILURE constants, RunVerdict, evaluate_run, validate_binary_path, detect_binary, build_argv, new_run_dir — stdlib-only PURE module"
      min_lines: 90
      contains: "--ohess"
    - path: "tests/test_xtbenv.py"
      provides: "fixture-driven contract tests (the five .err files) + DI-based detection tests + argv/dir tests"
      min_lines: 130
      contains: "evaluate_run"
  key_links:
    - from: "tests/test_xtbenv.py"
      to: ".planning/research/xtb-spike-fixtures/*.err"
      via: "read bytes in place (committed; parallel-worktree-safe), decode utf-8, pass AS-IS to evaluate_run"
      pattern: "xtb-spike-fixtures"
    - from: "serpentrum/xtbenv.py"
      to: "STDERR_SUCCESS = 'normal termination'"
      via: "substring contract leg — must stay consistent with tests/run_gates.py gate 5's 'normal termination' assertion"
      pattern: "normal termination"
    - from: "serpentrum/xtbenv.py detect_binary"
      to: "which_fn parameter (default shutil.which)"
      via: "dependency injection — fake which functions in tests, zero module mocking"
      pattern: "which_fn"
---

<objective>
Build the xtb success-contract and binary-detection pure module (`serpentrum/xtbenv.py`): the 3-leg success contract evaluator (exit 0 + "normal termination" on stderr + expected output files), the `xtb`/`xtb.exe` detection with probe order, the list-argv builder with the `--ohess` constant, and the per-run temp-dir contract. This is the pure half of SPECTRA-02 / SETUP-05 and phase success criterion 3.

Purpose: Phase 6's runner will invoke Windows xtb from WSL through exactly this contract; the repro_oh fixture proves all three legs are load-bearing (a run with "normal termination" on stderr but no output files produced zero frequency data).

Output: `serpentrum/xtbenv.py`, `tests/test_xtbenv.py`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md
@tests/run_gates.py

Load-bearing verified facts:
- **The five .err fixture texts (committed at `.planning/research/xtb-spike-fixtures/`, exact bytes):** `ohess.err`, `co2.err`, `dimer2.err`, `repro_oh.err` are all `normal termination of xtb\r\n` (27 bytes); `bad.err` is `abnormal termination of xtb\r\n` (29 bytes). Exit codes are NOT recorded in the fixtures (PITFALLS 3 documents rc=128 for the bad run) — unit tests use synthetic rc values with the fixture stderr text.
- **The repro_oh story (why the files leg is load-bearing):** repro_oh.log/.err document a run whose stderr said "normal termination of xtb" but which produced NO Hessian/frequency output (no g98.out, no vibspectrum). Success must require ALL THREE legs.
- **Constants:** `XTB_OHESS = '--ohess'` (locked 2026-09-06 — NEVER `-o --hess`); `EXPECTED_FILES = ('g98.out', 'vibspectrum')`; `STDERR_SUCCESS = 'normal termination'`; `STDERR_FAILURE = 'abnormal termination'`.
- **Consistency anchor:** tests/run_gates.py gate 5 asserts `'xtb version'` AND `'normal termination'` in Windows-xtb output (run_gates.py:184-258) and resolves the exe at `xtb-6.7.1/bin/xtb.exe` with a /mnt/c-backed cwd + bare relative args. xtbenv's constants and argv/cwd contract must not contradict those literals (the gate's --version probe is dev-side only).
- **Detection order (Pitfalls 3 / AGENTS rule):** 1. configured_path if given: validate_binary_path; problems -> fall through; 2. which_fn('xtb.exe') — Windows conda env; 3. which_fn('xtb') — Linux. Returns the resolved path string or None.
- **Zero-stubs rule nuance:** the ban is on sys.modules stubbing (pymol/Qt/numpy). DI (`which_fn=shutil.which` parameter) or unittest.mock.patch of a callable seam is fine — DI is preferred here and needs no mock at all.
- **Hard conventions:** Python 3.6 syntax only (%-formatting, no dataclasses/walrus/f-string `=`). Stdlib only (os, shutil, tempfile). Zero pymol/pmg_tk/PyQt5/numpy anywhere (purity checker auto-classifies this module PURE and gates function bodies too).
- **Wave-1 parallelism:** do NOT import other Phase-2 modules (spectra/xyzio/stacking/molecule_data/game_engine/setup_logic/cgo_build) — parallel plans may not have them yet. This plan reads fixtures IN PLACE from `.planning/research/xtb-spike-fixtures/` (committed; present in every worktree). The `tests/fixtures/xtb/` copies created by plan 02-01 are for LATER-wave plans — do not depend on them here.
- **Test conventions:** discovery = `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (never `-t .`); tests/ has NO `__init__.py`; every test file repeats the sys.path self-insert from tests/test_skeleton.py. Never shell `rm` (opencode.json denies it) — clean tempdirs with `shutil.rmtree` in `addCleanup`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: success-contract evaluator (evaluate_run) vs the fixture .err texts</name>
  <files>serpentrum/xtbenv.py, tests/test_xtbenv.py</files>
  <action>
Create `serpentrum/xtbenv.py` (stdlib: os, shutil, tempfile; py3.6, %-formatting) with:
- Constants: `XTB_OHESS = '--ohess'`, `EXPECTED_FILES = ('g98.out', 'vibspectrum')`, `STDERR_SUCCESS = 'normal termination'`, `STDERR_FAILURE = 'abnormal termination'`.
- `RunVerdict = collections.namedtuple('RunVerdict', 'ok problems')` — problems is a list of human-readable strings, one per failed contract leg.
- `evaluate_run(exit_code, stderr_text, expected_files, present_files)`: ok iff `exit_code == 0` AND `STDERR_SUCCESS in stderr_text` AND `set(expected_files) <= set(present_files)`. Problem strings (exact substrings tests rely on): exit leg -> `'exit code 128 != 0'` (use the actual rc); stderr leg -> `"stderr lacks 'normal termination' (got: '<first 60 chars of stderr_text, whitespace-collapsed>')"`, or when STDERR_FAILURE is present: `"stderr reports 'abnormal termination'"`; files leg -> `"missing expected output file(s): g98.out, vibspectrum"` (comma-space joined, sorted).

Create `tests/test_xtbenv.py` (sys.path self-insert preamble + discovery-pin docstring per tests/test_skeleton.py; `FIXTURES = os.path.join(ROOT, '.planning', 'research', 'xtb-spike-fixtures')`). Test-first where practical; cases:
1. Fixture-driven success: read `ohess.err`, `co2.err`, `dimer2.err`, `repro_oh.err` bytes, decode utf-8, pass AS-IS (CRLF retained — proves the substring check tolerates `\r\n`): evaluate_run(0, text, ('g98.out', 'vibspectrum'), ('g98.out', 'vibspectrum')) -> ok is True, problems == [].
2. Fixture-driven failure: bad.err text + synthetic rc=128 + present=() -> ok False; problems contains 3 entries; assert 'exit code' in one, 'abnormal termination' in another, and both 'g98.out' and 'vibspectrum' in the third.
3. THE REPRO_OH CASE (criterion 3): evaluate_run(0, ohess.err text, ('g98.out', 'vibspectrum'), ()) -> ok False; len(problems) == 1; the single problem names both missing files and mentions neither exit nor termination.
4. Partial files: present=('g98.out',) -> ok False; the files problem mentions 'vibspectrum' but not 'g98.out'.
5. Subset expectation: expected=('vibspectrum',), present=('g98.out', 'vibspectrum') -> ok True (extra present files are fine).
6. Leg independence: evaluate_run(0, bad.err text, e, p) -> ok False with only the stderr problem; evaluate_run(1, ohess.err text, e, p) -> ok False with only the exit problem.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_xtbenv.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> green.</verify>
  <done>All three contract legs proven against the committed fixture stderr bytes; repro_oh story encoded as a test; gates green. Commit: `feat(02-02): xtb success contract evaluator vs committed .err fixtures`.</done>
</task>

<task type="auto">
  <name>Task 2: validate_binary_path + detect_binary (injected which_fn)</name>
  <files>serpentrum/xtbenv.py, tests/test_xtbenv.py</files>
  <action>
Add to serpentrum/xtbenv.py:
- `validate_binary_path(path)` -> list of problem strings (empty = valid): (a) empty/None/non-str -> `'xtb path is empty'`; (b) `not os.path.exists(path)` -> `'<path> does not exist'`; (c) exists but `not os.path.isfile(path)` -> `'<path> is not a file'`; (d) `'"' in path or "'" in path` -> `"path contains quote character(s): '<path>'"` (quote chars would break list-argv safety downstream). Problems may accumulate — check all four and return all.
- `detect_binary(configured_path=None, which_fn=shutil.which)`: 1. if configured_path: problems = validate_binary_path(configured_path); if not problems -> return configured_path (validated config wins); else fall through. 2. `w = which_fn('xtb.exe')`; if w -> return w (Windows conda env first — AGENTS.md rule). 3. `w = which_fn('xtb')`; if w -> return w. 4. return None.

Tests (build real files under tempfile.mkdtemp(), cleanup via shutil.rmtree in addCleanup; NO xtb install needed, NO sys.modules stubs — pass fake which functions):
1. validate: an existing tmp file -> []; a missing path -> problem mentions 'does not exist'; a tmp DIRECTORY -> problem mentions 'not a file'; a path containing `"` -> problem mentions 'quote'; empty string -> problem 'empty'.
2. detect: configured path pointing at a real tmp file + fake which_fn that records every query and returns None -> returns the configured path and which_fn was NEVER called.
3. detect: configured path missing on disk + fake which_fn returning `'C:\\xtb\\xtb.exe'` for 'xtb.exe' -> returns the xtb.exe path (fall-through works); the recorder shows 'xtb.exe' was queried BEFORE 'xtb'.
4. detect: configured None + fake which_fn returning None for 'xtb.exe' and '/usr/bin/xtb' for 'xtb' -> returns '/usr/bin/xtb'.
5. detect: configured None + fake which_fn returning None for both -> returns None.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_xtbenv.py" -v` -> OK; `python3.6 tests/run_gates.py` -> green.</verify>
  <done>Probe order (configured -> xtb.exe -> xtb) and validation rules proven with DI fakes; zero stubs. Commit: `feat(02-02): xtb binary detection with injected which-function`.</done>
</task>

<task type="auto">
  <name>Task 3: build_argv + new_run_dir + full verification</name>
  <files>serpentrum/xtbenv.py, tests/test_xtbenv.py</files>
  <action>
Add to serpentrum/xtbenv.py:
- `build_argv(exe_path, input_filename, extra_args=(XTB_OHESS,))` -> `[exe_path, input_filename] + list(extra_args)`. Assert (raise ValueError) that NO argument contains a quote character — list args only, never a shell string (Pitfalls 3.3 / security table). Docstring documents the cwd contract: input_filename is a BARE relative name; the Phase-6 runner writes snake.xyz INTO the run dir and launches with cwd=that dir.
- `new_run_dir(base_dir)` -> `tempfile.mkdtemp(prefix='srp_', dir=base_dir)`. Docstring: the Phase-6 runner owns writing/launching/copying/cleanup; this module provides the dir-name contract only (Pitfall 3.1: never the session dir).

Tests:
1. build_argv('C:\\xtb\\xtb.exe', 'snake.xyz') == ['C:\\xtb\\xtb.exe', 'snake.xyz', '--ohess'] (exact list equality; assert isinstance list); custom extra_args=('--ohess', '--gbsa', '1') are appended in order.
2. build_argv raises ValueError when exe_path or input_filename contains ' or ".
3. new_run_dir under a tmp base: returned path exists, isdir, basename starts with 'srp_', and sits under base_dir; two calls return DIFFERENT paths; cleanup with shutil.rmtree in addCleanup.
4. Module-surface sanity: `XTB_OHESS == '--ohess'`, `EXPECTED_FILES == ('g98.out', 'vibspectrum')`, `STDERR_SUCCESS == 'normal termination'` (guards against typo drift; these literals must stay consistent with tests/run_gates.py gate 5's assertions).
Then run the full verification battery.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_xtbenv.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> exit 0; `python3.6 tools/check_purity.py` -> clean.</verify>
  <done>Full xtbenv surface implemented and tested: 3-leg contract, DI detection, list argv with --ohess, srp_ run-dir contract; zero stubs; gates green. Commit: `feat(02-02): argv builder + per-run dir contract`.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_xtbenv.py" -v` -> OK
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK
- `python3.6 tests/run_gates.py` -> exit 0
- `python3.6 tools/check_purity.py` -> clean
- The five committed .err fixtures were read IN PLACE (no copies made by this plan)
</verification>

<success_criteria>
- evaluate_run: ok iff exit 0 AND 'normal termination' in stderr AND expected files present; repro_oh case (stderr success, no files) rejected with exactly one files problem — phase criterion 3
- Detection probe order configured -> xtb.exe -> xtb via injected which_fn, testable with no xtb installed
- build_argv returns a list with --ohess, rejects quotes; XTB_OHESS constant (never '-o --hess')
- Zero sys.modules stubs; stdlib-only PURE module; py3.6 syntax; gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-02-SUMMARY.md`
</output>
