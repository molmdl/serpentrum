---
phase: 02-pure-core-game-chemistry-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/spectra.py
  - tests/test_spectra_g98.py
  - tests/fixtures/xtb/
autonomous: true

must_haves:
  truths:
    - "parse_g98 on the committed dimer g98.out returns 26 atoms and 72 modes with negative frequencies (first = -31.9175) and per-atom 3-component displacement vectors preserved"
    - "parse_vibspectrum returns 78 modes including the 6 trivial rows; g98<->vibspectrum index correspondence holds for all 72 real modes at the computed offset (3N - len(g98_modes) = 6)"
    - "Feeding bad.log raises SpectraParseError whose message mentions the missing frequency section (loud failure contract)"
    - "A zero-intensity mode (synthetic CO2 1424.95 cm^-1 @ 0.0) parses with intensity exactly 0.0 and contributes exactly 0.0 to the broadened curve"
    - "A synthetic remainder block (4 modes in 3+1 columns) parses; the last mode carries 1-float vector tuples per atom"
    - "broaden() over the dimer modes yields len(xs)==n_points, ys >= 0, and peak within +/-fwhm of 1150.6639 (verified argmax-intensity mode)"
  artifacts:
    - path: "serpentrum/spectra.py"
      provides: "Mode/Atom/Spectrum namedtuples, SpectraParseError, parse_g98(_text), parse_vibspectrum(_text), real_modes, broaden — stdlib-only PURE module"
      min_lines: 150
      contains: "SpectraParseError"
    - path: "tests/test_spectra_g98.py"
      provides: "fixture-driven + synthetic test suite (happy paths, correspondence, negatives, zero intensity, loud failures, remainder block, broaden)"
      min_lines: 200
      contains: "parse_g98"
    - path: "tests/fixtures/xtb/"
      provides: "17 byte-identical copies of the committed xtb spike fixtures (single copy; later plans consume from here)"
      contains: "g98.out"
  key_links:
    - from: "tests/test_spectra_g98.py"
      to: "tests/fixtures/xtb/g98.out"
      via: "open(path, encoding='utf-8').read() -> spectra.parse_g98(path)"
      pattern: "parse_g98"
    - from: "tests/test_spectra_g98.py"
      to: ".planning/research/xtb-spike-fixtures/"
      via: "byte-identity check proving the copies match the committed source fixtures"
      pattern: "xtb-spike-fixtures"
    - from: "serpentrum/spectra.py"
      to: "stdlib math/collections only"
      via: "module imports (purity gate auto-classifies this file PURE — no pymol/Qt/numpy anywhere)"
      pattern: "import math"
---

<objective>
Build the fixture-first g98.out / vibspectrum spectra parser (`serpentrum/spectra.py`) plus frequency-table filtering and Gaussian broadening, proven against the committed real xtb 6.7.1pre fixtures. This is the pure half of SPECTRA-03/05 and phase success criterion 2: frequencies, IR intensities and mode vectors — negatives and zero-intensity modes included — extracted from the real phenol/CO2/pi-stacked-dimer outputs, with loud failure on the corrupt fixture (bad.log).

Purpose: every later spectra feature (plot, table, mode arrows) consumes this module's output; it is written against the fixture grammar probed line-by-line, not against a guessed format.

Output: `serpentrum/spectra.py`, `tests/test_spectra_g98.py`, and the ONCE-only fixture copies under `tests/fixtures/xtb/`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md
@tests/test_skeleton.py

Load-bearing verified facts (probed 2026-09-06/07 against the committed fixtures — trust these over the research doc where they differ):
- **g98.out grammar (dimer fixture, 883 CRLF lines):** line 6 = `Standard orientation:`; lines 7-10 = dash / 2 header lines / dash; lines 11-36 = 26 atom rows of exactly 6 tokens (center, atomic_number, type, x, y, z); line 37 = dash. Frequency section: 24 blocks, first `Frequencies --` line at 46, last at 851, stride 35; file ENDS at EOF right after the final atom row (line 883) — EOF terminates the section. Block = 2 ignored lines (mode-index, symmetry) + `Frequencies --` + 5 property lines + `Atom AN` header + 26 rows of 11 tokens (index, AN, 3x ncols floats). Exact one-leading-space prefixes: `' Frequencies --'`, `' Red. masses --'`, `' Frc consts  --'`, `' IR Inten    --'`, `' Raman Activ --'`, `' Depolar     --'`.
- **g98 values:** 72 modes = 3x26-6. First freqs -31.9175 / -23.0766 / -18.1086 with IR inten 2.4191 / 0.0072 / 0.2541. Last freqs 3113.7215 / 3519.9549 / 3521.1843. Min intensity 0.00030, max 257.1018 (mode 42 @ 1150.6639 cm^-1 — the verified global argmax; note research's broaden row mentioned 364.42 which is only the strongest LOW mode, not the argmax). No exact-zero intensities among the 72. Raman/Depolar all 0.0000 (ignore).
- **vibspectrum grammar (82 CRLF lines):** line 1 `$vibrational spectrum`, line 82 `$end`; lines 4-9 trivial rows = 4 tokens `[mode, freq, intensity, '-']` (symmetry column empty; literal `-0.00` on rows 1-4); lines 10-81 real rows = 5 tokens `[mode, symmetry, freq, intensity, selection]`. 78 rows = 6 trivial + 72 real. Mode 7 = -31.92 @ 2.41910 YES; mode 8 = -23.08 @ 0.00717 NO; mode 9 = -18.11 @ 0.25411 YES. 58 YES / 14 NO / 6 '-'.
- **CORRECTED correspondence tolerances (research's 5e-5 is WRONG for frequencies):** vibspectrum's freq column has 2 decimals, g98 has 4. Verified max freq diff = 0.0049 -> use abs tolerance **5e-3**. Verified max intensity diff = 5.0e-5 (exactly at 5e-5 with float noise) -> use abs tolerance **1e-4**. Offset = 3*n_atoms - len(g98_modes), computed, NEVER hardcoded (6 nonlinear / 5 linear).
- **CO2 (co2.log only — no CO2 g98/vibspectrum fixture exists):** line 240 `linear (good luck)  true`; eigvals (lines 456-457) 0.00 x5, 600.18, 600.18, 1424.95, 2593.38; IR intensities (lines 546-547) 0.00 x5, 68.70, 68.70, 0.00, ******. The 1424.95 symmetric stretch has intensity exactly 0.00; 2593.38's intensity is the overflow token `******` (no parseable value -> its row is honestly OMITTED from the synthetic fixture; the parser must NOT enforce a 3N row count, and float() on `******` must raise SpectraParseError).
- **bad.log:** 105 lines; `[ERROR] Program stopped due to fatal error` at line 97; NO frequency section -> parse must raise loudly.
- **Fixture trap:** `dimer.xyz` is MISLABELED (a CO2 dimer, 6 atoms) — it is NOT copied and NEVER mined for phenol data. `dimer2.xyz` is the real pi-stacked phenol dimer.
- **Reading files:** open with `encoding='utf-8'` (the header contains UTF-8 `⁻¹`) and split with `str.splitlines()` — neutralizes CRLF and a missing trailing newline.
- **Hard conventions (AGENTS.md / STATE.md):** Python 3.6 syntax only — no dataclasses, no walrus, no f-string `=`; use %-formatting. WSL python3.6.9 has NO numpy — stdlib only (math, collections); ZERO pymol/pmg_tk/PyQt5/numpy anywhere (the purity checker auto-classifies new serpentrum/ modules PURE and gates function bodies too). Tests: zero sys.modules stubs; `unittest.mock.patch` of callables is acceptable but not needed here.
- **Test conventions:** discovery = `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (never `-t .`). tests/ has NO `__init__.py` and must never get one. Every test file repeats the sys.path self-insert preamble (copy from tests/test_skeleton.py). `tests/fixtures/xtb/` must NOT contain an `__init__.py` either (plugin-path safety, run_gates gate 1).
- **Wave-1 parallelism:** do NOT import other Phase-2 modules (xtbenv/xyzio/stacking/molecule_data/game_engine/setup_logic/cgo_build) — parallel plans may not have them yet; this plan is fully self-contained (stdlib + serpentrum/spectra only). Your OWN `tests/fixtures/xtb/` copies exist because this plan creates them in its own worktree; later-wave plans consume them after the merge.
</context>

<feature>
  <name>spectra parser: g98.out + vibspectrum, real_modes, broaden</name>
  <files>serpentrum/spectra.py, tests/test_spectra_g98.py</files>
  <behavior>
    All cases input -> expected output:
    1. parse_g98(fixture g98.out) -> Spectrum with n_atoms == 26; atoms[0] == Atom(6, ~0.020733, ~-1.840903, ~-0.006438); len(modes) == 72; modes[0].index == 1; modes[0].freq == -31.9175 (places=4); modes[0].intensity == 2.4191 (places=4); modes[-1].freq == 3521.1843 (places=4); modes[0].vectors is a list of 26 3-tuples of floats; min(m.freq) < 0 for the first three modes (negatives kept).
    2. parse_vibspectrum(fixture vibspectrum) -> len(modes) == 78; modes 1-6 trivial (abs(freq) < 10, symmetry == '', selection == '-'), their intensities parse `-0.00`/`0.00000` as floats; modes[6] (file mode 7): freq -31.92 (places=2), intensity 2.41910 (places=5), selection 'YES'; modes[7] (mode 8) selection 'NO' with intensity 0.00717 — selection-rule NO is NOT filtered; every Mode.vectors == () and Spectrum.atoms == [].
    3. Index correspondence on both fixtures: offset = 3 * g98.n_atoms - len(g98.modes) == 6 (computed); for every j in range(72): abs(vs.modes[j+6].freq - g98.modes[j].freq) <= 5e-3 AND abs(vs.modes[j+6].intensity - g98.modes[j].intensity) <= 1e-4; 0 mismatches.
    4. real_modes(vs_spectrum, threshold=10.0) -> exactly 72 modes (6 trivial dropped, 14 real NO-selection rows RETAINED); real_modes on the g98 spectrum is a no-op (72 in, 72 out).
    5. Negatives kept + broaden tail: modes[0].freq < 0 present in output; broaden(g98.modes) -> len(xs) == 800, all ys >= 0.0, and the total y at x == 0.0 is < 1e-3 (negative modes contribute only their negligible Gaussian tail; sigma = 16.0 / (2*sqrt(2*ln 2)) ~= 6.7936).
    6. broaden peak: argmax(ys) within +/-16.0 of x == 1150.6639 (the verified argmax-intensity mode 42 @ 257.1018).
    7. Synthetic CO2 vibspectrum (CLEARLY labeled synthetic in the test; built from co2.log eigvals/intensities — 8 rows: 5 trivial `0.00 @ 0.00000 -` + `600.18 @ 68.70000` x2 + `1424.95 @ 0.00000`; row 9/2593.38 honestly omitted because its log intensity is the overflow token `******`): parse -> 8 modes; real_modes -> exactly 3 modes [600.18, 600.18, 1424.95] (degenerate pair retained as separate rows); the 1424.95 mode's intensity == 0.0 exactly; broaden(modes_with_zero) == broaden(modes_without_zero) EXACTLY (list equality — a zero-intensity mode contributes exactly 0.0 by arithmetic).
    8. Loud failure (corrupt fixture): parse_g98_text(bad.log text) raises SpectraParseError and 'frequency section' in str(exc).
    9. Loud failure (truncation): g98 fixture text truncated after its line 60 (mid atom rows of block 1) raises SpectraParseError mentioning the atom-row stage (message contains 'atom').
    10. Loud failure (vibspectrum): fixture text with the `$end` line removed raises SpectraParseError; a text with a garbage row (`garbage row here`) raises SpectraParseError with a line number; an intensity token `******` raises SpectraParseError.
    11. Remainder block (synthetic, clearly labeled): 3-atom molecule, block 1 = modes 1-3 with 3 columns, block 2 = mode 4 with 1 column -> 4 modes parsed; modes[3].vectors is a list of 3 one-float tuples.
  </behavior>
  <implementation>
    serpentrum/spectra.py — stdlib only (collections, math). `Mode = namedtuple('Mode', 'index freq intensity vectors')`, `Atom = namedtuple('Atom', 'atomic_number x y z')`, `Spectrum = namedtuple('Spectrum', 'n_atoms atoms modes')`, `class SpectraParseError(ValueError)` (message MUST contain the parser stage, 1-based line number(s) where applicable, and the offending line's first ~80 chars). %-formatting everywhere.
    - parse_g98_text(text): lines = text.splitlines(). Atom block: find the line whose strip() == 'Standard orientation:'; scan forward to the next all-dash line; read rows of exactly 6 tokens (int, int, int, float, float, float) until the next all-dash line; n_atoms = row count (n_atoms < 3 -> SpectraParseError('atom block', ...)). Frequency scan: iterate lines; on one starting with ' Frequencies --': cols = line.split('--', 1)[1].split(); ncols = len(cols); require 1 <= ncols <= 3; the NEXT 5 lines must start with (in order) ' Red. masses --', ' Frc consts  --', ' IR Inten    --', ' Raman Activ --', ' Depolar     --', each yielding exactly ncols floats after '--'; the next line must start with ' Atom AN'; then exactly n_atoms rows of 2 + 3*ncols whitespace tokens with atom indices running 1..n_atoms in order — per-atom vector = tuple of ncols floats. Any violation or premature EOF -> SpectraParseError with stage + line number + excerpt. Mode index = running count from 1 across blocks. NO ' Frequencies --' found anywhere -> SpectraParseError('no frequency section found ...'). Ignore mode-index/symmetry lines entirely (column count comes from the Frequencies line, never fixed columns).
    - parse_vibspectrum_text(text): first line strip() == '$vibrational spectrum'; last NON-EMPTY line strip() == '$end' (missing -> error); iterate lines after the leading '$vibrational spectrum' until '$end', skipping lines starting with '#'; tokens = line.split(); len 4 -> trivial (symmetry ''), len 5 -> real (symmetry = t[1]); mode = int(t[0]); freq = float(t[-3]); intensity = float(t[-2]); selection = t[-1]; token '******' or non-numeric field -> SpectraParseError with the line number. Build Spectrum(0, [], modes) with vectors == () per Mode.
    - parse_g98(path) / parse_vibspectrum(path): open(path, encoding='utf-8'), delegate to the _text variants.
    - real_modes(spectrum, threshold=10.0): return [m for m in spectrum.modes if abs(m.freq) >= threshold]. Policy: threshold on |freq| ONLY — never on selection rules (14 real NO rows have nonzero intensity). On g98 spectra it is a harmless no-op (xtb already projected trivial modes out).
    - broaden(modes, fwhm=16.0, x_min=0.0, x_max=None, n_points=800): sigma = fwhm / (2.0 * math.sqrt(2.0 * math.log(2.0))); x_max defaults to max(3600.0, max(m.freq for m in modes) + 5.0 * sigma); empty modes -> (grid, [0.0]*n_points); xs = evenly spaced x_min..x_max inclusive (n_points points); ys[i] = sum(m.intensity * math.exp(-0.5 * ((x - m.freq) / sigma) ** 2) for m in modes). Zero-intensity modes contribute exactly 0.0 by arithmetic (0.0 * finite == 0.0); negatives are NOT special-cased out — their tail on the x >= 0 grid is negligible by construction.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 (RED-1): copy fixtures once + failing fixture-driven tests</name>
  <files>tests/fixtures/xtb/, tests/test_spectra_g98.py</files>
  <action>
1. Create `tests/fixtures/xtb/` (plain directory — NO `__init__.py`, ever) and copy EXACTLY these 17 files from `.planning/research/xtb-spike-fixtures/` (keep original names; cp is safe, never move):
   g98.out, vibspectrum, bad.log, co2.log, dimer2.log, ohess.log, repro_oh.log, bad.err, co2.err, dimer2.err, ohess.err, repro_oh.err, bad.xyz, co2.xyz, dimer2.xyz, phenol.xyz, xtbopt.xyz
   Do NOT copy `dimer.xyz` (mislabeled CO2-dimer trap — must never be mined for phenol data), `hessian` (Anti-Pattern 5: never parse/eigendecompose), `xtbrestart` (binary), charges/wbo/xtbtopo.mol/xtbopt.log/xtbhess.xyz/.xtboptok (context only). Keep `.planning/research/xtb-spike-fixtures/` untouched.
2. Write `tests/test_spectra_g98.py` following the tests/test_skeleton.py pattern: docstring pinning discovery (`python3.6 -m unittest discover -s tests -p "test_*.py" -v` — never `-t .`), then the sys.path self-insert `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`. Define `ROOT` and `FIXTURES = os.path.join(ROOT, 'tests', 'fixtures', 'xtb')`.
3. Write the RED-1 test cases (they must FAIL now — serpentrum/spectra.py does not exist; the import error IS the RED state): behavior cases 1-6 from the <feature> block (g98 happy path, vibspectrum happy path, computed-offset correspondence with the CORRECTED tolerances 5e-3 freq / 1e-4 intensity, real_modes filtering incl. NO-selection retention, negatives + broaden tail, broaden peak at 1150.6639), PLUS a byte-identity test asserting every copied file's bytes equal its `.planning/research/xtb-spike-fixtures/` counterpart (read 'rb' both sides).
   Use assertAlmostEqual with explicit places/delta per case; never bare float equality except the exact-equality cases specified.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` runs and FAILS (ModuleNotFoundError/ImportError for serpentrum.spectra = the RED state); `ls tests/fixtures/xtb | wc -l` -> 17; `python3.6 -c "import hashlib;print(hashlib.md5(open('tests/fixtures/xtb/g98.out','rb').read()).hexdigest())"` matches the same hash of `.planning/research/xtb-spike-fixtures/g98.out`.</verify>
  <done>17 fixture copies byte-identical to source; RED-1 test file fails for the single reason that serpentrum/spectra.py does not exist yet. Commit: `test(02-01): add failing fixture-driven spectra parser tests + fixture copies`.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN-1): implement parse_g98 + parse_vibspectrum + real_modes</name>
  <files>serpentrum/spectra.py</files>
  <action>
Implement serpentrum/spectra.py per the <implementation> block: the three namedtuples, SpectraParseError, parse_g98_text, parse_g98, parse_vibspectrum_text, parse_vibspectrum, real_modes. Minimal code to make Task 1's tests pass — no cleverness. Python 3.6 syntax (%-formatting, no dataclasses/walrus/f-string `=`), stdlib imports only (`collections`, `math` — the purity checker auto-classifies this module PURE and bans pymol/Qt/numpy even inside function bodies). splitlines() for CRLF; utf-8 opens; NEVER parse by fixed columns or fixed line numbers; the `' Frequencies --'`-style prefixes carry exactly one leading space (verified). Defer broaden and the synthetic/loud-failure edge cases to Task 3 — but the no-frequency-section error (case 8's contract) must already raise here since it falls out of the scan loop naturally.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> all Task 1 tests OK; `python3.6 tests/run_gates.py` -> all gates green (purity + syntax + full suite).</verify>
  <done>Both real fixtures parse to the exact verified values; correspondence holds for all 72 modes at the computed offset; gates green. Commit: `feat(02-01): implement g98+vibspectrum spectra parsers (fixture-exact)`.</done>
</task>

<task type="auto">
  <name>Task 3 (RED-2 + GREEN-2): broaden, loud failures, synthetic remainder + CO2</name>
  <files>tests/test_spectra_g98.py, serpentrum/spectra.py</files>
  <action>
RED-2: append test cases 7-11 from the <feature> block to tests/test_spectra_g98.py:
- Synthetic CO2 vibspectrum built as a module-level string constant clearly labeled synthetic (comment in the test: values from co2.log lines 456-457/546-547; row 9 omitted because the log's intensity token is the overflow `******`). Assert 8 modes, real_modes -> [600.18, 600.18, 1424.95] as separate rows, intensity of the 1424.95 mode == 0.0 exactly, and broaden-with == broaden-without EXACT list equality (zero contribution is arithmetic, not approximate).
- Loud failures: bad.log text -> SpectraParseError containing 'frequency section'; g98 text truncated after line 60 -> SpectraParseError containing 'atom' (use the copied fixture text, split and re-join its first 60 lines); vibspectrum without `$end`, with a garbage row, and with a `******` intensity token -> SpectraParseError each.
- Synthetic remainder block: build a 3-atom g98-shaped text (Standard orientation block with 3 rows; block 1 = 3 columns modes 1-3; block 2 = 1 column mode 4, e.g. ` Frequencies --  100.0000`, ` Red. masses --  1.0`, ` Frc consts  --  0.0`, ` IR Inten    --  5.0`, ` Raman Activ --  0.0`, ` Depolar     --  0.0`, ` Atom AN      X`, then rows `   1   6    0.10` / `   2   6    0.20` / `   3   1    0.30`). Assert 4 modes and the last mode's vectors == three 1-float tuples. Label synthetic in a comment (no real fixture has a remainder block — token-count-driven parsing is the asserted assumption).
- broaden: cases 5-6 already partially in place; add explicit len(xs)==800 / ys>=0 / peak-within-fwhm-of-1150.6639 assertions if not already covered.
Run -> the new tests MUST fail (broaden missing, error paths incomplete). Commit: `test(02-01): add broaden, loud-failure, synthetic remainder + CO2 spectra tests`.
GREEN-2: implement broaden + the remaining SpectraParseError paths + the `******` rejection in serpentrum/spectra.py. Run the full file -> green. Commit: `feat(02-01): implement broaden + loud-failure paths`.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (no regressions); `python3.6 tests/run_gates.py` -> all gates green.</verify>
  <done>All 11 behavior cases green; parser fails loudly on bad.log/truncation/vibspectrum corruption; zero-intensity and remainder-block semantics proven; full suite + gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> OK
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (existing skeleton/purity/winpath tests unbroken)
- `python3.6 tests/run_gates.py` -> exit 0, all gates green
- `python3.6 tools/check_purity.py` -> clean (spectra.py auto-classified PURE, zero violations)
- `ls tests/fixtures/xtb | wc -l` -> 17; no `__init__.py` anywhere under tests/
</verification>

<success_criteria>
- parse_g98 on the committed dimer g98.out: 26 atoms, 72 modes, first freq -31.9175, last 3521.1843, per-atom vectors present, negatives preserved (phase criterion 2, phenol/dimer leg)
- parse_vibspectrum: 78 modes, trivial/negative/zero-intensity rows handled; correspondence vs g98 exact at computed offset 6 (freq tol 5e-3, intensity tol 1e-4 — corrected from research)
- bad.log raises SpectraParseError mentioning 'frequency section' — the corrupt-fixture loud failure
- Synthetic CO2 (labeled synthetic): 5 trivial filtered, 1424.95 @ exactly 0.0 contributes exactly 0 to the curve, degenerate 600.18 x2 retained
- broaden: 800 points, ys >= 0, peak within fwhm of 1150.6639
- Zero sys.modules stubs; stdlib-only PURE module; py3.6 syntax; gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-01-SUMMARY.md`
</output>
