---
phase: 02-pure-core-game-chemistry-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/spectra.py
  - tests/test_spectra_g98.py
  - tests/fixtures/xtb/
autonomous: true

must_haves:
  truths:
    - "parse_g98 on the committed dimer g98.out returns 26 atoms and 72 modes with negative frequencies (first = -31.9175) and per-atom 3-component displacement vectors preserved"
    - "Block grammar holds end to end: 24 blocks of 3 columns at stride 35 (first 'Frequencies --' at line 46, last at 851) parse to modes 1..72 in order with per-block vector columns; the final block runs to EOF with no terminator required"
    - "A synthetic remainder block (4 modes in 3+1 columns) parses; the last mode carries 1-float vector tuples per atom"
    - "Feeding bad.log raises SpectraParseError whose message mentions the missing frequency section (loud failure contract; g98-side ****** and truncation tests land in 02-12)"
    - "All 17 fixture copies under tests/fixtures/xtb/ are byte-identical to .planning/research/xtb-spike-fixtures/ (single-copy rule; later plans consume from here)"
  artifacts:
    - path: "serpentrum/spectra.py"
      provides: "Mode/Atom/Spectrum namedtuples, SpectraParseError, parse_g98(_text) — the g98 core ONLY (parse_vibspectrum/real_modes are 02-09's; broaden/parse dispatcher are 02-12's) — stdlib-only PURE module"
      min_lines: 90
      contains: "SpectraParseError"
    - path: "tests/test_spectra_g98.py"
      provides: "fixture-driven g98 test suite (happy path, block structure, negatives, per-atom vectors, remainder block, loud failures, byte-identity)"
      min_lines: 120
      contains: "parse_g98"
    - path: "tests/fixtures/xtb/"
      provides: "17 byte-identical copies of the committed xtb spike fixtures (single copy; 02-09/02-12 consume from here)"
      contains: "g98.out"
  key_links:
    - from: "tests/test_spectra_g98.py"
      to: "tests/fixtures/xtb/g98.out"
      via: "open(path, encoding='utf-8').read() -> spectra.parse_g98(path)"
      pattern: "parse_g98"
    - from: "tests/test_spectra_g98.py"
      to: ".planning/research/xtb-spike-fixtures/"
      via: "byte-identity check proving the copies match the committed source fixtures"
      pattern: "xtb-spike-fixtures"
    - from: "serpentrum/spectra.py"
      to: "stdlib math/collections only"
      via: "module imports (purity gate auto-classifies this file PURE — no pymol/Qt/numpy anywhere)"
      pattern: "import math"
---

<objective>
Build the fixture-first g98.out parser core (`serpentrum/spectra.py`): parse_g98/parse_g98_text, the Mode/Atom/Spectrum namedtuples, SpectraParseError, 3-column frequency blocks with remainder handling, negatives preserved — plus the ONCE-only fixture copies under `tests/fixtures/xtb/`. This is wave 1 of the ROADMAP's three-plan spectra split: 02-01 = g98 core + fixtures (this plan), 02-09 = vibspectrum fallback parser + trivial-mode filter + synthetic CO2 fixture, 02-12 = broaden + parse dispatcher + corrupt-fixture loud failures + index correspondence.

Purpose: every later spectra feature consumes this module's output; it is written against the fixture grammar probed line-by-line, not against a guessed format. Keeping this plan scoped to the g98 core keeps each of the three spectra plans independently executable and contradiction-free.

Output: `serpentrum/spectra.py` (g98 core only), `tests/test_spectra_g98.py`, and the fixture copies under `tests/fixtures/xtb/`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md
@tests/test_skeleton.py

Scope note (ROADMAP split — do NOT exceed it): this plan implements parse_g98/parse_g98_text ONLY. Do NOT add parse_vibspectrum(_text), real_modes, broaden, or the parse()/parse_text() dispatcher — not even as stubs. 02-09 appends the vibspectrum parser + real_modes; 02-12 appends broaden + the dispatcher + the ****** / truncation loud-failure tests + the g98<->vibspectrum correspondence. Overlap here would triple-define downstream contracts (the exact drift the phase checker rejected).

Load-bearing verified facts (probed 2026-09-06/07 against the committed fixtures — trust these over the research doc where they differ):
- **g98.out grammar (dimer fixture, 883 CRLF lines):** line 6 = `Standard orientation:`; lines 7-10 = dash / 2 header lines / dash; lines 11-36 = 26 atom rows of exactly 6 tokens (center, atomic_number, type, x, y, z); line 37 = dash. Frequency section: 24 blocks, first `Frequencies --` line at 46, last at 851, stride 35; file ENDS at EOF right after the final atom row (line 883) — EOF terminates the section. Block = 2 ignored lines (mode-index, symmetry) + `Frequencies --` + 5 property lines + `Atom AN` header + 26 rows of 11 tokens (index, AN, 3x ncols floats). Exact one-leading-space prefixes: `' Frequencies --'`, `' Red. masses --'`, `' Frc consts  --'`, `' IR Inten    --'`, `' Raman Activ --'`, `' Depolar     --'`.
- **g98 values:** 72 modes = 3x26-6. First freqs -31.9175 / -23.0766 / -18.1086 with IR inten 2.4191 / 0.0072 / 0.2541. Last freqs 3113.7215 / 3519.9549 / 3521.1843. Min intensity 0.00030, max 257.1018 (mode 42 @ 1150.6639 cm^-1 — the verified global argmax; note research's broaden row mentioned 364.42 which is only the strongest LOW mode, not the argmax). No exact-zero intensities among the 72. Raman/Depolar all 0.0000 (ignore).
- **bad.log:** 105 lines; `[ERROR] Program stopped due to fatal error` at line 97; NO frequency section -> parse must raise loudly.
- **Fixture trap:** `dimer.xyz` is MISLABELED (a CO2 dimer, 6 atoms) — it is NOT copied and NEVER mined for phenol data. `dimer2.xyz` is the real pi-stacked phenol dimer.
- **Reading files:** open with `encoding='utf-8'` (the header contains UTF-8 `⁻¹`) and split with `str.splitlines()` — neutralizes CRLF and a missing trailing newline.
- **Hard conventions (AGENTS.md / STATE.md):** Python 3.6 syntax only — no dataclasses, no walrus, no f-string `=`; use %-formatting. WSL python3.6.9 has NO numpy — stdlib only (math, collections); ZERO pymol/pmg_tk/PyQt5/numpy anywhere (the purity checker auto-classifies new serpentrum/ modules PURE and gates function bodies too). Tests: zero sys.modules stubs; `unittest.mock.patch` of callables is acceptable but not needed here.
- **Test conventions:** discovery = `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (never `-t .`). tests/ has NO `__init__.py` and must never get one. Every test file repeats the sys.path self-insert preamble (copy from tests/test_skeleton.py). `tests/fixtures/xtb/` must NOT contain an `__init__.py` either (plugin-path safety, run_gates gate 1).
- **Wave-1 parallelism:** do NOT import other Phase-2 modules (xtbenv/xyzio/stacking/molecule_data/game_engine/setup_logic/cgo_build) — parallel plans may not have them yet; this plan is fully self-contained (stdlib + serpentrum/spectra only). Your OWN `tests/fixtures/xtb/` copies exist because this plan creates them in its own worktree; later-wave plans consume them after the merge.
</context>

<feature>
  <name>spectra parser core: g98.out fixture grammar, namedtuples, loud SpectraParseError</name>
  <files>serpentrum/spectra.py, tests/test_spectra_g98.py</files>
  <behavior>
    All cases input -> expected output:
    1. parse_g98(fixture g98.out) -> Spectrum with n_atoms == 26; atoms[0] == Atom(6, ~0.020733, ~-1.840903, ~-0.006438); len(modes) == 72; modes[0].index == 1; modes[0].freq == -31.9175 (places=4); modes[0].intensity == 2.4191 (places=4); modes[-1].freq == 3521.1843 (places=4); modes[0].vectors is a list of 26 3-tuples of floats; min(m.freq) < 0 for the first three modes (negatives kept).
    2. Block structure: modes[0..2] are block 1 (-31.9175 / -23.0766 / -18.1086 with intensities 2.4191 / 0.0072 / 0.2541); modes[69..71] are block 24 (3113.7215 / 3519.9549 / 3521.1843); mode indices run 1..72 sequentially across blocks; every mode's vectors is a list of 26 tuples of exactly 3 floats (column count taken from the Frequencies line, never fixed).
    3. Remainder block (synthetic, clearly labeled): 3-atom molecule, block 1 = modes 1-3 with 3 columns, block 2 = mode 4 with 1 column -> 4 modes parsed; modes[3].vectors is a list of 3 one-float tuples. (No real fixture has a remainder block — token-count-driven parsing is the asserted assumption.)
    4. Loud failure (corrupt fixture): parse_g98_text(bad.log text) raises SpectraParseError and 'frequency section' in str(exc).
    5. Loud failure (malformed block, synthetic in-memory): a g98-shaped text where one block's `' IR Inten    --'` line is replaced by garbage raises SpectraParseError carrying the parser stage and a 1-based line number. (Truncation and `******`-overflow loud-failure tests are 02-12's scope — do not add them here; the numeric-conversion wrap itself IS this plan's job so no raw ValueError ever escapes.)
    6. Byte-identity: every file in tests/fixtures/xtb/ is byte-identical ('rb') to its .planning/research/xtb-spike-fixtures/ counterpart.
  </behavior>
  <implementation>
    serpentrum/spectra.py — stdlib only (collections, math). `Mode = namedtuple('Mode', 'index freq intensity vectors')`, `Atom = namedtuple('Atom', 'atomic_number x y z')`, `Spectrum = namedtuple('Spectrum', 'n_atoms atoms modes')`, `class SpectraParseError(ValueError)` (message MUST contain the parser stage, 1-based line number(s) where applicable, and the offending line's first ~80 chars). %-formatting everywhere.
    - parse_g98_text(text): lines = text.splitlines(). Atom block: find the line whose strip() == 'Standard orientation:'; scan forward to the next all-dash line; read rows of exactly 6 tokens (int, int, int, float, float, float) until the next all-dash line; n_atoms = row count (n_atoms < 3 -> SpectraParseError('atom block', ...)). Frequency scan: iterate lines; on one starting with ' Frequencies --': cols = line.split('--', 1)[1].split(); ncols = len(cols); require 1 <= ncols <= 3; the NEXT 5 lines must start with (in order) ' Red. masses --', ' Frc consts  --', ' IR Inten    --', ' Raman Activ --', ' Depolar     --', each yielding exactly ncols floats after '--'; the next line must start with ' Atom AN'; then exactly n_atoms rows of 2 + 3*ncols whitespace tokens with atom indices running 1..n_atoms in order — per-atom vector = tuple of ncols floats. Any violation or premature EOF -> SpectraParseError with stage + line number + excerpt. Mode index = running count from 1 across blocks. NO ' Frequencies --' found anywhere -> SpectraParseError('no frequency section found ...'). Ignore mode-index/symmetry lines entirely (column count comes from the Frequencies line, never fixed columns). EVERY float()/int() conversion is wrapped: a non-numeric token raises SpectraParseError (stage + line + excerpt), never a raw ValueError (02-12 exercises this with `******`; do not test it here).
    - parse_g98(path): open(path, encoding='utf-8'), delegate to parse_g98_text.
    - Explicitly OUT of scope (02-09/02-12 own them): parse_vibspectrum(_text), real_modes, broaden, parse()/parse_text().
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 (RED-1): copy fixtures once + failing fixture-driven g98 tests</name>
  <files>tests/fixtures/xtb/, tests/test_spectra_g98.py</files>
  <action>
1. Create `tests/fixtures/xtb/` (plain directory — NO `__init__.py`, ever) and copy EXACTLY these 17 files from `.planning/research/xtb-spike-fixtures/` (keep original names; cp is safe, never move):
   g98.out, vibspectrum, bad.log, co2.log, dimer2.log, ohess.log, repro_oh.log, bad.err, co2.err, dimer2.err, ohess.err, repro_oh.err, bad.xyz, co2.xyz, dimer2.xyz, phenol.xyz, xtbopt.xyz
   Do NOT copy `dimer.xyz` (mislabeled CO2-dimer trap — must never be mined for phenol data), `hessian` (Anti-Pattern 5: never parse/eigendecompose), `xtbrestart` (binary), charges/wbo/xtbtopo.mol/xtbopt.log/xtbhess.xyz/.xtboptok (context only). Keep `.planning/research/xtb-spike-fixtures/` untouched. (vibspectrum/co2.log/ohess.log copies are consumed by 02-09/02-12 — copy them NOW per the single-copy rule even though this plan only parses g98.out.)
2. Write `tests/test_spectra_g98.py` following the tests/test_skeleton.py pattern: docstring pinning discovery (`python3.6 -m unittest discover -s tests -p "test_*.py" -v` — never `-t .`), then the sys.path self-insert `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`. Define `ROOT` and `FIXTURES = os.path.join(ROOT, 'tests', 'fixtures', 'xtb')`.
3. Write the RED-1 test cases (they must FAIL now — serpentrum/spectra.py does not exist; the import error IS the RED state): behavior cases 1-2 from the <feature> block (g98 happy path with exact verified values; block structure incl. first/last block values, sequential indices, per-mode 3-float vectors), PLUS the byte-identity test (case 6) asserting every copied file's bytes equal its `.planning/research/xtb-spike-fixtures/` counterpart (read 'rb' both sides).
   Use assertAlmostEqual with explicit places/delta per case; never bare float equality except the exact-equality cases specified.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` runs and FAILS (ModuleNotFoundError/ImportError for serpentrum.spectra = the RED state); `ls tests/fixtures/xtb | wc -l` -> 17; `python3.6 -c "import hashlib;print(hashlib.md5(open('tests/fixtures/xtb/g98.out','rb').read()).hexdigest())"` matches the same hash of `.planning/research/xtb-spike-fixtures/g98.out`.</verify>
  <done>17 fixture copies byte-identical to source; RED-1 test file fails for the single reason that serpentrum/spectra.py does not exist yet. Commit: `test(02-01): add failing fixture-driven g98 parser tests + fixture copies`.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN-1): implement the g98 parser core (namedtuples, SpectraParseError, parse_g98)</name>
  <files>serpentrum/spectra.py</files>
  <action>
Create serpentrum/spectra.py implementing ONLY the g98 core per the <implementation> block: the three namedtuples, SpectraParseError, parse_g98_text, parse_g98. Minimal code to make Task 1's tests pass — no cleverness. Python 3.6 syntax (%-formatting, no dataclasses/walrus/f-string `=`), stdlib imports only (`collections`, `math` — the purity checker auto-classifies this module PURE and bans pymol/Qt/numpy even inside function bodies). splitlines() for CRLF; utf-8 opens; NEVER parse by fixed columns or fixed line numbers; the `' Frequencies --'`-style prefixes carry exactly one leading space (verified); the numeric-conversion wrap (SpectraParseError, never raw ValueError) is part of this core. Do NOT add parse_vibspectrum/real_modes/broaden/parse-dispatcher — later plans append them.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> all Task 1 tests OK; `python3.6 tests/run_gates.py` -> all gates green (purity + syntax + full suite).</verify>
  <done>The real g98 fixture parses to the exact verified values (26 atoms, 72 modes, block-1/block-24 anchors, negatives, per-atom vectors); fixtures byte-identical; gates green. Commit: `feat(02-01): implement g98 spectra parser core (fixture-exact)`.</done>
</task>

<task type="auto">
  <name>Task 3 (RED-2 + GREEN-2): remainder block + g98 error paths (loud failures)</name>
  <files>tests/test_spectra_g98.py, serpentrum/spectra.py</files>
  <action>
RED-2: append test cases 3-5 from the <feature> block to tests/test_spectra_g98.py:
- Synthetic remainder block (case 3): build a 3-atom g98-shaped text (Standard orientation block with 3 rows; block 1 = 3 columns modes 1-3; block 2 = 1 column mode 4, e.g. ` Frequencies --  100.0000`, ` Red. masses --  1.0`, ` Frc consts  --  0.0`, ` IR Inten    --  5.0`, ` Raman Activ --  0.0`, ` Depolar     --  0.0`, ` Atom AN      X`, then rows `   1   6    0.10` / `   2   6    0.20` / `   3   1    0.30`). Assert 4 modes and the last mode's vectors == three 1-float tuples. Label synthetic in a comment (no real fixture has a remainder block — token-count-driven parsing is the asserted assumption).
- Loud failure, bad.log (case 4): parse_g98_text(bad.log text) raises SpectraParseError containing 'frequency section'. NOTE: after GREEN-1 this may already pass (the scan loop raises naturally) — include it as a pinned regression test, not a theatrical RED.
- Loud failure, malformed block (case 5, synthetic in-memory): take the real g98 fixture text, replace block 1's `' IR Inten    --'` line (line 49) with `' IR Inten    --  garbage not-floats'`, re-join -> SpectraParseError whose message contains a 1-based line number and an excerpt. This exercises the stage/line/excerpt contract 02-12 relies on.
Run -> case 3 and case 5 MUST fail (remainder handling and error-path details incomplete). Commit: `test(02-01): add remainder-block and g98 loud-failure tests`.
GREEN-2: implement the remainder handling and complete the SpectraParseError paths (stage + 1-based line + excerpt on every violation, wrapped conversions). Run the full file -> green. Commit: `feat(02-01): remainder blocks + g98 error paths`.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (no regressions); `python3.6 tests/run_gates.py` -> all gates green.</verify>
  <done>All 6 behavior cases green; the g98 core fails loudly (bad.log, malformed blocks) with stage+line+excerpt; remainder blocks parse; full suite + gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_spectra_g98.py" -v` -> OK
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK (existing skeleton/purity/winpath tests unbroken)
- `python3.6 tests/run_gates.py` -> exit 0, all gates green
- `python3.6 tools/check_purity.py` -> clean (spectra.py auto-classified PURE, zero violations)
- `ls tests/fixtures/xtb | wc -l` -> 17; no `__init__.py` anywhere under tests/
- `grep -nE "parse_vibspectrum|real_modes|broaden|def parse_text|def parse\(" serpentrum/spectra.py` -> no matches (scope guard: none of the 02-09/02-12 functions leak into this plan)
</verification>

<success_criteria>
- parse_g98 on the committed dimer g98.out: 26 atoms, 72 modes, first freq -31.9175, last 3521.1843, per-atom vectors present, negatives preserved (g98 leg of phase criterion 2)
- Block grammar proven end to end: 24 blocks x 3 columns at stride 35, sequential indices, EOF-terminated final block
- Remainder block (3+1 columns) parses; bad.log and malformed blocks raise SpectraParseError with stage + line + excerpt — the loud-failure contract 02-12 extends
- 17 fixture copies byte-identical; zero sys.modules stubs; stdlib-only PURE module; py3.6 syntax; gates green; NO vibspectrum/real_modes/broaden code in the module
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-01-SUMMARY.md`
</output>
