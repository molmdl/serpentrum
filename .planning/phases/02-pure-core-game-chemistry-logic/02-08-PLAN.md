---
phase: 02-pure-core-game-chemistry-logic
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/cgo_build.py
  - tests/test_cgo_build.py
autonomous: true

must_haves:
  truths:
    - "All CGO opcode constants are LOCAL float literals matching the verified pymol cgo.py table (POINTS 0.0, BEGIN 2.0, VERTEX 4.0, COLOR 6.0, SPHERE 7.0, LINES 1.0, LINEWIDTH 10.0, CYLINDER 9.0, END 3.0, STOP 0.0, TRIANGLES 4.0, TRIANGLE_STRIP 5.0, LINE_STRIP 3.0, NULL 1.0, CONE 27.0) — importing pymol.cgo would pull `from pymol import cmd` at module level AND violate the purity gate"
    - "There is NO VERSION constant (verified: pymol/cgo.py has none — nothing to copy)"
    - "box_cgo emits [LINEWIDTH, w] + [BEGIN, LINES, COLOR, r,g,b] + 12 edges (24 VERTEX floats triplets) + [END, STOP] — a parseable CGO stream built purely from two corners"
    - "mode_arrows emits one CYLINDER (14 floats: opcode + 13) + one CONE (17 floats: opcode + 16, TWO radii + 6 color floats + 2 cap floats = flat caps 1.0) per nonzero displacement vector, skipping near-zero vectors (< 1e-6)"
    - "spheres_cgo emits one COLOR header + [SPHERE, x, y, z, radius] per point + [STOP]"
    - "Every builder returns a plain float list whose structure is verified positionally by a mini CGO-stream interpreter in the tests — no PyMOL needed"
  artifacts:
    - path: "serpentrum/cgo_build.py"
      provides: "local CGO constants, box_cgo, mode_arrows, spheres_cgo — stdlib-only PURE module (bridge-facing pure half of the rendering stack)"
      min_lines: 100
      contains: "CONE"
    - path: "tests/test_cgo_build.py"
      provides: "structural stream tests via opcode-size interpreter + exact arithmetic checks (shaft/tip offsets, radii, caps)"
      min_lines: 120
      contains: "box_cgo"
  key_links:
    - from: "serpentrum/cgo_build.py"
      to: "local literal constants (NOT pymol.cgo)"
      via: "module imports stdlib only — pymol.cgo imports `from pymol import cmd` at ITS module level, so importing it here would break purity (verified cgo.py:79-156)"
      pattern: "^import|^from"
    - from: "serpentrum/cgo_build.py mode_arrows"
      to: "verified CONE float layout"
      via: "CONE = opcode + [x1,y1,z1, x2,y2,z2, r_base, r_tip, cr1,cg1,cb1, cr2,cg2,cb2, cap1, cap2] (CGO_CONE_SZ = 16, CGO.h:143; write order CGO.cpp:916-943; caps cCylCap Flat=1, Basis.h:41-46)"
      pattern: "mode_arrows"
    - from: "tests/test_cgo_build.py"
      to: "the CGO stream as positional float list"
      via: "mini interpreter consuming (opcode, operand-count) pairs — LINES blocks contain VERTEX pairs, closed by END"
      pattern: "VERTEX"
---

<objective>
Build the pure CGO construction module (`serpentrum/cgo_build.py`): local opcode constants, the boundary-box builder (STACK.md §3's displayed boundary), the vibrational mode-arrow builder (SPECTRA-05's static displacement vectors), and a sphere builder (GAME-03's head rendering) — all returning plain float lists, unit-tested without PyMOL. This is the bridge-facing pure half of the rendering stack.

Purpose: CGO geometry must be constructible and verifiable in WSL python3.6 with zero PyMOL imports; the constants are LOCAL literals because `pymol/cgo.py` pulls `from pymol import cmd` at its module level (which would violate INFRA-02 purity AND drag the GUI runtime into pure-land). Layouts (SPHERE 4, CYLINDER 13, CONE 16 operands; flat caps) are verified from the C source, not guessed.

Output: `serpentrum/cgo_build.py`, `tests/test_cgo_build.py`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-demo-data.md

Verified CGO facts (research §9 — probed against pymol-src, trust these exactly):
- **Constants table (cgo.py:21-65):** POINTS 0.0, STOP 0.0, SPHERE 7.0, LINES 1.0, NULL 1.0, CYLINDER 9.0, LINE_STRIP 3.0, BEGIN 2.0, LINEWIDTH 10.0, TRIANGLES 4.0, END 3.0, COLOR 6.0, TRIANGLE_STRIP 5.0, VERTEX 4.0, CONE 27.0. **There is NO VERSION constant in pymol/cgo.py** — the research question's list included it in error; assert its absence.
- **Float-list layouts (C-source verified):** SPHERE = opcode + 4 floats [x, y, z, radius] (CGO_SPHERE_SZ = 4, CGO.h:96); needs a preceding COLOR triplet. CYLINDER = opcode + 13 floats [x1,y1,z1, x2,y2,z2, radius, r1,g1,b1, r2,g2,b2] (CGO_CYLINDER_SZ = 13, CGO.h:100). CONE = opcode + **16** floats [x1,y1,z1, x2,y2,z2, r1, r2, cr1,cg1,cb1, cr2,cg2,cb2, cap1, cap2] — TWO radii (base, tip) and cap flags (CGO_CONE_SZ = 16, CGO.h:143; write order CGO.cpp:916-943; caps read CGO.cpp:5754-5756; cCylCap: None=0, Flat=1, Round=2, Basis.h:41-46). Emit cap=1 (Flat) for arrow tips.
- **LINES block shape:** [BEGIN, LINES] then per edge [VERTEX, x,y,z, VERTEX, x,y,z], closed by [END]; [LINEWIDTH, w] precedes the block. Box EDGES via LINES (not TRIANGLE_STRIP faces — STACK.md §3 boundary decision; prior art wizard/box.py:261-311 uses faces, edges need no normals).
- **Box edges (standard enumeration, corners from (min, max)):** bottom z-min rectangle c000-c100-c110-c010-c000; top z-max rectangle c001-c101-c111-c011-c001; four verticals c000-c001, c100-c101, c110-c111, c010-c011. (cXYZ = (x?, y?, z?) with 0=min, 1=max.)
- **mode_arrows arithmetic:** unit vector v_hat = v / |v|; shaft end = p + v_hat * scale * length_a * 0.7; tip = p + v_hat * scale * length_a. CYLINDER shaft p -> shaft_end (both colors = the arrow color); CONE head shaft_end -> tip with (r_base=base_radius, r_tip=0.0, caps 1.0, 1.0). Vectors shorter than 1e-6 are skipped.
- **Hard conventions:** Python 3.6 (%-formatting, no dataclasses/walrus); stdlib math only; purity auto-classifies PURE — importing pymol.cgo is FORBIDDEN (constants are local literals); zero sys.modules stubs; do NOT import other Phase-2 modules.
- **Test conventions:** discovery = `python3.6 -m unittest discover -s tests -p "test_*.py" -v` (never `-t .`); tests/ has NO `__init__.py`; sys.path self-insert preamble.
</context>

<tasks>

<task type="auto">
  <name>Task 1: local CGO constants + box_cgo builder + structural tests</name>
  <files>serpentrum/cgo_build.py, tests/test_cgo_build.py</files>
  <action>
Create `serpentrum/cgo_build.py` (stdlib math only; py3.6; %-formatting):
- Module docstring: why constants are LOCAL (pymol.cgo imports `from pymol import cmd` at module level — purity violation; verified cgo.py:21-65, 79-156) and that NO VERSION constant exists.
- The 15 constants from the verified table as float literals with a comment citing cgo.py:21-65.
- `box_cgo(min_corner, max_corner, color=(1.0, 0.4, 0.1), linewidth=2.0)` -> float list: `[LINEWIDTH, linewidth, BEGIN, LINES, COLOR, r, g, b]` + 12 edges each `[VERTEX, x,y,z, VERTEX, x,y,z]` + `[END, STOP]`. Corners enumerated EXPLICITLY (c000..c111 as named locals — no clever loops that could reorder; the 12 edge pairs in the standard order from <context>). min_corner/max_corner are 3-tuples. Total length = 4 + 12*7 + 2 = 90 floats.

Create `tests/test_cgo_build.py` (sys.path self-insert + discovery-pin docstring):
1. Constants test: each of the 15 names equals its verified float value; `assertFalse(hasattr(cgo_build, 'VERSION'))`.
2. Mini CGO-stream interpreter helper in the test file: walk (opcode, operand_count) with LINEWIDTH=1, COLOR=3, VERTEX=3, BEGIN=0, END=0, STOP=0, LINES=0, SPHERE=4, CYLINDER=13, CONE=16; assert the stream divides exactly into valid records with no trailing garbage and no unknown opcodes.
3. box_cgo structure: interpreter passes; first two floats == [10.0, 2.0]; exactly one BEGIN record, one LINES record right after it, one COLOR record with the requested color, one END, one STOP (positional check: floats[2] == BEGIN, floats[3] == LINES, floats[4] == COLOR, floats[5:8] == color, floats[-2:] == [END, STOP]); VERTEX record count == 24; every value is a float instance (ints not allowed — CGO streams are float arrays).
4. Corner proof: the multiset of 24 vertex coordinate triples contains each of the 8 corners EXACTLY 3 times (each corner touches 3 edges); all vertex coords lie within [min, max] per axis.
5. Geometry sanity: box_cgo((-1.0, -2.0, -3.0), (1.0, 2.0, 3.0)) contains a VERTEX at exactly (-1.0, -2.0, -3.0) and one at (1.0, 2.0, 3.0); custom color/linewidth propagate to the COLOR/LINEWIDTH records.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_cgo_build.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> green.</verify>
  <done>Constants match the verified table (no VERSION); box_cgo produces a parseable 90-float 12-edge stream with correct corner multiplicities. Commit: `feat(02-08): local CGO constants + boundary box builder`.</done>
</task>

<task type="auto">
  <name>Task 2: mode_arrows + spheres_cgo + full verification</name>
  <files>serpentrum/cgo_build.py, tests/test_cgo_build.py</files>
  <action>
Add to serpentrum/cgo_build.py:
- `mode_arrows(atoms, vecs, scale=1.0, color=(0.2, 0.6, 1.0), base_radius=0.06, length_a=1.2)` -> float list: `[COLOR, r, g, b]` once, then per atom whose vector magnitude >= 1e-6: CYLINDER record (opcode + p, shaft_end, base_radius, color, color — 14 floats) followed by CONE record (opcode + shaft_end, tip, base_radius, 0.0, color, color, 1.0, 1.0 — 17 floats), then `[STOP]`. atoms = sequence of (x, y, z) positions; vecs = same-length sequence of (x, y, z) displacements (as extracted from spectra Mode.vectors by the caller). Docstring notes the CONE layout (two radii + caps) and the 0.7 shaft fraction.
- `spheres_cgo(points, radius, color)` -> float list: `[COLOR, r, g, b]` + per point `[SPHERE, x, y, z, radius]` + `[STOP]`.

Tests:
1. Single arrow arithmetic: atoms [(0.0, 0.0, 0.0)], vecs [(1.0, 0.0, 0.0)], defaults -> interpreter passes; stream == [COLOR, 0.2, 0.6, 1.0] + [CYLINDER, 0,0,0, 0.84,0,0, 0.06, 0.2,0.6,1.0, 0.2,0.6,1.0] + [CONE, 0.84,0,0, 1.2,0,0, 0.06, 0.0, 0.2,0.6,1.0, 0.2,0.6,1.0, 1.0, 1.0] + [STOP] — assert positionally with assertAlmostEqual(delta=1e-9): shaft end x == 1.0*1.0*1.2*0.7 == 0.84, tip x == 1.2; CONE's two radius slots == (0.06, 0.0); the two cap floats == (1.0, 1.0); total length == 4 + 14 + 17 + 1 == 36.
2. Scaled arrows: scale=2.0 -> shaft end at 1.68, tip at 2.4 along the unit direction; a diagonal vector (1.0, 1.0, 0.0) -> tip at p + (1.2/sqrt(2), 1.2/sqrt(2), 0) (v_hat normalization proof).
3. Zero-vector skip: 3 atoms, vecs [(1,0,0), (0,0,0), (0.5, 0.5, 0.5)] -> exactly 2 CYLINDER + 2 CONE records (interpreter counts); total length == 4 + 2*31 + 1 == 67. Vector (1e-7, 0, 0) is also skipped (below the 1e-6 threshold).
4. Non-unit direction: vec (0.0, 3.0, 0.0) with scale 1.0 -> tip at (0.0, 1.2, 0.0) (direction normalized BEFORE scaling — a 3 A displacement does not make a 3.6 A arrow).
5. spheres_cgo: points [(1,2,3), (4,5,6)], radius 0.5, color (1.0, 0.0, 0.0) -> [COLOR, 1,0,0, SPHERE, 1,2,3, 0.5, SPHERE, 4,5,6, 0.5, STOP], length 15; interpreter passes; empty points -> [COLOR, ..., STOP] length 4.
Then run the full verification battery.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_cgo_build.py" -v` -> OK; `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK; `python3.6 tests/run_gates.py` -> exit 0; `python3.6 tools/check_purity.py` -> clean; `grep -n "pymol\|numpy" serpentrum/cgo_build.py` -> no matches (purity + local-constants rule).</verify>
  <done>All three builders produce parseable, arithmetically-exact CGO float streams; CONE two-radii + flat-cap layout proven; zero vectors skipped; gates green. Commit: `feat(02-08): vibrational mode arrows + sphere builder (pure CGO streams)`.</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_cgo_build.py" -v` -> OK
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` -> OK
- `python3.6 tests/run_gates.py` -> exit 0
- `python3.6 tools/check_purity.py` -> clean (cgo_build.py auto-classified PURE; stdlib math only)
- `grep -n "import" serpentrum/cgo_build.py` -> only stdlib (math); no pymol/PyQt5/numpy
</verification>

<success_criteria>
- 15 local opcode constants exactly match the verified pymol cgo.py table; NO VERSION constant; no pymol import anywhere (purity + GUI-runtime independence)
- box_cgo: 90-float stream, LINEWIDTH + BEGIN/LINES/COLOR + 24 VERTEX + END/STOP, corners with 3x multiplicity each
- mode_arrows: CYLINDER(14) + CONE(17, two radii + caps 1.0) per nonzero vector, v_hat normalization before scaling, 0.7 shaft fraction, < 1e-6 vectors skipped
- spheres_cgo: one COLOR + [SPHERE, x,y,z,r] per point + STOP
- Zero sys.modules stubs; stdlib-only PURE module; py3.6 syntax; gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-08-SUMMARY.md`
</output>
