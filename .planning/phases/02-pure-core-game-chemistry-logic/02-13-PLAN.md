---
phase: 02-pure-core-game-chemistry-logic
plan: 13
type: execute
wave: 3
depends_on: ["02-06", "02-10"]
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_turns.py
autonomous: true

must_haves:
  truths:
    - "A turn request rotates the WHOLE chain rigidly about the head over exactly TURN_TICKS=6 ticks (15 deg/tick — TURN_DEGREES/TURN_TICKS, constants defined by THIS plan): after completion every segment centroid sits at its pre-turn position rotated exactly +/-90 deg about the head (assertAlmostEqual 1e-9), atoms rotate with their segment (x, y rotate; z and symbol preserved), and heading == target"
    - "A turn whose sampled arc is unsafe is REFUSED with ('turn_refused', reason) and ZERO state change (heading, segments, sweeping, pending all unchanged) — three legs at all 7 sampled poses, ONE consistent model with 02-10: boundary = rotated segment CENTROIDS outside the BOUNDARY_MARGIN_A-adjusted box; body = head-centroid vs rotated polyline edges (same edge set, _point_segment_distance_sq, BODY_COLLISION_RADIUS_A, SEGMENT_SKIP_RECENT as 02-10); pickups = any rotated chain ATOM within SWEEP_PICKUP_CLEARANCE_A=2.5 of any live pickup ATOM (atom-level, matching 02-04's 2.5 A clash threshold; head excluded — it is the invariant pivot)"
    - "180 deg is enforced at SWEEP level: requests are judged against the sweep's TARGET heading while sweeping, so 'down' during an east->up sweep is ignored while 'left' is buffered and chains into a second sweep after completion"
    - "Sweep ticks pause forward motion (no 'moved' events) and emit ('turning', tick/TURN_TICKS) each tick; the pending direction is applied EXACTLY ONCE at the START of the step AFTER sweep completion (never inside the completion tick) — a refused chained request is consumed and that same tick falls through to forward motion"
    - "reset() wipes all turn state (sweeping, pending) — no stale turn survives a restart; no crash can occur mid-sweep because the 7-sample pre-check covers every swept pose"
  artifacts:
    - path: "serpentrum/game_engine.py"
      provides: "rigid-pivot sweep machinery: _rotate_xy helper, TURN_DEGREES/TURN_TICKS constants, start_sweep() with the 7-sample swept-region pre-check (boundary + body + pickups), sweep progression in step(), pending application at next-step start"
      contains: "start_sweep"
    - path: "tests/test_engine_turns.py"
      provides: "rotation-exactness, 3-leg refusal, sweep-level 180-degree, chained-turn, and reset-epoch tests (with shown arithmetic for every exact-tick assertion)"
      min_lines: 150
  key_links:
    - from: "serpentrum/game_engine.py start_sweep"
      to: "02-10's collision helpers and constants"
      via: "body leg reuses _point_segment_distance_sq + BODY_COLLISION_RADIUS_A + SEGMENT_SKIP_RECENT on the same edge set; boundary leg reuses BOUNDARY_MARGIN_A; pickup leg uses SWEEP_PICKUP_CLEARANCE_A (2.5, atom-level) over pickup['atoms'] — PICKUP_RADIUS_A (3.0) is 02-10's CAPTURE radius and is deliberately NOT the sweep clearance"
      pattern: "_point_segment_distance_sq|_point_segment_distance"
    - from: "serpentrum/game_engine.py step() sweep branch"
      to: "TURN_DEGREES / TURN_TICKS (defined by this plan — 02-06's trimmed movement scope no longer carries sweep constants)"
      via: "one tick-share of TURN_DEGREES/TURN_TICKS = 15 deg per tick; ('turning', tick/total) events"
      pattern: "TURN_TICKS"
    - from: "tests/test_engine_turns.py"
      to: "serpentrum.game_engine.GameEngine"
      via: "direct segment/head/heading state injection + exact rotated-position assertions"
      pattern: "GameEngine"
---

<objective>
Implement GAME-10's rigid chain pivot: a turn rotates the entire chain as a rigid body about the head, animated over TURN_TICKS ticks, REFUSED (with zero state change) when any sampled pose of the sweep would hit the boundary, overlap the body, or swing a chain atom into a live pickup — with 180-degree enforcement evaluated at sweep level, one pending-application timing rule, and turn state that survives resets cleanly.

Purpose: The 02-06 movement core queues directions (pending inert); 02-10 made the engine a collision referee. GAME-10 (user-approved, locked) demands the third referee power: "a turn whose sweep would hit the boundary or body is refused; 180-degree reversal is forbidden" — while pairwise stacking transforms stay frozen ("stacking geometry immutable at all times"). Rigid rotation about the head is the only turn model compatible with that invariant (grid steps would tear the chain; per-segment steering would break the frozen stacking geometry).

Output: extended serpentrum/game_engine.py + tests/test_engine_turns.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@serpentrum/game_engine.py
@tests/test_engine_core.py
@tests/test_engine_rules.py

Wave/dependency note: 02-06 (wave 1) delivered the MOVEMENT-ONLY core (head/heading/segments state, request_direction with 180-degree rejection vs the CURRENT heading + max-1 buffer, step(dt) constant-speed forward movement emitting ('moved', (x, y)) with pending deliberately INERT, pause/resume/reset — NO collision/capture/sweep machinery). 02-10 (wave 2) delivered boundary + body collision (head-centroid vs polyline edges built from segment centroids), pickup capture/attach/reject (records carry 'atoms'; ('refused', pickup_id, reason) returns), counters, finished/result, and the helpers/constants this plan reuses (_point_segment_distance_sq, BOUNDARY_MARGIN_A, BODY_COLLISION_RADIUS_A, SEGMENT_SKIP_RECENT, PICKUP_RADIUS_A, SWEEP_PICKUP_CLEARANCE_A). READ serpentrum/game_engine.py and BOTH existing engine test files FIRST. Extend the file; keep test_engine_core.py (movement) AND test_engine_rules.py (collisions/rules) green (both are owned by prior plans — never edit them; if a prior assertion genuinely contradicts this contract, STOP and report a dependency violation instead of editing).

Load-bearing verified facts / locked decisions:
- GAME-10 (locked 2026-09-06): rigid chain pivot turns; turn refused on boundary/body sweep collision; no 180-degree reversal. Research §7 spec: "sample K=TURN_TICKS+1 intermediate angles of the WHOLE chain rotated rigidly about the head... run boundary + body + (remaining pickups stay put — only chain moves) checks at every sample. Any hit -> return refusal event, state unchanged."
- Constants: TURN_DEGREES = 90.0, TURN_TICKS = 6 — DEFINED BY THIS PLAN (02-06's trimmed movement scope defines only SPEED_A_PER_S + DIRS; 02-10's constants are collision/capture only). Sweep tick share = TURN_DEGREES / TURN_TICKS = 15 deg. Pre-check samples K = TURN_TICKS + 1 = 7 poses (k = 0..6, inclusive of both ends).
- Rotation math (CCW-positive, about the head): dx = px - hx; dy = py - hy; x' = hx + dx*cos(th) - dy*sin(th); y' = hy + dx*sin(th) + dy*cos(th). Atoms are (sym, x, y, z) 4-tuples: rotate x, y; KEEP sym and z (rotation is about the vertical axis through the head — the 2D game plane is xy, z is display-only). Verify: point (3, 0) about origin by +90 deg -> (0, 3).
- Sweep direction sign: cross(start, target) = sx*ty - sy*tx; > 0 -> CCW (+90 deg), < 0 -> CW (-90 deg). East (1,0) -> up (0,1): cross = +1 -> CCW. Direction map (02-06's DIRS): left (-1,0), right (1,0), up (0,1), down (0,-1).
- Why the sweep pauses forward motion: research step() spec — the sweeping branch replaces the head-advance branch ("sweeping: advance sweep by one tick-share...; else: head += heading * SPEED * dt").
- Why the body leg is in the pre-check despite rigid invariance: rotation about the head preserves point-to-point distances to the pivot, so head-vs-SEGMENT-POINT checks are pose-invariant — but head-vs-POLYLINE-EDGE (chord) distances are NOT (a chord between two rotated centroids can pass nearer the head than either endpoint distance suggests), and the leg doubles as a defensive guard if the running state was already overlapping. Implement it; document the nuance in the module docstring. The body leg uses the SAME model as 02-10's forward check (rotated polyline edges, same edge set i in range(0, n - 1 - SEGMENT_SKIP_RECENT), same _point_segment_distance_sq helper, same BODY_COLLISION_RADIUS_A, STRICT <) — one body-collision model across the whole engine.
- Pickup leg rationale (contract, atom-level — the revision-pinned model): pickups stay put; only the chain moves. Refuse when any rotated CHAIN ATOM passes within SWEEP_PICKUP_CLEARANCE_A (2.5 A) of any LIVE pickup ATOM (distance < 2.5, STRICT) — the SAME threshold 02-04's check_clash uses for inter-fragment contacts: a swept chain atom that would sit within 2.5 A of a pickup atom is exactly the geometry stacking would reject, so the turn is refused pre-emptively. This leg consumes pickup['atoms'] (02-10's records carry it). The HEAD is excluded from this leg: it is the pivot (its position is sweep-invariant) and head-pickup proximity is 02-10's capture mechanic, not a collision. PICKUP_RADIUS_A (3.0) is the centroid-capture radius — deliberately NOT the sweep clearance. Centroid-level pickup clearance was the rejected alternative (too coarse: a molecule's atoms, not its centroid, are what clash).
- Boundary leg (contract): any rotated segment CENTROID outside the BOUNDARY_MARGIN_A-adjusted box [x0 + BOUNDARY_MARGIN_A, x1 - BOUNDARY_MARGIN_A] x [y0 + BOUNDARY_MARGIN_A, y1 - BOUNDARY_MARGIN_A] (inclusive bounds reversed: outside means strictly beyond) -> refuse 'boundary'. Atoms rotate rigidly with their centroids; centroid-level checking at 7 samples is the v1 sampled approximation (Phase 5 refinement adds samples/atom checks if playtesting demands). Only applies when box_min/box_max are set (02-10's fields).
- Pending-application timing (ONE rule, the revision-pinned choice): pending is applied ONLY at the START of a step while not sweeping. Pop the front request: perpendicular to the current heading -> attempt start_sweep(front) — on success THIS tick is sweep tick 1 (no forward move); on refusal emit ('turn_refused', reason), CONSUME the request, and fall through to the forward move in that same tick. Same-direction request -> dropped silently, fall through to forward movement. (180-degree requests never reach pending — request_direction filters them.) Sweep COMPLETION (tick == TURN_TICKS) sets heading = target and clears sweeping but does NOT touch pending — a chained turn is attempted at the NEXT step's start, by the same rule. This is 02-06's movement timing carried through unchanged; no second application point exists.
- Sweep-level 180-degree rule: while sweeping, judge a request against sweep.target_heading (the heading that will be in effect when the request applies) — dot(d_unit, target) < -0.5 -> ignored; same-as-target -> ignored; else buffer (max 1, newest wins — an INTENTIONAL in-sweep override of 02-06's first-kept static rule; do not unify the two policies). Against the static heading (not sweeping) 02-06's original rule stands unchanged.
- Determinism: pure float math, fixed sample counts, no RNG/wall-clock; assertAlmostEqual for positions. Python 3.6.9 syntax (%-formatting); module stays PURE (stdlib math); tests zero sys.modules stubs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rigid rotation helper + TURN constants + start_sweep() with the 7-sample 3-leg pre-check</name>
  <files>serpentrum/game_engine.py, tests/test_engine_turns.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Constants TURN_DEGREES = 90.0 and TURN_TICKS = 6 (module-level, documented: this plan owns them — sweep animation polish is Phase 5).
2. Pure helper:
```python
def _rotate_xy(x, y, cx, cy, cos_t, sin_t):
    """Rotate (x, y) about (cx, cy) by the angle whose cos/sin are given (CCW positive)."""
```
3. Engine gains `sweeping` state: None or `{'start_heading', 'target_heading', 'angle_signed', 'total_ticks', 'tick'}` (02-06 reserved nothing for it — own the name now; 02-06's trimmed step() never touches it).
4. `start_sweep(self, direction)` (public; also the internal path used by step()): direction is one of the four DIRS strings. Compute target = DIRS[direction]; reject 180-degree/same cases exactly as request_direction does (dot vs current heading; keep 02-06's semantics for the static case). Compute angle_signed = +TURN_DEGREES if cross(heading, target) > 0 else -TURN_DEGREES. BEFORE any mutation, run the pre-check over K = TURN_TICKS + 1 sampled poses th_k = angle_signed * k / TURN_TICKS, k = 0..TURN_TICKS:
   - rotate EVERY segment (centroid + each atom's x, y; z and sym preserved) about the head by th_k (build the rotated pose in local variables — do NOT write it into engine state)
   - boundary leg (only if box set): any rotated CENTROID outside [x0 + BOUNDARY_MARGIN_A, x1 - BOUNDARY_MARGIN_A] x [y0 + BOUNDARY_MARGIN_A, y1 - BOUNDARY_MARGIN_A] -> refuse reason 'boundary'
   - body leg: head-centroid vs the rotated chain polyline edges (same edge set as 02-10: i in range(0, n - 1 - SEGMENT_SKIP_RECENT)) using _point_segment_distance_sq < BODY_COLLISION_RADIUS_A ** 2 -> refuse reason 'body' (defensive; see context nuance)
   - pickup leg (atom-level): any rotated CHAIN ATOM within SWEEP_PICKUP_CLEARANCE_A (2.5, STRICT <) of any LIVE pickup ATOM -> refuse reason 'pickup' (head excluded — the pivot; pickup['atoms'] from 02-10's records)
   On any hit: emit ('turn_refused', reason), mutate NOTHING (heading, segments, sweeping, pending all unchanged), and return False. Else open the sweep state and return True.
5. request_direction (sweep-level 180-degree enforcement): while sweeping, evaluate the request against sweep['target_heading'] — same-as-target or dot < -0.5 -> ignored; otherwise buffer (pending max 1, newest wins). While NOT sweeping, 02-06's original behavior stands.

Create tests/test_engine_turns.py (sys.path self-insert; construct like test_engine_core.py; set state directly). Tests (SHOW the arithmetic in comments for every exact-position assertion):
- rotation math sanity via the helper's contract: rotate (3, 0) about origin by +90 deg -> (0, 3); by -90 deg -> (0, -3); an atom (4, 1, 2.0) (x, y, z) rotates to (-1, 4) with z exactly 2.0 preserved
- boundary refusal (centroid leg): box ((-2.0, -2.0), (20.0, 2.0)); head (4.0, 0.0) heading (1, 0); segment centroids (7.0, 0.0), (10.0, 0.0). Request 'up' (CCW +90 deg): centroid (10.0, 0.0) sits at offset (6.0, 0.0) from the head -> rotated +90 deg -> offset (0.0, 6.0) -> position (4.0, 10.0), and 10.0 > y1 - BOUNDARY_MARGIN_A = 2.0 - 1.0 = 1.0 -> refuse 'boundary'. Assert ('turn_refused', 'boundary') emitted; heading STILL (1.0, 0.0); centroids STILL (7.0, 0.0) / (10.0, 0.0); sweeping still None; pending empty
- body refusal (defensive leg; call start_sweep directly on an injected state — real gameplay would have crashed on the prior step, this proves the leg is wired): head (4.0, 0.0) heading (1, 0); segments c0=(-2.0, 1.0), c1=(2.5, 1.0), c2=(3.5, 0.0), c3=(4.0, -0.5). n=4 -> only edge 0 (c0 -> c1) is checked; head (4.0, 0.0) to edge 0: nearest point is the clamped endpoint (2.5, 1.0), distance sqrt(1.5^2 + 1.0^2) = 1.80 < 2.0 -> the CURRENT pose (sample k=0) already violates -> refusal fires with reason 'body', state unchanged
- pickup refusal (ATOM-level leg, arithmetic shown): head (0, 0) heading (1, 0); segment m1 {'molecule_id': 'm1', 'centroid': (3.0, 0.0), 'atoms': [('C', 3.0, 0.0, 0.0)], 'atoms_n': 1}; live pickup p1 {'id': 'p1', 'centroid': (0.0, 3.0), 'atoms': [('O', 0.0, 3.0, 0.0)], 'atoms_n': 1}. Request 'up' (CCW): the chain atom (3, 0) rotates on the radius-3 circle about the origin; at sample k=3 (theta=45 deg) it sits at (3*cos45, 3*sin45) = (2.1213, 2.1213); distance to the pickup atom (0, 3) = sqrt(2.1213^2 + 0.8787^2) = sqrt(4.5 + 0.7721) = 2.296 < 2.5 -> refuse 'pickup'. Assert ('turn_refused', 'pickup'), state unchanged. Positive control: same geometry, pickup atom at (12.0, 0.0, 0.0) -> min distance from the rotating atom (radius-3 circle) to (12, 0) is 12 - 3 = 9 > 2.5 -> start_sweep returns True and the sweep opens
- head exclusion (pivot not a chain atom): live pickup with its atom EXACTLY at the head/pivot (0.0, 0.0, 0.0) (centroid (0.0, 0.0)); chain atom (3, 0, 0) rotates on the radius-3 circle — its distance to the origin is 3.0 at EVERY pose > 2.5 -> the sweep OPENS (start_sweep returns True). This proves the head/pivot point itself is not treated as a checked chain atom (a head-level check would refuse at distance 0 every pose)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_turns -v` → Task 1 tests OK; `python3.6 -m unittest tests.test_engine_core -v` and `tests.test_engine_rules -v` → still OK</verify>
  <done>start_sweep refuses on boundary/body/pickup with reason-tagged events and ZERO state mutation; the pickup leg is atom-level at 2.5 A (matching 02-04's clash threshold) over pickup['atoms']; rotation helper exact (including z preservation); sweep-level request semantics in place; prior engine suites green</done>
</task>

<task type="auto">
  <name>Task 2: Sweep progression in step() + pending applied at next-step start + epoch-safe state</name>
  <files>serpentrum/game_engine.py, tests/test_engine_turns.py</files>
  <action>
Extend step(dt) in serpentrum/game_engine.py — ONE pending-application rule (see context: applied at the START of a step while not sweeping; sweep completion never touches pending):

1. If finished -> no-op [] (existing 02-10 behavior).
2. If sweeping: advance one tick: tick += 1; th = angle_signed * tick / total_ticks; rotate the whole chain (centroids + atoms, x/y only) about the head to th and WRITE the rotated poses into engine state; emit ('turning', tick / float(total_ticks)). NO forward motion and no ('moved',) event during sweeps. If tick == total_ticks: heading = target_heading; sweeping = None. (Pending is NOT applied here — the next step's start applies it, per the pinned timing.)
3. If not sweeping and pending non-empty (step START): pop the front request d. If d is perpendicular to the current heading: attempt start_sweep(d) — on success THIS tick is sweep tick 1 (rotate 15 deg, emit ('turning', 1/6), no 'moved'); on refusal ('turn_refused', reason) is emitted, the request is CONSUMED, and the tick FALLS THROUGH to normal forward movement. If d is same-direction as the current heading: drop it silently and fall through to forward movement. (The turn-attempt runs before the forward branch, so a successful turn tick never also moves the head.)
4. Else: the existing 02-06/02-10 forward-movement + collision + pickup behavior, unchanged.
5. reset(): clear sweeping and pending (epoch-safety — no stale turn state survives a restart), alongside 02-06's/02-10's existing resets.

Tests to append to tests/test_engine_turns.py:
- rotation exactness over a full sweep: head (0,0) heading (1,0); segments (3,0), (6,0), (9,0) with segment 0 carrying an atom at (4.0, 1.0, 2.0). request 'up'; call step(0.1) 6 times. Events: ('turning', 1/6) .. ('turning', 6/6) (assert the 6 turning events in order, no 'moved' events). After: heading == (0.0, 1.0) (assertAlmostEqual each component); centroids (3,0)->(0,3), (6,0)->(0,6), (9,0)->(0,9) at 1e-9; the atom at (-1.0, 4.0) with z == 2.0 exactly
- mid-sweep pose: fresh engine, same start; after 3 steps assert segment (3,0) centroid at (3*cos45, 3*sin45) = (2.1213203436, 2.1213203436) (assertAlmostEqual places=6) and a ('turning', 0.5) event was emitted on tick 3
- forward motion paused: during the 6 sweep ticks the head does NOT move (head stays (0,0) — it is the pivot anyway; assert no 'moved' events and that a segment 8 A away traverses the arc instead)
- CW turn: heading (1,0), request 'down' -> after 6 ticks segment (3,0) is at (0,-3); heading (0,-1)
- sweep-level 180: heading (1,0); request 'up' (sweep opens); during the sweep request 'down' -> ignored (pending stays empty; after completion no chained sweep, heading (0,1)); separately: request 'up' then during the sweep request 'left' -> buffered; after sweep 1 completes (tick 6, heading (0,1), pending still ['left']), the NEXT step(0.1) opens sweep 2 at its START (first event ('turning', 1/6), no 'moved' on that tick); after 6 more ticks heading (-1,0); original segment (3,0) now at (-3.0, 0.0) (net 180 deg)
- newest-wins during a sweep: buffering 'left' and then 'right' during a sweep leaves only 'right' pending (max 1) -> after completion the next step starts the chained CW sweep targeting (1,0) from (0,1)
- refused chained sweep (timing shown): geometry where the chained turn would exit the box -> after sweep 1 completes, the NEXT step emits ('turn_refused', reason) AND ('moved', ...) in that same step (the refusal consumed the request; forward motion continues on the new heading); the step after that emits only 'moved' events (the request is not retried — pending applied exactly once)
- pending applied exactly once: after a chained attempt (open or refuse), pending is empty; no later step re-attempts it
- epoch safety: start a sweep, call reset() mid-sweep -> sweeping is None, pending empty, and the following step() does normal forward movement (no 'turning' events); also request a direction, reset(), then step() several times -> no turn ever opens from the stale request
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_turns -v` → all OK; `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → core + rules + turns all OK; `python3.6 tests/run_gates.py` → exit 0</verify>
  <done>Sweeps rotate the chain rigidly and exactly (90 deg in 6 ticks, positions to 1e-9, z preserved), pause forward motion, refuse cleanly via the 3-leg pre-check, enforce 180 at sweep level, apply pending exactly once at the next step's start (refusals fall through to forward motion, never retried), and leave no stale turn state after reset; full engine trio + gates green</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_engine_turns -v` → OK (rotation exactness, 3-leg refusals, sweep-level 180, chaining, epoch safety)
- `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → OK (02-06 + 02-10 suites untouched and green)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK
- `python3.6 tests/run_gates.py` → exit 0 (game_engine.py stays PURE stdlib-only)
- `grep -rn "sys.modules\\[" tests/` → no matches
</verification>

<success_criteria>
- GAME-10 implemented pure: rigid pivot sweeps over TURN_TICKS=6 ticks with exact rotated geometry (1e-9), stacked-molecule transforms frozen (atoms rotate WITH their segment — pairwise stacking geometry immutable)
- Swept-region refusal covers boundary (centroids vs BOUNDARY_MARGIN_A-adjusted box) + body (rotated polyline edges, 02-10's exact model) + pickups (chain ATOMS vs pickup ATOMS < 2.5 A, 02-04's clash threshold) at all 7 sampled poses with zero state mutation and reason-tagged ('turn_refused', reason) events
- No 180-degree reversal at sweep level (judged against the sweep target); pending applies exactly once at the start of the step after completion; reset wipes turn state
- Deterministic pure floats; prior engine tests unmodified and green; purity + full gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-13-SUMMARY.md`
</output>
