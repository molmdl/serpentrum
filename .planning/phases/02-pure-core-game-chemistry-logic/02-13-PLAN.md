---
phase: 02-pure-core-game-chemistry-logic
plan: 13
type: execute
wave: 3
depends_on: ["02-06", "02-10"]
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_turns.py
autonomous: true

must_haves:
  truths:
    - "A turn request rotates the WHOLE chain rigidly about the head over exactly TURN_TICKS=6 ticks (15 deg/tick): after completion every segment centroid sits at its pre-turn position rotated exactly +/-90 deg about the head (assertAlmostEqual 1e-9), atoms rotate with their segment (x, y rotate; z preserved), and heading == target"
    - "A turn whose swept arc would leave the boundary margin, overlap a checked chain edge, or swing a segment centroid within PICKUP_RADIUS_A of a live pickup is REFUSED: ('turn_refused', reason) emitted, zero state change (heading, segments, sweeping, pending all unchanged)"
    - "180 deg is enforced at SWEEP level: requests are judged against the sweep's TARGET heading while sweeping, so 'down' during an east->up sweep is ignored while 'left' is buffered and chains into a second sweep after completion"
    - "Sweep ticks pause forward motion (no 'moved' events), emit ('turning', tick/TURN_TICKS) each tick, and apply the pending direction exactly once at completion"
    - "reset() wipes all turn state (sweeping, pending) — no stale turn survives a restart; no crash can occur mid-sweep because the 7-sample pre-check covers every swept pose"
  artifacts:
    - path: "serpentrum/game_engine.py"
      provides: "rigid-pivot sweep machinery: _rotate_xy helper, start_sweep() with the 7-sample swept-region pre-check (boundary + body + pickups), sweep progression in step(), pending application at sweep end"
      contains: "start_sweep"
    - path: "tests/test_engine_turns.py"
      provides: "rotation-exactness, refusal, sweep-level 180-degree, chained-turn, and reset-epoch tests"
      min_lines: 150
  key_links:
    - from: "serpentrum/game_engine.py start_sweep"
      to: "02-10's collision helpers and constants"
      via: "reuses _point_segment_distance_sq, BOUNDARY_MARGIN_A, BODY_COLLISION_RADIUS_A, SEGMENT_SKIP_RECENT, PICKUP_RADIUS_A at every sampled pose"
      pattern: "_point_segment_distance_sq|_point_segment_distance"
    - from: "serpentrum/game_engine.py step() sweep branch"
      to: "TURN_DEGREES / TURN_TICKS"
      via: "one tick-share of TURN_DEGREES/TURN_TICKS = 15 deg per tick; ('turning', tick/total) events"
      pattern: "TURN_TICKS"
    - from: "tests/test_engine_turns.py"
      to: "serpentrum.game_engine.GameEngine"
      via: "direct segment/head/heading state injection + exact rotated-position assertions"
      pattern: "GameEngine"
---

<objective>
Implement GAME-10's rigid chain pivot: a turn rotates the entire chain as a rigid body about the head, animated over TURN_TICKS ticks, REFUSED (with zero state change) when any sampled pose of the sweep would hit the boundary, overlap the body, or sweep a segment over a live pickup — with 180-degree enforcement evaluated at sweep level and turn state that survives resets cleanly.

Purpose: The 02-06 core queues directions; 02-10 made the engine a collision referee. GAME-10 (user-approved, locked) demands the third referee power: "a turn whose sweep would hit the boundary or body is refused; 180-degree reversal is forbidden" — while pairwise stacking transforms stay frozen ("stacking geometry immutable at all times"). Rigid rotation about the head is the only turn model compatible with that invariant (grid steps would tear the chain; per-segment steering would break the frozen stacking geometry).

Output: extended serpentrum/game_engine.py + tests/test_engine_turns.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/02-pure-core-game-chemistry-logic/02-RESEARCH-pure-core.md
@serpentrum/game_engine.py
@tests/test_engine_core.py
@tests/test_engine_rules.py

Wave/dependency note: 02-06 (wave 1) delivered the core (head/heading/segments/pending/request_direction with 180-degree rejection vs the static heading/step forward movement/events); 02-10 (wave 2) delivered boundary + body collision, pickup capture, counters, and the helpers/constants this plan reuses (_point_segment_distance_sq, BOUNDARY_MARGIN_A, BODY_COLLISION_RADIUS_A, SEGMENT_SKIP_RECENT, PICKUP_RADIUS_A). READ serpentrum/game_engine.py and BOTH existing engine test files FIRST. Extend the file; keep test_engine_core.py AND test_engine_rules.py green (both are owned by prior plans — never edit them; if a core assertion genuinely contradicts this contract, STOP and report a dependency violation instead of editing).

Load-bearing verified facts / locked decisions:
- GAME-10 (locked 2026-09-06): rigid chain pivot turns; turn refused on boundary/body sweep collision; no 180-degree reversal. Research §7 spec: "sample K=TURN_TICKS+1 intermediate angles of the WHOLE chain rotated rigidly about the head... run boundary + body + (remaining pickups stay put — only chain moves) checks at every sample. Any hit -> return refusal event, state unchanged."
- Constants: TURN_DEGREES = 90.0, TURN_TICKS = 6 (02-06 defines them; reuse). Sweep tick share = TURN_DEGREES / TURN_TICKS = 15 deg. Pre-check samples K = TURN_TICKS + 1 = 7 poses (k = 0..6, inclusive of both ends).
- Rotation math (CCW-positive, about the head): dx = px - hx; dy = py - hy; x' = hx + dx*cos(th) - dy*sin(th); y' = hy + dx*sin(th) + dy*cos(th). Atoms are (sym, x, y, z) 4-tuples: rotate x, y; KEEP sym and z (rotation is about the vertical axis through the head — the 2D game plane is xy, z is display-only). Verify: point (3, 0) about origin by +90 deg -> (0, 3).
- Sweep direction sign: cross(start, target) = sx*ty - sy*tx; > 0 -> CCW (+90 deg), < 0 -> CW (-90 deg). East (1,0) -> up (0,1): cross = +1 -> CCW. Direction map (02-06's strings): left (-1,0), right (1,0), up (0,1), down (0,-1).
- Why the sweep pauses forward motion: research step() spec — the sweeping branch replaces the head-advance branch ("sweeping: advance sweep by one tick-share...; else: head += heading * SPEED * dt").
- Why the body leg is in the pre-check despite rigid invariance: rotation about the head preserves point-to-point distances to the pivot, so head-vs-SEGMENT-POINT checks are pose-invariant — but head-vs-POLYLINE-EDGE (chord) distances are NOT (a chord between two rotated centroids can pass nearer the head than either endpoint distance suggests), and the leg doubles as a defensive guard if the running state was already overlapping. Implement it; document the nuance in the module docstring.
- Pickup leg rationale (contract): pickups stay put; only the chain moves. A segment centroid sweeping within PICKUP_RADIUS_A (3.0) of a LIVE pickup would have a body part capture/overlap the pickup — refused. The head is excluded from this leg (head-pickup distance is sweep-invariant; head proximity is 02-10's capture mechanic, not a collision).
- Sweep-level 180-degree rule: while sweeping, judge a request against sweep.target_heading (the heading that will be in effect when the request applies) — dot(d_unit, target) < -0.5 -> ignored; same-as-target -> ignored; else buffer (max 1, newest wins, per 02-06's pending semantics). Against the static heading (not sweeping) 02-06's original rule stands unchanged.
- Determinism: pure float math, fixed sample counts, no RNG/wall-clock; assertAlmostEqual for positions. Python 3.6.9 syntax (%-formatting); module stays PURE (stdlib math); tests zero sys.modules stubs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rigid rotation helper + start_sweep() with the 7-sample swept-region pre-check</name>
  <files>serpentrum/game_engine.py, tests/test_engine_turns.py</files>
  <action>
Extend serpentrum/game_engine.py:

1. Pure helper:
```python
def _rotate_xy(x, y, cx, cy, cos_t, sin_t):
    """Rotate (x, y) about (cx, cy) by the angle whose cos/sin are given (CCW positive)."""
```
2. Engine gains `sweeping` state: None or `{'start_heading', 'target_heading', 'angle_signed', 'total_ticks', 'tick'}` (02-06 reserved the name; own it now). Also a module-level DIRECTIONS map if 02-06 did not already define one: {'left': (-1.0, 0.0), 'right': (1.0, 0.0), 'up': (0.0, 1.0), 'down': (0.0, -1.0)}.
3. `start_sweep(self, direction)` (public; also the internal path used by step()): direction is one of the four strings. Compute target = DIRECTIONS[direction]; reject 180-degree/same cases exactly as request_direction does (dot vs current heading; keep 02-06's semantics for the static case). Compute angle_signed = +TURN_DEGREES if cross(heading, target) > 0 else -TURN_DEGREES. BEFORE any mutation, run the pre-check over K = TURN_TICKS + 1 sampled poses th_k = angle_signed * k / TURN_TICKS, k = 0..TURN_TICKS:
   - rotate EVERY segment (centroid + each atom's x, y; z and sym preserved) about the head by th_k (build the rotated pose in local variables — do NOT write it into engine state)
   - boundary leg (only if box set): any rotated centroid outside [x0 + BOUNDARY_MARGIN_A, x1 - BOUNDARY_MARGIN_A] x [y0 + BOUNDARY_MARGIN_A, y1 - BOUNDARY_MARGIN_A] -> refuse reason 'boundary'
   - body leg: head-centroid vs the rotated chain polyline edges (same edge set as 02-10: i in range(0, n - 1 - SEGMENT_SKIP_RECENT)) using _point_segment_distance_sq < BODY_COLLISION_RADIUS_A ** 2 -> refuse reason 'body' (defensive; see context nuance)
   - pickup leg: any rotated SEGMENT centroid within PICKUP_RADIUS_A of a live pickup (distance <= PICKUP_RADIUS_A; head excluded) -> refuse reason 'pickup'
   On any hit: emit ('turn_refused', reason), mutate NOTHING (heading, segments, sweeping, pending all unchanged), and return False. Else open the sweep state and return True.
4. request_direction (sweep-level 180-degree enforcement): while sweeping, evaluate the request against sweep['target_heading'] — same-as-target or dot < -0.5 -> ignored; otherwise buffer (pending max 1, newest wins). While NOT sweeping, 02-06's original behavior stands. A queued same-direction-as-current request is discarded when popped (no-op).

Create tests/test_engine_turns.py (sys.path self-insert; construct like test_engine_core.py; set state directly). Tests:
- rotation math sanity via a completed sweep (Task 2 runs it, but assert here on the helper's contract): rotate (3, 0) about origin by +90 deg -> (0, 3); by -90 deg -> (0, -3); an atom (4, 1, 2.0) (x, y, z) rotates to (-1, 4) with z exactly 2.0 preserved
- boundary refusal: box ((-2.0, -2.0), (20.0, 2.0)); head (4.0, 0.0) heading (1, 0); segment centroids (7.0, 0.0), (10.0, 0.0). Request 'up' (CCW +90 deg): centroid (10.0, 0.0) sits at offset (6.0, 0.0) from the head -> rotated +90 deg -> offset (0.0, 6.0) -> position (4.0, 10.0), and 10.0 > y1 - BOUNDARY_MARGIN_A = 2.0 - 1.0 = 1.0 -> refuse 'boundary'. Assert ('turn_refused', 'boundary') emitted; heading STILL (1.0, 0.0); centroids STILL (7.0, 0.0) / (10.0, 0.0); sweeping still None; pending empty
- body refusal (defensive leg; call start_sweep directly on an injected state — real gameplay would have crashed on the prior step, this proves the leg is wired): head (4.0, 0.0) heading (1, 0); segments c0=(-2.0, 1.0), c1=(2.5, 1.0), c2=(3.5, 0.0), c3=(4.0, -0.5). n=4 -> only edge 0 (c0 -> c1) is checked; head (4.0, 0.0) to edge 0: nearest point is the clamped endpoint (2.5, 1.0), distance sqrt(1.5^2 + 1.0^2) = 1.80 < 2.0 -> the CURRENT pose (sample k=0) already violates -> refusal fires with reason 'body', state unchanged
- pickup refusal: no box (or wide box); head (0, 0) heading (1, 0); segment centroid (0, 6.0) (offset (0, 6) from head; rotated -90 deg -> (6.0, 0.0)); live pickup at (6.5, 0.0) — distance 0.5 <= 3.0 -> refuse 'pickup'. Also positive control: same geometry, pickup at (12.0, 0.0) -> min swept distance 5.5 > 3.0 -> sweep opens
- head-exclusion: a live pickup near the HEAD's own position does not refuse the sweep (head is the pivot; its pickup distance is invariant): pickup at (0.5, 0.0) with head (0,0) -> sweep still opens (assert start_sweep returns True)
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_turns -v` → Task 1 tests OK; `python3.6 -m unittest tests.test_engine_core -v` and `tests.test_engine_rules -v` → still OK</verify>
  <done>start_sweep refuses on boundary/body/pickup with reason-tagged events and ZERO state mutation; rotation helper exact (including z preservation); sweep-level request semantics in place; prior engine suites green</done>
</task>

<task type="auto">
  <name>Task 2: Sweep progression in step() + pending application at sweep end + epoch-safe state</name>
  <files>serpentrum/game_engine.py, tests/test_engine_turns.py</files>
  <action>
Extend step(dt) in serpentrum/game_engine.py:

1. If finished -> no-op [] (existing 02-10 behavior).
2. If sweeping: advance one tick: tick += 1; th = angle_signed * tick / total_ticks; rotate the whole chain (centroids + atoms, x/y only) about the head to th and WRITE the rotated poses into engine state; emit ('turning', tick / float(total_ticks)). NO forward motion and no ('moved',) event during sweeps. If tick == total_ticks: heading = target_heading; sweeping = None; then apply pending exactly once: if pending non-empty, pop the front request d — if d is perpendicular to the new heading, attempt start_sweep(d) (a chained sweep; if it refuses, ('turn_refused', reason) is emitted and d is dropped — the snake continues on the new heading); same-direction or 180-degree requests are dropped silently at this point (they were already filtered at request time).
3. If not sweeping and pending has a perpendicular front request: attempt start_sweep(front) — if it opens, THIS tick is sweep tick 1 (rotate 15 deg, emit ('turning', 1/6)); if refused, the request is dropped (already handled by Task 1's semantics) and the tick falls through to normal forward movement. (The turn-attempt runs before the forward branch, so a turn tick never also moves the head.)
4. Else: the existing 02-06/02-10 forward-movement + collision + pickup behavior, unchanged.
5. reset(): clear sweeping and pending (epoch-safety — no stale turn state survives a restart), alongside 02-06's/02-10's existing resets.

Tests to append to tests/test_engine_turns.py:
- rotation exactness over a full sweep: head (0,0) heading (1,0); segments (3,0), (6,0), (9,0) with segment 0 carrying an atom at (4.0, 1.0, 2.0). request 'up'; call step(0.1) 6 times. Events: ('turning', 1/6) .. ('turning', 6/6) (assert the 6 turning events in order, no 'moved' events). After: heading == (0.0, 1.0) (assertAlmostEqual each component); centroids (3,0)->(0,3), (6,0)->(0,6), (9,0)->(0,9) at 1e-9; the atom at (-1.0, 4.0) with z == 2.0 exactly
- mid-sweep pose: fresh engine, same start; after 3 steps assert segment (3,0) centroid at (3*cos45, 3*sin45) = (2.1213203436, 2.1213203436) (assertAlmostEqual places=6) and a ('turning', 0.5) event was emitted on tick 3
- forward motion paused: during the 6 sweep ticks the head does NOT move (head stays (0,0) — it is the pivot anyway; assert no 'moved' events and that a segment 8 A away traverses the arc instead)
- CW turn: heading (1,0), request 'down' -> after 6 ticks segment (3,0) is at (0,-3); heading (0,-1)
- sweep-level 180: heading (1,0); request 'up' (sweep opens); during the sweep request 'down' -> ignored (pending stays empty; after completion no chained sweep, heading (0,1)); separately: request 'up' then during the sweep request 'left' -> buffered; after completion a SECOND sweep runs (6 more ticks) ending heading (-1,0); original segment (3,0) now at (-3.0, 0.0) (net 180 deg)
- pending applied exactly once: during a sweep, buffering 'left' and then 'right' leaves only 'right' pending (newest wins, max 1) -> after completion the chained sweep targets (1,0) from (0,1) (a CW sweep)
- refused chained sweep: geometry where the chained turn would exit the box -> after sweep 1 completes, ('turn_refused', reason) emitted and the snake continues moving on the new heading (next step produces a 'moved' event)
- epoch safety: start a sweep, call reset() mid-sweep -> sweeping is None, pending empty, and the following step() does normal forward movement (no 'turning' events); also request a direction, reset(), then step() several times -> no turn ever opens from the stale request
  </action>
  <verify>`python3.6 -m unittest tests.test_engine_turns -v` → all OK; `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → core + rules + turns all OK; `python3.6 tests/run_gates.py` → exit 0</verify>
  <done>Sweeps rotate the chain rigidly and exactly (90 deg in 6 ticks, positions to 1e-9, z preserved), pause forward motion, refuse cleanly via the pre-check, enforce 180 at sweep level, chain pending turns once, and leave no stale turn state after reset; full engine trio + gates green</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_engine_turns -v` → OK (rotation exactness, refusals, sweep-level 180, chaining, epoch safety)
- `python3.6 -m unittest discover -s tests -p "test_engine_*.py" -v` → OK (02-06 + 02-10 suites untouched and green)
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → full suite OK
- `python3.6 tests/run_gates.py` → exit 0 (game_engine.py stays PURE stdlib-only)
- `grep -rn "sys.modules\\[" tests/` → no matches
</verification>

<success_criteria>
- GAME-10 implemented pure: rigid pivot sweeps over TURN_TICKS=6 ticks with exact rotated geometry (1e-9), stacked-molecule transforms frozen (atoms rotate WITH their segment — pairwise stacking geometry immutable)
- Swept-region refusal covers boundary + body + pickups at all 7 sampled poses with zero state mutation and reason-tagged ('turn_refused', reason) events
- No 180-degree reversal at sweep level (judged against the sweep target); pending applies exactly once at sweep end; reset wipes turn state
- Deterministic pure floats; prior engine tests unmodified and green; purity + full gates green
</success_criteria>

<output>
After completion, create `.planning/phases/02-pure-core-game-chemistry-logic/02-13-SUMMARY.md`
</output>
