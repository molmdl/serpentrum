---
phase: 05-stacking-game-rules
plan: 16
type: execute
wave: 7
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08", "05-09", "05-10", "05-11", "05-12", "05-13", "05-14", "05-15"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All gates green: python3.6 tests/run_gates.py (full suite incl. the new Phase-5 test files) and --smoke (7/7 required smokes with flushed SMOKE-OK sentinels)"
    - "Live Windows PyMOL gameplay confirms all five phase success criteria: dataset-exact stacking with full info content, rigid refused-or-not sweeps, complete-snake crashes, win at cap, completion handoff with Get Spectra"
    - "Skip and refuse paths demo live: an uploaded molecule is skipped with its reason; biphenyl exercises the clash-refuse path"
  artifacts:
    - path: "tests/run_gates.py"
      provides: "The full gate pass (default + --smoke) — executed here, never modified"
    - path: "05-16 checkpoint:human-verify (Task 2, this plan)"
      provides: "The 8-step live-PyMOL APPROVED verdict — the phase's blocking human gate"
  key_links:
    - from: "tests/run_gates.py --smoke"
      to: "smoke/ SMOKE-OK sentinels"
      via: "verdict = flushed sentinels, never exit codes"
      pattern: "SMOKE-OK"
---

<objective>
Phase-closing verification: run every gate (including the two new REQUIRED smokes), then ONE consolidated human-verify checkpoint in real Windows PyMOL covering the full Phase-5 success-criteria list. GUI/visual verdicts cannot be automated (01-05: offscreen Qt is a dead end — never retried); this is the phase's single blocking human gate.

Purpose: prove the complete v1 game before Phase 6/7 consume the snake.
Output: gate evidence + human APPROVED checkpoint (or a diagnosed fix list).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 5 success criteria — the checklist below mirrors them 1:1)
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (what to look for: edge-on rings, 3.6 Å stacks)
@serpentrum/data/manifest.json (set_a order: benzene, naphthalene, anthracene, phenanthrene, biphenyl)
</context>

<tasks>

<task type="auto">
  <name>Task 1: full gate pass</name>
  <files>none — diagnosis-only; may patch gate-owned files found broken</files>
  <action>
Run from the repo root in WSL:
1. `python3.6 tests/run_gates.py` — must be green: syntax walk + plugin-path safety + AST purity + full unittest discovery (Phase-2 pins, Phase-5 new files: test_ring_cycle, test_orientation, test_spawn, test_engine_win_desync, test_placement, test_setloader_stack_ring, test_hud_content, test_phase5_integration).
2. `python3.6 tests/run_gates.py --smoke` — verdict = flushed SMOKE-OK sentinels (NEVER exit codes): 01 SKELETON, 03 VIEWER-BRIDGE, 04 DEMO-E2E, 05 LOOP-CAMERA, 06 INPUT-WIZARD, 07 TRANSFORM/SWEEP/COMPLETION, 08 EDGEON/PLACE360/PICKUPS.
If anything fails: diagnose, fix in the owning file (conventional commit `fix(05): ...`), re-run. Do not proceed to the checkpoint with red gates.
  </action>
  <verify>Both commands exit with all gates green; smoke output contains all 7 required smokes' sentinels</verify>
  <done>Machine-verifiable half of the phase is green end-to-end</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete v1 stacking game: edge-on molecules, deterministic pickups as sticks, dataset-cited π-stack placement (3.60 Å centroid-centroid @ 20°) with structured info blocks, rigid whole-chain turns with refusal pre-checks, boundary/body crashes that leave the snake complete, win at the molecule cap, reasoned skips (uploads) and clash refusals (biphenyl), and a completion flow that frames the snake, reveals length + score, and activates Get Spectra (switching to the placeholder Spectra tab).
  </what-built>
  <how-to-verify>
Install/refresh the plugin (dev plugin path points at the repo root), open serpentrum from the PyMOL menu, then in the Setup tab pick Demo Set A, default medium box, any head molecule, cap 10 -> Apply (head + box + ONE stick pickup materialize; the molecule rings are EDGE-ON — thin slabs facing you, NOT flat on the board) -> Start.

1. STACK + GEOMETRY [GAME-04, STACK-01, STACK-04]: steer into the stick pickup (head + arrow keys, click the 3D viewer first). The pickup jumps to the chain behind the head, parallel-displaced; the info box shows a block like `+ benzene: pi-pi stacking (parallel-displaced), 3.38 A plane gap (centroid 3.60 A @ 20.0 deg off-normal) - <dataset explanation> [Janiak 2000]`. The remaining-molecules label decrements.
2. RIGID TURN [GAME-10]: press a perpendicular arrow — the ENTIRE chain (segments + head) sweeps rigidly about the head over ~0.6 s (6 visible 15° steps), no wobble, no segment tearing. Aim a turn at the wall or back along the chain until you see `turn refused: boundary` (or body/pickup) — a refused turn must not move the chain. Try to reverse 180° (press the opposite arrow) — nothing happens (impossible by design).
3. CRASH [GAME-05]: drive into the boundary wall — `crashed into boundary`, movement stops, the stacked snake REMAINS complete on screen.
4. SKIP [STACK-03]: upload any small SDF (e.g. regenerate tmp/upload_test/accept_naphthalene.sdf per 03-08-SUMMARY, or any single-molecule SDF), Apply + Start; when its pickup is captured the info box shows `skipped <name>: no verified stacking entry for this molecule (no invented chemistry)` and the run continues.
5. REFUSE [STACK-05]: play set_a until the 5th species (biphenyl — the last in the cycle) is captured: it always refuses with a clash reason (shipped conformer's rings are at 90°; every biphenyl stack clashes). This is the designed refuse-path demonstrator — the pickup stays visible and the run continues.
6. WIN + COMPLETION [GAME-06, GAME-09]: keep stacking (molecules cycle; cap 10) until `YOU WIN` — the un-stacked pickups vanish, the camera frames the whole snake, the info box shows result/score (10 molecules stacked)/snake length/atoms, the per-interaction breakdown, and the `Get Spectra` button lights up. Click it — the dialog switches to the (placeholder) Spectra tab.
7. CRASH COMPLETION: start another run and crash intentionally — the same completion presentation appears (result: crashed), Get Spectra still activates.
8. HYGIENE: Restart mid-run rebuilds the same first-pickup scene deterministically; Cleanup (Setup tab) still removes only srp_* objects; no orphaned camera lock (mouse works after the run) and arrows don't step frames after the run ends.
Type "approved" (or list the failing step numbers + what you saw).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Gates: run_gates + run_gates --smoke fully green (machine half)
- Human: 8-step checklist APPROVED in live Windows PyMOL (human half)
- Any failed step -> diagnose -> fix(05) commit -> re-run the affected gate + the failed step only
</verification>

<success_criteria>
Phase 5 success criteria 1-5 each demonstrated live (steps 1-7 above) with green machine gates — the complete v1 game hands a complete snake to the spectra stage. [GAME-04/05/06/09/10, STACK-01/03/04/05]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-16-SUMMARY.md` (include the human verdict per step)
</output>
