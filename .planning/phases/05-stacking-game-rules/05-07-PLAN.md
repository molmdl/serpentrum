---
phase: 05-stacking-game-rules
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/pymol_bridge.py
  - smoke/07_transform_sweep_smoke.py
  - tests/run_gates.py
autonomous: true

must_haves:
  truths:
    - "apply_matrix transforms an object exactly per the verified TTT layout (probe-A1 regression, <=1.2e-07 A)"
    - "sweep_chain rotates srp_head + all srp_seg_* objects rigidly about an explicit head pivot with camera=0 (one cmd.rotate per tick; <5e-07 A distortion, sign = CCW positive)"
    - "Completion ops work headless: chain_object_names lists srp_head/srp_seg_*, zoom_chain frames them, delete_pickups pattern-deletes srp_pickup_*, rename_pickup renames"
  artifacts:
    - path: "serpentrum/pymol_bridge.py"
      provides: "apply_matrix, sweep_chain, zoom_chain, delete_pickups, rename_pickup, chain_object_names — thin cmd seams (G7 part 1)"
      contains: "def sweep_chain"
    - path: "smoke/07_transform_sweep_smoke.py"
      provides: "REQUIRED headless Windows-PyMOL smoke with SMOKE-OK TRANSFORM/SWEEP/COMPLETION sentinels"
    - path: "tests/run_gates.py"
      provides: "smoke 07 added to REQUIRED_SMOKES"
  key_links:
    - from: "serpentrum/pymol_bridge.py sweep_chain"
      to: "pymol cmd.rotate"
      via: "ONE call per ('turning',) tick, explicit origin= + camera=0 (P5-4 trap)"
      pattern: "cmd\\.rotate"
---

<objective>
Add the Phase-5 bridge primitives (G7 part 1) for placement, sweep and completion — every function a thin cmd forwarder (matrix math STAYS in the pure layer; the bridge never parses SDFs, per its docstring discipline). All mechanisms are researcher-VERIFIED — this plan wires them, it does not re-spike them.

Purpose: gui_game's stacked/turning/completion branches (plans 05-13/05-14/05-15) call exactly these seams.
Output: 6 bridge functions + REQUIRED headless smoke 07 replicating probes A1/B/C/G as regression.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (matrix verdict, Pattern 2 sweep-includes-head, Pattern 4 completion ordering, P5-2/P5-4/P5-5/P5-7 pitfalls, code examples)
@serpentrum/pymol_bridge.py (existing thin-seam style, BOX_DISPLAY_Z, cleanup_srp pattern semantics)
@tests/run_gates.py (REQUIRED_SMOKES tuple at :55)
@smoke/05_loop_camera_smoke.py (smoke style: sentinel-flushed, no exit-code reliance)
</context>

<tasks>

<task type="auto">
  <name>Task 1: bridge primitives</name>
  <files>serpentrum/pymol_bridge.py</files>
  <action>
Append a Phase-5 section to serpentrum/pymol_bridge.py (keep every function <= ~15 lines, python3.6, no prints/logging, BRIDGE class: pymol/pmg_tk allowed, PyQt5/numpy banned, no .exec_):

- `apply_matrix(name, m16)` — `cmd.transform_selection(name, m16)`; docstring pins the layout (y = R.(x+pre)+t; rows 0-2 = row-major R, col 3 = post-t, bottom row = pre-t; verified probe A1 1.2e-07 A; NEVER call cmd.get_object_ttt — segfaults, 04 probe M5).
- `sweep_chain(delta_deg, pivot_xy, selection='srp_head or srp_seg_*')` — ONE `cmd.rotate('z', float(delta_deg), selection, camera=0, origin=[pivot_xy[0], pivot_xy[1], 0.0])`. Docstring: origin= is MANDATORY (default = view center, P5-4); camera=0 = model axes; >0 = CCW (engine _rotate_xy sign); selection INCLUDES srp_head (rotates in place about the pivot, keeping its ring normal on the chain axis — Pattern 2, probe B); CGO members (srp_box) are harmless (P5-7).
- `zoom_chain(selection='srp_head or srp_seg_*')` — `cmd.zoom(selection)` + `cmd.refresh()` (completion framing; MUST run AFTER unlock_camera — set_view clobbers prior zooms, P5-3).
- `delete_pickups()` — `cmd.delete('srp_pickup_*')` (pattern delete verified G4; idempotent; teardown + completion scope; srp_seg_*/srp_head untouched).
- `rename_pickup(old, new)` — `cmd.set_name(old, new)` (bog-standard naming.py API, unprobed — smoke verifies; documented fallback if it ever fails: load fresh + apply composed matrix, but set_name is the shipping path).
- `chain_object_names()` — sorted list of public object names starting 'srp_head' or 'srp_seg' via cmd.get_names('public_objects') (the PROVEN type; completion-only read, never per-tick).
  </action>
  <verify>python3.6 tests/run_gates.py — green (bridge syntax + purity); grep: no numpy/PyQt5/cmd.get_object_ttt in the new functions</verify>
  <done>Six thin primitives exist with pitfall-documenting docstrings; gates green</done>
</task>

<task type="auto">
  <name>Task 2: REQUIRED smoke 07 + gate registration</name>
  <files>smoke/07_transform_sweep_smoke.py, tests/run_gates.py</files>
  <action>
1. Create `smoke/07_transform_sweep_smoke.py` following smoke/05's style (headless Windows PyMOL via cmd.exe; verdict = flushed SMOKE-OK sentinels in stdout, NEVER exit codes; cleanup srp_* first; import the pure modules it needs under the staged plugin path like smoke 03/04 do).
   Steps (each flushes its own PASS detail, then the sentinel):
   a. TRANSFORM: load benzene.sdf as srp_tmpA; build m16 for RotZ(90°)+t(1,2,3) INLINE (16-float literal from the probe-A1 fixture — do not import orientation; this smoke pins the RAW layout); pymol_bridge.apply_matrix; read back one known atom coord via cmd.get_model and assert the RotZ90+t mapping to <=1e-4. Flush `SMOKE-OK TRANSFORM`.
   b. SWEEP: load 3 benzene copies (srp_head, srp_seg_1, srp_seg_2) at distinct xy (translate), record centroids via cmd.get_model; call sweep_chain(+90.0, (0.0, 0.0)) once with a 90-degree-equivalent... — replicate probe B properly: apply SIX sweep_chain(15.0, (0,0)) calls (the real per-tick pattern); assert every object's centroid rotated +90° CCW about (0,0) to <=1e-3 and internal geometry (atom-set diameter) unchanged to <=1e-3 (rigid). Flush `SMOKE-OK SWEEP`.
   c. COMPLETION: rename_pickup('srp_seg_1', 'srp_seg_renamed') + assert in cmd.get_names; chain_object_names() contains srp_head + srp_seg_*; zoom_chain() runs without error; delete_pickups() after loading an object named srp_pickup_x — assert it is gone and srp_head survives. Flush `SMOKE-OK COMPLETION`.
   End with a final `print('SMOKE-07 DONE')` line. Wrap every step in try/except that prints the traceback AND the missing sentinel (a failed step simply omits its sentinel — the gate reads sentinels).
2. Register in tests/run_gates.py: add `'smoke/07_transform_sweep_smoke.py'` to REQUIRED_SMOKES.
3. Run: `python3.6 tests/run_gates.py --smoke` — all required smokes pass INCLUDING 07 (sentinels present in output).
  </action>
  <verify>python3.6 tests/run_gates.py --smoke — output contains SMOKE-OK TRANSFORM, SMOKE-OK SWEEP, SMOKE-OK COMPLETION and all other required smokes pass</verify>
  <done>Bridge primitives verified live in headless Windows PyMOL (A1 regression + rigid sweep + completion ops); smoke 07 is gate-required</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- `python3.6 tests/run_gates.py --smoke` — 6/6 required smokes green incl. 07's three sentinels
- No re-spiking: the smoke replicates probe fixtures as regression, using the researcher-verified mechanisms as-is
</verification>

<success_criteria>
The placement/sweep/completion cmd seams exist, are pitfall-documented (origin=, camera=0, no get_object_ttt, zoom-after-unlock), and are proven live by a REQUIRED headless smoke. [G7 part 1; unlocks GAME-10 rendering + STACK-01 viewer apply + GAME-09 framing]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-07-SUMMARY.md`
</output>
