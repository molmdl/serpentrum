---
phase: 05-stacking-game-rules
plan: 13
type: execute
wave: 4
depends_on: ["05-11", "05-12", "05-05", "05-09"]
files_modified:
  - serpentrum/gui_game.py
autonomous: true

must_haves:
  truths:
    - "Every ('stacked',) event resolves synchronously in one tick: skip/refuse -> reject_pickup + info line + respawn gate; placed -> attach + viewer transform + rename to srp_seg_N + structured pickup block"
    - "reject_pickup is called on EVERY non-placed outcome (counters can never desync — locked decision 13)"
    - "The 'won' handler is guarded by engine.finished so a clash-refused cap capture (un-finished by 05-04) does not log a false YOU WIN"
  artifacts:
    - path: "serpentrum/gui_game.py"
      provides: "_handle_stack_event (the Phase-5 connector) + 'stacked'/'won' branches in _handle_event + stacked_history session log"
      contains: "_handle_stack_event"
  key_links:
    - from: "serpentrum/gui_game.py _handle_stack_event"
      to: "serpentrum/placement.py resolve"
      via: "the pure seam decides; the GUI only attaches/rejects/renders/logs"
      pattern: "placement.resolve"
    - from: "serpentrum/gui_game.py _handle_stack_event"
      to: "serpentrum/game_engine.py attach_segment/reject_pickup"
      via: "attach ONLY on placed; reject_pickup ALWAYS on skip/refuse"
      pattern: "reject_pickup"
---

<objective>
Implement the capture seam in GameTab — the "Phase-5 connector" the engine docstrings name: the 'stacked' branch that turns engine capture events into placements, refusals, viewer updates, info-box content and history. All decisions were made and pinned in the pure plans; this plan is glue.

Purpose: success criteria 1 + 5 (stack at cited geometry with tracked counts and full info content; skips with reasons; clash refusals).
Output: _handle_stack_event + two _handle_event branches.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (lifecycle_spec item 4 — the a/b/c/d order this implements)
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (engine_contracts stacked-seam steps 1-5 + skip_policy_matrix)
@serpentrum/gui_game.py (_handle_event :279, _on_tick :254)
@serpentrum/placement.py (resolve + codes), @serpentrum/hud_logic.py (builders from 05-09)
@serpentrum/pymol_bridge.py (apply_matrix, rename_pickup, delete via teardown)
@tests/test_phase5_integration.py (the `capture` helper — this is its GUI transcription, SAME ORDER)
</context>

<tasks>

<task type="auto">
  <name>Task 1: the 'stacked' capture seam</name>
  <files>serpentrum/gui_game.py</files>
  <action>
1. New method `_handle_stack_event(self, pickup_rec, engine)` — synchronous, called from _handle_event's new 'stacked' branch (in event order — 'stacked' arrives BEFORE 'budget'/'won' in the same list):
   a. session lookups: records_by_id, stacking_data, head_atoms, head_stack_ring, box from setup_logic.BOX_PRESETS via the session engine's box_min/box_max, display_z = pymol_bridge.BOX_DISPLAY_Z.
   b. outcome = placement.resolve(pickup_rec, records_by_id, stacking_data, head_atoms, head_stack_ring, engine.heading, engine.segments, existing_atoms, engine.box_min, engine.box_max, display_z) where existing_atoms = list(head_atoms) + [atom for seg in engine.segments for atom in seg['atoms']] + [atom for p in engine.pickups if p['id'] in engine.live_pickup_ids and p['id'] != pickup_rec['id'] for atom in p['atoms']].
   c. skipped/refused -> `engine.reject_pickup(pickup_rec['id'], outcome['code'])` (ALWAYS — locked decision 13); log hud_logic.reason_text(code, detail, record name) via hud_logic.skip_text; append {'name', 'outcome': code} to session['stacked_history']; on 'refused' also leave the viewer object visible (re-armed pickup). Then the spawn gate: if session['spawner'].can_spawn(len(engine.live_pickup_ids)): next spawn (spawner.next_after(engine.head, engine.heading, chain_atoms=[a[1:3] flattened from segments' atoms], live_centroids=[p['centroid'] for live pickups])) -> build seed -> engine pickups... NOTE: the engine owns its pickups list — APPEND the seed dict to engine.pickups AND engine.live_pickup_ids AND bump engine.pickups_remaining (the engine has no spawn API; the controller extends the seeded list exactly as reset() would have — same record shape, engine-owned thereafter); materialize the object (m16 from edge_on + centroid) + session['live_pickup_names'].append.
   d. placed -> `engine.attach_segment(pickup_rec['molecule_id'], outcome['ring_centroid_xy'], outcome['placed_atoms'])`; viewer: m16 = pymol-side matrix from outcome['R'], outcome['t'] via orientation.matrix_rt(R, t) (pre=(0,0,0)); pymol_bridge.apply_matrix('srp_pickup_<id>', m16); new_name = 'srp_seg_%d' % len(engine.segments); pymol_bridge.rename_pickup(old, new_name) (set_name — smoke 07 verified); remove old name from session['live_pickup_names']; append {'name', 'outcome': 'stacked', 'distance_a', 'citation_short', 'interaction_id'} to stacked_history; log hud_logic.pickup_block(...); `self._update_remaining()` runs on the same tick already (GAME-04 counts stay engine-owned + hidden).
   e. Wrap the whole method body in try/except Exception -> log 'placement error: %s' + ALWAYS engine.reject_pickup(pickup_rec['id'], 'error') as the last-resort counter guard (never leave a capture dangling).
2. 'won' branch guard: change the existing `elif kind == 'won': self._log('YOU WIN')` to log only `if engine.finished:` — a clash-refused cap capture un-finishes the engine (05-04) before the 'won' event is handled (event order stacked->won), and hud_logic.resume_note(record name) is logged by the stacked branch in that case instead.
Keep 'turn_refused'/'crashed' branches untouched (05-14 owns 'turning'/'budget_warning').
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep audit: `grep -n "reject_pickup" serpentrum/gui_game.py` — present in BOTH the skip/refuse path and the exception guard</verify>
  <done>Capture seam live: skips/refusals reject + explain + respawn-gate; placements attach + transform + rename + structured block; false YOU WIN impossible</done>
</task>

<task type="auto">
  <name>Task 2: seam-order conformance audit</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Self-audit against the pinned contract (fix in place if any check fails):
1. Order inside _handle_stack_event matches tests/test_phase5_integration.py's capture helper EXACTLY (skip -> tail -> place -> gate -> attach/reject).
2. No per-tick cmd reads: the only cmd calls here are transform/rename/show-class writes (get_model/get_extent/get_names appear NOWHERE in _on_tick/_handle_stack_event — research Never-do list).
3. No second teardown path introduced; no engine internals mutated except the documented pickups-list append (spawn) — counters change ONLY via attach/reject.
4. The pickup object name flow: spawn 'srp_pickup_<pid>' -> rename 'srp_seg_<n>' — both under the srp_ prefix (cleanup_srp pattern contract).
5. Update the GameTab class docstring Phase-5 paragraph (stacked seam + history live).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep: no get_model/get_extent/get_names inside gui_game.py tick/event paths; docstring updated</verify>
  <done>Seam audited against the pure spec; viewer-read discipline and single-teardown discipline intact</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- The GUI seam is a line-by-line transcription of the pure integration helper (same order, same calls)
- reject_pickup reachable from every non-placed outcome including the exception guard
</verification>

<success_criteria>
Captures resolve in one synchronous tick with cited-geometry placements, structured STACK-04 blocks, reasoned skips/refusals, counter-safe rejection always, and rename-to-chain viewer bookkeeping. [STACK-01/03/05 GUI wiring, GAME-04, STACK-04 per-pickup]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-13-SUMMARY.md`
</output>
