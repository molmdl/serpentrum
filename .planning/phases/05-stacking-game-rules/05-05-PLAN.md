---
phase: 05-stacking-game-rules
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/placement.py
  - tests/test_placement.py
autonomous: true

must_haves:
  truths:
    - "A captured pickup with no APPROVED dataset entry (uploads included) resolves to a structured skip code, never a placement"
    - "A stackable pickup places at the dataset-stored geometry from the newest segment's ring frame (or the head's when the chain is empty), with the growth normal = -heading for the first capture and the newest segment's own normal thereafter (linear staircase, probe-verified clash-safe)"
    - "The clash gate checks placed atoms against head + all segments + other live pickups inside the 3D box (xy preset faces, z = +/-BOX_DISPLAY_Z) and refuses wall/atom violations"
  artifacts:
    - path: "serpentrum/placement.py"
      provides: "resolve() orchestrator + resolve_skip/tail_frame/attempt_place — the pure STACK-01/03/05 controller seam"
      contains: "def resolve"
    - path: "tests/test_placement.py"
      provides: "Skip taxonomy, tail-frame policy, exactness (3.6000 A), clash-refusal and win-desync-free outcome pins"
  key_links:
    - from: "serpentrum/placement.py attempt_place"
      to: "serpentrum/stacking.py place_pickup"
      via: "dataset distance_a/lateral_offset_a drive placement — no invented chemistry"
      pattern: "place_pickup"
    - from: "serpentrum/placement.py resolve"
      to: "serpentrum/molecule_data.py interaction_for/shipped_interactions"
      via: "APPROVAL-GATED lookup (interaction_for alone is not approval-filtered)"
      pattern: "shipped_interactions"
---

<objective>
Create the PURE placement controller seam `serpentrum/placement.py` — the single function the GUI's 'stacked' branch calls, plus its testable parts. This is where the five open policy questions are PINNED (stated explicitly per the planning mandate):

1. **Skip taxonomy (ordered, first match wins):** SKIP_NO_ENTRY (has_stack_entry False / interaction_for None — the `__upload__` keying, locked decision 7) -> SKIP_NOT_APPROVED (interaction id not in shipped_interactions' APPROVED set — DATA-02) -> SKIP_MODE (mode != 'pi_stack') -> SKIP_NO_RING (stack_ring absent on the record) -> SKIP_NONPLANAR (ring_frame ValueError).
2. **Tail/growth policy (G6):** chain empty -> tail = the HEAD's ring frame, growth normal = -(heading) (behind the head, snake-canonical); chain non-empty -> tail = NEWEST segment's ring frame recomputed from its current (sweep-rotated) atoms via records_by_id stack_ring, growth normal = that frame's computed normal (by construction it equals the direction the segment was placed with — the linear staircase the research measured clash-safe at >= 3.386 Å). Lateral always along tail ref (+y after edge-on — the visible in-plane axis, pinned in plan 05-02).
3. **Clash-gate existing set (research open Q7):** head atoms + ALL segment atoms + OTHER live pickups' atoms (prevents placing onto an unpicked pickup that would refuse mid-chain). Box: 3D — xy from the preset, z = ±BOX_DISPLAY_Z (the SAME constant the displayed box uses; passed as display_z).
4. **Outcome contract:** {'status': 'placed', placed_atoms, R, t, ring_centroid_xy, interaction, citation_short} | {'status': 'skipped', code} | {'status': 'refused', code, detail} — the GUI maps codes to text via hud_logic (plan 05-09) and ALWAYS calls engine.reject_pickup on skip/refuse (capture already counted).

Purpose: keep gui_game thin; make every placement decision WSL-testable.
Output: placement.py + pinning tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (skip_policy_matrix, engine_contracts stacked-seam order, clash matrix incl. biphenyl)
@serpentrum/stacking.py (ring_frame / place_pickup / check_clash — call AS-IS, never modify)
@serpentrum/molecule_data.py (interaction_for, shipped_interactions — approval gate is the caller's job)
@serpentrum/setloader.py (record keys: has_stack_entry, set, ring_atoms; stack_ring arrives via plan 05-08)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — skip taxonomy tests</name>
  <files>tests/test_placement.py</files>
  <action>
Create `tests/test_placement.py` (unittest, python3.6, zero stubs, real dataset via molecule_data.load_stacking(setloader.default_stacking_path()) + real records via setloader.load_demo_set(..., stacking_path=...)). Pin `placement.resolve_skip(record, stacking_data)`:
1. An upload-shaped record ({'set': '__upload__', 'has_stack_entry': False}) -> SKIP_NO_ENTRY.
2. A set_a record loaded WITH the dataset -> None (stackable).
3. A record with has_stack_entry True but a stacking_data whose interaction id is missing from shipped_interactions' approved set (mutate a copy: set the interaction's status to 'DRAFT' if the schema allows — else build a minimal dict) -> SKIP_NOT_APPROVED.
4. mode != 'pi_stack' -> SKIP_MODE.
5. record without 'stack_ring' key (upload shape) -> SKIP_NO_RING (assert order: an upload hits SKIP_NO_ENTRY first, NOT SKIP_NO_RING).
Run — RED.
  </action>
  <verify>python3.6 -m unittest tests.test_placement -v — ImportError/failures (module missing)</verify>
  <done>Skip taxonomy pinned incl. ordering (NO_ENTRY before NO_RING) and the approval gate</done>
</task>

<task type="tdd">
  <name>Task 2: RED — tail-frame + placement exactness + clash tests</name>
  <files>tests/test_placement.py</files>
  <action>
Extend tests/test_placement.py. Fixtures: use orientation.edge_on_atoms (plan 05-02) + molfile.read_sdf to build edge-on'd atom lists; build a fake newest-segment list [{'molecule_id': 'benzene', 'centroid': (cx, cy), 'atoms': placed_atoms, 'atoms_n': n}] mirroring engine records. Pin:
1. **tail_frame policy, chain empty:** tail_frame([], records_by_id, head_atoms, head_stack_ring, heading=(1.0, 0.0)) returns the head's ring centroid as centroid, normal == (-1.0, 0.0, 0.0) (behind the head), ref == the head's computed ring ref.
2. **tail_frame policy, chain non-empty:** with one segment (placed benzene atoms), tail_frame returns THAT segment's ring centroid and its computed normal — and for a same-species chain the normal equals the growth direction used at placement (assert dot(normal, segment_centroid - head_centroid_xy_promoted) > 0.99 — staircase continuity).
3. **attempt_place exactness (STACK-01):** place benzene onto a benzene tail with the dataset interaction (distance_a 3.383, lateral_offset_a 1.231): the placed ring centroid to tail ring centroid distance == sqrt(3.383**2 + 1.231**2) == 3.6000 within 1e-9; angle atan2(1.231, 3.383) == 20.0 deg within 1e-6 (formula pinned in test_stacking_dataset.py — reuse).
4. **Growth normal = -heading for first capture:** attempt_place output ring centroid sits at head_centroid + 3.6-ish along (-1, 0, 0) (x decreases, |dx| > |dy|).
5. **Clash gate assembly:** gate() with existing = head + segments + other live pickups: a placement overlapping a live pickup's atoms returns the atom-violation dict; a placement poking past (x1, y1, +display_z) returns the wall dict; a clean 3.6 Å stack returns None. Use stacking.check_clash THROUGH placement.gate (thin wrapper — no reimplementation).
6. **Biphenyl refuse (locked decision 6 — the refuse-path exercise case):** placing biphenyl onto ANY set_a tail at dataset geometry produces a check_clash atom violation (min pair distance 0.688-2.145 Å < 2.5) — assert resolve() returns status 'refused' with code REFUSE_ATOM for a biphenyl pickup. NOTE the data observation (do NOT fix): shipped biphenyl conformer has rings at 90.00°, so every biphenyl stack clashes; biphenyl is the permanent refuse-path demonstrator.
7. **resolve() orchestrator:** full happy path returns status 'placed' with placed_atoms as [(sym, x, y, z)...], R (3x3), t, ring_centroid_xy; upload path returns 'skipped'/SKIP_NO_ENTRY without calling place_pickup (assert via result only).
Run — RED.
  </action>
  <verify>python3.6 -m unittest tests.test_placement -v — new tests fail (functions missing)</verify>
  <done>Tail policy, 3.6000 Å exactness, gate assembly, biphenyl refusal, orchestrator contract — all pinned RED</done>
</task>

<task type="tdd">
  <name>Task 3: GREEN — implement placement.py</name>
  <files>serpentrum/placement.py</files>
  <action>
Create `serpentrum/placement.py` — PURE (stdlib math only; imports `from . import stacking, molecule_data`; python3.6 %-formatting; auto-classified PURE). Module docstring records the four pinned policies (skip order, growth/tail policy, clash existing-set incl. live pickups, box3d z = ±display_z).

Constants: outcome codes SKIP_NO_ENTRY, SKIP_NOT_APPROVED, SKIP_MODE, SKIP_NO_RING, SKIP_NONPLANAR, REFUSE_WALL, REFUSE_ATOM (string constants — hud_logic imports these in plan 05-09).

Functions:
- `resolve_skip(record, stacking_data)` — the ordered taxonomy from Task 1 (approval gate via `{i['id'] for i in molecule_data.shipped_interactions(stacking_data)}`; mode check on the FOUND interaction; stack_ring presence check).
- `tail_frame(segments, records_by_id, head_atoms, head_stack_ring, heading)` — per the Task-2 pinned policy: empty -> head frame with normal = (-heading[0], -heading[1], 0.0); else newest segment: (c, n, r) = stacking.ring_frame([atom[1:] for atom in seg['atoms']], records_by_id[seg['molecule_id']]['stack_ring']); return (c, n, r) — the computed normal IS the growth continuation.
- `attempt_place(pickup_atoms, pickup_stack_ring, tail_c, tail_n, tail_ref, interaction)` — stacking.place_pickup(pickup_atoms, pickup_stack_ring, tail_c, tail_n, tail_ref, interaction['distance_a'], interaction['lateral_offset_a'], lateral_along_ref=True); return (placed, R, t, ring_centroid) where ring_centroid = stacking.ring_frame(placed, pickup_stack_ring)[0].
- `gate(placed_atoms, existing_atoms, box_min2d, box_max2d, display_z)` — stacking.check_clash(placed, existing, (x0, y0, -display_z), (x1, y1, +display_z)).
- `resolve(pickup_rec, records_by_id, stacking_data, head_atoms, head_stack_ring, heading, segments, existing_atoms, box_min2d, box_max2d, display_z)` — orchestrator: skip -> {'status': 'skipped', 'code'}; else tail_frame + attempt_place (ring_frame ValueError -> SKIP_NONPLANAR) + gate (dict -> REFUSE_WALL/REFUSE_ATOM with detail '%.2f A' distance); else {'status': 'placed', ...} with interaction + citation_short (stacking_data['citations'][interaction['citation']]['short']).

Never import game_engine/pymol/Qt. Run gates.
  </action>
  <verify>python3.6 -m unittest tests.test_placement -v (all pass) && python3.6 tests/run_gates.py (green)</verify>
  <done>All pins green: taxonomy, tail policy, 3.6000 Å placement exactness, clash assembly, biphenyl refusal, orchestrator outcomes</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_placement -v` — green
- `python3.6 tests/run_gates.py` — green
- The 3.6000 Å / 20.0° dataset-formula assertions reproduce test_stacking_dataset's pinned values
</verification>

<success_criteria>
The pure placement seam exists with pinned skip taxonomy, snake-canonical growth policy, dataset-exact placement, and the 3D clash gate over head+chain+live pickups — the GUI's entire 'stacked' logic reduces to one resolve() call plus engine attach/reject. [STACK-01, STACK-03, STACK-05; GAME-04 counting stays engine-owned]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-05-SUMMARY.md`
</output>
