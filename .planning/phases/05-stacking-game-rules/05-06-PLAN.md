---
phase: 05-stacking-game-rules
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/__init__.py
  - serpentrum/gui_setup.py
autonomous: true

must_haves:
  truths:
    - "SetupTab Apply loads records WITH the stacking dataset (has_stack_entry is finally True for set_a) and anchors records + stacking_data on pmg_tk.startup._serpentrum"
    - "begin_game can read the anchored records after Apply AND after Restart-without-Apply"
    - "last_run anchor field exists (documented, None until the completion presenter writes it in plan 05-15)"
  artifacts:
    - path: "serpentrum/__init__.py"
      provides: "_SerpentrumState fields: records, stacking_data, last_run (reload-surviving anchor)"
      contains: "last_run"
    - path: "serpentrum/gui_setup.py"
      provides: "_on_apply passes stacking_path=setloader.default_stacking_path() to load_demo_set/load_upload and anchors results"
      contains: "stacking_path"
  key_links:
    - from: "serpentrum/gui_setup.py _on_apply"
      to: "serpentrum/__init__.py _SerpentrumState"
      via: "anchor.records / anchor.stacking_data written on successful load"
      pattern: "stacking_data"
---

<objective>
Close wiring gap G4 (research: records + stacking dataset are SetupTab-locals and never reach the game tab): pass `stacking_path=setloader.default_stacking_path()` into both load paths and anchor `records` + `stacking_data` (plus the future `last_run`) on the reload-surviving `_serpentrum` state — never module globals, never the scalar-only setup dict (locked decision 10).

Purpose: begin_game (plan 05-11), Restart determinism, the skip policy, and the info-box builders all consume these anchors.
Output: three new anchor fields + the two-line load-path fix.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (records gap + anchor fields)
@serpentrum/__init__.py (_SerpentrumState: dialog/controller/setup/game_session — init-once rules from 03-07)
@serpentrum/gui_setup.py (_on_apply at :407; load calls at :435/:438 currently WITHOUT stacking_path)
@serpentrum/setloader.py (default_stacking_path, load_demo_set/load_upload signatures)
</context>

<tasks>

<task type="auto">
  <name>Task 1: anchor fields on _SerpentrumState</name>
  <files>serpentrum/__init__.py</files>
  <action>
Add three class attributes to `_SerpentrumState` (after game_session), each with a one-line docstring comment in the established style:
- `records = None` — "the live setloader records list (Phase 5, plan 05-06): written by SetupTab apply on success; consumed by GameTab begin_game for pickups + info content; survives reload like game_session"
- `stacking_data = None` — "the live stacking dataset dict (Phase 5): the APPROVED-interaction source for the skip policy and STACK-04 content"
- `last_run = None` — "the completed-run handoff record (Phase 5, plan 05-15): {'result', 'molecules_stacked', 'atoms_total', 'chain_objects', 'snake_id'}; written at completion, consumed by the Spectra stage (Phases 6/7)"

Do NOT touch the init-once logic (state.setup is initialized on FIRST anchor creation only — 03-07 rule stays). The new fields default None on every anchor creation; writers are responsible for setting them (same model as game_session). python3.6 syntax.
  </action>
  <verify>python3.6 tests/run_gates.py — green (entry module purity: fields are plain class attributes, no imports added)</verify>
  <done>Three documented anchor fields exist on _SerpentrumState; entry purity unchanged; gates green</done>
</task>

<task type="auto">
  <name>Task 2: gui_setup loads + anchors the dataset</name>
  <files>serpentrum/gui_setup.py</files>
  <action>
In `SetupTab._on_apply` (around :435/:438):
1. Change both load calls to pass the stacking dataset:
   - `setloader.load_upload(path, stacking_path=setloader.default_stacking_path())`
   - `setloader.load_demo_set(set_id=source, stacking_path=setloader.default_stacking_path())`
2. After a successful load (records list non-empty), anchor on the state: `if self._anchor is not None: self._anchor.records = records; self._anchor.stacking_data = <the loaded stacking dict or None>`. To anchor stacking_data you need the dict — load it once via `molecule_data.load_stacking(setloader.default_stacking_path())` guarded by try/except (molecule_data.DataError, IOError) -> None + the existing error-surfacing path (reuse _refresh_status patterns; on dataset load failure anchor stacking_data=None and records=records — has_stack_entry was computed False anyway, so the skip policy degrades to SKIP_NO_ENTRY, which is correct).
   Note: setloader already consumed the dataset internally for has_stack_entry; anchoring a separately-loaded dict is deliberate (setloader returns only records+errors) — load it fresh; the file is tiny and static.
3. Do NOT change the bool return contract, the Start flow, or any widget. gui_setup stays GUI-class (pymol.Qt only; relative pure imports exempt).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep audit: `grep -n "stacking_path" serpentrum/gui_setup.py` shows both load calls; `grep -n "_anchor.records" serpentrum/gui_setup.py` shows the anchor write</verify>
  <done>Apply-path records carry has_stack_entry=True for set_a; records + stacking_data anchored on _serpentrum; no behavioral change to the Start/Apply contract; gates green</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- WSL probe (allowed, read-only): `python3.6 -c "from serpentrum import setloader; r,e = setloader.load_demo_set(stacking_path=setloader.default_stacking_path()); print([x['has_stack_entry'] for x in r])"` prints `[True, True, True, True, True]`
- grep: both load calls pass stacking_path; anchor writes present
</verification>

<success_criteria>
Records loaded via the Setup tab are dataset-aware and anchored on _serpentrum (records, stacking_data), and the last_run field exists for the completion handoff — the G4 wiring gap is closed. [locked decision 10; feeds GAME-04/STACK-03/STACK-04/GAME-09]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-06-SUMMARY.md`
</output>
