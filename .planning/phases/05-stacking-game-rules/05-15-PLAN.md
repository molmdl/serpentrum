---
phase: 05-stacking-game-rules
plan: 15
type: execute
wave: 6
depends_on: ["05-13", "05-14"]
files_modified:
  - serpentrum/gui_game.py
  - serpentrum/gui.py
autonomous: true

must_haves:
  truths:
    - "On win OR crash: verdict logs first, teardown clears live pickups, then the presenter zooms the surviving chain, reveals length + score + atoms, prints the interaction breakdown, anchors last_run, and enables Get Spectra"
    - "Get Spectra follows the locked model-A signal pattern: GameTab.spectra_requested -> PluginDialog owns setCurrentIndex(2); GameTab never reaches its parent"
    - "The button is disabled at construction/begin_game/teardown and enabled ONLY in the presenter; last_run carries result/counts/chain_objects/snake_id for Phases 6/7"
  artifacts:
    - path: "serpentrum/gui_game.py"
      provides: "_present_completion + get_spectra_btn + spectra_requested signal + teardown button reset"
      contains: "_present_completion"
    - path: "serpentrum/gui.py"
      provides: "spectra_requested connection -> setCurrentIndex(2) (model-A, 04-06 template)"
      contains: "spectra_requested"
  key_links:
    - from: "serpentrum/gui_game.py _present_completion"
      to: "serpentrum/__init__.py _SerpentrumState.last_run"
      via: "anchor {'result', 'molecules_stacked', 'atoms_total', 'chain_objects', 'snake_id'} at completion"
      pattern: "last_run"
    - from: "serpentrum/gui_game.py _end_run"
      to: "serpentrum/pymol_bridge.py zoom_chain"
      via: "zoom AFTER _teardown_round (unlock_camera's set_view clobbers prior zooms — P5-3)"
      pattern: "zoom_chain"
---

<objective>
Implement the GAME-09 completion flow — the same path for win and crash (locked decision 8): `_end_run` gains a presenter step AFTER teardown (never a second teardown), the viewer clears via the teardown's pickup deletion, the camera frames the completed snake, HUD reveals length + score, the STACK-04 breakdown prints, `last_run` anchors on _serpentrum, and the Get Spectra button activates with the model-A tab-switch signal.

Purpose: success criterion 4 — both endings hand a complete snake to the spectra stage.
Output: presenter + button + signal wiring (gui_game.py + gui.py).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (lifecycle_spec item 3 exact sequence + spectra_handoff + teardown_integration item 3)
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (Pattern 4 completion ordering — teardown FIRST then zoom)
@serpentrum/gui_game.py (_end_run :405, _teardown_round, _build_widgets)
@serpentrum/gui.py (PluginDialog._on_start_requested :81 — the model-A template; Spectra page index 2)
@serpentrum/hud_logic.py (completion_lines, breakdown_lines from 05-09)
</context>

<tasks>

<task type="auto">
  <name>Task 1: completion presenter + button</name>
  <files>serpentrum/gui_game.py</files>
  <action>
1. `_build_widgets`: add `self.get_spectra_btn = QtWidgets.QPushButton('Get Spectra', self)` to the button row (after restart, before the stretch); `setEnabled(False)` at construction.
2. Class signal: `spectra_requested = QtCore.Signal()` (class-level, like SetupTab.start_requested). `_wire_signals`: connect clicked -> `self.spectra_requested.emit` (GameTab does NOT switch tabs — PluginDialog owns the QTabWidget, locked decision 9).
3. `_present_completion(self, engine)`: called from `_end_run` AFTER `_teardown_round` (presenter, NOT a teardown):
   a. pymol_bridge.zoom_chain() (one-shot framing of srp_head + srp_seg_*; runs after unlock so the restored view doesn't clobber it — P5-3/Pattern 4).
   b. HUD: log hud_logic.completion_lines(engine.result, engine.molecules_stacked, len(engine.segments) + 1, engine.atoms_total) — score = molecules_stacked (GAME-06 cap = molecule count), length = segments + head, atoms now revealed (SPECTRA-06 context).
   c. Breakdown: log each line of hud_logic.breakdown_lines(session['stacked_history']).
   d. Anchor: `if self._anchor is not None: self._anchor.last_run = {'result': engine.result, 'molecules_stacked': engine.molecules_stacked, 'atoms_total': engine.atoms_total, 'chain_objects': pymol_bridge.chain_object_names(), 'snake_id': 'run_%d' % session['epoch']}`.
   e. `self.get_spectra_btn.setEnabled(True)`.
4. `_end_run`: keep the verdict-first log + `self._teardown_round()`, then call `self._present_completion(engine)` (guard: only when session existed). Order: log verdict -> teardown (timers/epoch/input/camera/pickup deletion/button reset) -> presenter (zoom/HUD/anchor/enable).
5. `_teardown_round`: add the button reset alongside the pause-button reset (inside the same blockSignals discipline — setEnabled(False) needs no blockSignals, but keep the pause-button block untouched): `self.get_spectra_btn.setEnabled(False)`. begin_game's teardown call therefore re-disables it on every new round (research: disabled at construction/begin_game/teardown).
6. Reload-mid-run hardening (research teardown_integration item 4, one line): in `_teardown_round`, when `self._session is None`, fall back to reading `self._anchor.game_session` for the saved_cam pop (same pop semantics) before unlocking — the stale-camera-after-reload gap closes under the same idempotent contract.
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep audit: `grep -n "zoom_chain\|_present_completion\|last_run" serpentrum/gui_game.py` — presenter called from _end_run only; zoom AFTER teardown in call order; `grep -c "def _teardown_round" serpentrum/gui_game.py` still 1</verify>
  <done>Both endings: verdict -> teardown (pickups die, chain survives, camera unlocks) -> zoom + revealed counts + breakdown + last_run anchor + enabled Get Spectra</done>
</task>

<task type="auto">
  <name>Task 2: PluginDialog model-A spectra switch</name>
  <files>serpentrum/gui.py</files>
  <action>
In PluginDialog (gui.py), where game_tab is created/connected (the same place _on_start_requested is wired): `self.game_tab.spectra_requested.connect(self._on_spectra_requested)`; new slot `_on_spectra_requested(self)`: `self.tabs.setCurrentIndex(2)` (+ a one-line docstring: model-A handoff per 04-06 — the dialog owns the QTabWidget; the Spectra page is the Phase-7 placeholder and Phase 7 replaces its content; the last_run anchor is already on _serpentrum for Phases 6/7). No other changes: focusInEvent/closeEvent untouched, modeless rule untouched (no .exec_ anywhere).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep: `grep -n "setCurrentIndex(2)" serpentrum/gui.py` present in the new slot; `grep -c "exec_(" serpentrum/gui.py` still 0</verify>
  <done>Get Spectra switches to the (placeholder) Spectra tab via the locked signal model; Phase 7 only replaces page content</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- Ordering audit: _end_run = verdict log -> _teardown_round -> _present_completion; zoom_chain strictly after unlock
- Anchor audit: last_run written in the presenter only; chain_object_names() is the only name read (completion-only, never per-tick)
- Button lifecycle: disabled at construction + teardown; enabled only in the presenter
</verification>

<success_criteria>
Win and crash both reach the identical completion flow: complete snake framed on screen, score/length/atoms revealed, interaction breakdown printed, last_run anchored, Get Spectra live and model-A-wired. [GAME-09; locked decisions 8/9; spectra handoff for Phases 6/7]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-15-SUMMARY.md`
</output>
