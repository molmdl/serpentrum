---
phase: 05-stacking-game-rules
plan: 09
type: execute
wave: 2
depends_on: ["05-05"]
files_modified:
  - serpentrum/hud_logic.py
  - tests/test_hud_content.py
autonomous: true

must_haves:
  truths:
    - "Per-pickup info content is dataset-composed: interaction name, stored distance, composed centroid distance @ angle, verbatim dataset explanation, citation short-code — no authored chemistry"
    - "Every skip/refuse code from placement.py maps to a stable educator-readable line; budget warnings never reveal counts (GAME-04 hidden during play)"
    - "End-of-run breakdown summarizes stacked counts + refusals from the session history; idle tips round-robin VERBATIM dataset explanations"
  artifacts:
    - path: "serpentrum/hud_logic.py"
      provides: "pickup_block, skip_text, budget_text, completion_lines, breakdown_lines, idle_tip, resume_note — pure builders (STACK-04)"
      contains: "def pickup_block"
    - path: "tests/test_hud_content.py"
      provides: "Format pins incl. the composed 3.60 A @ 20.0 deg formula and code->text mapping"
  key_links:
    - from: "serpentrum/hud_logic.py"
      to: "serpentrum/placement.py"
      via: "imports the outcome-code constants — one taxonomy, two consumers"
      pattern: "SKIP_NO_ENTRY|REFUSE_ATOM"
---

<objective>
Extend PURE hud_logic with the STACK-04 content builders — structured per-pickup blocks, skip reasons, the (count-free) budget advisory, completion lines, the end-of-run breakdown, and round-robin idle tips. All chemistry sentences come VERBATIM from the shipped dataset's own fields (no-fabrication rule); the existing format_elapsed/remaining_text pins stay byte-identical.

Purpose: the info box stays a dumb rolling log (research verdict — NO queue/priority system); these builders are the only new surface.
Output: hud builders + format-pinning tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (info_box_spec table + line-format recommendation)
@serpentrum/hud_logic.py (37 lines — existing pins in test_hud_logic.py must not change)
@serpentrum/placement.py (outcome-code constants from plan 05-05)
@serpentrum/data/stacking_pi_stack.json (interaction fields + citations short codes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: hud content builders + tests</name>
  <files>serpentrum/hud_logic.py, tests/test_hud_content.py</files>
  <action>
1. Write `tests/test_hud_content.py` FIRST (unittest, python3.6; real dataset via molecule_data.load_stacking(setloader.default_stacking_path())). Pin:
   - `pickup_block(name, interaction, citation_short)`: exact format `'+ %s: %s, %.2f A plane gap (centroid %.2f A @ %.1f deg off-normal) - %s [%s]'` with (d, l) = (interaction['distance_a'], interaction['lateral_offset_a']), composed = math.sqrt(d*d + l*l) (3.60 for the shipped set), angle = math.degrees(math.atan2(l, d)) (20.0), explanation with newlines collapsed to single spaces (the shipped explanation contains a newline), citation_short last. Assert the rendered line for the REAL pi_stack_pd entry contains 'pi-pi stacking (parallel-displaced)', '3.38', '3.60', '20.0', 'Janiak 2000'.
   - `skip_text(name, reason)`: `'skipped %s: %s'`.
   - `budget_text()`: constant `'atom budget exceeded - spectra on this snake may be slow'` — assert NO digit appears in the string (GAME-04 hidden counts during play).
   - `completion_lines(result, molecules, snake_molecules, atoms_total)`: list of lines — `['result: %s' % result, 'score: %d molecule(s) stacked' % molecules, 'snake: %d molecules (incl. head)' % snake_molecules, 'atoms: %d (spectra input size)' % atoms_total]` — counts REVEALED at completion (GAME-09/SPECTRA-06).
   - `breakdown_lines(history)`: history = list of dicts {'name', 'outcome', 'distance_a', 'citation_short'} where outcome is 'stacked' or a placement code; stacked entries grouped by name: `'stacked %dx %s at %.2f A plane gap [%s]'`; refusals/skips grouped by (outcome, name): `'refused %dx %s: %s'`. Order: first-appearance order; empty history -> [].
   - `idle_tip(explanations, index)`: round-robin `'tip: %s'` (newline-collapsed) or None when explanations empty.
   - `resume_note(name)`: `'placement refused - run continues (cap not reached yet)'` — the G2 un-finish info line.
   - `reason_text(code, detail, name)`: maps placement codes to the research's suggested literals — SKIP_NO_ENTRY: 'no verified stacking entry for this molecule (no invented chemistry)'; SKIP_NOT_APPROVED: 'stacking entry not approved yet'; SKIP_MODE: "interaction mode not supported in v1"; SKIP_NO_RING: 'no planar aromatic 6-ring found'; SKIP_NONPLANAR: 'ring geometry not planar'; REFUSE_WALL: 'placement would leave the play box'; REFUSE_ATOM: 'placement clashes (%s)' % detail — each rendered through skip_text(name, ...).
2. Implement in serpentrum/hud_logic.py — append ONLY (existing format_elapsed/remaining_text untouched; python3.6 %-formatting; PURE; `from . import placement` for the code constants — pure-to-pure import is fine).
Run gates.
  </action>
  <verify>python3.6 -m unittest tests.test_hud_content tests.test_hud_logic -v (new pins green, old pins untouched) && python3.6 tests/run_gates.py (green)</verify>
  <done>All STACK-04 builders pinned: dataset-composed pickup blocks, code->reason mapping, count-free budget line, completion/breakdown/idle/resume builders</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_hud_content tests.test_hud_logic -v` — green (old + new)
- `python3.6 tests/run_gates.py` — green
- Rendered pickup block for the shipped dataset contains the composed 3.60 A / 20.0 deg values and the Janiak 2000 short-code
</verification>

<success_criteria>
STACK-04 content is fully pure-composed from the dataset (name/distance/explanation/citation), skip reasons are stable strings keyed to placement codes, counts stay hidden during play and appear at completion. [STACK-04, STACK-03 display, GAME-04, GAME-09 display]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-09-SUMMARY.md`
</output>
