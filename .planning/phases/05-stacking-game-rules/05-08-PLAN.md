---
phase: 05-stacking-game-rules
plan: 08
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - serpentrum/setloader.py
  - tests/test_setloader_stack_ring.py
autonomous: true

must_haves:
  truths:
    - "Every accepted demo record carries stack_ring = molfile.ring_cycle(...)'s canonical ordered 6-cycle"
    - "Upload records NEVER carry stack_ring (skip-policy keying intact — locked decisions 5 and 7)"
  artifacts:
    - path: "serpentrum/setloader.py"
      provides: "stack_ring computed at load time in _build_record (demo only)"
      contains: "stack_ring"
    - path: "tests/test_setloader_stack_ring.py"
      provides: "Carry-through pins for demo + upload records"
  key_links:
    - from: "serpentrum/setloader.py _build_record"
      to: "serpentrum/molfile.py ring_cycle"
      via: "stack_ring computed from the SAME parsed record (bonds + 2-core)"
      pattern: "ring_cycle"
---

<objective>
Carry the canonical stacking ring on records (locked decision 5): `setloader._build_record` computes `stack_ring` via `molfile.ring_cycle` at load time for demo records; uploads stay ring-less (they are skip-at-pickup; STACK-03).

Purpose: placement (plan 05-05) and tail frames (plan 05-11/05-13) index placed atoms 1:1 by `stack_ring`; engine copies/sweeps carry unknown record keys through (verified), so a load-time attachment survives the whole game.
Output: setloader carry-through + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (ring_extraction_spec "Where it lives" + setloader record schema)
@serpentrum/setloader.py (_build_record :66, load_demo_set :102, load_upload :212)
@serpentrum/molfile.py (ring_cycle from plan 05-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: stack_ring carry in _build_record + wiring</name>
  <files>serpentrum/setloader.py</files>
  <action>
1. Extend `_build_record` with a keyword `stack_ring=None` (same pattern as the existing `ring_atoms=None`): when not None, add `record['stack_ring'] = list(stack_ring)` after the ring_atoms block. Docstring: stack_ring is the canonical ONE-ring cycle in ring-walk order (from molfile.ring_cycle) — the indices alignment contract (same record's coords/elements order) makes placed-atom indexing 1:1.
2. In `load_demo_set`: compute `stack_ring = molfile.ring_cycle(record)` from the already-parsed molfile record (the SAME `record` variable passed to _build_record — bonds and coords come from one parse) and pass it through. Place the computation AFTER the gate/mismatch checks (only accepted molecules pay the cost).
3. In `load_upload`: pass nothing (uploads omit stack_ring BY DESIGN — do not compute; the skip policy keys on the absent ring, research row 4 is reserved for future non-upload sets without 6-rings).
4. Update the module docstring record-key list with stack_ring. python3.6 syntax; purity unchanged (stdlib only).
Do NOT modify load_demo_set's validation order or error strings (pinned by test_setloader.py).
  </action>
  <verify>python3.6 tests/run_gates.py — green (existing test_setloader.py unmodified and passing)</verify>
  <done>stack_ring on demo records; absent on uploads; no validation-order changes</done>
</task>

<task type="auto">
  <name>Task 2: pinning tests</name>
  <files>tests/test_setloader_stack_ring.py</files>
  <action>
Create `tests/test_setloader_stack_ring.py` (unittest, python3.6, zero stubs, real shipped data):
1. `load_demo_set(stacking_path=setloader.default_stacking_path())` — every record has 'stack_ring', a list of exactly 6 ints, and equals the pinned canonical tuples from plan 05-01 per molecule id (benzene (0,1,3,5,4,2); naphthalene (0,1,3,7,6,2); anthracene (0,1,5,3,2,4); phenanthrene (0,1,3,5,4,2); biphenyl (0,2,6,10,8,4)).
2. stack_ring indices index into record['elements']/parsed coords 1:1: for each demo record, `molfile.read_sdf(record['file'])[0]['coords']` has >= max(stack_ring)+1 entries (alignment contract).
3. Upload path: build a minimal single-record SDF in a tempdir (or reuse tests' existing upload fixtures pattern from test_setloader.py) and assert `load_upload(path, stacking_path=...)` records have NO 'stack_ring' key.
4. Demo records still carry ring_atoms (the sorted 2-core) ALONGSIDE stack_ring — both keys coexist.
Run gates.
  </action>
  <verify>python3.6 -m unittest tests.test_setloader_stack_ring -v (green) && python3.6 tests/run_gates.py (green)</verify>
  <done>All 5 canonical tuples carried on demo records; uploads ring-less; alignment contract pinned</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_setloader_stack_ring -v` — green
- `python3.6 tests/run_gates.py` — green
</verification>

<success_criteria>
Demo records ship with the canonical stack_ring in ring-walk order; uploads are ring-less by design — the dataset-side contract placement consumes is complete. [G1 carry-through; locked decision 5]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-08-SUMMARY.md`
</output>
