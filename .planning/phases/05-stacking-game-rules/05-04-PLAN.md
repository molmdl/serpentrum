---
phase: 05-stacking-game-rules
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/game_engine.py
  - tests/test_engine_win_desync.py
autonomous: true

must_haves:
  truths:
    - "reject_pickup on a cap-reaching capture that fails the clash gate un-finishes the run (finished=False, result=None) when molecules_stacked drops back below cap — the player keeps playing"
    - "reject_pickup NEVER un-finishes a crashed run"
    - "step()'s pinned event order moved->boundary->body->stacked->budget->won is untouched — Phase 2 tests stay green"
  artifacts:
    - path: "serpentrum/game_engine.py"
      provides: "Additive reject_pickup un-finish branch (G2 — the narrowest possible fix)"
    - path: "tests/test_engine_win_desync.py"
      provides: "Won-rollback un-finish pins + crash-stays-finished pins"
  key_links:
    - from: "serpentrum/game_engine.py reject_pickup"
      to: "serpentrum/gui_game.py stacked handler"
      via: "GUI calls reject_pickup on clash; engine un-finishes so _on_tick's finished-funnel skips _end_run"
      pattern: "reject_pickup"
---

<objective>
Close engine gap G2 (win-vs-clash desync) with the narrowest ADDITIVE change: 'won' fires in the SAME tick as the cap-reaching 'stacked', BEFORE the controller runs the clash gate — rejecting that capture rolls counters back but left finished=True/result='won', freezing a sub-cap run. Fix: reject_pickup clears finished/result when it rolls a WON run back below cap.

Purpose: GAME-06 (win at cap) must not trap the player in a dead 'won' state when the winning placement is clash-refused (STACK-05).
Output: additive engine branch + new test file; ALL Phase-2 pinned tests stay green.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (gap G2 — options analysis, recommendation (a)-variant)
@serpentrum/game_engine.py (step() capture/win block :698-731; reject_pickup :765-798)
@tests/test_engine_rules.py (pinned event order + reject-then-recapture :421-448 — MUST stay green unmodified)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — won-rollback un-finish tests</name>
  <files>tests/test_engine_win_desync.py</files>
  <action>
Create `tests/test_engine_win_desync.py` (unittest, python3.6; follow tests/test_engine_rules.py conventions). Pin:
1. **Desync reproduction (pre-fix):** engine with cap=1, one pickup on the path; step until ('stacked', rec) then ('won',) fire together (assert both in the same event list, finished=True, result='won', molecules_stacked=1); then `engine.reject_pickup(rec['id'], 'clash')` — assert the POST-fix contract: molecules_stacked back to 0, pickup re-armed (id back in live_pickup_ids), AND finished=False, result=None (run continues).
2. **Cap-recheck guard:** same scenario but with the pickup's atoms_n rolled back — assert the un-finish only happens when molecules_stacked < cap after rollback (with cap=1 the rollback drops 1->0 < 1 ✓; construct a cap=1, two-pickup variant where a SECOND capture is rejected after a first successful stack left molecules_stacked at cap... — simpler direct pin: engine at cap reached via a prior attach (molecules_stacked == cap, finished='won'), reject an unrelated just-captured pickup -> rollback drops below cap -> un-finish; and a case where rollback keeps molecules_stacked >= cap is impossible via reject (single rollback) — document why the cap guard exists).
3. **Crash stays finished:** engine crashed into boundary (finished=True, result='crashed'); a controller-style reject on some pickup id -> finished stays True, result stays 'crashed' (only 'won' un-finishes).
4. **No un-finish when not finished:** reject on a normal (non-won) run — finished stays False (trivially), counters roll back per the existing pinned contract (mirror test_engine_rules.test_reject_then_recapture semantics WITHOUT duplicating it).
5. **Resume play:** after the un-finish, step() moves the head again (events non-empty) — the run is truly live.
Run first — assertions 1/2/5 must FAIL against current code (finished stays True).
  </action>
  <verify>python3.6 -m unittest tests.test_engine_win_desync -v — tests 1, 2, 5 fail (desync reproduced), 3/4 may pass</verify>
  <done>Desync pinned as failing tests; crash-stays-finished guard also pinned</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — additive reject_pickup un-finish branch</name>
  <files>serpentrum/game_engine.py</files>
  <action>
Edit `reject_pickup` in serpentrum/game_engine.py — ADDITIVE ONLY, no changes to step(), start_sweep, _advance_sweep, attach_segment, or any constant:

After the counter-rollback block (the branch where the pickup was actually captured and is now re-armed — after `self.pickups_remaining += 1`, before the return), append:

```python
# G2 (phase 5): a cap-reaching capture can be clash-rejected by the
# controller AFTER step() already emitted ('won',) and set
# finished/result on the same tick (event order is test-pinned and
# unchanged). Rolling the counters back below cap makes a frozen
# 'won' run a desync — un-finish so play resumes. Crash results are
# NEVER un-finished (a crashed run has no sub-cap rollback semantics).
if (self.finished and self.result == 'won' and
        (self.cap is None or self.molecules_stacked < self.cap)):
    self.finished = False
    self.result = None
```

Note: the already-live early-return branch (double-reject guard) is untouched — it cannot be a won-rollback (nothing was rolled back). Update the reject_pickup docstring with one paragraph documenting the un-finish. python3.6 syntax.

Then run the FULL suite: `python3.6 tests/run_gates.py` — every Phase-2 pinned test (test_engine_core/rules/turns) must stay green UNMODIFIED. If any pinned test fails, your change leaked beyond the additive branch — fix the change, never the test.
  </action>
  <verify>python3.6 -m unittest tests.test_engine_win_desync -v (all pass) && python3.6 tests/run_gates.py (green — all 451+ existing tests unmodified and passing)</verify>
  <done>Won-rollback un-finishes below-cap runs; crashes stay finished; zero pinned-test regressions; full gates green</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_engine_win_desync -v` — green
- `python3.6 tests/run_gates.py` — green with the full existing suite intact
- `grep -c "events.append(('won',))" serpentrum/game_engine.py` — still exactly 1 (event order untouched)
</verification>

<success_criteria>
The win-vs-clash desync is closed additively: rejecting the cap-reaching capture resumes play, crashed runs never resume, and the Phase-2 referee event order is byte-identical. [G2, GAME-06]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-04-SUMMARY.md`
</output>
