---
phase: 05-stacking-game-rules
plan: 12
type: execute
wave: 3
depends_on: ["05-02", "05-04", "05-05", "05-08"]
files_modified:
  - tests/test_phase5_integration.py
autonomous: true

must_haves:
  truths:
    - "A full pure-layer simulated run reaches 'won' at cap with exact counters (molecules_stacked == cap, atoms_total == sum of captured atoms_n)"
    - "A crash path (boundary or body) ends the run with the chain complete — segments preserved, finished/result set"
    - "A biphenyl capture refuses (REFUSE_ATOM) and the run continues; an upload-shaped capture skips (SKIP_NO_ENTRY); a cap-reaching refusal resumes play via the 05-04 un-finish"
  artifacts:
    - path: "tests/test_phase5_integration.py"
      provides: "The pure integration chain: records -> spawner -> engine -> placement -> attach/reject across win/crash/refuse/skip scenarios"
  key_links:
    - from: "tests/test_phase5_integration.py"
      to: "serpentrum/placement.py resolve"
      to2: "serpentrum/game_engine.py step/attach_segment/reject_pickup"
      via: "the exact call order the GUI 'stacked' branch will use (plan 05-13)"
      pattern: "resolve"
---

<objective>
Pin the ENTIRE pure integration chain end-to-end before the GUI consumes it (the 02-14 pattern): real records + real dataset + spawner + engine + placement, driving scripted scenarios to win, crash, refuse, skip and un-finish. This is the executable specification of the controller seam — plan 05-13 implements exactly what these tests prove.

Purpose: every requirement's pure half is proven here with real shipped data; GUI plans stay thin glue.
Output: tests/test_phase5_integration.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (engine_contracts call order — this test mirrors it)
@serpentrum/game_engine.py, @serpentrum/placement.py, @serpentrum/spawn.py, @serpentrum/orientation.py, @serpentrum/setloader.py, @serpentrum/molfile.py
@tests/test_integration_pure_core.py (the Phase-2 integration-chain precedent to follow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: pure integration scenarios</name>
  <files>tests/test_phase5_integration.py</files>
  <action>
Create `tests/test_phase5_integration.py` (unittest, python3.6, zero stubs, REAL shipped data: setloader.load_demo_set(stacking_path=setloader.default_stacking_path()), molecule_data.load_stacking, molfile.read_sdf, orientation.edge_on_atoms, spawn.PickupSpawner with a fixed seed, game_engine.GameEngine). Shared fixture builder: records + records_by_id + atoms_by_id (edge-on'd) + stacking_data + box (medium preset ±18) + display_z 5.0 (matching pymol_bridge.BOX_DISPLAY_Z). A helper `capture(engine, pickup_rec, state)` implementing the EXACT controller seam order from 05-RESEARCH-core-integration.md engine_contracts: skip check -> tail frame -> attempt_place -> gate -> attach_segment(molecule_id, ring_centroid_xy, placed) OR reject_pickup(id, code) — returns the outcome dict (this helper is the spec plan 05-13 codes up).

Scenarios (each its own test method):
1. **Win at cap (GAME-06/GAME-04):** cap=3; seed 3+ pickups along the head's path (use the spawner or hand-placed centroids on a straight line, spacing > 6 Å); step the engine (dt=0.1) driving no turns until each capture fires; run `capture` per ('stacked',) event; assert: 3 attachments, molecules_stacked == 3, atoms_total == sum of the 3 atoms_n (head excluded), finished True result 'won', and every placed ring-centroid distance to its tail == 3.6000 within 1e-6 (STACK-01).
2. **Boundary crash leaves the snake complete (GAME-05):** box small preset ±12, no cap; head steered straight into the wall (no pickups needed, or one); assert ('crashed', 'boundary') fires, finished/result set, segments list unchanged afterwards, and step() is a no-op [].
3. **Body crash (GAME-05, segment-based):** attach 4 segments behind the head via scripted captures (head moving +x, chain trailing -x), then steer two 90° turns to loop the head back into the chain; assert ('crashed', 'body') fires and the chain stays complete. (If scripting the loop-back proves flaky, instead directly seed segments via the constructor test-seam exactly as test_engine_rules does and drive the head into the polyline — the engine model is already pinned; THIS test proves the placement-produced chain geometry reaches the collision model intact.)
4. **Biphenyl refuse (STACK-05, locked decision 6):** biphenyl as the captured pickup over any set_a tail -> capture returns status 'refused', code REFUSE_ATOM; assert engine.reject_pickup was invoked by the helper (counters rolled back, pickup re-armed, refusal count 1) and molecules_stacked unchanged. Document in a comment: shipped biphenyl rings are at 90.00°, every pairwise stack clashes (0.688-2.145 Å < 2.5) — biphenyl is the permanent refuse-path demonstrator; the DATA observation is recorded, NOT fixed.
5. **Upload skip (STACK-03, locked decision 7):** a record {'id': 'upload_0', 'set': '__upload__', 'has_stack_entry': False, ...} as the captured pickup -> capture returns 'skipped'/SKIP_NO_ENTRY; reject_pickup invoked; counters net zero.
6. **Win-refusal un-finish (G2/05-04):** cap=1; the ONLY pickup is biphenyl; capture refuses -> assert finished False again (un-finished), result None, and the engine keeps stepping (head moves on the next step).
7. **Sweep-rotated tail frame (GAME-10 + STACK-01 interplay):** after attaching 1 segment, open a 90° sweep (start_sweep), run all 6 ticks, then capture a second pickup: the tail frame comes from the sweep-rotated atoms — assert the placed centroid is consistent with the ROTATED growth direction (dot(new_growth, rotated_old_growth) > 0.99) and ring_frame on the rotated atoms does not raise (planarity invariant under rotation).
8. **Spawn determinism across a full run:** the spawner's positions used in scenario 1 reproduce byte-identically on a second identical run.
Run gates after writing.
  </action>
  <verify>python3.6 -m unittest tests.test_phase5_integration -v (all 8 scenarios green) && python3.6 tests/run_gates.py (green)</verify>
  <done>The complete pure game loop — win, crash x2, refuse, skip, un-finish, sweep-consistent stacking, determinism — proven against real shipped data</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_phase5_integration -v` — green
- `python3.6 tests/run_gates.py` — green
- Scenario 1's 3.6000 Å assertions tie STACK-01 to the engine counters in one chain
</verification>

<success_criteria>
The pure integration chain replicates the GUI controller seam exactly and proves all five phase success criteria's pure halves on real data — win at cap, complete-snake crashes, cited-geometry stacking, refuse/skip policy, and the G2 un-finish. [GAME-04/05/06, STACK-01/03/05 pure proof; GAME-10 interplay]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-12-SUMMARY.md`
</output>
