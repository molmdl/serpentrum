---
phase: 05-stacking-game-rules
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/orientation.py
  - tests/test_orientation.py
autonomous: true

must_haves:
  truths:
    - "matrix_rt produces the exact 16-float TTT layout PyMOL 2.5.0 transform_selection accepts (y = R.(x+pre)+t), pinned to the probe-A1 fixture"
    - "edge_on_m16 maps every demo molecule's ring normal to +x, the LONGER in-plane ring axis to +y (screen-visible lateral/ref axis), the shorter to +z, and the ring centroid to the origin"
    - "Post-edge-on z-spans match the research's 'best' column (benzene 4.297 A ... phenanthrene 7.144 A), keeping BOX_DISPLAY_Z=5.0 valid"
  artifacts:
    - path: "serpentrum/orientation.py"
      provides: "matrix_rt, mat_vec3, edge_on_frame, edge_on_m16, edge_on_atoms — pure orientation math (G5)"
      contains: "def matrix_rt"
    - path: "tests/test_orientation.py"
      provides: "Probe-A1 matrix fixture + edge-on invariants + z-span anchors for all 5 molecules"
  key_links:
    - from: "serpentrum/orientation.py edge_on_frame"
      to: "serpentrum/stacking.py ring_frame"
      via: "ring frame (centroid/normal/ref) is the input basis"
      pattern: "ring_frame"
---

<objective>
Create the PURE orientation module `serpentrum/orientation.py` (G5): the verified TTT matrix builder (`matrix_rt`) + the edge-on canonicalization (`edge_on_m16` / `edge_on_atoms`) that maps ring planes perpendicular to the screen xy plane — the LOCKED 03-08 + 04-07 presentation decision that MUST land at materialization BEFORE any stacking works.

Purpose: every molecule object (head AND pickups) gets transformed exactly once at load into the canonical edge-on pose so `place_pickup`'s tail frame needs no viewer queries and stacks grow in the visible movement plane.
Output: orientation.py + pinning tests (probe-A1 fixture + z-span anchors).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (matrix verdict + Pattern 1 edge-on scheme + probe F)
@serpentrum/stacking.py (ring_frame returns (centroid, normal, ref))
@serpentrum/molfile.py (read_sdf record: elements/coords/bonds keys)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — matrix layout + edge-on invariant tests</name>
  <files>tests/test_orientation.py</files>
  <action>
Create `tests/test_orientation.py` (unittest, python3.6, zero stubs, real SDFs via molfile.read_sdf). Pin:
1. **Probe-A1 fixture (LOCKED layout):** `matrix_rt(R, t, pre)` with R = RotZ(90°) = ((0,1,0),(-1,0,0),(0,0,1)), t = (1.0, 2.0, 3.0), pre = (0,0,0) returns EXACTLY `[0.0, 1.0, 0.0, 1.0, -1.0, 0.0, 0.0, 2.0, 0.0, 0.0, 1.0, 3.0, 0.0, 0.0, 0.0, 1.0]` (16 floats, row-major R in cols 0-2, post-translation col 3, pre-translation bottom row — y = R.(x+pre)+t). Also assert matrix_rt(R, t, pre=(0.1, 0.2, 0.3)) puts pre in slots 12-14 and 1.0 in slot 15.
2. **Pivot idiom:** rotating (3,0,0) about pivot (1,0,0) by +90° via matrix_rt(RotZ90, pivot, pre=neg(pivot)) then mat_vec3 gives (1.0, 2.0, 0.0) (probe-A2 pattern, 1e-9).
3. **Edge-on invariants** for ALL 5 demo molecules (benzene, naphthalene, anthracene, phenanthrene, biphenyl; coords from read_sdf, ring = molfile.ring_cycle from plan 05-01 — import it; if 05-01 has not landed in your worktree yet, note the dependency and implement against its contract, reusing the pinned tuples): `edge_on_frame(elements, coords, ring)` returns (R_edge, pre) where:
   - normal maps to +x: mat_vec3(R_edge, normal) == (1.0, 0.0, 0.0) within 1e-9;
   - pre == neg(ring centroid) within 1e-12;
   - applying to atoms (edge_on_atoms) puts the ring centroid at origin (mean of ring atoms ≈ (0,0,0) within 1e-9) and every atom's ring-plane normal coordinate (dot with +x) ≈ 0 within 1e-9 for ring atoms.
4. **Azimuth rule (PINNED — planner decision):** of the two in-plane ring axes, the LONGER molecule span lands on +y (the visible screen axis carrying the dataset's lateral offset), the SHORTER on +z. Assert for each molecule: z-span(all atoms after edge-on) ≈ the research 'best' values within 2e-3 — benzene 4.297, naphthalene 5.547, anthracene 6.770, phenanthrene 7.144, biphenyl 4.317 — and z-span <= y-span + 1e-9 (longer axis on y).
5. **edge_on_atoms(elements, coords, ring)** returns [(sym, x, y, z), ...] with symbols preserved and the transform applied exactly (benzene: all atoms z within ±2.149 of 0 — half of 4.297 span).
6. **edge_on_m16(elements, coords, ring)** returns the 16-float list equivalent to matrix_rt(R_edge, (0,0,0), pre).
Run first — confirm RED (module missing).
  </action>
  <verify>python3.6 -m unittest tests.test_orientation -v — fails with ImportError (module missing)</verify>
  <done>Test file pins the A1 fixture, pivot idiom, edge-on invariants, azimuth/z-span anchors, and atoms transform — all RED</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — implement orientation.py</name>
  <files>serpentrum/orientation.py</files>
  <action>
Create `serpentrum/orientation.py` — PURE module (stdlib math only; auto-classified PURE by tools/check_purity.py — no registration needed; python3.6 %-formatting; module docstring states the verified matrix verdict: 16 floats, y = R.(x+pre)+t, layout confirmed by headless probes A1/A2 at <=1.2e-07 A, source pymol-src editing.py:1962-1988).

Functions:
- `matrix_rt(R, t, pre=(0.0, 0.0, 0.0))` — build the 16-float list per the locked layout (P5-8: this ONE helper is the only matrix composer; unit test pins its floats).
- `mat_vec3(R, v)` — row-major 3x3 times 3-vector.
- `edge_on_frame(elements, coords, ring_indices)` — compute (R_edge, pre):
  1. (centroid, normal, ref) = stacking.ring_frame(coords, ring_indices) — coords as (x,y,z) floats.
  2. Project ALL atoms onto ref and onto r2 = cross(normal, ref): span_ref = max-min projection on ref, span_r2 likewise.
  3. Choose the in-plane basis so the LONGER span axis becomes ref_out (the +y axis) and the shorter becomes r2_out (the +z axis): if span_r2 > span_ref, swap roles (ref_out = r2 direction, r2_out = ref direction) — i.e. B = (r1, r2, n) with r1 = the longer-span in-plane unit axis, r2 = cross(n, r1).
  4. C = ((0,0,1),(0,1,0),(1,0,0)) (r1->+y? NO — C maps B columns to world axes: r1->+y, r2->+z, n->+x): R_edge = C . B^T computed via the basis-tuple math (rows of R_edge = world-axis expressions; implement as: R_edge maps local coords so that n->+x, r1->+y, r2->+z; verify with mat_vec3 in tests).
  5. pre = neg(centroid). Return (R_edge, pre) with R_edge a 3x3 tuple-of-tuples.
- `edge_on_atoms(elements, coords, ring_indices)` — [(sym, R_edge.(p+pre)) for each atom] as (sym, x, y, z) tuples.
- `edge_on_m16(elements, coords, ring_indices)` — matrix_rt(R_edge, (0,0,0), pre).

Docstring MUST record the planner-pinned azimuth rule and why (longer in-plane axis carries the visible lateral offset; z keeps the SHORTER span so BOX_DISPLAY_Z=5.0 holds — locked decision 4 keeps 5.0, one-constant fallback note only, no checkpoint).
Import ONLY `from . import stacking` (+ math). No numpy. Run gates.
  </action>
  <verify>python3.6 -m unittest tests.test_orientation -v (all pass) && python3.6 tests/run_gates.py (green)</verify>
  <done>orientation.py passes all pins: A1 fixture exact, edge-on normal->+x, longer-span->+y with research z-span anchors matched within 2e-3, atoms transform exact</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_orientation -v` — green
- `python3.6 tests/run_gates.py` — green (orientation.py auto-classified PURE)
- Benzene z-span anchor 4.297 A ± 2e-3 reproduced (cross-checks the research probe F numbers)
</verification>

<success_criteria>
The verified TTT matrix layout and the edge-on canonicalization exist as pure, unit-pinned functions; applying edge_on_m16 to any demo molecule yields normal->+x / longer-axis->+y / shorter->+z with ring centroid at origin. [G5 pure half; prerequisite for edge-on-before-stacking (locked decision 3)]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-02-SUMMARY.md`
</output>
