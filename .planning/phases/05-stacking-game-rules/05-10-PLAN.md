---
phase: 05-stacking-game-rules
plan: 10
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-05", "05-07"]
files_modified:
  - serpentrum/pymol_bridge.py
  - serpentrum/gui_setup.py
  - smoke/08_stack_place_smoke.py
  - tests/run_gates.py
autonomous: true

must_haves:
  truths:
    - "The head materializes EDGE-ON at Apply (transform BEFORE place_head) — the locked 03-08/04-07 presentation lands before any stacking can work"
    - "A pickup materializes edge-on at its spawn centroid from a caller-supplied m16 (bridge never parses SDFs — discipline preserved)"
    - "Headless real-data verification: benzene edge-on z-span == pure prediction; a dataset-driven placement lands at 3.6000 A ring-centroid distance with pure coords == viewer coords"
  artifacts:
    - path: "serpentrum/pymol_bridge.py"
      provides: "materialize(setup, records, head_m16=None) edge-on step + materialize_pickup(path, name, m16)"
      contains: "def materialize_pickup"
    - path: "serpentrum/gui_setup.py"
      provides: "_on_apply computes head_m16 (pure) and passes it to materialize"
      contains: "edge_on_m16"
    - path: "smoke/08_stack_place_smoke.py"
      provides: "REQUIRED smoke: real-data edge-on + 3.6000 A placement end-to-end (probe F regression)"
    - path: "tests/run_gates.py"
      provides: "smoke 08 added to REQUIRED_SMOKES"
  key_links:
    - from: "serpentrum/pymol_bridge.py materialize"
      to: "cmd.transform_selection"
      via: "edge-on m16 applied BEFORE place_head extent-centering (Pattern 1 ordering)"
      pattern: "transform_selection"
---

<objective>
Land the edge-on orientation in the viewer (G5 viewer half + G7 part 2): `materialize` applies the pure edge-on matrix to the HEAD at Apply time (before extent-centering), and a new `materialize_pickup` spawns pickups edge-on at their spawn centroids. Then prove the whole stacking chain END-TO-END headless with REAL dataset values — the research's probe F replicated through the shipping code path.

Purpose: locked decision 3 — edge-on at materialization BEFORE any stacking works; the 04-07 user remark (head currently displays flat rings → pickups would contact via hydrogen edge) is resolved here.
Output: bridge/gui_setup edits + REQUIRED smoke 08.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (Pattern 1 ordering note: edge-on FIRST then place_head; probe F exactness; F5 pure==viewer)
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (pickup stick rendering + materialize patterns)
@serpentrum/pymol_bridge.py (materialize :247, place_head extent semantics, load_molecule zoom=0)
@serpentrum/orientation.py (edge_on_m16 / edge_on_atoms from plan 05-02)
@serpentrum/placement.py (resolve/attempt_place from plan 05-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: edge-on head materialization + pickup materializer</name>
  <files>serpentrum/pymol_bridge.py, serpentrum/gui_setup.py</files>
  <action>
1. pymol_bridge.materialize(setup, records, head_m16=None): add the optional keyword. After `load_molecule(record['file'], HEAD_NAME)` and BEFORE `place_head(HEAD_NAME)`: `if head_m16 is not None: cmd.transform_selection(HEAD_NAME, head_m16)` then `cmd.show('spheres', HEAD_NAME)` (existing) then place_head. Docstring: edge-on FIRST (locked 03-08) so the extent being centered is the canonical one (Pattern 1); head_m16 is computed by the CALLER from pure modules (the bridge never parses SDFs — docstring discipline kept).
2. pymol_bridge.materialize_pickup(path, name, m16): `cmd.load(path, object=name, zoom=0)` + `cmd.transform_selection(name, m16)` + `cmd.show('sticks', name)` (GAME-03/roadmap: pickups render as STICKS). Returns name. Docstring: m16 = edge-on + spawn-translation composed by the caller as matrix_rt(R_edge, centroid, pre) — y = R.(x+pre)+t lands the ring centroid at the spawn centroid.
3. gui_setup._on_apply: after selecting... compute head_m16 BEFORE calling materialize: when records non-empty, pick the SAME record materialize will pick — simplest correct approach: compute the m16 for `_select_head_record`'s choice by calling `pymol_bridge._select_head_record(setup, records, errors)` once more (it is pure-with-respect-to-cmd — no cmd usage; reuse the returned record) and, when it has 'stack_ring': `parsed = molfile.read_sdf(record['file'])[0]`; `head_m16 = orientation.edge_on_m16(parsed['elements'], parsed['coords'], record['stack_ring'])`; pass `materialize(setup, records, head_m16=head_m16)`. Wrap the computation in try/except Exception -> head_m16=None (a malformed head record degrades to today's flat head rather than blocking Apply; the error list already surfaces load problems). gui_setup imports: `from . import molfile, orientation` (pure relative imports, GUI-class legal).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep: materialize signature has head_m16=None; gui_setup passes head_m16; no SDF parsing inside pymol_bridge</verify>
  <done>Head materializes edge-on at Apply; pickup materializer exists (sticks); bridge stays parse-free</done>
</task>

<task type="auto">
  <name>Task 2: REQUIRED smoke 08 — real-data edge-on + placement end-to-end</name>
  <files>smoke/08_stack_place_smoke.py, tests/run_gates.py</files>
  <action>
Create `smoke/08_stack_place_smoke.py` (headless Windows PyMOL, sentinel-verdict style like smoke 07; imports the pure modules: molfile, orientation, placement, molecule_data, setloader, stacking — all importable under PyMOL's python). Steps:
a. EDGEON: load serpentrum/data/benzene.sdf as srp_pickup_benzene via pymol_bridge.load_molecule; compute edge-on m16 purely; pymol_bridge.apply_matrix; measure the object's z-extent via cmd.get_extent and assert == the pure edge_on_atoms z-span within 1e-3, and == 4.297 within 2e-3 (research anchor). Flush `SMOKE-OK EDGEON`.
b. PLACE360 (the STACK-01 proof): build a two-benzene scenario in PURE code first: tail atoms = edge_on_atoms(benzene), head-frame tail via placement.tail_frame([], ...) with heading (1,0); pickup atoms = same edge-on atoms; resolve via placement.resolve-equivalent path (attempt_place with the REAL dataset interaction) -> (placed, R, t, ring_centroid). Assert pure placed-vs-tail ring-centroid distance == 3.6000 within 1e-6. THEN materialize the pickup object (edge-on m16 at origin), apply matrix_rt(R, t) via pymol_bridge.apply_matrix, read the viewer's two ring centroids via cmd.get_model (atom indices per stack_ring), and assert the viewer distance == 3.6000 within 1e-3 AND sorted/rounded pure-vs-viewer placed coords match within 5e-3 (probe F5 pattern). Flush `SMOKE-OK PLACE360`.
c. STICKS: assert the pickup object shows sticks (cmd.get-rep check is flaky headless — instead assert materialize_pickup ran cmd.show without error and the object exists; keep the assertion on object existence + no exception). Flush `SMOKE-OK PICKUPS`.
Cleanup srp_* at start; final `print('SMOKE-08 DONE')`; failed steps omit their sentinel. Register `'smoke/08_stack_place_smoke.py'` in tests/run_gates.py REQUIRED_SMOKES. Run `python3.6 tests/run_gates.py --smoke`.
  </action>
  <verify>python3.6 tests/run_gates.py --smoke — SMOKE-OK EDGEON, SMOKE-OK PLACE360, SMOKE-OK PICKUPS present; 7/7 required smokes green</verify>
  <done>Edge-on + dataset-exact placement proven end-to-end in real headless PyMOL through the shipping code path</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- `python3.6 tests/run_gates.py --smoke` — 7/7 required smokes green (01,03,04,05,06,07,08)
- Viewer placed distance 3.6000 Å == dataset sqrt(3.383²+1.231²) — the phase success criterion 1 core claim, machine-verified
</verification>

<success_criteria>
Molecules materialize edge-on (head at Apply, pickups at spawn), pickups render as sticks, and the dataset-driven placement lands at the cited 3.6000 Å in the real viewer with pure==viewer coordinates. [G5 viewer half, G7 part 2; STACK-01 viewer proof; locked decision 3]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-10-SUMMARY.md`
</output>
