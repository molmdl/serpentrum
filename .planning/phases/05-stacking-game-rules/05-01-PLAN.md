---
phase: 05-stacking-game-rules
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/molfile.py
  - tests/test_ring_cycle.py
autonomous: true

must_haves:
  truths:
    - "ring_cycle(record) returns ONE planar aromatic 6-ring in ring-walk order for every shipped demo molecule, deterministically"
    - "Feeding the extracted cycle to stacking.ring_frame passes the 0.15 A planarity check for all 5 demo molecules (benzene included)"
    - "Feeding the raw manifest ring_atoms (sorted 2-core) to ring_frame still fails for benzene — the trap is proven, not assumed"
  artifacts:
    - path: "serpentrum/molfile.py"
      provides: "ring_cycle(record) — bounded-DFS canonical 6-cycle extractor next to find_ring_atoms"
      contains: "def ring_cycle"
    - path: "tests/test_ring_cycle.py"
      provides: "Pins the 5 canonical tuples + sorted-order-fails regression + determinism"
  key_links:
    - from: "serpentrum/molfile.py ring_cycle"
      to: "serpentrum/molfile.py find_ring_atoms"
      via: "2-core restricts the adjacency before cycle search"
      pattern: "find_ring_atoms"
---

<objective>
Add `molfile.ring_cycle(record)` — the pure canonical 6-ring extractor that turns the manifest's SORTED 2-core `ring_atoms` into ONE planar ring IN RING-WALK ORDER (G1). This is the mandatory shim before any `stacking.ring_frame` call: manifest order fails planarity even for flat benzene (spurious 1.133 Å deviation from star-polygon Newell normal).

Purpose: STACK-01 placement consumes ring frames; without ring order the first benzene pickup raises ValueError at game start (P5-1).
Output: `ring_cycle` in molfile.py + a pinning test file with the 5 probe-verified canonical tuples.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-core-integration.md (section "ring_extraction_spec" — the algorithm + verified tuples)
@serpentrum/molfile.py (find_ring_atoms at :524 — the 2-core this builds on)
@serpentrum/stacking.py (ring_frame contract: ring_indices MUST be in ring order)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — failing tests pinning the canonical cycles</name>
  <files>tests/test_ring_cycle.py</files>
  <action>
Create `tests/test_ring_cycle.py` (unittest, python3.6 syntax, %-formatting; follow tests/test_molfile.py conventions — import `serpentrum.molfile` and `serpentrum.stacking` directly, zero stubs). Test data: load the real shipped SDFs via `molfile.read_sdf('serpentrum/data/<name>.sdf')` (single record each).

Pin these EXACT behaviors:
1. Canonical tuples (locked, probe-verified in 05-RESEARCH-core-integration.md): benzene `(0, 1, 3, 5, 4, 2)`; naphthalene `(0, 1, 3, 7, 6, 2)`; anthracene `(0, 1, 5, 3, 2, 4)`; phenanthrene `(0, 1, 3, 5, 4, 2)`; biphenyl `(0, 2, 6, 10, 8, 4)`. Call: `molfile.ring_cycle(record)` where record is `read_sdf(path)[0]`.
2. THE TRAP REGRESSION: `stacking.ring_frame(benzene['coords'], benzene_record['ring_atoms'])`... — manifest ring_atoms are NOT on the molfile record; construct the sorted 2-core via `molfile.find_ring_atoms(record)` and assert `stacking.ring_frame(record['coords'], find_ring_atoms(record))` RAISES ValueError for benzene (spurious non-planarity), while `ring_frame(record['coords'], ring_cycle(record))` does NOT raise.
3. Planarity pass: for ALL 5 molecules, `stacking.ring_frame(record['coords'], molfile.ring_cycle(record))` returns a 3-tuple (centroid, normal, ref) without raising (0.15 Å planarity holds).
4. Biphenyl: the extracted cycle is planar even though the full 2-core (12 atoms, rings at 90.00°) is not — assert ring_cycle returns a 6-element list and ring_frame passes.
5. Determinism: calling ring_cycle twice on the same record returns identical lists.
6. Acyclic input: a synthetic ethanol-like record (no rings) → ring_cycle returns [] (or None — assert whichever the implementation contract below pins: []).
Write tests FIRST, run `python3.6 -m unittest tests.test_ring_cycle -v`, confirm they FAIL (ImportError/AttributeError counts as RED).
  </action>
  <verify>python3.6 -m unittest tests.test_ring_cycle -v — all tests fail with AttributeError (ring_cycle missing) or assertion errors, never pass</verify>
  <done>Test file exists, exercises real shipped SDFs, and is RED for the right reason (missing ring_cycle)</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — implement ring_cycle per the verified algorithm</name>
  <files>serpentrum/molfile.py</files>
  <action>
Add `ring_cycle(record)` to serpentrum/molfile.py next to find_ring_atoms (keep it PURE: stdlib only — the file is auto-classified PURE; python3.6 %-formatting; ~30 lines per the research spec):

1. `core = find_ring_atoms(record)`; build adjacency RESTRICTED to core indices from `record['bonds']` (skip bonds touching non-core atoms).
2. Enumerate simple cycles of length <= 6 via bounded DFS from each start node with `nb > start` pruning (prototype exists in tmp/p5_pymol_probe.py `dfs()/cycle` — reimplement cleanly; n <= 14, trivially fast).
3. Canonical selection: shortest length first; tie-break by lexicographic MINIMUM over all 2n rotation/reversal normalizations (rotate so the smallest index is first, then take min(cycle_tuple, reversed_tuple)). This fixes BOTH the starting atom and the orientation deterministically (reversal flips the ring_frame normal — the rule must pin it).
4. If no cycle of length 3..6 exists (e.g. only 5-membered rings), return `[]`.
5. Docstring: state the contract (returns ONE planar 6-ring in ring-walk order; manifest ring_atoms are the sorted 2-core and MUST NOT be fed to ring_frame directly; biphenyl yields one phenyl ring — matching stacking.py's docstring intent).

Do NOT modify find_ring_atoms, gate_set, or any reader. Do NOT touch stacking.py (call as-is). Run the FULL suite after: `python3.6 tests/run_gates.py` must stay green (existing 451 tests + new file).
  </action>
  <verify>python3.6 -m unittest tests.test_ring_cycle -v (all pass) && python3.6 tests/run_gates.py (all gates green, no regressions)</verify>
  <done>All 5 canonical tuples pinned; benzene sorted-order-fails / walk-order-passes regression green; determinism + acyclic cases green; full gate suite green</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest tests.test_ring_cycle -v` — green
- `python3.6 tests/run_gates.py` — green (syntax + plugin-path safety + AST purity + full unittest discovery)
- The 5 pinned tuples byte-match the research doc's probe-verified values
</verification>

<success_criteria>
ring_cycle exists in molfile.py (PURE), returns the pinned canonical 6-cycles for all shipped demo molecules, [] when no 6-ring exists, and its output passes stacking.ring_frame's planarity check — while the raw 2-core demonstrably fails for benzene. [G1; unlocks STACK-01]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-01-SUMMARY.md`
</output>
