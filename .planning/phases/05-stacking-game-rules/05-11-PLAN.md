---
phase: 05-stacking-game-rules
plan: 11
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-06", "05-08", "05-10"]
files_modified:
  - serpentrum/gui_game.py
autonomous: true

must_haves:
  truths:
    - "begin_game builds the edge-on head-atom mirror, spawns the first pickup deterministically, seeds GameEngine(pickups=...) and materializes the pickup object as sticks"
    - "The controller's head_atoms stay exactly equal to the viewer's srp_head coordinates (edge-on + extent-center + per-tick translate, all mirrored in pure math)"
    - "_teardown_round deletes live pickup objects (srp_pickup_*) — pickup teardown folded INTO the ONE helper (locked decision 8), chain objects untouched"
  artifacts:
    - path: "serpentrum/gui_game.py"
      provides: "begin_game records/anchor consumption, _build_engine pickups seeding, head_atoms mirror + moved-translation, teardown pickup deletion"
      contains: "head_atoms"
  key_links:
    - from: "serpentrum/gui_game.py _build_engine"
      to: "serpentrum/spawn.py PickupSpawner"
      via: "deterministic seeds: spawner.first() -> build_pickup_seed -> GameEngine(pickups=[seed]) + materialize_pickup"
      pattern: "PickupSpawner"
    - from: "serpentrum/gui_game.py _teardown_round"
      to: "serpentrum/pymol_bridge.py delete_pickups"
      via: "step (f): pattern-delete srp_pickup_* inside the single teardown"
      pattern: "delete_pickups"
---

<objective>
Wire the game-start half of Phase 5 into GameTab (G4 consumption + G3 + head mirror): begin_game reads the anchored records/stacking_data, rebuilds the head edge-on pose (pure mirror == viewer), spawns + materializes the first pickup, seeds the engine with real pickup records, and folds pickup deletion into `_teardown_round`.

Purpose: without this, the engine's capture leg has nothing to capture and Restart cannot reconstruct the scene deterministically.
Output: gui_game.py begin_game/teardown extension (GUI class — pymol.Qt only; logic already lives in the pure plans).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (lifecycle_spec items 1-3, teardown_integration items 2-5)
@.planning/phases/04-game-loop-input/04-08-SUMMARY.md (the locked teardown contract this extends)
@serpentrum/gui_game.py (begin_game :157, _build_engine :187, _teardown_round :421, _on_tick moved branch :254)
@serpentrum/orientation.py (edge_on_atoms, matrix_rt), @serpentrum/spawn.py (PickupSpawner, build_pickup_seed)
@serpentrum/molfile.py (read_sdf), @serpentrum/pymol_bridge.py (materialize_pickup, delete_pickups, place_head)
</context>

<tasks>

<task type="auto">
  <name>Task 1: begin_game — head mirror, spawn, seed, materialize</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Extend GameTab (python3.6, GUI purity — pymol.Qt only at module level; pure relative imports of game_engine/hud_logic/setup_logic already exist; ADD `from . import molfile, orientation, placement, spawn as spawn_mod`):

1. New helper `_build_head_state(self, records_by_id, setup)` -> (head_atoms, head_stack_ring) or (None, None): select the head record the SAME way materialize does (`pymol_bridge._select_head_record(setup, records, errors)` — pass a local errors list); if None or no stack_ring -> (None, None). Else: parsed = molfile.read_sdf(record['file'])[0]; atoms = orientation.edge_on_atoms(parsed['elements'], parsed['coords'], record['stack_ring']); extent-center in PURE math (min/max per axis over atoms -> midpoint; subtract) — replicating place_head exactly; return (atoms, record['stack_ring']).
2. `_reset_head_viewer(self, records_by_id, setup)`: re-apply the edge-on pose to srp_head when the object exists — compute head_m16 = orientation.edge_on_m16(...) for the head record; `pymol_bridge.apply_matrix(pymol_bridge.HEAD_NAME, head_m16)` (guarded by pymol_bridge.object_exists) THEN `pymol_bridge.place_head(pymol_bridge.HEAD_NAME)` (replaces the existing bare place_head call in begin_game — this is the restart pose reset: sweeps rotate the head's atomic coords, so every begin_game must restore the canonical pose before centering).
3. `_build_engine(self, setup)` extension: after building the engine kwargs, build pickups:
   - records = getattr(self._anchor, 'records', None) or []; stacking_data = getattr(self._anchor, 'stacking_data', None); if not records -> log 'no records anchored - play without pickups' and keep pickups=None (degraded but non-crashing).
   - head_atoms/head_stack_ring via _build_head_state; atoms_by_id = {r['id']: orientation.edge_on_atoms(molfile.read_sdf(r['file'])[0]...) for demo records WITH stack_ring} (origin-centered — edge_on_atoms already centers the ring centroid at origin).
   - spawner = spawn_mod.PickupSpawner(records, (x0, y0), (x1, y1), spawn_mod.seed_from_setup(setup), atoms_by_id); result = spawner.first((0.0, 0.0), (1.0, 0.0)); if result: record, pid, centroid = result; seed = spawn_mod.build_pickup_seed(record, pid, centroid, translated atoms); pass pickups=[seed] to GameEngine.
   - Session additions (anchor stays _serpentrum.game_session): 'head_atoms', 'head_stack_ring', 'records_by_id' ({r['id']: r}), 'spawner', 'stacked_history' ([]), 'live_pickup_names' ([]), 'last_turn_delta' (0.0).
4. begin_game ordering (AFTER `self._session = {...}` creation, BEFORE the countdown): materialize spawned pickups — for each engine pickup record: m16 = orientation.matrix_rt(R_edge, (cx, cy, 0.0), pre) where (R_edge, pre) come from a fresh edge_on computation for that record; name = 'srp_pickup_%s' % seed['id']; pymol_bridge.materialize_pickup(record['file'], name, m16); append name to session['live_pickup_names']. One-shot `pymol_bridge.frame_scene()` so the box+head+pickup are framed (zoom=0 on loads; one-shot framing is the 03-06 contract — NOT per-tick).
5. `moved` branch of `_on_tick`: after the existing move_head_delta call, translate the mirror: session['head_atoms'] = [(sym, x+dx, y+dy, z) for ...] with (dx, dy) = the SAME delta passed to move_head_delta (pure truth stays equal to viewer coords).
Keep _begin_play/_apply_pause_state/request_auto_pause byte-identical.
  </action>
  <verify>python3.6 tests/run_gates.py — green (syntax + purity: gui_game stays GUI-class, no numpy/PyQt5-direct); grep audit: `grep -c "lock_camera\|unlock_camera" serpentrum/gui_game.py` still 1 call site each</verify>
  <done>begin_game seeds a deterministic pickup, materializes it edge-on as sticks, mirrors head atoms purely, resets the head pose on restart; gates green</done>
</task>

<task type="auto">
  <name>Task 2: teardown pickup cleanup (INTO the single helper)</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Extend `_teardown_round` (locked 04-08: this is THE extension point — NEVER a second helper):
- After the existing unlock step (d) and BEFORE the pause-button reset (e), add step (f): `pymol_bridge.delete_pickups()` (pattern-delete srp_pickup_*; idempotent like unlock; srp_head/srp_seg_* untouched — on the _end_run path teardown runs BEFORE the completion presenter, which is exactly the GAME-09 'viewer clears' semantics: un-stacked pickups die, the snake stays complete).
- Keep every existing step byte-identical (timers -> epoch -> input teardown -> unlock via popped saved_cam -> blockSignals reset). Do NOT touch the camera-lock audit invariants (exactly one lock/unlock call site each).
Also extend the class docstring's Phase-5 note (pickups + completion are now live).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep audit: `grep -n "delete_pickups" serpentrum/gui_game.py` shows exactly one call site inside _teardown_round; the four teardown call sites (begin_game/_on_restart/_end_run/shutdown) unchanged</verify>
  <done>Every end path now clears live pickups through the ONE teardown helper; chain objects survive; gates green</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- Lifecycle grep audit: one lock_camera call site, one unlock_camera call site, one game_input.teardown call site, one delete_pickups call site — all inside their locked homes
- Restart determinism reasoning check (code read): same setup -> same crc32 seed -> same first spawn (no per-run RNG leakage)
</verification>

<success_criteria>
begin_game produces a deterministic, edge-on, pickup-seeded game with a pure head-atom mirror equal to viewer truth, and every end path clears pickups via the single teardown helper. [G4 consume, G3 consume, G5 apply, GAME-04 setup; locked decisions 8/10/11]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-11-SUMMARY.md`
</output>
