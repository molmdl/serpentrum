---
phase: 05-stacking-game-rules
plan: 14
type: execute
wave: 5
depends_on: ["05-13"]
files_modified:
  - serpentrum/gui_game.py
autonomous: true

must_haves:
  truths:
    - "Each ('turning',) tick applies ONE rigid cmd.rotate of angle_signed/TURN_TICKS about the head pivot to srp_head + all srp_seg_* (camera=0, explicit origin) — the whole chain sweeps as a rigid body on screen"
    - "The controller's head_atoms mirror rotates by the same per-tick delta, staying exactly equal to viewer truth for the next tail frame and the clash gate"
    - "The sweep-final tick (sweeping already cleared) reuses the stored per-tick delta so tick 6 still rotates"
    - "('budget_warning',) logs a count-free advisory line (GAME-04 hidden counts preserved)"
  artifacts:
    - path: "serpentrum/gui_game.py"
      provides: "'turning' branch (bridge sweep + head mirror + last_turn_delta store) and 'budget_warning' branch"
      contains: "sweep_chain"
  key_links:
    - from: "serpentrum/gui_game.py turning branch"
      to: "serpentrum/pymol_bridge.py sweep_chain"
      via: "ONE call per tick, delta = engine.sweeping['angle_signed']/TURN_TICKS, pivot = engine.head"
      pattern: "sweep_chain"
    - from: "serpentrum/gui_game.py turning branch"
      to: "serpentrum/game_engine.py _rotate_xy"
      via: "the SAME rotation primitive mirrors head_atoms x/y about (hx, hy)"
      pattern: "_rotate_xy"
---

<objective>
Implement the GAME-10 viewer half: the 'turning' branch that renders the engine's rigid sweep (one bridge call per tick, sign from the engine) plus the pure head-atom rotation mirror; and the 'budget_warning' advisory branch. The engine owns ALL sweep math/time (locked decision: engine emits ('turning', frac); GUI adds ONE rotation call per tick, ±15° about the head, sign from the engine).

Purpose: success criterion 2 — a turn visibly sweeps the entire chain as a rigid body.
Output: two _handle_event branches (GUI class).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stacking-game-rules/05-RESEARCH-pymol-mechanics.md (Pattern 2 sweep-includes-head; P5-4 origin trap; one-call-per-tick perf)
@.planning/phases/05-stacking-game-rules/05-RESEARCH-gui-lifecycle.md (turn request flow item 3 — sweep animation ownership)
@serpentrum/gui_game.py (_handle_event :279 — 'turning' currently silent by design; this plan replaces the silence)
@serpentrum/pymol_bridge.py (sweep_chain from 05-07), @serpentrum/game_engine.py (sweeping dict, _rotate_xy, TURN_TICKS=6)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'turning' branch — bridge sweep + head mirror</name>
  <files>serpentrum/gui_game.py</files>
  <action>
In `_handle_event`, replace the silent-'turning' fall-through with a 'turning' branch:
1. delta = engine.sweeping['angle_signed'] / engine.sweeping['total_ticks'] when engine.sweeping is not None, ELSE session['last_turn_delta'] (the FINAL sweep tick clears `sweeping` inside _advance_sweep BEFORE the event list is handled — the stored delta from tick 5 is the same ±15°; store it: session['last_turn_delta'] = delta on every non-None path).
2. pymol_bridge.sweep_chain(delta, engine.head) — ONE call (bridge defaults: selection 'srp_head or srp_seg_*', camera=0, explicit origin; srp_head INCLUDED so it spins in place and its ring normal follows the chain — Pattern 2, probe-verified).
3. Mirror: session['head_atoms'] = [rotated atoms] using game_engine._rotate_xy(sym-atom x, y) about (engine.head[0], engine.head[1]) with cos/sin of math.radians(delta) — z and sym preserved EXACTLY like _advance_sweep does for segments. The mirror must use the same delta as the bridge call (pure truth == viewer truth for the next tail frame + clash gate).
4. No collision logic here (pre-checked at sweep open — engine contract); no second timer (the sweep tick IS the movement tick); pause mid-sweep needs nothing (timers stop; engine retains sweeping; resume continues).
5. NO info-box line per tick (the existing chattiness policy: 'turning' stays silent in the log; the sweep is visible in the viewer).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep: exactly one sweep_chain call site; no timer creation in the branch (no QTimer/singleShot added); python3.6 tests/run_gates.py --smoke — verdict = flushed SMOKE-OK sentinels, never exit codes (gui_game touched)</verify>
  <done>Turns render as rigid whole-chain sweeps with the head pivoting in place; head mirror tracks exactly; final tick rotates via the stored delta</done>
</task>

<task type="auto">
  <name>Task 2: 'budget_warning' branch</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Add the 'budget_warning' branch to _handle_event: `self._log(hud_logic.budget_text())` — the count-free advisory (builder from plan 05-09; the event payload's atoms_total is deliberately NOT displayed during play — GAME-04 hidden counts; the number reappears only at completion via completion_lines). One line, once per run (engine emits exactly once).
  </action>
  <verify>python3.6 tests/run_gates.py — green; grep: budget_text call site present; no str(ev[1]) rendering of the payload; python3.6 tests/run_gates.py --smoke — verdict = flushed SMOKE-OK sentinels, never exit codes (gui_game touched)</verify>
  <done>Budget advisories surface without revealing counts during play</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — green
- Perf discipline audit: the turning branch issues exactly ONE cmd call (via sweep_chain) and one pure list comprehension per tick — no reads, no object rebuilds
- Engine contract respected: no collision re-checking, no second timer, sweeping-state reads only
</verification>

<success_criteria>
A 90° turn sweeps the whole chain (head included) rigidly about the head pivot in six visible 15° steps with zero drift-relevant state, and the pure mirror keeps controller truth aligned for subsequent stacking. [GAME-10 viewer half; STACK-01 interplay]
</success_criteria>

<output>
After completion, create `.planning/phases/05-stacking-game-rules/05-14-SUMMARY.md`
</output>
