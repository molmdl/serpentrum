---
phase: 04-game-loop-input
plan: 07
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - smoke/manual_wizard_keys_check.py
  - serpentrum/input.py
autonomous: false
must_haves:
  truths:
    - "A human in REAL Windows PyMOL 2.5.0 sees all four arrow keypresses forwarded through the wizard route (C PyMOL_Special → WizardDoSpecial → do_special → steer callback) while the 3D viewer has focus — the live half of claim W7 that headless tests cannot prove."
    - "Left/right arrows do NOT step movie frames during play (do_special grabs, W4) and the up/down command-line history churn is confirmed cosmetic (W8)."
    - "Focus behavior is mapped and judged: arrows do NOT steer while the dialog/command line has focus (Q3); the human accepts the click-viewer UX OR mandates the designed eventFilter fallback."
    - "Teardown restores wizard state (srp_keys_off clears the steering wizard; prior wizard reappears if one was active) — the no-leak half of W5."
  artifacts: []
  key_links:
    - from: "real arrow keypress in Windows PyMOL"
      to: serpentrum/input.py
      via: "PyMOLQtGUI.keyPressEvent → C PyMOL_Special → WizardDoSpecial → KeySteerWizard.do_special(k) → steer_fn (manual harness nudges srp_head 0.3 A + prints)"
      pattern: "do_special"
---

<objective>
The roadmap-mandated EARLY keys human-verify checkpoint — "First human-verify of keys happens here so failure is cheap." The input mechanism (plan 04-04) is implemented and headless-proven; this checkpoint proves the LIVE half (real keypress dispatch + focus behavior) in real Windows PyMOL BEFORE the play wiring (plan 04-08) piles onto it. A failure here costs one small gap plan (the designed eventFilter fallback); the same failure discovered at Phase end would cost a rewrite.

Output: a human verdict — "approved" (wizard route stands; plan 04-08 wires it) or "fallback: <what failed>" (orchestrator spawns a gap plan implementing serpentrum/gui_input.py SteerEventFilter per 04-RESEARCH-input.md Q4, and plan 04-08's single import line swaps routes).

This plan contains NO implementation work up front — only the checkpoint. Fixes discovered DURING verification (harness bugs, input.py bugs) are made inside this plan and re-verified (03-08 precedent).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` (+ `--smoke`) from repo root after any fix.

@.planning/phases/04-game-loop-input/04-RESEARCH-input.md (the "Human-verify checklist (the first real-GUI key test)" section items 1-5 + 8 are THE script for this checkpoint; Q3 focus-stealing analysis; Q4 the fallback design; W4/W5/W7/W8 claims with expected vs fail-mode per item)
@smoke/manual_wizard_keys_check.py (the harness from plan 04-04: run in real PyMOL via the command line; installs the wizard; nudges the head 0.3 A per keypress; srp_keys_off tears down)
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-08-SUMMARY.md (human-verify checkpoint style: numbered steps, expected vs actual, verdict table, resume-signal APPROVED)
@.planning/STATE.md (Phase-4 blocker: "up/down arrow-key binding is the one open mechanism question... First human-verify of keys lands in Phase 4 so failure is cheap")
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the wizard steering route live (keys + focus + teardown)</name>
  <what-built>
The input mechanism from plan 04-04: serpentrum/input.py (KeySteerWizard, event mask 12, install/set_active/teardown) plus the real-GUI harness smoke/manual_wizard_keys_check.py, which materializes the demo scene and installs the wizard with a steer callback that nudges srp_head 0.3 Å per accepted keypress and prints every dispatch. This verifies the LIVE dispatch chain (real keypress → C PyMOL_Special → WizardDoSpecial → do_special) that headless smokes cannot reach.
  </what-built>
  <how-to-verify>
Run in REAL Windows PyMOL 2.5.0 (not headless). Open the serpentrum dialog first (Plugins menu → serpentrum) so the focus test in step 5 can use it.

1. In PyMOL's command line, run the harness (forward slashes work):
   `run C:/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum/smoke/manual_wizard_keys_check.py`
   EXPECT: box + benzene head materialize and frame; the wizard prompt line appears under the viewer ("serpentrum: click the 3D viewer, then steer with arrow keys"); the console prints HARNESS READY instructions.
2. Click the 3D viewer once, then press each arrow key several times.
   EXPECT: the head visibly nudges 0.3 Å per press in the pressed direction (left=−x, up=+y, right=+x, down=−y); the console prints `KEY left -> request_direction=...` for every press. **This is the core claim (W7): up/down reach do_special — set_key could never do this.** [GAME-03 mechanism]
   FAIL MODE: up/down print nothing / no nudge → the wizard route is dead live → verdict "fallback".
3. Watch the PyMOL console while pressing left/right (no movie loaded).
   EXPECT: steering only; NO movie-frame stepping ("frame" counters unchanged). [W4]
4. Press up/down a few times and glance at the command-line text field.
   EXPECT: the command line may scroll through history (cosmetic churn) — gameplay/steering unaffected. [W8 — acceptable, will be documented in Help]
5. Click the serpentrum plugin dialog (any button/blank area), then press arrows WITHOUT clicking back to the viewer. Then click into PyMOL's command-line text field and press up/down.
   EXPECT: NO steering in both cases (the wizard route only receives keys when the viewer has focus — Q3). JUDGMENT CALL: is "click the viewer to steer" (with the persistent prompt line) acceptable UX for v1? The auto-pause safety net lands in plan 04-08 so a stolen focus cannot kill the snake unattended.
6. Click the 3D viewer again, press arrows.
   EXPECT: steering resumes immediately.
7. Type `srp_keys_off` in PyMOL's command line, then press arrows.
   EXPECT: console prints `wizard now: None`; arrows no longer nudge the head; a second `srp_keys_off` is a clean no-op. [W5 teardown — no orphaned steering]
8. (Optional — prior-wizard preservation) Activate PyMOL's measurement wizard (Wizard menu → measurement), re-run the harness script (console prints the displaced prior), steer briefly, then `srp_keys_off`.
   EXPECT: the measurement wizard's panel reappears. [W5 prior-restore]

Record actual vs expected for every step. If steps 1-4, 6-7 pass and step 5's UX is acceptable → verdict "approved".
  </how-to-verify>
  <resume-signal>
Type "approved" (wizard route stands — plan 04-08 wires it), or "fallback: <what failed>" (the designed Qt app-level eventFilter route from 04-RESEARCH-input.md Q4 replaces it via a gap plan), or describe any defect for an in-plan fix + re-verify.
  </resume-signal>
</task>

</tasks>

<verification>
- All 8 steps executed in real Windows PyMOL with verdicts recorded; any in-plan fix is followed by `python3.6 tests/run_gates.py` + the affected smoke re-run, and the failed step re-verified live.
- The verdict (approved / fallback / fixed-then-approved) is recorded in this plan's SUMMARY with the per-step table.
</verification>

<success_criteria>
- The one open mechanism question (up/down arrow-key dispatch live) is RESOLVED with a human verdict before plan 04-08 builds on it.
- The teardown/no-leak contract is confirmed live (arrows revert, prior wizard preserved).
- Focus behavior is mapped with an explicit UX verdict, so plan 04-08's auto-pause mitigation targets the real behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-07-SUMMARY.md` (include the per-step verdict table + the route decision)
</output>
