---
phase: 04-game-loop-input
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - serpentrum/pymol_bridge.py
  - smoke/05_loop_camera_smoke.py
  - tests/run_gates.py
autonomous: true
must_haves:
  truths:
    - "move_head_delta translates srp_head by (dx, dy, 0.0) in MODEL axes (camera=0) with zero drift — 5 x 0.3 A lands at exactly 1.5 A (probe-verified M3), z untouched (2D plane, P3)."
    - "lock_camera() saves {ortho, view}, switches ortho on, frames the WHOLE box (zoom srp_box, never srp_*), and disables all 16 mouse button actions — GAME-02's mechanism."
    - "unlock_camera(saved) restores mouse defaults (cmd.mouse()), ortho and the 18-float view — called by EVERY teardown path (wired in plan 04-08)."
    - "object_exists(name) reports srp_* presence via the proven cmd.get_names('public_objects') type, enabling plan 04-05's guarded head re-centering on restart."
  artifacts:
    - path: serpentrum/pymol_bridge.py
      provides: "move_head_delta(dx, dy, dz=0.0), lock_camera() -> saved dict, unlock_camera(saved), object_exists(name)"
      contains: "def move_head_delta"
    - path: smoke/05_loop_camera_smoke.py
      provides: "REQUIRED headless Windows-PyMOL smoke: zero-drift translate, lock/restore round-trip, sentinel SMOKE-OK LOOP-CAMERA"
      contains: "SMOKE-OK LOOP-CAMERA"
    - path: tests/run_gates.py
      provides: "smoke/05_loop_camera_smoke.py added to REQUIRED_SMOKES"
      contains: "smoke/05_loop_camera_smoke.py"
  key_links:
    - from: serpentrum/gui_game.py
      to: serpentrum/pymol_bridge.py
      via: "GameTab._on_tick calls pymol_bridge.move_head_delta per ('moved',) event (wired in plan 04-05); _begin_play/_teardown_round call lock_camera/unlock_camera (wired in plan 04-08)"
      pattern: "move_head_delta|lock_camera|unlock_camera"
    - from: serpentrum/pymol_bridge.py
      to: "cmd.translate(camera=0)"
      via: "model-axes movement — camera=1 would desync from engine truth if the view were ever non-axis-aligned (gameloop pitfall 4)"
      pattern: "camera=0"
---

<objective>
Add the game-loop's cmd-side seams to the single BRIDGE module (gameloop research Q2/Q3 recipes, verbatim): per-tick head movement (`move_head_delta` via `cmd.translate(..., camera=0)`, zero-drift verified) and the locked 2D camera (`lock_camera`/`unlock_camera`: ortho + zoom(srp_box) + button('none') x16; restore via cmd.mouse() + ortho + set_view). Plus a tiny `object_exists` helper for guarded scene checks. Prove all of it headlessly in a new REQUIRED smoke.

Purpose: GAME-02 (2D plane, locked camera, box visible) and the movement half of GAME-01/GAME-08 live HERE, at the only legal cmd seam. The GUI tick (plan 04-05) and the play wiring (plan 04-08) consume these functions — they must exist and be machine-proven before those plans land.

Output: 4 new bridge functions + smoke/05_loop_camera_smoke.py (SMOKE-OK LOOP-CAMERA) registered in REQUIRED_SMOKES.

Phase-3 boundary honored: STATE.md 03-06 says "camera = one-shot cmd.zoom ONLY, NEVER ortho/set_view" — that was the PHASE-3 boundary; Phase 4 OWNS GAME-02 and is expected to use ortho/set_view/button (03-RESEARCH-viewer-bridge.md §5.2:359-363, restated in gameloop research Q3).

Scope guards (OUT of scope): NO per-tick re-zoom (camera locked, pitfall 14); NO set_object_ttt (display-only matrix breaks the Phase-5 xtb coord handoff — M3/M5, use atomic translate); NEVER cmd.get_object_ttt (segfaults, M5); no wizard/input code (plan 04-04); no GUI code.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` (syntax + safety + purity + unittest) and `python3.6 tests/run_gates.py --smoke` (headless Windows PyMOL; verdict = flushed SMOKE-OK sentinels, NEVER exit codes — PyMOL swallows rc through the .bat). Single-smoke debug run: `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\05_loop_camera_smoke.py"` from repo root.

@.planning/phases/04-game-loop-input/04-RESEARCH-gameloop.md (THE recipe source: Q2 movement claims M1-M6 + bridge contract sketch; Q3 locked-camera claims C1-C6 + LOCK/RESTORE recipes; Q4 2D-plane P1-P3; pitfalls 1-14 — esp. 4 (camera=0), 5 (zoom srp_box not head), 9 (unlock on every path), 14 (no per-tick re-zoom))
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-06-SUMMARY.md (bridge conventions: cmd.get_names('public_objects') is the proven 2.5.0 type; get_extent returns [[min],[max]] nested lists; bridge is THIN, pure logic lives elsewhere)
@serpentrum/pymol_bridge.py (BOX_NAME='srp_box', HEAD_NAME='srp_head', place_head centers via get_extent, cleanup_srp, module docstring contracts)
@smoke/04_demo_e2e_smoke.py (smoke template: _resolve_root, flush=True everywhere, check() runner, sentinel epilogue, fixture path tests/fixtures/molfile/methane.sdf)
@tests/run_gates.py (REQUIRED_SMOKES tuple, line ~55)
@.planning/STATE.md (decision 03-06 camera boundary + BOX_DISPLAY_Z 5.0)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge movement + locked-camera functions</name>
  <files>serpentrum/pymol_bridge.py</files>
  <action>
Add to serpentrum/pymol_bridge.py (after frame_scene, before _select_head_record), following the module's existing style (python3.6, %-formatting, <= ~15 lines per function, no prints):

1. `def move_head_delta(dx, dy, dz=0.0):` — `cmd.translate([float(dx), float(dy), float(dz)], HEAD_NAME, camera=0)`.
   Docstring MUST state: atomic-coordinate translate (object=None path), camera=0 = MODEL axes (pitfall 4: camera=1 would desync from engine truth if the view were non-axis-aligned); zero drift verified (5 x [0.3,0,0] -> exactly [1.5,0,0], probe TL_5TICKS_0.3A); dz stays 0.0 (2D plane contract, gameloop P3); called once per ('moved',) tick; NEVER get_object_ttt for verification (segfaults, M5) — use get_extent (bare-name prose only — never the call-form `get_object_ttt(`; the verify greps the call-FORM and must find zero).
2. `def lock_camera():` — per the gameloop Q3 LOCK recipe verbatim:
   ```python
   saved = {'ortho': cmd.get('ortho'), 'view': cmd.get_view()}
   cmd.set('ortho', 'on')
   cmd.zoom(BOX_NAME)          # frame the WHOLE play field, NOT the head (pitfall 5/14)
   cmd.refresh()
   for btn in ('left', 'middle', 'right', 'wheel'):
       for mod in ('None', 'Shft', 'Ctrl', 'CtSh'):
           cmd.button(btn, mod, 'none')
   return saved
   ```
   Docstring: returns the saved state for unlock_camera; called at _begin_play (plan 04-08); button 'none' = code 22 (controlling.py:80); the mouse lock does NOT touch the keyboard wizard (independent mechanisms, C5).
3. `def unlock_camera(saved):` — per the RESTORE recipe: `if saved is None: return`; `cmd.mouse()` (re-applies the current button_mode's defaults — controlling.py:648-676; caveat C4: restores MODE defaults, not per-user customizations — document "acceptable for v1, document in Help"); `cmd.set('ortho', saved['ortho'])`; `cmd.set_view(saved['view'])`; `cmd.refresh()`. Docstring: called by EVERY teardown path; idempotent via the None guard.
4. `def object_exists(name):` — `return name in cmd.get_names('public_objects')` (the PROVEN 2.5.0 type — 03-06; 'all_objects' raises). Docstring: guarded scene checks (e.g. re-centering srp_head on Restart when the scene may not exist).

Also extend the module docstring's camera paragraph: Phase 4 owns GAME-02 — ortho/set_view/button are now legal HERE (one-shot framing + play lock), replacing the Phase-3 "zoom only" boundary note.
  </action>
  <verify>python3.6 tests/run_gates.py — gates green (purity: bridge may import pymol at any level, no Qt/numpy; syntax walk clean). Existing smoke 03/04 still pass (bridge changes are additive). Call-site contract: `grep -n "get_object_ttt(" serpentrum/pymol_bridge.py` → zero hits — the move_head_delta docstring's mandated bare-name warning (NEVER get_object_ttt) is allowed and expected; the call-FORM with parenthesis is not.</verify>
  <done>4 functions live in the BRIDGE module with research-faithful contracts; default gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Headless smoke 05 (movement + camera) + REQUIRED registration</name>
  <files>smoke/05_loop_camera_smoke.py, tests/run_gates.py</files>
  <action>
Create smoke/05_loop_camera_smoke.py copying the smoke/04 template rules verbatim: _resolve_root() defensive ROOT resolution (never trust __file__ under -cq), sys.path.insert(0, ROOT), `from pymol import cmd` + `from serpentrum import pymol_bridge`, flush=True on EVERY print, NO widget construction, check(name, fn) runner, final sentinel. Sentinel: `SMOKE-OK LOOP-CAMERA` / `SMOKE-FAIL <step>: ...`.

Steps:
1. `s_scene` — `pymol_bridge.load_box('medium')`; load the methane fixture (`os.path.join(ROOT, 'tests', 'fixtures', 'molfile', 'methane.sdf')`) via `pymol_bridge.load_molecule(fixture, pymol_bridge.HEAD_NAME)`; `pymol_bridge.place_head(pymol_bridge.HEAD_NAME)`; assert srp_box AND srp_head in cmd.get_names('public_objects').
2. `s_move_zero_drift` — record ext0 = cmd.get_extent(HEAD_NAME); loop 5x `pymol_bridge.move_head_delta(0.3, 0.0, 0.0)`; ext1 = get_extent; assert (ext1[0][0] - ext0[0][0]) == 1.5 within 1e-9 AND z extents identical (dz=0 contract) AND y identical.
3. `s_lock_camera` — saved = pymol_bridge.lock_camera(); assert saved['ortho'] == 'off' (pre-lock default, probe C1); assert cmd.get('ortho') == 'on'; assert len(saved['view']) == 18 (probe C2); assert no exception from the 16 button('none') calls (implicit — C3 probe showed zero errors).
4. `s_unlock_camera` — pymol_bridge.unlock_camera(saved); assert cmd.get('ortho') == 'off'; v = cmd.get_view(); assert v == saved['view'] (round-trip stable, probe VIEW_STABLE); cmd.mouse() restore proven by no-exception (per-button READ-BACK does not exist in 2.5.0 — real mouse-drag restore is human-verify in plan 04-09; state this in the step's comment).
5. `s_object_exists` — assert pymol_bridge.object_exists('srp_head') is True and object_exists('srp_nope') is False.
6. `s_cleanup` — load a non-srp sentinel (`cmd.load(fixture, object='user_mol')`), then n = pymol_bridge.cleanup_srp(); assert srp_* gone and 'user_mol' survives (namespace hygiene unchanged).

Register in tests/run_gates.py: add 'smoke/05_loop_camera_smoke.py' to REQUIRED_SMOKES (after smoke/04, keeping the tuple sorted).

Commit: `test(04-03): add loop-camera smoke + required registration`.
  </action>
  <verify>
- `python3.6 -m py_compile smoke/05_loop_camera_smoke.py` OK.
- `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\05_loop_camera_smoke.py"` from repo root → output contains `SMOKE-OK LOOP-CAMERA` and NO `SMOKE-FAIL`.
- `python3.6 tests/run_gates.py --smoke` → gate 4 green with the new sentinel echoed in notes.
  </verify>
  <done>smoke/05 flushes SMOKE-OK LOOP-CAMERA through the real headless Windows PyMOL; REQUIRED_SMOKES covers 01/03/04/05; both gate commands green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — exit 0.
- `python3.6 tests/run_gates.py --smoke` — exit 0 with notes echoing SMOKE-OK SKELETON, SMOKE-OK VIEWER-BRIDGE, SMOKE-OK VIEWER-DEMO, SMOKE-OK LOOP-CAMERA.
- grep contracts: `grep -n "camera=0" serpentrum/pymol_bridge.py` hits move_head_delta; `grep -n "get_object_ttt(" serpentrum/pymol_bridge.py` → zero hits — the contract is zero EXECUTABLE call sites (the call-FORM with parenthesis); the docstring's mandated bare-name safety warning "NEVER get_object_ttt" is allowed and expected.
</verification>

<success_criteria>
- move_head_delta/lock_camera/unlock_camera/object_exists live in the BRIDGE seam with gameloop-research-faithful contracts, machine-proven headless (zero drift exact, ortho/view/button round-trip).
- Plans 04-05 (tick seam) and 04-08 (begin_play/teardown wiring) can consume them without further bridge edits.
- GAME-02's mechanism exists and is regression-protected as a REQUIRED smoke.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-03-SUMMARY.md`
</output>
