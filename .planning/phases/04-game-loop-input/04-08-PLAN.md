---
phase: 04-game-loop-input
plan: 08
type: execute
wave: 4
depends_on: ["04-05", "04-06", "04-07"]
files_modified:
  - serpentrum/gui_game.py
  - serpentrum/gui.py
autonomous: true
must_haves:
  truths:
    - "_begin_play locks the camera (lock_camera at GO!, after the countdown) and installs the steering wizard (input.install(engine.request_direction)) — GAME-02's lock and GAME-03's steering arm together at the same instant, on the same GUI thread."
    - "ONE teardown helper (_teardown_round) serves EVERY end path (crash _end_run, restart, dialog close): stop timers → bump epoch → input.teardown → unlock_camera — the Pitfall 9.2 centralization that prevents leaked mouse locks or orphaned wizards."
    - "During pause the wizard stays installed but grabs-and-no-ops (input.set_active(False)) and the camera stays LOCKED (research D5: unlocking would let the user rotate a frozen scene)."
    - "The dialog auto-pauses when it gains focus mid-run (PluginDialog.focusInEvent → GameTab.request_auto_pause, status=='playing' only) — the Q3 focus-stealing safety net."
  artifacts:
    - path: serpentrum/gui_game.py
      provides: "_begin_play/_teardown_round wiring of lock_camera + input install/teardown/set_active; _apply_pause_state/_resume_play internals shared with request_auto_pause; public shutdown() for dialog close"
      contains: "_teardown_round"
    - path: serpentrum/gui.py
      provides: "PluginDialog.focusInEvent → game_tab.request_auto_pause(); PluginDialog.closeEvent → game_tab.shutdown()"
      contains: "focusInEvent"
  key_links:
    - from: serpentrum/gui_game.py
      to: serpentrum/pymol_bridge.py
      via: "_begin_play: sess['saved_cam'] = pymol_bridge.lock_camera(); _teardown_round: pymol_bridge.unlock_camera(sess.pop('saved_cam', None)) — lock/restore paired on every path"
      pattern: "lock_camera|unlock_camera"
    - from: serpentrum/gui_game.py
      to: serpentrum/input.py
      via: "_begin_play: self._input_handle = game_input.install(engine.request_direction); _teardown_round: game_input.teardown(self._input_handle); pause/resume: game_input.set_active(False/True) — the route-agnostic seam (04-07 verdict decides which module game_input names)"
      pattern: "install|set_active|teardown"
    - from: serpentrum/gui.py
      to: serpentrum/gui_game.py
      via: "focusInEvent → request_auto_pause(); closeEvent → shutdown() — dialog-level lifecycle hooks"
      pattern: "request_auto_pause|shutdown"
---

<objective>
Wire the play lifecycle (gameloop research Q6 recipes, verbatim): at GO! lock the camera and install the steering wizard; at EVERY end path run ONE teardown helper (input.teardown + unlock_camera + timers + epoch); gate steering during pause with set_active(False) while keeping the wizard and the camera lock; add the focus-stealing auto-pause mitigation and the dialog-close teardown. This completes the phase's mechanics — after this plan, the full playable loop exists and only the phase-closing verification (04-09) remains.

Purpose: the roadmap note "the input spike should precede/steer HUD wiring" resolves here — the 04-07 verdict selected the route, and this plan wires THAT route into the session lifecycle without leaking camera or key state (Pitfalls 9.2, 4.3).

Output: extended GameTab lifecycle + two PluginDialog hooks. Gates green.

Scope guards (OUT of scope): NO new bridge/engine/input functions (all exist); NO pickups/stacking; NO win/crash handoff to spectra (GAME-09/10 — crash simply ends the run with a logged verdict); NO per-tick re-zoom.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` + `python3.6 tests/run_gates.py --smoke` from repo root (GUI code verified by py_compile + AST purity + grep; the smokes prove the bridge/input seams this plan calls).

@.planning/phases/04-game-loop-input/04-RESEARCH-gameloop.md (Q6 "Countdown + pause mechanics" — the _begin_play ordering (lock_camera → input.install → timers → start_time) and the RESUME rebase steps; "Wizard teardown during pause — RECOMMENDATION: keep the wizard active, set_active(False)"; D1-D5 claims; pitfalls 6/9/11/13: single _teardown_round, camera stays locked during pause, wizard+mouse installed AND torn down together)
@.planning/phases/04-game-loop-input/04-RESEARCH-input.md (Q3 mitigation (b) auto-pause on dialog focusInEvent; Q5 the controller-side wiring sketch: install at _begin_play, set_active on pause/resume, teardown in _teardown_round "called by EVERY path")
@.planning/phases/04-game-loop-input/04-07-SUMMARY.md (THE route verdict: approved wizard route → `from . import input as game_input`; fallback verdict → `from . import gui_input as game_input` after the gap plan lands — identical install/set_active/teardown(handle) signatures either way)
@serpentrum/gui_game.py (the plan 04-05 GameTab: _begin_play seam comment, _teardown_live_session, _on_pause_toggled, _end_run — all extended here)
@serpentrum/gui.py (the plan 04-06 PluginDialog: game_tab attribute, slot — hooks added here)
@serpentrum/input.py (install returns the prior/handle; teardown(handle=None); set_active)
@.planning/research/PITFALLS.md (#9.2 one teardown helper on EVERY path; #8 timers/dialog lifetime; #4.2 focus)
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameTab play wiring — lock camera + install input at GO!, one teardown for every path</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Extend serpentrum/gui_game.py (python3.6, no new module-level imports beyond `from . import input as game_input`):

1. Import (after the other relative imports): `from . import input as game_input` — ROUTE-AGNOSTIC seam. If the 04-07 verdict was "fallback", this single line becomes `from . import gui_input as game_input` (identical install/set_active/teardown signatures); note this swap point in a comment.
2. `_begin_play(scheduled)` — replace the plan 04-05 seam comment with the researched ordering (gameloop Q6):
   ```python
   sess['start_time'] = time.time()
   sess['status'] = 'playing'
   sess['saved_cam'] = pymol_bridge.lock_camera()          # GAME-02 lock
   self._input_handle = game_input.install(
       sess['engine'].request_direction)                   # GAME-03 steering
   self._tick_timer.start()
   self._elapsed_timer.start()
   self._update_remaining()
   self.pause_btn.setEnabled(True)
   self._log('Move with the arrow keys.')
   ```
   (Order matters: lock + install BEFORE timers so the first tick is already locked/steerable; everything runs on the GUI thread — gameloop Q1/T4 — so no marshaling.)
3. Rename/extend `_teardown_live_session` into `self._teardown_round()` — THE single teardown helper (Pitfall 9.2), idempotent, called by EVERY path. Body in this order: (a) stop tick + elapsed timers; (b) `self._epoch += 1` (kills stale singleShot chains); (c) input teardown: `handle = getattr(self, '_input_handle', None); game_input.teardown(handle); self._input_handle = None` (teardown(handle) accepts-and-ignores on the wizard route — uniform with the fallback); (d) camera restore: read the CURRENT session BEFORE nulling it — `sess = self._session`; if sess is not None: `pymol_bridge.unlock_camera(sess.pop('saved_cam', None))`; (e) the existing blockSignals-wrapped pause-button reset. Keep `begin_game`/`_on_restart` calling it first; add `_end_run(engine)` calling it AFTER logging the verdict (verdict must stay visible in the info box; timers/input/camera die with the run). Add a public `shutdown(self)` → `self._teardown_round()` with a docstring "dialog-close hook (PluginDialog.closeEvent) — every end path funnels here (Pitfall 9.2)".
4. `_on_pause_toggled(checked)` — gate steering around the existing engine/timer logic (research D + input open-q 5): pause branch adds `game_input.set_active(False)` (wizard stays installed, grabs-and-no-ops — arrows neither steer nor step movie frames); resume branch adds `game_input.set_active(True)` AFTER the paused_accum rebase. Camera is NOT touched on pause (stays locked — research D5).
5. Refactor the pause/resume internals for reuse: extract `self._apply_pause_state(True)` (pause branch body) and `self._apply_pause_state(False)` (resume branch body); `_on_pause_toggled(checked)` becomes a thin guard (needs live session) + call. `request_auto_pause()` (next task) reuses `_apply_pause_state(True)`.
6. Add `request_auto_pause(self)` — public, the Q3 focus mitigation: `sess = self._session; if sess is None or sess.get('status') != 'playing': return`; then `self.pause_btn.blockSignals(True); self.pause_btn.setChecked(True); self.pause_btn.blockSignals(False)` + `self._apply_pause_state(True)` + log 'auto-paused (dialog took focus — click Resume, then the 3D viewer)'. blockSignals prevents the programmatic setChecked from re-entering _on_pause_toggled (Pitfall G).

Commit: `feat(04-08): wire camera lock + steering install into the play lifecycle`.
  </action>
  <verify>
- `python3.6 tests/run_gates.py` — all gates green (purity: game_input is a relative import, exempt; no cmd import in gui_game; no .exec_).
- grep contracts: `grep -n "lock_camera\|unlock_camera" serpentrum/gui_game.py` → lock in _begin_play, unlock ONLY inside _teardown_round; `grep -n "set_active" serpentrum/gui_game.py` → pause + resume branches; `grep -c "def shutdown" serpentrum/gui_game.py` → 1.
- Structural trace: every end path (_end_run, _on_restart→begin_game, shutdown) reaches _teardown_round — no path stops timers without it.
  </verify>
  <done>The play lifecycle arms lock+steering at GO! and disarms them on every end path through ONE helper; pause gates steering without tearing down; auto-pause API exists.</done>
</task>

<task type="auto">
  <name>Task 2: PluginDialog focus + close hooks</name>
  <files>serpentrum/gui.py</files>
  <action>
Extend serpentrum/gui.py's PluginDialog (python3.6; NO .exec_; GUI purity unchanged):

1. `focusInEvent(self, event)` — call `QtWidgets.QDialog.focusInEvent(self, event)` FIRST, then `game_tab = getattr(self, 'game_tab', None)`; if game_tab is not None: `game_tab.request_auto_pause()`. Docstring: the Q3 focus-stealing safety net (04-RESEARCH-input.md) — fires on ANY dialog focus gain (including the Start click), but request_auto_pause guards `status == 'playing'` so countdown/idle/over states are unaffected; the snake can no longer crash unattended while the user reads the HUD.
2. `closeEvent(self, event)` — `game_tab = getattr(self, 'game_tab', None)`; if game_tab is not None: `game_tab.shutdown()`; then `QtWidgets.QDialog.closeEvent(self, event)`. Docstring: mid-game dialog close must tear down timers + wizard + camera lock (Pitfall 8/9 — a closed dialog must never leave a locked mouse or an orphaned do_special; the session anchor survives for a later reopen via begin_game's teardown-first).
3. Update the class docstring's switching-contract paragraph with the two new lifecycle hooks. Nothing else changes (reserved row, find_existing, page order).

Commit: `feat(04-08): dialog focus auto-pause + close teardown hooks`.
  </action>
  <verify>
- `python3.6 tests/run_gates.py` + `python3.6 tests/run_gates.py --smoke` — all gates green (all five smokes still pass; gui.py purity clean; `grep -n "exec_(" serpentrum/gui.py` → zero).
- grep contracts: `grep -n "focusInEvent\|closeEvent" serpentrum/gui.py` → both hooks; `grep -n "request_auto_pause\|shutdown" serpentrum/gui.py` → both delegations.
  </verify>
  <done>Dialog-level lifecycle complete: focus gain mid-run auto-pauses; closing the dialog mid-game tears down camera + wizard + timers.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` and `python3.6 tests/run_gates.py --smoke` — both exit 0.
- Lifecycle audit (grep + read): exactly ONE unlock_camera call site (inside _teardown_round); exactly ONE teardown call site (inside _teardown_round); _begin_play order = lock → install → timers; pause branch = engine.pause + timer.stop + set_active(False), resume = rebase + set_active(True); camera untouched during pause.
- The full playable loop now exists mechanically; live verdicts (camera immovable, steering mid-run, pause/restart behavior, no leaks) are plan 04-09's checkpoint.
</verification>

<success_criteria>
- GAME-02 (locked camera at GO!, restored on every end path) and GAME-03 (steering installed at GO!, gated during pause, torn down on every end path) are fully wired with the Pitfall-9.2 single-teardown discipline.
- The focus-stealing mitigation (auto-pause) and dialog-close teardown close the two live-behavior risk holes flagged by the research.
- No leaked state is possible by construction: one helper, every path, idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-08-SUMMARY.md`
</output>
