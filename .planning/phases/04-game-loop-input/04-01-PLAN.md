---
phase: 04-game-loop-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_purity.py
  - tests/test_purity_gates.py
autonomous: true
must_haves:
  truths:
    - "The purity checker classifies serpentrum/input.py as BRIDGE (pymol/pmg_tk legal at any level; Qt/numpy banned) BEFORE that file exists."
    - "The purity checker classifies serpentrum/gui_game.py as GUI (pymol.Qt only; bare pymol/pmg_tk banned anywhere) BEFORE that file exists."
    - "test_purity_gates.RealRepoCleanTest still passes, so plans 04-04/04-05 can create their modules with zero purity regression."
  artifacts:
    - path: tools/check_purity.py
      provides: "BRIDGE_MODULES gains 'serpentrum/input.py'; GUI_MODULES gains 'serpentrum/gui_game.py'"
      contains: "serpentrum/input.py"
    - path: tests/test_purity_gates.py
      provides: "fixture cases proving both new classifications (BRIDGE for input.py, GUI for gui_game.py), mirroring cases 11-17 style"
      contains: "serpentrum/input.py"
  key_links:
    - from: tools/check_purity.py
      to: serpentrum/input.py
      via: "BRIDGE_MODULES allowlist entry — safe pre-registration because check_tree only walks EXISTING files under serpentrum/"
      pattern: "BRIDGE_MODULES"
    - from: tools/check_purity.py
      to: serpentrum/gui_game.py
      via: "GUI_MODULES allowlist entry"
      pattern: "GUI_MODULES"
---

<objective>
Register the two new Phase-4 module purity classes in the AST purity gate BEFORE the modules exist: `serpentrum/input.py` (wizard steering, needs `from pymol import cmd` + `pymol.wizard` at module level → BRIDGE) and `serpentrum/gui_game.py` (Game tab HUD, needs `from pymol.Qt import ...` → GUI).

Purpose: both modules are created by parallel Wave-2 plans (04-04, 04-05). Default-strict classification would mark them PURE and fail gate 2 the moment they land. Pre-registering the allowlist entries (the deliberate-extension pattern from 03-01, STATE.md decision "BRIDGE purity class live") removes that collision and lets Wave 2 run fully parallel. `check_tree` only walks existing files, so entries for not-yet-created paths are inert until the modules appear.

Output: extended allowlists in tools/check_purity.py + fixture tests pinning both classifications.

Scope guards (OUT of scope): no new module files created here; no engine/bridge/HUD changes; no Qt code.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates (run from repo root in WSL):**
- `python3.6 tests/run_gates.py` — syntax walk + plugin-path safety + AST purity + scoped unittest.
- `--smoke` / `--xtb` NOT needed for this plan (no viewer behavior, no xtb).

@.planning/phases/04-game-loop-input/04-RESEARCH-input.md (§ "Module placement under purity rules" table — input.py = BRIDGE with pymol.wizard + cmd, no Qt; eventFilter fallback = separate GUI file)
@.planning/phases/04-game-loop-input/04-RESEARCH-hud.md (Q5 "Purity + gate integration" — gui_game.py GUI allowlist edit mirrors gui_setup.py)
@tools/check_purity.py (GUI_MODULES line 58, BRIDGE_MODULES line 65, classify(), fixture contract in the module docstring)
@tests/test_purity_gates.py (fixture-tree pattern make_tree() + cases 11-17 = the template for the new cases)
@.planning/STATE.md (decision 03-01: BRIDGE purity class — pymol/pmg_tk at ANY level in BRIDGE; PyQt5/numpy banned; .exec_() banned; GUI allowlist = pymol.Qt only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend purity allowlists for input.py (BRIDGE) and gui_game.py (GUI)</name>
  <files>tools/check_purity.py</files>
  <action>
Edit tools/check_purity.py ONLY (no logic changes — the two sets plus their comments):

1. GUI_MODULES (line 58): add 'serpentrum/gui_game.py' —
   `GUI_MODULES = {'serpentrum/gui.py', 'serpentrum/gui_setup.py', 'serpentrum/gui_game.py'}`
   and extend the GUI docstring table row (lines ~21-23) to mention gui_game.py.
2. BRIDGE_MODULES (line 65): add 'serpentrum/input.py' —
   `BRIDGE_MODULES = {'serpentrum/pymol_bridge.py', 'serpentrum/input.py'}`
   and extend the BRIDGE comment/docstring row (~lines 24-25) to mention input.py.
3. Update the module-docstring classification table (lines ~13-28) rows for both new paths, citing the Phase-4 rationale: input.py = KeySteerWizard steering (pymol.wizard + cmd at module level, NEVER Qt — 04-RESEARCH-input.md placement table); gui_game.py = Game tab HUD (pymol.Qt only — 04-RESEARCH-hud.md Q5).

Do NOT touch classify()/BANNED_ROOTS/check logic. Do NOT create the module files — later plans own them. Note in the comment that entries for not-yet-created files are inert (check_tree walks existing files only) and that test_purity_gates.RealRepoCleanTest must stay green when the files land.
  </action>
  <verify>python3.6 tests/run_gates.py — all gates green (real-repo purity clean: the new entries reference files that do not exist yet, so gate 2 must still exit 0).</verify>
  <done>check_purity.classify('serpentrum/input.py') == 'BRIDGE' and classify('serpentrum/gui_game.py') == 'GUI' (provable via a throwaway python3.6 -c snippet); all default gates green; no other logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture tests pinning the two new classifications</name>
  <files>tests/test_purity_gates.py</files>
  <action>
Append fixture cases to tests/test_purity_gates.py following the EXISTING make_tree() pattern (tempdir tree + check_tree + exact-violation assertions; shutil cleanup via addCleanup; NEVER `rm`). Add:

1. `test_input_bridge_module_level_pymol_clean` — tree with serpentrum/input.py containing `from pymol import cmd` + `from pymol.wizard import Wizard` at module level → check_tree(root) == [] (BRIDGE allowance, mirrors case 11).
2. `test_input_bridge_pyqt5_banned` — input.py fixture with `from PyQt5 import QtWidgets` → exactly 1 violation naming PyQt5 (mirrors case 13).
3. `test_input_bridge_numpy_banned` — input.py fixture with `import numpy` inside a function → exactly 1 violation naming numpy (mirrors case 14).
4. `test_gui_game_pymol_qt_clean` — tree with serpentrum/gui_game.py containing `from pymol.Qt import QtWidgets, QtCore` → [] (mirrors case 16).
5. `test_gui_game_bare_pymol_flagged` — gui_game.py with module-level `from pymol import cmd` AND (second tree) body-level `import pmg_tk.startup` → each tree exactly 1 violation naming pymol/pmg_tk (mirrors case 17).

Each fixture's docstring cites the Phase-4 plan (04-01) and the research file that mandated the classification. python3.6 syntax only; no new imports beyond the file's existing os/shutil/sys/tempfile/unittest + check_purity.
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_purity_gates.py" -v — all cases green including the 5 new ones; then python3.6 tests/run_gates.py — full suite green (all prior tests unbroken).</verify>
  <done>5 new fixture cases pass; the full default gate run is green with no regressions; both new modules are now legally classifiable before they exist.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` exits 0 (syntax + plugin-path safety + purity + full unittest suite including the new cases).
- `python3.6 -c "import sys; sys.path.insert(0,'tools'); import check_purity; print(check_purity.classify('serpentrum/input.py'), check_purity.classify('serpentrum/gui_game.py'))"` prints `BRIDGE GUI`.
- git diff shows ONLY tools/check_purity.py + tests/test_purity_gates.py changed.
</verification>

<success_criteria>
- Wave-2 plans (04-04 input.py, 04-05 gui_game.py) can create their modules and pass gate 2 with NO further check_purity.py edits.
- Classification + fixture tests are pinned so a future accidental reclassification fails loudly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-01-SUMMARY.md`
</output>
