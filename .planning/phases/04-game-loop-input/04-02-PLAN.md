---
phase: 04-game-loop-input
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/hud_logic.py
  - serpentrum/game_engine.py
  - tests/test_hud_logic.py
autonomous: true
must_haves:
  truths:
    - "Elapsed seconds render as M:SS with fractional seconds floored and negatives clamped to 0:00 (the Game tab's delta-based elapsed display logic is WSL-testable)."
    - "The molecules-remaining label renders 'Remaining: N' or 'Remaining: -' with None-safe handling (Phase-4 frozen-cap display)."
    - "GameEngine.molecules_remaining returns cap - molecules_stacked and None when cap is None (pure arithmetic, HUD research Q4 recommendation)."
  artifacts:
    - path: serpentrum/hud_logic.py
      provides: "format_elapsed(seconds) -> 'M:SS'; remaining_text(value) -> 'Remaining: N' | 'Remaining: -' (PURE, stdlib-only)"
      min_lines: 25
    - path: serpentrum/game_engine.py
      provides: "read-only molecules_remaining property (cap - molecules_stacked, None-safe)"
      contains: "molecules_remaining"
    - path: tests/test_hud_logic.py
      provides: "unit tests for format_elapsed + remaining_text + the engine property (RED-first)"
      min_lines: 40
  key_links:
    - from: serpentrum/hud_logic.py
      to: serpentrum/gui_game.py
      via: "GameTab._on_elapsed_tick calls hud_logic.format_elapsed; GameTab._update_remaining calls hud_logic.remaining_text (wired in plan 04-05)"
      pattern: "hud_logic"
    - from: serpentrum/game_engine.py
      to: serpentrum/gui_game.py
      via: "GameTab._update_remaining reads engine.molecules_remaining instead of raw cap/stacked arithmetic (wired in plan 04-05)"
      pattern: "molecules_remaining"
---

<objective>
Extract the Game tab's two display calculations into WSL-testable pure code (TDD): `hud_logic.format_elapsed` / `hud_logic.remaining_text` (new PURE module `serpentrum/hud_logic.py`) and the `GameEngine.molecules_remaining` property (HUD research Q4: keeps `cap - molecules_stacked` arithmetic in the testable pure layer so GameTab stays thin).

Purpose: GAME-07's elapsed timer and molecules-remaining label are GUI widgets (human-verify only per 01-05), but their MATH is pure. Extracting it now means the only untestable part of the HUD is widget assembly. Also satisfies GAME-08's constant-speed display-independence: remaining is length-agnostic arithmetic.

Output: serpentrum/hud_logic.py + engine property + tests/test_hud_logic.py, all green under python3.6.

Scope guards (OUT of scope): NO Qt code, NO pymol imports (hud_logic must classify PURE), no countdown logic (GUI-owned, plan 04-05), no pickups/win changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` from repo root (syntax + plugin-path safety + purity + scoped unittest). TDD: write tests FIRST, watch them fail, then implement (RED → GREEN → commit per phase-plan conventions, e.g. `test(04-02):` then `feat(04-02):`).

@.planning/phases/04-game-loop-input/04-RESEARCH-hud.md (Q2 elapsed-mechanism rows: delta-based wall-clock recompute "M:SS"; Q6 test-convention paragraph: extract pure helpers to a PURE module + test file, keep GameTab thin; engine-API table row "Molecules-remaining = cap + molecules_stacked attrs")
@.planning/phases/04-game-loop-input/04-RESEARCH-gameloop.md (Q5/S3: engine is PURE with NO wall-clock — elapsed is GUI-owned by design; the property must not add any timing)
@serpentrum/game_engine.py (lines 214-217, 262-268: cap / molecules_stacked attrs; reset() lines 233-270 — property must survive reset semantics)
@tests/test_engine_core.py (test conventions: sys.path self-insert, no __init__.py, no stubs, assertAlmostEqual DELTA)
@.planning/STATE.md (01-05: headless dialog construction dead end → GUI verdicts human-verify only; this plan exists precisely to keep HUD math out of that dead zone)
</context>

<feature>
  <name>Pure HUD display helpers: hud_logic.format_elapsed + hud_logic.remaining_text + GameEngine.molecules_remaining</name>
  <files>serpentrum/hud_logic.py, serpentrum/game_engine.py, tests/test_hud_logic.py</files>
  <behavior>
    format_elapsed(seconds) — returns 'M:SS' (minutes not zero-padded, seconds zero-padded to 2):
    - format_elapsed(0)        -> '0:00'
    - format_elapsed(65)       -> '1:05'
    - format_elapsed(600)      -> '10:00'
    - format_elapsed(75.9)     -> '1:15'   (fractional seconds FLOORED, not rounded)
    - format_elapsed(-3.2)     -> '0:00'   (negative clamped — a tick landing before start_time must never render negative)
    - format_elapsed(3599)     -> '59:59'

    remaining_text(value) — the molecules-remaining label string (GAME-07):
    - remaining_text(10)  -> 'Remaining: 10'
    - remaining_text(0)   -> 'Remaining: 0'
    - remaining_text(None)-> 'Remaining: -'  (cap is None → unknown; Phase 4 never hits this with default setups, but the engine allows cap=None)

    GameEngine.molecules_remaining (read-only property):
    - engine with cap=10, molecules_stacked=0 -> 10
    - engine with cap=10, molecules_stacked=3 -> 7
    - engine with cap=None -> None
    - property reflects reset(): after reset(cap=5) -> 5
    - property never mutates state; step()/pause() unaffected.
  </behavior>
  <implementation>
    RED first — tests/test_hud_logic.py:
    - Mirror tests/test_engine_core.py conventions exactly: `sys.path.insert(0, <repo root>)` self-insert, NO __init__.py, no sys.modules stubs, python3.6 syntax (%-formatting).
    - Classes: TestFormatElapsed (all 6 cases above via assertEqual), TestRemainingText (3 cases), TestEngineMoleculesRemaining (4 cases; construct GameEngine(head=(0.0,0.0), heading='right', cap=...) keyword args per the engine's keyword-friendly contract; simulate stacking by setting engine.molecules_stacked directly — plain public attr — then verify the property; one reset() case).
    - Run: `python3.6 -m unittest discover -s tests -p "test_hud_logic.py" -v` — MUST FAIL with ImportError (serpentrum.hud_logic does not exist) + AttributeError (no molecules_remaining). Commit: `test(04-02): add failing tests for HUD pure display helpers`.

    GREEN — serpentrum/hud_logic.py:
    - Module docstring: "serpentrum.hud_logic — pure display helpers for the Game tab HUD (plan 04-02). PURE module: stdlib only (no pymol / pmg_tk / PyQt5 / numpy — check_purity auto-classifies PURE). The wall clock lives in the GUI (game_engine has NO wall-clock by design, 04-RESEARCH-gameloop.md Q5/S3); only the formatting lives here."
    - `def format_elapsed(seconds):` — clamp negatives to 0.0, `total = int(seconds)` (floor for positives; the clamp handles negatives before int()), `return '%d:%02d' % (total // 60, total % 60)`.
    - `def remaining_text(value):` — `return 'Remaining: -' if value is None else 'Remaining: %d' % value`.
    - python3.6-compatible; nothing else in the module.

    GREEN — serpentrum/game_engine.py:
    - Add a read-only property AFTER the reset/counters section (near molecules_stacked, ~line 268 context), docstring citing 04-RESEARCH-hud.md Q4 ("keeps cap - molecules_stacked arithmetic in the pure engine, WSL-testable; the HUD reads engine.molecules_remaining — GAME-07"):
      ```python
      @property
      def molecules_remaining(self):
          """cap - molecules_stacked (None if cap is None). For the HUD (GAME-07)."""
          return None if self.cap is None else self.cap - self.molecules_stacked
      ```
    - NOTHING else changes in the engine (no timing, no RNG — preserves the PURE/deterministic contract; existing 433-test suite must stay green).

    Run: `python3.6 -m unittest discover -s tests -p "test_hud_logic.py" -v` — green. Then `python3.6 tests/run_gates.py` — all gates green. Commit: `feat(04-02): implement HUD pure display helpers + engine molecules_remaining`.
  </implementation>
</feature>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_hud_logic.py" -v` — all cases green (ran RED first, then GREEN).
- `python3.6 tests/run_gates.py` — all default gates green (purity: hud_logic auto-classifies PURE — verify no violation line for serpentrum/hud_logic.py; full suite green including prior 433 tests).
- `grep -n "pymol\|PyQt5\|numpy" serpentrum/hud_logic.py` — zero hits outside the docstring policy text (AST gate is the authority; grep is a sanity check).
</verification>

<success_criteria>
- serpentrum/hud_logic.py exists as a PURE stdlib-only module with format_elapsed + remaining_text, unit-tested.
- GameEngine.molecules_remaining property live, None-safe, tested.
- GameTab (plan 04-05) can now consume `hud_logic.format_elapsed(...)` and `engine.molecules_remaining` with zero GUI-side math.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-02-SUMMARY.md`
</output>
