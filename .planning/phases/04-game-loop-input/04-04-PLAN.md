---
phase: 04-game-loop-input
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - serpentrum/input.py
  - smoke/06_input_smoke.py
  - smoke/manual_wizard_keys_check.py
  - tests/run_gates.py
  - AGENTS.md
autonomous: true
must_haves:
  truths:
    - "A KeySteerWizard with get_event_mask() == event_mask_key + event_mask_special (12) is the ONLY idiomatic route that receives up/down arrows — set_key('up'/'down') is stored but NEVER invoked (C PyMOL_Special forces grabbed=1 for UP/DOWN and skips the key_mappings dispatch, PyMOL.cpp:2320-2336)."
    - "do_special maps GLUT codes 100/101/102/103 to 'left'/'up'/'right'/'down' and forwards them to the injected steer callback (the engine.request_direction seam); unmapped codes no-op without crashing."
    - "install/set_active/teardown give the game a leak-free wizard lifecycle: prior wizard saved and restored, set_active(False) grabs-but-no-ops (pause semantics), no cmd.key_mappings mutation ever."
    - "The headless smoke machine-proves the mechanism (mask, do_special dispatch, lifecycle round-trips); the real-GUI keys harness is staged for the plan 04-07 human-verify."
  artifacts:
    - path: serpentrum/input.py
      provides: "KeySteerWizard(Wizard) + install(steer_fn) (idempotent, returns prior) + set_active(active) + teardown(handle=None) — BRIDGE purity class"
      min_lines: 80
      contains: "class KeySteerWizard"
    - path: smoke/06_input_smoke.py
      provides: "REQUIRED headless smoke: mask==12, do_special dispatch, install/set_active/teardown round-trips, prior-wizard restore, key_mappings untouched; sentinel SMOKE-OK INPUT-WIZARD"
      contains: "SMOKE-OK INPUT-WIZARD"
    - path: smoke/manual_wizard_keys_check.py
      provides: "REAL-GUI (not headless, NOT NN_-prefixed so run_gates never auto-runs it) keys harness: materialized scene + installed wizard + srp_keys_off teardown command"
      contains: "srp_keys_off"
  key_links:
    - from: serpentrum/input.py
      to: serpentrum/game_engine.py
      via: "steer callback IS engine.request_direction — the wizard never touches engine internals (stateless input layer; buffers max-1 at tick boundary, game_engine.py:311-365)"
      pattern: "request_direction"
    - from: serpentrum/input.py
      to: serpentrum/gui_game.py
      via: "GameTab._begin_play/_teardown_round call input.install/set_active/teardown (wired in plan 04-08) — teardown(handle=None) accepts the opaque handle BOTH routes return so a fallback swap is a one-line import change"
      pattern: "def teardown"
---

<objective>
Land the INPUT SPIKE's primary mechanism (04-RESEARCH-input.md bottom line, verbatim): a PyMOL Wizard subclass (`do_special` route) as the arrow-key steering layer — the ONLY route that receives up/down — with the researched install/set_active/teardown lifecycle, proven headlessly by a new REQUIRED smoke, and a real-GUI harness staged for the early keys human-verify (plan 04-07, failure-cheap per the roadmap).

Purpose: GAME-03 (all four arrow keys steer) hinges entirely on this mechanism. Everything downstream (pause input-gating, focus mitigation, the final loop) builds on the install/set_active/teardown seam, so it lands FIRST in Wave 2 and gets its own human-verify before more wiring piles on.

Output: serpentrum/input.py (BRIDGE) + smoke/06_input_smoke.py (REQUIRED) + smoke/manual_wizard_keys_check.py (real-GUI harness) + AGENTS.md convention note.

Scope guards (OUT of scope): NO cmd.set_key anywhere (dead for up/down — S2; mutates global key_mappings — S5); NO Qt/eventFilter code in this plan (the designed FALLBACK serpentrum/gui_input.py is only built if plan 04-07's human-verify fails — different purity class, separate gap plan); NO engine changes; NO tick loop. NEVER use `internal._special(...)` as evidence of up/down bindability (the documented TRAP: the Python path fires set_key callbacks that the REAL C path skips).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` + `python3.6 tests/run_gates.py --smoke` (verdict = flushed SMOKE-OK sentinels, NEVER exit codes). Single-smoke debug: `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\06_input_smoke.py"` from repo root.

@.planning/phases/04-game-loop-input/04-RESEARCH-input.md (THE source — follow it verbatim: bottom line + W1-W8 claims; Q2 event-mask gate (mask 12 MANDATORY, base returns pick+select and do_special NEVER fires without it); Q3 focus stealing + mitigations; Q5 recommended route + the two purity-class placement table + "teardown(handle=None) uniformity"; the "Concrete code sketches" section is the implementation template; pitfalls 1-12; human-verify checklist = plan 04-07's script)
@serpentrum/game_engine.py (request_direction, game_engine.py:311-365 — the seam contract: unknown name raises ValueError, 180-degree/same-direction/buffer-full return False; the input layer ignores the return value or logs it)
@serpentrum/pymol_bridge.py (HEAD_NAME, move_head_delta — the manual harness nudges the head visibly per keypress; object_exists from plan 04-03)
@smoke/01_skeleton_smoke.py (smoke template rules: _resolve_root, flush=True, check() runner, sentinels)
@tools/check_purity.py (BRIDGE_MODULES already contains 'serpentrum/input.py' from plan 04-01 — gate 2 will pass with `from pymol import cmd` at module level; Qt/numpy banned)
@.planning/STATE.md (Phase-4 blocker note: "up/down arrow-key binding is the one open mechanism question... First human-verify of keys lands in Phase 4 so failure is cheap")
</context>

<tasks>

<task type="auto">
  <name>Task 1: serpentrum/input.py — KeySteerWizard + lifecycle (BRIDGE)</name>
  <files>serpentrum/input.py</files>
  <action>
Create serpentrum/input.py following the research code sketch (04-RESEARCH-input.md "Primary route") with these contracts:

- Module docstring: state the verified dispatch chain (real keypress → PyMOLQtGUI.keyPressEvent → pymolwidget.pymol.button → C PyMOL_Special → WizardDoSpecial FIRST (PyMOL.cpp:2318) → do_special(k,x,y,mod)); WHY the wizard and not set_key (S2: PyMOL.cpp:2320-2336 forces grabbed=1 for UP/DOWN, skipping the _special/key_mappings path — set_key('up'/'down') callbacks are stored but never invoked; S5: set_key mutates session-global key_mappings, a leak); the TRAP warning (internal._special fires set_key callbacks for all four arrows headlessly — NEVER evidence); BRIDGE purity statement (pymol.wizard + cmd, NEVER Qt/numpy, .exec_() never); python3.6 syntax.
- `from pymol import cmd` + `from pymol.wizard import Wizard` at module level (BRIDGE-legal per plan 04-01).
- `_CODE_TO_DIR = {100: 'left', 101: 'up', 102: 'right', 103: 'down'}` — the GLUT special-key codes (internal.special_key_codes, internal.py:414-417; do_special receives the INT, W3/pitfall 7).
- `class KeySteerWizard(Wizard):`
  - `__init__(self, steer_fn, _self=cmd)`: `Wizard.__init__(self, _self)`; store steer_fn (callable(name)->ignored; the controller binds engine.request_direction); `self._active = True`.
  - `get_event_mask(self)`: `return Wizard.event_mask_key + Wizard.event_mask_special` — 12; MANDATORY (W1/pitfall 2: the base returns pick+select=3 and do_special never fires without event_mask_special; both constants are verified CLASS attributes, pymol/wizard/__init__.py:6-9).
  - `get_prompt(self)`: 'serpentrum: click the 3D viewer, then steer with arrow keys' (focus mitigation (a), Q3).
  - `get_panel(self)`: `[[1, 'serpentrum', ''], [2, 'Quit game', 'cmd.set_wizard()']]` (per sketch).
  - `do_special(self, k, x, y, mod)`: map `int(k)` through _CODE_TO_DIR; if a name AND self._active: call self._steer(name) — IGNORE the steer return value (request_direction's False for 180-degree/same/buffer-full is the ENGINE's authority, not ours); ALWAYS `return True` (grab: suppresses the left/right movie-frame-step default, W4; for up/down the C-layer grab is unconditional anyway).
- Module functions (the lifecycle the game calls):
  - `install(steer_fn)` — idempotent: if cmd.get_wizard() is already a KeySteerWizard, return its saved prior WITHOUT re-installing; else save prior = cmd.get_wizard(), create the wizard, stash `wiz._saved_wizard = prior`, `cmd.set_wizard(wiz)`, `cmd.refresh_wizard()`, return prior. (Pitfall 5/6: wizard displacement + singleShot double-fire safety.)
  - `set_active(active)` — find the active KeySteerWizard; set `_active = bool(active)`. Pause semantics: wizard STAYS installed but grabs-and-no-ops (PITFALLS.md #4 "pausing may keep bindings but queue them"; research open-q 5 resolution: keep during pause, tear down only at game-over/restart/quit).
  - `teardown(handle=None)` — accept-and-IGNORE the handle argument (uniformity with the future eventFilter fallback so GameTab code is route-agnostic, Q5 note); restore: if the active wizard is a KeySteerWizard, `cmd.set_wizard(getattr(wiz, '_saved_wizard', None))` + `cmd.refresh_wizard()`. Idempotent (no wizard / foreign wizard → no-op). NEVER touches cmd.key_mappings (assert-by-design: grep 'set_key' must be zero).

Commit: `feat(04-04): wizard steering input route (BRIDGE)`.
  </action>
  <verify>python3.6 tests/run_gates.py — gates green (gate 2 purity: input.py classifies BRIDGE; no Qt/numpy; no .exec_; plugin-path safety unaffected). grep 'set_key' serpentrum/input.py → zero hits.</verify>
  <done>input.py implements the researched KeySteerWizard + install/set_active/teardown exactly; purity clean; no key_mappings mutation anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Headless smoke 06 (input mechanism) + REQUIRED registration</name>
  <files>smoke/06_input_smoke.py, tests/run_gates.py</files>
  <action>
Create smoke/06_input_smoke.py on the smoke/01 template (defensive _resolve_root, sys.path.insert(0, ROOT), flush=True everywhere, check() runner, sentinel `SMOKE-OK INPUT-WIZARD`). Steps — every claim from the research's headless-proven set (W1-W6):

1. `s_build` — collector = []; wiz = srp_input.KeySteerWizard(collector.append); assert wiz.get_event_mask() == 12 (and == Wizard.event_mask_key + Wizard.event_mask_special).
2. `s_do_special_dispatch` — for code, name in ((100,'left'),(101,'up'),(102,'right'),(103,'down')): assert wiz.do_special(code, 0, 0, 0) is True; after the loop assert collector == ['left','up','right','down'] IN ORDER (W3/W6: direct do_special call proven headless by the 2026-09-12 probe).
3. `s_unmapped_code` — collector.clear(); assert wiz.do_special(1, 0, 0, 0) is True (still grabs) and collector == [] (F1=1 unmapped → no-op, pitfall 7).
4. `s_install` — prior = srp_input.install(collector.append); assert isinstance(cmd.get_wizard(), srp_input.KeySteerWizard); prior2 = srp_input.install(collector.append); assert prior2 == prior and isinstance(cmd.get_wizard(), srp_input.KeySteerWizard) — idempotent, no double-install (pitfall 6).
5. `s_set_active` — srp_input.set_active(False); n0 = len(collector); assert cmd.get_wizard().do_special(102, 0, 0, 0) is True and len(collector) == n0 (grabs-but-no-ops — pause semantics); srp_input.set_active(True); do_special(102,...) → collector grew (steers again).
6. `s_teardown` — srp_input.teardown(); assert cmd.get_wizard() is None; srp_input.teardown() again → no exception (idempotent); assert srp_input.teardown(handle='opaque') also works (route-swap uniformity contract).
7. `s_prior_restore` — from pymol.wizard import Wizard as BaseWizard; pw = BaseWizard(); cmd.set_wizard(pw); ours = srp_input.install(collector.append) — assert ours is pw (install returns the displaced prior); srp_input.teardown(); assert cmd.get_wizard() is pw (W5 round-trip — the bioCHEMeleon save/restore pattern).
8. `s_no_key_leak` — assert cmd.key_mappings.get('up') is None and cmd.key_mappings.get('down') is None after the full lifecycle (S5: the wizard route never mutates global key_mappings; probe DEFAULTS_BEFORE showed up/down default None).

Register 'smoke/06_input_smoke.py' in tests/run_gates.py REQUIRED_SMOKES (after smoke/05, sorted). Commit: `test(04-04): input-wizard smoke + required registration`.
  </action>
  <verify>
- `python3.6 -m py_compile smoke/06_input_smoke.py` OK.
- `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\06_input_smoke.py"` from repo root → `SMOKE-OK INPUT-WIZARD`, no SMOKE-FAIL.
- `python3.6 tests/run_gates.py --smoke` → gate 4 green with all five sentinels echoed.
  </verify>
  <done>All 8 mechanism steps machine-proven headless; REQUIRED_SMOKES = 01/03/04/05/06; gate green.</done>
</task>

<task type="auto">
  <name>Task 3: Real-GUI keys harness (manual_wizard_keys_check.py) + AGENTS.md convention</name>
  <files>smoke/manual_wizard_keys_check.py, AGENTS.md</files>
  <action>
1. Create smoke/manual_wizard_keys_check.py — a REAL-GUI harness the human runs from PyMOL's command line in plan 04-07. Naming rule: NOT NN_-prefixed so `run_gates --smoke` (glob smoke/[0-9][0-9]_*.py) NEVER auto-runs it headless. Content:
   - Same defensive _resolve_root + sys.path.insert(0, ROOT) as the other smokes; flush=True on every print.
   - Imports: `from pymol import cmd`; `from serpentrum import pymol_bridge, setup_logic, setloader, game_engine`; `from serpentrum import input as srp_input`.
   - Scene: records, errs = setloader.load_demo_set(setloader.package_data_dir(), 'set_a', setloader.default_stacking_path()); assert not errs; setup = setup_logic.new_setup(); pymol_bridge.materialize(setup, records) (box + benzene head appear, framed).
   - Engine seam: (x0, y0), (x1, y1) = setup_logic.BOX_PRESETS['medium']; engine = game_engine.GameEngine(head=(0.0, 0.0), heading='right', box_min=(x0, y0), box_max=(x1, y1)).
   - `def _steer(name):` — ok = engine.request_direction(name); d = game_engine.DIRS[name]; pymol_bridge.move_head_delta(d[0] * 0.3, d[1] * 0.3, 0.0); print('KEY %-5s -> request_direction=%s (head nudged 0.3 A; False = engine refused: reversal/same/buffer-full)' % (name, ok), flush=True). (The nudge makes the wizard route VISIBLE without a tick loop — this verifies W7 exactly; the real snake movement arrives with the wired loop.)
   - prior = srp_input.install(_steer); print the prompt line + instructions: click the 3D viewer, press each arrow; then click the serpentrum dialog / command line and press arrows (focus test); note up/down may scroll command-line history (cosmetic, W8).
   - `def _keys_off():` — srp_input.teardown(); print('wizard now: %r' % (cmd.get_wizard(),), flush=True). Register with `cmd.extend('srp_keys_off', _keys_off)` so the human types `srp_keys_off` in the PyMOL command line to tear down.
   - Print: 'HARNESS READY — steer with arrows after clicking the viewer; type srp_keys_off to restore.' (flush=True).
   - python3.6 syntax; NO widget construction (works in real GUI; harmless headless but never auto-run).
2. AGENTS.md: under the plugin conventions section, add one bullet: "Arrow-key steering (Phase 4) uses the KeySteerWizard (serpentrum/input.py, BRIDGE): cmd.set_wizard(prior) save/restore on teardown; NEVER cmd.set_key for arrows (up/down never fire — C PyMOL_Special skips them; left/right would leak global key_mappings). smoke/manual_wizard_keys_check.py is the real-GUI keys harness (type srp_keys_off in PyMOL to restore)." Commit: `docs(04-04): real-GUI keys harness + steering convention`.
  </action>
  <verify>
- `python3.6 -m py_compile smoke/manual_wizard_keys_check.py` OK; `python3.6 tests/run_gates.py` green (gate 1 syntax walk covers the new file; plugin-path safety unaffected — it is inside smoke/, not root-level, and no __init__.py added).
- `grep -n "manual_wizard_keys_check" tests/run_gates.py` → zero hits (NOT registered as a smoke).
- AGENTS.md diff is the single convention bullet.
  </verify>
  <done>Real-GUI harness staged for the plan 04-07 checkpoint (exact run command + srp_keys_off teardown); AGENTS.md documents the steering convention; gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — exit 0.
- `python3.6 tests/run_gates.py --smoke` — exit 0 with notes echoing all five sentinels: SMOKE-OK SKELETON, SMOKE-OK VIEWER-BRIDGE, SMOKE-OK VIEWER-DEMO, SMOKE-OK LOOP-CAMERA, SMOKE-OK INPUT-WIZARD.
- grep contracts: `grep -rn "set_key" serpentrum/` → zero hits; `grep -n "event_mask" serpentrum/input.py` → returns 12 via the two Wizard class constants.
</verification>

<success_criteria>
- The input spike's PRIMARY route is implemented, machine-proven headless (mask 12, dispatch order, lifecycle, no leak), and staged for live human verification.
- The seam contract (install/set_active/teardown with opaque handle) is fixed so plan 04-08 wires it route-agnostically and a fallback swap (if 04-07 mandates it) is a one-line import change.
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-04-SUMMARY.md`
</output>
