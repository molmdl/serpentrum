---
phase: 04-game-loop-input
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - serpentrum/__init__.py
  - serpentrum/gui_game.py
autonomous: true
must_haves:
  truths:
    - "begin_game(setup) tears down any live session first, builds a deterministic GameEngine from the setup dict (head (0,0), heading 'right', preset box, pickups=None, cap, atom_budget), re-centers srp_head when present, anchors the session on _serpentrum.game_session, and runs the epoch-guarded 3-2-1-GO! countdown."
    - "After GO! the tick QTimer fires at 100 ms (dt=0.1 → 0.3 A/tick at SPEED_A_PER_S=3.0): engine.step events move srp_head via pymol_bridge.move_head_delta, log turn-refusals/crash into the read-only info box, and a finished engine ends the run."
    - "The elapsed label recomputes from the wall clock every 1 s (time.time() - start_time - paused_accum, clamped ≥ 0) via hud_logic.format_elapsed — never accumulated tick counts (Pitfall 5)."
    - "Pause freezes the tick and rebases elapsed on resume (paused_accum += now - pause_time, Pitfall 9.3); Restart re-runs the countdown deterministically with no double timer (epoch guard, Pitfall 9.1)."
    - "The game SESSION (engine + epoch + start_time + paused_accum + status) anchors on pmg_tk.startup._serpentrum.game_session — never module globals, never the widget (Pitfall 7); QTimers are Qt-parent-owned and die with the widget on reload."
  artifacts:
    - path: serpentrum/gui_game.py
      provides: "GameTab(QWidget): countdown label, read-only info box, elapsed + remaining labels, pause/resume toggle, restart, two QTimers (100 ms tick, 1 s elapsed), epoch guard, session lifecycle — GUI purity class"
      min_lines: 200
      contains: "class GameTab"
    - path: serpentrum/__init__.py
      provides: "game_session = None field on _SerpentrumState (anchor survives reload; initialized None on FIRST anchor creation only)"
      contains: "game_session"
  key_links:
    - from: serpentrum/gui_game.py
      to: serpentrum/pymol_bridge.py
      via: "_on_tick: ('moved', (nx, ny)) → pymol_bridge.move_head_delta(nx - old_x, ny - old_y, 0.0) — the per-tick movement seam from plan 04-03"
      pattern: "move_head_delta"
    - from: serpentrum/gui_game.py
      to: serpentrum/hud_logic.py
      via: "_on_elapsed_tick → hud_logic.format_elapsed; _update_remaining → hud_logic.remaining_text(engine.molecules_remaining)"
      pattern: "hud_logic"
    - from: serpentrum/gui_game.py
      to: serpentrum/game_engine.py
      via: "_build_engine seeds GameEngine from setup_logic.BOX_PRESETS + setup dict; _on_tick calls engine.step(TICK_DT); pause/resume/restart via engine methods"
      pattern: "engine\\.step"
    - from: serpentrum/__init__.py
      to: serpentrum/gui_game.py
      via: "GameTab writes self._anchor.game_session = session (mirrors the 03-07 setup-dict anchor pattern)"
      pattern: "game_session"
---

<objective>
Build the Game tab HUD and the tick loop's GUI half (HUD research Q1-Q6): a new `serpentrum/gui_game.py` with `GameTab(QWidget)` — epoch-guarded 3-2-1 countdown, 100 ms movement tick driving `engine.step` + `move_head_delta`, 1 Hz delta-based elapsed label, molecules-remaining label, read-only rolling info box, pause/resume with time rebase, deterministic restart — plus the `game_session` anchor field in `__init__.py`. This is the GAME-01/GAME-07/GAME-08 engine-and-display core; the countdown + tick run here, camera/input wiring lands in plan 04-08.

Purpose: the phase's "loop+countdown" plan, running in parallel with the input plan (04-04) exactly as the roadmap note prescribes ("loop+countdown can be a parallel plan once the spike lands"). Consumes the Wave-1 seams (hud_logic, bridge movement, purity GUI entry).

Output: serpentrum/gui_game.py + serpentrum/__init__.py anchor field; gates green; GUI verdicts deferred to the checkpoints (01-05 decision: headless widget construction is a dead end).

Scope guards (OUT of scope): NO camera lock/unlock and NO input install in this plan (plan 04-08 wires them into _begin_play/_teardown — a clearly-marked seam comment is left); NO Start button / tab switching (plan 04-06); NO auto-pause-on-focus (plan 04-08); NO pickups/stacking/win-handoff (Phase 5 — pickups=None is seeded, 'won' is logged-but-unreachable, GAME-09/10 excluded).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
**Repo path discipline (binding):** ALL agent work uses /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum — NEVER the /home/lwng/... symlink view (caused permission rejections twice).

**Gates:** `python3.6 tests/run_gates.py` from repo root. NOTE: gui_game.py is a GUI module — NOT importable in WSL python3.6 (pymol.Qt at module level); it is verified by py_compile + AST purity + grep contracts, NEVER by headless widget construction (STATE.md 01-05: dialog construction kills headless PyMOL — do NOT retry).

@.planning/phases/04-game-loop-input/04-RESEARCH-hud.md (THE source — Q2 widget inventory + two-distinct-timers table; Q3 anchor/epoch design; Q4 engine seam table; Q5 purity imports; the full GameTab class skeleton (CODE SKETCHES) is the implementation template; Pitfalls A-G)
@.planning/phases/04-game-loop-input/04-RESEARCH-gameloop.md (Q1 thread model: the QTimer timeout slot IS on the GUI thread — cmd-safe, no threads; Q6 countdown/pause mechanics + the _begin_play/_teardown_round sketches; S2: tick 100 ms = 0.3 A/tick)
@.planning/phases/03-molecules-in-the-viewer-setup-tab/03-07-SUMMARY.md (SetupTab conventions this plan mirrors: _build_widgets/_build_layout/_wire_signals, anchor write-back, blockSignals discipline, static-QMessageBox-only rule)
@serpentrum/gui_setup.py (the pattern source: __init__(anchor_state, parent), _loading guard, btn_row layout)
@serpentrum/game_engine.py (step events list game_engine.py:560-718; pause/resume 720-731; reset 233-270; SPEED_A_PER_S=3.0 line 55; finished/result 198-202)
@serpentrum/hud_logic.py + serpentrum/pymol_bridge.py (the Wave-1 seams consumed here: format_elapsed, remaining_text, molecules_remaining, move_head_delta, object_exists, HEAD_NAME, place_head)
@serpentrum/__init__.py (the _anchor() pattern: class-body fields initialize on FIRST anchor creation only; reload reuses the existing anchor)
@tools/check_purity.py (GUI_MODULES already contains 'serpentrum/gui_game.py' from plan 04-01)
@.planning/STATE.md (01-05 dead end; 03-07 anchor decision)
</context>

<tasks>

<task type="auto">
  <name>Task 1: game_session anchor field in __init__.py</name>
  <files>serpentrum/__init__.py</files>
  <action>
In serpentrum/__init__.py, add ONE field to the _SerpentrumState class body (inside _anchor(), after `setup = None`):

```python
game_session = None  # the live game session dict (Phase 4, plan 04-05):
                     # {'engine', 'epoch', 'start_time', 'paused_accum',
                     #  'status'} — created by GameTab.begin_game; None
                     #  until first Start; anchor survives Plugin-Manager
                     #  reload (Pitfall 7) so the session dict is
                     #  single-instance by construction.
```

Mirror the setup-dict anchor pattern exactly (03-07): class-body default → initialized None on FIRST anchor creation only; reload reuses the existing anchor object so a live session survives; GameTab (this plan's Task 2) is the sole writer. No other changes to __init__.py (entry stays stdlib-only at module level; the comment must not contain banned literals — no `exec_(` text).
  </action>
  <verify>python3.6 tests/run_gates.py — all gates green (entry purity unchanged: module level still stdlib-only; test_skeleton tests unaffected).</verify>
  <done>_SerpentrumState declares game_session = None with the documented lifecycle; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: GameTab module — widgets, session lifecycle, countdown, tick, pause/restart</name>
  <files>serpentrum/gui_game.py</files>
  <action>
Create serpentrum/gui_game.py following the HUD research GameTab skeleton (CODE SKETCHES section) with these exact contracts:

- Module docstring: GUI purity class (pymol.Qt ONLY at module level — check_purity GUI_MODULES entry landed in 04-01; relative imports of pure/bridge modules are exempt; .exec_() BANNED — no modals, the info box shows verdicts; python3.6 %-formatting). State the reload model: QTimers are Qt-parent-owned (die with the widget on Plugin-Manager reload); the session anchors on _serpentrum.game_session (survives reload); every singleShot/timeout callback is epoch-guarded (Pitfall 9.1).
- Module-level imports EXACTLY: `import time`, `from pymol.Qt import QtWidgets, QtCore`, `from . import game_engine`, `from . import hud_logic`, `from . import pymol_bridge`, `from . import setup_logic`. NEVER `from pymol import cmd` (GUI rule).
- Constants: `TICK_DT = 0.1`, `TICK_INTERVAL_MS = 100` (0.3 A/tick at SPEED_A_PER_S=3.0 — matches the engine's tested parameter), `ELAPSED_INTERVAL_MS = 1000`.
- `class GameTab(QtWidgets.QWidget)` with `__init__(self, anchor_state=None, parent=None)` mirroring SetupTab: store anchor; `self._epoch = 0`; `self._session = None`; `_build_widgets()`, `_build_layout()`, `_wire_signals()`, two QTimers `QtCore.QTimer(self)` (tick + elapsed, setInterval + timeout.connect), `_set_idle_state()`.

Widgets (research Q2 inventory): countdown QLabel ('ready', 28 pt, AlignCenter); info_box = read-only QTextEdit; elapsed_label ('0:00'); remaining_label ('Remaining: -'); pause_btn = checkable QPushButton('Pause'); restart_btn = QPushButton('Restart'); hint_label = word-wrapped QLabel 'Click Start on the Setup tab. Steer with arrow keys; click the 3D viewer first if keys seem dead.' Layout: VBox[countdown, info_box(stretch 1), HBox[Elapsed: + elapsed_label + stretch + Molecules + remaining_label], HBox[pause, restart, stretch], hint].

Session lifecycle:
- `begin_game(setup)` — called by PluginDialog (plan 04-06) and by Restart: (1) `self._teardown_live_session()` FIRST (restart-mid-game + reload-mid-game paths, research Pitfall A/12); (2) re-center the head if the scene exists: `if pymol_bridge.object_exists(pymol_bridge.HEAD_NAME): pymol_bridge.place_head(pymol_bridge.HEAD_NAME)` (Restart must return the head to origin — the engine resets to (0,0) and the object must match); (3) `engine = self._build_engine(setup)`; (4) session dict `{'engine': engine, 'epoch': self._epoch, 'start_time': None, 'paused_accum': 0.0, 'status': 'countdown'}`; (5) `self._anchor.game_session = session` when anchor is not None; (6) clear info box, log 'Get ready...'; (7) `self._run_countdown(3)`.
- `self._epoch` is the SINGLE authority — CORRECTED EPOCH DESIGN (the research sketch increments twice; do NOT copy that): _teardown_live_session bumps self._epoch; begin_game records session['epoch'] = self._epoch AFTER the bump (equal); every countdown callback closes over `scheduled = self._epoch` at SCHEDULE time and no-ops if `scheduled != self._epoch`; _on_tick no-ops if `self._session is None or self._session['epoch'] != self._epoch`. No second increment in begin_game.
- `_build_engine(setup)` — `(x0, y0), (x1, y1) = setup_logic.BOX_PRESETS[setup['box_preset']]`; `game_engine.GameEngine(head=(0.0, 0.0), heading='right', box_min=(x0, y0), box_max=(x1, y1), pickups=None, cap=setup.get('win_cap_molecules'), atom_budget=setup.get('atom_budget'))` (Phase 4 seeds: pickups=None).
- `_run_countdown(n)` / `_countdown_step(n, scheduled)` — recursive `QtCore.QTimer.singleShot(1000, ...)` chain, epoch-guarded; n>0: label str(n) + log; n==0: label 'GO!', log, `self._begin_play(scheduled)`.
- `_begin_play(scheduled)` — stale guard first; session['start_time'] = time.time(); status 'playing'; tick + elapsed timers .start(); _update_remaining(); pause_btn.setEnabled(True); log 'Move with the arrow keys.'; THEN a clearly-marked seam comment: `# Plan 04-08 wires pymol_bridge.lock_camera() + input install HERE`.
- `_on_tick()` — guards (stale session / engine.paused / engine.finished → return); `old = engine.head`; `events = engine.step(TICK_DT)`; iterate: on `('moved', (nx, ny))` set moved-new pos; all other events → `self._handle_event(ev, engine)`; after the loop, if a moved event occurred: `pymol_bridge.move_head_delta(nx - old[0], ny - old[1], 0.0)` (compute nx/ny from the LAST moved event or engine.head — engine truth, drift-free per gameloop M3); `self._update_remaining()`; if engine.finished: `self._end_run(engine)`.
- `_handle_event(ev, engine)` — 'turn_refused' → log 'turn refused: %s' % ev[1]; 'crashed' → log 'crashed into %s' % ev[1]; 'won' → log 'YOU WIN' (unreachable in Phase 4 — pickups=None; kept for Phase 5); 'moved'/'turning' → silent (too chatty at 10 Hz, research Q2).
- `_on_elapsed_tick()` — guards; `elapsed = time.time() - session['start_time'] - session['paused_accum']`; clamp < 0 → 0.0; `self.elapsed_label.setText(hud_logic.format_elapsed(elapsed))` (delta-based, NEVER accumulated — Pitfall 5).
- `_update_remaining()` — `hud_logic.remaining_text(engine.molecules_remaining)`; no session → 'Remaining: -'.
- `_on_pause_toggled(checked)` — checked: engine.pause(); tick timer .stop(); session['_pause_time'] = time.time(); status 'paused'; pause_btn.setText('Resume'); log 'paused'. else: pt = session.pop('_pause_time', None); if pt: session['paused_accum'] += time.time() - pt (Pitfall 9.3 rebase); engine.resume(); tick .start(); status 'playing'; pause_btn.setText('Pause'); log 'resumed'. (input.set_active gating is plan 04-08; camera stays untouched here.)
- `_on_restart()` — needs a live session AND anchor.setup (guard + log 'restart needs a setup dict' if missing); teardown then `self.begin_game(self._anchor.setup)`.
- `_end_run(engine)` — stop BOTH timers; status 'over'; pause_btn disabled; log 'run over: %s' % (engine.result or 'over'). (Full camera/input teardown is plan 04-08's _teardown_round — leave a seam comment.)
- `_teardown_live_session()` — stop both timers; `self._epoch += 1`; reset the pause button PROGRAMMATICALLY under `blockSignals(True/False)` (Pitfall G: programmatic setChecked(False) would fire toggled → spurious resume): block, setChecked(False), setText('Pause'), setEnabled(False), unblock.
- `_log(msg)` — `self.info_box.append(str(msg))`. `_set_idle_state()` — pause + restart disabled.

Commit: `feat(04-05): GameTab HUD + epoch-guarded countdown/tick + anchored game_session`.
  </action>
  <verify>
- `python3.6 tests/run_gates.py` — all gates green: gate 1 py_compile walks gui_game.py; gate 2 purity passes (GUI class: pymol.Qt only, relative imports exempt, zero .exec_ calls); gate 3 full suite green (no behavioral tests possible headless — 01-05).
- grep contracts: `grep -n "from pymol import" serpentrum/gui_game.py` → ZERO hits; `grep -n "exec_(" serpentrum/gui_game.py` → zero hits; `grep -n "QTimer(" serpentrum/gui_game.py` → both timers parented to self.
- Diff review against the research skeleton: epoch single-authority design (no double increment), move_head_delta seam present, hud_logic consumed.
  </verify>
  <done>GameTab implements the researched HUD + tick + countdown + pause/restart + anchored session; purity/syntax green; seams for 04-06 (begin_game entry) and 04-08 (camera/input wiring) clearly marked.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` — exit 0 (purity explicitly: no violation line for serpentrum/gui_game.py).
- grep contracts as listed in Task 2's verify.
- `grep -n "game_session" serpentrum/__init__.py serpentrum/gui_game.py` — the field declaration + the anchor write in begin_game.
- Structural self-check: two DISTINCT QTimer instances (100 ms + 1000 ms); epoch guard present in _countdown_step AND _on_tick; paused_accum rebase present; blockSignals around programmatic pause-button mutation.
</verification>

<success_criteria>
- The Game tab's engine-and-display core is complete and purity-clean: countdown, tick movement via the bridge, elapsed/remaining/info-box, pause/resume with rebase, deterministic restart, anchored session.
- Plans 04-06 (dialog wiring) and 04-08 (camera/input wiring) can integrate without restructuring GameTab.
- GAME-01's countdown+movement mechanics, GAME-07's HUD/pause/restart, and GAME-08's fixed-dt constant speed all have their GUI implementation in place (live verdicts land in plans 04-07/04-09).
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-loop-input/04-05-SUMMARY.md`
</output>
