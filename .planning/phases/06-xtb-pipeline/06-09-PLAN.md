---
phase: 06-xtb-pipeline
plan: 09
type: execute
wave: 3
depends_on: ["06-01", "06-04", "06-05", "06-06"]
files_modified:
  - serpentrum/gui.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Clicking Get Spectra launches the pipeline: reads last_run, parses snake_xyz, logs the counts line + budget_guard warnings, resolves the xtb binary, disarms Get Spectra, and starts XtbRunController on a fresh %TEMP% run dir — the dialog and viewer never block"
    - "Precondition failures surface as CLEAR lines and no launch: last_run/snake_xyz missing ('complete a game first'), unparseable snake_xyz (xyzio.XyzError quoted), xtb binary not found ('set the xtb path on the Setup tab')"
    - "The Spectra placeholder page carries the Phase-6 affordances: a status label, a streaming log area, and a Cancel/Run-again button — wired to the controller's signals; Phase 7 replaces the page CONTENT, the signal contract survives"
    - "After any terminal branch (ok/failed/cancelled) Get Spectra re-enables and the placeholder button flips to 'Run again' — a new run can start after cancel or completion (SC2)"
    - "The spectra_requested signal contract is UNCHANGED (model-A, locked decision 9); the bottom 6-button row stays untouched (Phase 8)"
  artifacts:
    - path: serpentrum/gui.py
      provides: "_on_spectra_requested launch pipeline (counts -> guard -> binary -> start), _build_spectra_placeholder (status/log/cancel), _launch_spectra_run/_on_run_finished handlers, controller create-or-reuse on the anchor"
      contains: "_launch_spectra_run"
  key_links:
    - from: "serpentrum/gui.py _on_spectra_requested"
      to: "serpentrum/budget_guard.launch_budget_warnings"
      via: "head-inclusive counts parsed from last_run['snake_xyz'] (engine truth) vs viewer counts from pymol_bridge.chain_atom_counts over the frozen chain_objects — warnings logged verbatim"
      pattern: "launch_budget_warnings"
    - from: "serpentrum/gui.py _launch_spectra_run"
      to: "anchor.spectra_runner.start(...)"
      via: "xtbenv.detect_binary(setup xtb_path) -> controller start with xtbenv.new_run_dir base = tempfile.gettempdir(); snake_id from last_run"
      pattern: "detect_binary"
---

<objective>
Wire the user-facing launch path: interpose the Phase-6 pipeline into `PluginDialog._on_spectra_requested` (counts re-check + guard warnings + binary resolution + async runner start + Get Spectra disarm) and give the Spectra placeholder page its temporary status/log/cancel affordances.

Purpose: SC1 (user launches a run, UI stays responsive), SC2 (cancel + relaunch reachable), SC3 (clear failures, never fake success) and SC4 (warning shown before launch) all become USER-REACHABLE in Phase 6 — without building any Phase-7 Spectra-tab UI.
Output: one edited GUI module (gui.py only).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@.planning/phases/05-stacking-game-rules/05-15-SUMMARY.md
@serpentrum/gui.py

Key facts:
- `_on_spectra_requested` (gui.py:96-105) today only does tabs.setCurrentIndex(2). The GameTab -> Dialog signal contract DOES NOT CHANGE (locked decision 9 / model-A). Extend the SLOT (composition root), never the signal.
- last_run contract (05-15-SUMMARY.md:105 + 06-06): {'result','molecules_stacked','atoms_total','chain_objects','snake_id','snake_xyz'} — snake_xyz None/missing = refuse with a clear line (EQ-desync-1's refuse case: precondition failure, not a desync warning).
- Head-inclusive counting (research guard Q1/pitfall 1): atoms_engine = len(xyzio.read_xyz_text(snake_xyz).atoms) — NEVER last_run['atoms_total'] (head-excluded); molecules_engine incl. head = last_run['molecules_stacked'] + 1; viewer counts via pymol_bridge.chain_atom_counts(last_run['chain_objects']) (sum for atoms, len for molecules incl. head).
- Guard: budget_guard.launch_budget_warnings(molecules_stacked, molecules_view, atoms_engine, atoms_view, atom_budget) + budget_guard.launch_counts_line(...) — log verbatim; atom_budget from self._anchor.setup, falling back to setup_logic.DEFAULTS['atom_budget'] when setup is None (reload hole; NEVER crash).
- Binary: exe = xtbenv.detect_binary(setup.get('xtb_path') when setup else None); None -> clear failure line ('xtb not found - set the xtb path on the Setup tab (auto-detect found nothing)') + no launch + Get Spectra stays enabled (nothing started). SETUP-05's configured path is the user seam; the runner adds no fallback list (EQ-binary-1: smoke-only convenience).
- Corrupted input (SC3): wrap the xyzio read in try/except XyzError -> clear line quoting exc + return (no launch). Engine-built input should never be corrupt; the guard is the launch-boundary contract.
- EQ-ux-1 (placeholder affordances): the Phase-5 pattern for not-yet-UI'd behavior is a placeholder surface + signals (research guard Q6). Phase 6 replaces the generic placeholder page for Spectra with: status QLabel (wordwrap), a read-only log area (QTextBrowser, bounded ~200 lines), and one QPushButton labeled contextually ('Cancel xtb run' while running / 'Run again' when terminal). Phase 7 replaces the page CONTENT (05-15-SUMMARY:61); these widgets are explicitly temporary scaffolding.
- EQ-ux-2 (message routing): launch/progress/verdict lines go to the placeholder status+log area (visible on the tab the user just switched to); the counts/warning lines ALSO go to the game info box via game_tab.log_external (added in 06-06) — the established channel stays fed.
- EQ-guard-2: log-lines-only for over-budget (no confirm dialog).
- Re-entrancy (research guard Q5): the launch API owns the Get Spectra disarm (setEnabled(False) at launch); re-enable on run_finished (terminal). The placeholder 'Run again' path bypasses Get Spectra (same _launch_spectra_run routine, re-reading last_run).
- Controller ownership: create-or-reuse `self._anchor.spectra_runner` (XtbRunController(self._anchor)) at FIRST launch; connect its signals ONCE (guard flag) — reload creates a new dialog which reuses the anchored controller; connect to the NEW dialog's slots then (the old dialog is gone; Qt connections die with it — reconnect fresh each dialog construction via the flag stored on the DIALOG, not the anchor).
- Runtime base_dir: tempfile.gettempdir() evaluated INSIDE Windows PyMOL (never /mnt/c paths at runtime — STACK.md:168 anti-pattern).
- gui.py is NOT in Phase 5.2's file list (consent lives in gui_setup/gui_game) — no 5.2 gate needed beyond its wave dependencies.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Placeholder page rebuild — status, log, Cancel/Run-again</name>
  <files>serpentrum/gui.py</files>
  <action>
    In PluginDialog.__init__, replace the generic _TAB_DEFS loop usage for the 'spectra' entry with an explicit `_build_spectra_placeholder(parent)` call returning the page; keep _TAB_DEFS for any OTHER future placeholders (only spectra exists today — move its text into the builder).
    _build_spectra_placeholder builds (Qt via pymol.Qt ONLY):
    - status QLabel (wordwrap True) — self.spectra_status; initial text: the existing placeholder hint + 'complete a game, then press Get Spectra on the Game tab'.
    - self.spectra_log = QTextBrowser (read-only, max ~200 lines trimmed on append) — the streaming area for runner log_line events.
    - self.spectra_run_btn = QPushButton('Cancel xtb run') — initially DISABLED with tooltip 'no xtb run yet'; clicked -> self._on_spectra_run_button().
    - Layout: status on top, log stretching, button row bottom-right. NO new bottom-row buttons (Phase 8's reserved row stays empty).
    Handler `_on_spectra_run_button`: if the anchored controller exists and status() == 'running' -> controller.cancel(); elif last_run has snake_xyz -> self._launch_spectra_run() (Run again); else log the refuse line into the status area.
    Wire runner signals lazily in `_connect_runner(controller)`: guard with a self._runner_connected flag (dialog-scoped, NOT anchor-scoped); connect started -> _on_runner_started, log_line -> _on_runner_log_line, run_finished -> _on_run_finished. Slots: started -> status 'xtb running... (async - the dialog stays responsive)'; log_line -> append to spectra_log; run_finished(status, problems) -> status line 'xtb finished: %s' + problems joined ('; ') when non-empty; re-enable game_tab.get_spectra_btn (guard getattr None); flip spectra_run_btn to 'Run again' + enabled.
    Run gates. Commit: `feat(06-09): Spectra placeholder status/log/cancel affordances wired to the runner signals`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST check: PluginDialog defines _build_spectra_placeholder, _connect_runner, _on_spectra_run_button, _on_run_finished; grep confirms no .exec_( and no new bottom-row buttons</verify>
  <done>The placeholder page is the temporary Phase-6 control surface: status, streaming log, contextual cancel/run-again button — all signal-wired, all replaceable by Phase 7.</done>
</task>

<task type="auto">
  <name>Task 2: Launch pipeline interpose in _on_spectra_requested</name>
  <files>serpentrum/gui.py</files>
  <action>
    Extend `_on_spectra_requested` (keep setCurrentIndex(2) FIRST — the tab switch is GAME-09/Phase-7 UX):
    1. anchor guard: self._anchor None -> status line 'plugin state unavailable - reopen the plugin' + return (never raise).
    2. record = getattr(anchor, 'last_run', None); refuse when record is None or not record.get('snake_xyz'): line 'no completed snake to run - play a game to completion first (Get Spectra activates on win or crash)' into spectra_status + game_tab.log_external (guard getattr); return (Get Spectra stays as-is — nothing started).
    3. Counts (head-inclusive): try atoms/molecules from xyzio.read_xyz_text(record['snake_xyz']) except xyzio.XyzError as exc -> clear corrupted-input line ('run input is corrupt: <exc>') + return (SC3). molecules_stacked = record['molecules_stacked']; names = record['chain_objects'] or []; view_counts = pymol_bridge.chain_atom_counts(names) (import pymol_bridge — pure-module + bridge imports from GUI are legal); atoms_view = sum(view_counts); molecules_view = len(names).
    4. Guard + log: atom_budget = anchor.setup.get('atom_budget') if getattr(anchor, 'setup', None) else setup_logic.DEFAULTS['atom_budget'] (import setup_logic); lines = [budget_guard.launch_counts_line(molecules_stacked, atoms_engine, atom_budget)] + budget_guard.launch_budget_warnings(molecules_stacked, molecules_view, atoms_engine, atoms_view, atom_budget); for each: spectra_status append + game_tab.log_external(line).
    5. Binary: exe = xtbenv.detect_binary(anchor.setup.get('xtb_path') if anchor.setup else None); None -> 'xtb not found - set the xtb path on the Setup tab (auto-detect found nothing)' + log_external + return.
    6. Disarm + launch: game_tab.get_spectra_btn.setEnabled(False) (guard getattr; the launch API owns the disarm — research guard Q5); self._launch_spectra_run(record, exe).
    `_launch_spectra_run(self, record, exe)`: controller = getattr(anchor, 'spectra_runner', None); if None: controller = xtb_runner.XtbRunController(anchor); anchor.spectra_runner = controller; self._connect_runner(controller); started = controller.start(record['snake_xyz'], exe, tempfile.gettempdir(), record['snake_id']); if not started: re-enable get_spectra_btn (the guard emitted its own log line) + status note.
    Import block additions (module top, GUI-legal): from . import budget_guard, setup_logic, xtb_runner, xtbenv, xyzio; from . import pymol_bridge. NO Qt beyond pymol.Qt; NO .exec_().
    Do NOT change: spectra_requested signal signature, GameTab internals, the reserved bottom row.
    Run gates + AST checks. Commit: `feat(06-09): launch pipeline — counts re-check, guard warnings, binary resolve, async runner start (SPECTRA-02/06)`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST: _on_spectra_requested calls _launch_spectra_run and references budget_guard + detect_binary + chain_atom_counts; spectra_requested connect line unchanged (grep 'spectra_requested.connect' exactly one)</verify>
  <done>Get Spectra now runs the full Phase-6 pipeline: re-check, warn, resolve, disarm, async start — with clear refuse lines for every precondition failure.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- AST/grep audit: `grep -c "exec_" serpentrum/gui.py` == 0; `grep -c "spectra_requested.connect" serpentrum/gui.py` == 1; `grep -n "addStretch(1)" serpentrum/gui.py` still shows the reserved row untouched.
- The live end-to-end behavior (launch/cancel/relaunch in the real GUI) is the 06-12 checkpoint's job — this plan's bar is structural green + AST-verified wiring.
</verification>

<success_criteria>
- SC1: a Get Spectra click starts the real runner asynchronously with visible status; SC2: cancel + run-again are one click away; SC3: every failure path produces a specific clear line and never a fake success; SC4: the counts line + any over-budget warning appear BEFORE the run starts.
- Phase 7 can replace the placeholder page content wholesale; the runner signal contract and the launch routine survive.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-09-SUMMARY.md`
</output>
