---
phase: 06-xtb-pipeline
plan: 11
type: execute
wave: 3
depends_on: ["06-02", "06-07", "5.2-09"]
files_modified:
  - serpentrum/setup_logic.py
  - tests/test_setup_logic.py
  - serpentrum/xtb_run.py
  - tests/test_xtb_run.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The HESSIAN_WARNING literal reflects the MEASURED ~104-atom wall time from 06-CALIBRATION.md (amended only if the measurement materially contradicts '30-90 s'; confirmed-and-kept otherwise) with every test pin updated — an owner-visible literal diff"
    - "The runner's default thread posture is DECIDED from the measurement: either DEFAULT_RUN_KNOBS / a '-P' default lands in xtb_run (jank mitigation) or the uncapped default is explicitly confirmed and documented — no undecided middle state"
    - "atom_budget default stays 100 unless the measurement says otherwise (research Q7: constants + notes over schema churn); any change is a documented owner-visible decision"
    - "Full gates stay green after the edits (test_setup_logic warning pins + test_xtb_run default pins re-pinned together with the literals)"
  artifacts:
    - path: serpentrum/setup_logic.py
      provides: "HESSIAN_WARNING literal (amended or confirmed-with-comment) citing the measured calibration numbers"
      contains: "HESSIAN_WARNING"
    - path: serpentrum/xtb_run.py
      provides: "DEFAULT_RUN_KNOBS final value (+ optional DEFAULT_THREAD_ARG) with the calibration cross-reference in the docstring"
      contains: "DEFAULT_RUN_KNOBS"
    - path: tests/test_setup_logic.py
      provides: "re-pinned HESSIAN_WARNING literal expectations (tests/test_setup_logic.py:109-110 region + validate-warning pins)"
      contains: "HESSIAN_WARNING"
    - path: tests/test_xtb_run.py
      provides: "re-pinned DEFAULT_RUN_KNOBS expectation (the plan 06-02 'uncapped until calibration' pin superseded)"
      contains: "DEFAULT_RUN_KNOBS"
  key_links:
    - from: "06-CALIBRATION.md (plan 06-07)"
      to: "serpentrum/setup_logic.HESSIAN_WARNING + serpentrum/xtb_run.DEFAULT_RUN_KNOBS"
      via: "SC5: 'warning threshold and OMP environment calibrated from the measurement' — the committed table is the authority; no re-measurement"
      pattern: "06-CALIBRATION"
---

<objective>
Apply the calibration outcomes (SC5): reconcile the HESSIAN_WARNING wording with the measured ~104-atom wall time and fix the runner's default thread posture in code + pins.

Purpose: SC5's "warning threshold and OMP environment calibrated from the measurement" is a CODE edit fed by 06-CALIBRATION.md — owner-visible literal diffs, re-pinned tests, gates green.
Output: focused edits to setup_logic.py / xtb_run.py and their two test files. Nothing else.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-CALIBRATION.md
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@serpentrum/setup_logic.py

Key facts:
- HESSIAN_WARNING = 'hessian cost scales ~N^3; a ~100-atom snake may take 30-90 s' (setup_logic.py:118-119); the literal is pinned at tests/test_setup_logic.py:109-110 and asserted via [HESSIAN_WARNING] equality in the validate-warning tests (:221,:229,:239). budget_guard's drift-pin (06-01) makes the launch line follow automatically.
- Calibration decision inputs (06-CALIBRATION.md from plan 06-07): measured 104-atom wall range, thread sweep (-P 1/2/4 vs default), OMP_STACKSIZE verdict, jank rationale (PITFALLS.md:333,348).
- EQ-omp-1 outcome slot: xtb_run.DEFAULT_RUN_KNOBS currently {} (the 06-02 pin says 'plan 06-11 may change this ONE literal'). The runner's start() already merges knobs into QProcessEnvironment; a '-P' argv default would be carried via extra_args — if the decision wants one, add xtb_run.DEFAULT_THREAD_ARG = ('-P', 'N') tuple-or-None and have the CONTROLLER's extra_args default (06-05) consume it — NOTE: the controller reads xtb_run.DEFAULT_RUN_KNOBS today; adding DEFAULT_THREAD_ARG requires a 2-line controller touch — ALLOWED here (xtb_runner.py joins files_modified ONLY if the decision picks an argv cap; otherwise untouched).
- Research Q7 guardrails: prefer constants + notes over schema churn; the THRESHOLD is atom_budget (default 100) — change ONLY with a strong measurement rationale; OMP_STACKSIZE only if the uncapped run crashed ([TRAIN] discharge).
- SHARED-FILE RULE: setup_logic.py + tests/test_setup_logic.py are Phase-5.2-shared files — depends_on: ["5.2-09"] orders this after the 5.2 wrap (owner directive).
- Amendment rule (speed-tier precedent, setup_logic.py:96-106): value/wording tweaks are edits to the constant + its test pins; the pin diff IS the reviewable change.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply calibration decisions to literals + defaults + pins</name>
  <files>serpentrum/setup_logic.py, tests/test_setup_logic.py, serpentrum/xtb_run.py, tests/test_xtb_run.py</files>
  <action>
    Read 06-CALIBRATION.md. Apply EXACTLY its decision inputs:

    1. HESSIAN_WARNING: if the measured ~104-atom wall range falls outside roughly half-to-double the stated '30-90 s' window, amend the literal to carry the measured range (keep the '~N^3' framing and the ~100-atom anchor, e.g. 'a ~100-atom snake may take <LOW>-<HIGH> s (measured)' — ASCII, same sentence shape); if the measurement CONFIRMS the window, leave the literal byte-identical and add a one-line comment citing 06-CALIBRATION.md ('measured 2026-09-XX: <range> — window confirmed'). Update tests/test_setup_logic.py:109-110 (and any other literal pins found by grep '30-90') to match EXACTLY.
    2. Thread posture: per 06-CALIBRATION.md's recommendation —
       - If a default cap is recommended: set xtb_run.DEFAULT_RUN_KNOBS to the chosen env knob dict (e.g. {'OMP_NUM_THREADS': '2'}) and, if the recommendation is argv-level, add DEFAULT_THREAD_ARG = ('-P', 'N') to xtb_run + a 2-line edit in serpentrum/xtb_runner.py start() so extra_args defaults to (XTB_OHESS,) + DEFAULT_THREAD_ARG when the caller passed None (keep the explicit-override path intact); update tests/test_xtb_run.py's DEFAULT_RUN_KNOBS pin + add a DEFAULT_THREAD_ARG pin.
       - If uncapped is recommended: keep DEFAULT_RUN_KNOBS = {} and REPLACE the 'plan 06-11 may change' docstring line with the confirmed decision + the calibration cross-reference; tests/test_xtb_run.py's pin stays {} (assert still passes).
    3. atom_budget default: keep 100 unless 06-CALIBRATION.md argues otherwise with the measured numbers; if changed, update DEFAULTS + every affected pin and flag it loudly in the SUMMARY as an owner-visible behavior change (it shifts the in-run budget warning too).
    4. Re-pin + run: `python3.6 -m unittest discover -s tests -p "test_setup_logic.py" -v` and `-p "test_xtb_run.py" -v`, then the FULL gate suite.
    Commit: `feat(06-11): calibration applied — HESSIAN_WARNING + runner thread default (SC5)`
  </action>
  <verify>python3.6 tests/run_gates.py green; grep '30-90' serpentrum/ tests/ returns only lines consistent with the applied decision (amended literal or confirmed comment); python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import xtb_run; print(xtb_run.DEFAULT_RUN_KNOBS)" prints the decided value</verify>
  <done>SC5's code half is done: the warning wording and the runner's thread posture are calibration-derived, pinned, and gate-green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (full suite incl. the re-pinned files).
- 06-CALIBRATION.md's decision inputs are each reflected in exactly one code location (literal / constant / comment) — no orphan decisions.
- `git diff --stat` shows only the four (or five, with the controller cap) listed files.
</verification>

<success_criteria>
- The shipped warning text and runner default are derived from measurements, not extrapolation; every pin travels with its literal; the controller honors the default without behavior surprises (explicit caller overrides still win).
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-11-SUMMARY.md`
</output>
