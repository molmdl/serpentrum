---
phase: 06-xtb-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["5.2-09"]
files_modified:
  - serpentrum/pymol_bridge.py
  - smoke/10_chain_count_smoke.py
  - tests/run_gates.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "pymol_bridge.chain_atom_counts(names) returns the per-object atom count for each named chain object, in input order, via cmd.get_model (the verified-safe reader) — the viewer-side head-inclusive count channel for the SPECTRA-06 cross-check"
    - "A missing object yields 0 (documented) instead of raising, so a stale scene becomes a guard desync WARNING (plan 06-09), never a launch crash"
    - "smoke/10_chain_count_smoke.py is a REQUIRED gate smoke proving the count channel against real loaded molecules cross-pinned to the manifest atom_count values"
  artifacts:
    - path: serpentrum/pymol_bridge.py
      provides: "chain_atom_counts(names) -> [int] — BRIDGE-class addition (pymol/pmg_tk any level, no Qt); no coordinate export added (EQ-xyz-1)"
      contains: "def chain_atom_counts"
    - path: smoke/10_chain_count_smoke.py
      provides: "REQUIRED headless smoke: load benzene/naphthalene as srp_* chain objects, assert chain_object_names + chain_atom_counts vs manifest atom_count (12/18), cleanup_srp"
      contains: "SMOKE-OK CHAIN-COUNT"
    - path: tests/run_gates.py
      provides: "REQUIRED_SMOKES gains smoke/10_chain_count_smoke.py (bridge-class smoke, same requiredness class as smokes 03-08)"
      contains: "10_chain_count_smoke.py"
  key_links:
    - from: "serpentrum/pymol_bridge.chain_atom_counts"
      to: "cmd.get_model(name)"
      via: "per-object model read; len(model.atom) — get_model is documented safe (pymol_bridge.py:323), NEVER get_object_ttt (segfaults)"
      pattern: "get_model"
    - from: "smoke/10_chain_count_smoke.py"
      to: "serpentrum/data/manifest.json"
      via: "atom_count cross-pin (benzene 12, naphthalene 18) — the same validated field setloader carries (setloader.py:108-109)"
      pattern: "atom_count"
---

<objective>
Add the viewer-side atom-count seam to the bridge (`chain_atom_counts`) and pin it with a REQUIRED headless smoke — the counts-only half of the SPECTRA-06 cross-check channel.

Purpose: the launch re-check (plan 06-09) compares the head-inclusive run-input count against what the viewer actually shows; per 06-RESEARCH-guard Q1 the viewer channel is the desync detector. Per EQ-xyz-1 NO coordinate export is built (the engine-atom xyz from plans 06-02/06-06 is the single run-input truth).
Output: one bridge function, one REQUIRED smoke, one REQUIRED_SMOKES entry.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@serpentrum/pymol_bridge.py

Key facts:
- chain_object_names() already exists (pymol_bridge.py:443-455): sorted srp_head/srp_seg* public names, completion-only read, frozen by 05-15-SUMMARY.md:105 as the handoff membership list. The launch API reads names from the FROZEN last_run record (reload-safe), then counts via this new function.
- cmd.get_model(name) is safe (pymol_bridge.py:320-323 notes get_extent/get_model safe; get_object_ttt segfaults — NEVER used). The model object exposes .atom (list); len(model.atom) is the atom count.
- BRIDGE purity class (tools/check_purity.py:70-73): pymol/pmg_tk any level; PyQt5/numpy NEVER; .exec_() never. chain_atom_counts stays inside pymol_bridge.py — no new module, no allowlist edit.
- EQ-xyz-1 (this plan's half): counts only. A full chain_xyz coord export is deliberately NOT built — the run input comes from engine/session atoms (06-02 build_run_input + 06-06 seam); reading viewer coords would create a second coordinate-truth channel and duplicate the engine path.
- Missing-object policy: return 0 for a name that no longer resolves (documented in the docstring) — a vanished chain object is a DESYNC (guard warning), not a crash.
- Manifest cross-pin data: benzene atom_count 12, naphthalene 18 (serpentrum/data/manifest.json, verified).
- Smoke template obligations: smoke/01_skeleton_smoke.py:1-21 (named steps, check(), flush=True, _resolve_root, no widgets, sentinel verdict `SMOKE-OK CHAIN-COUNT`).
- run_gates.py:55-61 REQUIRED_SMOKES is a tuple; gate_smoke (:150-188) fails the gate when a required smoke is missing or fails. Adding the entry makes the count channel a permanent regression gate.
- SHARED-FILE RULE: serpentrum/pymol_bridge.py is Phase-5.2-shared per the ROADMAP Phase-6 note (the 5.2-09 wrap's conditional-fix task may touch any file) — depends_on: ["5.2-09"] orders it after the 5.2 wrap (owner directive). NOTE: no shipped 5.2 plan's files_modified actually lists pymol_bridge.py — the edge exists because 5.2-09's fix reach is open-ended, not because of a listed 5.2 edit.

RESOLVED DECISIONS owned by this plan:
- EQ-xyz-1 (bridge half): counts-only bridge seam (no coord export).
- EQ-smoke-1 (requiredness): smoke 10 is REQUIRED — it is bridge-class (no xtb.exe dependency, same class as smokes 03-08 which are all required); smokes 09/11 (xtb-dependent) stay informational.
- Smoke numbering: 09 = qprocess (lands wave 1, plan 06-03), 10 = chain count (this plan), 11 = runner (plan 06-08) — chronological landing order; requiredness is explicit in REQUIRED_SMOKES, not encoded in the number.
</context>

<tasks>

<task type="auto">
  <name>Task 1: bridge chain_atom_counts + REQUIRED smoke 10 + run_gates entry</name>
  <files>serpentrum/pymol_bridge.py, smoke/10_chain_count_smoke.py, tests/run_gates.py</files>
  <action>
    1. serpentrum/pymol_bridge.py — append to the Phase-5 completion-seams section (after chain_object_names):
       `def chain_atom_counts(names)` -> list[int]: for each name, `model = cmd.get_model(name)`; count = len(model.atom) when the model resolves, else 0 (docstring: a vanished object is a desync signal for budget_guard, never a crash; the launch API compares this sum against the run-input count). Order-preserving (input order). Docstring names the consumer: SPECTRA-06 launch cross-check (plan 06-09) + EQ-xyz-1 (counts-only — coords come from engine atoms, never re-read here). BRIDGE rules respected: no Qt, no .exec_().
    2. smoke/10_chain_count_smoke.py — template obligations from smoke/01 (named steps + check() + flush=True + _resolve_root + sentinel `SMOKE-OK CHAIN-COUNT`):
       - s_bridge_import: import serpentrum.pymol_bridge under the loader name path (smoke 01 pattern).
       - s_load_head: pymol_bridge.cleanup_srp() first (clean slate); cmd.load(<root>/serpentrum/data/benzene.sdf, object='srp_head'); assert chain_object_names() == ['srp_head'].
       - s_count_head: chain_atom_counts(['srp_head']) == [12]; cross-pin vs manifest: json-load serpentrum/data/manifest.json, find the benzene record, assert its atom_count == 12 AND the bridge count equals it.
       - s_load_segment_count: cmd.load(<root>/serpentrum/data/naphthalene.sdf, object='srp_seg_0'); names now sorted ['srp_head', 'srp_seg_0']; counts == [12, 18]; manifest cross-pin naphthalene 18.
       - s_missing_object_zero: chain_atom_counts(['srp_seg_99']) == [0] (no raise).
       - s_cleanup: pymol_bridge.cleanup_srp() returns 2; chain_object_names() == [] afterwards.
    3. tests/run_gates.py — append 'smoke/10_chain_count_smoke.py' to REQUIRED_SMOKES with the comment `# (Phase 6, plan 06-04 — bridge chain atom-count channel)`.
    4. Run the WSL gates, then the smoke live.
    Commit: `feat(06-04): bridge chain_atom_counts + REQUIRED chain-count smoke (SPECTRA-06 viewer cross-check)`
  </action>
  <verify>python3.6 tests/run_gates.py green; timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\10_chain_count_smoke.py" output contains SMOKE-OK CHAIN-COUNT; python3.6 tests/run_gates.py --smoke reports smoke 10 as REQUIRED and PASSING</verify>
  <done>The viewer count channel exists, is smoke-pinned against manifest numbers, and is a permanent required gate.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (purity: the bridge addition is BRIDGE-legal; no new module).
- `python3.6 tests/run_gates.py --smoke`: REQUIRED smoke 10 PASS via SMOKE-OK CHAIN-COUNT sentinel.
- `grep -n "def chain_atom_counts" serpentrum/pymol_bridge.py` finds exactly one definition, placed after chain_object_names.
</verification>

<success_criteria>
- The SPECTRA-06 cross-check channel exists (names from the frozen record -> per-object counts), smoke-proven with manifest cross-pins, and regression-gated as REQUIRED.
- No coordinate export was added — the single run-input truth remains the engine-atom xyz (plans 06-02/06-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-04-SUMMARY.md`
</output>
