---
phase: 06-xtb-pipeline
plan: 05
type: execute
wave: 2
depends_on: ["06-02", "06-03", "5.2-09"]
files_modified:
  - serpentrum/xtb_runner.py
  - tools/check_purity.py
  - serpentrum/__init__.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "XtbRunController owns exactly one QProcess and exposes start(xyz_text, exe_path, base_dir, snake_id) / cancel() — start is a guarded no-op (returns False + log line) unless xtb_run.can_start passes (no-double-runs)"
    - "start writes snake.xyz INTO a fresh xtbenv.new_run_dir, launches with cwd=that dir and bare-relative argv via xtbenv.build_argv, injects only xtb_run-approved env knobs, and never calls waitForFinished"
    - "The terminal branch (finished OR error-fired) is the ONLY place status is resolved: cancel-requested -> 'cancelled', else xtbenv.evaluate_run verdict -> 'ok'/'failed'; it copies g98.out/vibspectrum/xtbopt.xyz/snake.xyz/xtb.log into the stable srp_spectra/<snake_id> dir, DELETES the spray run dir, writes _serpentrum.spectra_run, and clears the cancel flag — on EVERY terminal branch (bioCHEMeleon discipline)"
    - "The controller never imports pymol.cmd (GUI purity: pymol.Qt only) and anchors nothing in module globals — it lives on pmg_tk.startup._serpentrum"
    - "check_purity.classify('serpentrum/xtb_runner.py') == 'GUI' via a deliberate GUI_MODULES entry (04-05 precedent), and _SerpentrumState declares spectra_run with the frozen key docstring"
  artifacts:
    - path: serpentrum/xtb_runner.py
      provides: "XtbRunController(QtCore.QObject): signals started/log_line(str)/run_finished(str, list); thin shell over xtb_run + xtbenv; app-ensure helper; artifact copy-out + spray-dir deletion in the terminal branch"
      min_lines: 150
    - path: tools/check_purity.py
      provides: "GUI_MODULES gains 'serpentrum/xtb_runner.py' with a comment citing 06-RESEARCH-runner Q2 + the 04-05 gui_game precedent"
      contains: "xtb_runner"
    - path: serpentrum/__init__.py
      provides: "_SerpentrumState.spectra_run attribute (docstring: key set = xtb_run.SPECTRA_RUN_KEYS; written by the runner terminal branch, consumed by Phase 7) + last_run docstring extended with the 'snake_xyz' key (written by plan 06-06)"
      contains: "spectra_run"
  key_links:
    - from: "serpentrum/xtb_runner.py start()"
      to: "xtbenv.new_run_dir + build_argv + xtb_run.build_env"
      via: "fresh per-run temp dir, bare-relative 'snake.xyz' argv, knob-merged QProcessEnvironment — the dont_hand_roll surface reused verbatim"
      pattern: "new_run_dir"
    - from: "serpentrum/xtb_runner.py terminal branch"
      to: "xtbenv.evaluate_run + _serpentrum.spectra_run"
      via: "3-leg contract verdict -> status/problems -> frozen record -> run_finished(status, problems) signal"
      pattern: "evaluate_run"
---

<objective>
Build the GUI runner shell `serpentrum/xtb_runner.py` (XtbRunController): the single QProcess owner that turns `xtb_run`'s pure decisions into an async, cancellable subprocess lifecycle — plus its deliberate GUI allowlist entry and the `spectra_run` anchor attribute.

Purpose: SC1/SC2 mechanics (async start, cancel, no-double-runs) and SC3's contract-only success live here as a THIN shell over xtbenv + xtb_run; the roadmap's QProcess smoke (06-03) already proved every Qt mechanism in this exact runtime.
Output: one new GUI module, one allowlist edit, one anchor attribute.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@serpentrum/xtbenv.py
@tools/check_purity.py
@serpentrum/__init__.py

Key facts (research Q1-Q5, Q8 — all live-probed):
- Qt 5.12.9 via pymol.Qt; new-style .connect() only (house precedent; NEVER SIGNAL() strings). finished slot signature (code, status) — compare status against QtCore.QProcess.CrashExit/NormalExit enums, never magic ints.
- Error signal: `getattr(proc, 'errorOccurred', proc.error)` defensively (EQ-runner-1; smoke 09 prints which exists).
- App: QtCore.QCoreApplication.instance() — create ONLY if None (headless); never a second app (EQ-app-1). In the real GUI a QApplication already exists.
- Cancel: proc.kill() only; the killed run fails evaluate_run on its own (exit != 0 + missing files); the controller's _cancel_requested flag decides the 'cancelled' label (xtb_run.resolve_status).
- QByteArray -> text: bytes(proc.readAllStandardError()).decode('utf-8', 'replace'); stderr arrives CRLF — evaluate_run is CRLF-tolerant; feed stderr VERBATIM (never pre-filtered — the 'abnormal termination' substring trap).
- Run-dir lifecycle (EQ-artifact-1): spray dir = xtbenv.new_run_dir(tempfile.gettempdir()) (Windows %TEMP% at runtime — NEVER /mnt/c paths, NEVER the session dir); terminal branch copies g98.out, vibspectrum, xtbopt.xyz + writes snake.xyz + xtb.log into the stable dir os.path.join(tempfile.gettempdir(), 'srp_spectra', snake_id) (keep-until-replaced: the NEXT start deletes the previous stable dir before creating the new one), then shutil.rmtree the spray dir (guarded try/except; deletion only AFTER finished — pitfall 10).
- No polling: signals only; NEVER waitForFinished/subprocess.run/processEvents pumping (pitfall 4).
- Module identity: the controller instance is stored on the anchor (pmg_tk.startup._serpentrum) by the CALLER (plan 06-09 creates-or-reuses anchor.spectra_runner); this module must not touch module globals.
- Purity: GUI class = pymol.Qt imports only, any level; NO pymol.cmd (the runner never touches the viewer); no PyQt5/numpy; no .exec_(). tools/check_purity.py:57-63 GUI_MODULES is the deliberate allowlist (04-05 edit shape; entries for not-yet-created files are INERT so the allowlist edit can land first).
- Log lines: emit QtCore.Signal(str) per decoded line (split on newlines, skip empties); keep a bounded tail (last 500 lines) for the record's log file.
- python3.6 syntax (STACK.md:190): %-formatting, no f-strings/dataclasses/walrus — the WSL gate is binding even though the runtime is 3.9.13.

RESOLVED DECISIONS owned by this plan:
- EQ-app-1 + EQ-runner-1: as above (consume smoke 09's probe findings).
- EQ-artifact-1: copy-out-then-delete policy (research guard Q5a open question resolved: keep-until-replaced via the stable srp_spectra/<snake_id> dir; the spray dir dies at every terminal branch so temp never litters).
- Single-writer rule for serpentrum/__init__.py: this plan owns ALL Phase-6 edits to it (spectra_run attr + the last_run docstring extension mentioning 'snake_xyz', which plan 06-06 introduces in the same wave — doc-only forward reference).
- start() signature: start(self, xyz_text, exe_path, base_dir, snake_id, knobs=None, extra_args=None) -> bool. knobs None -> xtb_run.DEFAULT_RUN_KNOBS; extra_args None -> (xtbenv.XTB_OHESS,) only. Returns False (no-op) when can_start fails; emits a log_line explaining why.
</context>

<tasks>

<task type="auto">
  <name>Task 1: GUI allowlist entry (inert-first) + anchor attribute</name>
  <files>tools/check_purity.py, serpentrum/__init__.py</files>
  <action>
    1. tools/check_purity.py: add 'serpentrum/xtb_runner.py' to GUI_MODULES with the comment: `# xtb_runner.py = async QProcess xtb runner (Phase 6, plan 06-05): pymol.Qt only — 06-RESEARCH-runner.md Q2 (edit mirrors gui_game.py's 04-05 entry). The runner NEVER imports pymol.cmd — the viewer is not its concern.` Run `python3.6 tests/run_gates.py` — must stay green BEFORE the module exists (the entry is inert; check_tree walks existing files only).
    2. serpentrum/__init__.py: inside _SerpentrumState add `spectra_run = None` with the docstring: the live xtb run record (Phase 6, plan 06-05) — keys frozen by xtb_run.SPECTRA_RUN_KEYS ('snake_id','status','problems','input_path','g98_path','vibspectrum_path','xtbopt_path','log_path'); written ONLY by the runner's terminal branch; consumed by the Spectra stage (Phase 7). Extend the last_run attribute docstring: the record gains 'snake_xyz' (the head-inclusive engine-atom xyz text built at completion by plan 06-06 via xtb_run.build_run_input; None when the head mirror was unavailable).
    Commit: `feat(06-05): purity allowlist entry + spectra_run anchor attribute (inert-first)`
  </action>
  <verify>python3.6 tests/run_gates.py green; python3.6 -c "import sys; sys.path.insert(0,'tools'); import check_purity; print(check_purity.classify('serpentrum/xtb_runner.py'))" prints GUI</verify>
  <done>Allowlist entry inert-green before the module lands; anchor documents both records.</done>
</task>

<task type="auto">
  <name>Task 2: XtbRunController implementation</name>
  <files>serpentrum/xtb_runner.py</files>
  <action>
    Create serpentrum/xtb_runner.py (GUI class: `from pymol.Qt import QtCore` ONLY as the pymol-family import; stdlib os/shutil/tempfile/time fine; %-formatting; python3.6; module docstring: thin shell — every RULE lives in xtb_run/xtbenv, this file only wires Qt).

    Class XtbRunController(QtCore.QObject):
    - Signals (QtCore.Signal): started = Signal(); log_line = Signal(str); run_finished = Signal(str, list).
    - __init__(self, anchor_state=None): super().__init__(); self._anchor = anchor_state; self._status = None (xtb_run states); self._cancel_requested = False; self._log_lines = [] (bounded tail 500); self._stderr_text = ''; self._run_dir = None; self._stable_dir = None; self._snake_id = None; self._proc = None. Do NOT create the QProcess here (lazy in start) so an unused controller costs nothing.
    - `_ensure_app()` module-level helper: QtCore.QCoreApplication.instance() or QtCore.QCoreApplication([]) (EQ-app-1; called at the top of start()).
    - `status()` -> self._status (read-only accessor for the UI).
    - `start(self, xyz_text, exe_path, base_dir, snake_id, knobs=None, extra_args=None)`:
      1. _ensure_app(); if not xtb_run.can_start(self._status): emit log_line('an xtb run is already active - cancel or wait for it to finish'); return False (no-double-run guard, PITFALLS.md:66).
      2. Guards: not xyz_text -> _fail_before_start(['run input is empty - complete a game first'], snake_id) and return False (via the same record path as a failure so state never sticks at running).
      3. argv: try xtbenv.build_argv(exe_path, 'snake.xyz', extra_args=extra_args if extra_args is not None else (xtbenv.XTB_OHESS,)) except ValueError as exc -> _fail_before_start([str(exc)], snake_id); exe falsy -> _fail_before_start(['xtb not found - set the xtb path on the Setup tab'], snake_id).
      4. Run dir: self._run_dir = xtbenv.new_run_dir(base_dir); write snake.xyz into it (open(path,'w',encoding='utf-8')).write(xyz_text).
      5. Stable dir replace: if self._anchor is not None and getattr(self._anchor, 'spectra_run', None): old = that record's input_path dir (os.path.dirname) — shutil.rmtree(old, ignore_errors=True) when it exists under os.path.join(tempfile.gettempdir(), 'srp_spectra') (keep-until-replaced; NEVER rmtree an arbitrary path — guard with the prefix check).
      6. Process: self._proc = QtCore.QProcess(self); setWorkingDirectory(self._run_dir); env = QtCore.QProcessEnvironment.systemEnvironment(); for k, v in xtb_run.build_env({}, knobs if knobs is not None else xtb_run.DEFAULT_RUN_KNOBS).items(): env.insert(k, v); setProcessEnvironment(env); connect started -> self._on_started; finished(int, QProcess.ExitStatus) -> self._on_finished (new-style .connect); readyReadStandardOutput/readyReadStandardError -> _on_stdout/_on_stderr; the getattr error signal -> _on_error (EQ-runner-1). Reset self._stderr_text='', self._log_lines=[], self._cancel_requested=False; self._status = xtb_run.RUNNING; proc.start(argv[0], argv[1:]); emit started(); return True.
    - `cancel()`: if self._status == xtb_run.RUNNING and self._proc is not None: self._cancel_requested = True; self._proc.kill(); emit log_line('cancel requested - killing xtb'); else emit log_line('no running xtb job to cancel').
    - `_on_started`: emit log_line('xtb started') (minimal; the caller decorates).
    - `_on_stdout`/`_on_stderr`: decode chunk ('utf-8','replace'); stderr ALSO accumulates into self._stderr_text verbatim; split into lines; append non-empty to _log_lines (tail 500); emit log_line(line) per line.
    - `_on_finished(self, code, status)`: THE terminal branch — (a) verdict = xtbenv.evaluate_run(code, self._stderr_text, xtbenv.EXPECTED_FILES, os.listdir(self._run_dir) if self._run_dir and os.path.isdir(self._run_dir) else []); (b) status_text = xtb_run.resolve_status(self._cancel_requested, verdict.ok); (c) copy-out: stable = os.path.join(tempfile.gettempdir(), 'srp_spectra', self._snake_id); os.makedirs(stable, exist_ok=True); for fname in ('g98.out', 'vibspectrum', 'xtbopt.xyz'): if present in run_dir -> shutil.copy2 into stable; write snake.xyz (the input text — keep a reference self._input_text captured in start) and xtb.log ('\n'.join(self._log_lines)) into stable; (d) record = xtb_run.new_spectra_run(self._snake_id); record['status'] = status_text; record['problems'] = list(verdict.problems); record['input_path'] = os.path.join(stable, 'snake.xyz'); record['g98_path'/'vibspectrum_path'/'xtbopt_path'] = stable paths or None when absent; record['log_path'] = os.path.join(stable, 'xtb.log'); if self._anchor is not None: self._anchor.spectra_run = record; (e) shutil.rmtree(self._run_dir, ignore_errors=True) (spray dir dies ALWAYS — pitfall 10: only after finished); (f) terminal reset: self._status = {'ok': xtb_run.DONE, 'failed': xtb_run.FAILED, 'cancelled': xtb_run.CANCELLED}[status_text]; self._cancel_requested = False; self._proc = None; self._run_dir = None; (g) emit run_finished(status_text, list(verdict.problems)). Every branch flows through (a)-(g) — the bioCHEMeleon every-terminal-branch discipline; NO early returns after (a).
    - `_on_error(self, *args)`: if the process never started (FailedToStart) -> _fail_before_start(['xtb could not be started: %s' % (err description,)], self._snake_id) — same terminal discipline (record + signals + state reset), guarded so it no-ops when the run already finished.
    - `_fail_before_start(self, problems, snake_id)`: record = xtb_run.new_spectra_run(snake_id or 'unknown'); record['status'] = xtb_run.FAILED; record['problems'] = problems; anchor write; self._status = xtb_run.FAILED; emit run_finished(xtb_run.FAILED, problems).
    - NO pymol.cmd anywhere; NO .exec_(); NO module-level state.
    Run gates. Commit: `feat(06-05): XtbRunController — async cancellable QProcess runner shell (SPECTRA-02)`
  </action>
  <verify>python3.6 tests/run_gates.py green (purity classifies the new module GUI via the Task-1 entry; syntax py3.6); grep confirms no 'cmd.' and no 'waitForFinished' and no '.exec_(' in serpentrum/xtb_runner.py</verify>
  <done>The controller exists as a thin GUI shell; every decision (guard, verdict, env, artifact policy) delegates to xtb_run/xtbenv; live behavior proof lands in smoke 11 (plan 06-08).</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `python3.6 -c "import sys; sys.path.insert(0,'tools'); import check_purity; print(check_purity.classify('serpentrum/xtb_runner.py'))"` prints GUI.
- `grep -c "waitForFinished\|subprocess" serpentrum/xtb_runner.py` == 0; `grep -c "spectra_run" serpentrum/__init__.py` >= 1.
</verification>

<success_criteria>
- The runner shell exists with the full lifecycle (guarded start -> async signals -> single terminal branch with verdict + copy-out + spray deletion + anchor record + signal), purity-legal and py3.6-clean.
- Live verification (success contract, cancel, no-double-run) is smoke 11's job (plan 06-08) — this plan's bar is structural green + purity classification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-05-SUMMARY.md`
</output>
