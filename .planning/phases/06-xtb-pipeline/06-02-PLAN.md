---
phase: 06-xtb-pipeline
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/xtb_run.py
  - tests/test_xtb_run.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The run state machine admits start() only from idle/terminal states (idle, ok, failed, cancelled) — the no-double-run guard is a pure decision, tested exhaustively"
    - "resolve_status maps a cancel-requested finish to 'cancelled' regardless of the contract verdict, and otherwise to 'ok'/'failed' from xtbenv.evaluate_run"
    - "build_run_input assembles the head-inclusive snake xyz (head atoms first, then segments in engine order) via xyzio.write_xyz and the text round-trips through xyzio.read_xyz_text"
    - "build_env merges ONLY explicitly-passed, help-verified OMP knob keys (values stringified, empties rejected) and never mutates its input — no invented env knobs"
    - "The Phase-7 handoff record shape is frozen as data: SPECTRA_RUN_KEYS + new_spectra_run initial dict"
  artifacts:
    - path: serpentrum/xtb_run.py
      provides: "PURE runner half: state constants + can_start/resolve_status, build_env knob merge, build_run_input xyz assembler, new_spectra_run/SPECTRA_RUN_KEYS, DEFAULT_RUN_KNOBS"
      min_lines: 110
    - path: tests/test_xtb_run.py
      provides: "state-machine matrix, cancel mapping, env-merge contract (copy semantics + unknown-key rejection), xyz assembly/round-trip, record-shape pin"
      min_lines: 110
  key_links:
    - from: "serpentrum/xtb_run.py build_run_input"
      to: "serpentrum/xyzio.write_xyz"
      via: "the frozen xyz writer (fixture-proven round-trip); comment = 'serpentrum snake <snake_id>' single line"
      pattern: "write_xyz"
    - from: "serpentrum/xtb_run.py SPECTRA_RUN_KEYS"
      to: "serpentrum/xtb_runner.py (plan 06-05 terminal branch) and Phase 7 consumers"
      via: "the frozen _serpentrum.spectra_run record key set — Phase 7 parses/broadens from these paths without reshaping"
      pattern: "SPECTRA_RUN_KEYS"
---

<objective>
Create the PURE module `serpentrum/xtb_run.py`: the runner's decision half as WSL-testable code — run state machine (no-double-run guard, terminal discipline), cancel->status resolution, OMP env-knob merge, the snake run-input (xyz) assembler, and the frozen `spectra_run` record shape.

Purpose: keep every decision rule out of the Qt controller (plan 06-05) so python3.6 can unit-test them with zero stubs — the 02-02 xtbenv DI precedent.
Output: one new pure module + its test file. No existing module modified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@serpentrum/xtbenv.py
@serpentrum/xyzio.py

Key facts (research file:line anchors):
- Q2 layering: `serpentrum/xtb_run.py` (NEW, PURE, auto-covered) holds the state machine, env-dict merge and kill->verdict mapping; the GUI shell (06-05) stays thin and never re-implements a rule already here or in xtbenv.
- Q4: cancel = kill() -> finished(exitStatus=CrashExit) -> the killed run ALREADY fails the 3-leg contract (evaluate_run); the runner reports 'cancelled' via its own tracked flag. bioCHEMeleon discipline: pending flags cleared on EVERY terminal branch (done/failed/cancelled/error-fired) — the pure machine defines the terminal set.
- Q5/dont_hand_roll: argv via xtbenv.build_argv, run dir via xtbenv.new_run_dir, contract via xtbenv.evaluate_run — this module adds ONLY what those lack.
- Q6/finding 6 + 05-RESEARCH-core-integration:151: the final snake xyz = head_atoms + sum(seg['atoms']) assembled at completion — do NOT re-read PyMOL. head_atoms live in the GUI session (gui_game session['head_atoms'], engine never tracks them); segments carry (sym, x, y, z) 4-tuples (game_engine.py:192-237, _translate_chain keeps them live truth).
- Q6 thread knobs (VERIFIED only): argv `-P N` (help:197-198, log echo) and env OMP_NUM_THREADS / MKL_NUM_THREADS / OMP_STACKSIZE (help:229-231). OMP_STACKSIZE necessity is [TRAIN] LOW. WSL-set env does NOT cross into Windows reliably — env injection is the QProcess path's job (06-05).

RESOLVED DECISIONS owned by this plan:
- EQ-omp-1 (default thread cap): DEFAULT_RUN_KNOBS = {} — the runner ships UNCAPPED until the calibration experiment (plan 06-07) measures; plan 06-11 may set a default (e.g. {'OMP_NUM_THREADS': '2'}) from 06-CALIBRATION.md. The merge MECHANISM ships now; the default is a later data edit.
- EQ-xyz-1 (handoff seam shape): the run input is assembled at COMPLETION from engine/session atoms and stashed on last_run['snake_xyz'] (plan 06-06 wires the call). The viewer is NOT a coordinate source (no second truth channel; 05-RESEARCH:151 forbids re-reads) — plan 06-04's bridge addition is a counts-only cross-check. build_run_input raises ValueError on head_atoms None (the caller decides the refuse path).
- Record shape (EQ-artifact-1 carrier): SPECTRA_RUN_KEYS = ('snake_id','status','problems','input_path','g98_path','vibspectrum_path','xtbopt_path','log_path'); status values 'running'|'ok'|'failed'|'cancelled' (None record = idle). xtbopt.xyz is preserved for Phase 7's optimized-frame overlay decision (06-RESEARCH-guard Q5c).
- Status strings double as state-machine states: 'idle' (or None), 'running', 'ok', 'failed', 'cancelled'.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for xtb_run</name>
  <files>tests/test_xtb_run.py</files>
  <action>
    Create tests/test_xtb_run.py (house header: sys.path self-insert; NO __init__.py; unittest; %-formatting). Import `from serpentrum import xtb_run, xyzio, xtbenv` (module absent -> ImportError = RED).

    Class TestStateMachine:
    - test_can_start_matrix: can_start(None) True; can_start('idle') True; can_start('ok'|'failed'|'cancelled') True; can_start('running') False; can_start('bogus') False (unknown states never start).
    - test_terminal_set: 'ok','failed','cancelled' in xtb_run.TERMINAL_STATES; 'running','idle' not; the set is a frozenset.
    - test_resolve_status_cancel_wins: resolve_status(True, True) == 'cancelled' and resolve_status(True, False) == 'cancelled' (a cancelled run is never reported ok — even if a lingering stderr said 'normal termination').
    - test_resolve_status_contract: resolve_status(False, True) == 'ok'; resolve_status(False, False) == 'failed'.

    Class TestBuildEnv:
    - test_default_empty: build_env({'A': '1'}, None) == {'A': '1'} and the returned dict is NOT the input object (copy).
    - test_merge_stringifies: build_env({}, {'OMP_NUM_THREADS': 2}) == {'OMP_NUM_THREADS': '2'}.
    - test_no_mutation: base = {'X': '1'}; build_env(base, {'OMP_NUM_THREADS': '2'}); base unchanged.
    - test_unknown_key_rejected: build_env({}, {'FOO': '1'}) raises ValueError (only the help-verified keys OMP_NUM_THREADS/MKL_NUM_THREADS/OMP_STACKSIZE legal).
    - test_empty_value_rejected: build_env({}, {'OMP_NUM_THREADS': ''}) raises ValueError.
    - test_default_run_knobs_uncapped: xtb_run.DEFAULT_RUN_KNOBS == {} (EQ-omp-1 pin — plan 06-11 may change this ONE literal).

    Class TestBuildRunInput:
    - test_head_first_then_segments: head = [('C', 0.0, 0.0, 0.0), ('H', 1.0, 0.0, 0.0)]; segs = [[('O', 2.0, 0.0, 0.0)]] -> text round-trips via xyzio.read_xyz_text; atoms order is head then segment; count 3; comment line startswith 'serpentrum snake '.
    - test_comment_single_line: a snake_id containing '\\n' never breaks the frame (writer sanitizes; assert the parsed comment has no newline).
    - test_none_head_raises: build_run_input(None, [], 'run_1') raises ValueError.
    - test_multi_segment_order: two segments -> atoms in segment order (index 0 oldest first).
    - test_real_fixture_round_trip: parse .planning/research/xtb-spike-fixtures/co2.xyz via xyzio.read_xyz; rebuild atoms as head (no segments); build_run_input -> parse back -> same atom count (3) and symbols.

    Class TestSpectraRunRecord:
    - test_keys_frozen: set(xtb_run.SPECTRA_RUN_KEYS) == {'snake_id','status','problems','input_path','g98_path','vibspectrum_path','xtbopt_path','log_path'}.
    - test_new_record_initial: new_spectra_run('run_3') == {'snake_id': 'run_3', 'status': 'running', 'problems': [], 'input_path': None, 'g98_path': None, 'vibspectrum_path': None, 'xtbopt_path': None, 'log_path': None} (exact dict equality).

    Run: `python3.6 -m unittest discover -s tests -p "test_xtb_run.py" -v` -> ImportError (RED).
    Commit RED: `test(06-02): failing tests for xtb_run (state machine, env knobs, run-input builder, record shape)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_xtb_run.py" -v fails with ImportError; no other test file touched</verify>
  <done>RED confirmed: the runner's pure decision contract exists as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement serpentrum/xtb_run.py</name>
  <files>serpentrum/xtb_run.py</files>
  <action>
    Create serpentrum/xtb_run.py — PURE module (stdlib + `from . import xyzio` only; NO xtbenv import needed unless the docstring references it — keep imports minimal; %-formatting; python3.6). Module docstring: the runner's pure half (06-RESEARCH-runner Q2); the GUI controller (06-05) consumes these and adds NO rules of its own.

    1. Status constants: IDLE = 'idle'; RUNNING = 'running'; DONE = 'ok'; FAILED = 'failed'; CANCELLED = 'cancelled'. TERMINAL_STATES = frozenset((DONE, FAILED, CANCELLED)).
    2. `can_start(status)`: True iff status is None, IDLE, or in TERMINAL_STATES. Docstring: this IS the no-double-run guard; the controller calls it before every start.
    3. `resolve_status(cancel_requested, verdict_ok)`: 'cancelled' when cancel_requested (cancel WINS over the verdict — a killed run's partial stderr must never read as success), else 'ok'/'failed'.
    4. KNOWN_ENV_KNOBS = ('OMP_NUM_THREADS', 'MKL_NUM_THREADS', 'OMP_STACKSIZE') (xtb --help:229-231 verbatim set — the ONLY verified env knobs; comment cites the help capture and the no-invented-knobs rule).
    5. `build_env(base_env, knobs)`: base_env None -> {}; copy = dict(base_env or {}); knobs None -> return copy; for each key, value: key not in KNOWN_ENV_KNOBS -> ValueError("unknown xtb env knob '%s' (verified set: ...)"); value None or '' -> ValueError; copy[key] = str(value). Return the copy (input never mutated).
    6. DEFAULT_RUN_KNOBS = {} with the EQ-omp-1 docstring (uncapped until 06-CALIBRATION.md; plan 06-11 owns the default edit).
    7. `build_run_input(head_atoms, segment_atom_lists, snake_id)`: head_atoms None -> ValueError('head atoms unavailable - cannot build the run input'); elements/coords flattened head-first then each segment list in order (tuples are (sym, x, y, z)); return xyzio.write_xyz(elements, coords, comment='serpentrum snake %s' % (snake_id,)).
    8. SPECTRA_RUN_KEYS tuple + `new_spectra_run(snake_id)` returning the exact initial dict (status RUNNING, problems [], all paths None). Docstring: written by the controller's terminal branch onto _serpentrum.spectra_run; Phase 7 consumes the paths (parse g98 first, vibspectrum fallback — spectra.py:478-506 contract) and must never reshape the record.
    9. Run the file suite green, then full gates: `python3.6 tests/run_gates.py`.
    10. Commit GREEN: `feat(06-02): xtb_run — runner state machine, env knobs, run-input builder, spectra_run record (SPECTRA-02 pure half)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_xtb_run.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green</verify>
  <done>xtb_run.py exists (pure), state machine / cancel mapping / env merge / xyz assembly / record shape all pinned, gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import xtb_run; print(xtb_run.can_start('running'), xtb_run.resolve_status(True, True), xtb_run.DEFAULT_RUN_KNOBS)"` prints `False cancelled {}`.
</verification>

<success_criteria>
- Every rule the Qt controller needs (guard, cancel semantics, env knobs, input assembly, record shape) is a tested pure function; the controller (06-05) can be a thin shell.
- The Phase-7 handoff record shape is frozen as data before any GUI code exists.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-02-SUMMARY.md`
</output>
