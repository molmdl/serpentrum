---
phase: 06-xtb-pipeline
plan: 06
type: execute
wave: 2
depends_on: ["06-02", "5.2-09"]
files_modified:
  - serpentrum/gui_game.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "_present_completion anchors last_run WITH the new 'snake_xyz' key: the head-inclusive engine-atom xyz text built by xtb_run.build_run_input(session['head_atoms'], engine segment atoms, snake_id) — assembled at completion while the engine is alive, never re-read from PyMOL"
    - "When the head mirror is unavailable (session['head_atoms'] is None), snake_xyz is None — the launch API (plan 06-09) refuses with a clear line instead of crashing"
    - "A public GameTab.log_external(msg) wraps _log so the Phase-6 launch API (gui.py, plan 06-09) can surface launch decisions in the game-tab info box without reaching into privates"
    - "The 05-15 frozen contract is EXTENDED, not reshaped: existing five keys unchanged; Get Spectra lifecycle untouched (presenter-only enable)"
  artifacts:
    - path: serpentrum/gui_game.py
      provides: "_present_completion: snake_xyz assembly + last_run['snake_xyz']; GameTab.log_external public wrapper"
      contains: "snake_xyz"
  key_links:
    - from: "serpentrum/gui_game.py _present_completion"
      to: "serpentrum/xtb_run.build_run_input"
      via: "head atoms (session mirror) + engine segment atoms -> xyz text stashed on the anchor record — single write, reload-safe, no PyMOL re-read (05-RESEARCH-core-integration:151)"
      pattern: "build_run_input"
    - from: "serpentrum/gui_game.py log_external"
      to: "serpentrum/gui.py _on_spectra_requested (plan 06-09)"
      via: "the established one-line info-box channel for launch-time counts/warnings (06-RESEARCH-guard Q6 item 3)"
      pattern: "log_external"
---

<objective>
Wire the final-snake handoff seam: `_present_completion` assembles the run input (head-inclusive xyz text) from engine/session atoms and stashes it on `last_run['snake_xyz']`; GameTab gains the public `log_external` line channel for the Phase-6 launch API.

Purpose: SC1's "handed to a real xtb run" needs the input bytes at launch time. Per EQ-xyz-1 the source is the ENGINE/SESSION atom truth (single write at completion — reload-safe, zero PyMOL re-reads); the runner research's live-probed recommendation (finding 6) resolves the seam the guard research left as viewer-export.
Output: one focused edit to gui_game.py. No other file touched (the __init__.py last_run docstring update is owned by plan 06-05 — single-writer rule).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@.planning/phases/05-stacking-game-rules/05-15-SUMMARY.md
@serpentrum/gui_game.py

Key facts:
- The seam: `_present_completion` (gui_game.py:1289-1333) runs AFTER _teardown_round on the IDENTICAL win/crash path; the engine argument is alive; `session = self._session` is still set (status 'over'). Head atoms live in the session mirror `session['head_atoms']` (gui_game.py:390,460-517; maintained through moves AND sweeps at :662-667/:835-843 — pure float truth, probe-verified zero drift vs the viewer). Engine segments carry live ('sym', x, y, z) atom tuples kept current by _translate_chain/_advance_sweep (game_engine.py:527-547).
- 05-15-SUMMARY.md:105 freezes the consumption contract: last_run = {'result','molecules_stacked','atoms_total','chain_objects','snake_id'} — this plan EXTENDS it with 'snake_xyz' (additive evolution; nothing downstream reshapes).
- 05-RESEARCH-core-integration.md:151: "final snake xyz = head_atoms(translated) + sum of seg['atoms'] — do not re-read PyMOL". build_run_input (plan 06-02) wraps xyzio.write_xyz (comment sanitized).
- atoms_total EXCLUDES the head (test_phase5_integration.py:267-273) — snake_xyz is the head-inclusive truth the guard and xtb consume; the HUD completion line's 'spectra input size' label stays AS-IS (pinned test_hud_content.py:172,180; budget_guard's launch counts line carries the true number).
- `xtb_run.build_run_input(head_atoms, segment_atom_lists, snake_id)` raises ValueError on head_atoms None — the seam MUST branch explicitly (snake_xyz = None), never try/except theater.
- SHARED-FILE RULE: gui_game.py is in Phase 5.2's unmerged file list — depends_on: ["5.2-09"] orders this after the 5.2 wrap (owner directive). 5.2-06's consent edits land first; this edit touches only _present_completion + one new 3-line method, no overlap with consent logic.
- Verification reality: gui_game is a GUI module — NOT importable under WSL python3.6 (module-level pymol.Qt). Verification = gates + AST assertions (no widget construction in smokes; headless dialog checks are the 01-05 dead end).
</context>

<tasks>

<task type="auto">
  <name>Task 1: snake_xyz assembly + log_external in gui_game.py</name>
  <files>serpentrum/gui_game.py</files>
  <action>
    1. Module imports: add `from . import xtb_run` alongside the existing pure imports (purity-legal: GUI may import pure modules).
    2. `_present_completion` edit — the last_run dict literal block (gui_game.py:1325-1332 region) becomes:
       - `head_atoms = session['head_atoms'] if session is not None else None`
       - if head_atoms is not None: `snake_xyz = xtb_run.build_run_input(head_atoms, [seg['atoms'] for seg in engine.segments], 'run_%d' % session['epoch'])`; else `snake_xyz = None`.
       - Add `'snake_xyz': snake_xyz` to the anchored dict (all five existing keys byte-unchanged).
       - Extend the method docstring's item (d): the record now carries 'snake_xyz' — the head-inclusive engine-atom run input assembled HERE (engine alive), consumed by the Phase-6 runner; None when the head mirror was unavailable (the launch API refuses with a clear line); NEVER a PyMOL re-read (05-RESEARCH-core-integration:151).
    3. New public method after _log/_log_reason:
       `def log_external(self, msg)`: docstring — one line from an EXTERNAL stage (Phase-6 launch API: counts/warnings/verdicts); wraps _log (the coalescer break is correct: these are non-reason messages and the run is over); body: `self._log(msg)`.
    4. Do NOT touch: get_spectra_btn lifecycle, _teardown_round, completion_lines/breakdown_lines calls, any 5.2 consent code.
    5. Gates + AST assertion (GUI code is WSL-unimportable) — a small python3.6 heredoc script: ast.parse serpentrum/gui_game.py; find the _present_completion FunctionDef; assert its body references 'snake_xyz' and calls `build_run_input`; assert the GameTab class defines `log_external`; assert get_spectra_btn setEnabled(True) still occurs exactly once inside _present_completion.
    Commit: `feat(06-06): completion anchors snake_xyz (engine-atom run input) + log_external channel (SPECTRA-02 handoff)`
  </action>
  <verify>python3.6 tests/run_gates.py green; the AST assertion passes (snake_xyz assignment + build_run_input call inside _present_completion; log_external exists; presenter-only enable unchanged)</verify>
  <done>last_run carries the launch-ready xyz text at completion; the launch API has its info-box channel; the 05-15 contract is additively extended.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (the Phase-5 completion pins — test_phase5_integration.py — stay untouched-green).
- AST check: `_present_completion` contains `build_run_input` and writes `'snake_xyz'`; `GameTab.log_external` exists; the single presenter-only `setEnabled(True)` is unchanged.
- `git diff --stat` shows ONLY serpentrum/gui_game.py modified.
</verification>

<success_criteria>
- At completion, `_serpentrum.last_run['snake_xyz']` holds a parseable head-inclusive xyz text (proven by build_run_input's tests + the 06-10 integration chain + the 06-12 checkpoint's live atom-count consistency step).
- The launch API's refuse path (head mirror unavailable) has a defined data state (snake_xyz None), not an exception path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-06-SUMMARY.md`
</output>
