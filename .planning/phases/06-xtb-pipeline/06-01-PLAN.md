---
phase: 06-xtb-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/budget_guard.py
  - tests/test_budget_guard.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "launch_budget_warnings NEVER blocks and NEVER raises: clean run -> [], atom overage -> exactly one line carrying setup_logic.HESSIAN_WARNING verbatim WITH the revealed head-inclusive count and budget"
    - "Viewer-vs-run-input count mismatches produce desync WARNING lines (warn-and-proceed), never refusals — a default win snake legitimately exceeds atom_budget, so blocking would make the shipped win path un-runnable"
    - "None/non-numeric atom counts produce an 'unavailable' line instead of a crash (validate() errors-are-data precedent, bool-is-int trap respected)"
    - "The molecule convention is pinned: molecules_stacked EXCLUDES the head, viewer chain-object count INCLUDES it — the guard does the +1 internally"
  artifacts:
    - path: serpentrum/budget_guard.py
      provides: "PURE SPECTRA-06 launch re-check: launch_budget_warnings + launch_counts_line (stdlib-only, imports setup_logic for HESSIAN_WARNING only)"
      min_lines: 60
    - path: tests/test_budget_guard.py
      provides: "behavior matrix: clean/over-budget/desync-molecule/desync-atoms/unavailable-inputs/counts-line + HESSIAN_WARNING drift-pin"
      min_lines: 90
  key_links:
    - from: "serpentrum/budget_guard.py"
      to: "serpentrum/setup_logic.HESSIAN_WARNING"
      via: "literal reused verbatim in the over-budget line (SETUP-06/SPECTRA-06 wording parity; one source)"
      pattern: "HESSIAN_WARNING"
    - from: "tests/test_budget_guard.py"
      to: "setup_logic.HESSIAN_WARNING"
      via: "drift-pin: the over-budget warning line must CONTAIN the setup_logic literal, so plan 06-11's literal amendment flows through automatically"
      pattern: "assertIn"
---

<objective>
Create the PURE module `serpentrum/budget_guard.py`: the SPECTRA-06 pre-launch atom-budget re-check as pure, WSL-testable functions — `(inputs) -> [warning strings]` — plus the launch counts line builder.

Purpose: SC4 ("before launch, hidden molecule/atom counts re-checked against the configured cap; exceeding shows a warning") must be a pure decision the GUI merely logs — the house builder pattern (hud_logic.speed_note precedent: pure layer composes sentences, GUI logs once).
Output: one new pure module + its test file. No existing module modified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@serpentrum/setup_logic.py

Key facts (research file:line anchors):
- WARN-NOT-BLOCK is structurally mandatory: a default WIN snake is 11 molecules / 130-260 atoms vs atom_budget=100 (06-RESEARCH-guard.md Q2; game_engine.py:726-731 freezes molecules_stacked AT the cap). SPECTRA-06 wording is "exceeding it shows a warning" (REQUIREMENTS.md:55).
- `setup_logic.HESSIAN_WARNING` = 'hessian cost scales ~N^3; a ~100-atom snake may take 30-90 s' (setup_logic.py:118-119). Reuse verbatim — the launch re-check warns about THE RUN (actual counts), never call validate() (that warns about CONFIG at edit time; setup_logic.py:15-16 assigns the runtime half to Phase 6).
- Head-inclusive counts: `last_run['atoms_total']` EXCLUDES the head (test_phase5_integration.py:267-273); the guard's budget comparison must use the head-inclusive run-input count, passed by the caller.
- Errors are DATA, not exceptions (setup_logic.validate precedent, :203-224); non-numeric inputs must not crash (:175-179 bool trap).
- Drift-pin precedent: tests/test_xtbenv.py:298 (test_module_constants_guard_against_drift).

RESOLVED DECISIONS owned by this plan:
- EQ-guard-1 (module placement): NEW pure module `serpentrum/budget_guard.py` — NOT a hud_logic appendix (hud_logic is Phase-5.2-shared; a 2-builder family justifies a focused module per the house note in the research). budget_guard imports setup_logic (pure->pure import is purity-legal) ONLY for HESSIAN_WARNING.
- EQ-desync-1 (desync severity): viewer-vs-engine count mismatches -> WARNING lines, launch proceeds (the run input is engine truth; a stale scene is cosmetic). The REFUSE case (`last_run` None / no snake_xyz) is a launch-precondition failure owned by plan 06-09, not by this guard.
- EQ-guard-2 (log-only vs confirm dialog): log-lines-only — SETUP-06 warn-and-proceed precedent; nothing in the requirements demands a confirm dialog.
- Molecule leg: molecules_stacked can never legally exceed win_cap (win freezes AT cap, PROJECT.md "win when length EXCEEDS the cap" would make incl-head vs cap always warn on wins) -> the molecule leg is the counts line + desync check only; ONLY the atom budget can trigger a warning. Document this in the module docstring so nobody "fixes" it later.
- The caller (plan 06-09) computes: molecules_view = len(last_run['chain_objects']) (incl. head), molecules_stacked from the record, atoms_engine = len(xyzio.read_xyz_text(snake_xyz) atoms) (head-inclusive, the true xtb input size), atoms_view = sum(bridge.chain_atom_counts(...)). This module stays signature-simple: ints in, strings out.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for budget_guard</name>
  <files>tests/test_budget_guard.py</files>
  <action>
    Create tests/test_budget_guard.py (house header: sys.path self-insert like tests/test_xtbenv.py:1-34; NO __init__.py; unittest; %-formatting). Import `from serpentrum import budget_guard, setup_logic` (module absent -> ImportError = RED).

    Class TestLaunchBudgetWarnings:
    - test_clean_run_empty: (molecules_stacked=9, molecules_view=10, atoms_engine=96, atoms_view=96, atom_budget=100) -> [].
    - test_over_budget_single_hessian_line: atoms_engine=130, atom_budget=100 -> exactly ONE line; assertTrue(it contains setup_logic.HESSIAN_WARNING verbatim); assertTrue it contains '130' and '100' (revealed counts).
    - test_at_budget_not_over: atoms_engine == atom_budget (100/100) -> [] (strictly greater triggers).
    - test_molecule_desync_line: molecules_stacked=9 but molecules_view=7 -> exactly one desync line containing '7' and '10' (the guard renders molecules_stacked+1 = incl-head); launch still "proceeds" (a warning string, not an exception).
    - test_atom_desync_line: atoms_view=104 vs atoms_engine=96 -> one desync line containing both numbers.
    - test_both_desyncs_then_budget: molecule desync + atom desync + over budget -> exactly 3 lines, desync lines FIRST, hessian line LAST (pin the order).
    - test_unavailable_atoms_line: atoms_engine=None -> exactly one line containing 'unavailable'; atoms_engine='many' (non-numeric) -> same; no exception.
    - test_bool_not_crash: atoms_engine=True does not raise (bool-is-int trap; accept as 1 — pin whatever it does, just no crash).
    - test_none_view_counts_skip_desync: molecules_view=None and atoms_view=None with over-budget atoms -> only the hessian line (None view = cross-check unavailable, not a desync).
    - test_no_budget_key: atom_budget=None -> no budget line even when atoms_engine large.

    Class TestLaunchCountsLine:
    - test_format: launch_counts_line(9, 96, 100) contains '10 molecules', '96 atoms', '100' (incl-head rendering).
    - test_none_atoms: launch_counts_line(9, None, 100) -> an 'unavailable' variant line, no crash.

    Class TestDriftPin (test_xtbenv.py:298 pattern):
    - test_warning_reuses_setup_literal: build an over-budget case; assertIn(setup_logic.HESSIAN_WARNING, line) — a literal amendment (plan 06-11) must flow through without editing this test's expectation of the literal itself.

    Run: `python3.6 -m unittest discover -s tests -p "test_budget_guard.py" -v` -> ImportError (RED).
    Commit RED: `test(06-01): failing tests for budget_guard (SPECTRA-06 launch re-check)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_budget_guard.py" -v fails with ImportError; no other test file touched</verify>
  <done>RED confirmed: the full behavioral contract for budget_guard exists as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement serpentrum/budget_guard.py</name>
  <files>serpentrum/budget_guard.py</files>
  <action>
    Create serpentrum/budget_guard.py — PURE module (stdlib + `from . import setup_logic` only; no pymol/pmg_tk/Qt/numpy; %-formatting; python3.6). Module docstring names SPECTRA-06 and states: warn-and-proceed (why: a default win snake exceeds atom_budget — game_engine.py:726-731), counts are HEAD-INCLUSIVE (atoms_engine is the true xtb input size; last_run atoms_total is head-excluded), the molecule leg is counts+desync only (win freezes AT cap — incl-head vs cap would always warn on wins).

    1. `def _is_count(value)`: isinstance(value, (int, float)) and not isinstance(value, bool).
    2. `def launch_budget_warnings(molecules_stacked, molecules_view, atoms_engine, atoms_view, atom_budget)`:
       - molecules_stacked: the engine counter EXCLUDING the head (caller passes last_run['molecules_stacked']).
       - molecules_view: viewer chain-object count INCLUDING the head (len(chain_objects)); None = cross-check unavailable.
       - atoms_engine: head-inclusive run-input atom count; atoms_view: viewer count; atom_budget: from setup (caller may pass the DEFAULTS fallback).
       - atoms_engine not _is_count -> return ['spectra input atom count unavailable - budget re-check skipped'] (single line, nothing else).
       - lines = []; molecule desync: _is_count(molecules_view) and molecules_view != molecules_stacked + 1 -> "molecule count mismatch: viewer chain has %s object(s), completed run counted %d (incl. head) - scene may be stale"; atom desync: _is_count(atoms_view) and atoms_view != atoms_engine -> "viewer chain atom count %s != run input atom count %s - scene may be stale; using the run input count"; budget: _is_count(atom_budget) and atoms_engine > atom_budget -> '%s (this snake: %d atoms > budget %s)' % (setup_logic.HESSIAN_WARNING, int(atoms_engine), atom_budget). Desync lines first, budget last. NEVER raises, NEVER returns a block signal.
    3. `def launch_counts_line(molecules_stacked, atoms_engine, atom_budget)`: 'spectra input: %d molecules (incl. head), %s atoms (budget %s)' with atoms rendered via the _is_count guard ('unavailable' when not a count).
    4. Run the file suite green, then full gates: `python3.6 tests/run_gates.py` (the new module is auto-covered as PURE).
    5. Commit GREEN: `feat(06-01): budget_guard — SPECTRA-06 launch re-check (warn-and-proceed, head-inclusive counts)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_budget_guard.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green</verify>
  <done>budget_guard.py exists (pure), behavior matrix pinned, HESSIAN_WARNING reuse drift-pinned, gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import budget_guard; print(budget_guard.launch_budget_warnings(9, 10, 130, 130, 100))"` prints exactly one line containing 'hessian cost scales' and '130'.
</verification>

<success_criteria>
- The SPECTRA-06 re-check is a pure data function: clean -> [], over-budget -> one HESSIAN_WARNING-carrying line with revealed counts, desyncs -> warning lines, garbage inputs -> 'unavailable' line, never a raise, never a block.
- Downstream consumers (plan 06-09 launch API) can log the output verbatim with zero formatting logic of their own.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-01-SUMMARY.md`
</output>
