---
phase: 06-xtb-pipeline
plan: 08
type: execute
wave: 3
depends_on: ["06-03", "06-05"]
files_modified:
  - smoke/11_xtb_runner_smoke.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The headless smoke proves the CONTROLLER end-to-end against real xtb.exe: success contract (status 'ok' + artifacts copied out + spray dir deleted), cancel (status 'cancelled', contract NOT ok, no fake success), no-double-run guard, and a clean start-failure path (bad exe -> failed, no crash)"
    - "Every terminal branch leaves the controller startable again — the smoke launches a second run after both cancel AND completion (SC2's 'new run can start after cancel or completion')"
    - "The smoke is INFORMATIONAL in run_gates (EQ-smoke-1) and is the runner's live verification before the GUI wiring lands"
  artifacts:
    - path: smoke/11_xtb_runner_smoke.py
      provides: "Controller-contract smoke: runner_constructs on the anchor, arun_success_contract, responsiveness_tick, cancel_path, no_double_run_guard, start_failure_path — sentinel SMOKE-OK XTB-RUNNER"
      contains: "SMOKE-OK XTB-RUNNER"
  key_links:
    - from: "smoke/11_xtb_runner_smoke.py"
      to: "pmg_tk.startup.serpentrum XtbRunController"
      via: "module load under the loader name + controller on the anchor (never module globals); smoke 01 identity mechanics"
      pattern: "pmg_tk.startup.serpentrum"
    - from: "smoke/11_xtb_runner_smoke.py arun_success_contract"
      to: "_serpentrum.spectra_run + the stable srp_spectra dir"
      via: "record status 'ok' with g98/vibspectrum/xtbopt/snake.xyz/xtb.log paths present; spray run dir ABSENT (deleted at terminal)"
      pattern: "spectra_run"
---

<objective>
Commit smoke/11_xtb_runner_smoke.py — the live verification of XtbRunController against a real xtb.exe: success contract, cancel, no-double-run, responsiveness, and the start-failure path.

Purpose: the roadmap gates the runner on headless proof; research Q7 designed this smoke's steps against the phase success criteria. Everything it asserts was live-probed (06-03) — this plan repoints the probes at the real controller.
Output: one smoke file. No serpentrum/ changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@smoke/01_skeleton_smoke.py

Key facts:
- Controller API (plan 06-05): XtbRunController(anchor) with signals started / log_line(str) / run_finished(str, list); start(xyz_text, exe_path, base_dir, snake_id) -> bool (False when guarded); cancel(); status(). Record lands on the anchor as spectra_run (xtb_run.SPECTRA_RUN_KEYS); stable artifacts under <tempdir>/srp_spectra/<snake_id>/; spray dir deleted at every terminal branch.
- Inputs: committed fixtures co2.xyz (~0.25 s success) and dimer2.xyz (~2.8 s; kill at ~300 ms) — read the files and pass their TEXT to start() (the controller writes snake.xyz itself).
- Binary resolution: the smoke 09 candidate list (SRP_XTB_PATH -> detect_binary -> C:\xtb-6.7.1\bin\xtb.exe) — reuse the same helper code shape.
- Template obligations (smoke/01): named steps + check() + flush=True + _resolve_root + NO widget construction + sentinel `SMOKE-OK XTB-RUNNER`; QEventLoop + safety-timeout per step; QElapsedTimer PROBE lines; connect signals new-style; bytes(...).decode('utf-8','replace') for any QByteArray.
- Waiting pattern: connect run_finished -> loop.quit(); QTimer.singleShot(120000, loop.quit) safety; process events via the loop only (no processEvents pumping).
- Steps map to success criteria: arun_success_contract -> SC1/SC3; cancel_path -> SC2; no_double_run_guard -> SC2; responsiveness_tick -> SC1 (headless async-ness; in-GUI responsiveness is the 06-12 human checkpoint).
- EQ-smoke-1: informational first (xtb-environment dependent); promotion decided at 06-12.
- The no-fabrication rule: real exe, real fixtures; a FAILED run asserted as failed is legitimate testing, not fabrication.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run the runner contract smoke</name>
  <files>smoke/11_xtb_runner_smoke.py</files>
  <action>
    Create smoke/11_xtb_runner_smoke.py with steps:
    1. `s_qprocess_available`: QtCore import + app-ensure (same helper as smoke 09).
    2. `s_runner_constructs`: append ROOT to sys.path; import pmg_tk.startup.serpentrum under the loader name (smoke 01 mechanics); anchor = pmg_tk.startup._serpentrum; build `ctrl = serpentrum_module.XtbRunController(anchor)`; assert ctrl is not None and ctrl.status() in (None, 'idle', 'ok', 'failed', 'cancelled'); store module-global for later steps.
    3. `s_resolve_xtb`: candidate list (smoke 09 shape) -> exe global; SMOKE-FAIL with all probes named when none.
    4. `s_arun_success_contract`: read co2.xyz text; connect ctrl.run_finished -> record + loop.quit (fresh loop per step); base_dir = tempfile.gettempdir(); snake_id = 'smoke11_ok'; assert ctrl.start(text, exe, base_dir, snake_id) is True; wait; assert started fired (flag), at least one log_line seen, run_finished status == 'ok' with problems == []; anchor.spectra_run['status'] == 'ok'; the stable dir <tempdir>/srp_spectra/smoke11_ok contains snake.xyz, xtb.log, g98.out, vibspectrum, xtbopt.xyz; the spray run dir from the record's... (record keeps stable paths only — assert no 'srp_' dir remains in base_dir newer than the run: scan base_dir for 'srp_' prefixes created during this step and assert none exist — the spray dir was deleted). ALSO assert a SECOND run can start after completion: ctrl.start(...) with a fresh snake_id returns True, then cancel it immediately (cleanup path to keep the smoke fast) and wait for 'cancelled'.
    5. `s_responsiveness_tick`: during a dimer2 run (started but not finished), QTimer.singleShot(300, flag) fires while ctrl.status() == 'running' — record both facts; then cancel to clean up.
    6. `s_cancel_path`: start dimer2 text (snake_id 'smoke11_cancel'); QTimer.singleShot(300, ctrl.cancel); wait; assert run_finished status == 'cancelled'; anchor.spectra_run['status'] == 'cancelled'; the stable dir for that snake has snake.xyz + xtb.log but NO g98.out / vibspectrum (no fake success — the files leg fails on kill, probe-B fact); assert a new run can start (start returns True for a follow-up tiny co2 run; cancel it after).
    7. `s_no_double_run_guard`: start dimer2 (running); assert ctrl.start(co2_text, exe, base_dir, 'smoke11_double') is False (guarded); a log_line about the active run was emitted; then ctrl.cancel() and wait for the terminal branch (leave state clean).
    8. `s_start_failure_path`: ctrl.start(co2_text, exe + '_missing', base_dir, 'smoke11_badexe'); assert it does NOT raise and terminates in 'failed' quickly (run_finished status 'failed' with a problem mentioning the start failure) — the pitfall-9 'surface, never die' proof; state is terminal afterwards.
    Per-step tmp hygiene: every snake_id is unique per step; stable dirs are left in place (they ARE the artifact policy under test); each step's wait has a safety timeout (success 120 s, others 60 s).
    Verdict: `SMOKE-OK XTB-RUNNER` iff FAILURES empty.
    Run: `timeout 300 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\11_xtb_runner_smoke.py"` (longer timeout: several real runs).
    Commit: `test(06-08): runner contract smoke — success/cancel/no-double-run/failure paths (SPECTRA-02 live proof)`
  </action>
  <verify>the cmd.exe run output contains SMOKE-OK XTB-RUNNER plus SMOKE-STEP OK for all eight steps; python3.6 tests/run_gates.py --smoke lists smoke 11 as informational PASS</verify>
  <done>The controller's full contract is proven live: contract-only success, cancel with no fake success, no-double-run, responsiveness, clean failure path — all with real xtb.exe runs.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (syntax walk of smoke/).
- `python3.6 tests/run_gates.py --smoke`: 'informational smoke smoke/11_xtb_runner_smoke.py: PASS'.
- The SMOKE-OK XTB-RUNNER sentinel line appears in the captured gate notes.
</verification>

<success_criteria>
- SC1/SC2/SC3 mechanics are headless-proven at the controller level before any GUI wiring exists; the only remaining unverified surface is in-GUI responsiveness/UX (06-12 checkpoint).
- Smoke 11 + smoke 09 together give the runner track a committed, re-runnable regression pair.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-08-SUMMARY.md`
</output>
