---
phase: 06-xtb-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - smoke/09_qprocess_smoke.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A committed headless smoke proves, against the REAL Windows conda PyMOL + REAL xtb.exe: QProcess availability, QCoreApplication ensure, signal-driven start/finish, stderr contract literal, kill->CrashExit cancel, and timer responsiveness DURING a run"
    - "The smoke FAILS with a clear message when no xtb binary resolves (env override -> shutil.which -> verified fallback path), never dying in an opaque QProcess error"
    - "The smoke is INFORMATIONAL in run_gates (not in REQUIRED_SMOKES) per EQ-smoke-1 — but it GATES the runner plans via depends_on: 06-05 and 06-08 must not start until it flushed SMOKE-OK QPROCESS"
  artifacts:
    - path: smoke/09_qprocess_smoke.py
      provides: "The roadmap's QProcess-in-conda gate smoke: probe-level mechanics only (no serpentrum runner module), template obligations honored (named steps, check(), flush=True, _resolve_root, no widgets, sentinel verdict)"
      contains: "SMOKE-OK QPROCESS"
  key_links:
    - from: "smoke/09_qprocess_smoke.py resolve step"
      to: "the real xtb.exe"
      via: "candidate list: SRP_XTB_PATH env -> shutil.which('xtb.exe') -> shutil.which('xtb') -> C:\\xtb-6.7.1\\bin\\xtb.exe (EQ-binary-1)"
      pattern: "xtb-6.7.1"
    - from: "smoke/09_qprocess_smoke.py"
      to: "tests/run_gates.py --smoke"
      via: "informational smoke: found by the [0-9][0-9]_*.py glob, run non-blocking, never fails the gate (research Q7 promotion policy)"
      pattern: "SMOKE-OK"
---

<objective>
Commit the QProcess-in-conda gate smoke (`smoke/09_qprocess_smoke.py`) that discharges the roadmap's Phase-6 gate: "QProcess in the Windows conda PyMOL build" — proven against the real environment, not stubs.

Purpose: the roadmap says this smoke GATES the runner plan. Every mechanism was live-probed today (06-RESEARCH-runner Q1/Q3/Q4/Q7: Qt 5.12.9, QProcess signals, kill->CrashExit, env injection, timer-during-run); this plan converts the probe into a committed regression smoke.
Output: one smoke file. No serpentrum/ or tests/ changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@smoke/01_skeleton_smoke.py

Key facts (all live-probe VERIFIED 2026-09-24, values quoted in the research):
- Runtime: conda PyMOL 2.5.0, PyQt5 Qt 5.12.9, Python 3.9.13; `-cq` headless has NO Q(Core)Application (`COREAPP_BEFORE: None`) — the smoke must create a QCoreApplication if instance() is None (EQ-app-1).
- Signals: `started`, `finished(int, QProcess.ExitStatus)` (slot takes two ints), `readyReadStandardOutput/Error` all connect new-style `.connect()`. `error` connects; `errorOccurred` availability unverified — use `getattr(proc, 'errorOccurred', proc.error)` defensively (EQ-runner-1) and PRINT which one exists (a PROBE line settles research open question 5).
- stderr contract: a successful `--version` run emits stderr containing `normal termination of xtb\r\n` (CRLF) — the xtbenv.STDERR_SUCCESS literal.
- Cancel: `proc.kill()` at ~300 ms into a real `--ohess` run -> `finished(exitcode=62097, exitstatus=1)`; `QProcess.CrashExit == 1`; state returns to NotRunning immediately. NEVER terminate() (unverified, unneeded).
- Responsiveness: `QTimer.singleShot(300, ...)` fires DURING a live run (probe-verified) — the headless async-ness proof (in-GUI responsiveness stays human-verify per the 01-05 dead end).
- Binary resolution (EQ-binary-1): xtb.exe is NOT on the Windows PATH (`where xtb.exe` -> not found); the repo-root `xtb-6.7.1` is a WSL symlink Windows cannot traverse (OSError 22); the verified working binary is `C:\xtb-6.7.1\bin\xtb.exe`. Smoke candidate list: (1) `SRP_XTB_PATH` env var if set, (2) `xtbenv.detect_binary()`-equivalent which() probes — the smoke may import serpentrum.xtbenv (PURE, safe headless) and call detect_binary(None), (3) `C:\xtb-6.7.1\bin\xtb.exe` existence check. No resolution -> SMOKE-FAIL with a message naming all four probes (never an opaque FailedToStart).
- Fixtures: committed `.planning/research/xtb-spike-fixtures/co2.xyz` (3 atoms, ~0.25 s run) and `dimer2.xyz` (26 atoms, ~2.8 s at default threads) — real inputs, no fabricated data. Resolve via _resolve_root() + the fixtures relative path.
- Smoke template obligations (smoke/01_skeleton_smoke.py:1-21): named step functions + check() runner, flush=True on EVERY print, _resolve_root() candidate validation (never trust __file__), NO widget construction, no smoke/__init__.py, verdict = flushed `SMOKE-OK QPROCESS` sentinel ONLY (rc through the .bat is always 0).
- Event-loop waiting: QEventLoop + signal->loop.quit() + QTimer.singleShot safety timeout per step (probe mechanism); QElapsedTimer for per-step elapsed PROBE lines.
- EQ-smoke-1: this smoke ships INFORMATIONAL (research Q7: "add as informational first, promote to required only after it proves stable"); it still gates plans 06-05/06-08 through depends_on — their executors must run it and see SMOKE-OK QPROCESS first.

RESOLVED DECISIONS owned by this plan:
- EQ-app-1 (headless app): ensure exactly one QCoreApplication — instance() or create; NEVER a second app. The runner (06-05) reuses the same helper pattern.
- EQ-runner-1 (error signal): connect defensively via getattr(proc, 'errorOccurred', proc.error); print a PROBE line recording which attribute exists.
- EQ-binary-1 (fallback list): as above — smoke-only convenience; the SHIPPED runner uses xtbenv.detect_binary(configured) + a clean 'xtb not found' message (plan 06-09), because SETUP-05's configured path is the user seam.
- Smoke step order: availability -> resolve -> version-run -> responsiveness-during-run -> kill/cancel -> error-path probe. Each step's QEventLoop has its own safety timeout (version 30 s, co2 run 60 s, kill step 30 s).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write smoke/09_qprocess_smoke.py on the probe mechanics</name>
  <files>smoke/09_qprocess_smoke.py</files>
  <action>
    Create smoke/09_qprocess_smoke.py copying the 01_skeleton_smoke template obligations (header docstring citing the probe evidence + run command `timeout 120 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\09_qprocess_smoke.py"`; _resolve_root; check() runner; every print flush=True; final epilogue prints `SMOKE-OK QPROCESS` iff FAILURES is empty else `SMOKE-FAIL <steps>`).

    Steps (named functions, connected in order):
    1. `s_qprocess_available`: `from pymol.Qt import QtCore`; assert hasattr(QtCore, 'QProcess') and hasattr(QtCore, 'QEventLoop'); app = QtCore.QCoreApplication.instance(); if app is None: app = QtCore.QCoreApplication([]) (EQ-app-1); assert app is QtCore.QCoreApplication.instance().
    2. `s_resolve_xtb`: import serpentrum.xtbenv; candidates: os.environ.get('SRP_XTB_PATH'); xtbenv.detect_binary(None) (which-probes); os.path.isfile('C:\\xtb-6.7.1\\bin\\xtb.exe') -> use it. Store the resolved exe in a module global for later steps; if none resolved: raise AssertionError naming every probe tried ('xtb.exe not on PATH; repo symlink not Windows-traversable; set SRP_XTB_PATH or install at C:\\xtb-6.7.1').
    3. `s_version_run`: QProcess; connect started/finished/readyReadStandardError defensively incl. the getattr error-signal (EQ-runner-1) + a PROBE print of which error attribute exists; proc.start(exe, ['--version']); QEventLoop + finished->quit + 30 s safety singleShot; assert finished delivered (0, QtCore.QProcess.NormalExit); stderr bytes -> decode('utf-8','replace') contains 'normal termination of xtb'; print PROBE elapsed via QElapsedTimer.
    4. `s_responsiveness_during_run`: read the co2.xyz fixture text; write it into a fresh mkdtemp under tempfile.gettempdir() as snake.xyz; proc.start(exe, ['snake.xyz', '--ohess']) with setWorkingDirectory(tmpdir); BEFORE start, QTimer.singleShot(300, tick-flag-setter); wait loop; assert the tick flag fired while the process was Running (state check at tick time recorded); assert finished (0, NormalExit); cleanup: delete tmpdir AFTER finished (pitfall 10 — never delete while running).
    5. `s_kill_cancel`: same pattern with dimer2.xyz; QTimer.singleShot(300, proc.kill); wait; assert finished exitStatus == QtCore.QProcess.CrashExit (enum compare, not magic 1); assert proc.state() == QtCore.QProcess.NotRunning at finished; assert the run dir does NOT contain g98.out or vibspectrum (partial files cannot masquerade — the research probe-B fact); cleanup tmpdir.
    6. `s_error_path_probe`: proc.start on a NONEXISTENT exe path (e.g. exe + '_missing'); wait briefly via loop on the error/finished signal + 10 s timeout; assert the error signal fired (or FailedToStart state observed) and NOTHING crashed; print PROBE which error attribute fired. (This is the pitfall-9 'surface not-found instead of dying' evidence.)
    NEVER use waitForFinished; NO subprocess; NO widget construction; the xtb runs are REAL (no-fabrication rule).
  </action>
  <verify>timeout 120 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\09_qprocess_smoke.py" output contains SMOKE-OK QPROCESS and SMOKE-STEP OK lines for all six steps; python3.6 tests/run_gates.py still green (gate 1 syntax-walks smoke/)</verify>
  <done>The QProcess gate smoke is committed and green against the real environment; the roadmap's Phase-6 gate is discharged with evidence lines (PROBE prints) in the output.</done>
</task>

<task type="auto">
  <name>Task 2: Record gate status + handoff note</name>
  <files>.planning/phases/06-xtb-pipeline/06-03-SUMMARY.md</files>
  <action>
    In the SUMMARY: record the smoke output's key PROBE lines (which error attribute exists, elapsed times, kill exitstatus), confirm the smoke is INFORMATIONAL (run `python3.6 tests/run_gates.py --smoke` and quote the `informational smoke smoke/09_qprocess_smoke.py: PASS` note line), and state that plans 06-05/06-08 may now proceed (their depends_on is satisfied by this SMOKE-OK).
    Commit: `docs(06-03): qprocess gate smoke green — runner plans unblocked`
  </action>
  <verify>python3.6 tests/run_gates.py --smoke output contains 'informational smoke smoke/09_qprocess_smoke.py: PASS'; SUMMARY exists</verify>
  <done>Gate status recorded; downstream runner plans formally unblocked.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (smoke/ is syntax-walked).
- `python3.6 tests/run_gates.py --smoke` shows smoke 09 as informational PASS (sentinel SMOKE-OK QPROCESS echoed in the notes).
- `git status` shows NO serpentrum/ or tests/ modifications (this plan is smoke-only).
</verification>

<success_criteria>
- The roadmap's Phase-6 gate ("QProcess-in-conda smoke") is discharged by a committed, re-runnable smoke whose every mechanism is research-proven, with the sentinel verdict SMOKE-OK QPROCESS.
- The runner plans' dependency is satisfied and the probe evidence (error-signal attribute, timings) is recorded for 06-05/06-08 to reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-03-SUMMARY.md`
</output>
