---
phase: 06-xtb-pipeline
plan: 07
type: execute
wave: 2
depends_on: ["06-03"]
files_modified:
  - tools/build_calibration_snake.py
  - tools/measure_calib_qprocess.py
  - tests/fixtures/calib_snake_52.xyz
  - tests/fixtures/calib_snake_104.xyz
  - .planning/phases/06-xtb-pipeline/06-CALIBRATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A ~100-atom capped snake fixture (104 atoms) completes a real `xtb --ohess` run headless with a MEASURED wall time — SC5's extrapolation is replaced by a measurement"
    - "The wall-time table covers size x thread settings (-P default/1/2/4, 2 repeats each) from WSL direct-exec, plus one true user-perceived QProcess-timed run from headless PyMOL"
    - "The calibration outcome is committed as 06-CALIBRATION.md with VERIFIED (measured) vs ASSUMED (extrapolated) labels, an OMP_STACKSIZE verdict, and explicit decision inputs for plan 06-11 (HESSIAN_WARNING literal + DEFAULT_RUN_KNOBS)"
  artifacts:
    - path: tools/build_calibration_snake.py
      provides: "Deterministic fixture builder: tiles the committed dimer2.xyz motif (13-atom phenol layers at the xtb-validated ~3.4 A stack offset) into N-layer stacks via xyzio round-trip — never hand-edited geometry"
      contains: "dimer2"
    - path: tests/fixtures/calib_snake_104.xyz
      provides: "The ~100-atom capped-snake input fixture (104 atoms = 8 phenol layers), round-trip parseable"
      contains: "104"
    - path: .planning/phases/06-xtb-pipeline/06-CALIBRATION.md
      provides: "The measurement table + calibration decisions (SC5) — the authority plan 06-11 consumes"
      contains: "wall"
  key_links:
    - from: "tools/build_calibration_snake.py"
      to: ".planning/research/xtb-spike-fixtures/dimer2.xyz"
      via: "layer geometry derived from the committed, xtb-accepted dimer fixture (centroid delta of the two 13-atom halves) — no invented geometry"
      pattern: "xtb-spike-fixtures"
    - from: "06-CALIBRATION.md"
      to: "serpentrum/setup_logic.HESSIAN_WARNING + serpentrum/xtb_run.DEFAULT_RUN_KNOBS (plan 06-11)"
      via: "measured ~104-atom wall time + thread-sweep result drive the warning literal amendment and the default thread cap decision"
      pattern: "DEFAULT_RUN_KNOBS"
---

<objective>
Run the SC5 calibration experiment: build a capped ~100-atom snake fixture programmatically from committed data, measure real `--ohess` wall times across thread settings (WSL direct-exec sweep + one headless QProcess-timed run), and commit the results as 06-CALIBRATION.md.

Purpose: SC5 demands "a capped ~100-atom snake completes --ohess headless with measured wall time; warning threshold and OMP environment calibrated from the measurement". The method is fully research-verified (06-RESEARCH-runner Q6: gate-5 WSL exec pattern + probe QElapsedTimer pattern); the ~100-atom wall time is the one remaining unknown.
Output: builder script + two committed fixtures + measurement script + CALIBRATION.md. No serpentrum/ changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md

Key facts (Q6 — method verified, absolute time unknown):
- Measured points so far: phenol 13 atoms 0.67 s; 26-atom dimer 1.75 s (default threads; 2.8 s with OMP_NUM_THREADS=2 via QProcess incl. startup). ~100-atom "30-90 s" is an EXTRAPOLATION (PITFALLS.md:71) — this experiment measures it.
- WSL direct-exec pattern (tests/run_gates.py gate 5 + test_wsl_winxtb.sh): exec the exe by WSL path (repo-root symlink xtb-6.7.1/bin/xtb.exe), cwd = a /mnt/c-backed dir, BARE relative args; parse the LAST `* wall-time:` block (`total:`) + the header `omp threads : N` echo. `-P N` is argv-level and verified functional from WSL (log echoes `omp threads : N`).
- QProcess timing pattern (probe): QElapsedTimer around a headless run gives the true user-perceived wall incl. PyMOL-side startup overhead — smoke 09's app-ensure + QEventLoop mechanics.
- Thread knobs (help:197-198, 229-231): `-P, --parallel INT`; env MKL_NUM_THREADS=<NCORE>, OMP_NUM_THREADS=<NCORE>,1, OMP_STACKSIZE=4G. OMP_STACKSIZE necessity for ~100-atom hessian is [TRAIN] LOW — adopt ONLY if an uncapped run crashes.
- Fixture rule: NEVER hand-edited geometry. The committed dimer2.xyz (26 atoms = two 13-atom phenol layers, comment 'stacked phenol dimer 3.4A z-offset') is xtb-accepted; tiling its inter-layer centroid delta builds deterministic N-layer stacks (13*N atoms: N=4 -> 52, N=8 -> 104).
- Jank rationale (PITFALLS.md:333,348): uncapped xtb grabs all cores and can stall PyMOL rendering DURING play-adjacent GUI use — the sweep decides whether the runner ships a default cap.
- UI-jank measurement note: the WSL sweep measures xtb wall time; the QProcess-timed run measures the user-perceived in-plugin wall. Keep both in the table.
- Timeout discipline: use generous per-run timeouts (>= 600 s) for the 104-atom runs; log every command + wall line into 06-CALIBRATION.md (raw logs stay in git-ignored tmp/xtb_runs/).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic fixture builder + committed fixtures</name>
  <files>tools/build_calibration_snake.py, tests/fixtures/calib_snake_52.xyz, tests/fixtures/calib_snake_104.xyz</files>
  <action>
    Create tools/build_calibration_snake.py (python3.6, stdlib only, sys.path self-insert; syntax-walked by gate 1):
    1. Read .planning/research/xtb-spike-fixtures/dimer2.xyz via serpentrum.xyzio.read_xyz -> (comment, 26 atoms). Assert len(atoms) == 26.
    2. layer_a = atoms[0:13]; layer_b = atoms[13:26]; delta = centroid(layer_b) - centroid(layer_a) (per-axis mean). Assert |delta| is between 3.0 and 4.0 Angstrom (the xtb-validated stack offset sanity bound) — abort loudly otherwise.
    3. For N in (4, 8): layer_i = [(sym, x + i*dx, y + i*dy, z + i*dz) for (sym, x, y, z) in layer_a] for i in 0..N-1; text = xyzio.write_xyz(elements, coords, comment='serpentrum calib snake N=%d layers from dimer2 motif' % N); round-trip assert via xyzio.read_xyz_text (count == 13*N); write tests/fixtures/calib_snake_%d.xyz % (13*N).
    4. Print the atom counts + delta vector. Deterministic: no RNG, no clock.
    Run it; commit BOTH fixture files (small text files) + the script.
    Commit: `feat(06-07): calibration snake fixtures (52/104 atoms) built from the dimer2 motif`
  </action>
  <verify>python3.6 tools/build_calibration_snake.py prints '52' and '104' and exits 0; python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import xyzio; c,a=xyzio.read_xyz('tests/fixtures/calib_snake_104.xyz'); print(len(a))" prints 104; both fixtures committed</verify>
  <done>Deterministic, round-trip-verified ~100-atom (104) and mid-size (52) fixtures exist, geometry derived only from the committed xtb-accepted dimer.</done>
</task>

<task type="auto">
  <name>Task 2: Wall-time sweep (WSL) + QProcess-timed run + 06-CALIBRATION.md</name>
  <files>tools/measure_calib_qprocess.py, .planning/phases/06-xtb-pipeline/06-CALIBRATION.md</files>
  <action>
    1. WSL direct-exec sweep (bash, gate-5 pattern): for each fixture (52, 104) x thread setting ('' default, '-P 1', '-P 2', '-P 4') x 2 repeats: mkdir -p tmp/xtb_runs/calib/<label>; cp the fixture in as snake.xyz; run `timeout 600 <ROOT>/xtb-6.7.1/bin/xtb.exe snake.xyz --ohess [-P N]` with cwd=that dir; capture output; extract the LAST 'total:' wall-time line and the 'omp threads : N' echo. If a run FAILS the contract (no 'normal termination'), record the failure verbatim — do not silently retry more than the 2 repeats.
    2. tools/measure_calib_qprocess.py (python3.6 syntax; RUN inside headless PyMOL via cmd.exe — like smoke 09: app-ensure, QEventLoop, QElapsedTimer): run the 104-atom fixture once at the BEST thread setting from the sweep (and once uncapped), printing PROBE WALL-MS lines + the stderr head. Run via `timeout 900 cmd.exe /c "C:\src\run-conda-pymol.bat -cq tools\measure_calib_qprocess.py"` from the repo root (resolve the exe with the smoke 09 candidate list).
    3. Write .planning/phases/06-xtb-pipeline/06-CALIBRATION.md:
       - Table: atoms x threads x wall (both repeats) + the QProcess-timed user-perceived walls.
       - VERIFIED vs ASSUMED section (measured numbers = VERIFIED; any scaling commentary = ASSUMED).
       - OMP_STACKSIZE verdict: needed only if an uncapped run crashed (record which).
       - Decision inputs for plan 06-11: (a) the measured ~104-atom wall range vs the current '30-90 s' HESSIAN_WARNING wording; (b) recommended DEFAULT_RUN_KNOBS / -P default (or explicitly 'uncapped is fine') with the jank rationale; (c) whether atom_budget=100 default stays (research Q7 prefers constants over schema churn).
    Commit: `docs(06-07): ~104-atom --ohess calibration measured (SC5) + decision inputs`
  </action>
  <verify>06-CALIBRATION.md exists with a populated table (>= 12 measured runs) and the three decision inputs; the 104-atom run(s) show 'normal termination' (SC5 completion); tools/measure_calib_qprocess.py committed</verify>
  <done>SC5's measurement exists: a ~104-atom snake completed --ohess headless with recorded wall times across thread settings; 06-11 has its decision inputs.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (new tools/ files are syntax-walked only).
- 06-CALIBRATION.md contains at least one measured wall time for the 104-atom fixture and a 'normal termination' quote.
- No serpentrum/ file modified (`git status`).
</verification>

<success_criteria>
- The ~100-atom extrapolation is replaced by committed measurements; the thread-sweep and OMP_STACKSIZE questions have recorded answers.
- Plan 06-11 can apply the calibration WITHOUT re-running any experiment (all numbers + decision inputs are in the committed doc).
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-07-SUMMARY.md`
</output>
