---
phase: 06-xtb-pipeline
plan: 10
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/test_phase6_integration.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The pure Phase-6 chain is pinned end-to-end: engine-shaped atoms -> build_run_input -> xyzio round-trip -> budget_guard verdict -> state machine transitions -> xtbenv.evaluate_run -> spectra_run record — one test file proving the modules compose with NO glue logic in between"
    - "The killed-run scenario proves no-fake-success: cancel-requested resolution + a partial-files evaluate_run both fail the contract legs exactly as the live probe measured"
    - "The over-budget win-snake scenario proves the guard warns (never blocks) with revealed head-inclusive counts"
  artifacts:
    - path: tests/test_phase6_integration.py
      provides: "Integration scenarios: happy path, cancel chain, desync chain, no-double-run matrix, record-shape pin — house integration-suite pattern (02-14 / 5.2-08)"
      min_lines: 120
  key_links:
    - from: "tests/test_phase6_integration.py"
      to: "xtb_run.build_run_input -> xyzio.read_xyz_text -> budget_guard.launch_budget_warnings -> xtb_run.can_start/resolve_status -> xtbenv.evaluate_run -> xtb_run.new_spectra_run"
      via: "the exact call order the GUI (06-09) and controller (06-05) implement — a regression pin on the seams, not on implementations"
      pattern: "build_run_input"
    - from: "tests/test_phase6_integration.py killed-run scenario"
      to: "the live probe-B facts (06-RESEARCH-runner Q4)"
      via: "killed run = nonzero exit + only optimizer-stage files present -> evaluate_run NOT ok + resolve_status 'cancelled'"
      pattern: "resolve_status"
---

<objective>
Pin the Phase-6 pure integration chain in one test file: the exact module composition the GUI launch pipeline and the runner controller implement, using real fixture bytes — before the GUI wiring lands, so any seam drift fails here first.

Purpose: the house integration-suite pattern (02-14 pure-core chain, 5.2-08 scenario chains): composition bugs surface as failing scenarios, not as live GUI surprises.
Output: one test file. No serpentrum/ changes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@serpentrum/xtbenv.py

Key facts:
- Modules under composition (all landed by wave 1): xtb_run (build_run_input, can_start, resolve_status, new_spectra_run, build_env, SPECTRA_RUN_KEYS), budget_guard (launch_budget_warnings, launch_counts_line), xyzio (read/write round-trip), xtbenv (evaluate_run, EXPECTED_FILES, build_argv quote rejection).
- Fixture bytes available in-place: .planning/research/xtb-spike-fixtures/ (ohess.err success stderr, bad.err abnormal, co2.xyz 3 atoms, dimer2.xyz 26 atoms). The killed-run scenario reconstructs the probe-B state synthetically: exit code 62097 (nonzero), stderr 'normal termination of xtb' ABSENT-or-present is irrelevant because cancel wins — assert BOTH: resolve_status(True, anything) == 'cancelled' AND evaluate_run(62097, success_text, EXPECTED_FILES, ('.xtboptok', 'snake.xyz', 'xtbopt.log')).ok is False (files leg fails — no fake success even with a success-looking stderr).
- Head-inclusive counting: build the head from co2.xyz atoms (3) + one synthetic segment (2 atoms) -> run input has 5 atoms; budget_guard receives atoms_engine=5; with atom_budget=4 the warning fires WITH '5' in the text.
- Test conventions: sys.path self-insert header; NO __init__.py; unittest; %-formatting; zero sys.modules stubs; fixtures read IN PLACE.
- This is an execute-type pin suite (house pattern: integration chains are written directly — the modules they compose are already TDD-green), NOT a RED/GREEN plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration scenario suite</name>
  <files>tests/test_phase6_integration.py</files>
  <action>
    Create tests/test_phase6_integration.py (house header; import serpentrum.xtb_run, budget_guard, xyzio, xtbenv, setup_logic).

    Class TestHappyPathChain:
    - test_completion_to_record: head = co2.xyz atoms via xyzio.read_xyz (3 atoms); segments = [two-atom synthetic list]; text = xtb_run.build_run_input(head_atoms, segments, 'run_7'); (comment, atoms) = xyzio.read_xyz_text(text); assert len(atoms) == 5 and comment.startswith('serpentrum snake run_7'); warnings = budget_guard.launch_budget_warnings(1, 2, len(atoms), len(atoms), 100) == [] (molecules_view=2 = stacked 1 + head); assert budget_guard.launch_counts_line(1, 5, 100) mentions '2 molecules' and '5 atoms'; assert xtb_run.can_start('idle'); argv = xtbenv.build_argv('C:/xtb/xtb.exe', 'snake.xyz'); assert argv == ['C:/xtb/xtb.exe', 'snake.xyz', '--ohess']; verdict = xtbenv.evaluate_run(0, ohess.err text, xtbenv.EXPECTED_FILES, ('g98.out', 'vibspectrum')); status = xtb_run.resolve_status(False, verdict.ok) == 'ok'; record = xtb_run.new_spectra_run('run_7'); record['status'] = status; record['problems'] = verdict.problems; assert set(record) == set(xtb_run.SPECTRA_RUN_KEYS) and record['status'] == 'ok'.
    - test_over_budget_win_snake_warns_not_blocks: molecules_stacked=10 (win at cap), head 12 + segments 22*9=198+... build atoms_engine = 210; warnings = launch_budget_warnings(10, 11, 210, 210, 100); assert exactly one line; assertIn(setup_logic.HESSIAN_WARNING, line); assertIn('210', line) — and the chain CONTINUES (a returned list, no exception: the warn-and-proceed pin).

    Class TestCancelChain:
    - test_killed_run_never_fake_success: resolve_status(True, True) == 'cancelled' and resolve_status(True, False) == 'cancelled'; verdict = xtbenv.evaluate_run(62097, SUCCESS_ERR_TEXT, xtbenv.EXPECTED_FILES, ('.xtboptok', 'snake.xyz', 'xtbopt.log')); assert verdict.ok is False; assert any('exit code' in p for p in verdict.problems) and any('missing expected output' in p for p in verdict.problems).
    - test_state_machine_cancel_cycle: can_start('idle') -> start -> 'running' -> can_start('running') is False -> resolve_status(True, False) -> 'cancelled' -> can_start('cancelled') is True (relaunch after cancel — SC2).
    - test_state_machine_complete_then_relaunch: 'running' -> resolve_status(False, True) -> 'ok' -> can_start('ok') is True (relaunch after completion — SC2).

    Class TestDesyncChain:
    - test_viewer_mismatch_warns: launch_budget_warnings(9, 7, 96, 80, 100) -> two desync lines (molecule 7 vs 10 incl-head; atoms 80 vs 96), no budget line, no exception.
    - test_garbage_inputs_unavailable_line: launch_budget_warnings(9, 10, None, None, 100) -> exactly the 'unavailable' line.

    Class TestContractGuardRails:
    - test_build_argv_rejects_quotes: xtbenv.build_argv('C:/x/y.exe', "sn'ake.xyz") raises ValueError (the launch path's injection guard).
    - test_abnormal_stderr_never_ok: evaluate_run(0, bad.err text, EXPECTED_FILES, ('g98.out','vibspectrum')).ok is False (failure-before-success substring ordering stays pinned through Phase 6).
    - test_record_shape_frozen: set(xtb_run.SPECTRA_RUN_KEYS) fixed + new_spectra_run initial values (the Phase-7 contract pin lives here too).

    Run: `python3.6 -m unittest discover -s tests -p "test_phase6_integration.py" -v` then full gates.
    Commit: `test(06-10): Phase-6 pure integration chain — completion->input->guard->state->contract->record`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_phase6_integration.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green</verify>
  <done>The Phase-6 composition is pinned: any seam drift between xtb_run / budget_guard / xyzio / xtbenv fails the suite before GUI work relies on it.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green with the new suite included in discovery.
- `python3.6 -m unittest discover -s tests -p "test_phase6_integration.py" -v` reports all scenarios green.
</verification>

<success_criteria>
- The five phase success criteria's PURE halves are each covered by at least one scenario (SC1 input+dir contract, SC2 cancel/relaunch matrix, SC3 contract legs + quote guard, SC4 guard semantics, SC5 threshold behavior via the budget line).
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-10-SUMMARY.md`
</output>
