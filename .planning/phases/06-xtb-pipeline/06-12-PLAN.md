---
phase: 06-xtb-pipeline
plan: 12
type: execute
wave: 4
depends_on: ["06-08", "06-09", "06-10", "06-11"]
files_modified:
  - .planning/phases/06-xtb-pipeline/06-12-SUMMARY.md
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The FULL gate suite is green on the completed phase tree, including --smoke (smoke 10 REQUIRED; smokes 09/11 informational PASS) and --xtb"
    - "A live human checkpoint in real Windows PyMOL approves the Phase-6 pipeline behavior: async launch on the final snake, cancel + relaunch (no double-runs), contract-only success with artifacts, clear failure lines, and the head-inclusive counts/warning display"
    - "Spectra-tab PLOT/TABLE/LOG-PANEL verdicts are explicitly DEFERRED to Phase 7 (EQ-checkpoint-1) — this checkpoint covers what Phase 6 built: the pipeline + temporary placeholder affordances"
    - "Smoke-promotion decision (EQ-smoke-1) is recorded: default keeps 09/11 informational; REQUIRED promotion only on owner instruction"
  artifacts:
    - path: .planning/phases/06-xtb-pipeline/06-12-SUMMARY.md
      provides: "gate results + checkpoint verdict table + EQ decisions record + Phase-7 handoff notes"
      contains: "verdict"
  key_links:
    - from: "06-12 checkpoint steps"
      to: "the placeholder status/log area + game-tab info box"
      via: "every EXPECT reads a log/status line whose wording comes from budget_guard / the launch pipeline / the runner signals"
      pattern: "spectra input:"
---

<objective>
Close Phase 6: full gate pass (incl. --smoke and --xtb legs), then the ONE blocking human-verify checkpoint of the launch/cancel/relaunch pipeline in real Windows PyMOL, and record the phase's EQ decisions + Phase-7 handoff in the SUMMARY.

Purpose: every GUI verdict in this phase lands here (01-05 dead end: no offscreen dialog testing); the checkpoint model is 5.2-09 / 5.1-06 / 04-09 (numbered steps, per-step EXPECT lines, PASS/FAIL verdict table).
Output: green gates + an approved checkpoint verdict table + the phase SUMMARY.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-xtb-pipeline/06-RESEARCH-runner.md
@.planning/phases/06-xtb-pipeline/06-RESEARCH-guard.md
@.planning/phases/5.2-generic-upload-stacking-consent/5.2-09-PLAN.md
@.planning/phases/06-xtb-pipeline/06-CALIBRATION.md

Key facts:
- Checkpoint precedent: 5.2-09 (gates task + ONE blocking checkpoint + conditional fix task); per-item approvals ACCUMULATE across rounds; reference SYMBOLS never line numbers in steps.
- What Phase 6 can demonstrate live (EQ-checkpoint-1): play a short game -> completion -> Get Spectra -> the interposed pipeline launches the REAL xtb async; the placeholder status/log area + game info box carry the counts/warning/progress/verdict lines; Cancel/Run-again work; artifacts land in %TEMP%\srp_spectra\<snake_id>\; failure paths give clear lines. What it CANNOT: Spectra plot/table/log-panel UI (Phase 7), threshold feel (SC5 verified by 06-07/06-11 artifacts instead).
- Gate specifics: `python3.6 tests/run_gates.py` (default), `--smoke` (required: 01,03,04,05,06,07,08,10; informational: 02 known-dead-end, 09, 11), `--xtb` (asserts 'xtb version' + 'normal termination').
- Test-count baseline: quote the current total from the gate output (Phase 5.1/5.2 era ~640-700; record the actual delta).
- Phase success criteria mapping for the verdict table: SC1 -> steps 2-4; SC2 -> steps 5-7; SC3 -> steps 4, 8-9; SC4 -> step 3 (+ head-inclusive proof step 10); SC5 -> CALIBRATION.md + 06-11 SUMMARY cross-check (no live step needed).
- Promotion decision (EQ-smoke-1): smokes 09/11 depend on the local xtb.exe fallback list — default stays informational; the owner may promote either to REQUIRED (a one-line REQUIRED_SMOKES edit + this plan's files_modified would grow — apply only on explicit instruction).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final full-gates pass on the completed phase tree</name>
  <files>.planning/phases/06-xtb-pipeline/06-12-SUMMARY.md</files>
  <action>
    1. `python3.6 tests/run_gates.py` — must be green; record the unittest count and delta vs the phase-5.2-era baseline.
    2. `python3.6 tests/run_gates.py --smoke` — REQUIRED smokes (incl. the new 10_chain_count) all PASS via flushed SMOKE-OK sentinels; smokes 09 + 11 report as informational PASS; smoke 02 stays the known informational dead-end FAIL (non-blocking).
    3. `python3.6 tests/run_gates.py --xtb` — PASS ('xtb version' + 'normal termination').
    4. Record all three verdicts + counts in the SUMMARY. If anything fails: fix at symbol level and re-run (commit `fix(06-12): <fix>`); do not proceed to the checkpoint on a red tree.
    Commit (if fixes): `fix(06-12): phase-closing gate fixes` else `docs(06-12): final gate results recorded`.
  </action>
  <verify>all three gate commands green; SUMMARY records counts + sentinel lines</verify>
  <done>The merged phase tree is gate-green across WSL + smoke + xtb legs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — live pipeline feel-check (launch / cancel / relaunch / failures)</name>
  <files>.planning/phases/06-xtb-pipeline/06-12-SUMMARY.md</files>
  <what-built>
    The complete Phase-6 pipeline: completion anchors snake_xyz; Get Spectra runs the launch pipeline (head-inclusive counts re-check, budget_guard warnings, binary resolution, async QProcess runner in a fresh %TEMP% dir); the Spectra placeholder shows status + streaming log + Cancel/Run-again; terminal branches copy artifacts to %TEMP%\srp_spectra\<snake_id>\, delete the spray dir, and anchor spectra_run; calibration applied to the warning wording + thread default (06-11).
  </what-built>
  <how-to-verify>
    In REAL Windows PyMOL 2.5.0 with the plugin loaded. Record a PASS/FAIL verdict per step (reference symbols, not line numbers). A small box + win cap 2-3 keeps runs short.

    1. LAUNCH PRECONDITIONS: open the plugin fresh; click Get Spectra WITHOUT playing. EXPECT: the Spectra tab opens; the status area shows the refuse line ('no completed snake to run - play a game to completion first...'); nothing starts; no crash.
    2. PLAY + COMPLETE: configure a small game (cap 2-3) and win (or crash — both paths are the same pipeline). EXPECT: the completion lines appear (score/snake/atoms) and Get Spectra activates.
    3. LAUNCH (SC1+SC4): click Get Spectra. EXPECT: switch to the Spectra tab; the status area shows the counts line 'spectra input: N molecules (incl. head), M atoms (budget 100)' and — when M > 100 — the hessian warning line WITH the revealed counts; then 'xtb running...'; log lines stream into the log area; the SAME counts/warning lines also appear in the game-tab info box.
    4. RESPONSIVENESS (SC1): DURING the run: rotate the viewer, open menus, watch the game-tab HUD timers. EXPECT: everything stays responsive (no freeze, no modal).
    5. CANCEL (SC2): click 'Cancel xtb run' mid-run. EXPECT: a cancel line; the run stops; status shows the cancelled outcome (never 'ok'); the button flips to 'Run again'; Get Spectra re-enables.
    6. RELAUNCH AFTER CANCEL (SC2): click 'Run again'. EXPECT: a new run starts (fresh status lines) — no double-run error, no stuck state.
    7. NO DOUBLE-RUN (SC2): start a run and immediately try 'Run again'/Get Spectra again. EXPECT: the guard line ('an xtb run is already active...') and exactly ONE xtb process (Task Manager or the single status stream).
    8. COMPLETION + ARTIFACTS (SC3): let a run finish. EXPECT: status 'xtb finished: ok'; %TEMP%\srp_spectra\<snake_id>\ contains snake.xyz, xtb.log, g98.out, vibspectrum, xtbopt.xyz; NO leftover srp_* spray dir in %TEMP% from the run.
    9. FAILURE PATHS (SC3): (a) set the Setup-tab xtb path to something invalid (or clear it for auto-detect, which finds nothing on this machine) -> Get Spectra. EXPECT: the clear 'xtb not found - set the xtb path on the Setup tab...' line; no launch; Get Spectra usable again after fixing the path. (b) a cancelled/failed run NEVER displays success wording.
    10. HEAD-INCLUSIVE PROOF (SC4): compare the counts line's M with the completion HUD's atoms line plus the head molecule's manifest atom_count. EXPECT: M == atoms_total + head atom_count (the head-exclusion trap is visibly absent).
    11. CALIBRATION WORDING: read the over-budget warning (any snake > 100 atoms triggers it on a cap-10 win). EXPECT: the wording matches the 06-11-applied literal (owner approves or amends — amendments are literal-only edits + pin updates).
    12. DEFERRED SCOPE: confirm the Spectra tab shows ONLY the temporary status/log/cancel affordances (no plot/table promises). EXPECT: understood + accepted; plot/table/log-panel UX is Phase 7's checkpoint.

    Verdict: PASS/FAIL table over steps 1-12 + any amended literals.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with amended wording) or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply checkpoint fixes/amendments + close the phase SUMMARY</name>
  <files>.planning/phases/06-xtb-pipeline/06-12-SUMMARY.md</files>
  <action>
    IF the owner approved everything: record the verdict table + the EQ decisions record (EQ-xyz-1 engine-xyz handoff; EQ-guard-1/2/3; EQ-desync-1; EQ-omp-1 outcome; EQ-artifact-1 copy-out-then-delete; EQ-binary-1 smoke-only fallback; EQ-smoke-1 informational-first + promotion decision; EQ-ux-1/2 placeholder affordances; EQ-checkpoint-1 scope) + the Phase-7 handoff notes (spectra_run keys, stable artifact paths, the runner signal vocabulary: started/log_line/run_finished; placeholder widgets are temporary).
    IF the owner amended wording or reported defects: apply literal-only edits (budget_guard / setup_logic / gui strings) + pin updates, re-run the full gates, commit `fix(06-12): owner-approved wording amendments`.
    IF the owner instructed smoke promotion: edit tests/run_gates.py REQUIRED_SMOKES accordingly + re-run --smoke.
    Commit: `docs(06-12): Phase 6 closed — checkpoint approved` (or the fix variant).
  </action>
  <verify>python3.6 tests/run_gates.py green after any edits; the SUMMARY carries the verdict table + EQ record + Phase-7 handoff</verify>
  <done>Phase 6 is closed: gates green, checkpoint approved, decisions and handoffs recorded.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` (+ `--smoke`, `--xtb`) green on the final tree.
- The checkpoint verdict table exists in the SUMMARY with per-step verdicts and final literals.
- Cross-check vs phase SCs: SC1 (steps 3-4), SC2 (steps 5-7), SC3 (steps 4, 8-9), SC4 (steps 3, 10), SC5 (06-CALIBRATION.md + 06-11 artifacts).
</verification>

<success_criteria>
- All gates green; the human checkpoint APPROVED; every EQ decision recorded with its outcome; Phase 7 receives a written handoff (record shape, artifact paths, signal vocabulary, deferred UI scope).
</success_criteria>

<output>
After completion, create `.planning/phases/06-xtb-pipeline/06-12-SUMMARY.md`
</output>
