---
phase: 5.3-randomized-pickup-spawning
plan: 05
subsystem: game-policy
tags: [pymol, pure-python, spawn-policy, seeded-rng, gates, smokes, feel-check]

# Dependency graph
requires:
  - phase: 5.3-randomized-pickup-spawning
    provides: "5.3-01 uniform box-sampling + min-head-dist floor; 5.3-02 seeded grid-scan fallback offset; 5.3-03 s9 integration scenario; 5.3-04 gui call-site pins (zero-edit verdict)"
provides:
  - "Phase-closing gate/smoke verdict record (755 unittests, gates 1/2/3 PASS, 7/7 required smoke sentinels)"
  - "Owner feel-check verdict on randomized spawn feel + the approved MIN_HEAD_DIST_FACTOR value"
  - "Phase 5.3 wrap one-liner for STATE/ROADMAP consumption"
affects: [phase-6-xtb-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Owner feel-check as a phase-closing blocking gate for unpixellable play-feel numbers (5.1-06 / 5.2-09 precedent)"
    - "Single-knob owner retune: one DATA-ONLY constant line + re-gates (5.1 SPEED_TIERS retune precedent 4502143)"

key-files:
  created: []
  modified: [] # approved as-is — conditional retune of spawn.py/test_spawn.py NOT taken

key-decisions:
  - "Ship MIN_HEAD_DIST_FACTOR = 0.35 as-is — owner feel-check APPROVED (2026-09-26): 'good, all pass'"
  - "Defer edge-biased spawn distribution (owner suggestion) to a future plan — distribution-shape change is not a single-knob retune; owner sanctioned deferral"

patterns-established:
  - "Wrap-plan verdict recording: gates + smokes run and recorded BEFORE the blocking human feel-check"
  - "Owner enhancement ideas that exceed the locked single-knob scope are recorded as DEFERRED ideas in the SUMMARY, not implemented (5.1/5.2 precedent extended)"

# Metrics
duration: ~50 min span across 2026-09-26 (Task 1 executor-run + blocking owner feel-check + Task 3 wrap)
completed: 2026-09-26
---

# Phase 5.3 Plan 05: Phase Wrap — Gates, Smokes & Owner Feel-Check Summary

**Full gate suite green (755 unittests, gates 1/2/3 + 7/7 required smoke sentinels) and owner feel-check APPROVED ("good, all pass") — seeded box-wide spawns ship with MIN_HEAD_DIST_FACTOR = 0.35 unchanged; edge-bias idea deferred.**

## Performance

- **Started:** 2026-09-26T12:27:45Z (Task 1)
- **Completed:** 2026-09-26T12:44:20Z (Task 3 wrap, after owner verdict)
- **Tasks:** 3 of 3 complete (Task 2 = blocking human-verify, APPROVED; Task 3 = record, no retune)
- **Files modified:** 0 code files (verification-only + this SUMMARY)

## Task 1 Verdicts (2026-09-26, executor-run)

### Full gates — `python3.6 tests/run_gates.py`

| Gate | Verdict |
|------|---------|
| gate 1: syntax + plugin-path safety | **PASS** |
| gate 2: purity (AST) | **PASS** |
| gate 3: unittest (scoped discover) | **PASS — Ran 755 tests, OK** |

755 unittests includes the 5.3 net-new tests (uniform-sampling pins, min-head-dist property pins, seeded fallback-offset pins, s9 integration scenario) — merged-tree count matches the orchestrator-verified post-merge baseline.

### Required headless smokes — `python3.6 tests/run_gates.py --smoke`

Verdict = flushed `SMOKE-OK` sentinels in output (never exit codes — PyMOL swallows rc through the .bat). All 7 required smokes PASS:

| Smoke | Sentinel |
|-------|----------|
| `smoke/01_skeleton_smoke.py` | SMOKE-OK SKELETON |
| `smoke/03_viewer_bridge_smoke.py` | SMOKE-OK VIEWER-BRIDGE |
| `smoke/04_demo_e2e_smoke.py` | SMOKE-OK VIEWER-DEMO |
| `smoke/05_loop_camera_smoke.py` | SMOKE-OK LOOP-CAMERA |
| `smoke/06_input_smoke.py` | SMOKE-OK INPUT-WIZARD |
| `smoke/07_transform_sweep_smoke.py` | SMOKE-OK TRANSFORM |
| `smoke/08_stack_place_smoke.py` | SMOKE-OK EDGEON |

- gate 4: headless smokes (required) — **PASS**
- `smoke/02_dialog_smoke.py`: informational FAIL (non-blocking) — the known offscreen-Qt dead end (01-05 locked decision; GUI verdicts are human-verify only). Unrelated to 5.3.
- Zero spawn-position risk confirmed by construction: no smoke calls `PickupSpawner`; smoke 08 builds pickup scenes at hard-coded centroids via the position-parameterized materialize contract.

## Task 2 — Owner Feel-Check (BLOCKING checkpoint:human-verify)

**Status: APPROVED.**

The feel-check brief presented to the owner:

1. Launch the dev plugin in Windows PyMOL (usual setenv.bat / dev install route).
2. Setup tab: defaults (medium box +/-55, normal speed 6.0 A/s), press GO!.
3. FIRST molecule: must NOT appear ~8-14 A ahead of the nose — expect somewhere across the box (>= ~18 A away on medium).
4. Capture 2-3 molecules while roaming: successive spawns land in visibly DIFFERENT regions of the box (not a bubble tracking the head); reaching each one requires actual steering/turns.
5. Difficulty feel: captures neither trivial (straight-line drift) nor tedious (marathon cruises). The spread factor is retunable — say the word.
6. Refuse path: biphenyl REFUSE_ATOM demonstrator — pickup despawns and the next molecule spawns elsewhere; a refuse streak triggering the cooldown still shows the HUD DBG pause/resume lines.
7. Restart: game flow unchanged (scene rebuild + a fresh first spawn somewhere in the box).

**Owner verdict (2026-09-26, verbatim):** "good, all pass. i guess to make it better can increase the chance of spawn near box edge. if this is quick add it, otherwise just mark it and move on for now."

Context: live feel-check in real Windows PyMOL with the `SRP_DEBUG=1` tracer on, owner-driven session 2026-09-26. All feel-check behaviors observed passing: first spawn across the box, visibly different spawn regions per capture, fair difficulty, refuse/cooldown intact, restart unchanged.

**Verdict: APPROVED — no retune requested.** The randomized spawn policy ships as-is.

**Approved MIN_HEAD_DIST_FACTOR:** **0.35** (default, unchanged — small ~11.0 A / medium ~18.0 A / large ~28.5 A)

## Task 3 — Wrap Record (approved as-is + deferred idea)

**Status: COMPLETE — no code change (MIN_HEAD_DIST_FACTOR stays 0.35).**

- The retune branch was NOT taken: `serpentrum/spawn.py` and `tests/test_spawn.py` are untouched by this plan.
- The owner's edge-bias suggestion is NOT a single-knob data retune — it is a sampling-distribution change to `_spawn`'s draw loop (new RED/GREEN TDD cycle, seed-stream impact analysis, re-verification of spread pins). Per the plan's locked scope ("per-preset or non-knob tuning requests → record as DEFERRED, do not implement") it is deferred; the owner explicitly sanctioned deferral ("otherwise just mark it and move on").

### DEFERRED idea (for a future plan)

**Edge-biased spawn distribution** — owner suggestion 2026-09-26: increase the chance of spawns near the box edge.

- **Sketch:** mix the uniform draw with an edge-band sampler (e.g. with probability p draw one axis from the outer band `[x0, x0+band]` or `[x1-band, x1]`, the other uniform — band ~ 0.2-0.25 of half-span), keeping both seeded draws per attempt and the min-head-distance floor unchanged.
- **Impact:** changes the 32-draw stream shape (same-seed sequences differ from 0.35-uniform — acceptable: GAME-07 only requires same-seed reproducibility, not cross-version identity) and would need new/updated TestGeometry spread pins + s9 re-proof.
- **Status:** NOT implemented in 5.3 — owner sanctioned deferral.

## Phase-Wrap One-Liner (for STATE/ROADMAP consumption)

**Phase 5.3 COMPLETE 2026-09-26: seeded uniform box-wide pickup spawns (min-head floor 0.35x half-span, clamp [5.0, 0.8x h]), seeded grid-fallback offset, s9 end-to-end proof, gui pins; 755 tests + 7 smokes green; owner feel-check APPROVED (edge-bias idea deferred).**

## Accomplishments

- Full gates green on the merged Phase 5.3 tree — 755 unittests, gates 1/2/3 PASS
- All 7 required headless smokes flush their SMOKE-OK sentinels — gate 4 PASS
- Shipped knob verified in place: `MIN_HEAD_DIST_FACTOR = 0.35` at `serpentrum/spawn.py`, consumed by the `__init__` proportional default (`min(max(factor*h, HEAD_CLEARANCE_A), 0.8*h)`)
- Owner feel-check APPROVED ("good, all pass") — the spawn policy ships exactly as merged; zero code churn from the human-verify gate
- Edge-bias enhancement captured as a fully-sketched DEFERRED idea for a future plan

## Decisions Made

- **Ship MIN_HEAD_DIST_FACTOR = 0.35 as-is** — owner's live feel-check (2026-09-26, SRP_DEBUG=1) approved the feel at the default; no retune branch taken, zero code edits, zero re-verification churn.
- **Defer the edge-biased spawn distribution** — a distribution-shape change (not a knob value); implementing it inside a wrap plan would violate the locked single-knob scope. Recorded with an implementation sketch and impact analysis above; owner sanctioned deferral.

## Deviations from Plan

None — Task 1 executed exactly as written (verification-only, zero failures, zero fixes needed); Task 2 returned the APPROVED verdict so the Task 3 conditional retune branch collapsed to the planned "no code change" path; the deferred-idea recording is exactly the plan's prescribed handling for out-of-knob tuning requests.

## Issues Encountered

None. Smoke 02's informational non-blocking FAIL is the long-standing known state (01-05 offscreen-Qt dead end), not a 5.3 issue.

## Next Phase Readiness

- Phase 5.3 truths hold on the merged tree: box-wide seeded-random spawns, box-proportional min-head floor, per-seed determinism (GAME-07), demote-after-refuse + resumable exhaust cooldown intact, seeded grid-fallback offset, zero wiring churn (5.3-04 zero-edit verdict) — now owner-approved.
- Phase 6 (xtb Pipeline, 12 plans on disk) unblocked — the verdict this plan was gating on landed APPROVED; its `depends_on: ["5.2-09"]` edges were satisfied at 5.2 wrap; 5.3 leaves no new Phase-6 file contention (spawn.py/test_spawn.py touched by zero unexecuted plans).
- Carry-forward: the DEFERRED edge-bias idea is available for a future feel-polish plan if the owner revisits spawn distribution.

---
*Phase: 5.3-randomized-pickup-spawning*
*Completed: 2026-09-26*
