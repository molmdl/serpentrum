---
phase: 5.3-randomized-pickup-spawning
plan: 04
subsystem: testing
tags: [source-scan-pins, gui_game, pickup-spawning, unittest, python3.6]

# Dependency graph
requires:
  - phase: 5.3-randomized-pickup-spawning
    provides: "5.3-01: seeded uniform box-sampling spawn policy with identical PickupSpawner API signatures (heading validated but position-neutral; min_head_dist_a kwarg with proportional default)"
provides:
  - "TestGuiSpawnCallSites source-scan pins: ONE PickupSpawner construction site (_build_engine), ONE spawner.first begin-time spawn, ONE spawner.next_after respawn, seed_from_setup wiring intact, NO min_head_dist_a at the gui call site"
  - "Confirm-by-reading zero-edit compatibility verdict: 5.3 requires ZERO gui_game.py edits; 06-06's declared surface (_present_completion + GameTab.log_external) is untouched by every 5.3 edit (citable by the Phase-6 plan-check)"
affects: [06-xtb-pipeline, 06-06, 5.3-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Source-scan call-site pinning: open the GUI module source and count call forms (the 5.1-05/5.2-06 house pattern), one test per pin group"

key-files:
  created: []
  modified:
    - tests/test_hud_content.py

key-decisions:
  - "gui_game.py passes NO min_head_dist_a at the spawner call site — the proportional default applies and the owner retunes via spawn.py's MIN_HEAD_DIST_FACTOR (5.3-05 feel-check knob), never in the gui"
  - "The heading wiring (_heading_name(engine.heading)) stays supplied to spawner.first/next_after even though it is now position-neutral — dropping it is a Phase-6+ decision, not a 5.3 one"

patterns-established:
  - "TestGuiSpawnCallSites: class-appended-at-end pattern on the 5.2-04/5.2-06-shared tests/test_hud_content.py (sequential per-file discipline; zero restructuring of existing content)"

# Metrics
duration: 4 min
completed: 2026-09-26
---

# Phase 5.3 Plan 04: Gui Spawn Call-Site Pins + Zero-Edit Compatibility Verdict Summary

**Source-scan pins locking gui_game.py's three spawn call sites (one construction, one first, one respawn, seed wiring intact, no min_head_dist_a override) plus the recorded verdict that 5.3's randomized-spawn policy requires ZERO gui_game.py edits — so 06-06's declared surface stays intact.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-26T11:46:15Z
- **Completed:** 2026-09-26T11:50:04Z
- **Tasks:** 2
- **Files modified:** 1 (+ this SUMMARY)

## Accomplishments

- `TestGuiSpawnCallSites` (6 tests) appended to `tests/test_hud_content.py` in the house 5.1-05/5.2-06 source-scan pattern: `source.count('spawn_mod.PickupSpawner(') == 1`, `source.count('spawner.first(') == 1`, `source.count('spawner.next_after(') == 1`, `source.count('spawn_mod.seed_from_setup(') == 1`, `assertNotIn('min_head_dist_a', source)`, `assertIn('_heading_name(engine.heading)', source)`. All pass against the UNMODIFIED gui_game.py; module docstring gains the 5.3-04 pin-group bullet; no existing test or helper touched.
- Confirm-by-reading verdict recorded (below): the three seams satisfy the 5.3 signatures exactly as they stand, and 06-06's declared surface is untouched.
- Full gates green: gates 1/2/3 PASS, 751 unittests (745 baseline + 6 new); purity unchanged; `git diff --stat` for this plan shows only `tests/test_hud_content.py` (+ this SUMMARY).

## Compatibility Verdict (confirm-by-reading — citable by the Phase-6 plan-check)

The 5.3 randomized-spawn policy (5.3-01) was designed so `PickupSpawner.__init__` / `first` / `next_after` keep IDENTICAL signatures — verified by reading the three seams in `serpentrum/gui_game.py` against the 5.3-01 spawn.py, changing NOTHING:

1. `_build_engine`'s construction `spawn_mod.PickupSpawner(records, (x0, y0), (x1, y1), spawn_mod.seed_from_setup(setup), atoms_by_id)` satisfies the 5.3 signature `(records, box_min, box_max, seed, atoms_by_id, exhaust_cooldown_ticks=EXHAUST_COOLDOWN_TICKS, min_head_dist_a=None)` positionally; `min_head_dist_a` is correctly ABSENT, so the box-proportional default (min(max(0.35*h, 5.0), 0.8*h)) applies.
2. `spawner.first((0.0, 0.0), 'right')`: the heading arg is accepted (validated, position-neutral per the 5.3-01 docstrings) — begin-time first spawn needs no adaptation.
3. `_respawn_pickup`'s `spawner.next_after(engine.head, _heading_name(engine.heading), chain_atoms, live_centroids)`: same — heading validated but position-neutral; chain/live clearance legs consume the existing args unchanged.

Therefore gui_game.py needs ZERO edits for 5.3, and none were made (gui_game.py is byte-identical before and after this plan — and after 5.3-01).

**06-06 disjointness:** no 5.3 edit (5.3-01..04) touches `_present_completion` or `GameTab.log_external` — 5.3-01 modified only `serpentrum/spawn.py` + `tests/test_spawn.py`, 5.3-04 modified only `tests/test_hud_content.py` (and the sibling 5.3-02/5.3-03 branches own only `spawn.py`/`tests/test_spawn.py`-family and docs files). Phase 6's 06-06 declared surface in gui_game.py is fully intact.

## Task Commits

Each task was committed atomically on branch `exec/5.3-04`:

1. **Task 1: source-scan pins for the gui spawn call sites** — `60e921f` (test)
2. **Task 2: confirm-by-reading compatibility verdict (change nothing)** — recorded here; verified by the full gate run (no commit of its own — zero code changes by design)

**Plan metadata:** see below (docs: complete plan)

## Files Created/Modified

- `tests/test_hud_content.py` — added `TestGuiSpawnCallSites` (6 source-scan pins) + one module-docstring pin-group bullet; NOTHING else touched.
- `.planning/phases/5.3-randomized-pickup-spawning/5.3-04-SUMMARY.md` — this file (the verdict's permanent home).

## Decisions Made

- The gui call site carries NO `min_head_dist_a` kwarg: the proportional default is the intended production behavior, and the 5.3-05 feel-check retune knob is `spawn.py`'s `MIN_HEAD_DIST_FACTOR`, never gui_game.py (pinned by `assertNotIn`).
- The heading wiring stays: `_heading_name(engine.heading)` remains supplied to `next_after` (and the literal `'right'` to `first`) even though 5.3 made headings position-neutral — removing the wiring is a Phase-6+ decision; keeping it preserves the validation contract and zero-diff compatibility.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 5.3-05 (wrap/feel-check) can cite this verdict; the owner-retunable knob for the feel-check is `MIN_HEAD_DIST_FACTOR` in spawn.py (the gui call site is pinned override-free).
- Phase 6's plan-check for 06-06 can cite the 06-06 disjointness line above instead of re-deriving it: 5.3 touched `_present_completion` / `GameTab.log_external` zero times.

---
*Phase: 5.3-randomized-pickup-spawning*
*Completed: 2026-09-26*
