---
phase: 5.3-randomized-pickup-spawning
plan: 04
type: execute
wave: 2
depends_on: ["5.3-01"]
files_modified:
  - tests/test_hud_content.py
autonomous: true
must_haves:
  truths:
    - "gui_game.py constructs PickupSpawner at EXACTLY ONE site (_build_engine), calls spawner.first exactly once (begin-time), and spawner.next_after exactly once (_respawn_pickup) — pinned by source-scan tests in the house 5.1-05/5.2-06 style"
    - "gui_game.py passes NO min_head_dist_a — the proportional default applies; owner retunes happen via spawn.py's MIN_HEAD_DIST_FACTOR, never at the call site (pinned by assertNotIn)"
    - "Confirm-by-reading verdict recorded: the 5.3 policy change requires ZERO gui_game.py edits (signatures preserved; heading args validated but position-neutral), and 06-06's declared surface (_present_completion + GameTab.log_external) is untouched by every 5.3 edit"
  artifacts:
    - path: tests/test_hud_content.py
      provides: "TestGuiSpawnCallSites source-scan pins (one construction site, one first call, one next_after call, seed_from_setup wiring, no min_head_dist_a at the call site)"
      contains: "TestGuiSpawnCallSites"
  key_links:
    - from: "tests/test_hud_content.py TestGuiSpawnCallSites"
      to: "serpentrum/gui_game.py spawn seams"
      via: "house source-scan pattern (open + source.count == 1)"
      pattern: "spawn_mod\\.PickupSpawner\\("
    - from: "serpentrum/gui_game.py _build_engine"
      to: "spawn_mod.seed_from_setup(setup)"
      via: "spawner seed wiring (unchanged by 5.3 — pinned so future drift fails loudly)"
      pattern: "spawn_mod\\.seed_from_setup\\("
---

<objective>
Pin the gui spawn call sites and record the zero-edit compatibility verdict: 5.3's policy change was designed so `PickupSpawner.__init__`/`first`/`next_after` signatures stay IDENTICAL (heading validated but position-neutral), therefore `gui_game.py` needs ZERO edits. This plan makes that guarantee durable (source-scan pins) and records the cross-phase verdict the Phase-6 plan-check can cite.

Purpose: the ROADMAP 5.3 entry flagged shared-file ordering with 06-06 (`_present_completion` + `GameTab.log_external`); the sequencing research directs a symbol-anchored confirm-by-reading plan with the verdict in the SUMMARY (5.2-06's HEAD-eligibility precedent: "confirm-by-reading, change nothing").

Output: source-scan pins in `tests/test_hud_content.py` + a compatibility verdict in the plan SUMMARY. Zero source-file edits.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-spawn-policy.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-sequencing.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-tests-gates.md
@tests/test_hud_content.py
@serpentrum/gui_game.py

The three seams to pin/verify (BY SYMBOL — 5.2-06's merged consent wiring has shifted lines):
- `spawn_mod.PickupSpawner(` construction block inside `_build_engine` (seeded via `spawn_mod.seed_from_setup(setup)`, atoms_by_id passed).
- `spawner.first((0.0, 0.0), 'right')` — the begin-time first spawn.
- `spawner.next_after(engine.head, _heading_name(engine.heading), ...)` inside `_respawn_pickup`.

Locked constraints (do NOT revisit):
- gui_game.py is GUI-class in tools/check_purity.py (pymol.Qt only) — 5.3 adds NO import and NO edit to it; this plan reads it, never writes it.
- 5.3 must stay disjoint from 06-06's declared surface (`_present_completion`, `GameTab.log_external`) — trivially true when gui_game.py is untouched; state it in the SUMMARY verdict.
- House source-scan pattern (already in test_hud_content.py): `source_path = os.path.join(..., 'serpentrum', 'gui_game.py')`; `with open(source_path) as handle: source = handle.read()`; then `source.count('...')` assertions.
- test_hud_content.py is shared with 5.2-04/5.2-06 — BOTH execute before 5.3 (sequential phases), so appending a new class at the end is conflict-free; do not restructure existing content.
- Reference SYMBOLS, never line numbers. unittest only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: source-scan pins for the gui spawn call sites</name>
  <files>tests/test_hud_content.py</files>
  <action>
    Append a new class `TestGuiSpawnCallSites(unittest.TestCase)` at the END of tests/test_hud_content.py (house source-scan pattern, one test per pin group, docstring citing 5.3-04):

    - `test_single_spawner_construction_site`: `source.count('spawn_mod.PickupSpawner(') == 1` — the spawner is built in exactly one place (_build_engine).
    - `test_single_first_spawn_call`: `source.count('spawner.first(') == 1` — the begin-time first spawn.
    - `test_single_respawn_call`: `source.count('spawner.next_after(') == 1` — the _respawn_pickup path.
    - `test_seed_wiring_unchanged`: `source.count('spawn_mod.seed_from_setup(') == 1` — the crc32 setup seed still feeds the constructor.
    - `test_no_call_site_override_of_min_head_dist`: `self.assertNotIn('min_head_dist_a', source)` — the gui uses the proportional default; the owner retunes via spawn.py's MIN_HEAD_DIST_FACTOR (5.3-05 feel-check knob), never at the call site.
    - `test_heading_still_supplied_to_respawn`: `self.assertIn('_heading_name(engine.heading)', source)` — the position-neutral heading arg remains supplied (validated by the spawner; dropping the wiring is a Phase-6+ decision, not a 5.3 one).
    - Update the module docstring's pin-group list with one bullet for the new class.
    - Do NOT touch any existing test or helper.
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v  (all green incl. the new class)</verify>
  <done>New pins pass against the UNMODIFIED gui_game.py; existing test_hud_content tests green UNMODIFIED</done>
</task>

<task type="auto">
  <name>Task 2: confirm-by-reading compatibility verdict (change nothing)</name>
  <files>tests/test_hud_content.py</files>
  <action>
    Read the three seams in serpentrum/gui_game.py (BY SYMBOL, never line numbers) and verify — changing NOTHING:
    1. `_build_engine`'s PickupSpawner construction: positional/keyword args still satisfy the 5.3 signature (`records, box_min, box_max, seed, atoms_by_id, exhaust_cooldown_ticks=...`); `min_head_dist_a` correctly ABSENT (proportional default applies).
    2. `spawner.first((0.0, 0.0), 'right')`: heading arg accepted (validated, position-neutral per 5.3-01 docstrings).
    3. `_respawn_pickup`'s `spawner.next_after(engine.head, _heading_name(engine.heading), chain, live)`: same — no adaptation needed.
    4. Cross-phase verdict: no 5.3 edit (5.3-01..04) touches `_present_completion` or `GameTab.log_external`, and gui_game.py itself is untouched — 06-06's declared surface is intact.
    5. Run the full gates (Task-3 command below); confirm no new pymol-family imports anywhere (purity unchanged).
    Record the verdict as a short compatibility note in the plan SUMMARY (one paragraph + the 06-06 disjointness line) so the Phase-6 plan-check can cite it instead of re-deriving.
  </action>
  <verify>python3.6 tests/run_gates.py  (full suite green; gui_game.py absent from git diff)</verify>
  <done>Verdict recorded in SUMMARY; git diff for this plan shows ONLY tests/test_hud_content.py (+ the SUMMARY)</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_hud_content.py" -v` -> all green incl. TestGuiSpawnCallSites
- `python3.6 tests/run_gates.py` -> gates 1/2/3 + full suite green (purity unchanged; gui_game.py untouched)
- `git diff --stat` for this plan -> only tests/test_hud_content.py (+ SUMMARY)
</verification>

<success_criteria>
- The one-construction-site / one-first / one-respawn wiring is pinned against future drift.
- The zero-edit compatibility verdict (incl. 06-06 disjointness) is recorded in the SUMMARY.
- gui_game.py, spawn.py, and every other source file are byte-identical before and after this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/5.3-randomized-pickup-spawning/5.3-04-SUMMARY.md`
</output>
