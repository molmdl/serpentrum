# Phase 5.3: Randomized Pickup Spawning — Research (Sequencing & Planning Bookkeeping)

**Researched:** 2026-09-26 (UTC — dev shell is HKT UTC+8; never stamp from local date)
**Domain:** Cross-phase sequencing + planning bookkeeping (NOT the spawn-policy domain itself)
**Confidence:** HIGH (all findings read directly from plan frontmatter, ROADMAP, STATE, REQUIREMENTS in this repo)

## Summary

Phase 5.3 must be planned against TWO unexecuted downstream phases that are already fully planned on disk: 5.2 (9 plans, waves 1–5) and 06 (12 plans, waves 1–4). The decisive fact: `serpentrum/spawn.py` and `serpentrum/game_engine.py` are touched by **zero** unexecuted plans; only `serpentrum/gui_game.py` is shared (5.2-06 wave-4 edit + 5.2-09 open-ended wrap + 06-06 wave-2 edit). So the pure spawn-policy work is structurally free of cross-phase conflict; only the optional gui_game wiring seam needs the established "locate seams by symbol" discipline.

Because execute-phase orchestrators run phases sequentially and 5.3 runs AFTER 5.2, 5.3 plans carry **no** cross-phase `depends_on` entries (unlike 06's forward edges to `5.2-09`, which exist because 06 was planned before 5.2 executes). Within-5.3 `depends_on` alone defines the waves.

**Primary recommendation:** plan 5.3 as fine-grained single-seam plans (owner directive: many focused plans acceptable), keep all spawn.py + test_spawn.py edits same-plan (never split a file across same-wave plans), route any gui_game touch into one symbol-anchored wiring plan, and close with an `autonomous: false` wrap plan following the 5.2-09 / 5.1-06 / 04-09 checkpoint precedent.

## File-Ownership Map

Read from `files_modified:` frontmatter of every unexecuted plan (5.2-*, 06-*), plus historical Phase 5 ownership for context.

| File | Phase 5 owner (COMPLETE) | Unexecuted 5.2 plans touching it | Unexecuted 06 plans touching it | Consequence for 5.3 |
|------|--------------------------|----------------------------------|----------------------------------|---------------------|
| `serpentrum/spawn.py` | 05-03 (wave 1, TDD) | **none** | **none** | Completely free. No cross-phase conflict at all. |
| `tests/test_spawn.py` | 05-03 | **none** | **none** | Free; expect heavy pin churn (543 lines pin the LOOKAHEAD/lateral policy 5.3 replaces). |
| `serpentrum/game_engine.py` | 05-04 (wave 1, TDD) + 5.1-01 | **none** | **none** (06-01/06-02/06-05 grep hits are body text "engine-shaped atoms", not files_modified) | Free. Randomization is a spawn-policy change; the engine consumes `pickups=` records and needs no edit unless 5.3 deliberately changes record shape. |
| `serpentrum/gui_game.py` | 05-11/13/14/15 + 5.1-05 | **5.2-06** (wave 4 — consent restamp in `_handle_stack_event`, consent note slot in `begin_game` notes block, `generic_stack` import); **5.2-09** (wave 5 wrap — NOT listed in files_modified but its conditional fix task may touch any file) | **06-06** (wave 2 — `_present_completion` snake_xyz + `GameTab.log_external`); **06-12** (wrap, open-ended) | ONLY shared file. 5.3's wiring plan must symbol-anchor (5.2's edits shift lines) and avoid 06-06's surface (`_present_completion` + `log_external`) so the already-planned 06-06 stays valid. |
| `tests/test_hud_content.py` | 05-09 + 5.1-03/05/06 | 5.2-04 (wave 3) + 5.2-06 (wave 4) | none | Shared if 5.3 adds source-scan pins on gui_game spawn call sites (the house pattern — see 5.1-05/5.2-06). Sequential per-file discipline applies. |
| `serpentrum/__init__.py` | 01-01, 05-06 | none listed | 06-05 (adds `spectra_run` to `_SerpentrumState`) | Avoid unless 5.3 genuinely needs new anchor state — the spawner already lives in the passed `extras` dict (gui_game:482-485), so none should be needed. |
| `serpentrum/setup_logic.py`, `serpentrum/hud_logic.py`, `serpentrum/gui_setup.py` | various | 5.2-01, 5.2-04, 5.2-05 (+5.2-09) | 06-11 (setup_logic only) | Out of 5.3 scope per roadmap ("likewise NO Setup changes implied by the owner quote"); keep it that way. |

**Answers to the specific questions:**
- **(a) spawn.py completely free of 5.2/06 modifications?** YES — verified against every 5.2-* and 06-* `files_modified:` list. Zero hits (the only non-Phase-5 greps for `spawn.py` are body-text references in 5.2/06 RESEARCH files).
- **(b) which 5.2 plan modifies gui_game.py, in which wave?** `5.2-06-PLAN.md`, wave 4 (`type: execute`, `autonomous: true`), plus the wave-5 wrap 5.2-09 whose conditional fix task may touch any file (the reason all 06 forward edges point at `5.2-09`, not `5.2-06`).
- **(c) does any 06 plan modify game_engine.py or gui_game.py?** game_engine.py: **no**. gui_game.py: **yes, exactly one** — `06-06-PLAN.md` (wave 2, `depends_on: ["06-02", "5.2-09"]`), scoped by its own SHARED-FILE note to "`_present_completion` + one new 3-line method, no overlap with consent logic".
- **(d) what shared files drive the 06 → 5.2-09 edges?** 06-04 ← `pymol_bridge.py`; 06-05 ← `serpentrum/__init__.py`; 06-06 ← `gui_game.py`; 06-11 ← `setup_logic.py` + `tests/test_setup_logic.py`. Note: 06-04/06-05's own NOTES admit no shipped 5.2 plan lists those files — the edges exist because 5.2-09's fix reach is **open-ended**, not because of a listed edit.
- **Correction to the roadmap note:** it says "06-05, 06-09 carry `depends_on: ['5.2-09']`". Precisely: 06-04, 06-05, 06-06, and 06-11 carry the edge **directly**; 06-09 depends on 5.2-09 only **transitively** (via 06-04/06-05/06-06 — its direct list is `["06-01", "06-04", "06-05", "06-06"]`).

## Sequencing Constraints for 5.3

Precise rules the 5.3 plans must encode:

1. **No cross-phase `depends_on` entries in 5.3 plans.** Phases execute sequentially (`/gsd-execute-phase 5.2` then 5.3 then 6 — STATE.md "Next action"). The 06 `depends_on: ["5.2-09"]` edges are a forward-reference pattern that exists **only because** 06 was planned before 5.2 executed. 5.3 is both planned AND executed after 5.2, so its plans depend only on intra-phase siblings. Do not copy the `["5.2-09"]` edge style into 5.3 frontmatter.
2. **Assume 5.2-06's consent wiring is merged — locate gui_game seams by SYMBOL, never line numbers.** House precedent (5.1-05-PLAN.md:61, written when 5.2 was the pending phase): *"Reference SYMBOLS, never line numbers … locate seams by symbol (`_build_engine`'s GameEngine call, the `stack_mode_note` logging block, `_on_restart`) and add alongside; do not restructure 5.2's code."* For 5.3 the anchor symbols are: the `spawn_mod.PickupSpawner(` construction block inside `begin_game` (today :482-485, WILL shift after 5.2-06's `_handle_stack_event`/notes edits), the `extras['spawner']` seeding (today :393), and the once-per-run `begin_game` notes block (C1 note / consent note / speed note, today :422-435, gains one consent line from 5.2-06).
3. **Stay disjoint from 06-06's declared surface** (`_present_completion` + `GameTab.log_external`). The spawn wiring seam (spawner construction / extras) is naturally disjoint — state this explicitly in the wiring plan's context, mirroring 06-06's own SHARED-FILE note style, so plan-checker can verify the invariant.
4. **Plans touching only spawn.py + tests/test_spawn.py are content-independent of 5.2 execution** (verified via ownership map), but they still EXECUTE after 5.2 by phase sequencing. There is no value in early execution; do not suggest it in plans.
5. **Determinism contract is 5.3's core constraint, not sequencing — but it drives task shape.** spawn.py is deterministic by construction (`seed_from_setup` crc32 of the whole setup dict; private `random.Random(seed)` per spawner; 05-03 truth: "same setup + same call sequence => identical positions"). "More random" MUST remain seeded-deterministic (owner's complaint is about the LOOKAHEAD_A=8.0-ahead-of-heading placement policy, not about non-determinism). test_spawn.py (543 lines) pins the lookahead/lateral/grid-fallback policy verbatim — policy change = large pin rewrite; split the spawn.py work into fine-grained plans by seam (draw policy, `_legal` clearances, cooldown interplay, grid fallback) only if each split keeps one-file-per-plan.
6. **Wrap plan mirrors 5.2-09/5.1-06/04-09:** `autonomous: false`, files_modified = own SUMMARY + any conditional-fix files; Task pattern = full gates → ONE blocking owner feel-check (`checkpoint:human-verify`, gate="blocking") → conditional literal/fix task. Per 06-12-PLAN.md:47: *"Checkpoint precedent: 5.2-09 (gates task + ONE blocking checkpoint + conditional fix task); per-item approvals ACCUMULATE across rounds; reference SYMBOLS never line numbers in steps."*

## Recommended Wave/Depends_on Structure

Not the plan itself — the shape the planner should instantiate (fine-grained per owner directive; same-file rule governs parallelism):

```
Wave 1 (parallel):   5.3-01  TDD spawn position policy core        files: spawn.py, tests/test_spawn.py     depends_on: []
                     5.3-02  (optional) docs/research-adjacent     files: disjoint from 01                  depends_on: []
Wave 2 (sequential): 5.3-03  cooldown/demotion interplay re-pin    files: spawn.py, tests/test_spawn.py     depends_on: [5.3-01]   ← same files => next wave
Wave 3 (parallel):   5.3-04  gui_game spawn wiring (if needed)     files: gui_game.py, tests/test_hud_content.py  depends_on: [policy plans]
                     5.3-05  pure integration chain test           files: tests/test_phase53_integration.py depends_on: [policy plans]
Wave 4 (blocking):   5.3-NN  full gates + owner feel-check         files: 5.3-NN-SUMMARY + conditional      autonomous: false, depends_on: all
```

Sizing rules extracted from the codebase:
- **spawn.py edits and test_spawn.py edits always in the SAME plan** (5.2 wave-1 precedent: each plan owns its module + its test file).
- The **gui_game wiring plan is the ONLY plan that may touch a 5.2/06-shared file**; isolate it (one plan, one wave, symbol-anchored). If research/planning concludes the policy change is fully expressible inside spawn.py (constructor kwargs, e.g. the `exhaust_cooldown_ticks=EXHAUST_COOLDOWN_TICKS` trailing-kwarg precedent), consider making gui_game's touch a **docstring-only or zero-edit** outcome and stating that in the plan ("confirm-by-reading, change nothing" — 5.2-06's HEAD-eligibility precedent).
- Wave-parallel plans MUST have disjoint `files_modified` (see Worktree notes below). Wires: 5.3-04 and 5.3-05 are same-wave-safe (disjoint files).
- A larger count is explicitly welcomed ("I accept more than 20 or even 50-100 plans" — owner, this session) — but only where each plan is genuinely one seam; don't manufacture plans that split one file's coherent edit.

## ROADMAP Finalization Format

Byte-style to replicate (from 5.1/5.2 finalized entries, ROADMAP.md:170-214). The current 5.3 entry (ROADMAP.md:216-229) holds the goal/notes scaffold; the planner replaces `**Plans**: 0 plans` + the `TBD` checkbox line and fills/removes the `**Details:**` placeholder.

**Details-section answer:** no executed-phase entry (5.1, 5.2, 05, 06, 07) has a `**Details:**` section — only the 5.3 placeholder (line 226, an artifact of phase insertion) does. Recommendation: **remove the Details block** and follow the 5.1/5.2 structure exactly (`Notes:` paragraph flows directly after the plan checkboxes).

Finalized-entry template (matching 5.2's committed format):

```markdown
### Phase 5.3: Randomized Pickup Spawning *(INSERTED 2026-09-26 — urgent insertion, owner request)*

**Goal**: [rewrite of the current draft — pickup spawn positions are seeded-randomized across the box instead of LOOKAHEAD-biased ahead of the heading; determinism per seed_from_setup preserved]
**Depends on**: Phase 5 (COMPLETE) — executes after Phase 5.2, before Phase 6
**Requirements**: (none new — refines the Phase 5 pickup spawn policy; coverage stays 46/46)
**Success Criteria** (what must be TRUE):
  1. [behavioral truths — spawn spread across the box, clearance legs intact, seeded determinism preserved, cooldown machinery intact]
  5. A live human feel-check approves the spawn randomness (checkpoint).
**Plans**: N plans (M waves; planned 2026-09-26):

Plans:
- [ ] 5.3-01-PLAN.md — [one-line title] (Wave 1, parallel)
- [ ] 5.3-NN-PLAN.md — [full gates + owner feel-check] (Wave M, blocking)

Notes: Inserted per owner playtesting feedback 2026-09-26 ("the new mol to be eaten spawn too close to the current position, so its likely too simple. make it more random in the box"). [scope sentence: policy lives in 05-03's spawn.py; no new requirement ID — coverage stays 46/46]. [sequencing sentence, modeled on 5.2's: "executes after 5.2 (planned first), before Phase 6; the only 5.2/06-shared file 5.3 [may] touch is gui_game.py (5.2-06 / 06-06 surfaces) — wiring is symbol-anchored and disjoint from _present_completion/log_external"]. [pin-churn sentence: existing spawn determinism/position pins in tests/test_spawn.py are rewritten by the policy change; all other suites must stay green UNMODIFIED — 5.1-01's "they must stay green UNMODIFIED" convention.]
```

Also required ROADMAP edits (orchestrator/planner ownership — verify who stamps in the plan workflow):
- **Progress table row** (ROADMAP.md:349): `| 5.3. Randomized Pickup Spawning (INSERTED 2026-09-26) | 0/TBD | Inserted (not planned) | - |` → `| 5.3. Randomized Pickup Spawning (INSERTED 2026-09-26) | 0/N | Planned (2026-09-26; N plans in M waves) | - |` (Phase 6/7 row format).
- **Top phase-list line** (ROADMAP.md:26) already exists with the insertion marker — unchanged at planning time; flips `[ ]`→`[x]` only at completion.
- **Depends-on line nuance:** 5.2's finalized entry says `Planned 2026-09-23` in the Plans line and keeps "Plan BEFORE Phase 5.1 … execute sequentially" in Notes. For 5.3 the equivalent is "execute after 5.2" — already in the placeholder's Depends-on line.

STATE.md touchpoints at planning time (planner/orchestrator-owned, per HANDOFF rules): the "Roadmap Evolution" 5.3 entry (line 87) already exists and is complete; the "Next action" line currently reads "…then Phase 5.3 (Randomized Pickup Spawning — INSERTED 2026-09-26, needs /gsd-plan-phase 5.3 first)…" — update the "needs /gsd-plan-phase first" clause once plans land.

## Bookkeeping Rules

- **No new requirement ID.** Confirmed from REQUIREMENTS.md:175 (*"v1 requirements: 46 total (44 original + GAME-11 with Phase 5.1 + STACK-06 promoted from v2 with Phase 5.2, 2026-09-20)"*) and the STATE.md Roadmap Evolution 5.3 entry (*"no new requirement ID … coverage stays 46/46"*). 5.3 needs **no edit to REQUIREMENTS.md at planning time** — unlike 5.1 (added GAME-11, 44→45, footer note) and 5.2 (promoted STACK-06, 45→46, footer note). If a completion-time behavior note is wanted, mirror the 2026-09-20b footer style (dated italic footer paragraph), not a new row.
- **How 5.2 recorded its promotion (for contrast):** new `STACK-06` row in the v1 list (line 46) + v2 list entry annotated "PROMOTED to v1" (line 98) + a dated footer paragraph + coverage recount. 5.3 skips ALL of this; its recording surface is the ROADMAP Notes paragraph + STATE.md Roadmap Evolution entry (already written 2026-09-26).
- **Commit scopes** (from git log, verified): plan-doc creation `docs(5.3): create phase plan`, research `docs(5.3): research … domain`, code `feat(5.3-01):`, tests `test(5.3-01):`, completion `docs(5.3): complete … phase`. Wrap/merge commits use `merge: 5.3-0N …` (5.1 wave precedent). Stage files individually; STATE.md/ROADMAP.md are orchestrator-owned during execution (agents must not edit them mid-execution).
- **Dates:** planning-doc dates are UTC; git commit dates are authoritative; the dev shell is HKT (UTC+8) — never stamp from local wall-clock (STATE.md:114; today is 2026-09-26 UTC).
- **`commit_docs: true`** in `.planning/config.json` — plans/research are committed (not gitignored).
- **Research file naming:** this session uses `5.3-RESEARCH-sequencing.md`, matching the 5.2/06 multi-file convention (`5.2-RESEARCH-engine-generic-entry.md`, `06-RESEARCH-runner.md`). Additional domain research files (e.g. spawn-policy mechanics) may be added by parallel researchers under the same `5.3-RESEARCH-<topic>.md` pattern; the plan `@`-includes each.

## House-Style Plan Granularity

Measured from on-disk plans:

| Phase | Plans | Waves | Tasks/plan | Pattern |
|-------|-------|-------|------------|---------|
| 05 | 16 | 7 | 1–3 | pure TDD → bridge/smoke → gui wiring → integration → wrap |
| 5.1 | 6 | 3 | 1–3 (TDD plans embed RED/GREEN atomic commits in the objective, no `<task>` tags) | one module+its tests per plan; "one-line title" plans |
| 5.2 | 9 | 5 | 2 (wrap: 3) | match: 4 TDD + 4 execute + 1 wrap |
| 06 | 12 | 4 | 1–2 (wrap: 3) | several single-task plans — granularity ALREADY at owner-directed fineness |

Conventions to match:
- **One concern per plan, one sentence per plan title** (ROADMAP checkbox lines read as single-line titles).
- **Plan velocity ~5–25 min each** (STATE metrics: 5.1 avg ~7 min, Phase 4 avg ~4.6 min) — small plans merge cleanly and give clean TDD commit boundaries.
- **TDD plans** carry `type: tdd` with RED/GREEN multi-commit freedom inside; **GUI/wiring plans** are `type: execute` with exact seam instructions + source-scan pins in `tests/test_hud_content.py` (5.1-05/5.2-06 precedent: "exactly one call site" pins).
- **must_haves format** (frontmatter): `truths` (behavioral, quote-strings), `artifacts` (path + provides + contains/min_lines), `key_links` (from/to/via/pattern with a grep-able regex).
- **Wrap plan** is always last wave, `autonomous: false`, `files_modified: [SUMMARY (+ conditional-fix targets)]`, with the blocking `checkpoint:human-verify` task.

## Worktree/Parallel Notes

`parallelization: true` in `.planning/config.json`; AGENTS.md protocol: a wave with >1 plan runs each executor in `git worktree add tmp/exec-<plan> -b exec/<plan>`, merged back in dependency order; single-plan waves commit directly on main.

Implications for 5.3 frontmatter:
1. **Same-wave plans MUST have pairwise-disjoint `files_modified`.** Verified precedent: 5.1 wave 1 = 3 parallel plans (game_engine.py+2 engine test files / setup_logic.py+test / hud_logic.py+test) — disjoint. 5.2 wave 4 = 4 parallel plans (gui_setup.py / gui_game.py+test_hud_content.py / DATA_SOURCES.md / test_phase52_integration.py) — disjoint; note 5.2-04 (wave 3) and 5.2-06 (wave 4) share test_hud_content.py but are in DIFFERENT waves, which is fine.
2. **Same-file plans must be sequential via `depends_on`, never same-wave** — the reason any split of spawn.py work across multiple plans forces wave-sequential ordering.
3. **Executors skip STATE.md during parallel waves** (locked decision 2026-09-10, STATE.md:60) — orchestrator aggregates after the wave. 5.3 plans should not ask executors to edit STATE.md/ROADMAP.md.
4. **The worktree protocol operates per-phase-execution, not per-plan-pair across phases** — 5.3's internal parallelism cannot collide with 5.2/06 executors because phases execute sequentially (STATE.md:190's "no parallel waves across the two phases" note, written for 5.1/5.2, generalizes: 5.3 similarly never waves against 5.2 or 06).
5. Orchestrator-side nothing-to-do note: 06 plans' `depends_on: ["5.2-09"]` edges remain valid after 5.3 lands (5.2-09 stays the edge target); IF 5.3 ends up touching gui_game.py, the 5.3 wrap plan should record a one-line compatibility verdict against 06-06's declared surface (`_present_completion` + `log_external` untouched) so the Phase 6 plan-check doesn't need to re-derive it.

## RESEARCH COMPLETE

- **spawn.py is structurally conflict-free:** zero unexecuted plan (5.2-*, 06-*) lists `serpentrum/spawn.py`, `tests/test_spawn.py`, or `serpentrum/game_engine.py` in `files_modified` — the pure randomization work has no cross-phase file contention; only `gui_game.py` is shared (exactly one listed edit per pending phase: 5.2-06 wave 4, 06-06 wave 2, plus 5.2-09's open-ended wrap reach).
- **No forward-edge depends_on needed:** the `["5.2-09"]` pattern in 06 exists only because 06 was planned before 5.2 executed; 5.3 plans AND executes after 5.2, so intra-phase depends_on alone defines 5.3's waves — but all gui_game seams must be symbol-anchored (5.1-05 precedent) because 5.2-06's merged edits will have shifted line numbers, and must stay disjoint from 06-06's `_present_completion`/`log_external` surface.
- **Roadmap fix for the planner:** the roadmap note's "06-05, 06-09 carry depends_on ['5.2-09']" is imprecise — the direct edge carriers are 06-04/06-05/06-06/06-11; 06-09's is transitive via 06-04/05/06.
- **Bookkeeping is minimal:** no new requirement ID, no REQUIREMENTS.md edit at planning time (46/46 already recorded in STATE Roadmap Evolution); finalization = rewrite of the ROADMAP 5.3 entry in the 5.2/5.1 finalized format (Goal / Depends on / Requirements (none new) / Success Criteria / Plans: N plans (M waves) / checkbox list / Notes — and DROP the `**Details:**` placeholder, which no executed-phase entry has), plus the progress-table row update; commit scopes `docs(5.3)` / `feat(5.3-NN)` / `test(5.3-NN)`; UTC dates only.
- **House granularity is already fine-grained:** 1–3 tasks per plan, one module+its test file per plan, same-wave plans pairwise disjoint in `files_modified` (worktree protocol), wrap plan `autonomous: false` with ONE blocking owner feel-check — the owner directive accepts 20+ plans, so split by real seams (policy draw, `_legal` clearances, cooldown interplay, integration chain, wiring, wrap) rather than compressing.

**Output file:** `/mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum/.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-sequencing.md`

## Sources

- `.planning/phases/5.2-generic-upload-stacking-consent/5.2-01..09-PLAN.md` frontmatter + bodies (HIGH)
- `.planning/phases/06-xtb-pipeline/06-01..12-PLAN.md` frontmatter + SHARED-FILE notes (HIGH)
- `.planning/phases/05-stacking-game-rules/05-01..16-PLAN.md` frontmatter (HIGH)
- `.planning/phases/5.1-game-speed-difficulty/5.1-01..06-PLAN.md` (HIGH)
- `.planning/ROADMAP.md` lines 20-31, 160-240, 340-352 (HIGH)
- `.planning/STATE.md` (Current Position, Decisions, Roadmap Evolution, Date convention) (HIGH)
- `.planning/REQUIREMENTS.md` (STACK-06 rows, coverage count, footer notes) (HIGH)
- `.planning/HANDOFF-2026-09-20-phase5-resume.md` (standing rules) (HIGH)
- `serpentrum/spawn.py`, `serpentrum/gui_game.py` spawn seams (HIGH)
- `git log --oneline -25` (commit scope conventions) (HIGH)
