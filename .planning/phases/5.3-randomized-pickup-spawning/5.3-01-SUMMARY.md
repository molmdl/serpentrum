---
phase: 5.3-randomized-pickup-spawning
plan: 01
subsystem: gameplay
tags: [spawn-policy, determinism, random.Random, crc32, unittest, tdd, python3.6]

# Dependency graph
requires:
  - phase: 05-stacking-game-rules
    provides: PickupSpawner (05-03) seeded cyclical spawner with demote/cooldown machinery (2026-09-19b/c) that this plan's policy swap preserves
  - phase: 5.2-generic-upload-stacking-consent
    provides: phase completed 2026-09-26; 5.3 executes after 5.2 by phase sequencing (spawn.py conflict-free per sequencing research)
provides:
  - Seeded uniform-in-shrunk-box candidate generator in PickupSpawner._spawn (per-axis rng.uniform, two seeded draws per attempt)
  - Box-proportional minimum head distance (min(max(0.35*h, 5.0), 0.8*h) -> 11.025/18.025/28.525 A on small/medium/large) with HEAD_CLEARANCE_A as absolute hard floor
  - min_head_dist_a ctor kwarg + read-only min_head_dist property (owner/test/GUI tuning + introspection)
  - _shrunk_bounds() helper shared by sampling loop, grid fallback, half-span
  - Rewritten/extended spawn pins: uniform spread (>=3 quadrants, >30 A reach), floor enforcement, proportional defaults, kwarg override, tiny-box vacuous fold
affects: [5.3-02 seeded grid-fallback scan offset, 5.3-03 integration re-proof, 5.3-04 gui zero-edit verdict, 5.3-05 feel-check retune of MIN_HEAD_DIST_FACTOR, Phase 6 (signatures unchanged; 06 depends_on edges stay valid)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Uniform box-sampling rejection draw: seeded per-axis rng.uniform over shrunk bounds, MAX_DRAWS budget, deterministic grid fallback"
    - "Box-proportional tunable floor via trailing ctor kwarg + read-only property (exhaust_cooldown_ticks precedent)"
    - "Clamp-to-degenerate-box semantics: h <= 0 folds the floor <= 0 so the leg goes vacuous (exhaustion pins unchanged)"

key-files:
  created: []
  modified:
    - serpentrum/spawn.py
    - tests/test_spawn.py

key-decisions:
  - "Heading kept in first/next_after signatures (validated, position-neutral): zero edits in gui_game.py/game_engine.py/hud_logic.py and sidesteps the Phase-6 shared-file ordering hazard"
  - "min_head_dist default = box-proportional 0.35 x shrunk half-span (clamp 5.0..0.8h), owner-retunable constant MIN_HEAD_DIST_FACTOR (5.1 SPEED_TIERS retune precedent)"
  - "test_chain_atom_clearance downscaled to the +/-35 preset pocket lattice per the plan's cost note (write-time probe-verified: pocket is the ONLY legal region; fallback first hit 1.581 A from center)"

patterns-established:
  - "Policy-swap TDD on pinned geometry tests: rewrite the 3 lookahead-pinning tests in place, keep all position-independent pins byte-identical (5.1-01 'stays green UNMODIFIED' convention)"

# Metrics
duration: 18 min
completed: 2026-09-26
---

# Phase 5.3 Plan 01: Uniform Box-Sampling Spawn Policy + Min-Head-Distance Summary

**Pickup candidates are now seeded-uniform over the wall-margined box (per-axis `rng.uniform`, GAME-07 determinism intact) with a box-proportional minimum head distance (18.025 A on medium), retiring the head-anchored LOOKAHEAD/lateral bubble that kept every spawn within 10.0 A of the snake.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-09-26T11:17:26Z
- **Completed:** 2026-09-26T11:35:39Z
- **Tasks:** 1 feature (TDD: RED + GREEN; no REFACTOR needed)
- **Files modified:** 2

## Accomplishments
- `_spawn` candidate generator swapped: per-axis seeded `self._rng.uniform` over `_shrunk_bounds()` (two draws per attempt), heading validated but position-neutral; `LOOKAHEAD_A`/`LATERAL_MAX_A`/`LATERAL_QUANTUM_A`/`_draw_lateral` retired (grep-verified zero hits)
- Box-proportional min head distance `min(max(MIN_HEAD_DIST_FACTOR*h, HEAD_CLEARANCE_A), MIN_HEAD_DIST_CAP_FACTOR*h)` -> 11.025/18.025/28.525 A on small/medium/large; `min_head_dist_a` trailing ctor kwarg + read-only `min_head_dist` property; `_legal` leg 2 gates on `max(min_head_dist, HEAD_CLEARANCE_A)` (absolute hard floor preserved)
- Degenerate boxes fold `min_head_dist <= 0.8*h` (<= 0) so the leg goes vacuous — TestExhaustion's +/-6 and (0,0)-(2,2) None semantics stay green UNMODIFIED; demote/cooldown machinery, MAX_DRAWS=32, grid fallback, pid/order semantics, crc32 seeding all untouched
- `__init__`/`first`/`next_after` signatures otherwise identical — gui_game.py / game_engine.py / hud_logic.py needed ZERO edits (Phase-6 shared-file ordering hazard sidestepped)

## Task Commits

1. **RED: failing uniform-spread + min-head-distance pins, rewritten lookahead geometry tests** - `6a79fd8` (test)
2. **GREEN: seeded uniform box-sampling spawn policy with min-head-distance floor** - `3a6e496` (feat)

**Plan metadata:** see final docs commit (SUMMARY only; STATE.md orchestrator-owned).

_Note: TDD cycle produced 2 commits; no REFACTOR was needed (implementation already minimal/clean)._

## Files Created/Modified
- `serpentrum/spawn.py` — uniform draw loop in `_spawn`; `_shrunk_bounds()` helper; `MIN_HEAD_DIST_FACTOR`/`MIN_HEAD_DIST_CAP_FACTOR`; `min_head_dist_a` kwarg + `min_head_dist` property; `_legal` leg 2 max-floored; lookahead/lateral constants + `_draw_lateral` retired; module/class/method docstrings re-pinned with dated lineage
- `tests/test_spawn.py` (656 lines) — rewrote `test_different_seed_diverges` (full shape sequences), renamed `test_first_is_ahead_and_clears_head` -> `test_first_clears_head_by_min_dist`, rewrote `test_chain_atom_clearance` (box-covering lattice, one pocket, +/-35 preset); added 5 new pins (proportional defaults, kwarg override, 10-spawn floor enforcement, spread-across-box, tiny-box vacuous fold); module docstring re-pinned

## Decisions Made
- **Seed 42 pinned for the spread test via write-time probe** (`/tmp/opencode/probe_53.py` simulating the planned algorithm): 10 spawns visit all 4 quadrants, max reach 60.0 A (> 30.0 required), min 28.99 A (>= 18.025) — deterministic under the real implementation.
- **Pocket lattice geometry verified at write time as mandated**: exclusion disk radius 5.0 around (24.0, 24.0) on the 2.0 A grid (1,068 atoms kept); exact chain-leg sweep found ZERO legal centroids outside 6.0 A of the pocket and only 4 surely-legal grid centroids within 0.707 A; fallback's first legal hit is (24.5, 22.5), 1.581 A from center — the plan's generous `<= 6.0` assertion has confirmed slack.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Retirement grep found `LOOKAHEAD_A` in a dated-lineage docstring sentence**

- **Found during:** GREEN verification (`grep -n "LOOKAHEAD_A|LATERAL_MAX_A|LATERAL_QUANTUM_A|_draw_lateral" serpentrum/spawn.py` must be ZERO hits)
- **Issue:** the module docstring's dated policy lineage named the retired constant verbatim ("LOOKAHEAD_A = 8.0 + LATERAL in +/-6.0) is RETIRED"), tripping the plan's own retirement grep
- **Fix:** reworded the lineage sentence to describe the retired policy without the literal retired symbol names ("The pre-5.3 lookahead/lateral policy (every spawn confined to a 10 A bubble: 8.0 A ahead of the head plus a seeded lateral in +/-6.0) is RETIRED")
- **Files modified:** serpentrum/spawn.py
- **Verification:** grep returns zero hits; scoped suite still 30/30 OK
- **Committed in:** 3a6e496 (GREEN commit)

**2. [Rule 1 - Bug] Corrected stale box-preset numbers in the test module docstring (bounds narrative still said 18 after the 2026-09-19b re-scale to +/-55)**

- **Found during:** RED module-docstring rewrite (item 10 of the behavior block)
- **Issue:** pin-group narrative 2 read "|x|,|y| <= 18 - 3.5 + 1e-9 on the medium preset" — stale from the pre-2026-09-19b +/-18 box; the fixtures have been +/-55 since the owner re-scale
- **Fix:** 18 -> 55 in the bounds narrative alongside the mandated narratives 3/4 rewrite
- **Files modified:** tests/test_spawn.py
- **Verification:** docstring matches the BOX_MIN/BOX_MAX = +/-55.0 fixtures used by the tests
- **Committed in:** 6a79fd8 (RED commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - documentation literals)
**Impact on plan:** Zero behavioral scope creep; both fixes are literal/documentation accuracy inside the two files the plan already owns.

## Issues Encountered
None — RED failed exactly on the 6 planned drivers, GREEN passed first try, phase5 integration s8 (byte-identical determinism) green unmodified, full gates green.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- **5.3-02** (seeded grid-fallback scan offset): the fallback's x-major corner bias is now visible in principle under uniform spawns; `_shrunk_bounds()` + the untouched fallback provide the seam.
- **5.3-03/5.3-04**: signatures unchanged (heading position-neutral arg INTACT) — gui zero-edit verdict and randomized integration chain re-proof unblocked; this plan already re-proved `test_phase5_integration` s8 green unmodified.
- **5.3-05 feel-check**: `MIN_HEAD_DIST_FACTOR = 0.35` retunable live (5.1 SPEED_TIERS precedent); gates baseline now **745 unittests** (740 at 5.2 wrap + 5 net-new pins; the 3 rewrites kept the count).
- Test wall time: full suite 2.2 s — the +/-35 pocket downscale kept the chain-clearance pin cheap as planned.

---
*Phase: 5.3-randomized-pickup-spawning*
*Completed: 2026-09-26*
