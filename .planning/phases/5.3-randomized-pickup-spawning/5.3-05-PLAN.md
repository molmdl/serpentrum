---
phase: 5.3-randomized-pickup-spawning
plan: 05
type: execute
wave: 3
depends_on: ["5.3-02", "5.3-03", "5.3-04"]
files_modified:
  - .planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md
  - serpentrum/spawn.py
  - tests/test_spawn.py  # CONDITIONAL: touched only if the owner feel-check retunes MIN_HEAD_DIST_FACTOR (Task 3 retunes the three expected numbers in the property-pin subTest table)
autonomous: false
must_haves:
  truths:
    - "Full gates green (syntax walk + plugin-path safety + AST purity + FULL unittest suite) AND all required headless smokes flush their SMOKE-OK sentinels (zero spawn-position risk — no smoke calls PickupSpawner)"
    - "A live human feel-check APPROVES the randomized spawns: molecules visibly roam the whole box, difficulty is fair (never trivially close, never tediously far), and the refuse/cooldown behavior is intact"
    - "The owner-approved MIN_HEAD_DIST_FACTOR value is what ships (default 0.35 unless the feel-check retunes it — single data-line edit + re-gates if retuned)"
  artifacts:
    - path: .planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md
      provides: "phase-closing record: gate/smoke verdicts, feel-check verdict + approved factor, conditional-retune note"
      contains: "feel-check"
    - path: serpentrum/spawn.py
      provides: "MIN_HEAD_DIST_FACTOR constant — the owner-retunable spread knob (retuned only if the feel-check demands it)"
      contains: "MIN_HEAD_DIST_FACTOR"
  key_links:
    - from: "serpentrum/spawn.py MIN_HEAD_DIST_FACTOR"
      to: "__init__ min_head_dist proportional default"
      via: "single-knob retune path (0.35 x shrunk half-span, clamp [HEAD_CLEARANCE_A, 0.8 x h])"
      pattern: "MIN_HEAD_DIST_FACTOR"
---

<objective>
Close Phase 5.3: prove everything with the full gates + required smokes, get the owner's live feel-check verdict on the randomized spawn feel, and apply any owner retune (single-knob) before wrap.

Purpose: the randomized spawn feel is unpixellable in headless smokes — research tests-gates.md directs a live human-verify checkpoint (5.1-06/5.2-09 precedent) as the phase's closing gate. The 0.35 spread factor is a play-feel number; the owner may retune it exactly like the 5.1 speed tiers (2026-09-25 precedent).

Output: green gates + approved feel-check (+ optional one-line retune) + phase SUMMARY.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-spawn-policy.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-tests-gates.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-sequencing.md
@serpentrum/spawn.py
@tests/test_spawn.py

All four prior 5.3 plans are MERGED (5.3-01 policy core, 5.3-02 fallback offset, 5.3-03 s9 integration, 5.3-04 gui pins). This plan depends on all of them.

Locked constraints (do NOT revisit):
- Smoke verdict = flushed `SMOKE-OK` sentinels in output, NEVER exit codes (PyMOL swallows rc through the .bat — AGENTS.md rule).
- Zero smoke risk is EXPECTED: no smoke calls PickupSpawner; smoke 08 builds pickup scenes at hard-coded centroids via the position-parameterized materialize contract. A smoke failure here means something ELSE broke — investigate, do not hand-wave.
- The retune knob is `MIN_HEAD_DIST_FACTOR` in serpentrum/spawn.py (default 0.35) — DATA-ONLY, one line, mirroring the 5.1 SPEED_TIERS retune precedent (4502143). Do NOT widen scope to per-game UI tuning or new kwargs.
- python3.6 gates only; no pip/conda installs (opencode.json denies them); headless smokes go through `cmd.exe /c C:\\src\\run-conda-pymol.bat` via the gates harness.
- Reference SYMBOLS, never line numbers. Commit scopes: fixes `fix(5.3-05)`/`feat(5.3-05)`, docs `docs(5.3-05)`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: full gates + required smokes</name>
  <files>.planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md</files>
  <action>
    Run from the repo root in WSL:
    1. `python3.6 tests/run_gates.py` — gates 1/2/3 + FULL unittest suite must be green (expect the 5.3 net-new tests included).
    2. `python3.6 tests/run_gates.py --smoke` — all required smokes (01/03/04/05/06/07/08); verdict = SMOKE-OK sentinels flushed per smoke, NEVER exit codes.
    3. If anything fails: fix within 5.3 scope (spawn.py / its test files only), re-run both, commit `fix(5.3-05): ...`.
    4. Record both verdicts (test count, sentinel list) in the SUMMARY.
  </action>
  <verify>python3.6 tests/run_gates.py && python3.6 tests/run_gates.py --smoke  (full suite green; every required smoke flushes SMOKE-OK)</verify>
  <done>Gates + smokes green with verdicts recorded; no out-of-scope files touched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Pickups (molecules-to-be-eaten) now spawn seeded-randomly across the WHOLE game box instead of in a 10 A bubble 8 A ahead of the snake's nose, with a box-proportional minimum head distance (small ~11 A, medium ~18 A, large ~28.5 A at the 0.35 default factor). Determinism per seed, the four clearance legs, demote-after-refuse, and the resumable exhaust cooldown are all unchanged.
  </what-built>
  <how-to-verify>
    1. Launch the dev plugin in Windows PyMOL (usual setenv.bat / dev install route).
    2. Setup tab: defaults (medium box +/-55, normal speed 6.0 A/s), press GO!.
    3. FIRST molecule: it must NOT appear ~8-14 A ahead of the nose — expect it somewhere across the box (at least ~18 A away on medium).
    4. Capture 2-3 molecules while roaming: successive spawns land in visibly DIFFERENT regions of the box (not a bubble tracking the head); reaching each one requires actual steering/turns.
    5. Difficulty feel: captures should be neither trivial (straight-line drift) nor tedious (marathon cruises). The spread factor is retunable — say the word.
    6. Refuse path: if a stack attempt refuses (e.g. biphenyl REFUSE_ATOM), the pickup despawns and the next molecule spawns elsewhere; if a refuse streak triggers the cooldown, the HUD DBG pause/resume lines still appear.
    7. Restart: game flow unchanged (scene rebuild + a fresh first spawn somewhere in the box).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (too close / too far / too clustered / anything else).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: conditional owner retune + wrap record</name>
  <files>.planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md, serpentrum/spawn.py</files>
  <action>
    IF the owner retunes (e.g. "still too easy" -> raise, "too much travel" -> lower):
    1. Edit ONLY the `MIN_HEAD_DIST_FACTOR` value in serpentrum/spawn.py (one data line; default 0.35). Suggested scale: each +0.05 adds ~2.6 A on medium. If the owner asks for per-preset or per-game tuning, record it as a DEFERRED idea in the SUMMARY — do not implement.
    2. Re-run `python3.6 -m unittest discover -s tests -p "test_spawn.py" -v` then `python3.6 tests/run_gates.py` — all green (the property pins assert 11.025/18.025/28.525 at the 0.35 default: if the factor changed, update ONLY those three expected numbers in the subTest table + s9's derived expectations if any hardcoded the old value — they should not).
    3. Commit `feat(5.3-05): retune spawn min-head-distance factor per owner feel-check`.
    IF approved as-is: no code change; commit nothing for code.
    Either way: finish the SUMMARY — feel-check verdict + approved factor value, gate/smoke verdicts from Task 1, and the phase-wrap one-liner for STATE/ROADMAP consumption by the orchestrator.
  </action>
  <verify>python3.6 tests/run_gates.py  (green after any retune) && test -f .planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md</verify>
  <done>Owner verdict recorded; shipped MIN_HEAD_DIST_FACTOR matches the approved value; SUMMARY complete</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` -> gates 1/2/3 + FULL suite green
- `python3.6 tests/run_gates.py --smoke` -> every required smoke flushes its SMOKE-OK sentinel
- Feel-check checkpoint APPROVED (blocking gate — the phase is not complete without it)
- `grep -n "MIN_HEAD_DIST_FACTOR" serpentrum/spawn.py` -> the owner-approved value
</verification>

<success_criteria>
- Gates + smokes green; owner feel-check approved (with or without a single-knob retune).
- The approved spread factor is what ships; any retune is one data line + green re-gates.
- Phase 5.3 truths all hold: box-wide seeded-random spawns, min-head floor, determinism, cooldown machinery intact, zero wiring churn.
</success_criteria>

<output>
After completion, create `.planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md`
</output>
