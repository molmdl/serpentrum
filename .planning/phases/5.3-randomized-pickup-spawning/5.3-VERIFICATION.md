---
phase: 5.3-randomized-pickup-spawning
verified: 2026-09-26T20:50:37+08:00
status: passed
score: 5/5 must-haves verified
must_haves:
  truths:
    - "New pickups spawn anywhere in the wall-margined box via seeded uniform sampling (no LOOKAHEAD/lateral bubble)"
    - "Every spawn is >= box-proportional minimum head distance (0.35 x shrunk half-span, clamp <= 0.8 x h; tunable via min_head_dist_a / MIN_HEAD_DIST_FACTOR)"
    - "GAME-07 determinism holds (crc32 seeding, private random.Random); all clearance legs intact; degenerate-box exhaustion semantics unchanged"
    - "PickupSpawner __init__/first/next_after signatures unchanged; gui_game.py/game_engine.py ZERO edits; demote/cooldown machinery intact"
    - "Live human feel-check APPROVED (2026-09-26, verbatim 'good, all pass')"
  artifacts:
    - path: serpentrum/spawn.py
      provides: "uniform box-sampling draw loop, _shrunk_bounds, MIN_HEAD_DIST_FACTOR/MIN_HEAD_DIST_CAP_FACTOR, min_head_dist_a kwarg + min_head_dist property, seeded _grid_scan fallback, demote/cooldown state machine"
    - path: tests/test_spawn.py
      provides: "33 tests: TestGeometry (incl. 11.025/18.025/28.525 min-head pins), TestFallbackOffset, TestDeterminism, TestDemoteAfterRefuse, TestExhaustCooldown, TestExhaustion"
    - path: tests/test_phase5_integration.py
      provides: "test_s9_randomized_spawn_capture_chain (s1-s9 all green)"
    - path: tests/test_hud_content.py
      provides: "TestGuiSpawnCallSites source-scan pins (6 tests)"
    - path: .planning/phases/5.3-randomized-pickup-spawning/5.3-05-SUMMARY.md
      provides: "gate/smoke verdicts + APPROVED feel-check record (factor 0.35 ships unchanged)"
---

# Phase 5.3: Randomized Pickup Spawning — Verification Report

**Phase Goal:** Pickup spawn positions are seeded-randomized uniformly across the game box (instead of a LOOKAHEAD-anchored 10 Å bubble ahead of the heading) with a box-proportional minimum head distance — every capture demands real navigation while GAME-07 seeded determinism (same setup + same call sequence ⇒ identical positions) and the 2026-09-19b/c demote/cooldown machinery stay intact.
**Verified:** 2026-09-26T20:50:37+08:00
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | New pickups spawn anywhere in the wall-margined box (seeded uniform, no bubble) | ✓ VERIFIED | `spawn.py:399-401` — `for _ in range(MAX_DRAWS): cx = self._rng.uniform(x0, x1); cy = self._rng.uniform(y0, y1)` over `_shrunk_bounds()` (box_min/WALL_MARGIN_A ↔ box_max−WALL_MARGIN_A). Legacy symbols `LOOKAHEAD_A`/`LATERAL_MAX_A`/`LATERAL_QUANTUM_A`/`_draw_lateral` ABSENT (grep exit 1). `TestGeometry.test_spawns_spread_across_box` + s9 (all 4 quadrants, 62.1 Å max head distance) green. |
| 2 | Box-proportional min head distance (≈11/18/28.5 Å, clamp ≤0.8×h, tunable) | ✓ VERIFIED | `MIN_HEAD_DIST_FACTOR = 0.35` (line 105), `MIN_HEAD_DIST_CAP_FACTOR = 0.8` (line 110); `__init__` computes `min(max(0.35*h, HEAD_CLEARANCE_A), 0.8*h)` (lines 230-234); `min_head_dist_a=None` trailing kwarg (line 217) + read-only `min_head_dist` property (line 268). Pins 11.025 (small ±35) / 18.025 (medium ±55) / 28.525 (large ±85) at `test_spawn.py:294-296`; kwarg-override, enforced-by-default, and tiny-box-fold tests all green. |
| 3 | GAME-07 determinism + all clearance legs + degenerate exhaustion unchanged | ✓ VERIFIED | `random.Random(seed)` private RNG (line 221); `zlib.crc32` seeding in `seed_from_setup` (line 152, deterministic across processes). All 4 legs in `_legal` (wall margin, min-head floor, chain atoms 3.0 Å, live 6.0 Å). `_grid_scan` seeded start (`randrange(cell_count)`, line 441) with `nx<=0 or ny<=0` guard BEFORE any draw (lines 438-439). `TestDeterminism` (same-seed byte-identical / different-seed diverges), `TestExhaustion` (saturated ±6 → None; (0,0)-(2,2) → None), `TestFallbackOffset` (lands-in-pocket / varies-by-seed / deterministic-per-seed), s8 + s9 byte-identical re-runs — all green. |
| 4 | Signatures unchanged; gui_game.py/game_engine.py ZERO edits; demote/cooldown intact | ✓ VERIFIED | `__init__(records, box_min, box_max, seed, atoms_by_id, exhaust_cooldown_ticks=EXHAUST_COOLDOWN_TICKS, min_head_dist_a=None)` — matches documented API exactly; `first(head_xy, heading)` / `next_after(head_xy, heading, chain_atoms, live_centroids)` unchanged. Git audit: the ONLY 5.3 commits touching `serpentrum/` are `3a6e496` + `a32aedf`, both `serpentrum/spawn.py` ONLY (gui_game.py, game_engine.py, hud_logic.py, pymol_bridge.py: 0 edits). Demote machinery intact: `note_resolution` demote-to-back (lines 309-311), refuse streak → `cooldown_remaining = exhaust_cooldown_ticks` (lines 318-320), resumable `tick()` with resume-edge streak reset (lines 331-338). `TestDemoteAfterRefuse` (5) + `TestExhaustCooldown` (6) green. `TestGuiSpawnCallSites` (6 source-scan pins incl. `assertNotIn('min_head_dist_a', ...)` at call sites) green; call sites read directly: one `PickupSpawner(` (gui_game.py:508), one `spawner.first(` (514), one `spawner.next_after(` (1187), `seed_from_setup(setup)` wiring (510), no `min_head_dist_a` anywhere in gui_game.py. |
| 5 | Live human feel-check approves the spawn randomness | ✓ VERIFIED (human-approved, recorded) | `5.3-05-SUMMARY.md:84-100` — "**Status: APPROVED.**" Owner verdict 2026-09-26 verbatim: "good, all pass…" (live Windows PyMOL, SRP_DEBUG=1 tracer: first spawn across box, visibly different regions per capture, fair difficulty, refuse/cooldown intact, restart unchanged). `MIN_HEAD_DIST_FACTOR = 0.35` ships unchanged; edge-bias idea DEFERRED per owner instruction. Factored as satisfied per phase-close instruction. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `serpentrum/spawn.py` | Uniform draw loop, `_shrunk_bounds`, constants, kwarg+property, seeded `_grid_scan`, demote/cooldown | ✓ VERIFIED | 514 lines, substantive; all symbols present at exact locations cited above; legacy lookahead/lateral symbols absent; no stub patterns |
| `tests/test_spawn.py` | Rewritten geometry/determinism + new min-head/spread/fallback pins | ✓ VERIFIED | 756 lines (≥560 required); 33/33 green; TestFallbackOffset present (3 tests); min-head pins at lines 289-323 |
| `tests/test_phase5_integration.py` | s9 randomized capture chain, derived expectations | ✓ VERIFIED | s1-s9 green; s9 derives bounds (assertGreaterEqual/LessEqual), ≥3 quadrants, max head distance >30 Å, byte-identical re-run — zero hardcoded positions |
| `tests/test_hud_content.py` | TestGuiSpawnCallSites source-scan pins | ✓ VERIFIED | 6 tests green (single construction site, single first, single next_after, seed wiring, no min_head_dist_a override, heading still supplied) |
| `5.3-01..05-SUMMARY.md` | All 5 summaries exist; 05 carries APPROVED feel-check | ✓ VERIFIED | All 5 exist with per-plan completion records; 05 contains verbatim owner approval + shipped-factor record |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `PickupSpawner._spawn` draw loop | `self._rng.uniform` per axis over shrunk bounds | seeded candidate draw | ✓ WIRED | lines 396-404; draw order fixed and documented as part of the seed stream |
| `PickupSpawner._legal` head leg | `self._min_head_dist` (max with `HEAD_CLEARANCE_A`) | proportional floor gate | ✓ WIRED | lines 474-483; floor subsumes 5.0 Å absolute on real presets, folds vacuous on tiny boxes |
| `PickupSpawner.__init__` | `min_head_dist_a=None` trailing kwarg | tunable ctor pattern | ✓ WIRED | line 217, after `exhaust_cooldown_ticks` as planned; override branch lines 227-228 |
| `PickupSpawner._spawn` fallback | `self._grid_scan(...)` | factored helper call | ✓ WIRED | lines 408-409 |
| `PickupSpawner._grid_scan` | `self._rng.randrange(cell_count)` | seeded start cell, wrap coverage | ✓ WIRED | lines 441-443; consumed ONLY on fallback path (stream shape preserved) |
| `tests TestGuiSpawnCallSites` | gui_game.py spawn seams | source-scan (open + count==1) | ✓ WIRED | 6/6 green |
| `gui_game.py _build_engine` | `spawn_mod.seed_from_setup(setup)` | spawner seed wiring | ✓ WIRED | gui_game.py:510, pinned against drift |
| s9 integration test | `spawn.PickupSpawner` + `build_pickup_seed` | live spawner drive, growing chain | ✓ WIRED | imports both; chain grows by translated atoms via `build_pickup_seed` |
| `MIN_HEAD_DIST_FACTOR` | `__init__` proportional default | single-knob retune path | ✓ WIRED | constant consumed at lines 232-233; ships 0.35 (owner-approved) |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
| --- | --- | --- |
| (none — owner-inserted phase, no new REQUIREMENTS.md ids) | ✓ SATISFIED | Phase 5.3 modifies behavior within existing pickup-spawn scope; no new requirement IDs were mapped. The reused GAME-07 determinism property (spawn restart determinism) is pinned green by s8 + s9. Note: REQUIREMENTS.md's GAME-07 row refers to the Phase-4 info-box/restart requirement and is not modified by this phase. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| tests/test_hud_content.py | 362 | `test_refuse_atom_without_detail_renders_placeholder` (name contains "placeholder") | ℹ️ Info | Legitimate Phase-5 HUD test asserting the placeholder detail string — not a stub. |
| serpentrum/spawn.py | 379, 381, 449 | `return None` paths | ℹ️ Info | Documented exhaustion/cooldown semantics (`None` = no legal spawn, no state advance) — designed behavior, pinned by TestExhaustion/TestExhaustCooldown. |

No TODO/FIXME/HACK markers, no console-only handlers, no placeholder renders in any 5.3-touched file.

### Human Verification Required

None outstanding. The phase's single blocking human checkpoint (live spawn feel-check) was APPROVED by the owner on 2026-09-26 — verbatim "good, all pass", conducted in real Windows PyMOL with SRP_DEBUG=1, recorded in 5.3-05-SUMMARY.md. Edge-biased distribution idea explicitly DEFERRED per owner instruction (future plan, not this phase's goal).

### Gaps Summary

No gaps. Full evidence chain:

- **Code:** `serpentrum/spawn.py` implements seeded per-axis uniform sampling over the wall-margined box (2 draws/attempt, MAX_DRAWS=32), a seeded wrap-coverage grid-scan fallback, a box-proportional min-head-distance floor (`min(max(0.35·h, 5.0), 0.8·h)` → 11.025/18.025/28.525 Å on small/medium/large) with ctor override + read-only property, and all four clearance legs. The LOOKAHEAD/LATERAL bubble machinery is fully retired (zero residual symbols).
- **API stability:** `__init__`/`first`/`next_after` signatures byte-match the documented API; git audit proves 5.3 edited only `spawn.py` + test files — gui_game.py/game_engine.py/hud_logic.py untouched (criterion 4 satisfied both by history and by green source-scan pins).
- **Machinery preserved:** demote-after-refuse and resumable exhaust-cooldown state machines are present and fully pinned (11 dedicated tests green).
- **Determinism:** crc32 setup seeding, private `random.Random`, fixed draw order; byte-identical re-runs proven at unit (TestDeterminism), scenario (s8), and end-to-end (s9) levels; fallback randrange consumed only on the fallback path.
- **Gates:** `python3.6 tests/run_gates.py` → 755 tests OK, gates 1/2/3 all PASS (matches the expected 755). Targeted suites: test_spawn 33/33, test_phase5_integration 9/9, test_hud_content 76/76.
- **Human checkpoint:** owner feel-check APPROVED (recorded), factor 0.35 shipped unchanged.

---

_Verified: 2026-09-26T20:50:37+08:00_
_Verifier: OpenCode (gsd-verifier)_
