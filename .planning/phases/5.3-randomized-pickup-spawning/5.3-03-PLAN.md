---
phase: 5.3-randomized-pickup-spawning
plan: 03
type: execute
wave: 2
depends_on: ["5.3-01"]
files_modified:
  - tests/test_phase5_integration.py
autonomous: true
must_haves:
  truths:
    - "A randomized spawn sequence (first + 8x next_after with chain growing by each spawn's translated atoms and live accumulating) through the REAL set_a fixture returns no None, spreads across the box (>= 3 quadrants, max head distance > 30 A), and re-runs byte-identically (GAME-07 end-to-end)"
    - "Every clearance leg holds against the accumulated chain/live at every step (wall margin, min-head-distance, chain atoms, live pickups)"
    - "Scenarios s1-s8 stay green UNMODIFIED — s8's byte-identical determinism pin and all hand-placed engine scenarios are untouched by the policy change"
  artifacts:
    - path: tests/test_phase5_integration.py
      provides: "test_s9_randomized_spawn_capture_chain — the spawn+engine chain re-proven under the randomized policy, expectations DERIVED (never hardcoded positions)"
      contains: "test_s9_randomized_spawn_capture_chain"
  key_links:
    - from: "tests/test_phase5_integration.py s9"
      to: "spawn.PickupSpawner first/next_after"
      via: "live spawner drive with growing chain/live (mirror of s8's helper pattern)"
      pattern: "PickupSpawner\\("
    - from: "tests/test_phase5_integration.py s9"
      to: "spawn.build_pickup_seed"
      via: "translated-atom growth of the chain (executable capture spec, module-local helper)"
      pattern: "build_pickup_seed\\("
---

<objective>
Re-prove the spawn+engine integration chain under the randomized policy: s8 is today's only spawn integration scenario, and it asserts determinism but not the new box-wide spread. An s9 scenario pins that randomized spawns survive a realistic capture chain (growing snake, accumulating live pickups) without Nones, with all clearance legs intact, and still re-run byte-identically.

Purpose: GAME-07 ("same setup + same call sequence => identical positions") is the phase's hard contract; 5.3-01/02 pin it at the unit level, s9 pins it end-to-end through the real set_a fixture with the 5.3 policy live. Research tests-gates.md: "extend test_phase5_integration.py (add an s9 driving randomized spawns through several captures) rather than creating a new chain file".

Output: one new scenario in `tests/test_phase5_integration.py` — zero source-file edits, s1-s8 untouched.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-spawn-policy.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-tests-gates.md
@.planning/phases/5.3-randomized-pickup-spawning/5.3-RESEARCH-sequencing.md
@tests/test_phase5_integration.py
@serpentrum/spawn.py

Locked constraints (do NOT revisit):
- 5.3-01 is MERGED — its policy (uniform draws + min-head floor) is live; expectations are DERIVED from constants (`spawn.HEAD_CLEARANCE_A`, the spawner's `min_head_dist` property, WALL_MARGIN_A) never hardcoded positions (5.1-06 derivation habit: owner retunes must not break this file). 5.3-02 (parallel sibling, same wave) lands the seeded fallback offset — s9's assertions are offset-insensitive (they never assume which path produced a spawn); the wrap plan (5.3-05) proves the combined tree.
- s1-s8 are UNTOUCHED — s8 stays the byte-identical determinism pin; scenario 1's hand-placed pickup discipline (why scripted engine runs stay stable under randomization) is preserved.
- The spawner does NOT gate on MAX_LIVE itself (`can_spawn` is caller-side) — s9 may accumulate live centroids freely like s8 does.
- House chain-file conventions (already in the file): REAL shipped data with ZERO stubs, `DT = 0.1`, the file's existing `DELTA` constant (it is `DELTA = 1e-6` in test_phase5_integration.py — do NOT "fix" it to 1e-9), module-local helpers, no cross-test imports, no `__init__.py` concerns (file exists).
- Reference SYMBOLS, never line numbers. unittest only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: add s9 randomized-spawn capture-chain scenario</name>
  <files>tests/test_phase5_integration.py</files>
  <action>
    Add `test_s9_randomized_spawn_capture_chain` to the spawn scenario class (mirroring s8's structure and reusing the file's existing fixture helpers — `build_fixture()` real set_a records, `pickup_seed()` helper, module-local distance helpers):

    - Build a real `PickupSpawner` over the fixture records with `spawn_mod.seed_from_setup(...)` on a fixed integer seed and the +/-55 medium box (same fixture shape as s8).
    - Drive `first((0.0, 0.0), 'right')` then 8x `next_after((0.0, 0.0), 'right', chain_atoms, live_centroids)` where `chain_atoms` grows by each spawn's TRANSLATED atoms (the executable capture spec: the eaten molecule's atoms join the chain at the spawn centroid — mirror how s8 grows its chain/live), and `live_centroids` accumulates every issued centroid.
    - Assert at every step: result is not None; every translated atom >= 3.0 - DELTA from every prior chain atom; every centroid >= live 6.0 - DELTA from prior live centroids; every centroid >= spawner.min_head_dist - DELTA from (0.0, 0.0); bounds within 55.0 - WALL margin + DELTA.
    - Spread assertions (derived, not hardcoded): the 9 spawns visit >= 3 of the 4 quadrants AND max distance from the origin > 30.0 (far beyond the retired 10 A lookahead bubble).
    - Determinism: re-run the ENTIRE drive with a fresh same-seed spawner and assert the full `(record['id'], pid, centroid)` sequence is byte-identical (GAME-07 end-to-end).
    - Update the file's module docstring scenario list with the s9 one-liner (house style: docstrings enumerate the chain).
    - Do NOT touch any other test, helper, or constant in the file.
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_phase5_integration.py" -v  (s1-s9 all green, s9 new)</verify>
  <done>s9 passes with all derived assertions; s1-s8 byte-identical to before this plan (git diff shows only the new test + docstring line)</done>
</task>

<task type="auto">
  <name>Task 2: full-suite gate run</name>
  <files>tests/test_phase5_integration.py</files>
  <action>
    Run `python3.6 tests/run_gates.py` (syntax walk + plugin-path safety + AST purity + FULL unittest discovery). Confirm the total test count increased by exactly +1 vs the pre-edit count in THIS branch (this worktree has 5.3-01 merged but NOT 5.3-02 — 5.3-02's TestFallbackOffset tests are absent here; the combined tree is proven by the wrap plan 5.3-05). Everything green. If s9 exposes a genuine policy regression (a None mid-sequence, a clearance violation), STOP and fix in spawn.py only if the bug is real (report in the SUMMARY) — do not weaken assertions to pass.
  </action>
  <verify>python3.6 tests/run_gates.py  (gates 1/2/3 green, full suite green)</verify>
  <done>Full gates green with s9 included; no source edits outside tests/test_phase5_integration.py</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests -p "test_phase5_integration.py" -v` -> s1-s9 green
- `python3.6 tests/run_gates.py` -> full suite green (count = prior + 1)
- `git diff --stat HEAD~1` (or vs plan start) -> only tests/test_phase5_integration.py modified
</verification>

<success_criteria>
- s9 proves: no Nones across a 9-spawn randomized capture chain, all four clearance legs + min-head floor intact at every step, box-wide spread, byte-identical re-run.
- s1-s8 green UNMODIFIED; zero source-file edits in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/5.3-randomized-pickup-spawning/5.3-03-SUMMARY.md`
</output>
