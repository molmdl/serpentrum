---
phase: 07-spectra-ui
plan: 03
type: execute
wave: 1
depends_on: ["06-04"]
files_modified:
  - serpentrum/pymol_bridge.py
  - smoke/13_mode_arrows_smoke.py
  - tests/run_gates.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "load_mode_arrows and load_xtbopt land viewer objects ONLY under the srp_ prefix (srp_mode_vec / srp_xtbopt) so cleanup_srp / begin_game / Setup-Apply sweep them and a .pse reload stays fresh-process-cleanable (INFRA-04 contract)"
    - "zoom_mode_frame frames 'srp_xtbopt or srp_mode_vec' exactly once per invocation and no-ops safely (returns False) when neither object exists — never a Selector error"
    - "REQUIRED smoke 13 re-asserts the overlay assumption on EVERY gate run: the g98 Standard-orientation atom block and xtbopt.xyz agree within 1e-3 A, mode-arrow CGO loads as srp_mode_vec with a sane extent, and srp_xtbopt loads with 26 atoms — a silent misalignment (xtb-version drift) can never ship"
    - "The frozen Phase-2 arrow contract is untouched: cgo_build.mode_arrows keeps unit-normalized directions + uniform length (magnitude-proportional arrows would be a pinned-constant change, explicitly out of scope)"
  artifacts:
    - path: serpentrum/pymol_bridge.py
      provides: "load_mode_arrows(cgo, name='srp_mode_vec', zoom=0), load_xtbopt(path, srp_name='srp_xtbopt', zoom=0) with sticks rep, zoom_mode_frame() — thin cmd forwarders mirroring load_box/materialize_pickup"
      contains: "load_mode_arrows"
    - path: smoke/13_mode_arrows_smoke.py
      provides: "Headless REQUIRED smoke: parse fixture g98, build arrows via cgo_build.mode_arrows, load + assert object/extent; leg 2 = xtbopt.xyz load + <1e-3 A coordinate agreement vs the g98 atom block"
      contains: "SMOKE-OK"
    - path: tests/run_gates.py
      provides: "REQUIRED_SMOKES gains 'smoke/13_mode_arrows_smoke.py' with a comment citing plan 07-03"
      contains: "13_mode_arrows"
  key_links:
    - from: "serpentrum/pymol_bridge.py load_mode_arrows"
      to: "cmd.load_cgo (zoom=0)"
      via: "the load_box mirror (:91-109) — zoom=0 never reframes the completion camera; the one-shot zoom is zoom_mode_frame's job"
      pattern: "load_cgo"
    - from: "smoke/13_mode_arrows_smoke.py leg 2"
      to: "tests/fixtures/xtb/xtbopt.xyz + g98.out"
      via: "the always-on regression for the OPTIMIZED-FRAME overlay decision (07-RESEARCH-spectra-seam.md Q4b: max pairwise-distance diff 1e-6 A on this fixture; <1e-3 A asserted here)"
      pattern: "xtbopt"
---

<objective>
Add the three viewer seams the Spectra tab needs — `load_mode_arrows` (CGO arrows), `load_xtbopt` (optimized molecule as sticks), `zoom_mode_frame` (one-shot framing) — plus the REQUIRED headless smoke 13 that pins the mode-arrow CGO path and re-verifies the g98≡xtbopt coordinate identity on every gate run.

Purpose: SPECTRA-05's row-click → 3D vectors is a viewer operation; the GUI modules may NOT import pymol.cmd (GUI purity, check_purity.py:144-154), so every cmd touch goes through the BRIDGE class. The smoke is the tripwire for the single-fixture overlay assumption (research risk 2: "on mismatch ... never silently misalign").
Output: 3 small bridge functions, 1 required smoke, 1 REQUIRED_SMOKES tuple edit.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@serpentrum/pymol_bridge.py
@serpentrum/cgo_build.py
@tests/run_gates.py

Key facts (research file:line anchors):
- Bridge precedents to mirror (pymol_bridge.py): `load_molecule` :78-88 (cmd.load object= srp naming), `load_box` :91-109 (cmd.load_cgo(..., zoom=0) + return name), `materialize_pickup` :361-375 (load + transform + cmd.show('sticks', name)), `object_exists` :232-239 (PROVEN 'public_objects' type — 'all_objects' raises), `delete_object` :419-428 (guarded idempotent delete), `cleanup_srp` :129-143 (wildcard srp_* delete — the fresh-process contract, docstring :21-36: the prefix IS the .pse-reload cleanup mechanism).
- `cgo_build.mode_arrows(atoms, vecs, scale=1.0, color=(0.2,0.6,1.0), base_radius=0.06, length_a=1.2)` (cgo_build.py:134-208) is FROZEN: directions unit-normalized BEFORE scaling, every nonzero vector (mag >= 1e-6) drawn at uniform scale*1.2 Å (0.7 shaft / 0.3 cone). Relative per-atom magnitudes are NOT rendered — a magnitude-proportional variant would be a pinned-constant change in cgo_build + its tests (research Q8-7: flag, do not hand-roll a second builder). v1 pins scale=1.0.
- SRP naming is MANDATORY: `srp_mode_vec` / `srp_xtbopt` — a non-srp_ object would survive "Cleanup model" AND become an indistinguishable orphan after .pse reload (research Q4b). Delete semantics: a new table click deletes srp_mode_vec then reloads (replace, never accumulate — 07-09 wires this); Setup-Apply/begin_game materialize's cleanup_srp removes both (correct — research Q4b); a mid-flow Restart does NOT remove them (teardown deletes pickups only) — intentional keep-until-cleanup, tolerance lives in the GUI's guarded deletes (07-09).
- Overlay decision (research Q4b, probe-verified 2026-09-25): g98 Standard-orientation coords ≡ xtbopt.xyz (max per-atom |Δ| = 0.0000 Å; max pairwise-distance diff 1e-6 Å; symbol order 0 mismatches) → vectors + srp_xtbopt are exactly self-consistent; the game frame (srp_head/srp_seg_*) is wrong by up to 0.1423 Å pairwise. The OPTIMIZED frame is the shipping interpretation — owner sign-off at the 07-10 checkpoint.
- Zoom (research Q8-8): `cmd.zoom('srp_xtbopt or srp_mode_vec')` ONE call per record, never per click (pitfall 14); `zoom=0` on every load keeps the completion framing untouched. Guarded: cmd.zoom on a missing selection raises — check object_exists for both first.
- Smoke protocol (tests/run_gates.py + repo AGENTS.md): verdict = flushed `SMOKE-OK` sentinels, NEVER exit codes (the .bat swallows rc); REQUIRED_SMOKES tuple at run_gates.py:55-61 (Phase 6 claims 09-11; next free numbers 12/13 — THIS plan takes 13, the plot smoke takes 12 in plan 07-05); smoke scripts resolve ROOT defensively, never trust __file__ under -cq/exec (smoke/01 template); flush=True on every print; NO widget construction; fixtures read in place via ROOT-relative paths.
- Phase-6 shared-file note: 06-04 also edits pymol_bridge.py (chain_atom_counts) and tests/run_gates.py (smoke 10) — depends_on: ["06-04"] orders this plan's edits after Phase 6's land (Phase 6 completes before Phase 7 executes; the dep makes the file-ordering explicit).
- pymol_bridge.py is BRIDGE class: pymol/pmg_tk imports legal at any level; Qt/numpy banned; .exec_() banned. python3.6 + %-formatting.

What NOT to touch and why:
- cgo_build.mode_arrows — FROZEN Phase-2 contract (tests pin the uniform-length arithmetic); this plan only CONSUMES it.
- cleanup_srp / delete_object — already cover the new object names via the srp_ prefix + guarded delete; no edits needed.
- load_molecule — REUSED conceptually but NOT called by Phase 7 (xtbopt needs the sticks rep; load_xtbopt is the 4-line wrapper).
</context>

<tasks>

<task type="auto">
  <name>Task 1: bridge seams — load_mode_arrows, load_xtbopt, zoom_mode_frame</name>
  <files>serpentrum/pymol_bridge.py</files>
  <action>
    Append a new section to serpentrum_bridge.py after the Phase-5 section (before delete_object/rename_pickup/chain_object_names if placement reads better — keep the file's section-comment style):

    ```python
    # --- Phase 7: spectra mode vectors + optimized-frame overlay (SPECTRA-05) ----
    ```

    1. `MODE_VEC_NAME = 'srp_mode_vec'` and `XTBOPT_NAME = 'srp_xtbopt'` module constants (docstring: srp_ prefix MANDATORY — the cleanup_srp/.pse-reload contract, INFRA-04).
    2. `def load_mode_arrows(cgo, name=MODE_VEC_NAME, zoom=0)`: docstring — load a cgo_build.mode_arrows stream as a named srp_ CGO object; mirrors load_box (:91-109); `cmd.load_cgo(list(cgo), name, zoom=zoom)`; NEVER a non-srp_ name (cleanup contract); zoom=0 never reframes. Returns name.
    3. `def load_xtbopt(path, srp_name=XTBOPT_NAME, zoom=0)`: docstring — load the run's xtbopt.xyz (the OPTIMIZED frame) as sticks; mirrors materialize_pickup minus the transform (:361-375): `cmd.load(path, object=srp_name, zoom=zoom)` + `cmd.show('sticks', srp_name)`; return srp_name. Docstring states WHY sticks: view parity with the pickups/snake the player just watched (head stays spheres; the optimized snake reads as a molecule).
    4. `def zoom_mode_frame()`: docstring — ONE-shot framing of the spectra overlay objects ('srp_xtbopt or srp_mode_vec'); call ONCE per record (first vector draw), never per click (pitfall 14); returns True when a zoom was issued, False when neither object exists (guarded via object_exists — cmd.zoom on a missing selection raises a Selector error). Body: if not (object_exists(XTBOPT_NAME) or object_exists(MODE_VEC_NAME)): return False; cmd.zoom('%s or %s' % (XTBOPT_NAME, MODE_VEC_NAME)); cmd.refresh(); return True.
    5. NO Qt, NO .exec_(), NO new state. python3.6 + %-formatting.
    Run gates. Commit: `feat(07-03): bridge mode-vector seams — load_mode_arrows, load_xtbopt, zoom_mode_frame (SPECTRA-05 viewer half)`
  </action>
  <verify>python3.6 tests/run_gates.py green; grep confirms the three functions exist and MODE_VEC_NAME/XTBOPT_NAME both start with 'srp_'; no '.exec_(' anywhere in the file</verify>
  <done>The BRIDGE owns all three cmd seams; the GUI modules will never need pymol.cmd.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIRED smoke 13 — mode arrows + overlay assertion + tuple edit</name>
  <files>smoke/13_mode_arrows_smoke.py, tests/run_gates.py</files>
  <action>
    1. Create smoke/13_mode_arrows_smoke.py (house smoke template: resolve ROOT defensively — never trust __file__ under -cq/exec; sys.path.insert ROOT; flush=True on every print; NO widget construction; NO exit-code reliance):
       - Sentinel plan (flushed, one per stage): 'STAGE1 PARSE', 'STAGE2 ARROWS-LOADED', 'STAGE3 XTBOPT-IDENTITY', 'STAGE4 ZOOM', final 'SMOKE-OK MODE-ARROWS'.
       - Stage 1: `from serpentrum import spectra, cgo_build, pymol_bridge as pb`; spectrum = spectra.parse_g98(os.path.join(ROOT, 'tests', 'fixtures', 'xtb', 'g98.out')); print counts (26 atoms / 72 modes).
       - Stage 2: atoms_xyz = [(a.x, a.y, a.z) for a in spectrum.atoms]; vecs = spectrum.modes[0].vectors (mode 1 = the -31.9i mode); cgo = cgo_build.mode_arrows(atoms_xyz, vecs, scale=1.0); pb.delete_object(pb.MODE_VEC_NAME); pb.load_mode_arrows(cgo); assert pb.object_exists(pb.MODE_VEC_NAME); ext = cmd.get_extent(pb.MODE_VEC_NAME) (nested [[min],[max]] — 03-06 proven shape); assert finite floats and extent span > 0 on some axis; print sentinel.
       - Stage 3 (THE overlay regression): xtbopt_path = os.path.join(ROOT, 'tests', 'fixtures', 'xtb', 'xtbopt.xyz'); pb.delete_object(pb.XTBOPT_NAME); pb.load_xtbopt(xtbopt_path); assert pb.object_exists(pb.XTBOPT_NAME); model = cmd.get_model(pb.XTBOPT_NAME); assert len(model.atom) == 26; per-atom max |loaded - g98| < 1e-3 A (compare model.atom[i].x/y/z against spectrum.atoms[i] in order — the probe measured 0.0000; 1e-3 leaves drift headroom while still killing a real desync); print the measured max as the sentinel payload.
       - Stage 4: assert pb.zoom_mode_frame() is True; print sentinel. Cleanup: pb.delete_object(pb.MODE_VEC_NAME); pb.delete_object(pb.XTBOPT_NAME); final 'SMOKE-OK MODE-ARROWS'.
       - Failure discipline: any assertion failure prints 'SMOKE-FAIL ...' + flush (run_gates keys on the sentinels, not rc).
    2. tests/run_gates.py: append 'smoke/13_mode_arrows_smoke.py' to REQUIRED_SMOKES (run_gates.py:55-61) with the comment: `# 13 = mode arrows + g98/xtbopt overlay identity (Phase 7, plan 07-03) — re-asserts the OPTIMIZED-frame assumption on every gate run`.
    3. Run: `python3.6 tests/run_gates.py` (syntax/purity/unittest legs — the smoke file itself is only compiled here), then the smoke leg: `python3.6 tests/run_gates.py --smoke` — verdict = flushed SMOKE-OK sentinels (NEVER exit codes). Note: Phase-6 smokes 09-11 are not on disk yet — smoke 13's REQUIRED entry takes effect once Phase 6 merges; until then run the single smoke directly if needed: `timeout 90 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\13_mode_arrows_smoke.py"` from repo root and read the sentinels.
    Commit: `test(07-03): REQUIRED smoke 13 — mode-arrow CGO load + g98≡xtbopt overlay assertion`
  </action>
  <verify>python3.6 tests/run_gates.py green; the direct smoke run flushes STAGE1..STAGE4 + 'SMOKE-OK MODE-ARROWS' (never SMOKE-FAIL); grep run_gates.py contains '13_mode_arrows'</verify>
  <done>Mode-arrow CGO and the optimized-frame identity are regression-pinned on every gate run; the smoke joins the required set.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `timeout 90 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\13_mode_arrows_smoke.py"` from repo root flushes 'SMOKE-OK MODE-ARROWS' with the measured max per-atom delta < 1e-3.
- `grep -n "load_mode_arrows\|load_xtbopt\|zoom_mode_frame" serpentrum/pymol_bridge.py` — all three present, all names srp_-prefixed.
</verification>

<success_criteria>
- All viewer access for the spectra overlay flows through the BRIDGE with srp_-prefixed names (cleanup + .pse contracts hold); zoom is one-shot and guarded.
- The overlay assumption (g98 ≡ xtbopt) is an always-on regression, protecting SPECTRA-05 against xtb-version drift; a failure is loud, never a silent misalignment.
- cgo_build.mode_arrows remains byte-untouched (frozen Phase-2 contract).
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-03-SUMMARY.md`
</output>
