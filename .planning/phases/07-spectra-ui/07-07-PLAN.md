---
phase: 07-spectra-ui
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-04", "07-05", "06-09"]
files_modified:
  - serpentrum/gui_spectra.py
  - serpentrum/gui.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Page 2 of the dialog is a LIVE SpectraTab (the 06-09 placeholder page is gone): Get Spectra lands on a real status label, a streaming log panel, and a contextual run/cancel control — SPECTRA-01's entire remaining scope, and nothing else changes (model-A signal signature, launch actor = GameTab's button, Get Spectra lifecycle all untouched)"
    - "Runner progress streams LIVE into the tab's log panel — QPlainTextEdit read-only with setMaximumBlockCount(500) fed by controller.log_line via plain .connect (same-thread Qt); a late/reloading tab replays controller.log_tail() BEFORE connecting so early lines are never lost"
    - "Every 06-09 pinned placeholder behavior survives the replacement: Get Spectra re-enables on ANY terminal branch (getattr-guarded), the tab button flips contextually ('Cancel xtb run' while running / 'Run again' when terminal, disabled 'no xtb run yet' initially), Run-again re-enters the FULL launch pipeline (re-reads last_run, re-checks counts, re-runs guard, re-resolves binary, disarms), refuse/corrupt/binary lines stay clear, game_tab.log_external keeps receiving launch lines"
    - "The tab never reaches UP to the dialog or QTabWidget: cancel is called directly on the anchor-owned controller; Run-again emits run_again_requested which the dialog connects to its own launch slot (model-A discipline, locked decision 9)"
  artifacts:
    - path: serpentrum/gui_spectra.py
      provides: "SpectraTab(anchor_state, parent): status QLabel (wordwrap), log QPlainTextEdit (read-only, 500 blocks), run control button, replay_log/append_log_line/on_runner_started/on_run_finished slots, run_again_requested Signal, reflect_run_state"
      min_lines: 150
    - path: serpentrum/gui.py
      provides: "page 2 = SpectraTab(anchor_state, self.tabs); _build_spectra_placeholder and its widgets REMOVED; _on_spectra_requested pipeline preserved with status/log routed to tab methods; _connect_runner delegates to the tab + keeps the Get-Spectra re-enable"
      contains: "SpectraTab"
  key_links:
    - from: "serpentrum/gui.py _connect_runner"
      to: "controller.log_line / controller.started / controller.run_finished"
      via: "dialog-scoped connect-once flag (06-09 pin) — log_line/started go straight to tab methods; run_finished lands on a dialog slot that re-enables game_tab.get_spectra_btn THEN calls tab.on_run_finished (the dialog owns cross-tab orchestration; the tab never reaches up)"
      pattern: "log_line.connect"
    - from: "serpentrum/gui_spectra.py run_again_requested"
      to: "serpentrum/gui.py _on_spectra_requested"
      via: "the model-A return path: the tab emits, the dialog re-enters the FULL pipeline (06-09 Task-1 terminal-branch semantics preserved verbatim — never _launch_spectra_run with stale values)"
      pattern: "run_again_requested"
---

<objective>
Replace the 06-09 placeholder Spectra page with a live `SpectraTab` (gui_spectra.py, new GUI module) and rewire PluginDialog around it — delivering SPECTRA-01 (the tab the Get Spectra switch lands on is real) and SPECTRA-04 (calculation progress streams live into a log panel) with every pinned 06-09 behavior preserved.

Purpose: the payoff screen's control surface. The runner (Phase 6) already streams log_line/run_finished; the placeholder already proves the UX shape — this plan swaps the placeholder widgets for the real tab and moves the runner-slot logic where it belongs, WITHOUT touching the launch pipeline's decision logic.
Output: one new GUI module + one focused gui.py edit. Table, plot, and vectors arrive in 07-08/07-09 (the tab's layout leaves room; nothing throwaway is built here).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@.planning/phases/06-xtb-pipeline/06-09-PLAN.md
@.planning/phases/06-xtb-pipeline/06-05-PLAN.md
@serpentrum/gui.py
@serpentrum/xtb_runner.py
@serpentrum/gui_game.py

Key facts (research file:line anchors):
- SPECTRA-04 recommendation (research Q3): QPlainTextEdit, setReadOnly(True), setMaximumBlockCount(500) — bounded capacity matching the controller's 500-line tail so memory stays flat during streaming ("Phase 7 replaces the page CONTENT" explicitly permits swapping the 06-09 QTextBrowser ~200-line placeholder; [TRAIN] MEDIUM on the perf rationale — human-verified streaming at 07-10). Feed via plain .connect — all Qt work lands on the main thread (research Q2 ambiguity 6; cmd.* legal in slots; NEVER SIGNAL() strings — house rule).
- Early-line buffering (research Q3): the tab is constructed with the dialog (all pages at init) so the FIRST launch is connected before start() returns and loses nothing; the loss window is RELOAD — cover it by replaying controller.log_tail() (added by 07-04) then connecting log_line.
- 06-09 pinned behaviors that MUST survive (research Q8-4; 06-09 must-have 3 "Phase 7 replaces the page CONTENT, the signal contract survives"):
  1. `_on_spectra_requested` keeps setCurrentIndex(2) FIRST and the whole pipeline (anchor guard, last_run/snake_xyz refuse lines, xyzio.XyzError corrupt-input line, counts via build_run_input-parsed snake_xyz + pymol_bridge.chain_atom_counts, budget_guard.launch_counts_line/launch_budget_warnings verbatim, xtbenv.detect_binary + refuse line, Get Spectra disarm, _launch_spectra_run).
  2. `_on_run_finished` re-enables game_tab.get_spectra_btn (getattr-guarded) on every terminal branch.
  3. The run-control button flips contextually: 'Cancel xtb run' (running) / 'Run again' (terminal); Run-again re-enters the FULL pipeline — NEVER _launch_spectra_run(record, exe) with stale values (06-09 Task 1).
  4. `_connect_runner` guard flag stays DIALOG-scoped (reload creates a new dialog which reuses the anchored controller and reconnects fresh — 06-09:62).
  5. Launch lines keep feeding game_tab.log_external (06-06's channel) in addition to the tab surfaces.
- Slot migration (research Q2 ambiguity 3, recommended shape): the runner slots move INTO gui_spectra.py; PluginDialog delegates. The dialog RETAINS: the connect-once guard, the game_tab re-enable + log_external feed (cross-tab orchestration the tab must not reach up for — model-A), and the launch pipeline. The tab OWNS: status label, log panel, run button + its label logic, started/log/finished display updates.
- Controller seam (06-05 + 07-04): anchor.spectra_runner (declared field, 07-04) create-or-reuse; controller.status() read-only; controller.cancel() emits its own log line ('cancel requested - killing xtb'); log_tail() returns the copy to replay. The tab NEVER launches a run itself (research Q3: the launch actor is GameTab's button -> the dialog's pipeline, ALWAYS).
- spectra_ui.run_status_lines (07-01): verdict lines from the frozen vocabulary — the tab's on_run_finished displays exactly these (replacing the placeholder's inline 'xtb finished: %s' join; vocabulary + never-re-grep rule preserved).
- Widget precedents: construction pattern SetupTab(anchor_state, self.tabs) / GameTab(anchor_state, self.tabs) (gui.py:57-61); _log precedent gui_game.py:1392-1402 (info_box.append); model-A signal template gui_game.py spectra_requested (class-level QtCore.Signal, emit-only, connected by the dialog).
- Purity: gui_spectra.py is GUI — pymol.Qt imports ONLY (its inert GUI_MODULES entry landed with 07-05); relative imports of pure modules + pymol_bridge legal (check_purity.py:95-98; the 06-09 precedent); NO pymol.cmd, NO .exec_(), python3.6 + %-formatting. gui.py is GUI (already registered).
- gui.py single-writer: this is the ONLY Phase-7 plan that edits gui.py.
- Layout contract for the later plans (pin NOW so 07-08/07-09 insert at known indices): this plan builds the tab's QVBoxLayout as [0]=status label, [1]=log (stretch 2), [2]=button row (right-aligned). 07-08 then builds the plot panel into __init__ between log and buttons (status / log / PANEL / buttons). 07-09 finally inserts the table at index 2 (stretch 2), yielding the final order status[0] / log[1] / table[2] / plot[3] / buttons[4]. Build in this exact sequence so each plan's insert index is valid when it runs.

What NOT to touch and why:
- `_on_spectra_requested`'s pipeline decisions (counts/guard/binary/disarm) — frozen 06-09/06-01/06-02 contracts; only the line DESTINATIONS move from self.spectra_status/spectra_log to tab methods.
- GameTab internals (get_spectra_btn lifecycle, log_external, spectra_requested signal) — GAME-09/06-06 contracts; gui_game.py is 5.2-shared and NOT in this plan's files.
- The bottom reserved button row (Phase 8).
- spectra_run record shape — 06-02 frozen; the tab reads it via getattr(anchor, 'spectra_run', None) only for initial reflection.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpectraTab — status, streaming log, run control, slots</name>
  <files>serpentrum/gui_spectra.py</files>
  <action>
    Create serpentrum/gui_spectra.py (GUI class: `from pymol.Qt import QtWidgets, QtCore` ONLY; `from . import spectra_ui`; stdlib fine; %-formatting; python3.6; NO pymol.cmd, NO .exec_(), NO module-level state; module docstring: the Spectra tab — status/log/run-control surfaces wired by the dialog; table/plot/vectors arrive in 07-08/07-09 at the PINNED layout indices).

    Class SpectraTab(QtWidgets.QWidget):
    - Class-level signal: `run_again_requested = QtCore.Signal()` (emit-only; the dialog connects it to the launch slot — model-A).
    - __init__(self, anchor_state=None, parent=None): super(); self._anchor = anchor_state; QVBoxLayout. Build IN THIS ORDER (pinned indices for 07-08/07-09):
      [0] self.status_label = QtWidgets.QLabel(wordwrap True) — initial text: 'Spectra tab - complete a game, then press Get Spectra on the Game tab. Progress streams here.'
      [1] self.log_panel = QtWidgets.QPlainTextEdit(); setReadOnly(True); setMaximumBlockCount(500) (research Q3 — matches the controller's tail; appendPlainText of a 500-block-bounded widget self-trims).
      [2] Button row (QHBoxLayout, stretch first, then button): self.run_btn = QtWidgets.QPushButton('no xtb run yet'); setEnabled(False); tooltip 'no xtb run yet'; clicked -> self._on_run_button.
      Layout docstring records the insert contract: 07-08 inserts the plot panel at index 2, 07-09 inserts the table at index 2 (each later insert shifts the buttons to 4).
    - `append_log_line(self, text)`: self.log_panel.appendPlainText(str(text)) (the streaming sink — connected by the dialog to controller.log_line).
    - `set_status_line(self, text)`: self.status_label.setText(str(text)).
    - `replay_log(self, lines)`: for line in lines: append_log_line(line) (called by the dialog BEFORE connecting log_line — the reload early-line recovery, 07-04's accessor).
    - `on_runner_started(self)`: set_status_line('xtb running... (async - the dialog stays responsive)') (06-09 pinned started text); run_btn label 'Cancel xtb run' + enabled.
    - `on_run_finished(self, status, problems)`: record = getattr(self._anchor, 'spectra_run', None) or {'status': status, 'problems': problems}; for line in spectra_ui.run_status_lines(record): set_status_line for the first, append_log_line for each (verdict visible in BOTH surfaces); run_btn label 'Run again' + enabled. (07-08/07-09 extend this with plot/table population via the same record.)
    - `reflect_run_state(self)`: initial state for construction/reload — runner = getattr(self._anchor, 'spectra_runner', None); if runner is not None and runner.status() == 'running': on_runner_started(); else: record = getattr(self._anchor, 'spectra_run', None); if record and record.get('status') in ('ok', 'failed', 'cancelled'): on_run_finished(record.get('status'), record.get('problems') or []); else: leave the idle hint, button disabled. Called by the dialog AFTER _connect_runner (a terminal record that predates the dialog renders immediately).
    - `_on_run_button(self)`: runner = getattr(self._anchor, 'spectra_runner', None); if runner is not None and runner.status() == 'running': runner.cancel() (direct — the controller is anchor-owned; cancel emits its own log line; the dialog's log_line connection delivers it); else: self.run_again_requested.emit() (the dialog re-enters the FULL pipeline — 06-09 Run-again semantics; the tab NEVER launches).
    - `run_btn_label_for(state)` static-ish helper only if it reduces duplication — otherwise inline. Keep the class under ~180 lines.
    - NO exec_, NO parent reaching (self.parent() is NEVER called — the dialog pushes state in via methods/signals).
    Run gates. Commit: `feat(07-07): SpectraTab — status, streaming log, contextual run control (SPECTRA-01/04 surfaces)`
  </action>
  <verify>python3.6 tests/run_gates.py green (gui_spectra classified GUI via the 07-05 inert entry — now live); AST assertions (python3.6 heredoc): ast.parse; assert class SpectraTab defines run_again_requested (a Signal assignment), append_log_line, replay_log, on_runner_started, on_run_finished, _on_run_button; assert setMaximumBlockCount(500) and setReadOnly(True) appear in the source; grep no '.exec_(' and no 'self.parent()'</verify>
  <done>The tab owns its three surfaces + slots; Run-again is a signal; the dialog does the orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: gui.py — placeholder page replaced, delegation rewired, 06-09 behaviors audited</name>
  <files>serpentrum/gui.py</files>
  <action>
    Edit serpentrum/gui.py (the ONLY Phase-7 gui.py edit):
    1. Imports: add `from .gui_spectra import SpectraTab`.
    2. Page 2 construction: replace the `_build_spectra_placeholder(parent)` call (06-09's builder) with `self.spectra_tab = SpectraTab(anchor_state, self.tabs)` + `self.tabs.addTab(self.spectra_tab, 'Spectra')`. REMOVE _build_spectra_placeholder, self.spectra_status, self.spectra_log, self.spectra_run_btn, _on_spectra_run_button, and the 06-09 _on_runner_started/_on_runner_log_line placeholder slots — their logic migrates: status/log display -> tab methods; run-button logic -> tab._on_run_button + run_again_requested; started text -> tab.on_runner_started; verdict text -> tab.on_run_finished via spectra_ui.run_status_lines. Keep _TAB_DEFS only if other placeholder entries remain (today none — remove the loop if it is now dead, leaving the class docstring's page contract updated: page 2 = live SpectraTab, table/plot arrive in 07-08/07-09).
    3. `_connect_runner(self, controller)` (REWRITE, dialog-scoped guard flag `self._runner_connected` preserved — 06-09:62): (a) self._runner_connected guard; (b) self.spectra_tab.replay_log(controller.log_tail()) — replay BEFORE connecting (07-04's accessor; reload early-line recovery); (c) controller.log_line.connect(self.spectra_tab.append_log_line); (d) controller.started.connect(self.spectra_tab.on_runner_started); (e) controller.run_finished.connect(self._on_spectra_run_finished).
    4. `_on_spectra_run_finished(self, status, problems)` (NEW dialog slot — the cross-tab orchestration piece): re-enable game_tab.get_spectra_btn guarded (`game_tab = getattr(self, 'game_tab', None); if game_tab is not None: game_tab.get_spectra_btn.setEnabled(True)` — 06-09 pinned terminal re-enable); then self.spectra_tab.on_run_finished(status, problems).
    5. `_on_spectra_requested(self)` — PRESERVE the 06-09 pipeline byte-for-byte in DECISIONS, reroute DESTINATIONS: every `self.spectra_status`-family write becomes `self.spectra_tab.set_status_line(...)`; every log append becomes `self.spectra_tab.append_log_line(...)`; the game_tab.log_external(line) calls STAY (EQ-ux-2: the game info box keeps receiving counts/warnings/verdict refusals); setCurrentIndex(2) stays FIRST; the binary-refuse / no-snake-refuse / corrupt-input lines stay verbatim; the Get Spectra disarm stays; `_launch_spectra_run(record, exe)` stays (controller create-or-reuse on anchor.spectra_runner + self._connect_runner(controller) + start + re-enable on not-started). Note: _connect_runner's replay makes RE-launches (Run again) safe on a reused controller — the replay happens before reconnect, and start() resets the controller's tail (06-05 behavior).
    6. In __init__ (after the spectra_tab construction and the existing spectra_requested connect): `self.spectra_tab.run_again_requested.connect(self._on_spectra_requested)` (the FULL-pipeline re-entry — identical to pressing Get Spectra, which is 06-09's pinned Run-again semantics); and after building the tab: `runner = getattr(anchor_state, 'spectra_runner', None) if anchor_state is not None else None; if runner is not None: self._connect_runner(runner); self.spectra_tab.reflect_run_state()` (reload onto a live/terminal controller renders immediately).
    7. Class docstring: update the page-2 contract (live SpectraTab; Phase 7 plans 07-08/07-09 add plot + table at pinned indices; modeless + model-A unchanged).
    Run gates + AST audit. Commit: `feat(07-07): gui.py page 2 = live SpectraTab — delegation rewired, 06-09 behaviors preserved (SPECTRA-01/04)`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST/grep audit on gui.py: no '_build_spectra_placeholder', no 'spectra_status', no 'spectra_run_btn', no '_on_spectra_run_button' remnants; exactly one 'spectra_requested.connect'; exactly one "setCurrentIndex(2)" and it is the FIRST statement inside _on_spectra_requested; '_on_spectra_run_finished' defined and connected; 'run_again_requested.connect' present; grep -c 'exec_' == 0; the reserved bottom row untouched (addStretch(1) line still present)</verify>
  <done>Get Spectra lands on a live tab; streaming + verdicts + contextual control all work through the dialog-owned connections; every 06-09 pinned behavior survives.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (both GUI modules classified; py3.6 clean).
- AST audit (python3.6 heredoc): gui_spectra.SpectraTab defines the signal + five methods; gui.py has zero placeholder remnants and exactly one spectra_requested.connect + one run_again_requested.connect.
- Live behavior (launch/stream/cancel/relaunch in the real GUI) is the 07-10 consolidated checkpoint's job — this plan's bar is structural green + AST-verified wiring + preserved-behavior audit.
- `python3.6 tests/run_gates.py --smoke` after the gui.py/gui_spectra.py edits — verdict = flushed SMOKE-OK sentinels (house rule when GUI modules change; if Phase-6 smokes are not yet on disk, run smoke 01 directly and read sentinels).
</verification>

<success_criteria>
- SPECTRA-01's remaining scope is closed: the Get Spectra switch lands on a REAL tab (status + streaming log + run control).
- SPECTRA-04's user path exists: controller log lines stream into the tab live, with the reload early-line hole closed by log_tail replay.
- The placeholder swap is behavior-preserving: re-enable, contextual button, full-pipeline Run-again, refuse lines, log_external feed, dialog-scoped connect-once — all present and AST-auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-07-SUMMARY.md`
</output>
