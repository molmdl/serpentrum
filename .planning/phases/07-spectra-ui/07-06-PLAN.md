---
phase: 07-spectra-ui
plan: 06
type: execute
wave: 3
depends_on: ["07-05"]
files_modified:
  - smoke/manual_plot_check.py
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The plot widget's on-screen look, size-preset reflow, axis-label toggle, and the save-PNG-opens-outside-PyMOL path are HUMAN-VERIFIED in the real Windows PyMOL GUI before the tab integration piles on (fail cheap — the 04-07 house precedent)"
    - "The ascending-wavenumber-axis v1 convention gets its first owner look here (a flip is still cheap); final sign-off lands at the 07-10 closing checkpoint"
  artifacts:
    - path: smoke/manual_plot_check.py
      provides: "Real-GUI manual harness (non-NN_ name — never auto-run by gates): hosts SpectraPlotPanel in a modeless top-level widget with the fixture scene; prints the numbered human checklist"
      contains: "manual_plot_check"
  key_links:
    - from: "smoke/manual_plot_check.py"
      to: "serpentrum/gui_plot.py SpectraPlotPanel"
      via: "run manually via `run smoke\\manual_plot_check.py` in a real GUI PyMOL command line (the manual_wizard_keys_check.py convention) — widget construction is legal in a LIVE GUI, unlike the headless 01-05 dead end"
      pattern: "manual_plot_check"
---

<objective>
Stage the early, cheap human-verify of the plot render (SPECTRA-03's on-screen + saved halves) via a real-GUI manual harness, then RUN the checkpoint — before the Spectra tab, table, and vectors are built on top of it.

Purpose: the save route and renderer are headless-proven (smoke 12), but "looks right" is a human verdict (axis labels legible, curve smooth, combo reflow sane, PNG opens in a real viewer). Failing that NOW costs one plan of rework; failing it after the tab integration costs four (04-07's lesson).
Output: one manual harness + a blocking checkpoint verdict.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-qt-plot.md
@serpentrum/gui_plot.py
@serpentrum/plot_logic.py
@smoke/manual_wizard_keys_check.py

Key facts:
- Harness convention (smoke/manual_wizard_keys_check.py:1-31): non-NN_ filename keeps tests/run_gates.py --smoke (glob smoke/[0-9][0-9]_*.py) from EVER auto-running it; run MANUALLY inside a real Windows PyMOL GUI from the command line (`run smoke\\manual_plot_check.py`); resolve ROOT defensively (never trust __file__ under run/exec); flush=True prints. NOTE the deliberate difference from that template: THIS harness constructs widgets — legal here because the real GUI has a live QApplication and event loop (the 01-05 dead end was HEADLESS dialog construction only). Modeless discipline still applies: .show() only, NEVER .exec_().
- The harness stages the SAME scene the headless smoke proves: spectra.parse_g98 on tests/fixtures/xtb/g98.out + plot_logic.build_scene (72 modes, 3 imaginary, peak 1150.6639 @ 257.1018 km/mol).
- The human checklist discharges the plot-half [TRAIN] items: (2) ASCII labels legible ('wavenumber (cm-1)', 'IR intensity (km/mol)', '-31.9i' style minus signs in captions); (3) minimum-height readability with the real GUI font; (4) ascending-axis convention sign-off (first look; final at 07-10); (5) tab/paint responsiveness sanity; plus the save-PNG-opened-outside-PyMOL half that smoke 12 cannot prove (bytes prove reload, not "looks right in a viewer").
- Failure-cheap contract: if the human rejects the render, fixes land in gui_plot.py/plot_logic.py and THIS checkpoint re-runs — the tab plans (07-07+) have not started embedding yet.
- GUI visual verdicts are ALWAYS human-verify (01-05 decision: offscreen route closed). The checkpoint gate is blocking.
</context>

<tasks>

<task type="auto">
  <name>Task 1: real-GUI manual harness for the plot panel</name>
  <files>smoke/manual_plot_check.py</files>
  <action>
    Create smoke/manual_plot_check.py (follow the manual_wizard_keys_check.py template: defensive _resolve_root() via serpentrum/__init__.py existence; sys.path insert; flush=True prints; NO .exec_() anywhere):
    1. Imports: from pymol.Qt import QtWidgets; from serpentrum import spectra, plot_logic, gui_plot.
    2. Stage: spectrum = spectra.parse_g98(os.path.join(ROOT, 'tests', 'fixtures', 'xtb', 'g98.out')); scene = plot_logic.build_scene(spectrum.modes).
    3. Build a top-level QWidget window (title 'serpentrum plot check - close PyMOL when done'), a QVBoxLayout hosting ONE gui_plot.SpectraPlotPanel(window, status_cb=print), set the scene, window.resize(700, 560), window.show() (MODELESS — never exec_). Keep a module-level reference alive (the window must not be garbage-collected).
    4. status_cb=print routes the panel's save reports to the PyMOL console so the human sees 'plot saved: <path>'.
    5. Print the numbered checklist (flush=True) exactly as the checkpoint's how-to-verify steps below, then print 'PLOT-CHECK-STAGED'.
    Gates: `python3.6 tests/run_gates.py` (the non-NN_ name is compiled by gate 1 but never auto-run — confirm the gate summary shows it absent from the smoke list).
    Commit: `feat(07-06): real-GUI plot-check harness (manual, non-NN_ — 04-07 convention)`
  </action>
  <verify>python3.6 tests/run_gates.py green; grep confirms the harness contains '.show()' and contains no '.exec_('; filename does NOT match smoke/[0-9][0-9]_*.py (gates never auto-run it)</verify>
  <done>The harness stages the exact panel the tab will embed, with the save reports visible in the PyMOL console.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — early plot render human-verify (fail cheap)</name>
  <what-built>The SPECTRA-03 plot widget: IrPlotWidget + SpectraPlotPanel (size combo + axis-label toggle + Save Plot PNG) rendering the committed 72-mode dimer fixture through the paint_scene seam, plus the headless proof (smoke 12 SMOKE-OK: valid PNG, 2x pixels, empty-scene hint path).</what-built>
  <how-to-verify>
    1. Run the gates with the new required smoke: `python3.6 tests/run_gates.py --smoke` from the repo root in WSL — confirm note lines show 'smoke/12_plot_smoke.py: PASS' with a SMOKE-OK sentinel (verdict = sentinels, never exit codes).
    2. In a REAL Windows PyMOL GUI (not -cq), type: run smoke\manual_plot_check.py
    3. On the staged panel, verify in order:
       a. The broadened curve renders smoothly (antialiased), axes + tick labels legible, y label 'IR intensity (km/mol)' readable (rotated), x label 'wavenumber (cm-1)' below.
       b. The x axis runs ASCENDING 0 -> 3600 (v1 convention — say so in your verdict; a descending 4000->400 flip is still cheap now).
       c. Switch the size combo (medium/large/wide) — the widget reflows, labels never smear or clip into margins.
       d. Uncheck/recheck 'Show axis labels' — titles + tick labels hide/show; the curve stays.
       e. Click 'Save Plot (PNG)', choose a Windows path (e.g. Desktop), confirm the console prints 'plot saved: ...'.
       f. Open the saved PNG OUTSIDE PyMOL (Explorer/Paint) — it matches the on-screen plot (labels, curve, white background) and is a real PNG.
       g. Resize the PyMOL window/panel — no crashes, no label garbage at small sizes.
    4. Report any visual defect precisely (which step, what looks wrong).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the ascending-axis call), or describe the defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` + `--smoke` green (smoke 12 in the required set, SMOKE-OK flushed).
- Human verdict recorded in the checkpoint (approved / defect list); fixes (if any) land in gui_plot.py/plot_logic.py and this checkpoint re-runs before 07-08 embeds the panel.
</verification>

<success_criteria>
- The plot's on-screen look, adjustment surface, and save-PNG-outside-PyMOL path are human-approved BEFORE the Spectra tab integration begins — the cheap-failure point is spent, not banked.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-06-SUMMARY.md`
</output>
