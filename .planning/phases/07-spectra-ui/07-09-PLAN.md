---
phase: 07-spectra-ui
plan: 09
type: execute
wave: 5
depends_on: ["07-03", "07-08"]
files_modified:
  - serpentrum/gui_spectra.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The frequency table lists EVERY mode of the parsed spectrum in order — mode #, frequency via the SHARED freq_label (negatives as imaginary, e.g. -31.9i, ASCII minus), IR intensity via '%.4g' (zero-intensity rows included and visibly distinct from tiny-but-nonzero) [SPECTRA-05]"
    - "Clicking a row draws that mode's static displacement vectors: table row r ⇔ spectrum.modes[r] (single-source parse from 07-08 — no index desync possible), arrow CGO via the FROZEN cgo_build.mode_arrows at scale=1.0, loaded as srp_mode_vec; a new click REPLACES the previous arrows (delete-then-load, never accumulates)"
    - "Vectors draw on the OPTIMIZED frame: srp_xtbopt loaded from record['xtbopt_path'] once per record (snake_id-guarded, delete-then-reload on change) — g98 coords ≡ xtbopt within 1e-6 Å (probe); the game frame would be wrong by up to 0.14 Å; the camera zoom (zoom_mode_frame) fires ONCE per record, never per click"
    - "A vibspectrum-only parse renders the table but REFUSES vectors with a clear status line; failed/cancelled runs leave the table empty with the verdict + problems already shown; a vanished srp_ object (cleanup/restart) is tolerated (all deletes/zooms guarded)"
  artifacts:
    - path: serpentrum/gui_spectra.py
      provides: "freq QTableWidget at the pinned layout index 2 (3 read-only columns, SelectRows); _populate_table from spectra_ui.table_rows over self._spectrum; _on_table_cell_clicked → mode_arrow_primitives → cgo_build.mode_arrows → bridge load/zoom; _xtbopt_snake_id/_zoomed_snake_id once-per-record guards"
      contains: "cellClicked"
  key_links:
    - from: "gui_spectra._on_table_cell_clicked"
      to: "serpentrum/spectra_ui.py mode_arrow_primitives → serpentrum/cgo_build.py mode_arrows → serpentrum/pymol_bridge.py load_mode_arrows/load_xtbopt/zoom_mode_frame"
      via: "the pure selector feeds the FROZEN builder through the BRIDGE seams (07-03) — the GUI never imports pymol.cmd"
      pattern: "mode_arrow_primitives"
    - from: "gui_spectra table rows"
      to: "serpentrum/spectra_ui.py freq_label / table_rows"
      via: "the single shared imaginary formatter — table rows and any future plot caption render identically"
      pattern: "freq_label"
---

<objective>
Add the frequency table and the row-click → 3D mode-vector flow to the live SpectraTab: every mode listed (negatives as -31.9i, zero-intensity included), a click drawing that mode's static displacement arrows on the OPTIMIZED frame (srp_xtbopt) — completing SPECTRA-05.

Purpose: the interactive payoff. The pure selectors (07-01), bridge seams (07-03), and the parsed spectrum stored on the tab (07-08) all exist; this plan is the wiring plus the once-per-record overlay semantics.
Output: one focused gui_spectra.py edit (table widget + click handler + record guards).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@serpentrum/gui_spectra.py
@serpentrum/spectra_ui.py
@serpentrum/cgo_build.py
@serpentrum/pymol_bridge.py

Key facts (research file:line anchors):
- Table spec (research Q4a): QTableWidget, 3 columns ('mode', 'frequency (cm-1)', 'IR intensity (km/mol)'); read-only items (Qt.ItemIsEnabled | Qt.ItemIsSelectable flags — never editable); selectionBehavior SelectRows; row click via cellClicked(int row, int column); populate from the parsed g98 Spectrum in mode order — `spectrum.modes[r] ⇔ table row r` (self._spectrum stored by 07-08; Mode.index is display-only). real_modes() need NOT be applied to g98 (documented no-op there, spectra.py:415-416); the vibspectrum fallback was ALREADY filtered in 07-08's populate.
- Fixture counts (probe): dimer table = 72 rows, 3 negative (rows 0-2: -31.9i / -23.1i / -18.1i), zero-intensity rows ABSENT from this g98 but REQUIRED to render when present (synthetic + co2_vibspectrum coverage lives in 07-01's tests — the table renders whatever table_rows returns, no special-casing anywhere).
- Vectors (research Q4b, probe-verified): draw at spectrum.atoms (g98 Standard-orientation) coordinates loaded as srp_xtbopt — g98 ≡ xtbopt within 1e-6 Å (max pairwise-distance diff); the GAME frame (srp_head/srp_seg_*) is wrong by up to 0.1423 Å AND breaks when the snake was cleaned — the OPTIMIZED frame is the shipping interpretation, OWNER SIGN-OFF at 07-10. Atom order = run-input order = xtbopt order (no reordering seam). v1 pins scale=1.0 on the FROZEN cgo_build.mode_arrows (uniform-length arrows — magnitude-proportional is a pinned-constant change, out of scope, research Q8-7).
- Object lifecycle (research Q4b + 07-03): srp_mode_vec / srp_xtbopt (srp_ prefix MANDATORY — cleanup_srp wildcard + .pse contract). New click → delete_object('srp_mode_vec') → load_mode_arrows (replace, never accumulate). srp_xtbopt loads once per record: guard `not pymol_bridge.object_exists('srp_xtbopt') or self._xtbopt_snake_id != record['snake_id']` → delete_object + load_xtbopt + update guard. zoom_mode_frame() ONCE per record (self._zoomed_snake_id guard) — pitfall 14 (never per click). Death semantics: Setup-Apply/begin_game/rebuild cleanup_srp sweeps both (correct); mid-flow Restart does NOT (teardown deletes pickups only — intentional keep-until-cleanup); the tab tolerates vanished objects via guarded deletes (07-03's object_exists) and its own getattr guards.
- Missing-file tolerance (research Q8-10): record paths point into %TEMP%/srp_spectra/<snake_id>/ — if xtbopt_path is None (failed/cancelled) or the file is gone (temp wiped), REFUSE the vector draw with a clear line ('the optimized structure file is unavailable - cannot draw mode vectors') — never crash, never draw arrows on the stale game frame.
- vibspectrum-only (07-08 sets self._spectrum from the filtered fallback): mode_arrow_primitives returns None → status line 'this spectrum has no displacement vectors (vibspectrum fallback) - vectors need the g98 output'.
- Bridge seams (07-03): pymol_bridge.load_mode_arrows / load_xtbopt / zoom_mode_frame / delete_object / object_exists — ALL the cmd access; the tab imports pymol_bridge (GUI→BRIDGE import legal, check_purity.py:95-98; the 06-09 pymol_bridge precedent in gui.py).
- Pure seams (07-01): spectra_ui.table_rows(spectrum) → [(index, freq_label, intensity_label)]; spectra_ui.mode_arrow_primitives(spectrum, mode_index_1based) → (atoms, vecs) | None. Row r → mode_index = r + 1.
- Layout contract (pinned 07-07/07-08): insert the table at QVBoxLayout index 2 — final order status[0] / log[1] / TABLE[2] / plot_panel[3] / buttons[4]; table stretch 2, panel stretch 3. Update the layout docstring to the final order.
- Purity: GUI module — pymol.Qt only + relative imports (spectra_ui/cgo_build/pymol_bridge legal); NO pymol.cmd, NO .exec_(); python3.6 + %-formatting.

What NOT to touch and why:
- cgo_build.mode_arrows — FROZEN (uniform-length contract; smoke 13 + Phase-2 tests pin it).
- pymol_bridge — 07-03 delivered every seam needed; if a seam seems missing, re-read 07-03 before adding anything.
- plot panel / log / status surfaces — 07-05/07-07/07-08 verified; the table is additive at its pinned index.
- gui.py — single-writer was 07-07; no dialog change needed (the tab owns the table).
</context>

<tasks>

<task type="auto">
  <name>Task 1: frequency table — widget, populate, pinned layout insert</name>
  <files>serpentrum/gui_spectra.py</files>
  <action>
    1. Imports: add `from . import cgo_build, pymol_bridge, spectra_ui`.
    2. __init__ (pinned index 2, between log and plot panel): build self.table = QtWidgets.QTableWidget(0, 3, self); setHorizontalHeaderLabels(('mode', 'frequency (cm-1)', 'IR intensity (km/mol)')); setEditTriggers(QtWidgets.QAbstractItemView.NoEditTriggers); setSelectionBehavior(QtWidgets.QAbstractItemView.SelectRows); setSelectionMode single; verticalHeader().setVisible(False); resizeColumnsToContents after populate; horizontalHeader().setStretchLastSection(True); cellClicked.connect(self._on_table_cell_clicked); insert into the layout at index 2 with stretch 2 (plot panel shifts to 3, buttons to 4 — update the layout docstring to the FINAL order). Initialize self._xtbopt_snake_id = None and self._zoomed_snake_id = None.
    3. `_populate_table(self)`: self.table.setRowCount(0); if self._spectrum is None: return (failed/cancelled/no-record — never fabricated rows); rows = spectra_ui.table_rows(self._spectrum); self.table.setRowCount(len(rows)); for r, (idx, flabel, ilabel) in enumerate(rows): three QTableWidgetItem cells (text via str(); flags = QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsSelectable — read-only; no tooltips needed v1); after the loop resizeColumnsToContents(). Docstring: row r ⇔ self._spectrum.modes[r] (single-source parse — 07-08; never re-derive).
    4. Wire into the flows: _populate_spectrum (07-08) calls self._populate_table() right after set_scene (same source parse drives both); on the failed/cancelled/corrupt early-returns, ALSO self._populate_table() (self._spectrum was reset to None → table clears). Update the class docstring: SPECTRA-05 table live; vectors next task.
    Run gates + AST. Commit: `feat(07-09): frequency table — every mode listed, shared freq_label formatting (SPECTRA-05 table half)`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST assertions: SpectraTab defines _populate_table, the table is constructed with 3 columns + NoEditTriggers + SelectRows, cellClicked.connect(self._on_table_cell_clicked) is wired; grep no '.exec_(' and no 'pymol.cmd'; `python3.6 tests/run_gates.py --smoke` — flushed SMOKE-OK sentinels</verify>
  <done>The table renders every parsed mode (negatives as -31.9i, zero-intensity included) straight from the shared builders.</done>
</task>

<task type="auto">
  <name>Task 2: row-click → mode vectors on the optimized frame (once-per-record overlay)</name>
  <files>serpentrum/gui_spectra.py</files>
  <action>
    Implement `_on_table_cell_clicked(self, row, column)` (column ignored — the ROW selects the mode):
    1. Guards first: if self._spectrum is None: return (no spectrum — nothing to draw). record = getattr(self._anchor, 'spectra_run', None) or {}.
    2. prims = spectra_ui.mode_arrow_primitives(self._spectrum, row + 1) (row r ⇔ modes[r]; 1-based index). If prims is None: self.set_status_line('this spectrum has no displacement vectors (vibspectrum fallback) - vectors need the g98 output'); return.
    3. Optimized-frame guard: xtbopt_path = record.get('xtbopt_path'); if not xtbopt_path: self.set_status_line('the optimized structure file is unavailable - cannot draw mode vectors'); return.
    4. Load srp_xtbopt ONCE per record: if (not pymol_bridge.object_exists('srp_xtbopt')) or self._xtbopt_snake_id != record.get('snake_id'): try: pymol_bridge.delete_object('srp_xtbopt'); pymol_bridge.load_xtbopt(xtbopt_path); self._xtbopt_snake_id = record.get('snake_id'); except OSError as exc: self.set_status_line('the optimized structure could not be loaded: %s' % exc); return.
    5. Arrows: pymol_bridge.delete_object('srp_mode_vec'); cgo = cgo_build.mode_arrows(prims[0], prims[1], scale=1.0); pymol_bridge.load_mode_arrows(cgo) (replace, never accumulate; frozen builder; scale pinned v1).
    6. Zoom ONCE per record: if self._zoomed_snake_id != record.get('snake_id'): pymol_bridge.zoom_mode_frame(); self._zoomed_snake_id = record.get('snake_id') (guarded no-op when neither object exists — 07-03).
    7. Status line: set_status_line('mode %d: vectors drawn on the optimized structure (srp_xtbopt)' % (row + 1)) — the educational framing states the frame EXPLICITLY (owner sign-off at 07-10; if the owner amends the interpretation, this line + the load target are the only edits).
    8. Docstring on the handler: the OPTIMIZED-FRAME decision (research Q4b probe: g98 ≡ xtbopt 1e-6 Å; game frame off up to 0.14 Å); static vectors only (SPECTRA-05; animation is v2 GAME-09-v2); once-per-record zoom (pitfall 14); srp_ objects die at cleanup_srp/begin_game — the handler's guards tolerate their absence.
    Run gates + AST + smoke leg. Commit: `feat(07-09): row-click mode vectors on srp_xtbopt — replace-per-click, once-per-record overlay (SPECTRA-05)`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST assertions: _on_table_cell_clicked references mode_arrow_primitives, cgo_build.mode_arrows, load_mode_arrows, load_xtbopt, zoom_mode_frame, and guards on snake_id; grep confirms no 'pymol.cmd' and no '.exec_(' in gui_spectra.py; direct smoke re-run: `timeout 90 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\13_mode_arrows_smoke.py"` still flushes SMOKE-OK MODE-ARROWS (the seams 07-09 consumes are unchanged)</verify>
  <done>Clicking a row draws that mode's uniform-length arrows on the optimized structure; repeated clicks replace; the camera moves once per record; every refusal path is a clear line.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green; `python3.6 tests/run_gates.py --smoke` — flushed SMOKE-OK sentinels (GUI module touched).
- AST audit: table populated ONLY via spectra_ui.table_rows; vectors ONLY via mode_arrow_primitives + cgo_build.mode_arrows + the three bridge seams; srp_ names only; once-per-record guards present.
- Live click→vector behavior + the optimized-frame interpretation are human-verified at the 07-10 consolidated checkpoint (owner sign-off).
</verification>

<success_criteria>
- SPECTRA-05's user path exists end-to-end: every mode listed with the shared imaginary convention; a click draws that mode's static vectors on the optimized frame; repeat clicks replace; degenerate states refuse with clear lines.
- Index safety is structural: row r ⇔ spectrum.modes[r] from the ONE parse stored in 07-08 — no re-merge, no off-by-one surface (Pitfall 12).
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-09-SUMMARY.md`
</output>
