---
phase: 07-spectra-ui
plan: 08
type: execute
wave: 4
depends_on: ["07-06", "07-07"]
files_modified:
  - serpentrum/gui_spectra.py
  - serpentrum/spectra_ui.py
  - tests/test_spectra_ui.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "When a run finishes 'ok', the tab parses the frozen record's g98_path (vibspectrum fallback with an explicit vectors-unavailable note), reads broadening_fwhm LIVE from the anchor setup (setup_logic.DEFAULTS fallback — never re-pinned), builds the Scene via plot_logic and hands it to the embedded SpectraPlotPanel — the user sees THEIR snake's broadened IR spectrum on the tab [SPECTRA-03 on-screen]"
    - "A terminal record that already exists (plugin reload / dialog reopen after a completed run) populates the plot on construction — no stale empty panel after reflect_run_state"
    - "Degenerate states never fabricate: failed/cancelled runs (no artifact paths) leave the plot empty with the verdict + problems in the status; a missing/corrupt file on disk surfaces a clear line (SpectraParseError/OSError caught) — never a crash, never a fake curve (Phase-6 SC3)"
    - "The embedded panel keeps its own adjustments (size combo, axis toggle, Save Plot) and reports save outcomes through the tab's status line via status_cb — the panel stays data-in (Scenes only, no runner coupling)"
  artifacts:
    - path: serpentrum/gui_spectra.py
      provides: "plot panel embedded at the pinned layout index 2; _populate_spectrum(record) parse→build_scene→set_scene flow; _refresh_from_record wired into reflect_run_state AND on_run_finished; parse/error guards"
      contains: "set_scene"
    - path: serpentrum/spectra_ui.py
      provides: "record_spectrum_paths(record) pure helper — display path precedence (g98 first, vibspectrum fallback, none when degenerate), tested"
      contains: "record_spectrum_paths"
  key_links:
    - from: "serpentrum/gui_spectra.py _populate_spectrum"
      to: "record['g98_path'] (xtb_run.SPECTRA_RUN_KEYS, 06-02:64) → spectra.parse → plot_logic.build_scene → panel.set_scene"
      via: "the single-source parse: ONE Spectrum per record drives plot (this plan) and table (07-09) — index desync is structurally impossible (research Q4d)"
      pattern: "build_scene"
    - from: "serpentrum/gui_spectra.py fwhm read"
      to: "setup_logic.DEFAULTS['broadening_fwhm'] / anchor.setup"
      via: "LIVE read at populate time (06-11's lesson: runtime-read constants, never re-pinned); validated > 0 upstream by setup_logic:269-271"
      pattern: "broadening_fwhm"
---

<objective>
Embed the human-approved SpectraPlotPanel into the live SpectraTab and wire the data feed: on run_finished (and on construction when a terminal record already exists) parse the record's g98 output, build the Scene at the setup's live fwhm, and put the player's broadened IR spectrum on screen — completing SPECTRA-03's on-screen half.

Purpose: the payoff picture. The widget (07-05) is verified; the tab shell (07-07) streams logs; this plan connects the frozen Phase-6 record to the Scene so the plot appears the moment a run lands.
Output: one focused gui_spectra.py edit (the panel embed + populate flow + guards).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-qt-plot.md
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@serpentrum/gui_spectra.py
@serpentrum/plot_logic.py
@serpentrum/spectra.py
@serpentrum/setup_logic.py

Key facts:
- Record contract (06-02:64,98 — frozen, consumed WITHOUT reshaping): spectra_run keys ('snake_id','status','problems','input_path','g98_path','vibspectrum_path','xtbopt_path','log_path'); written ONLY by the controller's terminal branch; 'ok' implies g98_path + vibspectrum_path + xtbopt_path populated (stable srp_spectra/<snake_id> dir); failed/cancelled may carry None paths (research Q8-10: never fabricate rows/curves from a degenerate record).
- Parse precedence (research Q4d): g98 first (it alone carries displacement vectors + the atom block); vibspectrum fallback when g98_path is None/missing — via spectra.real_modes(parse(...)) with a status note 'showing vibspectrum - no displacement vectors available (table only)'. NEVER re-merge g98 vectors with vibspectrum rows (Pitfall 12.5; 06-RESEARCH-guard pitfall 11).
- Error surfaces: spectra.SpectraParseError (ValueError subclass, spectra.py:41-45, carries stage + line + excerpt) and OSError (temp wiped) → catch BOTH around the parse → clear status line ('spectrum could not be read: <exc>') + plot stays empty. The 787-line fixture parses in ms on the UI thread — legal (research Q8-5; NEVER the xtb run itself, that is Phase 6's QProcess).
- fwhm: `setup_logic.DEFAULTS['broadening_fwhm']` = 16.0 (setup_logic.py:57). Read LIVE: `setup = getattr(self._anchor, 'setup', None); fwhm = float(setup.get('broadening_fwhm')) if setup and setup.get('broadening_fwhm') else setup_logic.DEFAULTS['broadening_fwhm']` (the reload hole: setup may be None — fall back to DEFAULTS, never crash; 06-09's atom_budget precedent).
- Panel contract (07-05): SpectraPlotPanel(parent, status_cb); set_scene(scene); its Save Plot reports via status_cb. Empty scene (None) paints axes + hint; a real zero-curve scene (empty modes) paints a flat baseline — the tab passes build_scene(spectrum.modes) verbatim, never None when a parse succeeded.
- Caption: plot_logic.mode_caption(scene) — append to the log panel ('72 modes; 3 below 0 (imaginary) - see the table' / '' → nothing).
- Layout contract (pinned 07-07): insert the panel at QVBoxLayout index 2 (status / log / PANEL / buttons → buttons shift to 3; 07-09 later inserts the table at index 2, shifting panel to 3 and buttons to 4). Use widget insert via layout.insertWidget(2, panel, stretch=3)? NO — the panel is created in THIS plan (the tab was built without it), so __init__ builds: status / log / plot panel (stretch 3) / button row — and 07-09's insert-at-index-2 contract still holds (table slots between log and panel). Update the layout docstring accordingly.
- reflect_run_state / on_run_finished (07-07): this plan EXTENDS both to call the new _refresh_from_record()/populate path (plot now; table in 07-09). Keep the existing status/button logic byte-identical.
- Purity: gui_spectra is GUI (pymol.Qt only + relative pure imports legal); plot_logic/spectra/setup_logic are pure — importable. NO pymol.cmd; NO .exec_(); %-formatting; python3.6.
- 07-06 checkpoint approved the panel's look — this plan changes NOTHING in gui_plot.py; if a look defect resurfaces, it is a checkpoint follow-up, not scope creep here.

What NOT to touch and why:
- gui_plot.py — verified widget; embed + feed only.
- gui.py — single-writer was 07-07; the dialog needs no change for the embed (the tab owns its layout).
- The runner/pipeline — record is read-only here; the terminal branch (06-05) owns writes.
- The table — 07-09 owns it; do not build placeholder table widgets.
</context>

<tasks>

<task type="auto">
  <name>Task 1: embed SpectraPlotPanel + populate flow + guards</name>
  <files>serpentrum/gui_spectra.py</files>
  <action>
    1. Imports: add `from . import plot_logic, setup_logic, spectra` (pure relative imports — purity-legal).
    2. __init__ layout (pinned order): after the log panel ([1]) insert `self.plot_panel = gui_plot.SpectraPlotPanel(self, status_cb=self.set_status_line)` — import `from .gui_plot import SpectraPlotPanel` — added to the layout with stretch 3 BEFORE the button row (final build order: status[0] / log[1] / plot_panel[2] / buttons[3]; docstring records 07-09's insert-at-2 contract). No scene yet (panel paints the empty-scene hint).
    3. New method `_populate_spectrum(self, record)`:
       - paths = record or {}; g98_path = paths.get('g98_path').
       - try: if g98_path and os.path.isfile(g98_path): spectrum = spectra.parse(g98_path); source_note = None; else: vpath = paths.get('vibspectrum_path'); if not (vpath and os.path.isfile(vpath)): self.plot_panel.set_scene(None); return (degenerate/failed record — verdict + problems already in the status; nothing fabricated); spectrum = spectra.parse(vpath); spectrum = spectra.Spectrum(spectrum.n_atoms, spectrum.atoms, spectra.real_modes(spectrum)); source_note = 'showing vibspectrum - no displacement vectors available (table only)'; except (spectra.SpectraParseError, OSError, ValueError) as exc: self.plot_panel.set_scene(None); self.set_status_line('spectrum could not be read: %s' % exc); return.
       - setup = getattr(self._anchor, 'setup', None); fwhm = setup.get('broadening_fwhm') if setup else None; if not isinstance(fwhm, (int, float)) or fwhm <= 0: fwhm = setup_logic.DEFAULTS['broadening_fwhm'] (LIVE read + reload-safe fallback — never pinned).
       - scene = plot_logic.build_scene(spectrum.modes, fwhm=float(fwhm)); self.plot_panel.set_scene(scene).
       - Store for 07-09: self._spectrum = spectrum (single-source parse — the table reuses THIS object; index desync structurally impossible). self._spectrum_note = source_note.
       - if source_note: self.set_status_line(source_note).
       - caption = plot_logic.mode_caption(scene); if caption: self.append_log_line(caption).
       - Initialize `self._spectrum = None` in __init__ (07-09 consumes it).
    4. Wire into the existing flows (07-07's methods, extended not replaced): reflect_run_state's terminal branch calls self._populate_spectrum(record) after on_run_finished; on_run_finished(status, problems) — after the existing status/button updates, when status == 'ok' (xtb_run vocabulary — import `from . import xtb_run` or compare to the literal 'ok' consistently with 07-01's usage; prefer `record.get('status') == xtb_run.DONE` via the record) call self._populate_spectrum(record). Failed/cancelled: self.plot_panel.set_scene(None) + self._spectrum = None (clean reset on a new degenerate record).
    5. Update the class docstring: plot feed complete (SPECTRA-03 on-screen); table+vectors next (07-09) reusing self._spectrum.
    Run gates + AST. Commit: `feat(07-08): plot panel embedded + record→Scene feed with live fwhm and guarded parse (SPECTRA-03 on-screen)`
  </action>
  <verify>python3.6 tests/run_gates.py green; AST assertions (python3.6 heredoc): ast.parse serpentrum/gui_spectra.py; assert SpectraPlotPanel is constructed and set_scene is called; assert 'build_scene' and 'broadening_fwhm' and 'real_modes' appear; assert SpectraParseError is caught; grep no '.exec_(' and no 'pymol.cmd'; `python3.6 tests/run_gates.py --smoke` — flushed SMOKE-OK sentinels (GUI module touched)</verify>
  <done>A finished run puts the player's broadened spectrum on the tab; reloads repopulate from the anchored record; degenerate states degrade to clear lines + empty plot.</done>
</task>

<task type="auto">
  <name>Task 2: record_spectrum_paths pure seam + tab rewire</name>
  <files>serpentrum/spectra_ui.py, tests/test_spectra_ui.py, serpentrum/gui_spectra.py</files>
  <action>
    1. serpentrum/spectra_ui.py (07-01's module — 3 waves complete, sequential single-writer respected): add `record_spectrum_paths(record) -> (g98_path, vibspectrum_path, source)` — a 10-line PURE helper next to run_status_lines (it already owns record-shaped logic; docstring: display path precedence — g98 first (vectors live there), vibspectrum fallback, (None, None, 'none') when degenerate or keys missing; never touches the filesystem). Implementation: g98 = record.get('g98_path'); vibs = record.get('vibspectrum_path'); return (g98, vibs, 'g98') if g98 else ((None, vibs, 'vibspectrum') if vibs else (None, None, 'none')).
    2. tests/test_spectra_ui.py: add TestRecordSpectrumPaths — ok-record paths -> (g98, vibs, 'g98'); g98 None + vibs set -> (None, vibs, 'vibspectrum'); both None -> (None, None, 'none'); empty dict -> (None, None, 'none').
    3. Rewire the tab's _populate_spectrum (serpentrum/gui_spectra.py) to call spectra_ui.record_spectrum_paths FIRST, then the existing try/except around parse + build + set_scene (the Task-1 flow otherwise unchanged — the helper only selects the path; os.path.isfile existence checks stay in the tab).
    4. Full gates: `python3.6 tests/run_gates.py` green (suite incl. the new tests).
    Commit: `feat(07-08): record_spectrum_paths pure seam + tab rewire (degenerate matrix testable)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_spectra_ui.py" -v green (new TestRecordSpectrumPaths); python3.6 tests/run_gates.py green; git diff --stat shows exactly the three listed files</verify>
  <done>The path-precedence decisions are pure-tested; the tab's populate flow is thin over them.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (incl. the extended test_spectra_ui.py).
- AST: gui_spectra constructs SpectraPlotPanel + calls set_scene via build_scene; SpectraParseError/OSError guarded; spectra_ui.record_spectrum_paths exists + tested.
- Live plot population after a real run is human-verified at the 07-10 consolidated checkpoint (the 07-06 checkpoint approved the widget itself).
- `python3.6 tests/run_gates.py --smoke` — flushed SMOKE-OK sentinels.
</verification>

<success_criteria>
- run finished 'ok' → the tab shows the snake's broadened IR spectrum (live fwhm, g98-first parse, vibspectrum fallback with a note); reload/dialop-reopen repopulates from the anchored record.
- Failed/cancelled/corrupt/missing → clear status lines + empty plot; nothing fabricated, nothing crashes.
- One Spectrum per record is stored (self._spectrum) ready for 07-09's table/vector reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-08-SUMMARY.md`
</output>
