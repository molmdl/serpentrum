---
phase: 07-spectra-ui
plan: 01
type: tdd
wave: 1
depends_on: ["06-02"]
files_modified:
  - serpentrum/spectra_ui.py
  - tests/test_spectra_ui.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "freq_label renders negative frequencies in the imaginary convention with ASCII minus (-31.9175 -> '-31.9i') and non-negatives plain ('%.1f') — the SINGLE shared frequency formatter for the entire Spectra UI (table rows and any future plot caption); no other module may hand-roll a variant"
    - "table_rows lists EVERY spectrum mode in file order — negatives and zero-intensity included — with a '%.4g' intensity label that keeps tiny-but-nonzero values (0.00026 class) distinct from exact zero ('0')"
    - "mode_arrow_primitives returns (atom positions, displacement vectors) for a 1-based mode index from the g98 parse, and None (never an exception) when vectors are unavailable (vibspectrum parse), the atom list is empty, or the index is out of range"
    - "run_status_lines builds verdict text solely from the frozen record vocabulary ('ok'/'failed'/'cancelled' + problems) — it never re-scans log text (the 'abnormal termination' substring trap stays dead)"
  artifacts:
    - path: serpentrum/spectra_ui.py
      provides: "PURE Spectra-UI builders: freq_label, table_rows, mode_arrow_primitives, run_status_lines; imports xtb_run status constants (single source)"
      min_lines: 80
    - path: tests/test_spectra_ui.py
      provides: "freq_label table (fixture-anchored), table_rows over the committed g98 + synthetic co2_vibspectrum fixtures (zero-intensity rows), mode-arrow selector edge cases, status-line vocabulary pins"
      min_lines: 110
  key_links:
    - from: "serpentrum/spectra_ui.py freq_label"
      to: "serpentrum/gui_spectra.py table (07-09) and serpentrum/plot_logic.py consumers"
      via: "the one imaginary-convention formatter — REQUIREMENTS' '−31.9i' display convention pinned as ASCII '-31.9i' in code (U+2212 glyph coverage is [TRAIN]-risky; hud strings are ASCII)"
      pattern: "freq_label"
    - from: "serpentrum/spectra_ui.py run_status_lines"
      to: "serpentrum/xtb_run.py status constants"
      via: "imports DONE/FAILED/CANCELLED — never re-pins the vocabulary strings (06-02 owns them)"
      pattern: "xtb_run"
---

<objective>
Create the PURE module `serpentrum/spectra_ui.py`: the Spectra tab's decision/display half — the shared imaginary-frequency formatter, the every-mode table-row builder, the mode→arrow-primitives selector, and the run-verdict status-line builder — TDD'd in WSL python3.6 with zero stubs.

Purpose: keep every string/index decision out of the GUI modules (07-07..07-09 become thin wiring), and pin the ONE frequency-label convention both research halves agreed on (07-RESEARCH-spectra-seam.md Q7; 07-RESEARCH-qt-plot.md pitfall 10 — a hand-rolled second formatter is the named drift risk).
Output: one new pure module + its test file. No existing module modified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@serpentrum/spectra.py
@serpentrum/xtb_run.py
@tests/fixtures/xtb/

Key facts (research file:line anchors):
- spectra.py contracts (Q1): `Mode = namedtuple('Mode', 'index freq intensity vectors')` (spectra.py:29, index 1-based); `Spectrum = namedtuple('Spectrum', 'n_atoms atoms modes')` (:36); `Atom = namedtuple('Atom', 'atomic_number x y z')`; g98 carries the Standard-orientation atom block + per-atom vectors for ALL 3N−6 modes; vibspectrum carries `atoms == []` and every `Mode.vectors == ()` — NO vectors, ever (spectra.py:313-321). `real_modes(spectrum, threshold=10.0)` (:402-418) is the vibspectrum→table filter (threshold on |freq| only; negatives survive).
- Fixture anchors (probe 2026-09-25, research Q1): g98.out = 26 atoms, 72 modes, negatives at modes 1-3 (−31.9175 / −23.0766 / −18.1086), ZERO zero-intensity modes in this g98; vibspectrum = 78 modes (6 trivial + 72 real), 6 zero-intensity trivial rows; `tests/fixtures/xtb/synthetic/co2_vibspectrum` = 5 trivial + 3 real with the intensity-EXACTLY-0.00 symmetric stretch (1424.95) — the end-to-end zero-intensity test vehicle.
- GAP (risk 1, ACCEPTED): no co2 g98.out fixture exists anywhere — the linear 5-trivial-mode g98 leg cannot be unit-tested end-to-end today. Mitigation: zero-intensity table coverage via synthetic `Mode` tuples + the synthetic co2 vibspectrum fixture; the linear g98 leg stays smoke-only behind the real-xtb `--xtb` gate. State this limitation in the module docstring — do NOT invent a fixture.
- Frozen record vocabulary (06-02:64,98): `SPECTRA_RUN_KEYS = ('snake_id','status','problems','input_path','g98_path','vibspectrum_path','xtbopt_path','log_path')`; status values 'idle'/'running'/'ok'/'failed'/'cancelled'; constants `xtb_run.DONE/FAILED/CANCELLED` — import them, never re-pin strings.
- Purity: new PURE modules need NO registration (tools/check_purity.py:56-57 "everything else defaults PURE"); `from . import xtb_run` is an intra-package relative import, exempt (check_purity.py:95-98).
- House test header (tests/test_spectra_broadening.py:23-48): sys.path self-insert, NO `__init__.py` in tests/, unittest, %-formatting, fixtures read in place.
- Cross-half convention (COORDINATION, pinned at planning): `freq_label` lives HERE. `plot_logic.py` (07-02, same wave) deliberately imports NO frequency formatter — the v1 plot shows no raw frequency labels (ascending axis, count-only caption). If a future plot surface ever prints a frequency, it MUST import this function. Note this in the module docstring.
- python3.6 syntax, %-formatting only (STACK.md:190 discipline; runtime is 3.9 but the WSL gate is binding).

RESOLVED DECISIONS owned by this plan:
- Formatter home: `spectra_ui.freq_label` (seam research Q7 proposal). ASCII minus pinned; REQUIREMENTS.md keeps its typographic '−31.9i' as a prose display convention — docs unchanged.
- Intensity label precision: `'%.4g'` — the vibspectrum fixture's smallest real intensity is 0.00026; `'%.2f'` would render it '0.00', erasing SPECTRA-05's zero-intensity distinction. Exact 0.0 renders '0'.
- run_status_lines output shape: list of lines — `['xtb finished: %s' % status]` plus `['problems: %s' % '; '.join(problems)]` when problems is non-empty. (This replaces the 06-09 placeholder's inline join when 07-07 rewires the slot — the placeholder's exact string is not a pinned behavior; the vocabulary and never-re-grep rule are.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for spectra_ui</name>
  <files>tests/test_spectra_ui.py</files>
  <action>
    Create tests/test_spectra_ui.py (house header: sys.path self-insert; NO __init__.py; unittest; %-formatting; fixture paths via ROOT join). Import `from serpentrum import spectra_ui, spectra, xtb_run` (module absent -> ImportError = RED).

    ROOT/fixtures constants: FIXTURES = tests/fixtures/xtb; G98_PATH, VIB_PATH = fixtures/vibspectrum, CO2_VIB_PATH = fixtures/synthetic/co2_vibspectrum.

    Class TestFreqLabel:
    - test_negative_imaginary: freq_label(-31.9175) == '-31.9i'; freq_label(-23.0766) == '-23.1i'; freq_label(-18.1086) == '-18.1i' (fixture modes 1-3).
    - test_nonnegative_plain: freq_label(0.0) == '0.0'; freq_label(1150.6639) == '1150.7'; freq_label(257.1018) == '257.1'.
    - test_ascii_only: every output contains only ASCII (assert `.isascii()`), hyphen-minus never U+2212 (the doc-pinned convention).

    Class TestTableRows:
    - test_g98_all_modes: spectrum = spectra.parse_g98(G98_PATH); rows = spectra_ui.table_rows(spectrum); len == 72; rows[0][0] == 1 and rows[0][1] == '-31.9i'; rows[2][1] == '-18.1i'; the row whose freq label is '1150.7' has intensity label '257.1'.
    - test_row_shape: each row is a 3-tuple (index:int, freq_label:str, intensity_label:str); indices are 1..72 in order.
    - test_zero_intensity_listed: rows = spectra_ui.table_rows(spectra.Spectrum(1, [], [spectra.Mode(9, 1500.0, 0.0, ())])); rows == [(9, '1500.0', '0')] (synthetic tuple — the zero-intensity leg the g98 fixture cannot provide; PITFALLS.md 12.3).
    - test_co2_vibspectrum_end_to_end: spec = spectra.parse_vibspectrum(CO2_VIB_PATH); rows = spectra_ui.table_rows(spectra.real_modes(spec)); len == 3; exactly one row has intensity label '0' (the symmetric stretch) — tiny-but-nonzero values would render differently ('%.4g' contract).

    Class TestModeArrowPrimitives:
    - test_g98_mode1: spectrum = spectra.parse_g98(G98_PATH); prims = spectra_ui.mode_arrow_primitives(spectrum, 1); prims is not None; atoms, vecs = prims; len(atoms) == 26 == len(vecs); every atom is a 3-float tuple; every vec is a 3-float tuple.
    - test_mode_order: prims for index k match spectrum.modes[k-1].vectors element-wise (assert equality against spectrum.modes[4].vectors for index 5).
    - test_out_of_range_none: mode_arrow_primitives(spectrum, 0) is None; mode_arrow_primitives(spectrum, 73) is None.
    - test_vibspectrum_none: mode_arrow_primitives(spectra.parse_vibspectrum(VIB_PATH), 7) is None (every vectors == (); spectra.py:313-321).
    - test_empty_atoms_none: mode_arrow_primitives(spectra.Spectrum(0, [], [spectra.Mode(1, 100.0, 5.0, ())]), 1) is None.

    Class TestRunStatusLines:
    - test_ok_clean: spectra_ui.run_status_lines({'status': xtb_run.DONE, 'problems': []}) == ['xtb finished: ok'].
    - test_failed_with_problems: run_status_lines({'status': xtb_run.FAILED, 'problems': ['p1', 'p2']}) == ['xtb finished: failed', 'problems: p1; p2'].
    - test_cancelled: run_status_lines({'status': xtb_run.CANCELLED, 'problems': []}) == ['xtb finished: cancelled'].
    - test_no_log_scanning: a record with a 'log_path' key pointing at a nonexistent file still renders identically (the builder never opens files).

    Run: `python3.6 -m unittest discover -s tests -p "test_spectra_ui.py" -v` -> ImportError (RED).
    Commit RED: `test(07-01): failing tests for spectra_ui (freq_label, table_rows, arrow primitives, status lines)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_spectra_ui.py" -v fails with ImportError; no other test file touched</verify>
  <done>RED confirmed: the Spectra-UI pure contract exists as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement serpentrum/spectra_ui.py</name>
  <files>serpentrum/spectra_ui.py</files>
  <action>
    Create serpentrum/spectra_ui.py — PURE module (stdlib only; `from . import xtb_run` allowed; %-formatting; python3.6). Module docstring: the Spectra tab's pure half (07-RESEARCH-spectra-seam.md Q7); states the freq_label-is-shared coordination note and the no-co2-g98-fixture limitation.

    1. `freq_label(freq)`: freq < 0 -> '-' + ('%.1f' % abs(freq)) + 'i' (ASCII hyphen-minus; e.g. -31.9175 -> '-31.9i'); else '%.1f' % freq. Docstring pins: single shared formatter for table + plot surfaces; REQUIREMENTS' '−31.9i' is the docs' typographic rendering, code ships ASCII ([TRAIN] glyph risk, 07-RESEARCH-qt-plot.md [TRAIN] 2).
    2. `table_rows(spectrum)`: `[(m.index, freq_label(m.freq), '%.4g' % m.intensity) for m in spectrum.modes]` — every mode in order, no filtering (the caller decides g98-all vs real_modes fallback). Docstring: '%.4g' keeps 0.00026-class intensities visible; exact 0 renders '0'.
    3. `mode_arrow_primitives(spectrum, mode_index)`: return None when spectrum.atoms is empty, 1-based mode_index is out of range, or `not spectrum.modes[mode_index - 1].vectors`; else `([(float(a.x), float(a.y), float(a.z)) for a in spectrum.atoms], [tuple(float(c) for c in v) for v in mode.vectors])`. Docstring: the GUI passes these to the FROZEN cgo_build.mode_arrows (cgo_build.py:134-208 — directions unit-normalized, uniform length; scale is the only knob; v1 pins scale=1.0). Atom order = g98 Standard-orientation order == xtb run-input order == xtbopt.xyz order (research Q4b: no reordering seam anywhere).
    4. `run_status_lines(record)`: status = record.get('status'); lines = ['xtb finished: %s' % status]; problems = record.get('problems') or []; if problems: lines.append('problems: %s' % '; '.join(str(p) for p in problems)); return lines. Docstring: derived SOLELY from the frozen record vocabulary (xtb_run.DONE/FAILED/CANCELLED constants); never opens/re-scans the log ('abnormal termination' substring trap — 06-RESEARCH-guard pitfall 8).
    5. Run the file suite green, then full gates: `python3.6 tests/run_gates.py`.
    6. Commit GREEN: `feat(07-01): spectra_ui — shared freq_label, every-mode table rows, arrow primitives, status lines (SPECTRA-05 pure half)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_spectra_ui.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green; python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import spectra_ui; print(spectra_ui.freq_label(-31.9175))" prints -31.9i</verify>
  <done>spectra_ui.py exists (pure), the four builders are pinned by fixture-anchored tests, gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (new pure module auto-classified PURE — no registration edit).
- `python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import spectra_ui, spectra; s = spectra.parse_g98('tests/fixtures/xtb/g98.out'); rows = spectra_ui.table_rows(s); print(len(rows), rows[0][1], rows[2][1])"` prints `72 -31.9i -18.1i`.
- `grep -c "exec_\|import pymol\|from pymol" serpentrum/spectra_ui.py` == 0.
</verification>

<success_criteria>
- The one imaginary-convention formatter exists exactly once (here); table rows cover every mode incl. zero-intensity ('%.4g'); the arrow selector is total (None, never an exception) on unavailable vectors; verdict lines come only from the frozen vocabulary.
- Zero-intensity coverage proven via synthetic Mode tuples + the synthetic co2 vibspectrum fixture; the missing co2 g98 fixture is documented as an accepted limitation (linear g98 leg = smoke-only behind --xtb).
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-01-SUMMARY.md`
</output>
