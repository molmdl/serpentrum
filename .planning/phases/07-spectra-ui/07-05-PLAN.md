---
phase: 07-spectra-ui
plan: 05
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - serpentrum/gui_plot.py
  - smoke/12_plot_smoke.py
  - tools/check_purity.py
  - tests/run_gates.py
  - AGENTS.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "IrPlotWidget paints ANY Scene through the ONE paint_scene seam — paintEvent and the PNG save render the IDENTICAL scene (route A), antialiasing ON, margins computed from QFontMetrics so labels never clip or smear (volume.py:205-207/:215-223 precedents)"
    - "An empty scene (None) paints axes + a centered hint ('run a calculation to plot a spectrum') — never a crash; a real scene maps xs/ys to a QPolygonF float polyline clipped to the plot rect"
    - "Save Plot renders the scene into a 2x QImage and writes a valid PNG (probe-verified route A: probe stage2/stage4 SMOKE-OK) — QFileDialog.getSaveFileName static convenience (no .exec_ token), '' = cancel guarded, '.png' appended when the user omitted it"
    - "Plot adjustments are exactly the v1 cut: size-preset combo (house addItem(label, data) pattern) + 'Show axis labels' toggle (default ON) — NO FWHM control, NO zoom/pan (v2 bait), and QWidget.grab() exists only as a documented fallback comment (route B demoted)"
    - "Both new GUI modules are deliberately registered in ONE single-writer edit: GUI_MODULES gains gui_plot.py (live) and gui_spectra.py (inert — file arrives with 07-07); REQUIRED smoke 12 guards the renderer on every gate run"
  artifacts:
    - path: serpentrum/gui_plot.py
      provides: "IrPlotWidget (set_scene/set_size_preset/paintEvent), module-level paint_scene(painter, rect, scene, show_labels) seam, SpectraPlotPanel (combo + toggle + save button + status callback), module-level render_image(scene, logical_size, scale=2)"
      min_lines: 150
    - path: smoke/12_plot_smoke.py
      provides: "Headless REQUIRED renderer smoke: guarded QApplication([]) construct (the load-bearing probe finding), fixture scene, render_image -> temp PNG -> magic bytes + reload width 1600, axis-off leg"
      contains: "SMOKE-OK"
    - path: tools/check_purity.py
      provides: "GUI_MODULES gains 'serpentrum/gui_plot.py' + 'serpentrum/gui_spectra.py' with per-plan comments (inert-first convention, check_purity.py:56-61)"
      contains: "gui_plot"
    - path: tests/run_gates.py
      provides: "REQUIRED_SMOKES gains 'smoke/12_plot_smoke.py' (plan 07-05 comment)"
      contains: "12_plot_smoke"
    - path: AGENTS.md
      provides: "Spectra-phase gate note: the QApplication([])-guard requirement for any renderer smoke (font access with no Q*Application hard-kills the process — probe RUN A)"
      contains: "QApplication"
  key_links:
    - from: "serpentrum/gui_plot.py paint_scene"
      to: "serpentrum/gui_plot.py render_image"
      via: "the same module-level seam serves paintEvent AND the PNG save — identical scene, identical look on screen and on disk (SPECTRA-03's saveable-plot criterion)"
      pattern: "paint_scene"
    - from: "serpentrum/gui_plot.py"
      to: "serpentrum/plot_logic.py (plan 07-02)"
      via: "the GUI consumes Scenes as pure data — paint maps values to pixels and NEVER recomputes broaden/ticks (pitfall 2: paintEvent-computing)"
      pattern: "plot_logic"
---

<objective>
Build the plot GUI module `serpentrum/gui_plot.py` (IrPlotWidget + SpectraPlotPanel + the shared paint_scene seam + the render_image save route), its REQUIRED headless smoke 12, the deliberate GUI_MODULES registration for BOTH new Spectra GUI modules, and the AGENTS.md note encoding the probe's hard-kill lesson.

Purpose: SPECTRA-03's deliverable — a broadened IR plot with axis labels, adjustable size, and a saveable PNG — as a data-in widget the Spectra tab (07-08) embeds without coupling to the runner. The save route is the probe-SETTLED route A (one painter seam, two devices), not the demoted QWidget.grab().
Output: one new GUI module, one required smoke, one single-writer registration edit, one AGENTS.md gate note.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-qt-plot.md
@serpentrum/plot_logic.py
@serpentrum/gui_setup.py
@tools/check_purity.py
@tests/run_gates.py

Key facts (research file:line anchors — ALL probe/source-verified):
- SAVE ROUTE (settled, probe stage2/stage4 SMOKE-OK): QImage(w*scale, h*scale, Format_RGB32) + fill white + setDevicePixelRatio(float(scale)) + QPainter -> paint_scene(painter, QRect(0,0,w,h), scene) -> painter.end() -> img.save(path, 'PNG') — works widget-free and headless (probe: PNG magic + reload width round-trip). render_image(scene, logical_size, scale=2) is the exact research-recommended shape.
- THE HARD TRAP (probe RUN A): font access (QFontMetrics/drawText) with NO Q*Application SILENTLY HARD-KILLS the process (no traceback). A bare QApplication([]) — no widgets — revives it (probe stage3). In the real GUI an app always exists; in headless smokes the guard `app = QApplication.instance() or QApplication([])` is LOAD-BEARING. Never call render code at import time or from a non-Qt context.
- QPainter precedents (in-tree, file:line): volume.py:252-274 paintEvent (begin/setRenderHint(Antialiasing)/.../end — antialiasing ON, unlike dynoplot's commented-out :71); volume.py:257-258 + :87-88 margins via rect.adjust; volume.py:205-207 + :184-195 QFontMetrics-driven layout (fm.averageCharWidth/width/height); volume.py:215-223 tick-label dodge (`if x - lastx > w + 2*fw: skip`); volume.py:269-272 clip region around the curve; volume.py:98-99 sizeHint QSize(600, 200); dynoplot.py:21-23 import style (`from pymol.Qt import QtCore, QtGui, QtWidgets` — this module adds QtGui); dynoplot.py:77-80 fixed inset constants (start point; let QFontMetrics widen the left margin for y-tick labels); dynoplot.py:111-126 aligned multiline drawText (Qt.AlignHCenter|Qt.AlignBottom / AlignVCenter|AlignRight); dynoplot.py:320 self.update() on data change (NEVER repaint() — update coalesces; v1 has no drag handlers so zero mouse-driven repaints).
- Curve: QtGui.QPolygonF float polyline (probe stage2: 701-point QPolygonF paints+saves fine — preferred over int QPolygon rounding).
- File picker precedent: QFileDialog.getOpenFileName static convenience at gui_setup.py:400-411 (`if path:` cancel guard :403). getSaveFileName is the same static-convenience class — no .exec_() token, so the AST purity ban (check_purity.py:192-196) is untouched. Append '.png' if the user omitted it ([TRAIN]; getSaveFileName does not auto-append on all platforms).
- Combo pattern: `combo.addItem(label, data)` + findData + blockSignals on programmatic updates (gui_setup.py:106-108, :423-427).
- Purity mechanics: GUI class = ONLY pymol.Qt/pymol.Qt.* imports at any level; pymol/pmg_tk/PyQt5/numpy banned; gui_plot may NOT import pymol.cmd (status/log output belongs to the tab — 07-RESEARCH-qt-plot.md pitfall 8). New PURE modules need no registration; new GUI modules DO (check_purity.py:62-64; entries for not-yet-created files are INERT :56-61 — the inert-first edit lands gates-green before gui_spectra.py exists). matplotlib banned outright (ARCHITECTURE.md anti-pattern 6).
- Runtime/DPI facts (probe): Qt 5.12.9 / PyQt5 5.12.3; QImage.setDevicePixelRatio EXISTS (stage1); deviceIndependentSize does NOT (binding gap — do logical math manually); Windows runtime python 3.9 but the WSL gate compiles 3.6 (%-formatting, no f-strings).
- Scene contract (07-02): Scene(xs, ys, x_min, x_max, y_max, x_ticks, y_ticks, x_label, y_label, n_modes, n_imaginary, fwhm) with x_ticks/y_ticks as (value, label) pairs; size_presets() = [('medium (640x400)', (640,400)), ('large (800x500)', (800,500)), ('wide (960x500)', (960,500))]; mode_caption(scene) -> '' or 'N modes; K below 0 (imaginary) - see the table'.
- SPECTRA-03 'minimal adjustments' final cut (research "recommended design"): size preset combo + axis-label toggle. Nothing else in v1.
- Probe disposition: smoke/tmp_research_07_plot_probe.py was deleted by plan 07-02 (python3.6 os.remove — shell rm denied); its evidence lives in 07-RESEARCH-qt-plot.md and is superseded by smoke/12_plot_smoke.py (THIS plan). Confirm the probe is gone; never resurrect it.
- Smoke protocol: verdict = flushed SMOKE-OK sentinels, NEVER exit codes; REQUIRED_SMOKES at run_gates.py:55-61 (07-03 took 13; THIS plan takes 12); resolve ROOT defensively; flush=True prints.
- [TRAIN] discharges owned here: (2) ASCII labels pinned in code; (3) generous minimumHeight (spec 260) — readability human-verified at 07-06; (1) grab() demoted to a one-comment fallback (open question, not shipped); (5) no caching layer — set_scene stores, paint reads (verified at 07-10).

What NOT to touch and why:
- plot_logic.py — pure half is done and pinned; import it, never extend it from the GUI plan.
- gui.py / gui_spectra.py — the tab integration is 07-07/07-08; this plan must NOT touch page 2 or create gui_spectra.py (only its INERT allowlist entry).
- cgo_build/pymol_bridge — viewer seams are the other half's (07-03); the plot never touches the viewer.
</context>

<tasks>

<task type="auto">
  <name>Task 1: single-writer GUI_MODULES registration (inert-first) + AGENTS.md gate note</name>
  <files>tools/check_purity.py, AGENTS.md</files>
  <action>
    1. tools/check_purity.py GUI_MODULES (lines 62-64) gains BOTH entries in ONE edit (single-writer for the whole phase — 07-07 creates gui_spectra.py against the already-present entry):
       - `'serpentrum/gui_plot.py',` with comment: `# gui_plot.py = broadened-IR plot widget + save-PNG (Phase 7, plan 07-05): pymol.Qt only — 07-RESEARCH-qt-plot.md (edit mirrors gui_game.py's 04-05 entry). The plot NEVER imports pymol.cmd — it is data-in (Scenes from plot_logic).`
       - `'serpentrum/gui_spectra.py',` with comment: `# gui_spectra.py = SpectraTab (Phase 7, plan 07-07): pymol.Qt only. INERT until the file lands (check_tree walks existing files only).`
       Run `python3.6 tests/run_gates.py` — MUST stay green now (inert) and after Task 2 creates the file.
    2. AGENTS.md: in the "serpentrum plugin gates & conventions" section, add one bullet after the smoke one-liner bullet: headless smokes that touch QPainter/fonts MUST construct the app first — `app = QApplication.instance() or QApplication([])` — because font access with no Q*Application silently hard-kills PyMOL (no traceback; research probe RUN A); cite smoke/12_plot_smoke.py as the pattern. Keep it to 2-3 lines (AGENTS.md is the repo rulebook, not a tutorial).
    Run gates. Commit: `feat(07-05): GUI_MODULES single-writer registration (gui_plot + inert gui_spectra) + renderer-smoke app-guard note`
  </action>
  <verify>python3.6 tests/run_gates.py green BEFORE gui_plot.py exists (inert entries); python3.6 -c "import sys; sys.path.insert(0,'tools'); import check_purity; print(check_purity.classify('serpentrum/gui_plot.py'), check_purity.classify('serpentrum/gui_spectra.py'))" prints `GUI GUI`</verify>
  <done>Allowlist is single-writer and inert-green; the app-guard rule is in the repo rulebook.</done>
</task>

<task type="auto">
  <name>Task 2: gui_plot.py — IrPlotWidget, paint_scene seam, SpectraPlotPanel, render_image</name>
  <files>serpentrum/gui_plot.py</files>
  <action>
    Create serpentrum/gui_plot.py (GUI class: `from pymol.Qt import QtWidgets, QtCore, QtGui` ONLY as the pymol-family import; `from . import plot_logic`; stdlib os fine; %-formatting; python3.6; NO pymol.cmd, NO .exec_(), NO module-level state; module docstring: data-in plot widget — Scenes from plot_logic, one painter seam, route-A PNG save).

    1. Module-level `paint_scene(painter, rect, scene, show_labels=True)` — THE seam (used identically by paintEvent and render_image):
       - painter.setRenderHint(QtGui.QPainter.Antialiasing, True) (volume.py:266 — ON for widget AND image painters alike).
       - Empty scene (scene is None): draw the axes frame + centered hint 'run a calculation to plot a spectrum' (drawText rect-center, AlignCenter) and return. (A real zero-curve scene — spectra.py:465-466 pin — paints as a flat baseline on a normal axis, NOT the hint.)
       - Margins: fm = QtGui.QFontMetrics(painter.font()); left = max y-tick label width (fm.width over scene.y_ticks labels) + fm.averageCharWidth() * 2; bottom = fm.height() + 6; top = fm.height() / 2 + 4; right = 12 (start from dynoplot's constants :77-80, widen left via QFontMetrics per volume.py:205-207 — NEVER fixed-35px assumptions).
       - Plot rect: rect.adjust(left, top, -right, -bottom). Axes: drawLine x-axis and y-axis (dark gray). If show_labels: ticks — short 4px strokes at mapped positions + aligned drawText labels (dynoplot.py:111-126 forms: x labels Qt.AlignHCenter|Qt.AlignTop under the axis; y labels Qt.AlignVCenter|AlignRight left of the axis) with the volume.py dodge (track last drawn x pixel; skip a label when `x - lastx <= fm.width(label) + 2 * fm.averageCharWidth()`); axis titles only when show_labels: x title centered below the x-tick labels ('wavenumber (cm-1)'), y title rotated: painter.save(); translate to left-middle; rotate(-90); drawText centered ('IR intensity (km/mol)'); painter.restore().
       - Curve: map scene.xs/ys into the plot rect (value->pixel: px = left + (x - x_min) / (x_max - x_min) * plot_w; py = bottom_edge - (y / y_max) * plot_h) into ONE QtGui.QPolygonF; painter.save(); painter.setClipRect(plot_rect) (volume.py:269-272); blue QPen width 2; drawPolyline; painter.restore().
       - ALL mapping math reads the scene ONLY — no broaden, no tick recompute (pitfall 2).
    2. `class IrPlotWidget(QtWidgets.QWidget)`:
       - __init__: self._scene = None; self._show_labels = True; setMinimumSize(320, 260) (generous floor per [TRAIN] 3 — human-verified at 07-06).
       - sizeHint() -> QtCore.QSize(600, 200) (volume.py:98-99 precedent).
       - set_scene(scene): store + self.update() (dynoplot.py:320).
       - set_show_labels(flag): store + self.update().
       - paintEvent(event): painter = QtGui.QPainter(); painter.begin(self); paint_scene(painter, self.rect(), self._scene, self._show_labels); painter.end(). No pixmap double-buffer (both precedents skip it).
    3. `class SpectraPlotPanel(QtWidgets.QWidget)` — the piece the tab embeds:
       - __init__(self, parent=None, status_cb=None): status_cb is an optional callable(str) the TAB sets for save reporting (the panel has no status label of its own — 07-RESEARCH-qt-plot.md design).
       - Control row: size combo populated from plot_logic.size_presets() via addItem(label, data); currentIndexChanged -> self._on_size_changed (findData-free: currentData() gives (w, h)); 'Show axis labels' QCheckBox default checked; 'Save Plot (PNG)' QPushButton.
       - IrPlotWidget below, stretch 1.
       - _on_size_changed(w, h): self.plot.setMinimumSize(max(w, 320), max(h, 260)); self.update(); (the scene is x-range data — resize-responsive by construction, NO recompute).
       - _on_toggle(flag): self.plot.set_show_labels(bool(flag)).
       - _on_save(): scene = self.plot._scene; if scene is None: report 'nothing to save - run a calculation first' via status_cb and return; path, _ = QtWidgets.QFileDialog.getSaveFileName(self, 'Save spectrum plot', '', 'PNG image (*.png)'); if not path: return (cancel — gui_setup.py:403 guard); if not path.lower().endswith('.png'): path += '.png'; try: img = render_image(scene, (self.plot.width(), self.plot.height()), scale=2, show_labels=self.plot._show_labels); ok = img.save(path, 'PNG') except Exception as exc: report 'plot save failed: %s' % exc via status_cb; return; report 'plot saved: %s' % path via status_cb when ok else 'plot save failed (Qt returned False)'.
       - set_scene(scene): forward to self.plot.set_scene (+ update).
    4. Module-level `render_image(scene, logical_size, scale=2, show_labels=True)`: EXACTLY the research-verified shape — QtGui.QImage(w * scale, h * scale, QtGui.QImage.Format_RGB32); img.fill(QtGui.QColor('white')); img.setDevicePixelRatio(float(scale)); painter = QtGui.QPainter(img); paint_scene(painter, QtCore.QRect(0, 0, w, h), scene, show_labels); painter.end(); return img. (Called ONLY where a QApplication exists — real GUI, or the smoke's guarded construct.)
    5. Route B comment (ONE comment, no code): `# Fallback (NOT shipped, research [TRAIN] 1): QWidget.grab() renders from widget backing state and is unreliable for a never-exposed tab page — route A (render_image) needs no widget at all.`
    Run gates. Commit: `feat(07-05): gui_plot — IrPlotWidget + paint_scene seam + SpectraPlotPanel + route-A render_image (SPECTRA-03)`
  </action>
  <verify>python3.6 tests/run_gates.py green (gui_plot now classified GUI by the Task-1 entry; syntax py3.6); AST assertions (python3.6 heredoc): ast.parse; assert module defines paint_scene, render_image, IrPlotWidget, SpectraPlotPanel; assert IrPlotWidget defines set_scene and paintEvent; assert NO '.exec_(' and NO 'PyQt5' import and NO 'pymol.cmd'/'import pymol' anywhere in the module; assert 'grab()' appears only inside a comment line (route-B note)</verify>
  <done>The plot widget + panel exist as thin Scene consumers; the save route is the probe-verified seam re-render; purity-legal by construction.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIRED smoke 12 (guarded-app renderer smoke) + tuple edit</name>
  <files>smoke/12_plot_smoke.py, tests/run_gates.py</files>
  <action>
    1. Create smoke/12_plot_smoke.py (house smoke template: defensive ROOT resolve; flush=True prints; sentinels one per stage; SMOKE-FAIL on any failure; NO exit-code reliance; NO widget construction — images only):
       - Stage 0 THE APP GUARD (load-bearing): `from pymol.Qt import QtWidgets, QtCore, QtGui`; app = QtWidgets.QApplication.instance(); if app is None: app = QtWidgets.QApplication([]); print 'STAGE0 APP' (probe stage3-verified pattern — constructing a second app when one exists warns; never do that).
       - Stage 1: spectrum = spectra.parse_g98(ROOT/tests/fixtures/xtb/g98.out); scene = plot_logic.build_scene(spectrum.modes); print counts (72 modes / 3 imaginary).
       - Stage 2: img = gui_plot.render_image(scene, (800, 500), scale=2); tmp = os.path.join(tempfile.gettempdir(), 'srp_smoke12_plot.png'); ok = img.save(tmp, 'PNG'); assert ok; read bytes — assert starts with PNG magic b'\\x89PNG'; reload = QtGui.QImage(tmp); assert reload.width() == 1600 and reload.height() == 1000; print sentinel with file size.
       - Stage 3 (axis-toggle leg): img2 = gui_plot.render_image(scene, (640, 400), scale=2, show_labels=False); save + magic-byte assert; print sentinel.
       - Stage 4 (empty-scene leg): img3 = gui_plot.render_image(None, (640, 400), scale=2); save + magic-byte assert (the hint path renders, never crashes); print 'SMOKE-OK PLOT-RENDER'. Cleanup: remove the temp PNGs (guarded try/except — python os.remove is fine in the Windows runtime).
    2. tests/run_gates.py REQUIRED_SMOKES: append 'smoke/12_plot_smoke.py' with comment `# 12 = plot renderer via the paint_scene seam (Phase 7, plan 07-05) — needs the guarded QApplication([]) construct (probe RUN A: fonts without an app hard-kill the process)`.
    3. Run gates, then the smoke leg: `python3.6 tests/run_gates.py --smoke` — verdict = flushed SMOKE-OK sentinels (NEVER exit codes). (Phase-6 smokes 09-11 may be absent until Phase 6 merges; if the required-set mismatch blocks the full --smoke run, verify this smoke directly: `timeout 90 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\12_plot_smoke.py"` and read the sentinels.)
    4. Confirm the throwaway probe is gone (07-02 deleted smoke/tmp_research_07_plot_probe.py): `ls smoke/` must not list it. If it somehow reappeared, delete it the same way (python3.6 -c "import os; os.remove(...)") — NEVER leave it dangling.
    Commit: `test(07-05): REQUIRED smoke 12 — guarded-app plot renderer regression (route A)`
  </action>
  <verify>python3.6 tests/run_gates.py green; direct smoke run flushes STAGE0..STAGE4 + 'SMOKE-OK PLOT-RENDER' (never SMOKE-FAIL); grep run_gates.py contains '12_plot_smoke'; smoke/ lists no tmp_research_07_* file</verify>
  <done>The renderer + save route are regression-pinned headlessly; the probe's findings live on as a required smoke and an AGENTS.md rule.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green.
- `timeout 90 cmd.exe /c "C:\src\run-conda-pymol.bat -cq smoke\12_plot_smoke.py"` from repo root flushes SMOKE-OK PLOT-RENDER.
- AST/grep audit: classify('serpentrum/gui_plot.py') == 'GUI'; no pymol.cmd / PyQt5 / exec_ in gui_plot.py; REQUIRED_SMOKES contains both 12 and 13 (13 from 07-03).
- On-screen behavior (panel in a real GUI) is human-verified at 07-06 — this plan's bar is structural green + the headless PNG proof.
</verification>

<success_criteria>
- One painter seam serves screen and PNG identically (route A); the renderer is headless-verified incl. fonts (guarded app) and the empty-scene hint path.
- The v1 adjustment surface is exactly size presets + axis-label toggle; grab() is a documented non-route.
- Both new Spectra GUI modules are registered single-writer; the repo rulebook carries the hard-kill lesson.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-05-SUMMARY.md`
</output>
