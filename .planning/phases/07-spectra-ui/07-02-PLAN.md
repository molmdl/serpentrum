---
phase: 07-spectra-ui
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - serpentrum/plot_logic.py
  - tests/test_plot_logic.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "build_scene wraps spectra.broaden into a paint-ready Scene: the pinned zero-curve case (empty modes) yields a FLOORED y_max so the axis never has zero range (no tick-division blowup), x/y ticks are precomputed (value, label) pure data, and axis labels are ASCII ('wavenumber (cm-1)' / 'IR intensity (km/mol)')"
    - "nice_ticks selects classic 1/2/2.5/5x10^n steps (anchors: (0, 3600, 8) -> step 500; (0, 10, 4) -> step 2.5 with 1-decimal labels) and survives zero-range and negative-low inputs without crashing"
    - "The plot ships an ASCENDING wavenumber axis in v1 — an owner-signable convention decision (chemistry plots often run 4000->400; the flip is a v2 data-only change); NO FWHM control in v1 (SPECTRA-03 'minimal adjustments' = size presets + axis-label toggle only)"
    - "size_presets are pure (label, (w, h)) data the GUI renders via the house addItem(label, data) combo pattern — no widget code in this module"
  artifacts:
    - path: serpentrum/plot_logic.py
      provides: "PURE plot half: Scene namedtuple, build_scene (broaden wrapper + y_max floor + tick/label assembly), nice_ticks, size_presets, mode_caption"
      min_lines: 90
    - path: tests/test_plot_logic.py
      provides: "fixture-driven scene anchors (72 modes, 3 imaginary, peak within +-fwhm of 1150.6639), empty-scene floor pins, tick-selection matrix incl. 2.5-step and zero-range, preset/caption sanity"
      min_lines: 110
  key_links:
    - from: "serpentrum/plot_logic.py build_scene"
      to: "serpentrum/spectra.py broaden (spectra.py:433-475)"
      via: "the committed, fixture-pinned broadener — called with the LIVE setup fwhm (setup_logic.DEFAULTS['broadening_fwhm'] read by the caller), never a re-pinned constant"
      pattern: "broaden"
    - from: "serpentrum/plot_logic.py Scene"
      to: "serpentrum/gui_plot.py paint_scene (plan 07-05)"
      via: "the Scene is the ONLY thing the GUI paints — paint maps data to pixels, never recomputes (paintEvent-computing pitfall, 07-RESEARCH-qt-plot.md pitfall 2)"
      pattern: "Scene"
---

<objective>
Create the PURE module `serpentrum/plot_logic.py`: the broadened-IR plot's data half — a paint-ready `Scene` built over the committed `spectra.broaden`, classic nice-tick selection, size-preset data, and an imaginary-mode caption — TDD'd in WSL python3.6 with zero stubs.

Purpose: keep ALL plotting math/tick logic out of the GUI module (07-05's `paint_scene` only maps Scene -> pixels), so the renderer stays O(points) and every numeric decision is unit-testable (07-RESEARCH-qt-plot.md "Recommended design": pure/GUI split follows the hud_logic house pattern).
Output: one new pure module + its test file. No existing module modified.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spectra-ui/07-RESEARCH-qt-plot.md
@serpentrum/spectra.py
@tests/fixtures/xtb/g98.out

Key facts (research file:line anchors):
- `broaden(modes, fwhm=16.0, x_min=0.0, x_max=None, n_points=800) -> (xs, ys)` (spectra.py:433): grid [x_min, max(3600.0, max_freq + 5*sigma)] both ends inclusive; sigma = fwhm/2.35482 at runtime; EMPTY modes -> `(grid, [0.0]*n_points)` zero curve, never an exception (:465-466); negatives summed (invisible tail 2.9e-5 of peak at x=0); zero-intensity modes contribute exactly 0 (:473); raises ValueError on fwhm<=0 / n_points<2 (:450-455).
- Fixture anchors (02-12-PLAN.md:74-77 + spectra.py:12-15 docstring): g98.out = 72 modes; max-intensity 1150.6639 cm-1 @ 257.1018 km/mol (isolated — nearest sizable neighbor 41 cm-1 away); default grid exactly [0.0, 3600.0]; negatives at modes 1-3 (−31.9175/−23.0766/−18.1086 → n_imaginary == 3).
- fwhm source: `setup_logic.DEFAULTS['broadening_fwhm']` = 16.0 (setup_logic.py:57; validated > 0 at setup_logic.py:269-271). The CALLER (07-08's tab) reads it live from anchor.setup; plot_logic takes fwhm as a plain parameter — never imports setup_logic.
- Painter precedents the Scene feeds (07-RESEARCH-qt-plot.md): pmg_qt/volume.py:252-274 (paintEvent, antialiasing, QFontMetrics margins), dynoplot.py:68-130 (tick drawText forms), volume.py:215-223 (tick-label dodge). Scene carries x_ticks/y_ticks as (value, label_str) pairs so the GUI maps values to pixels with zero math beyond scaling.
- Pitfall 5 (07-RESEARCH-qt-plot.md): y-axis zero-range on the empty/zero curve — `max(ys) == 0.0` breaks tick math; the y_max floor is THIS plan's job.
- [TRAIN] 4 (axis convention) RESOLVED for v1: ship ASCENDING (mathematically native; broaden's grid is ascending). Chemistry-conventional descending (4000→400) is an owner-signable flip at the 07-06/07-10 checkpoints — if the owner asks, it is a v2 data-only change; NO reverse_x flag is built now (an untested flag is v2 bait).
- [TRAIN] 2 (glyph coverage) → ASCII only: labels 'wavenumber (cm-1)', 'IR intensity (km/mol)'; caption uses 'below 0 (imaginary)' phrasing; no U+2212/U+207B anywhere.
- Cross-half coordination (pinned at planning): freq_label lives in spectra_ui (07-01, same wave). plot_logic deliberately imports NO frequency formatter and NO spectra_ui symbol — the v1 plot displays no raw frequency values (ticks are plain wavenumbers; the caption counts imaginary modes). This keeps both pure modules wave-1 parallel with zero same-wave imports. Do NOT hand-roll a frequency formatter here.
- python3.6 syntax, %-formatting only; stdlib math allowed.

RESOLVED DECISIONS owned by this plan:
- Y_MAX_FLOOR = 1.0 (km/mol): y_max = max(max(ys) * 1.1, Y_MAX_FLOOR) — the empty-spectrum case gets a sane 0..1 axis instead of a zero-range blowup.
- Tick targets: x target 8, y target 5 (passed to nice_ticks by build_scene).
- Scene fields (planner-pinned contract for 07-05): xs, ys, x_min, x_max, y_max, x_ticks, y_ticks, x_label, y_label, n_modes, n_imaginary, fwhm.
- size presets (v1): ('medium (640x400)', (640, 400)), ('large (800x500)', (800, 500)), ('wide (960x500)', (960, 500)) — 'medium' is the default (first entry).
- NO QWidget.grab() route, NO zoom/pan, NO FWHM control — the panel's only adjustments are the size combo + axis-label toggle (SPECTRA-03 'minimal adjustments', research "final cut" recommendation).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — failing tests for plot_logic</name>
  <files>tests/test_plot_logic.py</files>
  <action>
    Create tests/test_plot_logic.py (house header: sys.path self-insert; unittest; %-formatting). Import `from serpentrum import plot_logic, spectra` (module absent -> ImportError = RED). G98_PATH = tests/fixtures/xtb/g98.out.

    Class TestNiceTicks:
    - test_anchor_3600: ticks = nice_ticks(0.0, 3600.0, 8); the step (ticks[1][0] - ticks[0][0]) == 500.0; ticks[0][0] == 0.0; last tick <= 3600.0; len(ticks) in range(6, 10); all labels ASCII.
    - test_two_point_five_step: nice_ticks(0.0, 10.0, 4) -> step 2.5 with 1-decimal labels: [t for t, _ in ticks] == [0.0, 2.5, 5.0, 7.5, 10.0] and labels '0.0','2.5','5.0','7.5','10.0'.
    - test_subunit_step: nice_ticks(0.0, 1.0, 5) -> step 0.2, labels one decimal ('0.0','0.2',...).
    - test_zero_range: nice_ticks(5.0, 5.0, 8) returns exactly one tick whose value == 5.0 (never a division-by-zero).
    - test_negative_low: nice_ticks(-100.0, 900.0, 5) -> all tick values within [-100, 900], strictly ascending, uniform step 200.0.
    - test_labels_match_step_precision: for a step < 1 the labels carry the step's decimal count (no '0' where '0.2' belongs).

    Class TestBuildScene:
    - test_fixture_scene: spectrum = spectra.parse_g98(G98_PATH); scene = plot_logic.build_scene(spectrum.modes); scene.n_modes == 72; scene.n_imaginary == 3; scene.x_min == 0.0; scene.x_max == 3600.0; len(scene.xs) == 800 == len(scene.ys).
    - test_peak_localization: argmax(ys) falls within [1150.6639 - 16.0, 1150.6639 + 16.0] in x (the isolated 257.1018 km/mol mode dominates — 02-12-PLAN.md:74-75 anchors); scene.ys[argmax] >= 250.0.
    - test_y_max_headroom: scene.y_max == max(scene.ys) * 1.1 (within 1e-9) and scene.y_max >= scene.ys[argmax].
    - test_empty_modes_floor: scene = plot_logic.build_scene([]); all(y == 0.0 for y in scene.ys) (broaden's pinned zero curve); scene.y_max == plot_logic.Y_MAX_FLOOR (1.0) — NEVER 0; scene.n_modes == 0; scene.n_imaginary == 0; x/y ticks non-empty (sane axis on the empty case).
    - test_ticks_within_range: every x tick value in [scene.x_min, scene.x_max]; every y tick value in [0.0, scene.y_max]; both strictly ascending.
    - test_ascii_labels: scene.x_label == 'wavenumber (cm-1)'; scene.y_label == 'IR intensity (km/mol)'; both .isascii().
    - test_fwhm_passthrough: build_scene(spectrum.modes, fwhm=32.0) differs from the fwhm=16 scene at the peak shoulders (ys lists not equal) — fwhm is a live parameter, never pinned here; scene.fwhm == 32.0.
    - test_invalid_fwhm_raises: build_scene([], fwhm=0.0) raises ValueError (broaden's contract surfaces, not swallowed).

    Class TestSizePresets:
    - test_shape: presets = plot_logic.size_presets(); len >= 2; first entry is the default ('medium (640x400)', (640, 400)); every entry is (str, (int, int)) with w >= 640 and h >= 400; labels ASCII.

    Class TestModeCaption:
    - test_imaginary_caption: caption = plot_logic.mode_caption(scene with n_modes=72, n_imaginary=3); == '72 modes; 3 below 0 (imaginary) - see the table'; ASCII.
    - test_no_caption_when_none_imaginary: mode_caption(scene with n_imaginary=0) == ''.

    Run: `python3.6 -m unittest discover -s tests -p "test_plot_logic.py" -v` -> ImportError (RED).
    Commit RED: `test(07-02): failing tests for plot_logic (scene builder, nice ticks, presets, caption)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_plot_logic.py" -v fails with ImportError; no other test file touched</verify>
  <done>RED confirmed: the plot's pure contract exists as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement serpentrum/plot_logic.py</name>
  <files>serpentrum/plot_logic.py</files>
  <action>
    Create serpentrum/plot_logic.py — PURE module (stdlib math + `from . import spectra` only; %-formatting; python3.6). Module docstring: the plot's data half (07-RESEARCH-qt-plot.md "Module layout"); the GUI (07-05) paints Scenes and never computes.

    1. `Scene = namedtuple('Scene', 'xs ys x_min x_max y_max x_ticks y_ticks x_label y_label n_modes n_imaginary fwhm')` — docstring: x_ticks/y_ticks are (value, label) pairs; paint maps values to pixels (never recomputes).
    2. `Y_MAX_FLOOR = 1.0` (km/mol) with the zero-curve docstring (spectra.py:465-466 pinned empty case + research pitfall 5).
    3. `nice_ticks(lo, hi, target=8)`:
       - if hi <= lo: return [(float(lo), _fmt(float(lo), None))] (single-tick zero-range).
       - raw = (hi - lo) / float(target); mag = 10.0 ** math.floor(math.log10(raw)); step = first of (1, 2, 2.5, 5, 10) * mag that is >= raw (classic 1/2/2.5/5x10^n).
       - ticks = [k * step for integer k in range(ceil(lo/step), floor(hi/step)+1)] (all within [lo, hi], ascending).
       - `_fmt(value, step)`: decimals = 0 when step is None or float(step).is_integer(); else count decimals of step (2.5 -> 1, 0.2 -> 1, 0.25 -> 2; loop-multiply by 10 up to 6 times); return ('%.' + str(decimals) + 'f') % value. Labels carry the STEP's precision, never the values'.
    4. `build_scene(modes, fwhm=16.0, x_min=0.0, x_max=None, n_points=800)`:
       - xs, ys = spectra.broaden(modes, fwhm=fwhm, x_min=x_min, x_max=x_max, n_points=n_points) (ValueError propagates — the caller's fwhm is validated upstream by setup_logic).
       - peak = max(ys) if ys else 0.0; y_max = max(peak * 1.1, Y_MAX_FLOOR).
       - x_ticks = nice_ticks(float(x_min... use xs[0]), float(xs[-1]), 8); y_ticks = nice_ticks(0.0, y_max, 5).
       - labels: x_label = 'wavenumber (cm-1)', y_label = 'IR intensity (km/mol)' (ASCII per [TRAIN] 2).
       - n_modes = len(modes); n_imaginary = sum(1 for m in modes if m.freq < 0.0).
       - Return Scene(...). Docstring: v1 ships an ASCENDING wavenumber axis (owner-signable; a descending flip is a v2 data-only change — no flag built); the fwhm arrives from setup_logic.DEFAULTS['broadening_fwhm'] read LIVE by the caller (07-08) — never re-pinned here.
    5. `size_presets()`: return the pinned v1 list [('medium (640x400)', (640, 400)), ('large (800x500)', (800, 500)), ('wide (960x500)', (960, 500))] — pure data for the house addItem(label, data) combo pattern (gui_setup.py:106-108 precedent).
    6. `mode_caption(scene)`: '' when scene.n_imaginary == 0; else '%d modes; %d below 0 (imaginary) - see the table' % (scene.n_modes, scene.n_imaginary). Docstring: educational framing only — the TABLE owns the rows (SPECTRA-05); the plot never prints raw frequencies in v1 (no freq formatter here by design — the single shared one is spectra_ui.freq_label).
    7. Run the file suite green, then full gates: `python3.6 tests/run_gates.py`.
    8. PROBE DISPOSITION (this is the FIRST plan touching the plot half, so it owns this): the researcher's throwaway `smoke/tmp_research_07_plot_probe.py` is UNTRACKED and shell `rm` is denied in this environment — delete it via `python3.6 -c "import os; os.remove('smoke/tmp_research_07_plot_probe.py')"`. Rationale: its probe evidence is preserved verbatim in 07-RESEARCH-qt-plot.md and is superseded by the REQUIRED smoke 12 (plan 07-05, `smoke/12_plot_smoke.py`) which re-proves the same facts as a regression. NEVER leave the probe dangling.
    9. Commit GREEN: `feat(07-02): plot_logic — Scene builder, nice ticks, size presets, caption (SPECTRA-03 pure half)`
  </action>
  <verify>python3.6 -m unittest discover -s tests -p "test_plot_logic.py" -v -> all pass; python3.6 tests/run_gates.py -> full suite green; python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import plot_logic; print(plot_logic.Y_MAX_FLOOR, plot_logic.size_presets()[0])" prints `1.0 ('medium (640x400)', (640, 400))`; `ls smoke/` no longer lists tmp_research_07_plot_probe.py</verify>
  <done>plot_logic.py exists (pure), Scene/ticks/presets/caption pinned by tests incl. the zero-curve floor, gates green.</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py` green (new pure module auto-classified PURE — no registration edit).
- `python3.6 -c "import sys; sys.path.insert(0,'.'); from serpentrum import plot_logic, spectra; s = spectra.parse_g98('tests/fixtures/xtb/g98.out'); sc = plot_logic.build_scene(s.modes); print(sc.n_modes, sc.n_imaginary, sc.x_max, len(sc.x_ticks))"` prints `72 3 3600.0 8`.
- `grep -c "exec_\|import pymol\|from pymol\|import numpy" serpentrum/plot_logic.py` == 0.
</verification>

<success_criteria>
- The Scene is complete paint-ready data (ticks precomputed, floored y_max, ASCII labels) so 07-05's paint_scene is a pure value->pixel map.
- Tick selection is total (zero-range, negative-low, 2.5-steps) and fixture-anchored; the empty-spectrum case produces a sane axis, never a crash or a zero-range axis.
- The ascending-axis v1 decision and the no-FWHM v1 scope are pinned in code + docstring, ready for owner sign-off at the checkpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-02-SUMMARY.md`
</output>
