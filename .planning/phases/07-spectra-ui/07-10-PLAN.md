---
phase: 07-spectra-ui
plan: 10
type: execute
wave: 6
depends_on: ["07-09"]
files_modified: []
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Full gates green including BOTH new REQUIRED smokes (12 plot renderer, 13 mode arrows + g98≡xtbopt overlay assertion) and the --xtb leg — the phase's headless proof is complete"
    - "The complete live flow is human-verified in real PyMOL: Get Spectra → dialog switches to the Spectra tab → calculation progress streams live into the log panel → cancel + Run again work → the broadened IR plot renders with axis labels and adjustable size → a table row click draws that mode's static vectors on srp_xtbopt → Save Plot writes a PNG that opens OUTSIDE PyMOL [SPECTRA-01/03/04/05]"
    - "The OPTIMIZED-frame interpretation is OWNER-SIGNED-OFF: mode vectors draw on srp_xtbopt (record['xtbopt_path']), not the game frame (probe: g98 ≡ xtbopt within 1e-6 Å; game frame wrong by up to 0.14 Å) — an amendment here is a small, pinned edit surface (07-09's load target + status line)"
    - "Every research [TRAIN] item is discharged or explicitly deferred with an owner-visible note (axis convention, glyph legibility, min-height readability, QPlainTextEdit streaming, tab-switch repaint, zoom framing, g98≡xtbopt generality via the always-on smoke)"
  artifacts: []
  key_links:
    - from: "07-10 checkpoint verdict"
      to: ".planning/phases/07-spectra-ui/07-10-SUMMARY.md"
      via: "the consolidated verdict + any [TRAIN] dispositions recorded for the verifier (the 05-16/06-12 house shape)"
      pattern: "07-10-SUMMARY"
---

<objective>
Close Phase 7: run the complete gate battery (syntax/purity/unittest + all required smokes + xtb leg), then the consolidated human-verify checkpoint covering the live end-to-end flow, the optimized-frame interpretation sign-off, and the [TRAIN] discharge list.

Purpose: the phase's three success criteria are user-reachable behaviors — only a human in the real GUI can certify them (01-05 decision: GUI verdicts are human-verify). The house shape is 05-16/06-12: gates first, then a consolidated checkpoint with numbered steps.
Output: green gates + a recorded human verdict (+ fix plans if defects surface).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-spectra-ui/07-RESEARCH-qt-plot.md
@.planning/phases/07-spectra-ui/07-RESEARCH-spectra-seam.md
@.planning/research/PITFALLS.md

Key facts:
- Gate battery (tests/run_gates.py + AGENTS.md): `python3.6 tests/run_gates.py` (syntax+plugin-path, purity, scoped unittest), `--smoke` (REQUIRED smokes now 01, 03-08, 10-13 — verdicts from flushed SMOKE-OK sentinels ONLY, never exit codes), `--xtb` (Windows xtb from WSL, asserts 'xtb version' + 'normal termination').
- Live-flow prerequisites in the real GUI: a completable game (small cap for speed — the dimer-class run is seconds per PITFALLS.md's measured table), Phase 6's launch pipeline (06-09) behind Get Spectra, the Phase-7 tab (07-07..07-09). The runner verdicts ('ok'/'failed'/'cancelled') drive the tab's populate path.
- Checkpoint coverage map: SC1 (Get Spectra → tab switch → live streaming) = SPECTRA-01/04; SC2 (broadened plot, axis labels, adjustable size, save-PNG outside PyMOL) = SPECTRA-03; SC3 (every-mode table, -31.9i negatives, zero-intensity rows, row-click vectors) = SPECTRA-05.
- [TRAIN] discharge list (plot research 1-5 + seam research): (1) QWidget.grab() — moot (route A shipped); (2) ASCII glyph legibility in the real GUI; (3) min-height/font readability; (4) ascending-axis sign-off (second + final look); (5) tab-switch repaint sanity; (6) QPlainTextEdit streaming behavior live; (7) zoom-once framing looks sane; (8) g98≡xtbopt generality — covered by smoke 13's always-on assertion (note the one-fixture origin in the summary).
- Known limitation to record (07-01): no co2 g98 fixture exists — the linear-molecule g98 table/vector leg is smoke-only behind --xtb real runs; zero-intensity coverage is unit-level (synthetic tuples + synthetic co2 vibspectrum).
- Failure handling: any checkpoint defect → a numbered fix list → targeted fix plans → THIS checkpoint re-runs (the 05-16 7-round precedent). Never approve with a known-broken step.
</context>

<tasks>

<task type="auto">
  <name>Task 1: full gate battery</name>
  <files></files>
  <action>
    From the repo root in WSL, run in order and CAPTURE the summaries:
    1. `python3.6 tests/run_gates.py` — syntax+plugin-path, purity, unittest (expect ~640+ tests with the Phase-7 additions).
    2. `python3.6 tests/run_gates.py --smoke` — every REQUIRED smoke passes via flushed SMOKE-OK sentinels (12 = plot renderer incl. the guarded QApplication + empty-scene + axis-off legs; 13 = mode arrows + the <1e-3 Å g98≡xtbopt assertion). Informational smokes are reported, never blocking.
    3. `python3.6 tests/run_gates.py --xtb` — version + normal-termination contract.
    If any leg fails: fix or spawn a targeted fix plan FIRST (checkpoint only runs on green gates) — a GUI-visual failure does NOT block, but a gate failure does.
    Record the three summaries verbatim in the SUMMARY. No commit unless fixes were needed.
  </action>
  <verify>All three gate runs report 'all gates green' (default) and the sentinel/xtb notes show PASS for every required leg; captured outputs stored in the summary notes</verify>
  <done>The phase's headless proof is complete: renderer, overlay identity, and the xtb contract all pinned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — consolidated live flow + optimized-frame sign-off + [TRAIN] discharges</name>
  <what-built>The complete Spectra payoff screen on top of the Phase-6 pipeline: live SpectraTab (status + streaming log + contextual run control), broadened IR plot (size presets + axis-label toggle + route-A PNG save), every-mode frequency table, and row-click static mode vectors on the optimized structure.</what-built>
  <how-to-verify>
    In a REAL Windows PyMOL GUI with the plugin loaded:
    1. Configure a SMALL game (Setup tab, low win cap) and play to completion (win or crash) — Get Spectra activates.
    2. Click "Get Spectra": the dialog switches to the Spectra tab (SPECTRA-01) and the launch line/counts appear in the tab's status + the game info box.
    3. Watch the log panel STREAM lines live while xtb runs (SPECTRA-04) — the dialog and viewer stay responsive throughout.
    4. Mid-run, click the tab's run button ('Cancel xtb run') — the run cancels with a clear line; click again ('Run again') — the FULL pipeline re-runs (counts re-checked, Get Spectra disarmed, fresh run).
    5. On completion ('ok'): the broadened IR spectrum renders with axis labels ('wavenumber (cm-1)', 'IR intensity (km/mol)'), ascending x-axis, and the caption notes imaginary modes; the frequency table lists 72 modes with rows 1-3 shown as -31.9i / -23.1i / -18.1i (SPECTRA-03/05).
    6. Exercise the plot: switch size presets (reflow), toggle axis labels, then Save Plot → pick a Windows path → open the PNG OUTSIDE PyMOL — it matches the screen (SPECTRA-03).
    7. Click a table row (try a strong-intensity mode, then mode 1 = -31.9i): displacement arrows appear on the OPTIMIZED structure (srp_xtbopt, sticks) in the viewer; clicking another row REPLACES the arrows; the camera frames once, not per click (SPECTRA-05).
    8. SIGN-OFF — optimized-frame interpretation: vectors are drawn on the OPTIMIZED structure (not the game frame). Research basis: g98 coordinates ≡ xtbopt.xyz within 1e-6 Å (probe); the game frame differs by up to 0.14 Å. Approve or amend (an amendment = vectors on a different frame — small pinned edit).
    9. SIGN-OFF — [TRAIN] discharges: (a) ascending axis accepted for v1; (b) ASCII labels legible; (c) plot text readable at default size; (d) tab switching repaints cleanly; (e) log streaming smooth; (f) zoom framing of molecule+arrows sane.
    10. Restart hygiene (optional but recommended): start a NEW game (begin_game sweeps srp_*) — srp_xtbopt/srp_mode_vec are gone and the Spectra tab degrades gracefully (clear lines, empty table/plot).
  </how-to-verify>
  <resume-signal>Type "approved" (with the two sign-offs noted), or list the numbered defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- All three gate runs green; sentinel evidence captured.
- Human verdict recorded with both sign-offs; SPECTRA-01/03/04/05 user-reachable end-to-end in the real GUI.
- Phase-7 wrap-up can proceed: REQUIREMENTS rows + verifier hand-off per house flow.
</verification>

<success_criteria>
- Phase 7's three success criteria are TRUE and human-certified; every research [TRAIN] item has an owner-visible disposition; the phase is ready for /gsd-verify-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spectra-ui/07-10-SUMMARY.md`
</output>
