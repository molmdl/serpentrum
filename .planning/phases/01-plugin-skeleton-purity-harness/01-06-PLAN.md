---
phase: 01-plugin-skeleton-purity-harness
plan: 06
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - AGENTS.md
  - .planning/research/PITFALLS.md
autonomous: false

must_haves:
  truths:
    - "Plugin installs via the plugin path and a single 'serpentrum' menu item opens the 3-tab dialog — human-verified in real Windows PyMOL [SETUP-01]"
    - "Double-open and Plugin-Manager reload still yield exactly one dialog with live controls — human-verified [INFRA-03]"
    - "PyMOL's viewer and command line stay responsive while the dialog is open — human-verified [INFRA-05]"
    - "The full gate suite is green end-to-end: default WSL gates + --smoke + --xtb"
    - "AGENTS.md documents the gate commands, plugin-path safety rules, true module name, and modeless rule for every future phase"
  artifacts:
    - path: "AGENTS.md"
      provides: "Gates & environment contract section (run_gates commands, plugin-path safety, pmg_tk.startup.serpentrum naming, modeless rule)"
      min_lines: 15
      contains: "tests/run_gates.py"
    - path: ".planning/research/PITFALLS.md"
      provides: "dated correction note: loader module name is pmg_tk.startup.<name>"
      contains: "pmg_tk.startup"
  key_links:
    - from: "AGENTS.md gates section"
      to: "python3.6 tests/run_gates.py [--smoke|--xtb]"
      via: "canonical commands every later phase runs"
      pattern: "tests/run_gates\\.py"
---

<objective>
Close Phase 1: document the gate suite and plugin conventions in AGENTS.md, correct the research record's module-name wording, run the full gate suite end-to-end as the phase's machine-verifiable proof, and run the human-verify checkpoint for the three things headless runs cannot prove — install via plugin path, single instance under double-open + reload, and modelessness.

Purpose: SETUP-01/INFRA-03/INFRA-05 have irreducibly visual/human halves (fake menuBar swallows registrations headlessly; widget construction C-aborts). This plan is the phase's acceptance gate.

Output: updated AGENTS.md + PITFALLS.md note, full-gate evidence, human approval.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/01-plugin-skeleton-purity-harness/01-RESEARCH-skeleton.md
@.planning/phases/01-plugin-skeleton-purity-harness/01-RESEARCH-harness.md
@tests/run_gates.py

Human-verify steps come from research §3.3 (dev install: Plugin Manager → "Add plugin directory" → repo root → restart) and §5 (human-only rows: menu item visible, dialog visible/modeless, tab labels, double-open/reload single-instance).
WSL `/mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/serpentrum` maps to Windows `C:\Users\nglok\Desktop\WORKDIR\molmdl\serpentrum`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document gates + plugin conventions in AGENTS.md; correct PITFALLS.md module name</name>
  <files>AGENTS.md, .planning/research/PITFALLS.md</files>
  <action>
1. Append a new section to `AGENTS.md` (after the GSD workflow section) titled `## serpentrum plugin gates & conventions (Phase 1)`:
   - **Gates (WSL):** `python3.6 tests/run_gates.py` (syntax walk + plugin-path safety + AST purity + scoped unittest), `--smoke` (headless Windows PyMOL smokes via cmd.exe — verdict = flushed `SMOKE-OK` sentinels, NEVER exit codes), `--xtb` (Windows xtb from WSL: direct exec, /mnt/c cwd, bare relative args, assert "normal termination").
   - **Smoke one-liner:** `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\01_skeleton_smoke.py"` from repo root (no staging copy).
   - **Plugin-path safety (hard rules):** the dev install points PyMOL at the repo root → NO `__init__.py` in `tests/`, `smoke/`, or `tools/`; NO top-level `*.py` at the repo root; findPlugins would autoload either as plugins at GUI startup (enforced by run_gates gate 1).
   - **Module identity:** the plugin loads as `pmg_tk.startup.serpentrum` — `pymol.plugins.startup` is only an attribute alias; smokes/asserts key on the `pmg_tk` name; live state anchors on `pmg_tk.startup._serpentrum` (never module globals).
   - **Modeless rule:** main dialog via `.show()` only, never `.exec_()`; AST checker fails any `.exec_()` until a later phase adds an explicit child-dialog allowlist.
   - **Purity classes:** entry (`serpentrum/__init__.py`) lazy-imports pymol/pmg_tk inside functions only; GUI modules are an explicit allowlist in `tools/check_purity.py` (pymol.Qt only); everything else under `serpentrum/` is pure — zero pymol/Qt/numpy anywhere.
2. In `.planning/research/PITFALLS.md`, at the loader-identity pitfall (~line 172, which says plugins load as `pymol.plugins.startup.<name>`), append a dated correction note (do not rewrite history):
   `> CORRECTED 2026-09-06 (phase-1 research [RUN] probe): the actual sys.modules key is pmg_tk.startup.<name>; pymol.plugins.startup is an attribute alias to the same module object. See phases/01-plugin-skeleton-purity-harness/01-RESEARCH-skeleton.md § correction.`
Keep both edits tight — conventions, not essays.
  </action>
  <verify>`grep -c "tests/run_gates.py" AGENTS.md` ≥ 1; `grep -c "pmg_tk.startup" .planning/research/PITFALLS.md` ≥ 1 (the correction); no `__init__.py` created anywhere by this task</verify>
  <done>Every future phase inherits the gate commands and the trap-avoidance conventions from AGENTS.md; the research record no longer misleads on module identity</done>
</task>

<task type="auto">
  <name>Task 2: Full gate suite end-to-end (phase machine-proof)</name>
  <files>tests/run_gates.py</files>
  <action>
Run the complete suite from repo root and capture evidence to `/tmp/opencode/phase1_gates/` (create with mkdir; no cleanup needed — /tmp/opencode is scratch):
1. `python3.6 tests/run_gates.py > /tmp/opencode/phase1_gates/default.txt 2>&1` → must exit 0 (syntax walk over serpentrum/tools/tests/smoke, safety checks, AST purity, full unittest discovery).
2. `python3.6 tests/run_gates.py --smoke > /tmp/opencode/phase1_gates/smoke.txt 2>&1` → must exit 0: required 01_skeleton_smoke green (SMOKE-OK SKELETON in output), 02_dialog_smoke reported informationally per its 01-05 verdict.
3. `python3.6 tests/run_gates.py --xtb > /tmp/opencode/phase1_gates/xtb.txt 2>&1` → must exit 0 with "normal termination".
4. `python3.6 tests/run_gates.py --smoke --xtb > /tmp/opencode/phase1_gates/all.txt 2>&1` → combined run, exit 0.
If ANY gate fails: fix the offending artifact (not the gate) and re-run until green. Record the three exit codes + one-line evidence in the task notes.
  </action>
  <verify>`grep -L "" /dev/null` no-op; concretely: `python3.6 tests/run_gates.py --smoke --xtb` exits 0 and its captured output contains "SMOKE-OK SKELETON" and "normal termination"</verify>
  <done>All gate combinations green in a single pass — the machine-verifiable half of phase success criteria 4 and 5</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — install, single instance, modeless dialog in real PyMOL</name>
  <files>AGENTS.md</files>
  <what-built>
The complete Phase 1 plugin: `serpentrum/` package (anchored single-instance entry + 3-tab modeless dialog shell), plus the full gate harness (purity checker, run_gates, headless smokes, xtb probe) — all machine gates green in Task 2. What remains is exactly what headless runs cannot prove: the visible menu item, the visible dialog, single-instance behavior, and viewer responsiveness.
  </what-built>
  <how-to-verify>
In Windows PyMOL 2.5.0 (GUI, not headless):
1. Plugin → Plugin Manager → "Add plugin directory" → select `C:\Users\nglok\Desktop\WORKDIR\molmdl\serpentrum` → OK. Restart PyMOL.
2. The Plugin menu shows EXACTLY ONE "serpentrum" item (under Plugin, not "Legacy Plugins").
3. Click it → a dialog titled "serpentrum" opens with exactly 3 tabs: Setup, Game, Spectra (placeholder text in each).
4. Modelessness: with the dialog open, rotate/zoom any molecule in the viewer AND type a command (e.g. `bg_color grey`) in the command line — both respond immediately.
5. Single instance: click the "serpentrum" menu item again → still exactly ONE dialog (raised/refocused, no duplicate).
6. Reload: Plugin → Plugin Manager → re-select the plugin directory (or restart PyMOL) → reopen via the menu → still exactly one dialog with responsive controls (anchor survived reload).
7. Optional: close the dialog, click the menu item again → it reopens without errors or duplicates.
  </how-to-verify>
  <resume-signal>Type "approved" — or describe exactly what failed (which step, what you saw) so the fix can be planned.</resume-signal>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py --smoke --xtb` → exit 0 (machine proof recorded)
- AGENTS.md contains the gates section; PITFALLS.md carries the dated module-name correction
- Human checkpoint: all 7 verification steps observed in real Windows PyMOL (steps 2–5 map to SETUP-01 / INFRA-05 / INFRA-03)
</verification>

<success_criteria>
- Phase 1 success criteria 1–3 confirmed by the human; criteria 4–5 confirmed by the gate suite
- Gate commands and safety conventions are codified in AGENTS.md for every later phase
- If the human reports failures, they are captured verbatim for gap-closure planning before the phase closes
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-skeleton-purity-harness/01-06-SUMMARY.md`
</output>
