---
phase: 01-plugin-skeleton-purity-harness
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - smoke/02_dialog_smoke.py
  - tools/winpath.py
  - tests/test_winpath.py
  - tests/run_gates.py
autonomous: true

must_haves:
  truths:
    - "Windows xtb is invocable from WSL (direct exec of the exe by WSL-style path, /mnt/c-backed cwd, bare relative args) and the gate asserts 'normal termination'"
    - "WSL→Windows path conversion exists as pure, unit-tested harness code (to_windows_path / to_windows_backslash)"
    - "The 3-tab dialog constructs and reports its tab labels in an offscreen Qt session when offscreen is supported; when it is not, the failure is reported as informational and never silently passes"
  artifacts:
    - path: "smoke/02_dialog_smoke.py"
      provides: "EXPERIMENTAL offscreen QApplication + PluginDialog construction smoke (informational in --smoke)"
      min_lines: 40
      contains: "QT_QPA_PLATFORM"
    - path: "tools/winpath.py"
      provides: "to_windows_path (C:/ form) + to_windows_backslash (C:\\\\ form), strict /mnt/<drive> validation"
      min_lines: 25
    - path: "tests/test_winpath.py"
      provides: "conversion rule tests (happy paths + ValueError on non-/mnt paths)"
      min_lines: 25
  key_links:
    - from: "tests/run_gates.py --xtb"
      to: "xtb-6.7.1/bin/xtb.exe --version"
      via: "direct WSL exec with cwd=repo root; assert 'xtb version' + 'normal termination'"
      pattern: "normal termination"
    - from: "tests/run_gates.py --xtb"
      to: "tools/winpath.to_windows_path"
      via: "conversion sanity check on repo root"
      pattern: "to_windows_path"
    - from: "smoke/02_dialog_smoke.py"
      to: "serpentrum/gui.py PluginDialog"
      via: "offscreen QApplication-first construction; tabs.count()==3 + tabText labels"
      pattern: "PluginDialog"
---

<objective>
Complete the environment contract (INFRA-01): wire the `--xtb` gate proving Windows xtb is invocable from WSL, add the tested WSL→Windows path-conversion helper, and attempt the one unverified mechanism — headless dialog construction via offscreen Qt — as an informational smoke that can never silently pass.

Purpose: The xtb probe + path rules are the verified pattern every later spectra phase builds on; the offscreen smoke converts an "unverified open question" into either a permanent cheap dialog check or a documented dead end.

Output: `smoke/02_dialog_smoke.py`, `tools/winpath.py`, `tests/test_winpath.py`, `--xtb` in `tests/run_gates.py`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/01-plugin-skeleton-purity-harness/01-RESEARCH-skeleton.md
@.planning/phases/01-plugin-skeleton-purity-harness/01-RESEARCH-harness.md
@tests/run_gates.py
@serpentrum/gui.py

Load-bearing verified facts (research Q3/Q4 — [RUN]-verified 2026-09-06):
- xtb pattern: from a `/mnt/c` cwd, exec the exe by WSL-style path with bare relative args: `timeout 60 ${ROOT}/xtb-6.7.1/bin/xtb.exe --version` → "xtb version 6.7.1pre …" + "normal termination of xtb", exit 0. The repo-root symlink `xtb-6.7.1 → /mnt/c/xtb-6.7.1` makes `${ROOT}/xtb-6.7.1/bin/xtb.exe` valid.
- Path rules: exe direct-exec → WSL-style path OK; cmd.exe targets → C:\... required; cwd must be /mnt/c-backed; file args → bare relative names, never absolute /mnt/c paths; the PLUGIN RUNTIME needs no conversion at all (Windows PyMOL + Windows exe) — winpath is harness-side only.
- Widget construction headless: QDialog() with no QApplication → uncatchable Qt C-abort (process dies, exit code still 0). UNVERIFIED one-liner spike: `QT_QPA_PLATFORM=offscreen` + constructing QApplication BEFORE any widget might make dialog asserts possible headlessly. 02_dialog_smoke tests exactly this; it must be INFORMATIONAL (an abort kills the process → no sentinel → reported, not gate-failing), because the authoritative dialog verdict is human-verify (01-06).
- `subprocess.run(capture_output=)` is 3.7+ — use `stdout=PIPE, stderr=PIPE/STDOUT`.
- run_gates.py (from 01-03) already auto-discovers informational smokes beyond the required `smoke/01_skeleton_smoke.py` — 02_dialog_smoke.py is picked up with NO runner changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: tools/winpath.py + tests/test_winpath.py — tested path conversion</name>
  <files>tools/winpath.py, tests/test_winpath.py</files>
  <action>
Write `tools/winpath.py` (stdlib-only, py3.6, pure string logic — importable by run_gates and tests):
- `to_windows_path(path)` → `/mnt/c/foo/bar` → `C:/foo/bar` (forward-slash form, safe where Windows tools accept forward slashes)
- `to_windows_backslash(path)` → `/mnt/c/foo/bar` → `C:\foo\bar` (cmd.exe-target form)
- Validation: path must start `/mnt/<single-letter-drive>/` (or equal `/mnt/<drive>` → maps to `<D>:/`); anything else (e.g. `/home/...`, `/mnt`, relative paths, already-Windows `C:\...`) raises `ValueError` — never silently mangle.
- Docstring must state the boundary: harness/dev-side ONLY; the plugin runtime never converts paths (Windows PyMOL + Windows exe + relative names).

Write `tests/test_winpath.py` (same conventions as test_skeleton.py: sys.path self-insert of repo root; also insert `tools/` to import winpath — or import via `sys.path.insert(0, .../tools)`; no `__init__.py` anywhere). Cases:
1. `/mnt/c/foo` → `C:/foo`; 2. `/mnt/c/foo/bar.txt` → `C:/foo/bar.txt`; 3. `/mnt/c` → `C:/`; 4. backslash variant of each → `C:\foo`, `C:\foo\bar.txt`; 5. `/mnt/d/data` → `D:/data`; 6. `ValueError` for `/home/user/x`, `relative/path`, `C:\already`, `/mnt` alone; 7. no mutation of the input string.
  </action>
  <verify>`python3.6 -m unittest discover -s tests -p "test_winpath.py" -v` → OK; `python3.6 -m py_compile tools/winpath.py` → rc 0</verify>
  <done>Conversion rules encoded as pure functions with full unit coverage including all error cases</done>
</task>

<task type="auto">
  <name>Task 2: Wire --xtb gate into tests/run_gates.py</name>
  <files>tests/run_gates.py</files>
  <action>
Edit `tests/run_gates.py` (landed by 01-03 — extend, do not rewrite): add argparse flag `--xtb` and gate 5:
1. Resolve exe: `os.path.join(ROOT, 'xtb-6.7.1', 'bin', 'xtb.exe')`; fail with a clear message if missing.
2. Conversion sanity (exercises tools/winpath from the gate): `sys.path.insert(0, os.path.join(ROOT, 'tools'))`; `import winpath`; assert `winpath.to_windows_path(ROOT).startswith('C:/')` — the repo must live on /mnt/c for the Windows legs to work at all.
3. Probe: `subprocess.run([exe, '--version'], cwd=ROOT, stdout=PIPE, stderr=subprocess.STDOUT, timeout=60)` (direct WSL exec of the exe by WSL-style path, /mnt/c-backed cwd, bare relative args — the research-verified pattern; NO cmd.exe here).
4. Verdict: output (decoded, errors='replace') must contain `'xtb version'` AND `'normal termination'`; else fail printing the last ~20 lines. Exit code is meaningful here (direct exec, not via .bat) but assert on content anyway.
5. Include gate 5 in the failure aggregation + summary block. Gate order: 1 syntax/safety → 2 purity → 3 unittest → (flag) 4 smoke → (flag) 5 xtb.
  </action>
  <verify>`python3.6 tests/run_gates.py --xtb` → gate 5 green ("xtb version 6.7.1pre" + "normal termination" found), exit 0; `python3.6 tests/run_gates.py` (no flags) → unchanged green</verify>
  <done>--xtb proves the Windows-xtb-from-WSL leg end-to-end and fails loudly if the exe, cwd, or output contract breaks</done>
</task>

<task type="auto">
  <name>Task 3: smoke/02_dialog_smoke.py — offscreen dialog construction (informational)</name>
  <files>smoke/02_dialog_smoke.py</files>
  <action>
Write `smoke/02_dialog_smoke.py` — the EXPERIMENTAL offscreen-QApplication spike, 3.6-parseable, same sentinel discipline as 01 (every print flush=True; NO reliance on exit codes):

1. FIRST LINE OF LOGIC: `os.environ.setdefault('QT_QPA_PLATFORM', 'offscreen')` — before any Qt import (headless PyMOL does not preload Qt, so setting it here still lands before Qt init).
2. Resolve ROOT (same `__file__`-or-cwd defensive pattern as 01_skeleton_smoke.py).
3. Import the plugin under the loader name (append ROOT to `pmg_tk.startup.__path__`, `importlib.import_module('pmg_tk.startup.serpentrum')`), then `importlib.import_module('pmg_tk.startup.serpentrum.gui')`.
4. `from pymol.Qt import QtWidgets`; `app = QtWidgets.QApplication.instance() or QtWidgets.QApplication([])` — the application MUST exist before any widget.
5. Construct `dlg = gui.PluginDialog()`; assert `dlg.tabs.count() == 3`; assert `[dlg.tabs.tabText(i) for i in range(3)] == ['Setup', 'Game', 'Spectra']`; assert `dlg.windowTitle() == 'serpentrum'`; assert `not dlg.modal()` (modeless default).
6. Epilogue: `print('SMOKE-OK DIALOG', flush=True)` only if all asserts passed; each failed step → `print('SMOKE-FAIL <step>: ...', flush=True)`.

Run it via the canonical one-liner from repo root and record the verdict honestly in the task notes:
`timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq smoke\\02_dialog_smoke.py" 2>&1 | tail -20`
- If `SMOKE-OK DIALOG` prints → offscreen construction works: the dialog now has a permanent cheap headless check (note this in the SUMMARY — later phases may promote it to required).
- If the process dies with a Qt abort / no sentinel → offscreen is a dead end: do NOT chase it. The smoke stays as the documented experiment; the runner treats it as informational (auto-discovered by 01-03's --smoke glob, never gate-failing). The authoritative dialog verdict is the 01-06 human-verify checkpoint.

Do NOT create `smoke/__init__.py`. Do NOT modify run_gates.py for this script (informational discovery already covers it).
  </action>
  <verify>`python3.6 -m py_compile smoke/02_dialog_smoke.py` → rc 0; the cmd.exe run produces either `SMOKE-OK DIALOG` (success path) or a documented no-sentinel abort (dead-end path) — record which; `python3.6 tests/run_gates.py --smoke` still exits per the REQUIRED script only (02 informational)</verify>
  <done>Offscreen dialog question answered empirically and recorded; dialog facts (3 tabs, labels, title, modeless) asserted in the success path; gate semantics unchanged</done>
</task>

</tasks>

<verification>
- `python3.6 tests/run_gates.py --xtb` → exit 0 with the xtb version + normal-termination evidence
- `python3.6 -m unittest discover -s tests -p "test_*.py" -v` → OK (now includes test_winpath)
- `python3.6 tests/run_gates.py` (default) → exit 0
- `python3.6 tests/run_gates.py --smoke` → required smoke green; 02_dialog_smoke reported informationally (OK or no-sentinel note), never failing the gate
- tools/ has no `__init__.py`; smoke/ has no `__init__.py`
</verification>

<success_criteria>
- INFRA-01 fully wired as runnable gates: headless PyMOL smoke (--smoke), WSL unittest suite (default), Windows xtb from WSL (--xtb) with the conversion discipline encoded and unit-tested in tools/winpath.py
- The offscreen dialog mechanism question is resolved empirically and documented (works → permanent check; doesn't → informational experiment), with no silent-pass path
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-skeleton-purity-harness/01-05-SUMMARY.md`
</output>
